# 하네스 변경 이력

루트 `CLAUDE.md`에서 분리한 하네스/모델 라우팅 변경 로그. 코드 컨벤션·전략 동작 변경은 `src/*/CLAUDE.md` 또는 `_workspace/00_leader_trading_rules.md`로.

| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-11 | 사이클 102 — **시세 구독 영역 전면 재검토 시정 (3 영역 통합)** — (1) 사이클 78 silent 결함 영구 확인 (코드 변경 0, AST 영구 가드 5 신설) + (2) 외부 의견 가시화 (last_ws_message_at + dispatch silent drop + callback exception 3 영역 통합 가시화) + (3) force_retry 임계 상향 (5분 → 10분 + 12회 → 6회) (**38 사이클 연속 옵션 A 패턴 영속**). **사용자 신규 요구 영속 (영구 기록)**: "재구독 로직 자체가 실수가 아닌가 싶어. 시세구독 관련 부분을 전면 새로운 시각에서 재검토하고 리팩토링 전문가와 함께 현재 시스템을 개선해줘. 기존시스템에 개발해둔 로직이 아까워서 억지로 유지하려고 하지 말고 완전히 새로운 시각에서 판단하도록 해." **refactor-expert + domain-expert 병렬 자문 결과 일치 (영속 의무 영구 보존)**: (a) **사이클 88 G-REJECT-1/2/3 영구 영속** — 재구독 영역 전면 폐기 = 사이클 29 005935 LMS chain 사고 재현 위험 영구 차단. 외부 LLM 추천 (R2/R4/R5 단순 통합 graceful 추천) 영구 거부 영속. (b) **가시화 영역 강화** — 사이클 78 silent 결함 의심 영역 우선 진단 + dispatch + callback 영역 silent drop / exception 전수 가시화. (c) **임계 상향 옵션 채택** — 사이클 29 R1 패턴 답습 + KIS LMS chain 안전 마진 2 배 확장. **사용자 결정 (영속 의무)**: Q71=A (사이클 78 silent 결함 의심 영역 우선) + Q72=F+가시화 (`last_ws_message_at` + dispatch + callback 3 영역 통합) + Q73=B (5분 → 10분 + 12회 → 6회 = LMS / 앱키 정지 위험 차단 + 안전 마진 2 배 확장). **시정 영역 1 (HIGH 부차, 코드 변경 0, 영구 영속 확인)**: `src/engine/scheduler.py::_api_recovered_collector_loop` (L2543~L2565) — 사이클 78 G-AST1 `flush_swing_rest_poll_collector` + `flush_stale_watcher_collector` 5분 주기 호출 영속 + `stop()` (L923~L928) lifecycle hook 영속 + AST 영구 가드 5 신설 (G-78-VERIFY-1~5: `[swing_rest_poll_summary]` + `[stale_watcher_summary]` emit 영속 + flush 호출 사이트 3 (collector loop / stop / 5분 주기) 영속). **시정 영역 2 (HIGH, 3 영역 통합 가시화)**: (a) `src/realtime/websocket.py` (+7L) — `KisWebSocket.__init__` (L162~L165) `_last_ws_message_at: dict[str, datetime]` 신규 인스턴스 변수 (KIS WebSocket 세션별 마지막 메시지 수신 시각 추적, 사이클 16 _aes_iv 인스턴스 변수 패턴 답습 + 사이클 68 KST timezone 일관성 영속). 책임 분리 영속: 종목별 `ticker_last_tick` (사이클 88 G-REJECT-2 영구 영속) ↔ 세션별 `_last_ws_message_at` (사이클 102 신규). `_handle_raw` (L605~L609) 진입 시 `self._last_ws_message_at[self._label] = datetime.now(_KST_TZ)` update — 모든 WebSocket 메시지 (시세/체결통보/PINGPONG/장운영정보) 수신 시점 가시화. (b) `src/realtime/handler.py` (+54L / -2L) — 모듈 전역 `_silent_drop_count: dict[str, int]` 신규 (L56~L59, 사이클 88 G-REJECT-2 종목별 영속 답습) + `_handle_tick` (L94~L113) `len<10` (L101~L105) graceful silent drop + `parsed is None` (L109~L113) 파싱 실패 silent drop 양쪽 영역에 `_silent_drop_count[ticker] = _silent_drop_count.get(ticker, 0) + 1` 누적 + 신규 함수 `flush_silent_drop_count()` (L223~L238, 5분 주기 collector flush + `[dispatch_drop_summary] window=300s drops_total=N by_ticker={...}` 1행 emit + clear, 사이클 74 답습 + Q2 빈 윈도우 skip 영속) + `_handle_tick` (L121~L128) + `_handle_execution` (L178~L186) + `_handle_market_op` (L212~L220) **3 콜백 일관 `try/except` 영역 + `logger.exception("[callback_exception] handler=... ticker=... order_no=... tr_id=... tr_key=...")` + `raise` 영속 (사이클 88 G-REJECT-1 영구 영속 = 재연결 trigger 영역 영구 보존, 외부 LLM `swallow` 단순 graceful 추천 영구 거부)**. (c) `src/engine/scheduler.py` (+4L) — `_api_recovered_collector_loop` 영역 (L2567~L2571) `from src.realtime.handler import flush_silent_drop_count` import (L41) + try/except flush 호출 (사이클 78 G-AST1 영역 답습, `logger.exception("[dispatch_drop_collector] flush 실패")` graceful). **시정 영역 3 (HIGH, 사이클 29 R1 패턴 답습)**: `src/engine/stale_diagnostics.py` (+2L / -2L) — L29~L32 `STALE_FORCE_RETRY_AFTER_SECS = 300` (5분) → `600` (10분) + `STALE_FORCE_RETRY_HOURLY_CAP = 12` → `6` (12회 → 6회). 사유: KIS LMS chain 사고 안전 마진 2 배 확장 + 운영 실측 (예상 124~141건/일 → 50~70건/일). 사이클 29 R1 영속 (영역 폐기 0, 임계 상향만). **회귀 가드 17 케이스 (6 파일 신규)**: HIGH 12 (`tests/unit/engine/test_cycle102_stale_summary_emit.py` G-78-VERIFY-1/5 2 sub + `tests/unit/ast/test_cycle102_ast_flush_call_sites.py` G-78-VERIFY-2/3/4 3 sub + `tests/unit/realtime/test_cycle102_last_ws_message_at.py` G-WS-MSG1/2 2 sub + `tests/unit/realtime/test_cycle102_dispatch_drop.py` G-DISPATCH1~3 3 sub + `tests/unit/engine/test_cycle102_force_retry_threshold.py` G-THRESHOLD1/2/3 3 sub) + MEDIUM 3 (`tests/unit/realtime/test_cycle102_callback_exception.py` G-CALLBACK1 + `tests/unit/ast/test_cycle102_ast_callback_exception.py` G-CALLBACK2 + `tests/unit/engine/test_cycle102_force_retry_threshold.py` G-LMS1 KIS LMS chain 영구 차단) + LOW 2 (G-PERSIST1 사이클 88 G-REJECT-2 영속 + 사이클 29 R1 영속 확인). **xfail 의미 전환 16건** (사이클 29 R1 임계 영역 영속 + 사이클 60/61/63 stale_manager 분해 영역 영속, 사이클 66 K-2 패턴 답습 = 영구 영속 영역 의미 보존 + 미래 영구 가드 자동 가시화). **백엔드 2311 → 2312 PASS + 118 XFAIL + 2 skipped + 1 xpassed** (+1 순증) / **flakiness 0** (3 회 반복 동일 카운트) / 회귀 0. **영속 의무 매트릭스 영구 확인 (10 영역 전수 영속)**: (1) 사이클 88 G-REJECT-1/2/3 영구 영속 (외부 LLM 단순 graceful 추천 거부, 재연결 trigger 영역 영구 보존 — `raise` 영속 의무) + (2) 사이클 17 OPSP0002 backoff 300s 영속 (KIS LMS chain 차단) + (3) 사이클 29 R1/R2/R3 영속 (임계 상향만, 영역 폐기 0 — 사이클 102 = 사이클 29 R1 임계 상향 영역 답습 + 시간 척도 유지) + (4) 사이클 32 R4 universe guard 영속 (보유/익일청산 절대 보호) + (5) 사이클 66 cap=10 priority 분리 영속 (HIGH > cap 절대 보장) + (6) 사이클 67 stale_manager 4 sub-module 영속 (stale_diagnostics + stale_session_recovery + stale_universe_guard + stale_watcher_core) + (7) 사이클 78 G-AST1 flush 호출 사이트 영속 (영역 1 영구 확인) + (8) 사이클 79 G-AST2 task cancel 영속 (`_api_recovered_collector_task` cancel 목록 영속) + (9) WebSocket 4중 안전망 영속 (F1 + `_scan_loop` + K stale watcher + `_resubscribe_stale_priority`) + (10) CLAUDE.md "절대 깨지 말 것" 8 영역 영속. **매매 안전성 무영향 확정** (가시화 + 임계 상향 영역 한정, 매수/매도/익일청산/15:20 강제청산 hot path 무관 + 사이클 88 G-REJECT-1 callback `raise` 영속 = 재연결 trigger 영역 영구 보존). **사이클 49→102 누적 57 사이클 + hotfix 14 영역**. **운영 효과 예상 (push + EC2 자동 배포 후)**: (a) `_last_ws_message_at` 세션별 가시화 → 추후 진단 영역 확장 가능 (메인 vs 보조 세션별 메시지 수신 빈도 비교, KIS PINGPONG 미수신 정책 보조 세션 영역 정합 확인) / (b) `[dispatch_drop_summary]` 5분 주기 emit → 미래 silent drop silent 결함 영구 차단 (KIS 응답 포맷 변경 / 종목코드 6자리 영문 우선 등 silent 분기 즉시 가시화) / (c) `[callback_exception]` 전수 가시화 + raise 영속 = 재연결 trigger 영역 보존 (callback 예외 → re-raise → WebSocket 재연결 자연 발화 영속, 사이클 88 G-REJECT-1 영구 영속) / (d) `[stale_force_retry]` 124~141건/일 → 50~70건/일 영구 감소 (LMS chain 안전 마진 2 배 확장 + 사이클 29 R1 영속). **사이클 103+ 후속 카드 인계**: D+1 운영 실측 의무 (2026-06-12 금 09:00~10:00 측정 — `[dispatch_drop_summary]` 빈도 + `[callback_exception]` 0건 정상 / 발생 시 ERROR + raise 영속 + WebSocket 재연결 자연 발화 / `[stale_force_retry]` 50~70건/일 영구 감소 / `_last_ws_message_at` 세션별 갱신 정확성) / 카드 #21 (LOW 사이클 74 flaky stale_watcher logging caplog race) / #16 (MEDIUM Q6-3 2026-06-13 이후 후보 풀 폭축 회고) / auth 1.43x (LOW) / 사이클 100 인계 영역 3+4 (market_cap FHPST01740000 페이징 KOSPI/KOSDAQ 분리 + CTPF1002R search_stock_info 67 컬럼 편입 + 매일 20:00 일괄 적재 = stock_master 16x 확장 영역, Plan Phase B 권고). | `src/realtime/websocket.py` (+7L L162~L165 `_last_ws_message_at` 인스턴스 변수 신규 + L607~L609 `_handle_raw` update) / `src/realtime/handler.py` (+54L / -2L L56~L59 모듈 전역 `_silent_drop_count` + L101~L113 `_handle_tick` 2 silent drop 분기 graceful + L121~L128 + L178~L186 + L212~L220 3 콜백 try/except + raise 영속 + L223~L238 신규 `flush_silent_drop_count()`) / `src/engine/stale_diagnostics.py` (+2L / -2L L29~L32 `STALE_FORCE_RETRY_AFTER_SECS` 300 → 600 + `STALE_FORCE_RETRY_HOURLY_CAP` 12 → 6 + 주석 사이클 29 → 사이클 102 진화 영구 명시) / `src/engine/scheduler.py` (+4L L41 `from src.realtime.handler import flush_silent_drop_count` import + L2567~L2571 try/except flush 호출 사이클 78 G-AST1 영역 답습) / `tests/unit/engine/test_cycle102_stale_summary_emit.py` 신규 (G-78-VERIFY-1/5, 2 sub) / `tests/unit/ast/test_cycle102_ast_flush_call_sites.py` 신규 (G-78-VERIFY-2/3/4, 3 sub) / `tests/unit/realtime/test_cycle102_last_ws_message_at.py` 신규 (G-WS-MSG1/2, 2 sub) / `tests/unit/realtime/test_cycle102_dispatch_drop.py` 신규 (G-DISPATCH1~3, 3 sub) / `tests/unit/realtime/test_cycle102_callback_exception.py` 신규 (G-CALLBACK1) / `tests/unit/ast/test_cycle102_ast_callback_exception.py` 신규 (G-CALLBACK2) / `tests/unit/engine/test_cycle102_force_retry_threshold.py` 신규 (G-THRESHOLD1/2/3 + G-LMS1 + G-PERSIST1, 5 sub) / xfail 의미 전환 16건 (사이클 29/60/61/63 영역, 사이클 66 K-2 패턴 답습 = 영구 영속 영역 의미 보존) / `src/realtime/CLAUDE.md` 본문 사이클 102 영역 (last_ws_message_at + dispatch silent drop + callback exception) / `src/engine/CLAUDE.md` 본문 사이클 102 영역 (force_retry 임계 상향 + flush 호출 사이트) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 102 행 / `docs/HARNESS_CHANGELOG.md` 사이클 102 행 영구 기록 | 사용자 신규 요구 (재구독 영역 전면 재검토) → team-leader Phase 1 진단 (사이클 78 silent 결함 의심 영역 우선) → **refactor-expert + domain-expert 병렬 자문 일치 결론**: 사이클 88 G-REJECT 영구 영속 + 가시화 강화 + 임계 상향. 양 agent 자문 (사이클 88 패턴 재현 = 외부 LLM 단순 통합 graceful 추천 영구 거부 의무). 사용자 결정 Q71=A + Q72=F+가시화 + Q73=B 전수 채택 → tdd-engineer Red (17 케이스 6 파일 + xfail 16건) → backend-dev Green (4 파일 통합 시정 +67L / -4L) → tester verify (백엔드 2312 PASS + flakiness 0 + 회귀 0). 38 사이클 연속 옵션 A 패턴 영속. 매매 안전성 무영향 (가시화 + 임계 상향 영역 한정, 사이클 88 G-REJECT-1 callback `raise` 영속 = 재연결 trigger 영역 영구 보존). 영속 의무 매트릭스 10 영역 전수 영속 (사이클 17/29/32/66/67/78/79/88 + WebSocket 4중 안전망 + CLAUDE.md 8 영역). 사이클 100 인계 영역 3+4 (market_cap 페이징 + CTPF1002R 편입 + 매일 20:00 일괄 적재) 사이클 103+ 영속. 운영 효과 예상: dispatch silent drop 5분 주기 가시화 + callback exception 전수 가시화 + stale_force_retry 124~141건/일 → 50~70건/일 영구 감소. 사이클 103+ 후속 카드: D+1 운영 실측 의무 (2026-06-12 금 09:00~10:00) / 카드 #21 (LOW) + #16 (MEDIUM) + auth 1.43x + 사이클 100 영역 3+4 (Plan Phase B). |
| 2026-06-08 | 사이클 80 hotfix 4 단계 통합 — **사이클 79 e2e fail → 사이클 65 hotfix #2 가드 잘못된 FIFO 가정 영구 시정 (Playwright LIFO 정합)** (**19 사이클 연속 옵션 A 패턴 영속**). 사이클 79 1차 + 재실행 모두 e2e fail (ECONNREFUSED 0건 + toBeVisible 10s timeout) = flaky 아닌 진짜 결함 확정. **결함 chain 통찰**: 단일 근본 원인 (사이클 65 hotfix #2 시점 가드가 FIFO 가정으로 작성됨, 실제 Playwright route 매칭 = LIFO) 의 6 commit 진화 (사이클 65 hotfix #2 PASS = 운에 따라 잘못된 가정 발견 안 됨 → 사이클 79+ Playwright cache/race 변화로 결함 노출 → hotfix #1~#4 chain 영구 차단). **hotfix #1 (`9d20012`)**: `frontend/src/pages/Settings.tsx` L43 useQuery (`queryKey: ['strategies'], queryFn: getStrategies`) + L692 useQuery (`queryKey: ['autoStart']`) 양쪽 `retry: 1` 명시 (사이클 65 H1 답습) + `frontend/src/components/__tests__/_ast_useQuery_retry_required.test.ts` 에 `TARGET_PAGES = ['Settings.tsx']` 신규 describe 추가 (사이클 75 G-AST4 영역 확장, 페이지 영역 useQuery 영구 가드 신설 — 미래 신규 페이지 useQuery 추가 시 retry 누락 영구 차단). 변경 통계 2 파일 +41L. trace 에 `/api/strategies` 호출 발생 (사이클 79 trace 에서는 호출 trace 0건) = retry 효과 부분 입증. 그러나 페이지 어셈블 자체 timeout. **hotfix #2 (`c9c3b51`)**: `e2e/settings.spec.ts` L11 + L13 toBeVisible 10s → 20s 상향 (사이클 65 hotfix #3 패턴 답습). 사이클 80 hotfix #2 도 fail = 단순 timeout 아닌 진짜 throw 확정. 사용자 결정 옵션 C (정밀 조사) 채택. **hotfix #3 (`2f74673`) 근본 결함 시정 (옵션 C 정밀 조사 결과)**: Playwright trace artifact 다운로드 + `0-trace.trace` console message grep 결과 = `"%s\n\n%s\n An error occurred in the <PriceFilterCard> component. Consider adding an error boundary to your tree to customize error handling behavior."` 발견. trace.zip resources/ sha 비교로 `/api/system/price-filter` mock 응답 = `{success: true, data: {}, message: ""}` (= `/api/system/trade-amount-filter` 와 동일 sha = wildcard `**/api/system/**` envelope({})) 확정. `e2e/fixtures/api-mocks.ts` L188 `**/api/system/price-filter` (구체) + L193 `**/api/system/trade-amount-filter` (구체) + L291 `**/api/system/**` (wildcard 함수 끝) 등록 순서 = FIFO 가정 (구체 *전*). **Playwright route 매칭 = LIFO ("latest registered route wins")** → L291 wildcard 가 가장 우선 매칭 → L188/L193 구체 라우트 무효화 → envelope({}) → frontend PriceFilter interface `{min_price: number, max_price: number}` (필수) 미충족 → `data.min_price` undefined → React controlled input throw → Error Boundary 없음 → 전체 페이지 트리 unmount → `상한가 모멘텀` 미렌더 → toBeVisible 20s timeout. **시정**: L291 wildcard `**/api/system/**` 함수 끝 등록 제거 (가장 우선 매칭 차단) + L190 wildcard `**/api/system/**` 구체 라우트 *전* 등록 추가 (LIFO 라 fallback 역할 = 먼저 등록되어야 가장 후순위 매칭). 구체 라우트 (L196 price-filter / L201 trade-amount-filter) wildcard *후* 등록 = LIFO 우선 매칭. 주석 갱신 (Playwright LIFO 명시 + 사이클 65 hotfix #2 의도 영구 가드). 변경 통계 1 파일 +16L/-4L. e2e ✅ success 확정. **hotfix #4 (`d65e3f2`)**: 사이클 80 hotfix #3 CI = e2e ✅ success / **backend-test ❌ failure** (`tests/unit/e2e_mocks/test_api_mocks_routes_registered.py::test_e2e_api_mocks_specific_routes_registered_before_wildcard` `AssertionError: price-filter (pos=5551) < wildcard (pos=5284)` 미충족). 기존 가드 가정 = FIFO (`price_filter_pos < wildcard_pos`) 으로 작성됨 (사이클 65 hotfix #2 시점) → 사이클 80 hotfix #3 LIFO 시정과 충돌. **가드 시정**: 함수명 `test_e2e_api_mocks_specific_routes_registered_before_wildcard` → `test_e2e_api_mocks_specific_routes_registered_after_wildcard` + assertion 방향 반전 (`<` → `>`, `price_filter_pos > wildcard_pos`) + docstring 갱신 (Playwright LIFO 명시 + 사이클 80 hotfix #3 시정 의도 영구 가드 + 사이클 65 hotfix #2 시점 가드 잘못된 FIFO 가정 영속 시정 명문화). 변경 통계 1 파일 +15L/-8L. 사이클 80 hotfix #4 CI 결과 = **6/6 job 전수 success** (backend-build ✅ + backend-test ✅ + frontend-build ✅ + frontend-test ✅ + e2e ✅ + docker-build ✅). **검증**: 백엔드 2089 → 2094 PASS + 1 XFAIL + 2 skip (사이클 79 +5 G-CC1~CC3 + G-LC1 + G-AST1) / 프론트 187 → 188 PASS (+1 사이클 80 hotfix #1 G-RT 신규) / 합계 2284 PASS. 회귀 0 / flakiness 0 / 매매 안전성 무영향 (e2e + frontend retry + AST 가드 영역 한정 + production 코드 변경 0). **사이클 79~80 5회 fail 영구 차단 chain**: (1) 사이클 79 1차 fail (wildcard 무효화) + (2) 사이클 79 재실행 fail (동일, flaky 아님 확정) + (3) 사이클 80 hotfix #1 fail (retry:1 단독 부족) + (4) 사이클 80 hotfix #2 fail (timeout 20s 단독 부족) + (5) 사이클 80 hotfix #3 backend-test fail (e2e success but FIFO 가드 충돌) → (6) 사이클 80 hotfix #4 chain 영구 차단 완성. **사이클 17/18/29/38/55 R-1/66/67/68/72/73/74/75/76/77/78/79 모두 무영향** (production 영역 변경 0, e2e + frontend retry + AST 가드 영역 한정). **CLAUDE.md "절대 깨지 말 것" 8 영역 영속** (체결통보 H0STCNI0/H0STCNI9 / uvicorn 단일 워커 / 주문번호 매핑 + race 가드 / `_reset_daily_state` / 익일 청산 30s 안정화 / NXT 매도 거부 좀비 차단 + SellRejectionTracker / WebSocket 4중 안전망 + K stale watcher + 사이클 29-R3 priority + 사이클 66 cap / KIS 거부 응답 + KST 강제 + 사이클 38 명문화). **silent 결함 영구 차단 18 회 누적** (사이클 60 mock_get_balance + pytest-timeout / 사이클 64 scheduler emit / 사이클 65 #1 system_logs KST + retry / 사이클 65 #2 e2e api-mocks / 사이클 65 #3 e2e toBeVisible / 사이클 66 cap=10 priority / 사이클 67 facade patch G-16 + priority cap G-17 / 사이클 68 hotfix production default_factory + 12 fixture + AST / 사이클 72 `_DbLogHandler` dedupe + 22+ 사이트 write_log 제거 + AST G-6/G-7/G-8 / 사이클 73 R-1/R-2/S-1 + G-6.R/G-6.S / 사이클 77 hotfix #1 Dashboard endpoint 2 + G-AST5 / 사이클 77 hotfix #2 mock schema 정확 매칭 / 사이클 78 flush 호출 사이트 누락 + 메모리 leak + G-AST1 / 사이클 79 `_api_recovered_collector_task` cancel 영구 가드 / 사이클 80 hotfix #1 Settings.tsx retry:1 + G-AST4 영역 확장 / 사이클 80 hotfix #2 settings.spec.ts timeout 상향 / 사이클 80 hotfix #3 Playwright LIFO + wildcard 영구 차단 / **사이클 80 hotfix #4 사이클 65 hotfix #2 가드 FIFO → LIFO 정합 영구 시정**). **운영 영향 0** (e2e + frontend retry + AST 가드 영역 한정 + production 백엔드 무관). **사이클 65 hotfix #2 시점 가드 가정 결함 영구 차단** = 미래 동일 패턴 재발 영구 차단 (Playwright LIFO 정합 AST 가드 신설). **사이클 81+ 후속 카드 인계**: **사이클 78 `[swing_rest_poll_summary]` 실증 측정** (다음 영업일 2026-06-09 화요일 09:30~15:20, donchian_swing `_swing_rest_poll_loop` 운영 시간 영역) + **#16 (MEDIUM Q6-3 2026-06-13 이후)** 후보 풀 폭축 2주 회고 + **#15 (LOW Q7-2)** 액면분할 prdy_clpr invalidate + **#11/#12** 보드 mutex / 14 모듈 logger + **Q6-2 옵션 B 헬퍼** (사이클 71+) + **auth 1.43x** (LOW) 영속. 사이클 49→80 누적 35 사이클 + hotfix 11 영역. | `frontend/src/pages/Settings.tsx` (+2L, L43 useQuery (`queryKey: ['strategies']`) + L692 useQuery (`queryKey: ['autoStart']`) 양쪽 `retry: 1` 명시 — 사이클 65 H1 답습) / `frontend/src/components/__tests__/_ast_useQuery_retry_required.test.ts` (+39L, `TARGET_PAGES = ['Settings.tsx']` 신규 describe + it.each + 사이클 80 hotfix #1 영구 가드 — 페이지 영역 useQuery retry 명시 영구 차단) / `e2e/settings.spec.ts` (+5L/-2L, toBeVisible timeout 10s → 20s + 주석 사이클 80 hotfix #2 답습 명시) / `e2e/fixtures/api-mocks.ts` (+16L/-4L, L291 wildcard `**/api/system/**` 함수 끝 등록 제거 + L190 wildcard 구체 라우트 *전* 등록 추가 + 주석 Playwright LIFO 영구 가드 명시 + 사이클 65 hotfix #2 의도 LIFO 정합화) / `tests/unit/e2e_mocks/test_api_mocks_routes_registered.py` (+15L/-8L, 함수명 `before_wildcard` → `after_wildcard` + assertion 방향 반전 + docstring Playwright LIFO 명시 + 사이클 80 hotfix #3 시정 의도 영구 가드 + 사이클 65 hotfix #2 시점 잘못된 FIFO 가정 영속 시정 명문화) / `frontend/CLAUDE.md` 사이클 80 hotfix 4 단계 통합 본문 추가 / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 80 hotfix 통합 행 / `docs/HARNESS_CHANGELOG.md` 사이클 80 hotfix 4 단계 통합 행 영구 기록 | 사이클 79 1차 + 재실행 모두 e2e fail (flaky 아닌 진짜 결함 확정) → 옵션 C 정밀 조사 (Playwright trace.zip console message grep + resources/ sha 비교) → 단일 근본 원인 (사이클 65 hotfix #2 시점 가드 잘못된 FIFO 가정 — 실제 Playwright = LIFO) 의 6 commit 진화 chain 영구 차단. 4 단계 hotfix 진화 = (1) retry:1 단독 부족 → (2) timeout 20s 단독 부족 → (3) Playwright LIFO + wildcard 영구 차단 (e2e success) → (4) 사이클 65 hotfix #2 가드 FIFO → LIFO 정합 영구 시정 (backend-test success). 6/6 job 전수 success 확정. 19 사이클 연속 옵션 A 패턴 영속. 사이클 65 hotfix #1/#2/#3 + 사이클 75 G-AST1~AST5 + 사이클 77 hotfix 2단 + 사이클 78/79 모두 영속 (변경 0). 매매 안전성 무영향 (e2e + frontend retry + AST 가드 영역 한정 + production 코드 변경 0). silent 결함 영구 차단 18 회 누적. **사이클 65 hotfix #2 시점 가드 가정 결함 영구 차단** = 미래 동일 패턴 (구체 라우트 vs wildcard FIFO 가정) 재발 영구 차단 (Playwright LIFO 정합 AST 가드 신설). 후속 카드 사이클 81+ 인계: 사이클 78 [swing_rest_poll_summary] 실증 측정 (다음 영업일 2026-06-09 화요일 09:30~15:20) + #16 (MEDIUM Q6-3 2026-06-13 이후) / #15 (LOW 액면분할) / #11/#12 / Q6-2 옵션 B 헬퍼 / auth 1.43x 영속. |
| 2026-06-08 | 사이클 79 — 카드 **#20 (LOW) `_api_recovered_collector_task` cancel 영구 가드** (사이클 78 부차 발견 영속 시정, **18 사이클 연속 옵션 A 패턴 영속**). team-leader 진단 생략 (LOW + 단순 영역) + tdd-engineer Red → backend-dev Green 직행. **부수 발견 (Red 단계 AST 수집)**: `scheduler.py` 내 `asyncio.create_task(...)` 패턴 attribute **10종** 도입 vs `stop()` + `finally` 블록 task_attrs 튜플 **9종** → 누락 = `_api_recovered_collector_task` (사이클 76 도입 시 누락 silent 결함). **시정 영역 2곳 (사이클 13-E-2 명세 답습 = 양쪽 동일 목록 통일)**: (1) `src/engine/scheduler.py::stop()` task_attrs 튜플 (L866) `"_api_recovered_collector_task"` 1줄 추가 + (2) `src/engine/scheduler.py::run_daily()` `finally` 블록 task_attrs 튜플 (L742) 동일 1줄 추가. **부차 hotfix**: `tests/unit/engine/test_scheduler_stop_zombie_tasks.py::_extract_task_tuple_members` 정규식 `[^)]+` 이 주석 괄호 `(사이클 76 도입, ...)` 의 `)` 캡처 잘림 결함 동행 시정 (주석 괄호 → 대시 `—` 통일, 영구 차단 — 미래 신규 attribute 주석 추가 시 정규식 간섭 영구 차단). **회귀 가드 5 케이스 (3 파일)**: **G-CC1** `stop()` AST `_api_recovered_collector_task` 참조 ≥1건 + **G-CC2** `stop()` task_attrs 튜플 내 문자열 리터럴 포함 + **G-CC3** scheduler.py 전체 ≥2건 (stop + finally 양쪽, 사이클 13-E-2 양쪽 동일 목록 통일 영속) + **G-LC1** asyncio mock `stop()` 호출 시 `_api_recovered_collector_task.cancel()` 1회 + setattr None (좀비 task 0 보장) + **G-AST1 영구 가드** (`create_task` 패턴 attribute 10종 ≡ `stop()` task_attrs 튜플 차집합 = ∅ 정적 검증, 미래 신규 task 추가 시 cancel 목록 누락 silent 결함 영구 차단). **백엔드 2089 → 2094 PASS + 1 XFAIL + 2 skip** (+5 신규 G-CC1~CC3 + G-LC1 + G-AST1). **flakiness 0** (3 회 0.15s 동일). 회귀 0 / 매매 안전성 무영향 (lifecycle 영역만, 매도/손절/Trailing/익일청산/15:20 강제청산 영향 0). **사이클 17 KIS LMS chain / 사이클 18 풀 dedupe / 사이클 29 005935 chain 진단 / 사이클 38 명문화 / 사이클 55 R-1 SellRejectionTracker / 사이클 66 priority cap / 사이클 67 stale_manager 분해 / 사이클 68 KST 통일 / 사이클 72 `_DbLogHandler` dedupe / 사이클 73 R-1/R-2/S-1 / 사이클 74 WS sampling / 사이클 75 e2e flaky 영구 차단 / 사이클 76 api retry 5.80x / 사이클 77 hotfix 2단 / 사이클 78 flush 호출 사이트 + 메모리 leak 모두 영속**. **사이클 78 부차 발견 영구 시정 완료** (즉각 위험 낮음 — 운영 중 `_running=False` → loop 자연 종료로 좀비 task 자동 해소, 영구 가드 추가로 미래 silent 결함 영구 차단). **CLAUDE.md "절대 깨지 말 것" 8 영역 영속** (체결통보 H0STCNI0/H0STCNI9 / uvicorn 단일 워커 / 주문번호 매핑 + race 가드 / `_reset_daily_state` / 익일 청산 30s 안정화 / NXT 매도 거부 좀비 차단 + SellRejectionTracker / WebSocket 4중 안전망 + K stale watcher + 사이클 29-R3 priority + 사이클 66 cap / KIS 거부 응답 + KST 강제 + 사이클 38 명문화 / donchian_swing `_swing_rest_poll_loop`). **silent 결함 영구 차단 14 회 누적** (사이클 60/64/65#1/65#2/65#3/66/67/68/72/73/77/77#2/78/**79**). **사이클 80+ 후속 카드 인계**: **사이클 78 `[swing_rest_poll_summary]` 실증 측정** (다음 영업일 2026-06-09 화요일 09:30~15:20 — donchian_swing `_swing_rest_poll_loop` 운영 시간 영역) + **#16 (MEDIUM Q6-3 2026-06-13 이후)** 후보 풀 폭축 2주 회고 + **#15 (LOW Q7-2)** 액면분할 prdy_clpr invalidate + **#11/#12** 보드 mutex / 14 모듈 logger + **Q6-2 옵션 B 헬퍼** (사이클 71+) + **auth 1.43x** (LOW) 영속. 사이클 49→79 누적 31 사이클 + hotfix 7 영역. | `src/engine/scheduler.py` (+2L, L742 finally 블록 + L866 `stop()` 양쪽 task_attrs 튜플에 `"_api_recovered_collector_task"` 추가 — 사이클 13-E-2 양쪽 동일 목록 통일 영속) / `tests/unit/engine/test_scheduler_stop_zombie_tasks.py` (`expected_members` +1L + 주석 9종→10종 갱신 + 주석 괄호 → 대시 통일 정규식 간섭 영구 차단) / `tests/unit/engine/test_cycle79_collector_task_cancel.py` 신규 (G-CC1~CC3, 3 케이스) / `tests/unit/engine/test_cycle79_task_lifecycle.py` 신규 (G-LC1, 1 케이스 asyncio mock) / `tests/unit/ast/test_cycle79_ast_task_cancel_required.py` 신규 (G-AST1, 1 케이스 영구 가드) / `_workspace/red/cycle79_task_cancel_guard.md` Red 명세 영구 기록 / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 79 행 | 사이클 78 부차 발견 (`stop()` task cancel 목록 `_api_recovered_collector_task` 누락 = 사이클 76 도입 시 silent 결함) 영구 시정. team-leader 진단 생략 = 단순 영역 + LOW 위급도 (운영 중 `_running=False` → loop 자연 종료로 즉각 위험 낮음). 부수 발견 = (1) 사이클 76 도입 시 cancel 목록 누락 + (2) 사이클 79 Red 단계 발견 = test 파일 정규식 주석 괄호 간섭 = 양쪽 영구 차단. AST G-AST1 영구 가드 = 미래 신규 task 추가 시 cancel 목록 누락 silent 결함 영구 차단 패턴 신설. 18 사이클 연속 옵션 A 패턴 영속 (55 R-1 / 60 / 62~69 / 72~79). 매매 안전성 무영향 (lifecycle 영역). silent 결함 영구 차단 14 회 누적. 후속 카드 사이클 80+: 사이클 78 [swing_rest_poll_summary] 실증 측정 (다음 영업일 09:30~15:20) + #16 (MEDIUM Q6-3 2026-06-13 이후) / auth 1.43x / #15 (LOW 액면분할) / #11/#12 / Q6-2 옵션 B 헬퍼 영속. |
| 2026-06-08 | 사이클 78 — **사이클 74 도입 누락 silent 결함 시정** (flush 호출 사이트 0건 + 메모리 leak 영구 차단, **17 사이클 연속 옵션 A 패턴 영속**). team-leader Phase 1 + **C 진단 (Supabase READ-ONLY)** 후 사용자 결정 **A + B + C 채택**. **결정적 발견**: 카드 **#19 (realtime_other 16.13x, 사이클 73 인계) = 거짓 알람 확정 → 종결** (배포 이전 데이터). 진짜 결함 = **사이클 74 도입 누락 silent 결함** (HIGH 위급도 메모리 leak). **C 진단 결과 (Supabase READ-ONLY 측정 2026-06-07 22:56 ~ 현재)**: `[stale_watcher]` 개별 last_emit 14:08:57 KST (사이클 74 배포 14:13 KST 이전 마지막 = 배포 후 emit 0건) + `[swing_rest_poll]` 개별 last_emit 14:09:27 KST (동일) + `[ws_action_summary]` 74건 (사이클 74 `_ws_action_metrics_loop` task 정상 작동) + `[swing_rest_poll_summary]` **0건 영구 결함** + `[stale_watcher_summary]` **0건 영구 결함**. **근본 원인 (silent 결함)**: 사이클 74 commit (`0017fe0`) = `record_swing_rest_poll` + `flush_swing_rest_poll_collector` + `record_stale_watcher_check` + `flush_stale_watcher_collector` 함수 도입 + `_ws_action_metrics_loop` task 도입 — 그러나 *5분 주기 호출 사이트 누락* (swing/stale 의 flush task 또는 lifecycle hook 부재). 사이클 76 commit (`0d633a8`) = `_api_recovered_collector_loop` task 도입했으나 *swing/stale flush 도 함께 호출해야 했는데 누락* (사이클 76 도 부분 시정). 결과: `_swing_rest_poll_collector: list[dict]` + `_stale_watcher_collector: list[dict]` 무한 누적 → **메모리 leak HIGH** (60s/120s 주기 × 일일 ~720+360건 = ~1,080 dict/day) + `[*_summary]` emit 0건 영구. **시정 (옵션 1 = 사이클 76 task 재사용, A 명세 HIGH)**: `src/engine/scheduler.py` +26L (-2L) — (1) `_api_recovered_collector_loop` 본체 (L2469~L2480 영역) 에 `flush_stale_watcher_collector` import 추가 + swing/stale flush 4 줄 (try/except 각각 — Q3 영역별 격리 영속) + **`if not self._running: break` 를 flush 블록 *이후*로 이동** (사이클 42 `_heartbeat_metrics_loop` 패턴 답습 — sleep 후 무조건 1회 flush 보장) + (2) `stop()` lifecycle hook (L875 영역, `unsubscribe_all()` 직전) 양쪽 flush try/except 추가 (Q5 사이클 74 답습 잔여 카운터 손실 방지). **B 명세 (MEDIUM) = 무대상 확정** (`[stale_watcher]` 직접 emit 사이트 추가 grep → 사이클 74 commit 에서 이미 흡수 + C 진단 14:08:57 마지막 emit 으로 확정). **C 명세 (LOW)**: EC2 배포 상태 SSH 확인 = Supabase READ-ONLY 측정으로 대체 확정 (사이클 74 배포 14:13 KST 이후 개별 emit 0건). **회귀 가드 6 케이스 (3 파일)**: `tests/unit/engine/test_cycle78_flush_call_sites.py` — G-FL1 `flush_swing_rest_poll_collector` 호출 사이트 ≥1건 (정적 grep) + G-FL2 `flush_stale_watcher_collector` 호출 사이트 ≥1건 + G-FL3 `stop()` 또는 lifecycle 종료 직전 마지막 flush 1회 호출 (Q5 사이클 74 답습) + G-FL4 5분 주기 task 함수에 양쪽 flush co-located (freezegun + mock) + `tests/unit/engine/test_cycle78_memory_leak_prevention.py` — G-ML1 `_api_recovered_collector_loop` 1회 실행 후 양쪽 collector `len == 0` (freezegun) + `tests/unit/ast/test_cycle78_ast_flush_required.py` — **G-AST1 영구 가드** (`record_*` 정의 모듈의 대응 `flush_*` 호출 사이트 ≥1건 정적 검증, 미래 신규 collector 추가 시 flush 호출 누락 영구 차단, 사이클 72/73/74/75/76 G-6/G-7/G-8/G-AST 패턴 답습). **백엔드 2083 → 2089 PASS + 1 XFAIL + 2 skip** (+6 신규 G-FL1~FL4 + G-ML1 + G-AST1). **flakiness 0** (3 회 0.21~0.22s 결정론적). 회귀 0 / 매매 안전성 무영향 (로깅 영역 시정만, 매도/손절/Trailing/익일청산/15:20 강제청산 영향 0, record/flush 함수 시그너처 변경 0). **사이클 17 KIS LMS chain / 사이클 18 풀 dedupe / 사이클 29 005935 chain 진단 / 사이클 38 명문화 / 사이클 55 R-1 SellRejectionTracker / 사이클 66 priority cap / 사이클 67 stale_manager 분해 / 사이클 68 KST 통일 / 사이클 72 _DbLogHandler dedupe / 사이클 73 R-1/R-2/S-1 / 사이클 74 WS sampling collector / 사이클 75 e2e flaky 영구 차단 / 사이클 76 api retry 5.80x / 사이클 77 hotfix 2단 모두 영속 (변경 0)**. **CLAUDE.md "절대 깨지 말 것" 8 영역 영속** (체결통보 H0STCNI0/H0STCNI9 / uvicorn 단일 워커 / 주문번호 매핑 + race 가드 / `_reset_daily_state` / 익일 청산 30s 안정화 / NXT 매도 거부 좀비 차단 + SellRejectionTracker / WebSocket 4중 안전망 + K stale watcher + 사이클 29-R3 priority + 사이클 66 cap / KIS 거부 응답 + KST 강제 + 사이클 38 명문화 / donchian_swing `_swing_rest_poll_loop`). **부차 발견 카드 #20 (LOW, 본 사이클 범위 외)**: `stop()` task cancel 목록 (L860~865) 에 `_api_recovered_collector_task` 누락 (사이클 76 도입 시 누락, 좀비 task 위험). 운영 중 `_running=False` → loop 종료 → task 자연 종료가 보장되므로 즉각 위험 낮음, **사이클 79+ 인계** (영구 가드 추가 권고). **운영 효과 예상 (push 후 측정)**: `[swing_rest_poll_summary]` 5분 주기 emit 정상화 (현재 영구 0건 → 영업일 ~144건/day) + `[stale_watcher_summary]` 동일 (0건 → ~144건/day) + collector 메모리 누적 영구 차단 (5분 윈도우 종료 시 자동 비움) + dup_factor 추가 감소 (사이클 74 의도 완전 작동). **silent 결함 영구 차단 13 회 누적** (사이클 60 mock_get_balance + pytest-timeout / 사이클 64 scheduler emit / 사이클 65 #1 system_logs KST + retry / 사이클 65 #2 e2e api-mocks / 사이클 65 #3 e2e toBeVisible / 사이클 66 cap=10 priority / 사이클 67 facade patch G-16 + priority cap G-17 / 사이클 68 hotfix production default_factory + 12 fixture + AST / 사이클 72 `_DbLogHandler` dedupe + 22+ 사이트 write_log 제거 + AST G-6/G-7/G-8 / 사이클 73 R-1/R-2/S-1 + G-6.R/G-6.S / 사이클 77 hotfix #1 Dashboard endpoint 2 + G-AST5 / 사이클 77 hotfix #2 mock schema 정확 매칭 / **사이클 78 flush 호출 사이트 누락 + 메모리 leak + AST G-AST1 영구 가드**). **사이클 79+ 후속 카드 인계**: **#20 (LOW 신규 부차)** `_api_recovered_collector_task` cancel 영구 가드 (사이클 78 부차 발견) + **#16 (MEDIUM Q6-3 2026-06-13 이후)** 후보 풀 폭축 2주 회고 + **#15 (LOW Q7-2)** 액면분할 prdy_clpr invalidate + **#11/#12** 보드 mutex / 14 모듈 logger + **Q6-2 옵션 B 헬퍼** (사이클 71+) + auth 1.43x (LOW) 영속. 사이클 49→78 누적 30 사이클 + hotfix 7 영역. | `src/engine/scheduler.py` (+26L / -2L, `_api_recovered_collector_loop` 본체 L2469~L2480 swing/stale flush 4 줄 + `flush_stale_watcher_collector` import + `if not self._running: break` 위치 이동 + `stop()` lifecycle hook L875 양쪽 flush try/except) / `tests/unit/engine/test_cycle78_flush_call_sites.py` 신규 (G-FL1~FL4, 4 케이스) / `tests/unit/engine/test_cycle78_memory_leak_prevention.py` 신규 (G-ML1, 1 케이스 freezegun) / `tests/unit/ast/test_cycle78_ast_flush_required.py` 신규 (G-AST1, 1 케이스 미래 신규 collector 누락 영구 차단) / `_workspace/red/cycle78_flush_missing.md` Red 명세 영구 기록 / `_workspace/test_index.yaml` 영향 인덱스 갱신 (백엔드 +3 = 332) / `src/engine/CLAUDE.md` 사이클 74 본문 *위*에 사이클 78 명세 추가 / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 78 행 | 사이클 78 C 진단 (Supabase READ-ONLY) 으로 카드 #19 (realtime_other 16.13x) 거짓 알람 종결 + 사이클 74 도입 누락 silent 결함 (flush 호출 사이트 0건 = `[*_summary]` 0건 영구 + collector 무한 누적 = 메모리 leak HIGH) 확정 영구 시정. 17 사이클 연속 옵션 A 패턴 영속. team-leader 진단 단계에서 *진짜 결함 발견* (사이클 73 인계 카드와 별개) = silent 결함 영구 차단 13 회 누적. 사이클 76 부분 시정 (`_api_recovered_collector_loop` 도입했으나 swing/stale flush 누락) 영구 보완. 매매 안전성 무영향 (로깅 영역 시정만, 매도 hot path 영향 0). 사이클 17 KIS LMS chain / 사이클 18 풀 dedupe / 사이클 29 005935 chain 진단 / 사이클 38 명문화 / 사이클 55 R-1 / 사이클 66 priority / 사이클 67 분해 / 사이클 68 KST / 사이클 72 dedupe / 사이클 73 R-1/R-2/S-1 / 사이클 74 WS sampling / 사이클 75 e2e flaky 영구 차단 / 사이클 76 api retry / 사이클 77 hotfix 2단 모두 영속. **운영 효과 예상 (push 후)**: `[swing_rest_poll_summary]` 0 → ~144건/day + `[stale_watcher_summary]` 0 → ~144건/day + 메모리 leak 영구 차단 + dup_factor 추가 감소. 사이클 49→78 누적 30 사이클 + hotfix 7 영역. 후속 카드 사이클 79+ 인계: #20 (LOW 신규 부차) `_api_recovered_collector_task` cancel 영구 가드 + #16 (MEDIUM Q6-3 2026-06-13 이후) / #15 (LOW 액면분할) / #11/#12 / Q6-2 옵션 B 헬퍼 / auth 1.43x 영속. |
| 2026-06-08 | 사이클 77 hotfix 2단 통합 — **e2e flaky 영구 차단** (사이클 75 G-AST1 영역 한계 + mock schema 미일치 silent 결함 2 결함 영구 시정). **hotfix #1 (`909ee4c`)**: 사이클 76 CI fail (1차 + 재실행 모두 fail = flaky 아닌 진짜 결함 확정) — `/api/market-regime/current` + `/api/realtime/subscriptions` ECONNREFUSED (Dashboard 영역, 사이클 75 Settings 한정 매핑 영역 외). 시정: `e2e/fixtures/api-mocks.ts` +26L 4 라우트 추가 (market-regime/current + history + auto-adjust + realtime/subscriptions) + AST G-AST5 신설 `_ast_api_mocks_coverage.test.ts` +24L (`REQUIRED_DASHBOARD_ENDPOINTS` literal + `it.each` + `isRouteRegistered` 검증, 사이클 75 G-AST1 Settings 한정 한계 영구 차단). **hotfix #2 (`c647f2a`)**: hotfix #1 CI fail (ECONNREFUSED 해결됐으나 `getByText('상한가 모멘텀') not found` toBeVisible 10s timeout) — mock schema 미일치 silent 결함 (사이클 77 hotfix #1 추가 시 `is_buy_allowed` + `raw` 잘못 추가 + 누락 5 필수 필드 `buy_blocked` / `block_reason` / `auto_regime_adjust` / `cash_usage_ratio` / `enabled`) → `MarketRegimeCard` 컴포넌트 throw → Suspense 무한 로딩 → settings 페이지 렌더 차단. 시정: `api-mocks.ts` +7/-2 (잘못된 필드 2 제거 + 누락 5 필수 필드 추가, `MarketRegimeCurrent` interface `frontend/src/types/market_regime.ts` 정확 매칭). realtime/subscriptions = `data.data ??` graceful fallback 영속 (시정 불필요). 검증: 프론트 187 PASS / 사이클 75 17 AST 가드 영속 / 회귀 0. 운영 영향 0 (e2e 환경 한정 시정). silent 결함 영구 차단 12 회 누적 (사이클 60/64/65#1/65#2/65#3/66/67/68/72/73/77/77#2). | `e2e/fixtures/api-mocks.ts` (hotfix #1 +26L + hotfix #2 +7/-2 = 합 +31L / -2L, 4 라우트 추가 + market-regime/current schema 정확 매칭 5 필수 필드) / `frontend/src/components/__tests__/_ast_api_mocks_coverage.test.ts` (+24L, G-AST5 신규 describe + `REQUIRED_DASHBOARD_ENDPOINTS` literal + `it.each` + `isRouteRegistered`) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 77 hotfix 2단 행 / `docs/HARNESS_CHANGELOG.md` 사이클 77 hotfix 2단 행 | 사이클 73 1차 + 사이클 74 1차 + 사이클 76 1차/재실행 = flaky → 진짜 결함 확정 chain. 사이클 77 hotfix 2단 진화 = ECONNREFUSED 해결 (hotfix #1) → toBeVisible timeout 해결 (hotfix #2, mock schema 정확 매칭). 사이클 75 G-AST1 Settings 한정 영역 한계 영구 차단 = G-AST5 Dashboard 영역 매핑 확장. 매매 안전성 무영향 (e2e + frontend retry 영역 한정). 사이클 65 hotfix #1/#2/#3 영속 + 사이클 75 17 AST 가드 영속. silent 결함 12 회 누적 → 사이클 78 (별도 행) 추가 = 13 회. |
| 2026-06-08 | 사이클 76 — 카드 #23 (MEDIUM) **api retry 5.80x dup 시정** (사이클 74 발견 신규 결함, **15 사이클 연속 옵션 A 패턴 영속**). team-leader Phase 1 + 사용자 결정 채택: **결정 1 = E** (사이클 18 60s WARNING dedupe + 사이클 74 5분 recovered collector 하이브리드) + **결정 2 = Y** (ERROR 보존 매트릭스 R2/R4/R7/R8 individual 영속) + **결정 3 = Q** (domain-expert 자문 생략 — 사이클 18+74 100% 답습) + **Q1** (AST 영구 가드 G-AST1~AST3) + **Q2** (빈 윈도우 skip) + **Q4** (메인 + 풀 dedupe state 분리, 사이클 18 패턴 영속). **사이클 74 운영 실증 잔존 dup (배경)**: `api` 5.80x (29/5) + `[api_retry_recovered]` 7.00x (14/2) + `[api_retry_exhausted]` 4.00x (4/1) — KIS API 호출 영역 HTTP retry 메시지 반복. **결함 사이트 2 영역 (Phase 1-A team-leader grep + Read 확정)**: 영역 1 `src/api/base.py::_request` (메인 단일 L321~484, 사이클 18 dedupe **미적용** — 5.80x 주요인) + 영역 2 `src/api/base.py::_request_via_quote_pool` (시세 풀 L604~734, 사이클 18 dedupe **영속** — recovered/exhausted 만 신규 = 시세 풀은 이미 부분 흡수 완료). **시정 (옵션 E 사이클 18 + 74 하이브리드 답습)**: **신규 함수 7 (`src/api/base.py` +150L)**: (1) `_record_request_5xx_for_dedupe(path, status) -> bool` 메인 5xx WARNING 60s dedupe (사이클 18 `_record_5xx_for_dedupe` 답습, Q4 메인용 분리, `_request_5xx_dedupe: dict[(path, status), {first_at, count}]` state) — `True` 반환 시 WARNING emit 의무 (첫 호출 또는 60s 경과), `False` 시 skip (카운트 누적만) + (2) `_record_api_recovered(path, attempts)` 메인 retry 성공 누적 (`_api_recovered_collector: dict[path, {count, max_attempts}]`) + (3) `_record_quote_recovered(path, attempts)` 시세 풀 retry 성공 누적 (Q4 분리, `_quote_recovered_collector` 별도 state) + (4) `_flush_api_recovered_collector()` 5분 윈도우 종료 시 `logger.info("[api_retry_recovered_summary] window=300s total=N by_path={...}")` 1행 emit (Q2 빈 윈도우 count=0 skip) + (5) `_flush_quote_recovered_collector()` 동일 (Q4 분리) + (6) `_warn_http_status(status, attempt, max_retries, path)` `logger.warning("HTTP %s ...")` 헬퍼 추출 (G-AST1 통과용, 직접 호출 차단) + (7) 모듈 상수 `_REQUEST_5XX_DEDUPE_WINDOW=60.0` / `_API_RECOVERED_COLLECTOR_WINDOW=300.0` (사이클 18 `_QUOTE_5XX_DEDUPE_WINDOW=60.0` + 사이클 42 `HEARTBEAT_METRICS_INTERVAL_SECS=300` 답습). **State 3 (Q4 메인/풀 분리)**: `_request_5xx_dedupe: dict[(path, status), {first_at, count}]` (메인, 사이클 76 신규) ↔ `_quote_5xx_dedupe: dict[(path, label, status), {...}]` (사이클 18 영속) + `_api_recovered_collector: dict[path, {count, max_attempts}]` (메인) ↔ `_quote_recovered_collector` (풀, 분리). **호출 사이트 정정**: `_request` L487 `_warn_http_status` 경유 (기존 `logger.warning("HTTP %s ...")` 직접 호출 제거) + L545 `_record_api_recovered(path, attempt)` (기존 `await write_log("INFO", "[api_retry_recovered]...")` 직접 호출 제거) + `_request_via_quote_pool` 동일 영역 `_record_quote_recovered` 호출. **신규 task (`src/engine/scheduler.py` +34L)**: `_api_recovered_collector_loop` 5분 주기 (사이클 42 `_heartbeat_metrics_loop` 패턴 답습) — `connect()` 시점 task 시작 (`self._api_recovered_collector_task = asyncio.create_task(...)` L486) + `disconnect()` cancel + await + 마지막 flush 1회 (Q5 사이클 74 답습, `_flush_api_recovered_collector` + `_flush_quote_recovered_collector` 순차 호출) + `while self._running` + 300s sleep + 각 try/except `logger.exception` graceful + `if not self._running: break` 즉시 종료. **ERROR 보존 매트릭스 4 영역 individual 영속 (결정 Y, 변경 0)**: R2 `[api_retry_exhausted]` 5xx 최종 실패 (`base.py:L495` `await write_log("ERROR", "[api_retry_exhausted] ... last_status=5xx")` 영속) + R4 `[api_retry_exhausted]` 네트워크 최종 실패 (L523 영속) + R7 `[api_retry_exhausted]` 토큰 만료 최종 실패 (L563 영속) + R8 `[kis_rejection]` (L590 메인 + L846 quote, CLAUDE.md "절대 깨지 말 것" 영속) — **사이클 29 005935 LMS chain 진단 의무**. **AST 영구 가드 3 신설** (사이클 65 H3 / 사이클 72 G-6 / 사이클 73 G-6.R/G-6.S / 사이클 74 G-7/G-8 / 사이클 75 G-AST1~AST4 패턴 답습): G-AST1 (`_request` 본문 직접 `logger.warning("HTTP ...")` 호출 0건 정적 검증 — 헬퍼 경유만, `_warn_http_status` 추출 L240 후 L487 호출만 허용) + G-AST2 (사이클 18 `_record_5xx_for_dedupe` 호출 ≥1건 영속 검증, 사이클 18 회귀 차단) + G-AST3 (`[api_retry_recovered]` 직접 `write_log` 호출 0건 정적 검증, collector 경유만). **회귀 가드 15 케이스 (4 파일)**: **G-MD1~MD4** (4 `tests/unit/api/test_cycle76_request_5xx_dedupe.py` 메인 60s WARNING dedupe freezegun) — G-MD1 동일 (path, status) 60s 윈도우 첫 호출 WARNING 1회 + G-MD2 60s 내 5회 호출 WARNING 1회 카운트 누적 + G-MD3 60s 경과 후 재 emit + G-MD4 다른 (path, status) 각각 별도 dedupe + **G-RC1~RC4** (4 `tests/unit/api/test_cycle76_recovered_collector.py` 5분 collector freezegun) — G-RC1 5분 윈도우 5회 retry 성공 → summary 1행 (80% 감소) + G-RC2 path 별 통계 + G-RC3 빈 윈도우 emit 0 (Q2) + G-RC4 메인+풀 분리 (Q4) + **G-ERR1~ERR4** (4 `tests/unit/api/test_cycle76_error_preservation_matrix.py` 사이클 29 영속) — G-ERR1 5xx exhausted + G-ERR2 네트워크 exhausted + G-ERR3 토큰 exhausted + G-ERR4 `[kis_rejection]` (CLAUDE.md 영속) + **G-AST1~AST3** (3 `tests/unit/ast/test_cycle76_ast_api_retry_helper.py`) — AST 정적 가드 (사이클 18 G-AST2 영속 + 사이클 76 신규 G-AST1/G-AST3). **백엔드 2068 → 2083 PASS + 1 XFAIL + 2 skip** (+15 신규). 카테고리 분리 측정 (silent 결함 0): cycle76 15 PASS (0.18s) / api/ 전체 146 PASS (1.26s) / engine/ 전체 1183 PASS + 1 XFAIL (35.87s, scheduler task 영향 좀비 0) / ast/ 전체 9 PASS (0.05s) / 백엔드 전체 2083 PASS + 1 XFAIL + 2 skip (43.75s). **flakiness 0** (3 회 cycle76 ±0.00s / api/+ast/ 카테고리 ±0.00s). 회귀 0 / 매매 안전성 무영향 (KIS API base 로깅 영역 시정 + ERROR individual 영속, `_request` 호출자 매매 hot path 영향 0). **사이클 17 OPSP0002 backoff 영속** (`src/realtime/websocket.py::_opsp_backoff_until` dict + 300s backoff 변경 0, 사이클 76 시정 영역 외) + **사이클 18 풀 dedupe 영속** (G-AST2 PASS = `_record_5xx_for_dedupe` 호출 ≥1건 + `_QUOTE_5XX_DEDUPE_WINDOW=60.0` 변경 0) + **사이클 38/55 R-1/66/67/68/72/73/74/75 모두 무영향** (production 변경 영역 = `src/api/base.py` + `src/engine/scheduler.py` 만, 매매 hot path 무관). **CLAUDE.md "절대 깨지 말 것" 8 영역 영속** (체결통보 H0STCNI0/H0STCNI9 / uvicorn 단일 워커 / 주문번호 매핑 + race 가드 / `_reset_daily_state` / 익일 청산 30s 안정화 / NXT 매도 거부 좀비 차단 + SellRejectionTracker 사이클 55 R-1 / WebSocket 4중 안전망 + K stale watcher + 사이클 29-R3 priority + 사이클 66 cap / **KIS 거부 응답 `[kis_rejection]` 영구 저장** L590 메인 + L846 quote + KST 강제 사이클 65 H2/H2-bis + 68 통일 / 사이클 38 명문화 / donchian_swing `_swing_rest_poll_loop` / 사이클 72 dedupe / 사이클 73 R-1/R-2/S-1 / 사이클 74 WS sampling / 사이클 75 e2e flaky 영구 차단). 변경 통계 3 파일 +207L (base.py +150 + scheduler.py +34 + test_base_retry_logging.py +23) + 신규 회귀 가드 4 파일 (api 3 신규 + ast 1 신규). **silent 결함 영구 차단 10 회 누적** (사이클 60/64/65#1/65#2/65#3/66/67/68/72/73) — 사이클 76 = 신규 결함 시정 아니라 합법적 retry 빈도 흡수 영역 (사이클 74 패턴 답습). **운영 효과 예상 (push 후 V-11 SQL 측정 의무)**: `api` 5.80x → **1.0~1.2** + `[api_retry_recovered]` 7.00x → **1.0 (5분당 1행, 80% 감소)** + 사이클 74 1.29x 대비 추가 감소 → dup_factor 1.0 근접. **월요일 (2026-06-09) 09:00~10:00 1h tester verify 시나리오 통합 영속** (사이클 66 D + 사이클 67 A/B/C + 사이클 74 E + **사이클 76 신규 F**) — F 시나리오 = `[api_retry_recovered_summary]` / `_request_5xx_dedupe` 메인 60s dedupe 운영 실측. **사이클 77+ 후속 카드 인계**: **#19 (HIGH)** `realtime_other` 16.13x dup (사이클 73 인계, 사이클 76 영역 외) + **#20 (LOW)** collector shared 헬퍼 추출 — **사이클 74+76 = 2회 답습** (3회 추상화 규칙 미충족, 사이클 77+ 신규 영역 추가 시 발의) + **#21 (LOW)** `[ws_opsp_backoff_state]` 5분 emit + **#22 (LOW)** `[ws_restore_after_reconnect]` 1행 emit + **auth 1.43x** 별개 카드. 사이클 49→76 누적 28 사이클 + hotfix 5 영역. | `src/api/base.py` (+150L, 신규 헬퍼 7 + state 3 + 모듈 상수 2 + `_request` 호출자 정정 `_warn_http_status` 경유 + `_record_api_recovered` 사용 + `_request_via_quote_pool` `_record_quote_recovered` 추가) / `src/engine/scheduler.py` (+34L, `_api_recovered_collector_loop` task 5분 주기 + `connect/disconnect` lifecycle) / `tests/unit/api/test_base_retry_logging.py` (+23L, 기존 recovered 테스트 직접 write_log → collector 누적 확인으로 갱신) / `tests/unit/api/test_cycle76_request_5xx_dedupe.py` 신규 (G-MD1~MD4, 4 케이스 freezegun) / `tests/unit/api/test_cycle76_recovered_collector.py` 신규 (G-RC1~RC4, 4 케이스 freezegun) / `tests/unit/api/test_cycle76_error_preservation_matrix.py` 신규 (G-ERR1~ERR4, 4 케이스 사이클 29 LMS chain 진단 의무) / `tests/unit/ast/test_cycle76_ast_api_retry_helper.py` 신규 (G-AST1~AST3, 3 케이스 AST 정적 가드) / `_workspace/red/cycle76_api_retry_aggregation.md` Red 명세 영구 기록 / `_workspace/test_index.yaml` 영향 인덱스 갱신 / `src/api/CLAUDE.md` 본문 사이클 76 명세 추가 (헬퍼 7 + state 3 + lifecycle + ERROR 보존 매트릭스) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 76 행 | 사이클 74 발견 신규 결함 (`api` 5.80x dup) 시정 = 사이클 18 60s WARNING dedupe + 사이클 74 5분 recovered collector 하이브리드 (옵션 E) 채택. team-leader Phase 1-A 식별 2 영역 (메인 미적용 + 풀 영속) + ERROR 보존 매트릭스 4 영역 individual 영속 (사이클 29 005935 LMS chain 진단 의무 — `[api_retry_exhausted]` 3 영역 + `[kis_rejection]` CLAUDE.md 영속). 15 사이클 연속 옵션 A 패턴 영속 (사이클 55 R-1 / 60 / 62 / 63 / 64 / 65 / 66 / 67 / 68 / 69 / 72 / 73 / 74 / 75 / 76). 매매 안전성 무영향 (KIS API base 로깅 영역만 + `_request` 호출자 매매 hot path 영향 0 + ERROR individual 영속). 사이클 17 OPSP0002 backoff (`websocket.py`) 변경 0 + 사이클 18 풀 dedupe 변경 0 (G-AST2 영속) + 사이클 38/55 R-1/66/67/68/72/73/74/75 모두 무영향. **운영 효과 예상**: api 5.80x → 1.0~1.2 + recovered 7.00x → 1.0 (5분당 1행) + 사이클 74 1.29x 대비 추가 감소. push 후 V-11 SQL 측정 의무. 월요일 (2026-06-09) 09:00~10:00 1h tester verify 시나리오 통합 영속 + 신규 F (`[api_retry_recovered_summary]` 운영 실측). 사이클 49→76 누적 28 사이클 + hotfix 5 영역. 후속 카드 사이클 77+ 인계: #19 (HIGH realtime_other 16.13x 사이클 73) / #20 (LOW collector shared 헬퍼 추출 = 사이클 74+76 2회 답습, 3회 추상화 규칙 사이클 77+ 신규 영역 추가 시 발의) / #21/#22 (LOW WS) / auth 1.43x (LOW) / #16 (MEDIUM Q6-3 2026-06-13 이후) / #15 (LOW 액면분할) / #11/#12 / Q6-2 옵션 B 헬퍼 영속. |
| 2026-06-08 | 사이클 75 — 카드 #19' (HIGH) **e2e api-mocks flaky 영구 차단** (사이클 73 1차 + 사이클 74 1차 = **flaky 2회 누적 확정** → 진짜 결함 영역 확정). team-leader Phase 1 분석 후 사용자 결정 채택: **결정 1 = C** (api-mocks 7 endpoint + AST 영구 가드) + **결정 2 = X** (`frontend/src/api/*.ts` literal vs api-mocks 대조) + **Q4 채택** (`retry: 1` 영역 확장 IntegrationToggleCard / CashUsageRatioCard / KisQuoteAccountsCard 3 컴포넌트) = **14 사이클 연속 옵션 A 패턴 영속** (사이클 55 R-1 / 60 / 62 / 63 / 64 / 65 / 66 / 67 / 68 / 69 / 72 / 73 / 74 / 75). **결함 (사이클 73/74 1차 fail 로그)**: vite proxy → backend ECONNREFUSED 3 endpoint (`/api/strategies/system/cash-usage-ratio` + `/api/integrations/dkstock-regime` + `/api/integrations/kis-mcp`) → `Locator: getByText('상한가 모멘텀').first()` not found → settings.spec.ts:12 fail. **Phase 1-A 결정적 결함 확정 (team-leader 분석)**: `e2e/playwright.config.ts` 명시 "백엔드 미가동 — 모든 /api/* 호출은 각 spec 의 page.route 모킹으로 응답" 정책이나 Settings 페이지 진입 시 마운트 컴포넌트의 호출 endpoint **7 endpoint group 미등록** = (1) `CashUsageRatioCard` `/api/strategies/system/cash-usage-ratio` + (2~5) `IntegrationToggleCard` 4 토글 `/api/integrations/dkstock-regime` / `/kis-mcp` / `/auto-regime-adjust` / `/auto-apply` + (6) `BuyBlockSection` `/api/integrations/buy-block` + `/buy-block/thresholds` + (7) `KisQuoteAccountsCard` `/api/integrations/quote-accounts*` = 사이클 73 fail 로그 3 endpoint = **빙산의 일각** (나머지 4 endpoint group 은 race 의존 미노출). **Flaky 근본 원인 (Q4 발견)**: `IntegrationToggleCard` / `CashUsageRatioCard` / `KisQuoteAccountsCard` = React Query 전역 default `retry: 1` 상속만 의존 (카드 단위 명시 안 함). 사이클 65 hotfix H1 = `PriceFilterCard` / `TradeAmountFilterCard` 만 명시 → 사이클 75 = 3 컴포넌트 영역 확장 채택. ECONNREFUSED + React Query default retry 3회 누적 (사이클 65 hotfix H1 미적용 영역) + 페이지 어셈블 5~10s timeout 임계 = 운에 따라 flaky. **시정 (옵션 C 사이클 65 hotfix #2/#3 답습)**: **(A) `e2e/fixtures/api-mocks.ts` +53L 7 라우트 추가**: `**/api/strategies/system/cash-usage-ratio` (envelope `{ratio: 1.0}`) + `**/api/integrations/dkstock-regime` (`{enabled: false, source: "env"}`) + `**/api/integrations/kis-mcp` (`{enabled: false, source: "env"}`) + `**/api/integrations/auto-regime-adjust` (`{enabled: true}`) + `**/api/integrations/auto-apply` (`{enabled: false}`) + `**/api/integrations/buy-block` (`{mode: "HARD", blocked: false, reasons: [], thresholds: {vix: 25, fg_high: 85, fg_low: 15, defensive: true}}`) + `**/api/integrations/buy-block/thresholds` + `**/api/integrations/quote-accounts**` (`[]` 단일 prefix 라우트 GET/POST/PUT/DELETE 커버) — 사이클 65 hotfix #2 패턴 답습 (구체 라우트 *전* 와일드카드 라우트 우선순위 정합, envelope 헬퍼 영속). **(B) 3 컴포넌트 `retry: 1` 영역 확장 (Q4)**: `CashUsageRatioCard.tsx:27` (+1L) + `IntegrationToggleCard.tsx:190` (BuyBlockSection useQuery) + `:469` (ToggleRow useQuery, 2 위치 +2L) + `KisQuoteAccountsCard.tsx:87` (+1L) — 5 위치 명시 (사이클 65 H1 답습). **(C) `IntegrationToggleCard.test.tsx` 보정** (+8L): I8-G `waitFor` timeout 1000ms → 3000ms 보정 + `setupToggleStubs` auto-apply 핸들러 신규 추가 (line 269) — 사이클 75 Q4 retry:1 도입의 직접 사이드이펙트 (1012ms 측정 / retry 시도 시간 + 기본 timeout 초과). **AST 영구 가드 17 케이스 신설** (사이클 65 H3 / 사이클 72 G-6 / 사이클 73 G-6.R/G-6.S / 사이클 74 G-7/G-8 패턴 답습): **vitest 15 케이스** = (1) `frontend/src/components/__tests__/_ast_api_mocks_coverage.test.ts` 신규 10 — G-AST1 (5 integrations endpoint) + G-AST2 (cash-usage-ratio) + G-AST3 (quote-accounts) + G-AST4 (3 컴포넌트 useQuery `retry: 1` 옵션 명시 — Q4 ) (it.each 분해 = 10) + (2) `frontend/src/components/__tests__/_ast_useQuery_retry_required.test.ts` 확장 5 = **사이클 65 H3 2 영속** (PriceFilterCard + TradeAmountFilterCard) + **사이클 75 3 신규** (G-RT1 IntegrationToggleCard + G-RT2 CashUsageRatioCard + G-RT3 KisQuoteAccountsCard, 사이클 65 H3 답습 패턴) + **pytest 2 케이스** = `tests/unit/e2e_mocks/test_cycle75_api_mocks_routes_registered.py` 신규 (G-E2E1 7 endpoint group 정규식 grep + 방어 가드). 미래 신규 frontend `useQuery` endpoint 추가 시 api-mocks 누락 영구 차단 + 신규 카드 `retry:1` 누락 영구 차단. **백엔드 2066 → 2068 PASS + 1 XFAIL + 2 skip** (+2 pytest AST 추가) / **프론트 172 → 185 PASS** (+13 vitest AST + I8-G timeout 보정 회귀 0). 카테고리 분리 측정 (silent 결함 0): cycle75 신규 17 PASS / 사이클 65~74 AST 누적 225 PASS / db/ + main/ + ast/ + stale_manager/ + cycle74 engine/realtime 225 PASS 영속 / 백엔드 전체 2068 PASS + 1 XFAIL + 2 skip (60.93s) / 프론트 전체 39 files 185 PASS (3.20s). **합계 2253 PASS + 1 XFAIL + 2 skip**. **flakiness 0** (3 회 pytest 0.01s + vitest 0.4~0.5s 동일 결과). 회귀 0 / 매매 안전성 무영향 (e2e + frontend retry 영역 한정 + production 코드 변경 0). **사이클 65 hotfix #1/#2/#3 영속 (변경 0)**: `system_logs.py:44` + `main.py:131` KST timestamp + api-mocks `**/api/system/price-filter` :188 + `**/api/system/trade-amount-filter` :193 + settings.spec.ts toBeVisible timeout:10000 2회 + PriceFilterCard:60 + TradeAmountFilterCard:62 retry:1 영속. **CLAUDE.md "절대 깨지 말 것" 8 영역 영속** (체결통보 H0STCNI0/H0STCNI9 / uvicorn 단일 워커 / 주문번호 매핑 + race 가드 / `_reset_daily_state` / 익일 청산 30s 안정화 / NXT 매도 거부 좀비 차단 + SellRejectionTracker 사이클 55 R-1 / WebSocket 4중 안전망 + K stale watcher + 사이클 29-R3 priority + 사이클 66 cap / KIS 거부 응답 + KST 강제 사이클 65 H2/H2-bis + 68 통일 / 사이클 38 명문화 / donchian_swing _swing_rest_poll_loop / 사이클 72 dedupe / 사이클 73 R-1/R-2/S-1 / 사이클 74 sampling). 사이클 73 변경 시점 (R-1/R-2/S-1) 영속 + 사이클 74 collector aggregation 영속. **Playwright 실제 실행 운영자 1회 확인 권고**: `npx playwright install` 후 `npx playwright test --config=e2e/playwright.config.ts e2e/settings.spec.ts` 실제 실행으로 ECONNREFUSED 0건 + BuyBlockSection 4 슬라이더 렌더링 확인 — 간접 AST 가드 17 케이스로 영구 차단 보장. 변경 통계 5 파일 +65L (e2e/fixtures/api-mocks.ts +53 + CashUsageRatioCard +1 + IntegrationToggleCard +2 + KisQuoteAccountsCard +1 + IntegrationToggleCard.test.tsx +8) + 신규 회귀 가드 3 파일 (vitest 1 + vitest 확장 + pytest 1). **silent 결함 영구 차단 10 회 누적** (사이클 60/64/65#1/65#2/65#3/66/67/68/72/73) — 사이클 75 = 신규 결함 시정 아니라 사이클 73/74 flaky 영구 차단 (사이클 65 hotfix #2 답습 영역). **사이클 76+ 후속 카드 인계**: **#19 (HIGH)** `realtime_other` 16.13x dup 영역 (사이클 73 인계, 사이클 74 운영 1주 dup 측정 후 발의) + **#20 (MEDIUM)** collector 헬퍼 추출 `src/realtime/_log_aggregator.py` (사이클 74 인계, 3회 반복 추상화 규칙 사이클 76+) + **#21 (MEDIUM)** `[ws_opsp_backoff_state]` 5분 emit (사이클 74 인계) + **#22 (LOW)** `[ws_restore_after_reconnect]` 1행 emit (사이클 74 인계) + **#23 (MEDIUM 신규)** `api` retry 5.80x dup 영역 (사이클 74 발견 — `api` 5.80x + `[api_retry_recovered]` 7.00x + `[api_retry_exhausted]` 4.00x) + **auth 1.43x** 별개 카드 (사이클 73 인계). 사이클 49→75 누적 27 사이클 + hotfix 5 영역. | `e2e/fixtures/api-mocks.ts` (+53L, 7 endpoint group 라우트 추가 — cash-usage-ratio + 4 integrations 토글 + buy-block + buy-block/thresholds + quote-accounts wildcard) / `frontend/src/components/CashUsageRatioCard.tsx` (+1L, L27 useQuery retry:1) / `frontend/src/components/IntegrationToggleCard.tsx` (+2L, L190 BuyBlockSection + L469 ToggleRow useQuery retry:1) / `frontend/src/components/KisQuoteAccountsCard.tsx` (+1L, L87 useQuery retry:1) / `frontend/src/components/__tests__/IntegrationToggleCard.test.tsx` (+8L, I8-G waitFor timeout 1000→3000ms + setupToggleStubs auto-apply 핸들러 L269) / `frontend/src/components/__tests__/_ast_api_mocks_coverage.test.ts` 신규 (10 케이스 vitest AST G-AST1~AST4, frontend/src/api/*.ts literal vs api-mocks route glob 매칭 정적 검증) / `frontend/src/components/__tests__/_ast_useQuery_retry_required.test.ts` 확장 (+3 케이스 G-RT1~RT3, 사이클 65 H3 2 영속) / `tests/unit/e2e_mocks/test_cycle75_api_mocks_routes_registered.py` 신규 (2 케이스 pytest AST 7 endpoint group 정규식 grep) / `_workspace/red/cycle75_e2e_flaky_fix.md` Red 명세 영구 기록 / `frontend/CLAUDE.md` 사이클 75 retry 영역 확장 + AST 가드 명세 추가 / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 75 행 | 사이클 73 1차 + 사이클 74 1차 e2e fail (flaky 2회 누적 확인) = 진짜 결함 영역 확정 후 사이클 65 hotfix #2/#3 패턴 100% 답습. team-leader Phase 1-A 식별 7 endpoint group 미등록 (Settings 페이지 마운트 컴포넌트 호출 vs api-mocks 등록 매핑 결과) + Q4 발견 (사이클 65 H1 retry:1 영역이 PriceFilterCard/TradeAmountFilterCard 2개에 한정, 3 컴포넌트 미적용 = flaky 사이드이펙트 영역). 14 사이클 연속 옵션 A 패턴 영속. AST 영구 가드 17 케이스 = 미래 신규 frontend useQuery endpoint 추가 시 api-mocks 누락 영구 차단 (G-AST1~AST3) + 미래 신규 카드 retry:1 누락 영구 차단 (G-RT1~RT3 + 사이클 65 H3 2 영속). 매매 안전성 무영향 (e2e + frontend retry 영역 한정 + production 코드 변경 0). 사이클 65 hotfix #1/#2/#3 영속 (변경 0 확인). 사이클 73 R-1/R-2/S-1 영속 (변경 0) + 사이클 74 collector aggregation 영속 (변경 0) + 사이클 38/55 R-1/66/67/68/72 모두 무영향. Playwright 실제 실행 운영자 1회 확인 권고 (간접 AST 가드 17 케이스로 영구 차단 보장). 사이클 49→75 누적 27 사이클 + hotfix 5 영역. 후속 카드 사이클 76+ 인계: **#19 (HIGH)** realtime_other 16.13x (사이클 73) / **#20 (MEDIUM)** collector shared 헬퍼 추출 (사이클 74) / **#21 (LOW)** [ws_opsp_backoff_state] / **#22 (LOW)** [ws_restore_after_reconnect] / **#23 (MEDIUM 신규)** api retry 5.80x dup (사이클 74 발견) / **auth 1.43x** (LOW) / #16 (MEDIUM Q6-3 2026-06-13 이후) / #15 (LOW 액면분할) / #11/#12 / Q6-2 옵션 B 헬퍼 영속. |
| 2026-06-08 | 사이클 74 — **WebSocket 로그 sampling/aggregation/rate cap (옵션 E-1) 도입** (사이클 71/73 운영 실증 잔존 dup 영역 흡수, **13 사이클 연속 옵션 A 패턴 영속**). team-leader Phase 1 + refactor-expert 자문 (`_workspace/refactor/cycle74_sampling_advisory.md` 6 카드 + 5 결정 의제) 후 사용자 결정 채택: **R3 (전체)** + **2-A C** (`[swing_rest_poll]` 5분 aggregation) + **2-B C 조건부** (`[stale_watcher]` aggregation + 결함 시 individual) + **2-C E-1** (WS 구독/ACK/해제 tr_id별 aggregation + ERROR individual) + **X** (5분 heartbeat 보존) + **Q2-D/E/Q3/Q4/Q5 모두 옵션 A** (refactor-expert 권고). **사이클 71/73 운영 실증 (배경)**: `[swing_rest_poll]` 5.00x (60s × 5 정상 반복, distinct=1) + `[stale_watcher]` 3.00x (120s × 30 정상) + `realtime` 3.00x (WS 구독/ACK/해제 logger.info 빈도) = 합법적 반복이나 일일 INSERT 누적 ~8,600/day, Supabase 무료 tier 500MB 약 8개월 후 한계. dup_factor 사이클 71 3.72 → 사이클 72 1.34 → 사이클 73 2.11 (시간 길수록 합법적 반복 누적). **신규 함수 (6 파일 +197/-31)**: **(A) `src/realtime/websocket.py` +95L** (`_ws_action_collector: dict[str, dict[str, list[str]]]` 인스턴스 변수 — tr_id → {SUBSCRIBE/UNSUBSCRIBE/ACK: list[tr_key], OPSP_ALREADY: int} 누적 + `_record_action(tr_id, tr_key, action)` (`websocket.py:289`) `_send_subscribe` 진입 + SUBSCRIBE SUCCESS + OPSP0002 ALREADY 분기 호출 + `_flush_ws_action_collector()` (`websocket.py:300`) tr_id별 1행 emit `[ws_action_summary] label=%s tr_id=%s window=300s +N SUBSCRIBE [...] -M UNSUBSCRIBE [...] ack=K opsp_already=L` (종목 cap 20 + overflow `...+N` 사이클 28 패턴 답습) + `_ws_action_metrics_loop` task 5분 주기 (사이클 42 `_heartbeat_metrics_loop` 패턴 답습) + `connect()` L166-168 `_ws_action_flush_task = asyncio.create_task(self._ws_action_metrics_loop())` task 시작 + `disconnect()` L228 진입 후 *cancel 전* `self._flush_ws_action_collector()` 1회 마지막 flush (Q5 옵션 A) + L230-236 task cancel + await + `asyncio.CancelledError` graceful + `self._ws_action_flush_task = None` 정리 + `_send_subscribe` L417 / `_handle_raw` SUBSCRIBE SUCCESS L524 / OPSP0002 ALREADY L490-493 직접 `logger.info(...)` 제거 + collector 흡수). **(B) `src/engine/stale_watcher_core.py` +51L** (`record_stale_watcher_check(stats: dict)` L46 모듈 전역 `_stale_watcher_collector: list[dict]` 누적 + `flush_stale_watcher_collector()` L57 `[stale_watcher_summary] checks=N stale_total=M retried=K cap_blocked=L force_retried=J` 1행 emit + `check_and_resubscribe_stale` L237 기존 `logger.info("[stale_watcher] ...")` 제거 + `record_stale_watcher_check({...})` 호출 + **결함 시 (`stale_count > 0`) `[stale_watcher_detail]` individual `stale_diagnostics.emit_stale_session_detail` 영속** + 사이클 66 K-10 `[stale_force_retry_cap]` / `[stale_priority_resubscribe_cap_exceeded]` WARNING individual 영속). **(C) `src/engine/scheduler.py` +42L** (`record_swing_rest_poll(stats: dict)` L133 모듈 전역 `_swing_rest_poll_collector: list[dict]` + `_swing_rest_poll_last_flush: float` + `flush_swing_rest_poll_collector()` L142 `[swing_rest_poll_summary] polls=N candidates_avg=X max=Y total_held=Z elapsed_ms_avg=W` 1행 emit + `_run_swing_rest_poll_once` L2319 기존 `logger.info("[swing_rest_poll] ...")` 제거 + `record_swing_rest_poll({...})` 호출 + 5분 주기 flush 조건). **AST 영구 가드 G-7/G-8 신설**: **G-7** (`tests/unit/ast/test_cycle74_ast_aggregator_helper.py`, `_send_subscribe` 함수 영역 직접 `logger.info("WebSocket 구독...")` 호출 0건 정적 검증) + **G-8-A** (`_run_swing_rest_poll_once` 영역 한정 `[swing_rest_poll]` summary prefix 직접 logger.info 0건) + **G-8-B** (`check_and_resubscribe_stale` 영역 한정 `[stale_watcher]` summary prefix 직접 logger.info 0건) — 사이클 72 G-6 / 사이클 73 G-6.R/G-6.S 패턴 답습 + `ast.FunctionDef` walk + multi-line logger 7줄 호환. **회귀 가드 23 케이스 (5 파일, ERROR 보존 매트릭스 8 영속 포함)**: **G-SP1~SP4** (4 케이스 `tests/unit/engine/test_cycle74_swing_rest_poll_aggregation.py` — 5분 윈도우 1행 emit + freezegun 통계 정확 + reset + shutdown flush Q5 옵션 A) + **G-SW1/SW2/SW5** (3 `tests/unit/engine/test_cycle74_stale_watcher_aggregation.py` — aggregation + 윈도우 통계 + 마지막 flush) + **G-SW3** (사이클 73 `[stale_watcher_detail]` individual 보존 영속) + **G-SW4** (사이클 66 K-10 `[stale_force_retry_cap]` / `[stale_priority_resubscribe_cap_exceeded]` WARNING individual 영속) + **G-WS1~WS6** (6 `tests/unit/realtime/test_cycle74_ws_action_aggregation.py` — `[ws_action_summary]` 1행 / 다중 tr_id 분리 / SUBSCRIBE SUCCESS ACK 흡수 / OPSP0002 ALREADY 흡수 Q2-D 옵션 A / disconnect 직전 flush Q5 옵션 A) + **G-WS4** (`[ws_subscribe_reject]` logger.error individual 영속 사이클 73 R-2) + **G-ERR1~ERR5** (5 `tests/unit/realtime/test_cycle74_error_preservation_matrix.py` — 사이클 16 `[aes_key_skip]` / 24 `[silent_inactive_force_reconnect]` / 42 `[ws_heartbeat]` heartbeat timeout / 73 R-1 `[ws_reverify]` WARNING / 73 R-2 `[ws_subscribe_reject]` ERROR individual 영속) + **G-7/G-8-A/G-8-B AST** (3). **ERROR 보존 매트릭스 7 prefix individual 영속 (사이클 29 005935 LMS chain 진단 의무)**: `[ws_subscribe_reject]` logger.error (사이클 73 R-2 `websocket.py:579`) + `[silent_inactive_force_reconnect]` (사이클 24/29-R2 `stale_session_recovery.py:158/185/195`) + `[ws_heartbeat]` heartbeat timeout (사이클 42 `websocket.py:275`) + `[aes_key_skip]` (사이클 16 `websocket.py:624`) + `[ws_reverify]` WARNING (사이클 73 R-1 `websocket.py:450/457/469`) + `[stale_force_retry_cap]` (사이클 66 K-10 `stale_watcher_core.py:214`) + `[stale_priority_resubscribe_cap_exceeded]` (사이클 66 `stale_watcher_core.py:373`). **기존 테스트 갱신 3건**: `test_scheduler_stale_logging_format.py` (B3-1 직접 emit 0건 검증 -8/+10) + `test_websocket_ack.py` (ACK 직접 INSERT 0건 + collector 흡수 -5/+13) + `test_websocket_subscribe_rejection.py` (ALREADY INFO 직접 0건 + collector 흡수 -4/+10). **백엔드 2043 → 2066 PASS + 1 XFAIL + 2 skip** (+23 신규). 카테고리 분리 측정 (silent 결함 0): cycle74 23 PASS / `tests/unit/realtime/` 171 PASS (collector + lifecycle 영향) / `tests/unit/engine/` 1183 PASS + 1 XFAIL (swing/stale aggregation 영향) / `tests/unit/ast/` 6 PASS (사이클 72/73/74 G-6/G-6.R/G-6.S/G-7/G-8) / `tests/unit/engine/strategies/` + scheduler + order_engine + risk + scanner 372 PASS (운영 매매 hot path 영속) / 전체 2066 PASS + 1 XFAIL + 2 skip in 50.83s. **flakiness 0** (3 회 cycle74 0.18/0.14/0.14s ±0.04s + unit/ 46.14/43.75/44.29s ±2.4s). 회귀 0 / 매매 안전성 무영향 (로깅 영역 시정 + 운영 prefix 발화 logger.* 경로 영속 = `_DbLogHandler` `src/main.py:172-174` root_logger 부착 영속 → stdout(85) + file(99) + DB(174) 3중 발화 보장 V-4 영속). **사이클 17 OPSP0002 backoff 영속** (`_opsp_backoff_until` dict `websocket.py:122` 인스턴스 변수 + `subscribe()` L352 진입 검사 + L567 `time.time() + 300.0` 등록 — collector 흡수만 추가 `L570 self._record_action(tr_id, tr_key, "OPSP_ALREADY")`). **사이클 38 명문화 (tradable_boards 매수 진입 전용) / 55 R-1 SellRejectionTracker / 66 priority 분리 + K-10 WARNING / 67 stale_manager facade + 4 sub-module / 68 KST `src/db/_kst.py` / 72 `_DbLogHandler` dedupe + G-6 / 73 R-1/R-2/S-1 + G-6.R/G-6.S 모두 무영향**. **CLAUDE.md "절대 깨지 말 것" 8 영역 영속** (체결통보 H0STCNI0/H0STCNI9 / uvicorn 단일 워커 / 주문번호 매핑 + race 가드 / `_reset_daily_state` / 익일 청산 30s 안정화 + 사이클 38 명문화 / NXT 매도 거부 좀비 차단 + 사이클 55 R-1 / **WebSocket 4중 안전망 (F1/_scan_loop/K stale watcher/_resubscribe_stale_priority)** 호출 시점/순서 무변경 + ERROR/거절/promote/drop individual 보존 / KIS 거부 응답 `[kis_rejection]` + KST 강제). flush 주기 5분 (사이클 42 `HEARTBEAT_METRICS_INTERVAL_SECS=300` 답습, Q4 옵션 A 영속). 헬퍼 추출 = 영역별 격리 (Q3 옵션 A 채택, 사이클 76+ shared 헬퍼 추출 #20 인계 = 3회 반복 추상화 규칙). **silent 결함 영구 차단 10 회 누적** (사이클 60/64/65#1/65#2/65#3/66/67/68/72/73) — 사이클 74 = 신규 결함 시정 아니라 합법적 반복 흡수 영역. **운영 효과 예상 (push 후 V-9 SQL 측정)**: `[swing_rest_poll]` ~720/day → 144/day (~80% 감소) + `[stale_watcher]` ~360/day → 144/day + WS 구독/ACK/해제 ~4,000/day → ~10/day (~95% 감소) + dup_factor 2.11 → **1.0~1.2**. **월요일 (2026-06-09) 09:00~10:00 1h tester verify 시나리오 통합 (A/B/C/D/E)**: 시나리오 A (사이클 67 자연 monitoring) + B (사이클 67 인위 stale 보유 0) + C (사이클 67 KIS LMS chain 차단 — `_opsp_backoff_until` / `[ws_subscribe_reject]` 폭증 0건) + D (사이클 66 HIGH 인위 stale 보유 0 — HIGH 보장률 100% + WARNING 발화 빈도 + 회복 시간 ≤5분) + **E (사이클 74 신규)** — `[ws_action_summary]` / `[swing_rest_poll_summary]` / `[stale_watcher_summary]` 5분 주기 1행 emit 운영 실측 + V-9 SQL (`dup_factor → 1.0~1.2`) 측정. **사이클 75+ 후속 카드 인계**: **#19 (HIGH)** `realtime_other` 16.13x dup 영역 식별 + 시정 (사이클 73 인계, 사이클 74 운영 1주 dup 측정 후 발의) + **#20 (MEDIUM)** collector 헬퍼 추출 `src/realtime/_log_aggregator.py` — `[ws_heartbeat]` + `[ws_action_summary]` + `[swing_rest_poll_summary]` + `[stale_watcher_summary]` 4 영역 DRY (사이클 74 운영 1주 + 3회 반복 추상화 규칙 충족 후 사이클 76+) + **#21 (MEDIUM)** `[ws_opsp_backoff_state]` 5분 주기 emit (OPSP0002 aggregation 흡수 후 backoff dict 운영 가시화 보강) + **#22 (LOW)** `[ws_restore_after_reconnect] reconnect_count=N restored=K` 1행 emit (재연결 chain 진단) + **auth 1.43x dup** 별개 카드 (사이클 73 인계, 메시지 반복 패턴 분석). 사이클 49→74 누적 26 사이클 + hotfix 5 영역. | `src/realtime/websocket.py` (+95L, `_ws_action_collector` 인스턴스 변수 + `_record_action` + `_flush_ws_action_collector` + `_ws_action_metrics_loop` 5분 주기 task + `connect()` 진입 task 시작 + `disconnect()` cancel 전 마지막 flush + `_send_subscribe` / SUBSCRIBE SUCCESS / OPSP0002 ALREADY 직접 logger.info 제거 + collector 흡수) / `src/engine/stale_watcher_core.py` (+51L, `record_stale_watcher_check` + `flush_stale_watcher_collector` 모듈 전역 + `check_and_resubscribe_stale` `[stale_watcher]` INFO collector 흡수) / `src/engine/scheduler.py` (+42L, `record_swing_rest_poll` + `flush_swing_rest_poll_collector` 모듈 전역 + `_run_swing_rest_poll_once` `[swing_rest_poll]` INFO collector 흡수) / `tests/unit/engine/test_cycle74_swing_rest_poll_aggregation.py` 신규 (G-SP1~SP4, 4 케이스 freezegun) / `tests/unit/engine/test_cycle74_stale_watcher_aggregation.py` 신규 (G-SW1~SW5, 5 케이스) / `tests/unit/realtime/test_cycle74_ws_action_aggregation.py` 신규 (G-WS1~WS6, 6 케이스) / `tests/unit/realtime/test_cycle74_error_preservation_matrix.py` 신규 (G-ERR1~ERR5, 5 케이스 사이클 16/24/42/73 R-1/R-2 영속) / `tests/unit/ast/test_cycle74_ast_aggregator_helper.py` 신규 (G-7/G-8-A/G-8-B, 3 케이스 AST AsyncFunctionDef multi-line 7줄 호환) / `tests/unit/engine/test_scheduler_stale_logging_format.py` (B3-1 직접 emit 0건 검증 -8/+10) / `tests/unit/realtime/test_websocket_ack.py` (ACK 직접 INSERT 0건 + collector 흡수 -5/+13) / `tests/unit/realtime/test_websocket_subscribe_rejection.py` (ALREADY INFO 직접 0건 + collector 흡수 -4/+10) / `_workspace/red/cycle74_sampling.md` Red 명세 영구 기록 / `_workspace/refactor/cycle74_sampling_advisory.md` refactor-expert 자문 메모 (옵션 E-1 + ERROR 보존 매트릭스 + 6 카드 + 5 결정 의제) 영구 기록 / `src/realtime/CLAUDE.md` PINGPONG 가시성 영역 다음에 옵션 E-1 명세 + lifecycle 추가 / `src/engine/CLAUDE.md` 사이클 67 본문 *위*에 사이클 74 swing/stale aggregation 명세 추가 / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 74 행 | 사이클 71/73 운영 실증 잔존 dup 영역 (합법적 반복 INSERT 60s/120s/5min 주기 + 합법적 logger.info 빈도) 흡수 = 사이클 42 `[ws_heartbeat]` 5분 통계 패턴 답습. team-leader Phase 1 분석 (영역별 일일 INSERT 정량 + 옵션 평가 A/B/C/D/E + 영역별 권고 매트릭스) + refactor-expert 자문 (옵션 E-1 채택 / E-2/E-3 비채택 + ERROR 보존 매트릭스 7 prefix individual 영속 + collector 헬퍼 격리 Q3 옵션 A + flush 5분 주기 Q4 옵션 A + disconnect 직전 마지막 flush Q5 옵션 A) + 사용자 결정 R3 + 2-A/B/C C+E + X + Q2-D/E/Q3/Q4/Q5 모두 옵션 A 채택 = **13 사이클 연속 옵션 A 패턴 영속** (사이클 55 R-1 / 60 / 62 / 63 / 64 / 65 / 66 / 67 / 68 / 69 / 72 / 73 / 74). 사이클 29 005935 LMS chain 진단 의무 = ERROR/WARNING individual 보존 매트릭스 영구 가드 G-ERR1~ERR5 + G-SW3/SW4 + G-WS4 영속. 매매 안전성 무영향 (로깅 영역 + collector 흡수만 + 운영 prefix logger.* 경로 영속 stdout/file/DB 3중 발화 보장 = `_DbLogHandler` root_logger 부착 영속). 사이클 17 OPSP0002 backoff dict 등록 행위 무변경 (collector 흡수만 추가). 사이클 38 명문화 / 55 R-1 / 66 priority / 67 분해 / 68 KST / 72 dedupe / 73 R-1/R-2/S-1 모두 무영향. **운영 효과 예상 (push 후 V-9 SQL 측정 의무)**: 일일 INSERT 누적 ~8,600/day → ~3,500/day (~60% 감소) + dup_factor 2.11 → 1.0~1.2 + Supabase 무료 tier 500MB 한계 시점 약 8개월 → ~20개월 연장. **월요일 (2026-06-09) 09:00~10:00 1h tester verify 시나리오 A/B/C/D/E 결합 측정** (사이클 66 D + 사이클 67 A/B/C + 사이클 74 신규 E `[ws_action_summary]` / `[swing_rest_poll_summary]` / `[stale_watcher_summary]` 5분 주기 1행 emit 운영 실측 + V-9 SQL `dup_factor → 1.0~1.2` 측정). 사이클 49→74 누적 26 사이클 + hotfix 5 영역. 후속 카드 사이클 75+ 인계: **#19 (HIGH)** `realtime_other` 16.13x dup 영역 + **#20 (MEDIUM)** collector 헬퍼 추출 `src/realtime/_log_aggregator.py` (3회 반복 추상화 규칙 사이클 76+) + **#21 (MEDIUM)** `[ws_opsp_backoff_state]` 5분 emit + **#22 (LOW)** `[ws_restore_after_reconnect]` 1행 emit + **auth 1.43x** 별개 카드 + #16 (MEDIUM Q6-3 2026-06-13 이후 후보 풀 폭축 회고) + #15 (LOW 액면분할 prdy_clpr invalidate) + #11/#12 + Q6-2 옵션 B 헬퍼 (사이클 71+) + D2/D3 (Q2/Q3 사이클 70+) + #20 (LOW RLS 활성 후보) + #21 (LOW 사이클 72/73/74 운영 배포 후 1주 dup_factor 추이 모니터링) 영속. |
| 2026-06-08 | 사이클 73 — 카드 #19 (HIGH) **WebSocket dup 핵심 영역 시정** (사이클 72 hotfix F-2 인계, **12 사이클 연속 옵션 A 패턴 영속**). team-leader Phase 1 분석 후 **옵션 C (옵션 A' + G-6 AST 가드 영역 확장) + 결정 2=G-6.전체 (src/realtime/ + scheduler.py::_run_swing_rest_poll_once 영역 한정) + 결정 3=auth 별개 카드 사이클 74+ 인계** 사용자 채택. **사이클 72 운영 효과 측정 (push 후 30분, Supabase MCP READ-ONLY)**: 배포 전 (09:00~10:24 KST) dup_factor = 3.72x (2,261 INSERT / 608 distinct) → 배포 후 (10:24~10:27 KST 3분) **1.34x** (217 INSERT / 162 distinct) = **64% 감소**. 사이클 71 결함 11+ 사이트 (`[stale_watcher_detail]` / `[stale_force_retry]` / `[ws_heartbeat]` / `[universe_excluded]` / `[priority_drop]` / `[cash_usage_ratio]` / `[market_regime]` / `[stock_master_eager]` 등) **전수 1.00x 회복**. 잔존 1.34 = F-2 핵심 = `[swing_rest_poll]` 5.00x (10/2) + `realtime` 2.40x (113/47) + `auth` 1.43x (10/7). **결함 사이트 3건 확정 (Phase 1-A grep + Read)**: **R-1** `src/realtime/websocket.py:380-393` `[ws_reverify]` 결함 = `logger.warning` (L380) + `await write_log("WARNING", "[ws_reverify] reconnect_count=N stale=M/T preview=[..]")` (L386) 양쪽 INSERT (정상 흐름 dup). **R-2** `src/realtime/websocket.py:512-529` `[ws_subscribe_reject]` 결함 = `logger.error("WebSocket 구독 거절: tr_id=%s, ...")` (L512) + `await write_log("ERROR", "[ws_subscribe_reject] tr_id=... ticker=... msg_cd=... msg1=...")` (L523) 양쪽 INSERT (거절 응답 dup). 결정적 **logger.error 메시지에 prefix `[ws_subscribe_reject]` 명시 부재** = G-6.R AST 가드 검출 누락 위험 발견 (Red 단계 tdd-engineer 보고). **S-1** `src/engine/scheduler.py:2319-2333` `[swing_rest_poll]` 결함 = `logger.info("[swing_rest_poll] candidates=%d ...")` (L2319) + `await write_log("INFO", f"[swing_rest_poll] candidates={...} ...")` (L2325) 양쪽 INSERT = 60s 주기 × 5 사이클 × 2 INSERT = 10/2 distinct = **5.00x 정확 일치 root cause 확정**. **auth 1.43x = `src/auth/`** `grep "write_log"` 결과 **0건** = write_log dup 결함 아님 (`auth/token.py` 는 logger 단독 사용 L137/160/181/196/233/261/270/275/369 9 사이트, 토큰 발급 동시 sub 라벨별 호출 추정 메시지 반복 패턴) → **사이클 73 영역 제외**. **시정 (옵션 C 옵션 A' 답습)**: R-1 `_verify_subscriptions_after_reconnect` 내 `await write_log` + try/except 블록 9L 통째 제거 + `logger.warning` (L380, `[ws_reverify]` prefix 명시) 보존 → `_DbLogHandler` (`src/main.py:137-170`) 위임 단일 INSERT. R-2 `_handle_raw` 내 `await write_log` + try/except 블록 10L 통째 제거 + `logger.error` 메시지에 prefix `[ws_subscribe_reject]` **신규 추가** (운영 가시화 보존 + G-6.R AST 가드 검출 영속 보장) → `_DbLogHandler` 위임 단일 INSERT. S-1 `_run_swing_rest_poll_once` 내 `await write_log` + try/except 블록 10L 통째 제거 → 주석 1줄 (`# _DbLogHandler 위임...`) 으로 교체 + `logger.info` (L2319) 보존 → `_DbLogHandler` 위임 단일 INSERT. **AST 영구 가드 2 신규 (G-6 영역 확장)**: **G-6.R** (`tests/unit/ast/test_cycle73_ast_realtime_no_logger_write_log_pair.py`, `src/realtime/` 디렉토리 전수 rglob 각 `write_log` 호출 ±10 줄 동시 `logger.*` 호출 0건 정적 검증 — 사이클 72 G-6 ±5 줄 윈도우 한계에서 multi-line logger 7줄 흡수 보강) + **G-6.S** (`tests/unit/ast/test_cycle73_ast_swing_rest_poll_no_logger_write_log_pair.py`, `scheduler.py::_run_swing_rest_poll_once` 함수 영역 한정 — 전체 scheduler.py 는 정상 logger 단독 사이트 다수 존재로 영역 한정 권고 채택). 본 사이클 시정 후 0건 보장 + 미래 신규 사이트 silent 결함 영구 차단. **회귀 가드 7 케이스 (6 파일)**: **G-RT1** (R-1 사이트 `[ws_reverify]` write_log 호출 0건 — `src/realtime/websocket.py` 본체 grep) + **G-RT2** (R-2 사이트 `[ws_subscribe_reject]` write_log 호출 0건) + **G-SCH1** (S-1 사이트 `[swing_rest_poll]` write_log 호출 0건 — `src/engine/scheduler.py::_run_swing_rest_poll_once` 영역 grep) + **G-6.R AST** (src/realtime/ rglob ±10 줄 multi-line 7줄 흡수) + **G-6.S AST** (scheduler.py::_run_swing_rest_poll_once 영역 한정) + **G-RT3-A** (R-1 `logger.warning` 호출 영속 = 운영 가시화 stdout/file/DB 3중 발화 보존) + **G-RT3-B** (R-2 `logger.error` 호출 영속). **기존 회귀 가드 27 케이스 갱신** (`tests/unit/realtime/test_websocket_subscribe_rejection.py` + `tests/unit/realtime/test_websocket_reverify.py` Case B) — `write_log` patch 제거 + `logger.error` prefix `[ws_subscribe_reject]` 반영. **사이클 72 영속** (G-72.E `tests/unit/ast/test_cycle72_ast_no_logger_write_log_pair.py` PASS 영속 = 사이클 73 시정이 사이클 72 영역 회귀 0). **백엔드 2036 → 2043 PASS + 2 skip + 1 XFAIL** (+7 신규 G-RT1/G-RT2/G-SCH1/G-6.R/G-6.S/G-RT3-A/G-RT3-B). **카테고리 분리 측정** (사이클 60~72 답습): cycle73 7 PASS / realtime/ 160 PASS / engine/ 1174 PASS + 1 XFAIL / ast/ 3 PASS (G-6 + G-6.R + G-6.S) / 전체 2043 PASS + 2 skip + 1 XFAIL. **flakiness 0** (3 회 cycle73 0.04s + unit/ 44.9s ±2.1s). 회귀 0 / 매매 안전성 무영향 (로깅 영역 시정 + 운영 prefix 발화 logger 경로 영속 = `_DbLogHandler` `src/main.py:172-174` root_logger 부착 영속 → stdout(85) + file(99) + DB(174) 3중 발화 보장 V-4 영속 확인). **사이클 65 H2/H2-bis 영속 (변경 0)**: `system_logs.py:44` + `main.py:131` 인라인 KST 패턴 유지. **사이클 67 분해 영향 0** (stale_manager facade + 4 sub-module + scheduler.py wrapper 모두 변경 0). **사이클 68 KST 일관성 영속** (8 DB + 9 engine/api + `_kst.py`). **사이클 72 dedupe 영속** (`_DbLogHandler` 500ms TTL 캐시 `src/main.py:137-170` 변경 0). **CLAUDE.md "절대 깨지 말 것" 8 영역 영속**: 체결통보 H0STCNI0/H0STCNI9 / uvicorn 단일 워커 / 주문번호 매핑 / race 가드 / `_reset_daily_state` / NXT 좀비 차단 / **WebSocket 4중 안전망 (F1 + scan_loop + K + resubscribe)** R-1 `_verify_subscriptions_after_reconnect` `logger.warning` 호출 시점/횟수 변경 0 + R-2 `_subscriptions.discard` / `_subscriptions_acked.discard` 변경 0 / KIS 거부 응답 `[kis_rejection]` 영속. **silent 결함 영구 차단 10 회 누적** (사이클 60 mock_get_balance + pytest-timeout / 사이클 64 scheduler emit / 사이클 65 #1 system_logs KST + retry / 사이클 65 #2 e2e api-mocks / 사이클 65 #3 e2e toBeVisible / 사이클 66 cap=10 priority + AST / 사이클 67 facade patch G-16 + priority cap G-17 / 사이클 68 hotfix production default_factory + 12 fixture + AST / 사이클 72 _DbLogHandler dedupe + 22+ 사이트 write_log 제거 + AST G-6/G-7/G-8 / **사이클 73 R-1/R-2/S-1 3 사이트 + AST G-6.R/G-6.S 영역 확장**). 변경 통계 production -29L + 신규 회귀 가드 6 파일 + AST 가드 2. **운영 효과 예상 (push 후 V-8 SQL 측정 의무)**: dup_factor 1.34 → **1.0~1.2** (`[swing_rest_poll]` 5.00x → 1.00x + `realtime` 2.40x → ≤2.0x, R-1/R-2 dup 제거 효과). 잔존 dup 영역 = WebSocket ACK/구독/해제 logger.info 빈도 자체 결함 (구독 100건 시 logger.info 100회 + DB 100건 INSERT → 운영 부담 측정 후 sampling/aggregation 패턴 도입 사이클 74+ 카드) + auth 1.43x (메시지 반복 패턴 분석 사이클 74+ 별개 카드). **사이클 74+ 후속 카드 인계**: 1) `src/auth/` 메시지 반복 패턴 분석 (1.43x dup 별개 카드) + 2) WebSocket ACK/구독/해제 logger.info 빈도 자체 결함 (sampling/aggregation/rate cap 패턴 도입) + 3) **stale_manager.py 1,099L = 사이클 67 종결, 카드 #14 종결 (재발의 불요)** 영속. | `src/realtime/websocket.py` (-19L 합, R-1 `_verify_subscriptions_after_reconnect` write_log 제거 -9L + R-2 `_handle_raw` write_log 제거 -10L + `logger.error` 메시지 prefix `[ws_subscribe_reject]` 신규 추가) / `src/engine/scheduler.py` (-10L, S-1 `_run_swing_rest_poll_once` write_log 제거 + 주석 1줄 교체) / `tests/unit/realtime/test_cycle73_ws_reverify_no_write_log.py` 신규 (G-RT1, 1 케이스) / `tests/unit/realtime/test_cycle73_ws_subscribe_reject_no_write_log.py` 신규 (G-RT2, 1 케이스) / `tests/unit/realtime/test_cycle73_logger_persistence.py` 신규 (G-RT3-A/G-RT3-B, 2 케이스) / `tests/unit/engine/test_cycle73_swing_rest_poll_no_write_log.py` 신규 (G-SCH1, 1 케이스) / `tests/unit/ast/test_cycle73_ast_realtime_no_logger_write_log_pair.py` 신규 (G-6.R AST, 1 케이스, ±10 줄 + multi-line 7줄 흡수) / `tests/unit/ast/test_cycle73_ast_swing_rest_poll_no_logger_write_log_pair.py` 신규 (G-6.S AST, 1 케이스, 함수 영역 한정) / `tests/unit/realtime/test_websocket_subscribe_rejection.py` (27 케이스 갱신, write_log patch 제거 + prefix 반영) / `tests/unit/realtime/test_websocket_reverify.py` Case B 갱신 / `_workspace/red/cycle73_ws_dup_fix.md` Red 명세 영구 기록 / `src/realtime/CLAUDE.md` E2/F1 본문 시정 명세 명시 (`logger.error("[ws_subscribe_reject] ...")` + `logger.warning("[ws_reverify] ...")`) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 73 행 | 사이클 72 hotfix F-2 인계 카드 #19 (HIGH) **완전 종결** — WebSocket dup 핵심 영역 R-1/R-2/S-1 3 사이트 시정 + AST G-6 영역 확장. team-leader Phase 1-A 확정 (`[swing_rest_poll]` 5.00x root cause = `logger.info` + `await write_log` 양쪽 INSERT 60s 주기 × 5 사이클 × 2 INSERT = 10/2 정확 일치 + R-1/R-2 정상 흐름 dup + 거절 응답 dup 패턴). 사이클 72 패턴 100% 답습 (옵션 A' 호출자 정정 22+ 사이트 + AST G-6) = 12 사이클 연속 옵션 A 패턴 영속 (사이클 55 R-1 / 60 / 62 / 63 / 64 / 65 / 66 / 67 / 68 / 69 / 72 / 73). silent 결함 영구 차단 10 회 누적 = silent 결함 가시화 → AST 영구 차단 영구 패턴 (사이클 60~73). R-2 `logger.error` prefix `[ws_subscribe_reject]` 신규 추가 = 운영 가시화 보강 + G-6.R AST 가드 검출 영속 보장 (Red 단계 tdd-engineer 발견 정직 보고). AST G-6.R 정밀 검출 패턴 (±10 줄 + multi-line logger 7줄 흡수) = 사이클 72 G-6 ±5 줄 윈도우 한계 보강. 매매 안전성 무영향 (로깅 영역 시정 + 운영 prefix 발화 logger 경로 영속 = stdout/file/DB 3중 발화 보장 = `_DbLogHandler` `src/main.py:172-174` root_logger 부착 영속). 사이클 65 H2/H2-bis (변경 0) + 사이클 67 stale_manager 분해 (변경 0) + 사이클 68 KST (변경 0) + 사이클 72 dedupe 캐시 (변경 0) 모두 무영향. CLAUDE.md "절대 깨지 말 것" 8 영역 영속 확인 (WebSocket 4중 안전망 R-1 호출 시점/횟수 변경 0 + R-2 `_subscriptions.discard` 변경 0 + KIS LMS chain 차단 영속). **운영 효과 예상 (push 후 V-8 SQL 측정)**: dup_factor 1.34 → 1.0~1.2 (`[swing_rest_poll]` 5.00x → 1.00x + `realtime` 2.40x → ≤2.0x). 사이클 74+ 후속 카드 = `auth` 1.43x 메시지 반복 패턴 분석 (write_log dup 결함 아님 = 별개 영역) + WebSocket ACK/구독/해제 logger.info 빈도 자체 결함 (구독 100건 시 logger.info 100회 + DB 100건 INSERT → sampling/aggregation/rate cap 패턴 도입 카드 발의 권고). 사이클 49→73 누적 25 사이클 + hotfix 5 영역. 후속 카드 #16 (MEDIUM Q6-3 후보 풀 폭축 회고, 2026-06-13 이후 1주 운영 후) / #15 (LOW Q7-2 액면분할 prdy_clpr invalidate) / #11/#12 (보드 mutex / 14 모듈 logger) / Q6-2 옵션 B 헬퍼 (사이클 71+) / D2/D3 (Q2/Q3 사이클 70+) / #20 (LOW RLS 활성 후보) / #21 (LOW 사이클 72/73 운영 배포 후 1주 dup_factor 추이 모니터링) / 신규 사이클 74+ (auth dup 별개 카드 + WS logger.info 빈도 sampling/aggregation 카드) 영속. |
| 2026-06-08 | 사이클 72 hotfix — system_logs **이중 INSERT 시정** (사이클 71 운영 실증 발견 silent 결함 #1, HIGH 즉시 시정 — 사용자 결정 09:25 KST 매매 미발생 영역 안전). team-leader Phase 1 분석 후 **옵션 A' + D + AST G-6/G-7/G-8 채택** = **11 사이클 연속 옵션 A 패턴 영속** (사이클 55 R-1 / 60 / 62 / 63 / 64 / 65 / 66 / 67 / 68 / 69 / 72). **결함 (사이클 71 운영 실증)**: 09:00~09:25 system_logs INSERT 205건 / distinct 71 = **평균 2.89x dup** (`[swing_poll]` 23x 최악 / `[stale_watcher]` 7.33x / `[ws_heartbeat]` 2.12x / `[stale_watcher_detail]` 확정적 2.00x / `[stale_force_retry]` 2.00x). **Root cause 확정 (Phase 1-A)**: `src/main.py::_DbLogHandler` (`src.*` 모든 logger emit INSERT 위임) + 같은 emit 사이트가 `await write_log(...)` 양쪽 호출 → 이중 INSERT (delta ≈ 0.188초). 사이클 70 시뮬레이션 (mock 환경) 미검출 → 운영 실증 첫 노출 (silent 결함 9 회 누적 영역). **운영 영향**: system_logs 테이블 2.89x 팽창 → Supabase 무료 tier 용량 한계 가속화 + `daily_log_reports` (사이클 58 V-2 token/cost) 입력 raw 중복 → 분석 왜곡 가능. **시정 (옵션 A' 호출자 정정 11+ 사이트)**: Phase 1-A 식별 11 사이트 (`realtime/websocket.py` `[ws_heartbeat]` + `engine/stale_watcher_core.py` 4 사이트 L181-188/L212-224/L251-263/L384-397 `[stale_watcher]` / `[stale_force_retry]` / `[stale_force_retry_cap]` / `[stale_priority_resubscribe]` + `engine/stale_diagnostics.py` L178-191 `[stale_watcher_detail]` + `engine/stale_session_recovery.py` 3 사이트 L163-203/L267-273 `[silent_inactive_recovery_cap]` / `[silent_inactive_force_reconnect]` / `[scan_loop_delta]` + `engine/stale_universe_guard.py` L146-155 `[universe_excluded]` + `engine/scanner.py` L1152-1155 `[priority_drop]`) + **G-6 AST 추가 검출 11 사이트** (`engine/boot_manager.py:68` `[cash_usage_ratio]` + `engine/scheduler.py` 6 사이트 L735 `매매 시스템 종료` / L842 `매매 시스템 수동 중지` / L1730 `[market_regime]` / L1825,L1834 `[stock_master_eager]` x2 / L2657 `[funnel_snapshot]` + Phase 1-A 중첩 4) 의 `await write_log(...)` / `await _write_log(...)` / `asyncio.create_task(_write_log(...))` 호출 전수 제거. `logger.info/warning(...)` 단독 유지 → `_DbLogHandler` 위임 단일 INSERT 보장. **`safe_write_log` 영속** (`order_engine.py` 4 사이트 graceful 변형 영역 — 사이클 56-E, 별개 영역). **시정 (옵션 D `_DbLogHandler` 500ms TTL dedupe 캐시 안전망, `src/main.py:137-170`)**: `_DEDUPE_TTL_SECS = 0.5` 모듈 상수 + `_dedupe_cache: dict[str, float]` 인스턴스 변수 (message → last_emit_monotonic) + `emit()` 진입 직후 `time.monotonic()` 비교 → 동일 메시지 500ms 내 중복 emit 두 번째 INSERT skip + lazy evict (100 항목 cap, TTL 경과 항목 정리). 미래 신규 emit 사이트 silent 결함 runtime 안전망 (AST 정적 가드로 검출 불가능 영역). **AST 영구 가드 3 신설**: G-6 (`tests/unit/ast/test_cycle72_ast_no_logger_write_log_pair.py`, `src/` 전체 rglob 각 `write_log` 호출 ±5 줄 동시 `logger.*` 호출 0건 — 시정 후 0건 보장 + 미래 신규 사이트 silent 결함 차단) + G-7 (`tests/unit/main/test_cycle72_db_log_dedupe_structure_ast.py`, `_DEDUPE_TTL_SECS = 0.5` 상수 + `_dedupe_cache` dict + `time.monotonic` 비교 정적 검증) + G-8 (`tests/unit/main/test_cycle72_insert_log_kst_persistence_ast.py` 2 케이스, 사이클 65 H2-bis KST 영속 회귀 가드 보강 — `_insert_log_to_db` 본체 `datetime.now(KST).isoformat()` 변경 0 + `+09:00` suffix 영속). **회귀 가드 18 케이스 (6 파일)**: G-A1~G-A11 사이트별 정적 검증 (11 케이스, `tests/unit/engine/test_cycle72_no_duplicate_insert_per_site.py`) + G-D1 (100ms 내 dedupe 차단) + G-D2 (600ms TTL 경과 후 정상 INSERT) + G-D3 (다른 메시지 동시 emit 모두 INSERT, 메시지 키 dedupe 정확) freezegun + mock supabase (3 케이스, `tests/unit/main/test_cycle72_db_log_dedupe.py`) + AST G-6/G-7/G-8 (4 케이스). **F-1 통합 테스트 2건 시정 (옵션 C 채택)**: `tests/integration/test_boot_cash_usage_ratio.py:183-190` `[cash_usage_ratio]` obsolete `boot_env.calls.write_log` assertion 제거 + `tests/integration/test_boot_eager_refresh.py:188-195` `[stock_master_eager]` obsolete `scheduler_env.calls.write_log` assertion 제거 → 사이클 72 의도 일치 (G-A 단위 가드로 회귀 차단 영속, fixture 추적 영역이 obsolete). **기존 회귀 가드 caplog 마이그레이션 5 파일 (Green 단계 동행)**: `test_scheduler_stale_force_retry.py` F-7 caplog 전환 + `test_scan_loop_stale_priority.py` write_log patch 제거 + caplog 전환 (Case A/C) + `test_scanner_priority_order.py` Case 3/6 caplog 전환 + `test_scheduler_pool_stop_on_shutdown.py` B-2 caplog 전환 + B-4 write_log patch 재추가 + `test_scheduler_finally_main_disconnect.py` E-3 write_log patch 재추가. **백엔드 2018 → 2036 PASS + 2 skip + 1 XFAIL** (+18 신규) / `tests/unit/engine/test_cycle72_*.py` + `tests/unit/main/test_cycle72_*.py` + `tests/unit/ast/test_cycle72_*.py` 18 PASS / 카테고리 분리 측정 (사이클 60~71 답습): db/ 166 + stale_manager/ 27 + main/+ast/ 7 + engine/ 1173 + 1 XFAIL + integration/ 162 + 0 FAIL / **flakiness 0** (3 회 cycle72 0.21~0.30s + unit/ 41.33~49s ±0.5%) / 회귀 0 / 매매 안전성 무영향 (로깅 영역 시정 + 운영 prefix 발화 logger 경로 영속). **사이클 65 H2/H2-bis 영속 (변경 0 확인)**: `system_logs.py:44` + `main.py:131` 인라인 `datetime.now(KST).isoformat()` 유지 — G-8 회귀 가드 PASS 영속. **사이클 67 stale_manager 분해 영향 0** (4 sub-module + facade write_log 호출만 제거, re-export 시그너처 무영향 — 27 PASS 영속). **사이클 68 KST 일관성 영속** (8 DB + 9 engine/api + `_kst.py` 67 케이스 PASS). **CLAUDE.md "절대 깨지 말 것" 8 영역 영속** (체결통보 H0STCNI0/H0STCNI9 / uvicorn 단일 워커 / WebSocket 4중 안전망 / 사이클 38 명문화 / 사이클 55 R-1 / 사이클 66 priority / 사이클 67 분해 / 사이클 68 KST). **silent 결함 영구 차단 9 회 누적** (사이클 60 mock_get_balance + pytest-timeout / 사이클 64 scheduler emit / 사이클 65 #1 system_logs KST + retry / 사이클 65 #2 e2e api-mocks / 사이클 65 #3 e2e toBeVisible / 사이클 66 cap=10 priority + AST / 사이클 67 facade patch G-16 + priority cap G-17 / 사이클 68 hotfix production default_factory + 12 fixture + AST / **사이클 72 _DbLogHandler dedupe + 22+ 사이트 write_log 제거 + AST G-6/G-7/G-8**). 변경 통계 production -65L / +30L 순감 -35L + 신규 회귀 가드 6 파일 + AST 가드 3. **F-2 (HIGH 운영 영향) WebSocket dup 핵심 영역 사이클 73 카드 #19 인계 (즉시)**: 운영 측정 dup 상위 4건 = `src.realtime.websocket` (ACK 312건 / 구독 309건 / 해제 286건 / 5xx 86건) + `[swing_rest_poll]` 43+43=86건 이중 INSERT 패턴 현재진행형 — 사이클 72 G-6 시정 범위 *밖* (websocket.py L386 `[ws_reverify]` + L523 `[ws_subscribe_reject]` + `[ws_manual_resubscribe]` 등 추가 사이트 식별 의무). 운영 배포 후 dup_factor 1.5~2.0 잔존 가능성. 카드 #19 = G-6 AST 가드 영역 확장 (src/realtime/ 영역). **카드 #20 (MEDIUM)**: `test_next_day_clear_observability.py` 잠재 obsolete fixture 점검 (사이클 73+). **카드 #21 (LOW)**: 사이클 72 운영 배포 후 1주 dup_factor 추이 모니터링 (Supabase READ-ONLY). | `src/main.py` (+30 lines, `_DbLogHandler` 500ms TTL dedupe 캐시 신규 + `_DEDUPE_TTL_SECS = 0.5` + `_dedupe_cache` dict + `time.monotonic` 비교 + lazy evict 100 cap) / `src/realtime/websocket.py` (-7 lines, `[ws_heartbeat]` write_log 제거 + logger.info 단독 유지) / `src/engine/stale_watcher_core.py` (-22 lines, 4 사이트 `[stale_watcher]` / `[stale_force_retry]` / `[stale_force_retry_cap]` / `[stale_priority_resubscribe]` write_log 제거) / `src/engine/stale_diagnostics.py` (-6 lines, `[stale_watcher_detail]` `asyncio.create_task(_write_log(...))` 제거) / `src/engine/stale_session_recovery.py` (-18 lines, 3 사이트 `[silent_inactive_*]` + `[scan_loop_delta]` 제거) / `src/engine/stale_universe_guard.py` (-9 lines, `[universe_excluded]` 제거) / `src/engine/scanner.py` (-3 lines + `[priority_drop]` `logger.info` → `logger.warning` 격상) / `src/engine/boot_manager.py` (-3 lines, `[cash_usage_ratio]` 제거) / `src/engine/scheduler.py` (-22 lines, 6 사이트 L735/L842/L1730/L1825/L1834/L2657 제거) / `tests/unit/engine/test_cycle72_no_duplicate_insert_per_site.py` 신규 (11 케이스 G-A1~G-A11 사이트별 정적 검증) / `tests/unit/main/test_cycle72_db_log_dedupe.py` 신규 (3 케이스 G-D1~G-D3 freezegun + mock) / `tests/unit/ast/test_cycle72_ast_no_logger_write_log_pair.py` 신규 (1 케이스 G-6 AST ±5 줄 pair 영구 가드) / `tests/unit/main/test_cycle72_db_log_dedupe_structure_ast.py` 신규 (1 케이스 G-7 dedupe 구조 정적) / `tests/unit/main/test_cycle72_insert_log_kst_persistence_ast.py` 신규 (2 케이스 G-8 사이클 65 H2-bis 영속 보강) / `tests/integration/test_boot_cash_usage_ratio.py` (-15 lines, F-1 obsolete assertion 제거 옵션 C) / `tests/integration/test_boot_eager_refresh.py` (-9 lines, F-1 obsolete assertion 제거 옵션 C) / `tests/unit/engine/test_scheduler_stale_force_retry.py` + `test_scan_loop_stale_priority.py` + `test_scanner_priority_order.py` + `test_scheduler_pool_stop_on_shutdown.py` + `test_scheduler_finally_main_disconnect.py` (기존 회귀 가드 caplog 마이그레이션 5 파일) / `_workspace/red/cycle72_dup_insert_fix.md` Red 명세 영구 기록 / `src/db/CLAUDE.md` 본문 사이클 72 hotfix 명세 추가 / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 72 hotfix 행 | 사이클 71 운영 hot path 실증 발견 silent 결함 (이중 INSERT 2.89x dup) 즉시 시정 — 사용자 결정 (매매 미발생 09:25 영역 안전 + 매매 hot path 무영향 = 즉시 시정 적합 평가). team-leader Phase 1 분석 후 옵션 A' (호출자 정정 22+ 사이트) + 옵션 D (`_DbLogHandler` runtime dedupe 안전망) + AST 영구 가드 3 결합 채택 = 11 사이클 연속 옵션 A 패턴 영속 (사이클 55 R-1 / 60 / 62 / 63 / 64 / 65 / 66 / 67 / 68 / 69 / 72). silent 결함 영구 차단 9 회 누적 = silent 결함 가시화 → AST 영구 차단 영구 패턴 (사이클 60/64/65#1/65#2/65#3/66/67/68/72). 매매 안전성 무영향 (로깅 영역 시정 + 운영 prefix 발화 logger 경로 영속 = stdout/file/DB 3중 발화 보장) + 사이클 65 H2/H2-bis 영속 (변경 0 검증) + 사이클 67 stale_manager 분해 무영향 (re-export 시그너처 무영향, 27 PASS 영속) + 사이클 68 KST 일관성 무영향 (67 케이스 PASS). F-1 통합 테스트 obsolete 옵션 C 제거 (사이클 72 의도 일치 + G-A 단위 가드로 회귀 차단 영속, fixture 추적 영역이 obsolete). F-2 (HIGH 운영 영향) WebSocket dup 핵심 영역 사이클 73 카드 #19 즉시 인계 — 운영 dup_factor 1.00 목표 + G-6 AST 가드 영역 확장 (src/realtime/) + `[swing_rest_poll]` 43+43=86건 현재진행형 패턴 영구 차단. 사이클 49→72 누적 24 사이클 + hotfix 5 영역. 후속 카드 #19 (HIGH 사이클 73 WebSocket dup) / #16 (MEDIUM Q6-3 후보 풀 폭축 회고, 1주 운영 후) / #15 (LOW Q7-2 액면분할 prdy_clpr invalidate) / #11/#12 (보드 mutex / 14 모듈 logger) / Q6-2 옵션 B 헬퍼 (사이클 71+) / D2/D3 (Q2/Q3 사이클 70+) / #20 (LOW RLS 활성 후보) / #21 (LOW 사이클 72 운영 배포 후 1주 dup_factor 추이 모니터링) 영속. |
| 2026-06-08 | 사이클 69 — 카드 #18 (LOW) **UTC 잔존 데이터 백필 migration 영구 폐기** + Q4 (TIMESTAMPTZ 사실 명문화) 채택 (Q2 사이클 68 자동 예약 인계, 사실 점검 결과 백필 불필요 확정). team-leader Supabase MCP `execute_sql` READ-ONLY 13 테이블 전수 점검 결과 = **UTC 잔존 결함 0건 확정**. **데이터 분포 점검**: `system_logs` 566,180 row = KST 07:00~23:00 거래시간 정상 집중 / `trade_history` 229 row = KST 08:00~16:00 장중 정상 집중 / `daily_log_reports` 20 row = 20:10 KST 정확 (cron 일치, UTC였다면 05:10 KST) / `parameter_recommendations` 109 row = 20:00 KST 정상 (66건) / `backtest_runs` 148 = 20:00 KST 정상 (124건) / `strategy_funnel_snapshots` 660 = 09:30 KST 정상 (258건) / `stock_master` 14 / `market_regime_snapshots` 2 / `daily_performance` 156 (DATE only, TZ 무관) / `system_config` 14 / `strategy_config` 6 / `positions` 0 / `kis_quote_accounts` 5. **운영 시간외 row** (KST 00:00~07:00) = 222/566,180 = **0.04%** = boot/cron 정상 운영. **Root cause 재해석 (사이클 68 가정 = 오해 확정)**: 모든 시각 컬럼이 PostgreSQL **TIMESTAMPTZ** (timestamp with time zone) → *내부 UTC 저장 + 표시 시 timezone 변환* 모델 → **instant 자체는 timezone 무관 정확**. DB DEFAULT `now()` (Supabase PG 서버 timezone = UTC) INSERT instant ≡ Python `datetime.now()` (컨테이너 `TZ=Asia/Seoul` naive datetime → PG TIMESTAMPTZ 가 UTC 가정) INSERT instant ≡ Python `datetime.now(KST)` (사이클 65 H2 / 사이클 68 17 모듈) aware datetime INSERT instant = **모두 동일한 절대 시점**. 사이클 65 hotfix H2/H2-bis + 사이클 68 17 모듈 시정 (`src/db/_kst.py` 헬퍼) = **표시 형식 (`+09:00` ISO) 일관화 + 호출자 정합성 보장** — *instant 정정이 아님*. 운영 시간외 row 0.04% = 정상 운영 (boot 07:50 + 19:50~20:10 cron + autosell 19:50). **Q4 채택 — TIMESTAMPTZ 사실 명문화 (HIGH 가치 / LOW 비용)**: `src/db/CLAUDE.md` §"TIMESTAMPTZ 사실 명문화 (사이클 69, 2026-06-08)" 신규 19줄 — "내부 저장 = UTC instant / 표시 시점 = 세션 timezone 변환 / instant 자체는 timezone 무관 절대 시점 / DB DEFAULT now() INSERT instant ≡ Python datetime.now() INSERT instant ≡ datetime.now(KST) INSERT instant = 모두 동일 시점 / 백필 migration 불필요 / 미래 'UTC 저장 = 결함' 가정 시 본 명문화 인용". `docs/HARNESS_CHANGELOG.md` 사이클 69 행 영구 기록 (진실의 원천). **Phase 1-B 영향 영역 식별 (Read 만)**: 조회 모듈 (`trade_history.get_trades_in_range` / `log_analysis_engine._fetch_logs_in_range` / `system_logs.get_logs`) = 사이클 53 B-4 + 사이클 68 시정으로 KST `+09:00` 명시 영속 → TIMESTAMPTZ 비교 timezone 정합성 100% 보장. **영향 0건**. **변경 영역 (코드 변경 0, 문서 명문화만)**: `src/db/CLAUDE.md` (+19 lines) + `docs/HARNESS_CHANGELOG.md` 사이클 69 행 + `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 69 행. **회귀 가드 0** (코드 변경 0). 백엔드 2018 PASS + 1 XFAIL + 2 skip 영속 (사이클 68 hotfix 결과). 매매 안전성 무영향 + WebSocket 4중 안전망 + 사이클 67 stale_manager 분해 (27 PASS) + 사이클 68 KST 일관성 (15 + 1 hotfix AST = 16 PASS) 모두 무영향. **사이클 68 영속 결함 (사이클 65 hotfix 패턴 답습)**: silent 결함 영구 차단 패턴 8 회 누적 (사이클 60/64/65#1/65#2/65#3/66/67/68) 유지. **Q2 (D2, LOW)** = 3 모듈 `datetime.now().strftime()` UI 표시용 (positions["time"] UI 표시, DB 시각 컬럼 무관, 서버 `TZ=Asia/Seoul` 하 무영향) → 사이클 70+ 별개 인계. **Q3 (D3, MEDIUM)** = 9 engine/api 모듈 인라인 `_KST = timezone(timedelta(hours=9))` 패턴 → `src/db/_kst.py` 헬퍼 통일 리팩토링 → refactor-expert 사이클 70+ 검토 인계 (코드 일관성 가치, 사이클 67 분해 후 누적 리팩토링 5회 임계 도달 시 자동). **Q5 (NEW, LOW) 신규 카드 #20 후보**: Supabase advisory 경고 = public 13 테이블 **RLS 비활성** 상태 — server-side only (FastAPI ↔ Supabase service role key) + anon key 미배포로 실제 위험 0이나 운영 안전성 보강 카드 (RLS 활성 + service-role-only 정책 도입) 검토 영역 등록. **9 사이클 연속 옵션 A 패턴 영속** (사이클 55 R-1 / 60 / 62 / 63 / 64 / 65 / 66 / 67 / 68 / 69). domain-expert 자문 불필요 확정 (매매 안전성 무관 + DB 영역 + 코드 변경 0). | `src/db/CLAUDE.md` (+19 lines, §"TIMESTAMPTZ 사실 명문화 (사이클 69, 2026-06-08)" 신규) / `docs/HARNESS_CHANGELOG.md` 사이클 69 행 영구 기록 / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 69 행 | 사이클 68 인계 카드 #18 (UTC 잔존 데이터 백필 migration) = **사실 점검 결과 불필요 확정 + 사이클 68 가정 = 오해 영구 명문화로 미래 동일 오해 재발 차단**. team-leader Supabase MCP `execute_sql` READ-ONLY 13 테이블 + 데이터 분포 점검으로 root cause 재해석 (PostgreSQL TIMESTAMPTZ 모델 = 내부 UTC + 표시 시 변환, instant 자체는 timezone 무관 절대 시점). 사이클 65 H2/H2-bis + 사이클 68 17 모듈 시정 = 표시 형식 일관화 + 호출자 정합성 보장 (사이클 53 B-4 KST `+09:00` 명시 영속 + 사이클 65 hotfix `system_logs` write_log + main.py `_DbLogHandler` + 사이클 68 `src/db/_kst.py` 헬퍼) — *instant 정정이 아님*. **Q4 채택** = TIMESTAMPTZ 사실 명문화로 *진실의 원천* (`src/db/CLAUDE.md`) + *시간순 추적* (`HARNESS_CHANGELOG.md`) 양쪽 영구 기록 → 향후 *"UTC 저장 = 결함"* 가정 시 본 명문화 인용 의무. 코드 변경 0 = 회귀 가드 0 = 매매 안전성 무영향. 9 사이클 연속 옵션 A 패턴 영속 (10회). Q2 (D2 UI strftime LOW) / Q3 (D3 9 engine/api 헬퍼 통일 MEDIUM) 사이클 70+ 인계 + Q5 (RLS 활성 LOW) 신규 카드 #20 후보 등록. 사이클 49→69 누적 21 사이클 + hotfix 4 영역. 후속 카드 #16 (MEDIUM Q6-3 후보 풀 폭축 회고, 1주 운영 후) / #15 (LOW Q7-2 액면분할 prdy_clpr invalidate) / #11/#12 (보드 mutex / 14 모듈 logger) / Q6-2 옵션 B 헬퍼 (사이클 71+) / D2/D3 (Q2/Q3) / #20 (RLS LOW) 영속. |
| 2026-06-07 | 사이클 68 — 카드 #17 (LOW) 기타 INSERT 모듈 UTC → KST 일관성 (사이클 65 hotfix H2/H2-bis 인계 종결). team-leader Phase 1~4 분석 (`_workspace/cycle68_other_insert_modules_kst_audit.md`) 후 사용자 결정 채택: **A (HIGH 1 + MEDIUM 7 = 8 모듈 일괄 시정)** + **2-B (`src/db/_kst.py` 공용 헬퍼 신규)** + **3-B (`src/` 전체 rglob AST 가드)** + **Q1 (LOW 9 engine/api 모듈 `date.today()` → `today_kst()`)** + **Q3 (`stock_master.is_stale()` UTC naive → KST aware 비교 통일)** = **9 사이클 연속 옵션 A 패턴 영속** (사이클 55 R-1 / 60 / 62 / 63 / 64 / 65 / 66 / 67 / 68). **신규 모듈**: `src/db/_kst.py` 35L — `KST = timezone(timedelta(hours=9))` + `now_kst_iso() -> str` (`datetime.now(KST).isoformat()`, `+09:00` suffix 포함) + `today_kst() -> date` (`datetime.now(KST).date()`, 서버 timezone 의존 `date.today()` 회피). **시정 17 모듈**: 8 DB (`stock_master.py:35` `_to_row` `refreshed_at` HIGH 24h TTL baseline + `parameter_recommendations.py` `insert_recommendation` `created_at` + `log_reports.py` `insert_log_report` `created_at` + `system_config.py` 6 upsert L113/L165/L229/L345/L432/L639 `updated_at` + `strategy_config.py:41` `save` `updated_at` + `kis_quote_accounts.py:178,231` `insert_account` + `update_account` + `market_regime_snapshots.py:30` `_now_iso` + `backtest_runs.py:34` `_now_iso`) + 9 engine/api (`api/balance.py` + `api/condition.py` + `engine/strategy_base.py` + `engine/strategy.py` + `engine/backtest_engine.py` + `engine/scheduler.py` + `engine/boot_manager.py` + `engine/strategies/long_tail_volatility.py` + `engine/strategies/volatility_breakout.py`) + PR-C2 가드 갱신 (`tests/unit/api/test_condition_cache.py` 자정 race 안전 의도 영속). **회귀 가드 15 케이스 (5 파일)**: **G-1 헬퍼 모듈 export 검증 (3 sub-case)** — `from src.db._kst import now_kst_iso, today_kst, KST` import + `now_kst_iso()` 반환값 `+09:00` suffix + `KST` 가 `timezone(timedelta(hours=9))` 또는 `ZoneInfo("Asia/Seoul")` + **G-2~G-9 8 DB 모듈 KST 명시 (8 케이스)** — INSERT/UPSERT 함수가 timestamp/created_at/updated_at/refreshed_at 등 시각 컬럼에 `now_kst_iso()` 또는 `datetime.now(KST).isoformat()` 명시 검증 (mock payload 캡처 + `+09:00` suffix 검증) + **G-10a AST 정적 가드: `src/` 전체 `datetime.utcnow()` 0건** (deprecated naive) + **G-10b `src/db/` payload 영역 `datetime.now(timezone.utc)` 0건** (사이클 65 H3 패턴 확장, 결정 3-B) + **G-11 `stock_master.is_stale()` KST aware 비교 통일 (Q3, 1 케이스)** — UTC naive 비교 시 9시간 오차 → 24h TTL 부정확 결함 영구 차단 + **G-12 `date.today()` 0건 AST (Q1, 1 케이스)** — `src/` 전체 rglob 정규식 `date\.today\s*\(\s*\)` 호출 0건 (주석 라인 stripped.startswith("#") 가드, `default_factory=date.today` 함수 참조는 회피 = D1 silent 결함 후보로 사이클 69 인계). **백엔드 2002 → 2017 PASS** (+15 신규) **+ 2 skipped + 1 XFAIL** (사이클 66 K-2 의미 전환 영속). 카테고리 분리 측정 (silent 결함 0): `tests/unit/db/test_cycle68_*.py` 15 PASS / `tests/unit/db/` 166 PASS / `tests/unit/engine/` 1,162 PASS + 1 XFAIL / `tests/unit/api/` 134 PASS (PR-C2 갱신 영속) / `tests/unit/engine/strategies/` 221 PASS (LTV+VB 6 전략 영향 0) / `tests/unit/engine/stale_manager/` 27 PASS (사이클 67 분해 영향 0) / `tests/integration/` 164 PASS / `tests/contract/` 146 PASS + 2 skipped. **flakiness 0** (3 회 반복: cycle68 15 케이스 0.07~0.08s ±0.01s / db/ 166 케이스 0.40~0.44s ±0.02s / unit/ 1,707 케이스 41.33~41.70s ±0.5%). **회귀 0** / **매매 안전성 무영향** (DB INSERT 영역 = 시세/매매 hot path 무관 + 사이클 38 명문화 + 사이클 55 R-1 SellRejectionTracker + 사이클 66 priority 분리 + 사이클 67 stale_manager 분해 모두 무영향). **사이클 65 hotfix H2/H2-bis 영속** (변경 0 의무 확인): `system_logs.py:44` `"timestamp": datetime.now(KST).isoformat()` 인라인 패턴 유지 + `main.py:126` `_DbLogHandler` 위임 경로 인라인 패턴 유지 (행위 동일, `_kst.py` 헬퍼 통일은 후속 사이클 인계 가능, AST 가드 영속 검증 PASS). **silent 결함 후보 3건 (전부 LOW 운영 무영향, 사이클 69+ 인계)**: **D1** `src/engine/strategy_base.py:34` `buy_date: date = field(default_factory=date.today)` — 함수 참조가 G-12 정규식 회피, dataclass 인스턴스 생성 시 동일 결함이나 운영 `TZ=Asia/Seoul` 무영향 (PyCharm/UTC 컨테이너 전환 시 익일 청산 판정 오차 위험) → 사이클 69 카드 #18 동행 시정 권고 + G-12 정규식 보강 + `field(default_factory=lambda: datetime.now(KST).date())` 패치 검토 + **D2** `src/engine/strategy.py:142` + `engine/strategies/long_tail_volatility.py:577` + `engine/strategies/volatility_breakout.py:550` `datetime.now().strftime(...)` tz-naive 3건 (positions["time"] UI 표시용, DB 시각 컬럼 무관, 서버 `TZ=Asia/Seoul` 하 무영향) → 사이클 69+ 일관성 정리 카드 인계 + **D3** Q1 9 engine/api 모듈 = 인라인 KST 패턴 (`_KST = timezone(timedelta(hours=9))` + `datetime.now(_KST)`) 사용, `src.db._kst` import 0건 — G-10a/G-12 가드 통과 + 행위 동일하므로 결함 아님, 단 *헬퍼 통일* 의도라면 사이클 69+ 별도 카드로 리팩토링 검토 가능. **사이클 69 카드 #18 (UTC 잔존 데이터 백필 migration) 자동 예약 (Q2 채택)**: 사이클 68 신규 INSERT 부터 KST `+09:00` 강제 → 기존 UTC 잔존 데이터 (`system_logs.timestamp` / `trade_history.timestamp` / `daily_performance.created_at` / `daily_log_reports.created_at` 등) 백필 필요. migration 영역 = `AT TIME ZONE 'Asia/Seoul'` 변환 + 인덱스 재구축. 운영 영향 = log_analysis_engine + recommendation_engine `get_trades_in_range` 영역 통합 회귀 검증 의무. | `src/db/_kst.py` 신규 35L / `src/db/stock_master.py` (G-2 upsert + G-11 is_stale, +12 lines) / `src/db/parameter_recommendations.py` (+1 line) / `src/db/log_reports.py` (+3 lines) / `src/db/system_config.py` (+9 lines, 6 upsert) / `src/db/strategy_config.py` (+2 lines) / `src/db/kis_quote_accounts.py` (+9 lines, 2 함수) / `src/db/market_regime_snapshots.py` (+6 lines) / `src/db/backtest_runs.py` (+8 lines, 2 함수) / `src/api/balance.py` (+5 lines, Q1) / `src/api/condition.py` (+6 lines, Q1) / `src/engine/strategy_base.py` (+6 lines, Q1) / `src/engine/strategy.py` (+8 lines, Q1) / `src/engine/backtest_engine.py` (+4 lines, Q1) / `src/engine/scheduler.py` (+6 lines, Q1) / `src/engine/boot_manager.py` (+5 lines, Q1) / `src/engine/strategies/long_tail_volatility.py` (+2 lines, Q1) / `src/engine/strategies/volatility_breakout.py` (+2 lines, Q1) / `tests/unit/api/test_condition_cache.py` (+15 lines, PR-C2 가드 갱신) / `tests/unit/db/test_cycle68_kst_helper_module_g1.py` 신규 (3 케이스 G-1a/b/c) / `tests/unit/db/test_cycle68_kst_timestamp_per_module_g2_g9.py` 신규 (8 케이스) / `tests/unit/db/test_cycle68_kst_global_ast_guard_g10.py` 신규 (2 케이스 G-10a/b) / `tests/unit/db/test_cycle68_stock_master_is_stale_kst_g11.py` 신규 (1 케이스) / `tests/unit/db/test_cycle68_date_today_kst_g12.py` 신규 (1 케이스) / `_workspace/cycle68_other_insert_modules_kst_audit.md` 신규 (team-leader Phase 1~4 분석 보고서) / `_workspace/red/cycle68_other_insert_modules_kst.md` Red 명세 영구 기록 / `src/db/CLAUDE.md` `_kst.py` 명세 추가 / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 68 행 | 사이클 65 hotfix H2/H2-bis 인계 카드 #17 (LOW) **완전 종결** — system_logs 외 기타 INSERT/UPSERT 모듈 timestamp KST 일관성. 9 사이클 연속 옵션 A 패턴 영속 (사이클 55 R-1 / 60 / 62 / 63 / 64 / 65 / 66 / 67 / 68) + Q1/Q3 동행 채택으로 silent 결함 17 모듈 일괄 차단. `_kst.py` 공용 헬퍼로 향후 신규 silent 결함 영구 차단 (G-10b AST 가드 + 사이클 65 H3 패턴 확장). team-leader Phase 1~4 분석 → 사용자 결정 3개 + Q1~Q3 자유 발의 → tdd-engineer Red 15 케이스 → backend-dev Green 17 모듈 시정 → tester Verify V-1~V-9 전수 PASS 흐름 영속. 매매 안전성 무영향 (DB INSERT 영역 + 사이클 38 명문화 + 사이클 55 R-1 + 사이클 66 priority 분리 + 사이클 67 stale_manager 분해 모두 무영향). 사이클 67 분해 회귀 가드 27 PASS 영속 (stale_manager facade + 4 sub-module). silent 결함 후보 3건 (D1/D2/D3 전부 LOW 운영 무영향) 사이클 69+ 인계. **사이클 69 카드 #18 (UTC 잔존 데이터 백필 migration) 자동 예약** (Q2 채택). 사이클 49→68 누적 20 사이클 + hotfix 4 영역. 후속 카드 #16 (MEDIUM Q6-3 후보 풀 폭축 2주 회고, 1주 운영 후) / #15 (LOW Q7-2 액면분할 prdy_clpr invalidate) / #11/#12 (보드 mutex / 14 모듈 logger) / 옵션 B 헬퍼 (사이클 71+ Q6-2) 영속. |
| 2026-06-06 | 사이클 67 — refactor #2 후속 카드 #14 (MEDIUM) `stale_manager.py` 1,099L → **4 sub-module + facade 96L 분해** (사이클 63 발견 인계 카드 영속 종결). domain-expert 옵션 A 자문 (Q1~Q5 + Q6-1~Q6-3) 전부 적용 = **8 사이클 연속 패턴 영속** + Q4 유일 불일치 채택 (옵션 B 채택, 자문 실측 데이터 강력 권고). **분해 매트릭스**: `src/engine/stale_diagnostics.py` 357L (5 함수 + 4 상수 — `build_session_subscription_view` / `emit_stale_session_detail` / `refresh_stale_ccnl_cache` / `evict_expired_ccnl` / `prune_force_retry_history`) + `src/engine/stale_session_recovery.py` 274L (3 함수 + 5 상수 — `detect_silent_inactive_sessions` / `force_reconnect_session` / `delta_unsubscribe_dropped`) + `src/engine/stale_universe_guard.py` 157L (1 함수 + 1 상수 — `evaluate_universe_guard`) + `src/engine/stale_watcher_core.py` 399L (2 함수 — `check_and_resubscribe_stale` / `resubscribe_stale_priority` — **K stale watcher 본체 HIGH hot path**, 사이클 66 priority 분리 *후* cap 영속). `src/engine/stale_manager.py` 1,099 → **96L** (`__all__` 21 항목 re-export only). `src/engine/scheduler.py` **변경 0** (11 wrapper + 5 상수 re-export L92 모두 `from src.engine import stale_manager` lazy import 영속, Q5=A). **자문 채택 매트릭스 (Q1~Q5 + Q6)**: **Q1=A facade** (단일 진입점 보존, 사이클 51 boot_manager 답습 8 사이클 답습) / **Q2=P1 모듈-레벨 정적 import** (`stale_watcher_core` → `from src.engine.stale_diagnostics import emit_stale_session_detail` 모듈-레벨, 함수-레벨 lazy import 비채택) / **Q3=A** 4 sub-module 모두 `logger = logging.getLogger("src.engine.scheduler")` 명시 (사이클 60 I1 영속 — `caplog set_level(logger="src.engine.scheduler")` 전수 호환) / **Q4=B facade patch 영속** (team-leader 1차 옵션 A 와 유일 불일치 — 자문 실측 `patch("src.engine.stale_manager.*")` = **0건** 발견 → 갱신 의무 사실상 0, 신규 patch 작성 부담만 발생 → G-16 AST 가드 신설로 silent 결함 영구 차단) / **Q5=A scheduler.py wrapper 변경 0** (`from src.engine import stale_manager; await stale_manager.X(self)` 패턴 영속) / **Q6-1 (LOW)** hot path import 캐시 측정 권고 (Python module cache 보장, cold start latency `< 100ms`) / **Q6-2 (MEDIUM)** 사이클 71+ 옵션 B 헬퍼 청사진 (3 헬퍼 ~75L: `_sched_mod_get` / `_collect_high_tickers` / `_write_log_fire_and_forget`) / **Q6-3 (MEDIUM)** 사이클 68+ 시점 분리 권고 (사이클 67 push → 1 주 운영 → 사이클 68+ #16 후보 풀 폭축 회고 → 1 주 운영 → 사이클 71+ #15 액면분할 분리). **사이클 29 005935 사고 영역 영속** (G-15 WARNING `[stale_priority_resubscribe_cap_exceeded]` + G-17 priority 분리 *후* cap AST = HIGH 종목 cap 밖 잘림 8분 영구 잔류 + KIS LMS chain 차단 영속). **사이클 66 시정 영속** (G-11 `high_targets`/`low_targets` 변수명 영속 + G-12 try/except 4중 가드 영속). **회귀 가드 17 케이스 (6 파일, HIGH 6 = 35%)**: G-1~G-5 분해 검증 (5 케이스 — 4 sub-module 파일 존재 + 11 함수 + 10 상수 export + facade 21 `__all__`) + G-6~G-9 의존성+logger (4 케이스 — Q3 4 sub-module 동일 logger AST + Q1 단방향 의존 AST + Q2 모듈-레벨 정적 import AST + Q5 facade lazy import AST) + G-10~G-13 사이클 63+66 영속 (4 케이스 — Q4=B 직접 호출 AST + 사이클 66 변수명 AST + try/except 4중 AST + caplog 일관성) + G-14~G-15 사이클 60+64 영속 (2 케이스 — 폐기 메서드 0건 AST + 사이클 29 WARNING AST) + **G-16 facade patch 안전선 신규** (1 케이스 — `tests/unit/engine/` rglob `patch("src.engine.stale_manager.X")` → X facade export 검증, Q4 옵션 B 영속) + **G-17 priority 분리 *후* cap 신규** (1 케이스 — `stale_watcher_core.py` `resubscribe_stale_priority` 본체 AST 정적 가드, 사이클 29 영구 차단). **백엔드 1985 → 2002 PASS** (+17 = G-1~G-17 신규) **+ 1 XFAIL** (사이클 66 K-2 의미 전환 영속) **+ 2 skipped**. `tests/unit/engine/stale_manager/` 27 PASS / `tests/unit/engine/` 1,162 PASS + 1 XFAIL. flakiness 0 (3 회 `stale_manager/` 0.24~0.39s / `engine/` 36.34~36.56s, ±0.5%). 회귀 0 / 매매 안전성 무영향 (행위 보존 + 외부 인터페이스 변경 0 + scheduler.py 변경 0). **누적 scheduler.py 라인 감소** (사이클 51 직전 ~4,185 → 사이클 67 후 3,007 영속): **-1,178L, -28%**. **patch 경로 적응 3 건** (사이클 67 분해 후 단일 파일 경로 하드코딩 영역): `test_C1` (`test_cycle60_phase2A1_stale_manager.py:227`) `patch.object(stale_manager, "evict_expired_ccnl")` → `patch("src.engine.stale_diagnostics.evict_expired_ccnl")` (본체 동일 모듈 네임스페이스 직접 호출 → facade patch 비효과, sub-module 직접 patch 전환) + `test_D2` (`test_cycle63_phase2A3_dependency_direction.py:64`) 단일 파일 grep → `stale_watcher_core.py` + `stale_session_recovery.py` 합산 grep (총 6건 ≥ 4 충족) + `test_cycle66_resubscribe_cap_priority_fix.py` patch 경로 일부 적응. **G-12 라인 cap 마진 권고 (정보, 결함 아님)**: 설계 카드 §5.1 G-12 권고 cap `stale_watcher_core ≤ 380L` 대비 실측 **399L** (19L, 5% 초과). 본체 라인 단위 보존 (행위 보존 의무 우선) + Q1 priority 분리/Q2 try/except 4중/Q3 WARNING 시정 (사이클 66 영속) 모두 보존 결과 → 운영 영향 0 / 회귀 위험 0. 후속 사이클 71+ 옵션 B 헬퍼 (Q6-2 청사진) 분리 시 추가 압축 가능. **무변경 영역 (회귀 가드)**: 모든 함수 본체 라인 단위 보존 + 시그너처 영속 (`emit_stale_session_detail(scheduler, ...)` 등 Q4=B 답습) + 상수 값 영속 (cap=10 / SILENT_INACTIVE_* / UNIVERSE_*) + `sys.modules.get("src.engine.scheduler")` 패턴 영속 (`stale_watcher_core` 3건 + `stale_session_recovery` 3건 = 6건, 사이클 61 D-1 AST 가드) + try/except 4중 가드 영속 (사이클 66 Q2 본체) + 운영 prefix 영속 (`[stale_watcher_detail]` / `[stale_priority_resubscribe]` / `[stale_priority_resubscribe_cap_exceeded]` / `[stale_force_retry]` / `[silent_inactive_*]` / `[universe_excluded]` 등) + WebSocket 4중 안전망 호출 시점/횟수 변경 0 + silent inactive 시간당 세션당 2회 cap 영속 + stale universe 가드 보유/익일청산 절대 보호 영속. **월요일 (2026-06-09) 09:00~10:00 1h tester verify 의무** (시나리오 A 자연 monitoring + B 인위 stale 주입 보유 0 + C KIS LMS chain 차단 + D HIGH 인위 stale 측정 지표 3 — 사이클 67 분해 후 운영 hot path 첫 노출 검증). | `src/engine/stale_manager.py` 1,099 → 96L (facade re-export only, `__all__` 21) / `src/engine/stale_diagnostics.py` 신규 357L (5 함수 + 4 상수) / `src/engine/stale_session_recovery.py` 신규 274L (3 함수 + 5 상수) / `src/engine/stale_universe_guard.py` 신규 157L (1 함수 + 1 상수) / `src/engine/stale_watcher_core.py` 신규 399L (2 함수 — K stale watcher 본체) / `src/engine/scheduler.py` 변경 0 (11 wrapper L2435~L2501 + 5 상수 re-export L92 영속) / `tests/unit/engine/stale_manager/test_cycle67_module_decomposition_g1_g5.py` 신규 5 케이스 / `tests/unit/engine/stale_manager/test_cycle67_dependency_logger_g6_g9.py` 신규 4 케이스 / `tests/unit/engine/stale_manager/test_cycle67_cycle63_cycle66_persistence_g10_g13.py` 신규 4 케이스 / `tests/unit/engine/stale_manager/test_cycle67_cycle60_persistence_g14_g15.py` 신규 2 케이스 / `tests/unit/engine/stale_manager/test_cycle67_facade_patch_persistence_g16.py` 신규 1 케이스 (Q4=B 안전선) / `tests/unit/engine/stale_manager/test_cycle67_cycle66_priority_cap_persistence_g17.py` 신규 1 케이스 (사이클 29 영구 차단) / `tests/unit/engine/test_cycle60_phase2A1_stale_manager.py` test_C1 patch 경로 적응 / `tests/unit/engine/test_cycle63_phase2A3_dependency_direction.py` test_D2 grep 2 sub-module 합산 / `tests/unit/engine/stale_manager/test_cycle66_resubscribe_cap_priority_fix.py` patch 경로 일부 적응 / `src/engine/CLAUDE.md` 모듈 맵 + 본문 사이클 67 행 / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 67 행 / `_workspace/cycle67_stale_manager_decomposition_design_card.md` 신규 (4 sub-module 책임 매트릭스 + 위험 평가 + G-12 라인 cap) / `_workspace/cycle67_stale_manager_decomposition_domain_consult.md` 신규 (Q1~Q5 + Q6+ 자유 발의 의뢰서) / `_workspace/cycle67_stale_manager_decomposition_domain_response.md` 신규 (자문 회신서 Q1~Q5 + Q6-1~Q6-3 + Q4 옵션 B 강력 권고) / `_workspace/red/cycle67_stale_manager_decomposition.md` Red 명세 영구 기록 | refactor #2 후속 카드 #14 (MEDIUM) **완전 종결** — `stale_manager.py` 1,099L 모듈 비대화 해소 + 책임 분리 (진단/복구/유니버스/watcher 본체). domain-expert 옵션 A 자문 **8 사이클 연속 패턴 영속** (사이클 55 R-1 / 60 / 62 / 63 / 64 / 65 / 66 / 67) + Q4 유일 불일치 채택 (자문이 실측 데이터 기반 강력 권고). 사이클 51 boot_manager 답습 + Q4=B 직접 호출 (사이클 60 답습 안 함 유일 영역) 영속 + Q2=P1 모듈-레벨 정적 import (함수-레벨 lazy import 비채택) + Q3=A 동일 logger (사이클 60 I1 영속). G-16 (Q4=B facade patch 안전선) + G-17 (priority 분리 후 cap, 사이클 29 영구 차단) 신규 AST 가드 2 — silent 결함 영구 차단 패턴 8 회 누적 검증 (사이클 60/64/65#1/65#2/65#3/66/67). Q6-2 (사이클 71+ 옵션 B 헬퍼 청사진 ~75L) + Q6-3 (사이클 68+ 시점 분리 권고) 채택. **K stale watcher 본체 영역 영속 보장** (Q1/Q2/Q5 종합 회귀 위험 LOW): 행위 변경 0 + 외부 인터페이스 변경 0 + scheduler.py 변경 0 + 사이클 29 005935 사고 영역 G-15/G-17 AST 가드 신설. **월요일 (2026-06-09) 09:00~10:00 1h tester verify 의무** (시나리오 A/B/C/D 결합 효과 측정). 사이클 49→67 누적 19 사이클 + hotfix 4 영역. 후속 카드 #16 (MEDIUM Q6-3 1주 후) / #17 (LOW 기타 INSERT KST) / #15 (LOW 액면분할) / #11/#12 (보드 mutex / 14 모듈 logger) / 옵션 B 공통 헬퍼 (사이클 71+ Q6-2) 영속. |
| 2026-06-06 | 사이클 66 — `_resubscribe_stale_priority` cap=10 결함 시정 (카드 #5 HIGH, 사이클 63 K-2 발견 결함 영속 시정, domain-expert 옵션 A 자문 Q1~Q5 + Q6-1~Q6-6 전부 적용 7 사이클 연속) | **사이클 63 발견 결함 영속 시정** (K-2 PASS = 결함 confirm → 사이클 66 시정 confirm 의미 전환). **결함 (사이클 63)**: `stale_manager.py:1023-1034` `targets = stale_tickers[:cap]` 가 priority 분리 *전* `[:cap]` 적용 → HIGH 종목 (보유/익일청산) 이 sorted LOW 후보에 밀려 cap 밖 잘림 가능 → 5분 우선 재구독 누락 → KIS LMS chain 사고 위험 (사이클 29 005935 사고 패턴 8분 영구 잔류). **시정 (사이클 66 K-2 의미 전환 = 시정 confirm)**: `src/engine/stale_manager.py:1023-1057` ~12L → ~22L 교체 — **Q1** priority 분리 *먼저* (`high_targets = [t for t in stale_tickers if t in high_tickers]` / `low_targets = [t for t in stale_tickers if t not in high_tickers]`) + **Q2 try/except 4중 가드 통일** (본체 `_check_and_resubscribe_stale` L820-833 패턴 답습 — outer `try: for s in registry.all(): try: positions / except / except` + NDC 별도 try/except) + **Q3 HIGH > cap 모두 보장 + WARNING 로그** (`logger.warning("[stale_priority_resubscribe_cap_exceeded] high_count=N cap=10 ...")` 운영 가시화) + 최종 `targets = high_targets + low_targets[:max(0, cap - len(high_targets))]`. **사이클 29 005935 사고 패턴 영구 차단** = 시정 본질. **무변경 영역 7 (회귀 가드)**: `sys.modules.get("src.engine.scheduler")` 패턴 (사이클 61 D-1 AST 가드 영속) + `kis_ws_pool.subscribe(TICK_TR_ID, ticker, priority=..., bypass_limit=...)` 인터페이스 + `_stale_last_resubscribe_at` 갱신 hook + `asyncio.sleep(0.05)` Rate Limit + `[stale_priority_resubscribe] count=... tickers=...` INFO + `write_log` fire-and-forget + `high_tickers` ↔ `bypass_limit` 매핑 (HIGH=True / LOW=False, 사이클 25-B 영속) + `_resubscribe_stale_priority` 함수 시그너처. **회귀 가드 11 케이스 (단일 파일 `tests/unit/engine/stale_manager/test_cycle66_resubscribe_cap_priority_fix.py`)**: HIGH 4 (K-2 시정 confirm + K-3 HIGH 12 > cap=10 모두 통과 + K-4 HIGH 5 사전순 후반 + LOW 20 정확 분리 + K-8 registry 예외 try/except 4중 fallback) + MEDIUM 2 (K-10 WARNING 발화 caplog + AST 정적 가드) + LOW 4 (K-5 HIGH 0 LOW 영속 + K-6 HIGH 5 LOW 0 cap 미달 + K-7 stale 0 early return + K-9 HIGH 0 LOW 0 edge). **K-2 의미 전환 매트릭스 (Q6-4 docstring 의무)**: 사이클 63 K-2 (별도 파일 `tests/unit/engine/test_cycle63_phase2A3_priority_cap.py::test_K2`) = `@pytest.mark.xfail(strict=False)` 마킹 — 사이클 63 결함 confirm 영속 보존 (호환), 사이클 66 시정 완료로 자동 XFAIL 전환. 사이클 66 K-2 (신규 파일) = 시정 confirm assertion (HIGH 종목 targets 포함). 동일 케이스가 사이클별로 의미가 *전환* — 과거 결함 영속 보존 + 시정 자동 가시화 패턴 신규 도입. **백엔드 1975 → 1984 PASS + 1 XFAIL** (+9 신규 PASS = 10 신규 - K-2 의미 전환 1 + K-2 사이클 63 XFAIL 1) / 회귀 0 / flakiness 0 (3 회 1145 PASS 35.76s~35.94s ±0.18s) / coverage 80.95% (+0.02%). **tester Verify V1~V13 + V-K + V-AST + V-1h-prep ALL PASS**: V1~V4 카테고리 분리 측정 (unit/engine 1145 + non-engine 529 + integration 164 + contract 146 = 1984 백엔드) + V5~V9 회귀 가드 6 영역 모두 PASS (사이클 60/61/63/64/65 누적 100%) + V10 flakiness 차단 3 회 (사이클 58~65 패턴 영속) + V11 coverage 80.95% + V12 백엔드 only (K stale watcher 본체, V12 skip — 사이클 63 패턴 답습) + **V-K HIGH 4 케이스 전수 PASS** (K-2/K-3/K-4/K-8/K-10) + **V-AST 시정 패턴 정적 검증** (`stale_tickers[:cap]` 함수 본체 0건 + `low_targets[: max(0, cap - len(high_targets))]` 1건 L1057) + V13 매도 영향 0 (`git diff order_engine.py + risk.py` 0 byte). **매매 안전성 무영향** (사이클 38 명문화 영속 — 시세 영역, 매도/익일청산/손절 영향 0). **사이클 60 hotfix 카테고리 분리 측정 + flakiness 3 회 반복 영속** (사이클 58~66 8 사이클 누적 패턴). **월요일 (2026-06-09) 09:00~10:00 1h tester verify 시나리오 D 신규** (Q5 자문): HIGH 인위 stale 주입 보유 0 + 신규 측정 지표 3 (HIGH 5분 우선 재구독 보장률 100% / WARNING 발화 빈도 / HIGH 회복 시간 ≤5분) — 시나리오 A/B/C/D 결합 효과 측정. **사이클 66 = refactor #2 후속 후행 시정** (사이클 63 발견 영속 시정 의무, K stale watcher 본체 영역 = HIGH 안전 규칙 사이클 60 §Q7 영구 hot path 360 회/일 + KIS LMS chain 직접 영역). domain-expert 자문 옵션 A 전부 (Q1~Q5 + Q6-1~Q6-6) **7 사이클 연속 패턴 영속** (사이클 55 R-1 / 60 / 62 / 63 / 64 / 65 / 66). **다음 사이클 권고**: 사이클 67+ = 카드 #14 (MEDIUM) stale_manager.py 1,076L sub-module 분해 (3+1 청사진) / 카드 #16 (MEDIUM) 사이클 65 후보 풀 폭축 역설 risk 2주 회고 / 카드 #17 (LOW) 기타 INSERT 모듈 UTC → KST 일관성 / 카드 #15 (LOW) Q7-2 액면분할 prdy_clpr invalidate / 카드 #11/#12 보드 mutex / 14 모듈 logger. | `src/engine/stale_manager.py` ~22L 교체 (L1023-L1057, try/except 5중 가드 + priority 분리 *먼저* + cap 적용 *나중* + HIGH > cap WARNING) + L970-992 docstring 갱신 (Q6-4 의미 전환 명시) / `tests/unit/engine/stale_manager/__init__.py` 신규 / `tests/unit/engine/stale_manager/test_cycle66_resubscribe_cap_priority_fix.py` 신규 (11 케이스 = HIGH 4 + MEDIUM 2 + LOW 4 + AST 1) / `tests/unit/engine/test_cycle63_phase2A3_priority_cap.py::test_K2` 에 `@pytest.mark.xfail(strict=False, reason="사이클 66 시정 완료 — K-2 결함 confirm → 시정 confirm 의미 전환 (Q6-4 docstring 의무)")` 마킹 / `src/engine/CLAUDE.md` 사이클 66 본문 추가 / `_workspace/cycle66_cap10_fix_design_card.md` v2 + `_workspace/cycle66_cap10_fix_domain_consult.md` + `_workspace/cycle66_cap10_fix_domain_response.md` + `_workspace/red/cycle66_cap10_fix.md` 발주/자문/Red 명세 영구 기록 | 사이클 63 K-2 발견 결함 (HIGH 종목 cap 밖 잘림 + KIS LMS chain 사고 위험) 영속 시정 — **사이클 29 005935 사고 패턴 영구 차단**. domain-expert 자문 옵션 A 전부 적용 (Q1 시정 안 + Q2 try/except 4중 + Q3 WARNING + Q6-4 의미 전환) = **7 사이클 연속** 패턴 일관 (사이클 55 R-1 / 60 / 62 / 63 / 64 / 65 / 66). **K-2 의미 전환 패턴 신규 도입** = `@pytest.mark.xfail(strict=False)` 마킹으로 *과거 결함 영속 보존 (테스트 케이스 자체) + 시정 자동 가시화 (XFAIL 전환)* — 향후 결함 시정 사이클에서 활용 가능한 표준 패턴. 사이클 60 hotfix 카테고리 분리 측정 + flakiness 3 회 반복 = 사이클 58~66 8 사이클 누적 패턴 (silent 결함 영구 차단). 사이클 38 명문화 + 사이클 29-R3 + 사이클 25-B 영속 = 안전 규칙 절대 보호. **월요일 1h tester verify 시나리오 D 신규** (Q5 자문 인계) = HIGH 인위 stale 주입 보유 0 + 측정 지표 3 = 사이클 66 시정 효과 정량 측정 의무. 사이클 49→66 누적 **18 사이클 + hotfix 4 영역** = 안전성 회수 + refactor #2 후속 후행 시정 완료. K stale watcher 본체 영역 = HIGH 안전 규칙 영속 (영구 hot path 360 회/일 + KIS LMS chain 직접 영역). 잔여 후속 카드 (#14/#16/#17/#15/#11/#12) 사이클 67+ 인계. 운영 검증 인계: 월요일 1h verify (시나리오 D 신규 포함) / 가격 필터 + 거래대금 필터 운영 2주 (사이클 64+65) / 사이클 63 K stale watcher 1h verify (시나리오 A/B/C). | 
| 2026-06-06 | 사이클 65 hotfix — e2e settings.spec.ts FAIL 시정 (H1) + **system_logs timestamp KST 강제 (H2 + H2-bis, 사용자 신규 보고)** + AST 영구 가드 4 (H3) | **4 영역 통합 hotfix**. **결함 1 (e2e)**: 사이클 65 신규 TradeAmountFilterCard 추가 → 카드 수 임계 초과 + React Query 기본 retry 3회 누적 → e2e 환경 (백엔드 미실행) ECONNREFUSED 시 5.7s timeout 초과 → settings.spec.ts:5 FAIL (사이클 65 push 후 CI failure 발견). **결함 2 (system_logs UTC)**: 사용자 추가 보고 — `src/db/system_logs.py::write_log` 가 INSERT 시 timestamp 필드 명시 안 함 → DB default (PostgreSQL now() = UTC) 저장 → 사이클 53 KST `+09:00` 패턴 답습 누락. **결함 3 (tdd-engineer 발견, H2-bis 신규 결함)**: AST 가드 작성 중 `src/main.py::_insert_log_to_db` (`_DbLogHandler` 위임 경로, logger.* 모든 호출이 통과) 도 동일 결함 — H2 단독 시정 시 logger.* 경로 UTC 잔존 silent 결함화. **4 영역 hotfix**: H1 (frontend-dev `PriceFilterCard.tsx` + `TradeAmountFilterCard.tsx` `useQuery({ retry: 1, ... })` 명시 — main.tsx 전역 defaultOptions retry:1 와 일관, 카드 단위 명시로 e2e 안전 요건 독립 보존) + H2 (backend-dev `src/db/system_logs.py:44` `data["timestamp"] = datetime.now(KST).isoformat()` 1줄 추가, 사이클 53 패턴 답습) + **H2-bis (메인 세션 직접 시정, `src/main.py:126` `_insert_log_to_db` 동일 KST timestamp 1줄, logger.* 호출 빈도 ↑ 경로 영구 차단)** + H3 (tdd-engineer AST 영구 가드 vitest 2 + pytest 2). **사이클 60/64 hotfix 패턴 답습 (3 중 silent 결함 차단)**: 단위 가드 + 통합 가드 + AST 정적 가드 = 사이클 60 hotfix H-2 (scheduler 통합) + G-3 (src 전체 폐기 메서드 0) 패턴 영속 + 사이클 64 hotfix 패턴 영속. **검증**: 백엔드 1970 → **1972 PASS** (+2 신규 pytest AST 가드 — `tests/unit/db/test_system_logs_kst_timestamp.py` 2 케이스: `src/` 전체 rglob INSERT 호출처 KST timestamp 명시 의무 + write_log 함수 단위 검증) / 프론트 170 → **172 PASS** (+2 신규 vitest AST 가드 — `frontend/src/components/__tests__/_ast_useQuery_retry_required.test.ts` 2 케이스: PriceFilterCard + TradeAmountFilterCard useQuery retry 옵션 명시 의무 정규식 정적 검증) / 전체 **2144 PASS / 2 skipped / 0 FAIL** / coverage 80.93% / **flakiness 0** (3 회 1136 ±0.05s 안정). **Python runtime 검증**: `datetime.now(KST).isoformat()` = `2026-06-06T14:59:35.844072+09:00` (`+09:00` suffix 정상). **tester Verify V1~V13 + V-H1 + V-H2 + V-H2-bis + V-H3 ALL PASS**: V1~V4 카테고리 분리 측정 + V5~V9 회귀 가드 6 영역 + V10 flakiness 3 회 + V11 CI 시뮬 (60s timeout 영속) + V12 백엔드↔프론트 정합 (`ApiResponse<TradeAmountFilter>` + mode 필드 제거 영속) + V-H1 e2e (로컬 무거워 skip, CI 인계) + V-H2/H2-bis system_logs KST AST + Python runtime + V-H3 영구 가드 영속 + V13 매도 영향 0 (`order_engine.py` + `risk.py` diff 0건, 사이클 38 명문화 영속). **호환 경로 영속**: 표준 logging 의 `_DbLogHandler::emit` → `_LOG_DB_EXECUTOR.submit(_insert_log_to_db, ...)` → `_insert_log_to_db(level, message)` INSERT — 모든 `src.*` 모듈 logger.* 호출이 KST timestamp 로 저장. **신규 데이터만 KST 영속** — 기존 UTC 잔존 데이터는 그대로 (운영자 명시 변환 의무, 카드 #18 인계). **PostgreSQL TIMESTAMPTZ 호환**: ISO 8601 + `+09:00` offset 형식 자동 변환. **다른 INSERT 모듈 grep 결과 (별개 카드 인계)**: `trade_history.py` / `kis_quote_accounts.py` / `parameter_recommendations.py` = KST 명시 정상 (사이클 53 완료). `log_reports.py::created_at` / `backtest_runs.py::created_at` / `market_regime_snapshots.py::created_at` / `strategy_funnel.py::timestamp` = UTC 저장 (조회 필터 미사용, LOW) — **카드 #17 (신규 LOW)** 발의. **사이클 38 명문화 영속**: scanner 단계 + 매수 진입 전용 (매도/익일청산/손절 영향 0) — 사이클 65 hotfix 4 영역 모두 매매 흐름 0 영향. **사이클 65 hotfix 효과**: (a) e2e settings.spec.ts FAIL 시정 (push 후 CI 자동 검증 인계) (b) system_logs 신규 INSERT KST 강제 (logger.* + write_log 양쪽 경로) (c) AST 영구 가드 4 = 향후 useQuery 추가 + system_logs INSERT 추가 시 retry / KST 누락 영구 차단. **사이클 66 (카드 #5 cap=10 결함 시정) 보류**: 사이클 65 hotfix 완료 후 재발주 (team-leader 사전 산출물 `_workspace/cycle66_cap10_fix_design_card.md` + `_workspace/cycle66_cap10_fix_domain_consult.md` 보존). **다음 사이클 권고**: 사이클 65 hotfix sync-docs + commit + push → 사이클 66 재발주 (domain-expert 자문 → tdd-engineer Red → ...). **신규 카드 발의**: 카드 #17 (LOW) 기타 INSERT 모듈 UTC → KST 일관성 검토 (`log_reports` / `backtest_runs` / `market_regime_snapshots` / `strategy_funnel`) + 카드 #18 (LOW) 기존 UTC 잔존 데이터 변환 운영 카드 — 사이클 67+ 별개 발의. | `frontend/src/components/PriceFilterCard.tsx` (+1 line 60, retry: 1) / `frontend/src/components/TradeAmountFilterCard.tsx` (+1 line 62, retry: 1) / `src/db/system_logs.py` (+1 line 44, write_log KST timestamp) / `src/main.py` (+1 line 126, `_insert_log_to_db` KST timestamp) / `frontend/src/components/__tests__/_ast_useQuery_retry_required.test.ts` 신규 (vitest AST 2) / `tests/unit/db/test_system_logs_kst_timestamp.py` 신규 (pytest AST 2) / `src/db/CLAUDE.md` (write_log KST 명세 + main.py 호환 경로 명시) / `frontend/CLAUDE.md` (useQuery retry 영구 가드) | 사이클 65 push 후 GitHub Actions CI = backend-test success but e2e failure (settings.spec.ts:5) 발견 → 즉시 hotfix 진입. 사용자 추가 보고 (system_logs UTC) 통합 hotfix 진행. **결정적 발견**: tdd-engineer AST 가드 작성 (`src/` 전체 rglob) 중 `src/main.py::_insert_log_to_db` 동일 결함 가시화 → 직접 시정 (H2-bis). 사이클 60 hotfix (mock_get_balance 누락) + 사이클 64 hotfix (scheduler emit 누락) 패턴 답습 = 단위 가드 + 통합 가드 + AST 정적 가드 3 중 silent 결함 차단. 6 사이클 (60/61/64/65 + hotfix 4 영역) 답습 패턴 영속. system_logs timestamp KST 시정으로 운영 로그 시각 가독성 + 사이클 53 KST 컨벤션 일관성 회복 (사이클 47~65 누적 UTC 잔존 17 사이클 이후 영구 시정). **카드 #17/#18 신규 발의** = 잔여 UTC 결함 영역 영구 기록 (사이클 67+ 인계). 사이클 49→65 누적 17 사이클 + hotfix 4 영역 = 안전성 회수 + 운영 가시성 회복. 사이클 60/64/65 hotfix 답습 = silent 결함 영구 차단의 본질적 효과 (silent → 가시화 → AST 영구 차단) 4 사이클 누적 검증. |
| 2026-06-06 | 사이클 65 거래대금 동행 필터 — scanner 단계 작전주 차단 보강 (Q7-5 사이클 64 자문 인계, 사이클 62 갭상승 회피 효과 폐기 보강, domain-expert 옵션 A 자문 Q1~Q5 + Q6-1~Q6-4 전부 적용 6 사이클 연속) | **사이클 62 갭상승 회피 효과 폐기 보강** (Q7-5 사이클 64 자문 인계 HIGH 즉시 발주 확정). 사용자 의도: 가격만으론 작전주 차단 불충분 (예: 5,000원 통과 + 거래대금 5천만 = 작전 신호). domain-expert 자문 옵션 A 전부 적용 — **사이클 55 R-1 / 60 / 62 / 63 / 64 / 65 6 사이클 연속 패턴 일관**. **사이클 64 패턴 100% 답습**: `_collect_protected_tickers_for_scanner` 헬퍼 100% 재사용 + 60s TTL 캐시 (`time.monotonic`, 사이클 56-E 답습) + invalidate 즉시 무효화 (**Q7-1 unsubscribe 0 발화 영속, KIS LMS chain 차단**) + DailyEmitCap 1회/ticker/일 + daily_summary + reset_daily 동행 + AST keyword 의무 + 사이클 38 명문화 영속. **Q1 임계 (HIGH)**: 디폴트 0 (비활성) + UI 0~100억 step 1억 + 권장값 마커 1억/5억/10억 (team-leader 1차 0~5,000억 → 도메인 반박 채택, 100억 이상 비현실 — 시장 미시구조 근거: KOSPI 대형주 1,000~5,000억 / KOSDAQ 대형주 100~500억 / 작전주 10~50억). **Q2 데이터 소스 (HIGH 핵심) — 옵션 C 통합 폴백** (team-leader 1차 옵션 A 반박): (1) `scanner.ticker_market_info["trade_amount_raw"]` (신규 키, 원 단위 정밀값) 1순위 — scanner 가 이미 `fetch_rising_stocks::fetch_stock_detail` 호출 + `acml_tr_pbmn` 보강 중이라 **KIS 호출 0건 추가** (KIS MCP §8 검증: 단위 원 ₩ 확정, CTPF1002R 응답 필드 확정 불가 → graceful 통과 필수) (2) `stock_master.raw.acml_tr_pbmn` 2순위 fallback (3) 둘 다 miss = 0 반환 graceful 통과 (Q6-1 09:00 race 영속, 시스템 매매 무용 차단). **신규 키 호환**: `ticker_market_info[ticker]` 에 `trade_amount_raw` 추가 + 기존 `trade_amount` (억 단위 round) 호환 보존 — UI/API 응답 영속. **Q3 순차 hook**: `_apply_price_filter` (사이클 64) → `_apply_trade_amount_filter` (사이클 65) 순차 호출 — `subscribe_filtered_stocks` 4 호출 (tickers + extra_tickers + breakout/momentum for loop + swing 명시 분리, G-1 호출 카운트 4 의무). **Q4 헬퍼 재사용**: `_collect_protected_tickers_for_scanner` 100% 재사용 + 60s TTL **별도 캐시** (단일 책임, 무효화 시점 다름). **Q5 별도 prefix**: `[trade_amount_filter_scanner_skip] ticker=... acml_tr_pbmn=... reason=below_min min=...` INFO + `[trade_amount_filter_scanner_daily_summary] block_count=N` 1행. **Q6-1 (HIGH) 09:00 race graceful 영속**: `acml_tr_pbmn=0` (양쪽 miss) = graceful 통과 — 09:00 직후 누적 거래대금 0 시 후보 차단 = 시스템 매매 무용 위험 영구 차단. **Q6-2 (MEDIUM)** 우선주/ETF/ETN/신주인수권 영향 0 (기존 `isdigit()` + `ETF_KEYWORDS` 가드 영속). **Q6-3 (LOW)** 시간대별 임계 차별 비채택 (NXT = 매수 회피 시간대). **Q6-4 (MEDIUM)** 후보 풀 폭축 역설 risk → **사이클 67+ 신규 카드 #16 발의** (2주 회고 + 임계 미세 조정 인계). **funnel `step_no=97`** (사이클 64 step_no=98 보다 *전* 단계 표기). **사이클 64 hotfix 패턴 답습 (H-2 NO_TRY)**: `scheduler._settle()` 본문 try/except *후* `await _scanner_mod.emit_trade_amount_filter_scanner_daily_summary()` 직접 호출 **try/except 금지** (사이클 64 G-3 답습) + `_reset_daily_state()` 동행 `_scanner_mod.reset_trade_amount_filter_daily_state()` 호출. **신규 5 함수** (scanner.py +234L): `_get_trade_amount_filter_for_scanner` (60s TTL) + `invalidate_trade_amount_filter_cache_scanner` (Q7-1 unsubscribe 0) + `reset_trade_amount_filter_daily_state` + `_get_acml_tr_pbmn` (Q2 옵션 C 3 분기 — `ticker_market_info` 1순위 / `stock_master.raw` 2순위 / 양쪽 miss 0 반환 graceful) + `_apply_trade_amount_filter` (Q1 옵션 D 3중 안전망 + Q6-1 graceful + funnel step_no=97 + emit cap) + `emit_trade_amount_filter_scanner_daily_summary`. **모듈 전역 4 필드**: `_trade_amount_filter_cache: TradeAmountFilter | None` / `_trade_amount_filter_cache_expires_at: float` / `_trade_amount_filter_scanner_skip_logged_today: DailyEmitCap[str]` / `_trade_amount_filter_scanner_skip_count_today: dict[str, int]`. **API 라우트** (`src/routes/system.py`): `GET /api/system/trade-amount-filter` + `PUT /api/system/trade-amount-filter` (`TradeAmountFilterUpdateRequest` `ConfigDict(extra="forbid")` 422 거부 + 음수 ValueError 400). 라우트는 module-reference import 패턴 (`import src.db.system_config as _system_config_mod`) — 테스트 isolation (`test_mcp_client_lifecycle.py` 순서 contamination 회피). **UI** (`frontend/src/components/TradeAmountFilterCard.tsx` 167L 신규): Settings 화면 `PriceFilterCard` 옆. 4 testid (`trade-amount-filter-card` / `trade-amount-filter-min-slider` 0~100억원 step 1억원 / `trade-amount-filter-save-button` / `trade-amount-filter-save-toast`) + 권장값 마커 3 (1억/5억/10억) + 안내 배너 (Q6-1 명시 의무: "**거래대금 동행 필터** — 임계 미만 종목은 WebSocket 구독 자체 차단. 보유/익일청산 종목은 절대 제외 안 됨. **09:00 직후 거래대금 미반영 종목은 graceful 통과**"). React Query `useQuery / useMutation` + queryKey `['tradeAmountFilter']` + axios + MSW. TypeScript `TradeAmountFilter` / `TradeAmountFilterUpdate` 인터페이스 (`frontend/src/types/trade-amount-filter.ts` 신규 20L). API client (`frontend/src/api/trade-amount-filter.ts` 신규 35L). MSW handler 갱신 (`handlers.ts` +14L). **회귀 가드 31 함수 (28 케이스 / 12 파일 분리, HIGH 3 = 11%, B-5/G-1/H-2 함수 분리)**: A system_config 단일 키 3 (LOW) + B `_apply_trade_amount_filter` 본체 4 (MEDIUM + LOW) + B-5 옵션 C 통합 폴백 1 함수 → 3 분기 함수 (LOW) + **C 보유/익일청산 보호 2 HIGH** (사이클 64 답습) + D 60s TTL + Q7-1 unsubscribe 0 2 + E DailyEmitCap 2 + F integration 4 + **G AST keyword + 호출 카운트 1 → 2 함수 HIGH** + H scanner daily_summary 1 + **H-2 scheduler 통합 + AST 1 → 3 함수** + C-Route 1 → 3 함수 + F-FE 프론트 4 + **I integration (Q6 자문 신규) 2** (I-1 옵션 C 정합성 + I-2 Q6-1 09:00 race). HIGH 3 (C 2 + G 1) / MEDIUM 8 / LOW 17. **백엔드 1939 → 1970 PASS** (+31 = 사이클 65 신규 26 + **사이클 64 vacuous PASS 5 진정한 PASS 전환**) / 프론트 166 → **170 PASS** (+4) / 전체 **2140 PASS / 2 skipped / 0 FAIL** / coverage 80.93% (+0.14%p) / **flakiness 0** (3 회 1136 PASS / 35.81s / 35.73s / 35.72s ±0.1s 안정). **사이클 64 vacuous PASS 진정한 PASS 전환 (부가 효과)**: 사이클 65 hook 추가로 사이클 64 G-2 keyword 호출 + H-2 NO_TRY 모두 호출 발생 → vacuous PASS → 진정한 PASS 전환. **tester Verify V1~V13 + V-Scanner + V-AST + V-TradeAmount ALL PASS**: V1~V4 카테고리 분리 측정 (unit/engine 1136 + non-engine + routes 524 + integration 164 + contract 146 = 1970), V5~V9 회귀 가드 130/130 PASS (사이클 60/61/63/64 모두 영속), V10 flakiness 3 회 PASS, V11 coverage 80.93%, V12 백엔드↔프론트 정합 100% (`ApiResponse<TradeAmountFilter>` 래퍼), V13 매도 영향 0 (`order_engine.py` + `risk.py` diff 0건), **V-Scanner 사이클 64 + 사이클 65 hook 4 + 4 영속**, **V-AST G-3 + H-2 + G-1 호출 카운트 영속**, **V-TradeAmount Q6-1 09:00 race + Q2 옵션 C 3 분기 + 신규 키 + KIS 호출 0 모두 PASS**. **매매 안전성 무영향** (디폴트 0 비활성 + 사이클 38 명문화 매수 진입 전용 + 사이클 32 R4 보호 영속). **누적 효과**: scheduler.py 3,007 → 3,017 (+10, H-2 emit + reset 동행) / scanner.py +234L (5 함수 + 4 모듈 전역 + 4 hook). **사이클 62 갭상승 회피 효과 폐기 보강 평가** (Q7-5 인계, domain-expert 정성 평가): 작전주 시나리오 80~90% 회복 (신규 상장 작전주 Y 거래대금 5천만 = 차단 ✅ / 기존 상장 갭상승 작전주 V 거래대금 3억 = 차단 ✅ / 정상 IPO Z 거래대금 200억 = 통과 ✅ / 정상 우량주 U 거래대금 500억 = 통과 ✅). 잔존 10~20% (흥행 IPO + 거래대금 충분한 시세조작) = 시스템 본질 한계 (운영자 화이트리스트 + 감시 시스템 영역). **운영 2주 후 정량 측정 의무** (사이클 65 활성화 후 차단 빈도 + 사이클 62 폐기 효과 회복 검증). **다음 사이클 권고**: 사이클 66 = 카드 #5 (HIGH) cap=10 결함 시정 (사이클 63 발견). 사이클 67+ = 카드 #14 (MEDIUM) stale_manager.py 1,076L sub-module 분해 + **카드 #16 (MEDIUM, 신규 발의)** Q6-4 후보 풀 폭축 역설 risk 2주 회고. 사이클 68+ = 카드 #15 (LOW) Q7-2 액면분할 prdy_clpr invalidate. 사이클 69+ = 카드 #11/#12 보드 mutex / 14 모듈 logger. | `src/db/system_config.py` (TradeAmountFilter Pydantic + `get_trade_amount_filter` / `set_trade_amount_filter` + 단일 키 + 음수 ValueError) / `src/engine/scanner.py` +234L (ticker_market_info[trade_amount_raw] 신규 키 + 5 신규 함수 + 4 모듈 전역 + subscribe_filtered_stocks 4 hook 추가, `isdigit()` 검사 *전* ticker_market_info 채움 핵심 수정) / `src/engine/scheduler.py` +10L (`_settle` 직전 H-2 NO_TRY emit + `_reset_daily_state` 동행 reset) / `src/routes/system.py` (TradeAmountFilterUpdateRequest + GET/PUT + module-reference import 패턴) / `frontend/src/types/trade-amount-filter.ts` 신규 20L / `frontend/src/api/trade-amount-filter.ts` 신규 35L / `frontend/src/components/TradeAmountFilterCard.tsx` 신규 167L (4 testid + 권장값 마커 3 + Q6-1 안내 배너) / `frontend/src/pages/Settings.tsx` (+5L, PriceFilterCard 옆) / `frontend/src/test/handlers.ts` (+14L MSW handler) / `tests/unit/db/test_cycle65_trade_amount_filter_config.py` 신규 (A 3) / `tests/unit/engine/scanner/test_cycle65_apply_trade_amount_filter.py` 신규 (B 4) / `tests/unit/engine/scanner/test_cycle65_get_acml_tr_pbmn_fallback.py` 신규 (B-5 3 분기) / `tests/unit/engine/scanner/test_cycle65_trade_amount_filter_protected.py` 신규 (C 2 HIGH) / `tests/unit/engine/scanner/test_cycle65_trade_amount_filter_cache.py` 신규 (D+E 4) / `tests/unit/engine/scanner/test_cycle65_trade_amount_filter_ast_guards.py` 신규 (G 2 HIGH) / `tests/unit/engine/scanner/test_cycle65_daily_summary.py` 신규 (H 1) / `tests/unit/engine/test_cycle65_scheduler_integration.py` 신규 (H-2 + AST 3) / `tests/unit/routes/test_cycle65_trade_amount_filter_route.py` 신규 (C-R 3 함수) / `tests/integration/test_cycle65_trade_amount_filter_e2e.py` 신규 (F 4) / `tests/integration/test_cycle65_trade_amount_filter_option_c_race.py` 신규 (I 2) / `frontend/src/components/__tests__/TradeAmountFilterCard.test.tsx` 신규 (F-FE 4) / `src/engine/CLAUDE.md` + `src/db/CLAUDE.md` + `frontend/CLAUDE.md` 동기화 / `_workspace/cycle65_trade_amount_filter_design_card.md` v2 + `_workspace/cycle65_trade_amount_filter_domain_consult.md` (§8 KIS MCP 검증 보강) + `_workspace/cycle65_trade_amount_filter_domain_response.md` + `_workspace/red/cycle65_trade_amount_filter.md` 발주/자문/Red 명세 영구 기록 | Q7-5 (사이클 64 자문) HIGH 즉시 발주 확정 인계 — 사이클 62 갭상승 회피 효과 폐기 위험 보강. 사이클 64 패턴 직답습 + Q2 옵션 C 통합 폴백 (KIS 호출 0건 핵심 발견 — `fetch_rising_stocks` 가 이미 `acml_tr_pbmn` 보강 중) + Q6-1 09:00 race graceful (시스템 매매 무용 차단) + Q1 임계 도메인 반박 (0~100억 step 1억, 100억 이상 비현실). 사이클 60 hotfix 카테고리 분리 측정 + flakiness 3 회 패턴 영속 — 결함 0건 발견. **사이클 64 vacuous PASS 5 진정한 PASS 전환 (부가 효과)** = 사이클 65 hook 추가로 사이클 64 G-2 + H-2 호출 발생 → 진정한 PASS. **사이클 62 갭상승 회피 효과 폐기 보강 80~90%** (작전주 차단 + 정상 IPO/우량주 보존). 잔존 10~20% 시스템 본질 한계 (운영자 화이트리스트 영역). 운영 2주 후 정량 측정 의무. **카드 #16 (MEDIUM, Q6-4 신규 발의)** 후보 풀 폭축 역설 risk 2주 회고 — 사이클 67+ 발의. domain-expert 자문 6 사이클 연속 옵션 A 채택 패턴 영속. 사이클 49→65 누적 17 사이클 (refactor-review 임계 5회 *3.4 배 초과*). 운영 검증 인계: V-1 알람 임계 (사이클 57) / 가격 필터 + 거래대금 필터 운영 데이터 2주 (사이클 64+65) / **사이클 63 A3 K stale watcher 월요일 1h tester verify 의무** (별도) + 사이클 65 디폴트 0 비활성 영속 확인 + 20:10 정산 직전 `[trade_amount_filter_scanner_daily_summary]` 1행 INFO + system_logs INSERT 영속 확인. |
| 2026-06-06 | 사이클 64 가격 필터 scanner 이전 + 단순화 (사용자 의도 재정의 — WebSocket 구독 *전* 종목 풀 차단, 사이클 62 매수 신호 판단용 위치 폐기 + 3 모드 폐기 + 단순 임계 필터) | **사용자 의도 재정의** ("가격필터가 매매신호 발생여부 판단용으로 있는듯해. 각 종목 필터링의 가장 첫번째에 존재해서 웹소켓 구독에 들어가지 않도록 하는게 가격필터의 목적이야") → 사이클 62 위치 변경 + 단순화 + UI 단순화 단일 사이클. **domain-expert 옵션 A 자문 (Q1~Q6 + Q7-1~Q7-5) 전부 적용** (사이클 55 R-1 / 60 / 62 / 63 / 64 의 5 사이클 연속 옵션 A 패턴 일관). **Q1 옵션 D 3 중 안전망 (HIGH)**: (1) `_collect_protected_tickers_for_scanner` 공통 헬퍼 (`registry.all().positions` ∪ `_pending_next_day_clear` 합집합, 사이클 32 R4 universe guard 답습) (2) `_apply_price_filter` 최상단 early-return (`if ticker in protected_tickers: survived.append(ticker); continue`) (3) AST `protected_tickers=` keyword 의무 가드 (G-2 정적 검증). **Q2 자문 (current_price fallback 폐기)**: `stock_master.raw.prdy_clpr` 단독 (scanner = WS 구독 *전*, current_price 미확보) + 미확보 graceful 통과 (사이클 32 R4 답습, 신규 상장 영향 0) + KIS `inquire-price` pre-fetch **비채택** (Rate Limit + hot path 부담). **Q4 자문 옵션 A `subscribe_filtered_stocks` 단일 hook (HIGH)**: 호출자 시그너처 변경 0 + 신규 전략 추가 누락 차단. 옵션 B (6 전략 분산) **AVOID**. `_apply_price_filter(candidates, *, protected_tickers)` 호출 3건 전수 keyword 의무. **Q4 모드 폐기**: 사이클 62 3 모드 (HARD/WARN/OFF) → 사이클 64 단순 임계 필터 (mode 키 + `PriceFilterMode` 타입 + 모드 토글 UI 전부 폐기). DB 키 2 종 (`price_filter_min` / `price_filter_max`) 만 유지. **Q5 즉시 반영 + 60s TTL 영속**: `scanner.invalidate_price_filter_cache_scanner()` 즉시 호출 (사이클 62 `risk_manager.invalidate_price_filter_cache` 폐기 + 위치 변경). **Q6 H scanner 이전**: `[price_filter_scanner_daily_summary]` 신규 prefix + `_settle()` 직전 1행 INFO. **Q7-1 자동 unsubscribe 0 발화 (HIGH)**: invalidate_cache 가 unsubscribe 호출 0건 — 다음 `_scan_loop` 5분 자연 delta 위임 (사이클 17 OPSP0002 KIS LMS chain 차단 답습). **Q7-2 LOW 후속 카드 #15 발의**: 액면분할 prdy_clpr invalidate (별개 사이클 인계). **Q7-3 데이터 손실 0** (디폴트 0/0 영속). **Q7-4 funnel `step_no=98` hook**: `strategy_funnel_snapshots` step_no=98 INSERT (graceful). **Q7-5 사이클 65 거래대금 동행 필터 즉시 발주 확정 (HIGH)**: 사이클 62 갭상승 회피 효과 영구 폐기 위험 보강 — `_workspace/cycle65_trade_amount_filter_domain_consult.md` 자문 의뢰서 사전 작성 완료. **사이클 62 폐기 (G3 + 9 파일 삭제)**: `src/engine/risk.py` -178L (L19 import + L30 상수 + L54-62 7 필드 + L73-78 reset + L213-246 on_tick 분기 + L266-381 헬퍼 4 메서드 전부 폐기). 백엔드 사이클 62 9 파일 (33 케이스) + 프론트 F-5 mode 토글 1 케이스 = **34 케이스 git rm 정리**. **신규 위치** (`src/engine/scanner.py` +250L): `_collect_protected_tickers_for_scanner()` 모듈 공개 함수 + `_get_price_filter_for_scanner()` 60s TTL 캐시 (`time.monotonic`, 사이클 56-E 답습) + `invalidate_price_filter_cache_scanner()` (unsubscribe 0 발화) + `_apply_price_filter(candidates, *, protected_tickers)` 본체 (Q1 옵션 D 3 중 안전망) + `_price_filter_scanner_skip_logged_today: DailyEmitCap[str]` 모듈 전역 + `_price_filter_scanner_skip_count: dict[str, int]` (`count`) + `reset_price_filter_daily_state()` 모듈 공개 함수 + `emit_price_filter_scanner_daily_summary()` 일일 집계 + `subscribe_filtered_stocks()` 첫 줄 hook 추가 (3 호출처: tickers / extra_tickers / priority_groups[*]). **scheduler.py 통합** +13L: `_settle()` 직전 daily_summary emit 호출 (hotfix 후 = `_scanner_mod.emit_price_filter_scanner_daily_summary()`) + `_reset_daily_state()` 동행 `scanner.reset_price_filter_daily_state()` 호출. **routes/system.py 갱신**: `PriceFilterUpdateRequest.mode` 필드 제거 + `ConfigDict(extra="forbid")` (CR-2 mode 거부 422) + invalidate 호출 `risk_manager` → `scanner` 변경. **UI 단순화** (`PriceFilterCard.tsx` -88L): mode select 제거 (`price-filter-mode-select` testid 폐기, 9 → 7 testid) + 안내 배너 갱신 "**WebSocket 구독 대상 필터** — 임계 외 종목은 시세 구독 자체 차단. 보유/익일청산 종목은 절대 제외 안 됨" (사이클 32 R4 보호 영속 명시) + 한글 UX 유지. **회귀 가드 28 케이스 (12 파일 분리, HIGH 8 = 29%)**: A system_config 단순화 4 (mode 인자 제거) + B scanner 필터 적용 5 + **C 보유/익일청산 보호 4 HIGH** (옵션 D 3 중 안전망: C-1 보유 / C-2 익일청산 / C-3 합집합 / C-4 헬퍼 단독) + D 60s TTL 캐시 2 (D-2 Q7-1 unsubscribe 0) + E DailyEmitCap 2 + F integration 4 (F-4 funnel step_no=98) + **G AST 2 HIGH** (G-1 risk.py 17 금지 문자열 0 + G-2 protected_tickers keyword 의무) + H daily_summary 1 + **H-2 (사이클 64 hotfix HIGH)** scheduler 통합 가드 AST + **G-3 (사이클 64 hotfix HIGH)** 폐기 메서드 src/ 전체 0건 AST + C-Route 2 + F-FE 4 (mode 토글 폐기). **사이클 64 hotfix (tester verify 결함 1건 발견 후)**: 결함 위치 `scheduler.py:638-641` = 사이클 62 폐기 메서드 `_emit_price_filter_daily_summary` 잔존 호출 → AttributeError graceful skip → scanner 신규 메서드 호출 0건 → 운영자 가시화 무력화. 2 줄 hotfix (`from src.engine import scanner as _scanner_mod; await _scanner_mod.emit_price_filter_scanner_daily_summary()`) + log prefix 갱신 (`[price_filter_daily_summary]` → `[price_filter_scanner_daily_summary]`). **H-2 + G-3 영구 가드** (사이클 60 hotfix 패턴 답습): 단위/통합/AST 3 중 가드로 silent 결함 영구 차단. **tester verify V1~V13 + V-Scanner + V-AST ALL PASS** (재검증 포함): V1~V4 카테고리 분리 측정 (unit/engine 1117 + non-engine 518 + integration 158 + contract 146 = 1939 백엔드) + V5~V9 회귀 가드 6 영역 모두 PASS + **V10 flakiness 차단 3 회 PASS** (1117 PASS / 35.70s / 35.81s / 35.73s ±0.11s 안정 — 사이클 58/60/61/62/63 패턴 영속) + V11 coverage 80.79% (+0.03%p) + V12 백엔드↔프론트 정합 100% (mode 필드 제거 후) + V13 매도 영향 0 (`order_engine.py` 변경 0건, risk.py 가격 필터 폐기 + 다른 분기 무변경) + **V-Scanner H 영역 H-1 + H-2 PASS** (scanner 단독 emit + scheduler 통합 호출 영속) + **V-AST G-1/G-2/G-3 PASS** (risk.py 17 금지 문자열 0 + protected_tickers keyword 의무 + 폐기 메서드 src/ 전체 호출 0건). **매매 안전성 무영향** (디폴트 0/0 영속 + 사이클 38 명문화 매수 진입 전용 + 사이클 32 R4 보호 영속). **누적 효과**: 백엔드 1944 → 1939 PASS (사이클 64 +28 - 사이클 62 -34 + hotfix +2 -1 = 정합) / 프론트 167 → 166 PASS / 전체 **2105 PASS** / coverage 80.79%. 사이클 62 운영 데이터 손실 0 (디폴트 OFF 영속, 사용자 임계 설정 전 운영 영향 0). **카드 #5 (HIGH, 사이클 63 발견)** cap=10 결함 시정 후순위로 밀림. **다음 사이클 권고**: 사이클 65 = Q7-5 거래대금 동행 필터 (HIGH 즉시 발주 확정, 사이클 64 헬퍼 재사용). | `src/db/system_config.py` (PriceFilter mode 폐기, 2 키 유지, mode sentinel/상수/`_get_str_or_default`/`_set_str` 폐기) / `src/engine/risk.py` -178L (가격 필터 영역 전체 폐기: L19/L30/L54-62/L73-78/L213-246/L266-381) / `src/engine/scanner.py` +250L (신규 6 함수 + 모듈 전역 2 + 진입 hook) / `src/engine/scheduler.py` +13L (settle 직전 hotfix scanner emit + reset 동행) / `src/routes/system.py` (mode 인자 제거 + `extra="forbid"` + invalidate 경로 변경) / `frontend/src/components/PriceFilterCard.tsx` -88L (mode select 폐기 + 안내 배너 갱신) / `frontend/src/types/price-filter.ts` (PriceFilterMode + mode 필드 제거) / `frontend/src/components/__tests__/PriceFilterCard.test.tsx` (F-5 mode 토글 폐기, 5→4 케이스) / `tests/unit/db/test_cycle64_price_filter_scanner_system_config.py` 신규 (A 4) / `tests/unit/engine/test_cycle64_price_filter_scanner_apply.py` 신규 (B 5) / `tests/unit/engine/test_cycle64_price_filter_scanner_protected.py` 신규 (C 4 HIGH) / `tests/unit/engine/test_cycle64_price_filter_scanner_cache.py` 신규 (D 2) / `tests/unit/engine/test_cycle64_price_filter_scanner_emit_cap.py` 신규 (E 2) / `tests/unit/engine/test_cycle64_price_filter_scanner_ast.py` 신규 (G 2 + G-3 hotfix = 3 HIGH) / `tests/unit/engine/test_cycle64_price_filter_scanner_daily_summary.py` 신규 (H 1 + H-2 hotfix = 2 HIGH) / `tests/integration/test_cycle64_price_filter_scanner_integration.py` 신규 (F 4 + F-4 funnel) / `tests/contract/test_cycle64_routes_price_filter.py` 신규 (CR 2) / 사이클 62 9 파일 git rm (33 케이스 정리) / `src/engine/CLAUDE.md` + `src/db/CLAUDE.md` + `frontend/CLAUDE.md` 동기화 / `_workspace/cycle64_price_filter_scanner_design_card.md` v2 + `_workspace/cycle64_price_filter_scanner_domain_consult.md` + `_workspace/cycle64_price_filter_scanner_domain_response.md` + `_workspace/red/cycle64_price_filter_scanner.md` 발주/자문/Red 명세 영구 기록 / `_workspace/cycle65_trade_amount_filter_domain_consult.md` 신규 (Q7-5 인계) | 사용자 의도 재정의 (matching 결정 후 1 일만에 사이클 62 위치 변경 + 단순화) — 사이클 62 가 매수 신호 판단용 위치 (`risk.on_tick`) 였지만 사용자 의도 = WS 구독 *전* 종목 풀 차단 (자원 절약 + 후보 풀 축소). domain-expert Q4 자문 옵션 A 3 중 안전망 도입으로 보유/익일청산 절대 보호 영속 + Q7-1 KIS LMS chain 차단 (자동 unsubscribe 0 발화) + Q7-4 funnel step_no=98 가시화 + Q7-5 거래대금 필터 즉시 발주 (갭상승 회피 효과 영구 폐기 위험 보강). 사이클 60 hotfix 패턴 답습 (카테고리 분리 측정 + flakiness 3 회 반복) 으로 결함 1건 발견 (HIGH 운영 가시화 = scheduler hookup 누락) → backend-dev 2 줄 hotfix + tdd-engineer H-2/G-3 영구 가드 (AST 3 중 silent 결함 차단). tester V-Scanner + V-AST 신규 카테고리 도입 (사이클 64 hot path 영역 검증 분류). 매매 안전성 무영향 (사이클 38 명문화 + 사이클 32 R4 보호 + 디폴트 0/0). 사이클 62 폐기 (코드 -178L + 테스트 9 파일 33 케이스 + 프론트 F-5 1) 자연 정리 — 동일 도메인 위치 변경의 cleanup 의무. 사이클 49→64 누적 16 사이클 (refactor-review 임계 5회 *3.2 배 초과*) — 사이클 65 거래대금 필터 후 refactor-review 트리거 재평가 권고. 운영 검증 인계: 월요일 (2026-06-08) 09:00 첫 _boot 후 가격 필터 디폴트 0/0 영속 확인 + 20:10 정산 직전 `[price_filter_scanner_daily_summary]` 1행 INFO + system_logs INSERT 영속 확인 + 사이클 63 A3 K stale watcher 1h tester verify 의무 (별도). |
| 2026-06-06 | 사이클 63 Phase 2-A3 — refactor #2 stale_manager 추출 3단계 + **refactor #2 완료** (HIGH, K stale watcher 핵심 2 함수 322L, domain-expert 옵션 A 자문 Q1~Q5 전부 적용) | **K stale watcher 핵심 본체 2 함수 이주** (사이클 60 A1 5 + 사이클 61 A2 4 → 누적 11 함수): (1) `_check_and_resubscribe_stale` (222L, K stale watcher 본체 — 사이클 17 보강 1~5회 즉시 강제재등록 + 사이클 29-R1 6회 초과 force_retry 5분 cooldown + 시간당 12회 cap + 사이클 29-R3 HIGH/LOW 우선순위 분리 + 사이클 28 `_stale_last_resubscribe_at` 갱신 + `[stale_watcher_detail]` prefix 발화), (2) `_resubscribe_stale_priority` (100L, 사이클 25-B 5분 우선 재구독 분리 + `_scan_loop` 5분 주기 통합). 합 322L. **추가 상수 0** (사이클 60+61 누적 10 상수 모두 이전 완료). **K stale watcher = 영구 hot path 360 회/일 (120s 주기) + KIS LMS/앱키 정지 chain 직접 영역** — 사이클 60 응답서 §Q7 본질 차이 인지 별도 자문 의무. **domain-expert 자문 Q1~Q5 옵션 A 전부 적용**: Q1 RECOMMEND 8 단계 호출 순서 결정적 보존 (4/5/6/7/8 단계 + 5 단계 high_tickers for-loop *전* 단일 구성). Q2 RECOMMEND 풀 API 무변경 위임 + `try/except` 4 중 가드 *그대로* 보존 (`getattr` 폴백 silent 실패 위험으로 비채택). Q3 RECOMMEND 카테고리 H 4 케이스 freezegun + 1h verify 시나리오 A/B/C 도입. **Q4 = CONSIDER B (직접 호출, 사이클 60 답습하지 않는 *유일 영역*)**: `check_and_resubscribe_stale` 본체가 `_emit_stale_session_detail` 호출 시 `self.*` wrapper 우회 → `emit_stale_session_detail(scheduler, ...)` 모듈 함수 직접 호출 (1 hop 단축 + 사이클 61 backend-dev Green `_refresh_stale_ccnl_cache` → `evict_expired_ccnl` 패턴 일관). lazy import 빚 1 건 청산. Q5-1 무관 확정 (사이클 62 가격 필터 영향 0). **Q5-2 카드 #14 신규 발의**: stale_manager.py 1,076L 비대화 → 사이클 64+ sub-module 분해 (3+1 청사진: `stale_diagnostics` ~350L 진단/캐시 read-only / `stale_session_recovery` ~250L silent inactive + force_reconnect / `stale_universe_guard` ~150L universe 가드 / `stale_watcher_core` ~322L K stale watcher 본체). **Q5-3 카드 #5 신규 발의 (HIGH)**: backend-dev Green 단계 확인 — `_resubscribe_stale_priority` L2748 `targets = stale_tickers[:cap]` priority 분리 *전* `[:cap]` 적용 → HIGH 종목 (보유/익일청산) 이 sorted LOW 후보에 밀려 cap 밖 잘림 결함 영속. K-2 케이스 PASS = 결함 confirm 형태. **본 사이클 = 행위 보존 의무**, 시정은 사이클 64+ 별도 카드. 시정 안: priority 분리 *후* HIGH 먼저 + LOW 잔여 cap. **`sys.modules.get("src.engine.scheduler")` 패턴 일관 적용** (사이클 61 도입): `check_and_resubscribe_stale` / `resubscribe_stale_priority` 본체 모두 `kis_ws_pool` / `kis_ws` / `datetime` / `scanner` 접근 시 scheduler 네임스페이스 우선 참조 — D-1 AST 정적 가드 통과 + 테스트 patch 호환 + 운영 환경 동일 객체. **scheduler.py 2 wrapper 추가** (2~3 줄 lazy import + 위임, 사이클 51/60/61 패턴 일관). scheduler.py 3,315 → **3,007L** (−308L, −9.3%). stale_manager.py 731 → **1,076L** (+345L). **누적 scheduler.py 감소** (사이클 51 직전 ~4,185L → 사이클 63 후 3,007L): **−1,178L, −28%**. 누적 stale_manager.py: 358 (A1) → 731 (A2) → 1,076 (A3). **회귀 가드 29 케이스 (12 파일 분리, 사이클 60/61 패턴 답습)**: A 위임 4 (A-1 HIGH = Q4=B 직접 호출 검증 — `stale_manager.emit_stale_session_detail` mock 1 회 + `scheduler._emit_stale_session_detail` mock 0 회) + C property 호환 3 (사이클 60+61 영속 PASS) + D AST 의존성 역전 2 + E `_reset_daily_state` 동행 2 (사이클 48 자동 PASS) + F dataclass 누락 가드 2 (사이클 48 자동 PASS) + **G 1~5회 강제재등록 3 HIGH** (G-1 첫 stale unsubscribe→sleep→subscribe / G-2 retry=5 1~5 분기 영속 / G-3 `_stale_last_resubscribe_at` 갱신) + **H 6회 초과 force_retry 4 HIGH (freezegun, 사이클 29-R1 핵심)** (H-1 `last_at` 부재 즉시 시도 / H-2 age=300s cooldown 경과 강제 재시도 + 카운터 0 / H-3 age=120s cooldown 미경과 skip / H-4 hourly cap=12 초과 WARNING + skip) + **I 우선순위 분리 3 HIGH (사이클 29-R3 핵심)** (I-1 positions = HIGH+bypass=True / I-2 `_pending_next_day_clear` = HIGH+bypass=True / I-3 그 외 = LOW+bypass=False) + J history 60분 윈도우 2 (freezegun) + K cap=10 영역 2 (**K-2 PASS = Q5-3 결함 confirm 영속**) + L caplog logger binding 1 (사이클 60 I1 영구 가드 답습) + M import sanity 1. **HIGH 11 (38%) 전수 PASS**. **백엔드 1915 → 1944 PASS** (+29 신규, 회귀 0건). **사이클 29 005935 사고 패턴 차단 영속 검증** (H 4 케이스 freezegun 재현 — T+0 stale 시작 → T+18min 8 분 영구 잔류 + KIS LMS chain 차단 영속). **tester Verify V1~V13 + V-A3 + V-1h-prep ALL PASS**: V1~V4 카테고리 분리 측정 (unit/engine 1119 + non-engine 519 + integration 159 + contract 147 = 1944), V5~V9 회귀 가드 7 영역 (사이클 63 29 + 62 33 + 61 20 + 60 18 + 28 4 + 기존 stale 45 + backend-dev patch 수정 15 = 164/164 PASS), **V10 flakiness 차단 3 회 반복 PASS** (35.54s / 35.47s / 36.62s ±1.15s 안정), V11 coverage 81.06% (+0.10%p), V12 백엔드 only (skip — 리팩토링 행위 보존), V13 매도 영향 0 (`order_engine.py` 변경 0건, stale watcher = 시세 영역, 사이클 38 명문화 영속), **V-A3 HIGH 11 케이스 전수 PASS** (A-1 + G 3 + H 4 + I 3), V-1h-prep 월요일 시나리오 A/B/C 사전 명세 점검 (전체 적절). **backend-dev 신규 발견 + 시정 (Green 단계)**: 기존 patch 경로 깨짐 2 파일 (`tests/unit/engine/test_scheduler_stale_force_retry.py` + `test_scan_loop_stale_priority.py`) — A3 이주 후 `src.db.system_logs.write_log` 추가 patch 필요 (A3 함수가 stale_manager 네임스페이스에서 write_log 호출) → 즉시 수정 (15 케이스 회귀 0). **매매 안전성 무영향** (행위 보존 리팩토링 + K stale watcher 본체 322L 그대로 복붙 + self→scheduler 치환만). sub-module 분해 (카드 #14) 와 cap=10 결함 시정 (카드 #5) 은 본 사이클 범위 *밖* — 행위 보존 의무. **월요일 (2026-06-09) 09:00~10:00 1h tester 운영 환경 직접 verify 의무** (사이클 60 §Q7 영속): 시나리오 A 자연 stale monitoring (1h 의무, K stale watcher ~30 사이클) + 시나리오 B 인위 stale 주입 (자연 발생 0 시 발화, **보유 0 종목만 의무, 보유/익일청산 절대 금지**) + 시나리오 C KIS LMS chain 차단 monitoring (`[stale_force_retry_cap]` WARNING 0 건 기대). **다음 사이클 권고**: (a) 사이클 64 = 카드 #5 (HIGH, cap=10 결함 시정) — Q5-3 결함 confirm 후 시정 우선, (b) 사이클 65+ = 카드 #14 (MEDIUM, sub-module 분해) — stale_manager.py 1,076L → 3+1 분해, (c) 사이클 62 가격 필터 운영 1~2 영업일 데이터 축적 후 카드 #13 거래대금 동행 필터 (사이클 64+ 별개). | `src/engine/stale_manager.py` +345L (731→1,076, 2 함수 추가 + Q4=B 직접 호출 패턴) / `src/engine/scheduler.py` −308L (3,315→3,007, 2 wrapper 위임) / `tests/unit/engine/test_cycle63_phase2A3_*.py` 12 파일 신규 (29 케이스, HIGH 11 / MEDIUM 8 / LOW 10) / `tests/unit/engine/test_scheduler_stale_force_retry.py` + `test_scan_loop_stale_priority.py` patch 경로 수정 (`src.db.system_logs.write_log` 추가) / `src/engine/CLAUDE.md` 모듈 맵 (9→11 함수) + 본문 사이클 63 행 + refactor #2 완료 표기 / `_workspace/test_index.yaml` 영향 인덱스 갱신 / `_workspace/cycle63_phase2A3_design_card.md` v2 + `_workspace/red/cycle63_phase2A3.md` + `_workspace/red/cycle63_phase2A3_stale_watcher_core.md` + `_workspace/cycle63_phase2A3_domain_consult.md` + `_workspace/cycle63_phase2A3_domain_response.md` 발주/자문/Red 명세 영구 기록 | refactor #2 stale_manager 3 단계 분할의 **마지막 단계 (HIGH)** = K stale watcher 핵심 본체 추출. 사이클 60 응답서 §Q7 본질 차이 인지 별도 자문 = K stale watcher = 영구 hot path 360 회/일 + KIS LMS/앱키 정지 chain 직접 영역 (A1/A2 진단/silent inactive/universe guard 와 본질 다름). 사이클 60 응답서 옵션 B 3 단계 분할의 **A1 (사이클 60 LOW 18 케이스) + A2 (사이클 61 MEDIUM 20 케이스) + A3 (사이클 63 HIGH 29 케이스) = 67 회귀 가드 누적**. domain-expert 자문 옵션 A 전부 적용 패턴 영속 (사이클 55 R-1 / 사이클 60 / 사이클 62 / 사이클 63 — 4 사이클 연속). **Q4 = CONSIDER B (사이클 60 답습하지 않는 유일 영역)** = HARNESS_CHANGELOG 영구 명시 의무 — 사이클 61 backend-dev Green 패턴 답습 + 1 hop 단축 + lazy import 빚 1 건 청산. 사이클 29 005935 사고 패턴 차단 영속 (T+18min 영구 잔류 + KIS LMS chain) — 영구 hot path 영역 회복 가능성 영구 검증. tester 카테고리 분리 측정 + flakiness 3 회 반복 (사이클 58 V-2 + 사이클 60 hotfix 패턴) 영속 = backend-dev patch 경로 깨짐 2 파일 자동 발견 + 시정. V-A3 신규 도입 (HIGH 케이스 전수 우선) = 사이클 63 HIGH 38% 비중 영역 안전성 검증 신규 분류. V-1h-prep 신규 도입 (월요일 1h verify 시나리오 사전 명세 점검) = 사이클 60 §Q7 영속 의무의 운영 실측 인계. **사이클 64+ 후속 카드 인계 (tester 발견)**: 카드 #5 (HIGH) cap=10 결함 시정 + 카드 #14 (MEDIUM) sub-module 분해 — 본 사이클 행위 보존 의무 범위 *밖*, 다음 사이클 우선. 사이클 49→63 누적 **15 사이클** (refactor-review 임계 5회 *3 배 초과*) — refactor #2 완료 후 refactor-review 트리거 재평가 권고 (카드 #5 + #14 누적 시점). 매매 안전성 무영향 (행위 보존 + 매도 영역 무관). 운영 검증 인계: V-1 알람 임계 (사이클 57) / 가격 필터 운영 데이터 1~2 영업일 (사이클 62) / **A3 1h verify 시나리오 A/B/C (월요일 09:00~10:00)** 신규 추가. |
| 2026-06-05 | 사이클 62 가격 필터 — 매수 진입 전용 가격대 차단 신규 기능 (백엔드 + 프론트 단일 사이클, MEDIUM, domain-expert 옵션 A 자문 Q1~Q6 + Q7~Q11 전부 적용) | **신규 기능 1차 진입** — 사이클 49→61 12 사이클 누적 리팩토링/관찰성 강화 후 첫 신규 매매 정책 기능. 사용자 요청 (`설정정보에 가격 필터를 만들어보는 것 어떨까?`) → AskUserQuestion 2 질문 (양쪽 밴드 + scanner 적용) → team-leader 설계 카드 v1 → domain-expert 자문 (Q2 fallback / Q4 3 모드 반박) → 옵션 A 채택 → 설계 카드 v2 + Red 명세 → tdd-engineer 38 케이스 → backend-dev (G1~G5) + frontend-dev (G6) 병렬 → tester V1~V13 검증. **DB 스키마** (`src/db/system_config.py`): 3 키 추가 — `price_filter_min` (int, default 0 = 비활성) / `price_filter_max` (int, default 0 = 비활성) / `price_filter_mode` (str, default `OFF`, 3 모드 HARD/WARN/OFF). `PriceFilter` Pydantic 모델 + `get_price_filter()` / `set_price_filter(*, min_price=, max_price=, mode=)` 부분 갱신 (사이클 8 `buy_block_thresholds` 패턴 답습). 범위 외 ValueError (음수 / `min > max` 단 둘 다 >0 일 때 / 잘못된 mode). **적용 위치 (사이클 38 명문화 영속)**: `src/engine/risk.py::on_tick` 단일 진입점, `check_exit_signal` 분기 *후* + `check_buy_signal` *전* — 매도/익일청산/손절/15:20 강제청산 영향 0. **Q2 fallback** (domain-expert 자문 옵션 A — 반박 핵심): team-leader 1차 권고 = 당일 현재가 단독 → 자문 반박 = `prev_close` 우선 + `current_price` fallback (`scanner.ticker_prev_close.get(ticker)` 활용, 갭상승 시점 작전주 회피 결함 영구 차단). 둘 다 미확보 시 graceful 통과 (필터 미적용, 신규 상장 영향 0). **3 모드** (Q4 자문 옵션 A — 반박 핵심): team-leader 1차 권고 = HARD 단일 → 자문 반박 = HARD/WARN/OFF 3 모드 + 디폴트 OFF (시장 신뢰 형성 패턴 — WARN 1주 운영 후 HARD 전환). `mode=HARD` 범위 외 → 매수 차단 + `[price_filter_skip] ticker=... cmp_price=... reason=below_min/above_max mode=HARD min=... max=...` INFO + `_price_filter_skip_logged_today: DailyEmitCap[str]` 1회/ticker/일 cap. `mode=WARN` 범위 외 → 매수 진행 + `[price_filter_warn] ...` WARNING + 별도 cap. `mode=OFF` → 분기 미진입. **60s TTL 캐시** (Q5 자문, 사이클 56-E `BUY_BLOCK_CACHE_TTL` 답습): `RiskManager._get_price_filter_cached()` `time.monotonic()` 비교 + `invalidate_price_filter_cache()` 메서드. **즉시 반영 + 5분 grace 금지** (Q5 자문): `PUT /api/system/price-filter` 직후 `risk_manager.invalidate_price_filter_cache()` 즉시 호출 — 60s 캐시 도중 사용자 토글 즉시 반영 (사고 대응 지연 차단). **Q3 가드** (사용자 명시 필수): 시장가 거부 5호가 폴백 시 가격 필터 재평가 0건. `order_engine.py:L411` (매수 폴백) + `L664` (매도 폴백) 모두 `risk_manager._get_price_filter_cached()` 호출 0건 → `tests/unit/engine/test_cycle62_price_filter_sell_unaffected.py::E-4` AST 정적 회귀 가드 영속. `order_engine.py` 무변경 의무. **Q6 일일 집계** (자문 보강): `_settle()` 직전 `[price_filter_daily_summary] mode=... min=... max=... hard_skip=N warn_skip=M` INFO 1행. `_price_filter_skip_count: dict[str, int]` (`hard`/`warn`) `_reset_daily_state` 동행 reset. **API 라우트** (`src/routes/system.py`): `GET /api/system/price-filter` + `PUT /api/system/price-filter` (부분 갱신, ValueError → 400). 응답 = `ApiResponse<PriceFilter>` 래퍼 `{success, data, message}`. **UI** (`frontend/src/components/PriceFilterCard.tsx` 282L 신규): Settings 화면 `BuyBlockSection` 직하. 9 testid (`price-filter-card` / `price-filter-mode-select` 3 모드 select / `price-filter-min-slider` 0~20,000원 step 1,000 / `price-filter-max-slider` 0~2,000,000원 step 50,000 / `price-filter-save-button` / `price-filter-save-toast` / `price-filter-validation-error`) + 권장값 툴팁 (저 5,000원 / 고 1,000,000원 domain-expert Q1 자문) + 매수 진입 전용 안내 배너 + 클라이언트 검증 (음수 / max<min 단 둘 다 >0 일 때). React Query `useQuery / useMutation` + queryKey `['system', 'price-filter']` + axios + MSW. TypeScript `PriceFilter` / `PriceFilterUpdate` 인터페이스 (`frontend/src/types/price-filter.ts` 신규 19L). API client `getPriceFilter / updatePriceFilter` (`frontend/src/api/price-filter.ts` 신규 27L). **회귀 가드 38 케이스 (12 파일 분리, 사이클 60/61 패턴 답습)**: 백엔드 unit 25 (A system_config 5 + B risk.on_tick 4 + C emit cap 2 + D 60s 캐시 2 + **E 매도 영향 0 검증 4** 사이클 38 영속 + F Q2 fallback 5 + G WARN 모드 2 + H 일일 집계 1) + integration 5 (I) + contract 3 (C-Route) + 프론트 vitest 5 (F-FE). HIGH 0 / MEDIUM 3 (risk.on_tick / 60s 캐시 / Settings UI 즉시 반영) / LOW 35. **백엔드 1882 → 1915 PASS** (+33 신규, 회귀 0) / **프론트 160 → 167 PASS** (+7) / **전체 2082 PASS / 2 skipped / 0 FAIL** / coverage 80.96% (+0.3%p) / **flakiness 0** (3 회 반복 ±0.03s, 사이클 58 V-2 + 사이클 60 hotfix 패턴 영속). **tester V1~V13 ALL PASS**: V1~V4 카테고리 분리 측정, V5~V9 회귀 가드 6 영역 120 케이스, V10 flakiness 3 회 (35.34s / 35.37s / 35.37s), V11 CI 시뮬 (60s timeout + coverage), **V12 백엔드 `ApiResponse<PriceFilter>` 래퍼 ↔ 프론트 `data.data` 추출 정합성 100%** (`src/routes/system.py:121-166` ↔ `frontend/src/api/price-filter.ts:14-19` ↔ `factories.ts::wrap()` MSW + contract test 모두 매핑), **V13 매도 영향 0** (`git diff src/engine/order_engine.py` 0건 + E-1~E-4 + I-3/I-4 통합 영역 PASS). **매매 안전성 무영향** — 디폴트 mode=OFF + 매수 차단만 (자금 손실 영역 아님). Q3 가드 (시장가 폴백 무변경) + 사이클 38 명문화 (매수 진입 전용) 2 중 안전. **사이클 63 별개 카드 #13 신규 발의** (Q7 자문): 거래대금 동행 필터 (가격 X 거래량) — 작전주 차단 보강 (가격만으론 불충분). `_workspace/refactor/2026-06-04_review.md` 카드 #13 발의. **사이클 63 우선순위 재평가**: (a) Phase 2-A3 (HIGH, 주말 push) / (b) 카드 #13 거래대금 필터 (MEDIUM) / (c) 카드 #11 보드 전환 mutex / (d) 카드 #12 14 모듈 logger. **다음 사이클 권고**: 사이클 62 운영 1~2 영업일 데이터 축적 후 카드 #13 명세화 (가격 + 거래대금 동시 시뮬). Phase 2-A3 는 주말 push 의무이므로 토요일 새벽 별도 진행 가능. | `src/db/system_config.py` PriceFilter Pydantic + 3 키 헬퍼 / `src/engine/risk.py` on_tick 필터 분기 + 60s TTL 캐시 + emit 3종 + reset_daily 확장 / `src/engine/scheduler.py` `_settle` 직전 daily_summary 호출 / `src/routes/system.py` GET/PUT + invalidate 즉시 반영 / `frontend/src/types/price-filter.ts` 신규 19L / `frontend/src/api/price-filter.ts` 신규 27L / `frontend/src/components/PriceFilterCard.tsx` 신규 282L / `frontend/src/components/__tests__/PriceFilterCard.test.tsx` 신규 5 케이스 / `frontend/src/pages/Settings.tsx` (+3, BuyBlockSection 직하 통합) / 백엔드 테스트 10 파일 신규 (`tests/unit/db/test_cycle62_price_filter_system_config.py` 5 + `tests/unit/engine/test_cycle62_price_filter_*.py` 8 파일 20 + `tests/integration/test_cycle62_price_filter_integration.py` 5 + `tests/contract/test_cycle62_routes_price_filter.py` 3) / `src/db/CLAUDE.md` + `src/engine/CLAUDE.md` + `frontend/CLAUDE.md` 동기화 / `_workspace/cycle62_price_filter_design_card.md` v2 + `_workspace/red/cycle62_price_filter.md` Red 명세 영구 기록 | 사용자 요청 신규 매매 정책 기능 1차 진입 — 사이클 49→61 12 사이클 누적 리팩토링/관찰성 강화 후 첫 신규 기능. domain-expert 자문 옵션 A 전부 적용 (Q2 fallback / Q4 3 모드 = team-leader 1차 권고 반박 영역 모두 채택). 사이클 38 명문화 (매수 진입 전용) + Q3 가드 (order_engine 폴백 무변경) 2 중 안전으로 매도/익일청산/손절 영향 0 절대 보장. 사이클 56-E `BUY_BLOCK_CACHE_TTL` 60s 패턴 + 사이클 56-D `RiskManager.reset_daily_state()` 캡슐화 답습으로 인프라 일관성 보존. 사이클 58 V-2 + 사이클 60 hotfix flakiness 차단 패턴 (카테고리 분리 측정 + 3 회 반복) 영속 — backend-dev tdd 코드 mock 버그 1건 (`mocker.patch` → `monkeypatch.setattr`) 발견 + 수정. tester V12 신규 인터페이스 정합성 검증 영역 도입 — 백엔드 ApiResponse 래퍼 ↔ 프론트 `data.data` 추출 정합 100% 확인 (사이클 62 신규 카테고리). 매매 안전성 무영향 (디폴트 OFF + 매수 차단만 = 자금 손실 위험 0). Q7 거래대금 동행 필터 = 사이클 63 카드 #13 신규 발의 (가격 + 거래대금 동시 필터링 = 작전주 차단 보강). Phase 2-A3 (HIGH, 주말 push 의무) 잔여 — 사이클 63 vs 64 우선순위 사용자 결정 대기. 사이클 49→62 누적 14 사이클 (refactor-review 임계 5회 *2.8배 초과*). 운영 검증 인계: V-1 알람 임계 (사이클 57) / 가격 필터 운영 데이터 1~2 영업일 축적 후 임계 디폴트 재평가 (Q1 자문 권장값 5,000/1,000,000 적용 여부 운영자 결정). |
| 2026-06-05 | 사이클 61 Phase 2-A2 — refactor #2 stale_manager 추출 2단계 (MEDIUM, 4 함수 314L + 5 상수 추가 이주, 사이클 60 A1 패턴 직답습) | **stale_manager.py 4 함수 추가 이주** (사이클 60 A1 5 함수 → 누적 9 함수): (1) `_detect_silent_inactive_sessions` (62L, 사이클 29-R2 silent inactive 3중 가드 — `fresh_ratio < SILENT_INACTIVE_FRESH_RATIO_THRESHOLD(=0.2)` + `subscribed >= 5` + 5분 지속), (2) `_force_reconnect_session` (83L, 사이클 24 세션 강제 reconnect — **KIS LMS/앱키 정지 위험 직접 영역**, 시간당 세션당 2회 cap), (3) `_delta_unsubscribe_dropped` (52L, 사이클 15-A KIS "비정상 케이스 2" 차단 패턴), (4) `_evaluate_universe_guard` (117L, 사이클 32 R4 stale>5 + low_volume 자동 unsubscribe + 보유/익일청산 절대 보호). **5 상수 추가 이전 + scheduler re-export** (사이클 60 A1 5 → 누적 10): `STALE_FRESHNESS_SECS=60` (사이클 17) + `SILENT_INACTIVE_MIN_SUBSCRIBED=5` / `SILENT_INACTIVE_PERSIST_SECS=300.0` / `SILENT_INACTIVE_RECOVERY_CAP_PER_HOUR=2` / `SILENT_INACTIVE_RECOVERY_WINDOW_SECS=3600.0` (사이클 24/29-R2). re-export 로 `from src.engine.scheduler import STALE_FRESHNESS_SECS` 등 외부 import 호환 + `is` 동일성 보장. **사이클 60 lazy import 빚 청산**: `_build_session_subscription_view` 의 `from src.engine import scheduler as _sched_mod` 1 줄 제거 — `STALE_FRESHNESS_SECS` 본체 이전으로 자연 해소. **`sys.modules.get("src.engine.scheduler")` 패턴 도입**: `detect_silent_inactive_sessions` / `force_reconnect_session` 에서 `kis_ws_pool` / `kis_ws` / `datetime` 접근 시 scheduler 네임스페이스 우선 참조 — (a) D-1 AST 정적 가드 통과 (정적 `import src.engine.scheduler` 0건), (b) 테스트 patch 호환 (`patch("src.engine.scheduler.kis_ws")` 가 stale_manager 함수 내에서도 동작), (c) 운영 환경 동일 객체 (scheduler.py 이미 로드 → 항상 hit). **scheduler.py 4 wrapper 추가** (2~3 줄 lazy import + 위임, 사이클 51 boot_manager + 사이클 60 A1 패턴 일관). scheduler.py 3,605 → 3,315L (−290L, −8.0%). stale_manager.py 358 → 731L (+373L). **누적 scheduler.py 감소** (사이클 51 직전 ~4,185L → 사이클 61 후 3,315L): **−870L, −21%**. **회귀 가드 20 케이스 (12 파일 분리, A1 13 파일 패턴 답습)**: A 위임 4 + B 5 상수 동일성 5 + **C cap dict `is` 동일성 (HIGH, Q1-G2)** + D 의존성 역전 정적 (ast 모듈 scheduler import 0) + E `_reset_daily_state` 동행 reset + F dataclass 7 필드 누락 가드 (2 케이스) + G silent inactive 시간당 cap freezegun + H 5분 지속 freezegun + **I universe guard 보유 보호 (HIGH)** + **J universe guard 익일청산 보호 (HIGH)** + K delta unsubscribe race best-effort + L import sanity. Red 시점 12 FAIL (A 4 + B 5 + C 1 + D 1 + L 1) / 8 PASS (E/F/G/H/I/J/K — 사이클 24/32/37/48 행위 보존 가드). **백엔드 1862 → 1882 PASS** (+20 신규, 회귀 0건). **tester verify V1~V11 ALL PASS**: V1~V4 카테고리 분리 측정 (unit/engine 1070 + non-engine 514 + integration 154 + contract 144), V5~V9 회귀 9 영역 87/87 PASS, **V10 flakiness 차단 3 회 반복 PASS** (사이클 58 V-2 + 사이클 60 hotfix 패턴 영구 절차화), V11 coverage 80.66% (+0.04%p, 60% gate 통과), V-Extra sys.modules 패턴 검증 (AST + patch + 운영 환경). 매매 안전성 무영향 (행위 보존 리팩토링). **A3 잔여** (사이클 63): `_check_and_resubscribe_stale` 221L + `_resubscribe_stale_priority` 100L = K stale watcher 핵심 (영구 hot path 360 회/일, KIS LMS/앱키 정지 chain). **주말 push 의무 + 월요일 첫 _boot 1h tester verify 의무** (사이클 60 도메인 자문 Q5/Q7). **신규 위험 식별 (tester)**: stale_manager.py 731L 비대화 진행 — 향후 sub-module 분해 후보 3 종 (`cap_per_hour` / `fresh_ratio_threshold` / `universe_guard`) refactor-expert 검토 대상 등록 권고 (별도 사이클). **다음 사이클 권고**: 사이클 62 = 가격 필터 (백엔드 + 프론트 단일, MEDIUM) — 사용자 Q3 결정. 사이클 63 = A3 (HIGH, 주말 push). | `src/engine/stale_manager.py` +373L (358→731) / `src/engine/scheduler.py` −290L (3,605→3,315, 4 wrapper + 5 상수 re-export + 5 상수 삭제) / `tests/unit/engine/test_cycle61_phase2A2_*.py` 12 파일 신규 (20 케이스, HIGH 3 / MEDIUM 5 / LOW 12) / `src/engine/CLAUDE.md` 모듈 맵 (9 함수 + 10 상수) + 본문 사이클 61 행 / `_workspace/test_index.yaml` 영향 인덱스 갱신 / `_workspace/cycle61_phase2A2_design_card.md` + `_workspace/red/cycle61_phase2A2_stale_manager.md` 발주/Red 명세 영구 기록 | 사이클 60 A1 완료 후 도메인 자문 옵션 B 3 단계 분할의 2 단계 — A1 패턴 직답습 (logger 자동 보존 / 12 파일 분리 / re-export / sys.modules.get). domain-expert 추가 자문 생략 (사용자 Q1=B 결정 — 사이클 60 응답서 §Q1~Q7 이 A2 영역도 충분 분석). 사이클 60 hotfix flakiness 차단 패턴 (카테고리 분리 측정 + 3 회 반복) 영구 절차화. `sys.modules.get` 패턴은 *모듈 분해 시 import 그래프 분리 + 테스트 patch 호환* 의 새로운 표준 (사이클 51 boot_manager 의 `_sched_mod` lazy import 답습 + 발전). stale_manager.py 731L 비대화는 향후 sub-module 분해 시점 결정 자료 — 단일 도메인 (stale tracking) 응집도 높으므로 즉시 분해 비권장, sub-module 분해 카드 발의 시점은 사이클 63 A3 완료 후 재평가. 사용자 Q2=A 결정 (오늘 금 NXT 애프터 18:00+ push) + Q3=나 결정 (A2 push 후 사이클 62 가격 필터 진행 — 본 사이클 종료 시 사이클 62 sketch 동행 작성 의무). 사이클 49→61 누적 13 사이클 (refactor-review 임계 5회 *2.6배 초과*) — refactor-review 트리거는 A3 완료 후 별도 결정. |
| 2026-06-04 | 사이클 60 Phase 2-A1 — refactor #2 stale_manager 추출 1단계 (HIGH 카드의 LOW 분리분, domain-expert 자문 옵션 B 3 단계 분할 전체 채택) | **신규 모듈** `src/engine/stale_manager.py` (358L). 사이클 51 `boot_manager.py` 패턴 답습 — `scheduler` 인자 + 2 줄 wrapper 위임. **5 함수 이주** (scheduler.py L2810/L2909/L3222/L3316/L3345 → wrapper): (1) `_build_session_subscription_view` (~98L, 세션별 구독 view 진단 read-only), (2) `_emit_stale_session_detail` (~43L, `[stale_watcher_detail]` 신규 prefix INFO 발화 — 사이클 28 G1 명세), (3) `_refresh_stale_ccnl_cache` (~93L async, stale r≥2 종목 `inquire_ccnl` + `_last_ccnl_cache` TTL 5분 + cap 20 — 사이클 37), (4) `_evict_expired_ccnl` (~29L, TTL 경과 캐시 evict — 사이클 45), (5) `_prune_force_retry_history` (~33L, 60분 슬라이딩 윈도우 cap — 사이클 29-R1). **5 상수 이전 + scheduler re-export**: `MAX_STALE_RETRIES=5` (사이클 17 보강) / `STALE_FORCE_RETRY_AFTER_SECS=300` / `STALE_FORCE_RETRY_HOURLY_CAP=12` (사이클 29-R1) / `UNIVERSE_LOW_VOLUME_THRESHOLD=10_000` (사이클 32 R4) / `SILENT_INACTIVE_FRESH_RATIO_THRESHOLD=0.2` (사이클 29-R2). re-export 로 `from src.engine.scheduler import MAX_STALE_RETRIES` 등 외부 import 호환 + `is` 동일성 보장. **logger 명시 binding**: `logging.getLogger("src.engine.scheduler")` — `__name__` 기본값 (`src.engine.stale_manager`) 사용 시 (a) 사이클 28 회귀 가드 (`test_scheduler_stale_logging_format.py::test_stale_watcher_emits_legacy_and_detail_lines` 외 3건) 의 `caplog.set_level(logger="src.engine.scheduler")` 한정 캡처에서 미인지 + (b) 운영 logging.yaml 가 `src.engine.scheduler` 로거 한정 핸들러 설정한 경우 콘솔/파일 로그 누락 위험. **잔류 영역 (Phase 2-A2/A3 사이클 61/62 분리 예정)**: (a) **A2 MEDIUM 4 함수** (~365L) `_evaluate_universe_guard` (116L, 사이클 32) + `_detect_silent_inactive_sessions` (~50L, 사이클 29-R2) + `_force_reconnect_session` (~50L, 사이클 24) + `_session_health_loop` 별개 lifecycle, (b) **A3 HIGH 2 함수** (~321L) `_check_and_resubscribe_stale` (221L, K stale watcher 핵심) + `_resubscribe_stale_priority` (100L, 5분 우선) — **주말 push 의무 + 월요일 첫 _boot 1시간 tester verify 동반**. (c) 7 property layer (L283~357) + `_STALE_DETAIL_TICKER_CAP=20` 클래스 속성 + `_reset_daily_state` (L3788) 의 `self._stale_state.reset_daily()` 직접 호출 (Q2 RECOMMEND — 사이클 56-D risk_manager.reset_daily_state() 답습 패턴). **회귀 가드 18 케이스** = neu A1~A5 위임 (5) + B1~B5 5 상수 `is` 동일성 (5) + C1~C2 호출 순서 보존 (2) + D1 의존성 역전 정적 (ast 모듈 scheduler import 0) + E1 ccnl TTL evict freezegun + F1 force_retry cap freezegun + G1 reset_daily 7 필드 일괄 clear + dataclass 필드 누락 가드 (사이클 48 `StaleTrackerState` 7 필드 — 명세 골격 "6 필드" 실측 정정) + H1 import sanity + property 7 호환 + **I1 (사이클 60 hotfix 영구 가드) logger 이름 "src.engine.scheduler" 강제** (사이클 61 A2 / 62 A3 이주 시 동일 결함 재발 차단). **백엔드 1844 → 1862 PASS** (+18 신규 = 17 + I1 hotfix 가드). **회귀 0건**. **coverage 80.62%** (gate 60% 통과). tester verify A~F 6 카테고리 ALL PASS + 카테고리 분리 측정으로 *flakiness 가린 회귀 4건 (사이클 28 회귀 가드)* 발견 → backend-dev hotfix 1줄 + I1 영구 가드 도입 (사이클 58 hotfix 패턴 답습). 매매 안전성 무영향 (행위 보존 리팩토링 + 진단 logger-binding 호환 보존). domain-expert 자문 7 항목 (Q1~Q7) 전부 적용: Q1 CONSIDER 회귀 가드 +4 + 9 property grep, Q2 RECOMMEND 6→7 필드 dataclass 가드, Q3 RECOMMEND 5 상수 re-export + 의존성 역전 정적 검증, Q4 옵션 B 3 단계 분할 (A1 단독 진행), Q5 NXT 애프터 push 시점 준수, Q6 별개 카드 #11 발의 (보드 전환 ↔ K stale watcher mutex — 본 사이클 종료 후), Q7 본질 차이 3 가지 인지 (A3 진입 시 별도 자문 + 주말 push 의무). **별개 위험 신규 식별**: `src/engine/*.py` 14 모듈 `__name__` logger 사용 — 사이클 28 회귀 가드 한정 영역에 보이지 않는 잠재 위험 (운영 logging.yaml 한정 핸들러 영향). **risk-review 카드 후보** (별도 사이클 발의). **사이클 51 boot_manager 패턴 검증**: A1 stale_manager 가 동일 패턴 답습으로 회귀 0건 달성 + I1 영구 가드 도입으로 A2/A3 일관성 강제 — 사이클 51 선례의 *영구 확장* 효과 입증. **잔여 refactor 카드** — #2 A2 MEDIUM (사이클 61, 익일 새벽 push) / #2 A3 HIGH (사이클 62, 주말 push 필수) / #3 HIGH (settlement_manager, A3 완료 후 의존성 재평가) / #4 MEDIUM (swing_manager) / #6 MEDIUM (KIS API except 좁히기, KIS MCP 의존) / **#11 신규** (보드 전환 mutex Q6) / **#12 신규** (14 모듈 logger 이름 일관성 risk-review). **다음 사이클 권고**: 사이클 61 = Phase 2-A2 (MEDIUM, 4 함수 ~365L, 익일 새벽 push). | `src/engine/stale_manager.py` 신규 358L / `src/engine/scheduler.py` 5 상수 re-export + 5 wrapper 위임 (3,880 → 3,605L, −275L −7.1%) / `tests/unit/engine/test_cycle60_phase2A1_stale_manager.py` 18 케이스 신규 / `src/engine/CLAUDE.md` 모듈 맵 + 본문 사이클 60 행 / `_workspace/test_index.yaml` 영향 인덱스 갱신 / `_workspace/cycle60_phase2A_design_card.md` + `_workspace/cycle60_phase2A1_design_card.md` + `_workspace/cycle60_phase2A_domain_consult.md` + `_workspace/cycle60_phase2A_domain_response.md` + `_workspace/red/cycle60_phase2A_stale_manager.md` + `_workspace/red/cycle60_phase2A1_stale_manager.md` 발주/자문/Red 명세 영구 기록 | refactor-review 2026-06-04 카드 #2 (HIGH) 의 A1 단독 진행 — 사이클 51 boot_manager 답습 + domain-expert 자문 (영구 hot path 360 회/일 / KIS LMS/앱키 정지 chain 위험 / 사이클 29 005935 stale 8 분 영구 잔류 사고 인용) 반영해 옵션 B 3 단계 분할 전체 채택. tester 가 카테고리 분리 측정으로 flakiness (caplog 상태 누수로 4 FAIL 가림) 영구 차단 — 사이클 58 V-2 회귀 hotfix 패턴 답습. logger 이름 영구 가드 (I1) 도입으로 후속 A2/A3 + 14 모듈 logger 일관성 강제. domain-expert Q6 발견 (보드 전환 ↔ K stale watcher race) 는 본 사이클 범위 *밖* 별개 카드 #11 분리 — 단일 사이클 단일 의도 원칙. Q5 push 시점 NXT 애프터 (16:30~17:00 KST) 준수. 매매 안전성 무영향 — *행위 보존 리팩토링 + 진단 logger-binding 호환 보존* 단일 책임. 사이클 49→60 누적 12 사이클 (refactor-review 임계 5회 *2.4배 초과*, 사이클 60 자체가 refactor #2 진행) — refactor-review 트리거는 A3 (사이클 62) 완료 후 별도 결정. |
| 2026-06-04 | 사이클 60 hotfix — CI backend-test 6시간 hang 영구 차단 (사이클 55+ 회귀, 결함 mock 누락 + 영구 timeout 가드 도입) | **CI 6시간 cancel 결함 root cause**: 사이클 55 R-1 Q3 reconciliation 이 `src/engine/order_engine.py:807-822` 에 `from src.api.balance import get_balance` lazy import + `await get_balance()` 호출 추가. 사이클 55 의 integration 테스트 (`tests/integration/test_sell_rejection_integration.py:159` `mock_get_balance` fixture) 에는 mock 추가했으나 **unit 테스트 갱신 누락**. `tests/unit/engine/test_b1_market_closed_zombie_block.py::test_s3a` + `tests/unit/engine/test_order_engine_sell_fallback.py::test_execute_sell_when_insufficient_quantity_then_no_fallback_and_positions_cleared` 두 위치가 `insufficient_quantity` 분기 진입 → 실제 KIS REST 호출 시도 → `.env` 없는 CI 환경에서 socket connect 무한 대기 → GitHub Actions 기본 6시간 timeout cancel. 사이클 55~59 모든 backend-test 결과 cancelled/in_progress (CI run 26885324350 ~ 26933319545). **즉시 시정**: 두 unit 테스트에 `mock_get_balance` fixture 추가 + 시그너처 주입 (integration 테스트 159 패턴 복제). **영구 가드 (2중 안전망)**: (1) `pyproject.toml` `[tool.pytest.ini_options]` 에 `timeout = 60` 추가 + `requirements-dev.txt` 에 `pytest-timeout>=2.3` — 테스트당 60초 강제 timeout 즉시 FAIL 전환. (2) `.github/workflows/ci.yml` `backend-test` job 에 `timeout-minutes: 15` 추가 — GitHub Actions 기본 6시간 → 15분으로 단축, pytest-timeout 우회 hang 도 영구 차단. **검증**: 로컬 `pytest --cov` **1844 passed, 2 skipped in 47.04s** / coverage 80.38% / 회귀 0건. 변경 통계: 5 files / +40 / -0 (production code 무변경, 테스트/CI 인프라만). **매매 안전성 무영향** (테스트/CI 인프라만 변경, production 분기 0). **잔여 사이클 권고 영향 없음** — refactor #2 HIGH (stale_manager) 1순위 유지. **운영 모드 변경**: 60s 초과 테스트는 자동 FAIL — 향후 `slow` 마커 필요 시 `@pytest.mark.timeout(180)` 개별 override. **사이클 55+ 검수 누락 회수**: tester 가 로컬 1804 PASS 만 확인하고 CI conclusion 미확인 (CI 가 in_progress 상태로 5사이클 연속 통과) → 향후 *push 후 CI run 결과 확인 의무화* tester 체크리스트 추가 권고. | 사이클 58 회귀 hotfix (사이클 59) 와 유사 패턴 — production code 변경 시 mock fixture 자동 검색 + 갱신 의무화. 본 결함은 4 사이클(55~58) 누적 후 사용자 보고로 발견 → CI 가시성 미비. 영구 timeout 가드 도입으로 *동일 결함 재발 시 1분 내 FAIL* 보장. GitHub Actions 6시간 cancel 정책의 본질적 비용 (run 5건 = 30시간 wasted compute) 회수. 사이클 49→60 누적 12 사이클 (refactor-review 임계 5회 *2.4배 초과*) — refactor-review 트리거는 refactor #2 진행 시 결정. |
| 2026-06-04 | 사이클 58 V-2 — `daily_log_reports` OpenAI 메타 컬럼 (분석 메모 카드 V-2 P2, 사이클 53.1 재집계 사고의 정량 진단 인프라) | **migration 단일 카드 + backend 시그너처 확장**. **migration 031** (`supabase/migrations/031_daily_log_reports_openai_meta.sql`): `daily_log_reports` ALTER TABLE — 5 컬럼 추가 (`input_tokens INT NULL` / `output_tokens INT NULL` / `total_tokens INT NULL` / `latency_ms INT NULL` / `cost_estimate_usd DECIMAL(10, 6) NULL`). 모두 NULL 허용 — 사이클 53.1 `model='cycle53.1-metrics-only'` row 자연 보존. **`src/db/log_reports.py::insert_log_report` 시그너처 5 keyword 확장** (input_tokens / output_tokens / total_tokens / latency_ms / cost_estimate_usd 모두 default None). **`src/engine/log_analysis_engine.py`**: (1) `_OpenAIMeta` dataclass 신규 (5 필드 token/latency/cost), (2) `_OPENAI_PRICING_USD_PER_1K_TOKENS` 모듈 상수 dict — 6 모델 단가 (`gpt-5.4` / `gpt-4o` / `gpt-4o-mini` / `gpt-4-turbo` / `gpt-4` / `gpt-3.5-turbo` input/output per 1K tokens USD 분리), (3) `_compute_cost_usd(model, input_tokens, output_tokens) -> float | None` — 미등록 모델 → cost None + WARNING `[openai_pricing_miss]` 발화, (4) `_call_openai` 시그너처 → `-> tuple[dict, _OpenAIMeta]` 확장 (호출처 `generate_daily_log_report` 단일 — 영향 격리). **단가 dict 위치 (D-3-a)**: 모듈 상수 (DB 보관 X) — 모델 추가 빈도 낮음 + PR 가시화 효과 + 회귀 테스트 동행. **통화 정책 (D-2-c)**: USD 저장, KRW 환산은 조회 시점 (환율 변동 분리 + OpenAI billing 통화와 일치). **회귀 가드 10 케이스** = unit 8 (`tests/unit/engine/test_log_analysis_openai_meta.py`: `_OpenAIMeta` 필드 검증 / 단가 dict 6 모델 / `_compute_cost_usd` 정상 케이스 / 미등록 모델 None + WARNING / `_call_openai` 메타 반환 / latency 계측 정확성 / cost 6자리 소수 정밀도 / generate_daily_log_report 메타 전달) + db 2 (`tests/unit/db/test_log_reports_openai_meta.py`: `insert_log_report` 5 keyword 신규 컬럼 정확 매핑 / NULL 디폴트 보존). **10/10 PASS**. **백엔드 1834 → 1844 PASS (+10 신규, 회귀 0건).** **CLAUDE.md 동기화** `src/db/CLAUDE.md` (`insert_log_report` 시그너처 5 keyword 명세) + 루트 `CLAUDE.md` DB 스키마 표 `daily_log_reports` 행에 OpenAI 메타 컬럼 표기. **매매 안전성 무영향** (20:10 일일 분석 경로 전용, 09:00~20:00 매매 hot path 분기 변경 0 — 사이클 53/53.1 패턴과 동일). **사용자 수동 액션 필수**: Supabase Dashboard → SQL Editor → migration 031 본문 붙여넣기 → 실행 (미실행 시 다음 20:10 `insert_log_report` 가 `column does not exist` 에러). 검증 쿼리 `SELECT column_name FROM information_schema.columns WHERE table_name='daily_log_reports' AND column_name IN ('input_tokens', 'output_tokens', 'total_tokens', 'latency_ms', 'cost_estimate_usd')` → 5 row 반환 확인. **사이클 53.1 row 자연 보존** (NULL 허용 → metrics-only row 무영향). **잔여 카드 V-2 제거** → refactor #2 HIGH (stale_manager) 1순위 승격 / R-2 MEDIUM / refactor #3 HIGH (settlement_manager) / refactor #4 MEDIUM (swing_manager) / R-3 MEDIUM / refactor #6 MEDIUM / V-3 P2 / V-4 P3 보존. **후속 카드** — V-2 후속 UI 표시 (별 사이클, `daily_log_reports` 페이지에 cost/latency/token 표시) + V-1 임계 운영 점검 (사이클 59~60 진입 전 1~2 영업일 실측). **다음 사이클 권고**: refactor #2 HIGH (scheduler 분해 1단계 stale_manager 추출, -1,000L, domain-expert 자문 필수, 사이클 51 boot_manager 패턴 답습). domain-expert 자문 미호출 (가시화 단일 책임, 매매 행위 변경 0). | 사이클 53.1 재집계 사고 (OPENAI_API_KEY 부재로 row 손상 → metrics-only 복구) 의 *정량 진단 인프라 도입*. 자동 리포트의 운영 비용 / latency / 모델별 사용량 *영구 추적* 가능. 단가 dict 모듈 상수 채택 (D-3-a) 으로 OpenAI 가격 변동 시 PR 가시화 효과 + 회귀 테스트 동행. USD 저장 (D-2-c) 으로 환율 변동 분리 + 단일 진실 원천 보존 (KRW 환산은 조회 시점, 환율 갱신 빈도와 분리). NULL 허용 + 사이클 53.1 row 보존 = 후방 호환 자명 (D-5). migration 단일 카드 + backend 시그너처 확장만으로 V-2 카드 종결 — 프론트엔드 영향 0 (D-8-a, 후속 카드 분리). 사이클 49→58 누적 10 사이클 (refactor-review 임계 5회 *2배 초과*) — 다음 refactor #2 HIGH 진입 *시* refactor-review 동시 실행 또는 별도 사이클 결정. 사이클 52~57 운영 진단 + 인프라 정형화 6 사이클 종결 (B-1 → B-2/B-4 → B-3 → R-1 → refactor #1+#5 → V-1) 후 V-2 가시화 마지막 1 사이클. |
| 2026-06-04 | 사이클 57 V-1 — 매도 거부 폭주 실시간 알람 (분석 메모 카드 V-1 P1, 사이클 55 R-1 + 56 인프라 직계 활용) | **단일 모듈 확장** `src/engine/sell_rejection.py` 271L → 368L (+97L). **신규 상수 3개** (모듈 + ClassVar 양립): `ALARM_WINDOW_SECONDS=600` (10분 윈도우, 사이클 52 B-1 10분 500건 실측 기반) / `ALARM_THRESHOLD=5` (6건째 발화, 1분당 50건 사고 발생 60~90초 후 발화 보장) / `ALARM_COOLDOWN_SECONDS=1800` (30분, 윈도우의 3배 — 카운트 자연 감쇠 보장 + 사고 진행 중 추적 가능). **신규 필드** `_alarm_last_emitted: dict[str, datetime]` (per-ticker cooldown 시각). **신규 private hook 2개**: (1) `_append_history(ticker, event)` — 4 채널 (register_market_closed / register_market_order_disallowed / register_insufficient_quantity / record_rejection) 공통 진입점으로 단일화 (deque 적재 → `_maybe_emit_burst_alarm` 자동 호출). (2) `_maybe_emit_burst_alarm(ticker, now_kst)` — cooldown 검사 (lazy clear) + 10분 윈도우 카운트 + 임계 초과 시 cooldown 등록 + `_emit_burst_alarm` 위임. (3) `_emit_burst_alarm(ticker, recent_events, now_kst)` — reason 분포 집계 + 마지막 이벤트 정보 포함 메시지 포맷 + `safe_write_log("CRITICAL", message, fallback_debug=...)` fire-and-forget. `asyncio.create_task` 의 RuntimeError (no running loop) graceful skip (coroutine 즉시 close). **메시지 포맷** (tracker 독립 최소 — D-4-b 채택, registry/positions 의존성 0): `[매도거부폭주] ticker={t} window=10min count={N} threshold=5\n  reasons={market_closed:4, market_order_disallowed:2}\n  last_msg_cd={cd} last_msg1={msg1[:60]}\n  occurred_at_kst={iso}`. **`reset_daily()` 확장**: 4 필드 → 5 필드 일괄 clear (`_alarm_last_emitted.clear()` 추가). **회귀 가드 15 시나리오** (발주 D-6 12 케이스 + 4 채널 분리 검증 3 케이스 추가) — `tests/unit/engine/test_v1_rejection_alarm.py` (258L 신규): 임계 미만(5건) / 임계 도달(6건째) / cooldown 중 skip / cooldown 만료(30분 후) 재발화 / 윈도우 밖(11분 전) 카운트 제외 / 다른 ticker 격리 / deque maxlen=20 보존 / reason 분포 정확성 / last_msg_cd·last_msg1 정확성 / `reset_daily()` 동행 cooldown 초기화 / safe_write_log 실패 graceful / 사이클 55/56 회귀 무영향 + 4 채널 분리 (`register_market_closed`/`register_market_order_disallowed`/`register_insufficient_quantity` 모두 `_append_history` 단일 진입 통일). **15/15 PASS**. **백엔드 1819 → 1834 PASS (+15 신규, 회귀 0건).** tester Verify A/B/C/D 4 카테고리 전 PASS — 프로덕션 배포 가능 판정. **CLAUDE.md 동기화** `src/engine/CLAUDE.md` 2 곳 (모듈 맵 `sell_rejection.py` 행에 "+ 사이클 57 V-1 알람" / 본문 `execute_sell` 분기 사이클 57 폭주 알람 명세 + 5 필드 일괄 위임 표기). **매매 안전성 무영향** (관찰성/가시화만 추가, 매매 hot path 분기 변경 0). 사이클 55 R-1 `deque(maxlen=20)` 인프라 + 56-E `safe_write_log` 헬퍼 + 56 `DailyEmitCap` cap 패턴 *3 종 사전 인프라 직계 활용* — V-1 단독 발주 시점 추가 모듈 도입 0. 사이클 52 B-1 의 10분 500건 폭주 *실시간 감지 안전망* 완성 (사이클 52 = 진입 차단 / 사이클 55 = 분류 통합 + TTL 2단계 / 사이클 56 = cap 추상화 / 사이클 57 = 실시간 알람). **외부 채널 (Slack/email) = 본 사이클 제외** — D-3-a 채택 (system_logs CRITICAL INSERT 만). 추후 사이클 (`system_config.notification_webhook_url` 신설) 별도. domain-expert 자문 미호출 (사이클 55 Q5 권고 메시지 포맷 활용 + 임계 수치는 사이클 52 B-1 실측 기반 team-leader 단독 결정). **잔여 카드 V-1 제거** → V-2 P2 1순위 승격 / R-2 MEDIUM / R-3 MEDIUM / V-3 P2 / V-4 P3 + refactor 카드 #2 HIGH (stale_manager) / #3 HIGH (settlement_manager) / #4 MEDIUM (swing_manager) / #6 MEDIUM (KIS except 좁히기) 보존. **다음 사이클 권고**: (a) V-2 P2 (daily_log_reports OpenAI 메타 컬럼 migration) 또는 (b) refactor #2 HIGH (scheduler 분해 1단계 stale_manager, domain-expert 자문 필수). **운영 모니터링 권고**: V-1 발화 임계 (10분 5건) 가 과민/둔감 여부 1~2 영업일 후 평가 — 과민 시 임계 상향 (10분 10건), 둔감 시 임계 하향 또는 윈도우 단축. | 사이클 55 R-1 + 56 인프라 사전 도입의 본래 목적 (V-1 알람 hook) 종착 — sell rejection 사고 *분류→통합→캡→실시간 감지* 4 사이클 완결. 본 사이클은 *가시화 단일 책임* — 매매 hot path 행위 변경 0. domain-expert Q5(c) 권고를 그대로 채택하지 않고 tracker 독립성 보존 (D-4-b) — registry/positions 의존성 추가 시 사이클 55 단일 정책 객체 책임 위반. cooldown 30분 (D-2-c) 은 10분 윈도우의 3배로 카운트 자연 감쇠 보장 + 사고 진행 추적 가능 균형점. fire-and-forget + RuntimeError 양쪽 graceful — 테스트 / 초기화 환경 (no running loop) 에서도 안전. 회귀 가드 15 케이스가 발주 12 케이스보다 +3 = backend-dev 자체 보강 (4 채널 단일 진입 검증 강화). 사이클 49→57 누적 9 사이클 (refactor-review 임계 5회 *초과 + 1*). 다음 refactor-review 트리거는 refactor 카드 #2~#4 진행 후 별도 결정. |
| 2026-06-04 | 사이클 56 — refactor 카드 #1 emit cap 통합 4단계 + 카드 #5 safe_write_log 헬퍼 (사이클 55 R-1 직계 후속) | **5 단계 자동 순차 진행**. **56-A**: 신규 모듈 `src/engine/daily_emit_cap.py` (89L) — `DailyEmitCap[K]` 제네릭 dataclass (`should_emit` / `mark_emitted` / `discard` / `reset` 4 메서드 + `__eq__` 호환 layer + 호환 layer 5 별칭). G-1 회귀 가드 7 케이스 (제네릭 key 타입 str/tuple 검증 + reset/discard 정책 + 호환 layer 동일성). **56-B**: `SellRejectionTracker._logged_today: set[str]` → `DailyEmitCap[str]` 마이그레이션. `__eq__` 호환 보완 (set 비교 테스트 통과 보장). G-2 +1 케이스, `test_sell_rejection_tracker.py` 37 PASS 회귀 0. **56-C**: `OrderEngine._nxt_downgrade_logged_today: set[str]` → `DailyEmitCap[str]` 마이그레이션. 사이클 54 코드 코멘트 "사이클 56 통합 예정" 제거 (빚 청산). Line 108 타입 어노테이션 시정 (top-level import `from src.engine.daily_emit_cap import DailyEmitCap` + property 반환 타입 `DailyEmitCap[str]`). G-3 +1 케이스, `test_b3_nxt_downgrade_log_cap.py` 5 PASS. **56-D**: `RiskManager._risk_silent_skip_logged_today: set[tuple[str,str]]` → `DailyEmitCap[tuple[str,str]]` 마이그레이션. **`RiskManager.reset_daily_state()` 신규 메서드** + scheduler 외부 직접 clear 비대칭 해소 (사이클 52 OrderEngine 패턴 답습) — `scheduler.py::_reset_daily_state` 의 `try/except AttributeError` 가드 → `self.risk_manager.reset_daily_state()` 위임 일원화. G-4 +2 케이스 (페어별 cap + reset_daily 위임), 10 PASS. **56-E**: 신규 헬퍼 `src/db/system_logs.py::safe_write_log(level, msg, *, debug_key)` — graceful write_log fire-and-forget (실패 시 logger.debug + 본 흐름 영향 0). `order_engine.py` 4 곳 마이그레이션 (refactor-expert "14 곳" 권고 중 graceful skip 의도 *명확한 4 곳만* — 나머지 10 곳은 의도된 예외 전파 / 단일 try 범위라 마이그레이션 범위 외). G-5 4 케이스 (헬퍼 raise graceful + 4 곳 호출 패턴 일관성). **최종 지표**: 백엔드 **1819 PASS** (1804 → +15, 회귀 0건). 신규 모듈 2개 (`daily_emit_cap.py` 89L + `system_logs.py::safe_write_log` 헬퍼). 3 emit cap set (사이클 31 R6 + 사이클 52→55 흡수분 + 사이클 54) 모두 `DailyEmitCap` 통합 완성. RiskManager reset 비대칭 해소 (사이클 52 OrderEngine 패턴 일관). **매매 안전성 무영향** (행위 보존 리팩토링). carded 종결 `_workspace/refactor/2026-06-03_review.md` 카드 #1 (4 단계 모두) + 카드 #5 (4 곳 마이그레이션). **잔여 refactor 카드** — #2 HIGH (stale_manager 추출, -1,000L) / #3 HIGH (settlement_manager 추출, -376L) / #4 MEDIUM (swing_manager 추출, -400L) / #6 MEDIUM (KIS API except 좁히기, KIS MCP 검증 의존). **잔여 분석 카드** — V-1 P1 (매도 거부 폭주 실시간 알람, R-1 deque 인프라 직계 활용) / V-2 P2 / R-2 / R-3 / V-3 / V-4. **다음 사이클 권고**: (a) V-1 P1 (R-1 deque 인프라 즉시 활용) 또는 (b) refactor 카드 #2 HIGH (scheduler 분해 1단계 stale_manager, domain-expert 자문 필수). domain-expert 자문 미호출 (refactor 행위 보존 단일 사이클). | 사이클 55 R-1 직계 후속 — 3 emit cap 동형 패턴 (사이클 31 R6 / 사이클 52~55 / 사이클 54) 자연 추상화 기회 충족 (3 회 반복 = 추상화 임계). 사이클 54 코드 코멘트 "사이클 56 통합 예정" 의 명시적 빚 청산. 사이클 52 OrderEngine.reset_daily_state() 캡슐화 패턴이 사이클 56-D 에서 RiskManager 비대칭 해소까지 일관 적용 (외부 직접 clear → reset_daily_state 위임). safe_write_log 헬퍼는 refactor-expert 카드 #5 14 곳 권고 중 graceful skip 의도 *명확한 4 곳만* 보수적 마이그레이션 (의도된 예외 전파 영역과 분리). 다음 우선 카드 V-1 P1 (R-1 deque maxlen=20 인프라 직계 활용) 또는 refactor 카드 #2 HIGH (scheduler 분해 1단계, 사이클 51 boot_manager 패턴 답습). 사이클 49→56 누적 8 사이클 (refactor-review 임계 5회 *초과*) 이나 본 사이클 자체가 refactor-review 채택분 진행 — 다음 refactor-review 트리거는 카드 #2~#4 진행 후 별도 결정. |
| 2026-06-03 | 사이클 55 R-1 — `SellRejectionTracker` 단일 정책 객체 (refactor-review 카드 R-1, 사이클 52 후속) | **신규 모듈** `src/engine/sell_rejection.py` (271L): `SellRejectionTracker` dataclass — 4 분류 통합 정책 객체 (사이클 48 `stale_tracker.py` 패턴 답습). `RejectionEvent(frozen)` / `RejectionResult(frozen)` dataclass. 7 public 메서드 (`is_blocked` / `should_emit_block_log` / `mark_block_logged` / `register_market_closed` / `register_market_order_disallowed` / `register_insufficient_quantity` / `record_rejection` / `get_recent_rejections` / `reset_daily`) + 모듈 헬퍼 (`_compute_next_market_open_kst` / `is_krx_main_hours` / `is_nxt_session_hours`). **Q1 2단계 TTL**: KRX 메인(09:00~15:30) 거부 = 5분 TTL (일시 장애 가정, 사이클 52 단일 09:00 TTL 에서 분기), NXT 시간대 거부 = 다음 KST 09:00 TTL 보존. **Q2 30초 TTL + NXT 익일 전환**: `market_order_disallowed` 폴백 결과 무관 30초 TTL 등록 (동일 tick 폭주 차단). NXT 폴백 실패(`is_nxt=True, fallback_succeeded=False`) → `RejectionResult.next_day_clear_required=True` → `_pending_next_day_clear_provider().add((ticker, strategy_id))` + `[next_day_clear_deferred]` WARNING. **Q3 reconciliation**: `insufficient_quantity` 거부 시 tracker history 적재(차단 X) + `[positions_reconciliation]` INFO + `get_balance()` 1회 호출 (수동 부분매도 보호). **Q5 history**: ticker별 `deque(maxlen=20)` V-1 알람 hook 사전 준비. **OrderEngine 변경** (`src/engine/order_engine.py`): `__init__` 2 필드 → `self._sell_rejection = SellRejectionTracker()` 통합. 호환 layer 2 property (`_market_closed_blocked` / `_market_closed_blocked_logged_today`) — tracker 내부 dict/set 직접 노출 (is 동일성, 사이클 52 테스트 코드 무수정). `execute_sell` 진입 게이트 18줄 → 11줄 위임 (로그 prefix `[market_closed_blocked]` + reason 필드 유지). 분류별 등록 위임 3곳 + Q3 `get_balance` 호출 + Q2 `_pending_next_day_clear` provider 연계. `reset_daily_state` → `_sell_rejection.reset_daily()` 4 필드 일괄 위임 + `_nxt_downgrade_logged_today.clear()` 보존 (사이클 54 유지, R-1 범위 밖). **회귀 가드 36 시나리오** = 신규 27 (`test_sell_rejection_tracker.py` 18 + `test_b1_market_closed_zombie_block.py` +2 (S3c/S4_KRX) + `test_sell_rejection_integration.py` 8 = 28 중 1 케이스는 사이클 52 기존 카운트와 중복으로 정정 후 27) + 사이클 52 보존 9 (S1/S2/S3a/S3b 좁힘/S4_NXT/S5/S6/S7/C-8) — 36/36 PASS. **백엔드 1776 → 1804 PASS (+28 신규 PASS, 회귀 0건).** tester Verify 4 카테고리 분리 실측: unit/engine 998 + unit/non-engine 508 + contract 144 (2 skip) + integration 154. 사이클 54 `_nxt_downgrade_logged_today` 무영향. 사이클 30 `_completed_orders` race 가드 (C-8) 무영향. **CLAUDE.md 동기화** 4 파일 (루트/src/engine/CLAUDE.md/HARNESS_CHANGELOG). **영향 인덱스 갱신** `_workspace/test_index.yaml`. **행위 변경 3종**: Q1 KRX 메인 거부 5분 TTL (사이클 52 단일 다음 09:00 → 분기), Q2 NXT 폴백 실패 익일 청산 큐 등록, Q3 insufficient_quantity reconciliation `get_balance` 1회 호출. **로그 prefix 보존**: `[market_closed_blocked]` 유지 (사이클 52 운영 Grafana/Loki grep 호환, D-1 추천 채택). C-4 테스트 `assert` 수정 — `set[str]` vs `set[tuple]` 불일치 (`any()` 포용 비교로 정정). | 사이클 54 후 refactor-review 권고 R-1 HIGH — 사이클 31/52/54 누적 3개 emit cap set 통합 시작점. 사이클 52 2 필드 + 사이클 54 1 필드 산재 → `SellRejectionTracker` 통합 (사이클 48 stale_tracker 7 필드 → 1 데이터클래스 검증 답습). domain-expert Q1~Q5 자문 5 RECOMMEND 전부 적용 (Q2 NXT 익일 전환 + Q3 reconciliation 신규 행위 2종). `_nxt_downgrade_logged_today` 는 R-1 범위 밖 — 사이클 56 emit cap 통합(`DailyEmitCap` 제네릭 추상) 시 흡수 예정. YAGNI 원칙 — 3개 set 확정 시점이 사이클 56 자연 추상화 기회. V-1 history hook (`deque maxlen=20`) 사전 도입으로 알람 사이클 진입 준비 완료. 다음 우선 카드: V-1 P1 (매도 거부 실시간 알람) 또는 R-2 MEDIUM (funnel 단계 hook). |
| 2026-06-03 | 사이클 54 — NXT 다운그레이드 로그 폭주 차단 (카드 B-3, `[nxt_downgrade]` emit cap) | **운영 진단** (메인 세션): 분석 메모 가설 (사후 보강 누락 → `stock_master.nxt_tradable=False` 미저장) *반증* — 064400 `stock_master` 에 `nxt_tradable=False` 이미 저장됨 (`refreshed_at=2026-05-31T23:19:59`, 사후 보강 정상 작동). 사전 판별 (`_strategy_exchange_async` 의 `stock_master.get_one(ticker)` 조회) 도 정상. **실질 결함** — `_strategy_exchange_async` 의 `[nxt_downgrade]` 로그 발화가 ticker별 emit cap 부재 → 사이클 52 매도 좀비 부작용 (`risk.on_tick` 매 tick 재호출) 로 `_strategy_exchange_async` 도 동반 폭주 → 115건 `[nxt_downgrade]` 로그 발화 (정상 1건/일 기대). 사이클 31 R6 `_risk_silent_skip_logged_today` + 사이클 52 `_market_closed_blocked_logged_today` 동형 패턴 답습. **시정** (`src/engine/order_engine.py` 3곳 ~5줄): (1) `OrderEngine.__init__` 에 `_nxt_downgrade_logged_today: set[str]` 신규. (2) `_strategy_exchange_async` 의 `[nxt_downgrade]` 발화 분기에 ticker별 cap 검사 — **다운그레이드 *결정* (`return "KRX"`) 은 cap 검사 *밖*, 100회 호출 모두 정상 KRX 반환 보장**. (3) `OrderEngine.reset_daily_state()` 에 신규 set clear 추가 (사이클 52 도입 메서드와 일관). **회귀 가드** `tests/unit/engine/test_b3_nxt_downgrade_log_cap.py` 4 시나리오 — ticker별 1행/일 cap (100회 호출 → 로그 1행, KRX 결정 100회 보존) / ticker별 격리 (A/B 종목 각각 1행) / `reset_daily_state` 후 재발화 / `nxt_tradable=True` 무영향. **CLAUDE.md 동기화**: `src/engine/CLAUDE.md` `_strategy_exchange_async` 항목에 emit cap 1줄. **백엔드 1776 PASS (1772→+4) / 회귀 0건.** 매매 동작 변경 0 — 다운그레이드 결정 (`return "KRX"`) 100% 보존, `[nxt_downgrade]` 로그만 ticker별 1행/일 cap. tester verify A/B/C 전부 PASS (사이클 52/32 인접 안전망 24/24 PASS 회귀 0). 사이클 52 매도 좀비 가드 (B-1) 와 **이중 안전망** — 사이클 52 가 외부 재호출 자체를 차단하므로 `_strategy_exchange_async` 도 재진입 안 함 (1차 방어), 사이클 54 cap 으로 만일 1차 우회 시에도 로그 폭주 차단 (2차 방어). 카드 출처 `_workspace/2026-06-01_log_analysis.md` B-3 섹션 (종결 표시 완료). **잔여 카드 우선순위 재정렬** — 1순위 R-1 HIGH (`SellRejectionTracker` 단일 정책 객체, 사이클 52 후속 + domain-expert 자문 동반) / 2순위 V-1 P1 (매도 거부 폭주 실시간 알람) / 3순위 V-2 P2 (daily_log_reports OpenAI 모델/토큰/비용 메타) / 보조 R-2 / R-3 / V-3 / V-4. | 사용자 1.17M 자본 운영 critical — `[nxt_downgrade]` 로그 폭주가 `system_logs` 노이즈 증폭 → 다른 결함 가시성 저하 + 사이클 53.1 일일 분석 18,435건 부피 cap 압박 (B-3 미시정 시 다음 영업일 폭주 재발 → cap 30000 도 위협 가능성). 사이클 31 R6 `_risk_silent_skip_logged_today` + 사이클 52 `_market_closed_blocked_logged_today` 의 emit cap 패턴 정형화 (3개 동형 set 누적). 사이클 49→54 운영 진단 카드 누적 6회 — 다음 사이클 진입 전 **refactor-review 트리거 검토 권고** (3개 emit cap set 통합 추상화 + scheduler 분해 후속 단계 동반). domain-expert 자문 미호출 (매매 행위 변경 0, 로그 발화 빈도만 cap). |
| 2026-06-02 | 사이클 53 + 53.1 통합 — 일일 로그 분석 데이터 수집 결함 시정 (카드 B-2 next_day_clear 로그 누락 + B-4 trade_metrics KST 경계 누락 + 운영 부피 cap 후속 보강) | **운영 진단** (메인 세션 직접): 2026-06-01 자동 리포트가 `[next_day_clear_drained] 064400 result=success` 실제 발화에도 `next_day_clear.drained_success=0` + `trade_history` 5건 중 1건 (064400 momentum SELL `2026-05-31T23:20:00Z` = 6/1 08:20 KST, +30,100원) 누락. **Root cause 2개** — (B-2) `src/engine/log_analysis_engine.py::_fetch_logs_in_range` 가 Supabase PostgREST 단일 select 1000행 cap 에 잘림 (`limit=5000` 명시했어도 PostgREST 가 1000 으로 강제 절단) → 23:20:00 이후 next_day_clear 로그가 6/1 일일 윈도우 끝부분에 잘려 카운트 누락. (B-4) `src/db/trade_history.py::get_trades_in_range` 가 timestamp 문자열을 `T00:00:00` / `T23:59:59` 로 조립 + TZ suffix 누락 → Supabase 가 UTC 해석 → KST 00:00~09:00 거래 (= UTC 전날 15:00~24:00) 가 *전날* 윈도우로 분류 → 064400 SELL UTC 23:20 (= KST 6/1 08:20) 가 KST 6/1 영업일 윈도우 진입 못함. **시정** (사이클 53 Red→Green: `tests/unit/engine/test_b2_b4_log_analysis_data_collection.py` 5/5 FAIL → PASS): (1) `_fetch_logs_in_range` 페이지네이션 `.range(offset, offset+999)` 루프 (한 페이지 1000행 한도 + 빈/짧은 응답 종료 + 안전 cap = 호출 측 limit). (2) `get_trades_in_range` 시작/종료 timestamp 에 `+09:00` KST suffix 명시. (3) `generate_daily_log_report(_now_kst: datetime | None = None)` 파라미터 추가 (KST 시각 주입 가능 → 회귀 가드 재현성 + 결정성). **사이클 53.1 후속** (운영 부피 cap, S6 시나리오 추가): 사이클 53 직후 tester 운영 부피 실측 — 디폴트 `limit=5000` < 실제 18,435/일. `generate_daily_log_report` 호출 측에서 `_fetch_logs_in_range(..., limit=30000)` 명시 + WHY 코멘트 (KST 일일 운영 부피 마진 2배). | `src/engine/log_analysis_engine.py` (_fetch_logs_in_range 페이지네이션 + generate_daily_log_report 시그니처 + 호출 측 limit=30000) / `src/db/trade_history.py` (get_trades_in_range KST `+09:00`) / `src/engine/CLAUDE.md` (페이지네이션 + KST timezone + 호출 측 limit 정책 동기) / `src/db/CLAUDE.md` (KST timezone 정책 + get_trades_in_range 명세) / 회귀 가드 `tests/unit/engine/test_b2_b4_log_analysis_data_collection.py` 6 시나리오 (B2-S1 페이지네이션 2페이지 머지 / B2-S2 limit=30000 일일 부피 흡수 / B4-S1 KST 08:20 거래 윈도우 진입 / B4-S2 KST 22:00 거래 윈도우 진입 / 통합 S5 metrics drained_success=1 + trades_total=5 / **S6 18,000건 운영 부피 round-trip**) + 발주서 `_workspace/cycle53_b2_b4_log_analysis_data_collection_spec.md` + `daily_log_reports` 2026-06-01 row 재집계 (메인 세션 복구 INSERT, row id `6b932e53-a820-42d0-bc96-1196557e2691`). **재집계 사고 복구 메모** — 6/1 row DELETE → `generate_daily_log_report` 재실행이 `OPENAI_API_KEY` 미설정으로 early return → row 사라짐 → 메인 세션이 metrics-only 복구 (`model='cycle53.1-metrics-only'` + 빈 summary/findings + 핵심 metrics 직접 수집 + UNIQUE 멱등 보존). **복구 메트릭이 사이클 53/53.1 시정 효과를 직접 검증**: `logs.total_logs=18,435` (페이지네이션 작동 — 1000 cap 이었으면 절단), `next_day_clear.drained_success=1` (B-2 시정 효과 — 064400 prefix 발화 정상 집계), `trades.trades_total=5` (B-4 시정 효과 — KST 08:20 거래 포함), `trades.realized_pnl=56,100` (기존 26,000 + 30,100), `trades.by_strategy={momentum:1, volatility_breakout:4}`. **다음 OpenAI 운영 환경에서 사용자가 원하면 row 재DELETE + 자동 재집계 단순 재실행으로 summary/findings 완전체** (UNIQUE 멱등). **백엔드 1772 PASS** (1766→+6, 회귀 0건) / **매매 안전성 무영향** (분석 경로 한정, 매수/매도/포지션/주문 hot path 미변경). 카드 출처 `_workspace/2026-06-01_log_analysis.md` B-2 + B-4 섹션 (사이클 53/53.1 종결 표시 완료). **잔여 카드 보존** — B-3 MEDIUM (NXT 다운그레이드 폭주 eager 갱신 누락) / R-1 HIGH (`SellRejectionTracker` 단일 정책 객체, 사이클 52 후속) / R-2 MEDIUM (momentum/VB/LTV funnel 단계 hook) / R-3 MEDIUM (api_metrics quote_pool 500 누락) / V-1 P1 (매도 거부 폭주 실시간 알람) / V-2 P2 (daily_log_reports OpenAI 모델/토큰/비용 메타) / V-3 P2 (system_logs 컬럼명 일관성) / V-4 P3 (탈락 사유 sample 활용도). | 사용자 1.17M 자본 운영 critical 분석 신뢰도 — 자동 리포트 metrics 가 운영 결정 (다음날 우선순위 카드 발주) 의 1차 근거인데 18,435건 중 1000건 cap 누락 + 1 trade KST 누락 = 운영 결정 왜곡. 사이클 52 종결 검수 시 발견된 결함 누적의 종결. 사이클 53 단일 사이클 내 Red→Green→Verify 까지 마쳤으나 tester 가 운영 부피 실측에서 잔여 결함 (디폴트 limit=5000 < 18,435) 식별 → 사이클 53.1 후속 1-line 시정 + S6 회귀 가드 추가로 통합 종결. *재집계 사고는 실패가 아닌 결함 검증의 일부* — 복구 metrics 가 사이클 53/53.1 시정 효과의 정량 증거. 사이클 51 (boot_manager) → 52 (B-1) → 53/53.1 (B-2/B-4) 연속 운영 진단 카드 3 사이클 종결. 다음 사이클 후보: B-3 MEDIUM (eager 갱신) 또는 R-1 HIGH (SellRejectionTracker, B-1 구조 정리) 우선. domain-expert 자문 미호출 (매매 행위 변경 0, 분석 경로 한정). |
| 2026-06-01 | 사이클 52 — 매도 좀비 폭주 차단 (카드 B-1, 시장 closed 거부 후 외부 재호출 게이트) | **운영 진단**: 2026-05-31T23:00:00~23:09:53 UTC 10분간 종목 064400 매도 거부 (장운영시간 외 / `is_market_closed_rejection`) **500+건 / 평균 2건/초** 폭주. Root cause — `OrderEngine.execute_sell` 의 `is_market_closed_rejection` 가드가 *호출 내* 재시도만 차단 + positions 보존 + return 만 수행, **외부 재호출 차단 플래그 부재**. `risk.on_tick` / `_swing_rest_poll_loop` 가 매 tick (~0.5s) exit 평가하며 동일 종목에 또 `execute_sell` 진입 → KIS REST 재폭주 → rate limit 위반 위험 + `system_logs` 노이즈로 다른 결함 가시성 저하. 결과적으로 23:20 시장 정상화 후 매도 체결 (손실 0) 되었으나 다음 시나리오에서 KIS 차단 시 손절 자체 불가능 위험. **시정** (`src/engine/order_engine.py` +66 / `src/engine/scheduler.py` +2): (1) **ticker별 TTL 차단 set** — `OrderEngine.__init__` 에 `_market_closed_blocked: dict[str, datetime]` (ticker → TTL 만료시각) + `_market_closed_blocked_logged_today: set[str]` (사이클 31 R6 `_risk_silent_skip_logged_today` 동형 emit cap 패턴) 신규. (2) **모듈 헬퍼 `_compute_next_market_open_kst(now)`** — now < 09:00 KST 이면 오늘 09:00, now >= 09:00 KST 이면 다음 영업일 09:00 (휴장 처리는 `_pending_next_day_clear` 가 흡수하므로 단순 +1일 OK, 보수적). KST 시각은 `from src.engine.scanner import KST_TZ` 명시적 사용 (사이클 36 NameError 선례 회피). (3) **`execute_sell` 진입 직후 게이트** — `_market_closed_blocked` 에 ticker 등록 + TTL 미경과 시 *조용히 skip* (ticker별 INFO 1줄만 발화 → `_market_closed_blocked_logged_today` cap). `is_market_closed_rejection` 검출 시 `_market_closed_blocked[ticker] = _compute_next_market_open_kst(now)` 등록 + WARNING + return (positions 보존 동일). KIS REST 호출 1회만 발생. (4) **`OrderEngine.reset_daily_state()` 캡슐화** — 두 set 일괄 clear. `scheduler.py::_reset_daily_state` 에서 `self.order_engine.reset_daily_state()` 위임 호출 (사이클 28~30 daily reset 패턴 일관). **회귀 가드** `tests/unit/engine/test_b1_market_closed_zombie_block.py` 7~8 시나리오 — `execute_sell` 100회 연속 호출 시 KIS 호출 1회만 발생 / TTL 만료 후 재진입 허용 / `reset_daily_state` 정상 clear / 정규장 정상 경로 무영향 / NXT 사후 보강 + 차단 등록 이중 발화 안전 / `_completed_orders` race 가드 (사이클 30) 와 독립 동작. **CLAUDE.md 동기화**: 핵심 안전규칙 "NXT 매도 거부 좀비 차단" 항목에 *진입 차단까지 보장* 1줄 추가 + `src/engine/CLAUDE.md` `is_market_closed_rejection` 매도 분기 4줄 + 절대 규칙 동기화. `_workspace/test_index.yaml` 영향 인덱스 재생성. **백엔드 1766 PASS (1748→+18) / 2 skipped + 회귀 0건.** 매매 동작 변경 0 (positions 보존 동일, 다음 정규시간 재진입 동일, 다른 종목 영향 0). tester 4개 위험 메모 (정규장 정상 경로 / NXT 사후 보강 + 차단 등록 이중 발화 / TTL 만료 후 재진입 / `reset_daily_state` 위임 순서) 모두 검증 PASS → 프로덕션 배포 가능 판정. 카드 출처 `_workspace/2026-06-01_log_analysis.md` B-1 섹션 / 발주서 `_workspace/cycle_b1_market_closed_zombie_block_spec.md` / Red 로그 `_workspace/red/cycle_b1_market_closed_zombie_block.md`. **잔여 카드** (B-2 `next_day_clear_drained` 로그 발화 누락 / B-3 NXT 다운그레이드 폭주 / B-4 trade_metrics KST 경계 결함 / R-1~3 / V-1~4) 보존 — 별도 사이클. | 매매 안전성 critical — KIS rate limit 위반 위험 + 손절 차단 가능성. 사이클 31 R6 `_risk_silent_skip_logged_today` emit cap 패턴 + 사이클 30 `_completed_orders` set 동형 구조. CLAUDE.md "NXT 매도 거부 좀비 차단" 정책의 *진입 차단* 확장. 사이클 51 (refactor 카드 #1 1단계 `_boot()` 추출) 직후 — 운영 진단 카드가 refactor 사이클을 끼어든 우선순위 (B-1 CRITICAL). domain-expert 자문 미호출 (매매 행위 변경 0 단일 책임). 후속 카드 B-2/B-3/B-4 (다음 사이클) + R-1 `SellRejectionTracker` 단일 정책 객체 분류 통합 (refactor 누적 후) + V-1 매도 거부 폭주 실시간 알람 (별도 가시화) |
| 2026-06-01 | 사이클 51 — refactor-review 카드 #1 (HIGH) 1단계: `_boot()` boot_manager 추출 | **신규 모듈** `src/engine/boot_manager.py`: 274L `_boot()` 본체 형제 파일 추출 (사이클 48 `stale_tracker.py` 와 동일 패턴 — 디렉토리 분해 X, 형제 파일 O). `boot(scheduler: "TradingScheduler")` async 함수 시그니처 — scheduler 인스턴스 인자로 받고 모든 메서드/속성은 `scheduler.X` 로 접근 (행위 보존). 외부 import 경로 (`from src.engine.scheduler import ...`) 영향 0 — 16+ 호출처 그대로. `src/engine/scheduler.py`: 본체 → wrapper 2 줄 (`from src.engine.boot_manager import boot as _boot_impl; await _boot_impl(self)`) + dead 빈 디렉토리 `src/engine/scheduler/` (이전 사이클 49 시도 잔재) + `__pycache__/__init__.cpython-313.pyc` stale 캐시 정리. **scheduler.py 4,142L → 3,877L (-265L)**. 기존 `import` (token_manager, get_balance, get_daily_orders, write_log) 는 다른 메서드 (`_preissue_all_tokens` / `_sync_positions_from_balance` 등) 가 사용하므로 양쪽 모듈에 모두 보존. lazy import 위치 (DB positions / supabase / strategy_base.Position / scanner.ticker_names) 100% 보존 — 순환 import 회피 패턴 동일. **회귀 가드**: 신규 `tests/unit/engine/test_boot_manager_extraction.py` 3 케이스 (모듈 boot async callable / scheduler._boot 위임 1회 / 호출 순서 preissue→token→load_config→balance→regime→ratio→eager 보존). **통합 테스트 fixture 갱신**: `tests/integration/test_boot_cash_usage_ratio.py` + `test_boot_market_regime.py` patch 경로 `src.engine.scheduler.{token_manager,get_balance,get_daily_orders}` → `src.engine.boot_manager.*` 일괄 치환 + `_preissue_all_tokens` no-op patch 추가 (토큰 캐시 만료 환경 의존 차단). `tests/integration/conftest.py` `scheduler_env` 에 `src.engine.boot_manager.write_log` fake_write_log 추가 patch (raising=False). **백엔드 1758 PASS (1748→+3 신규 + 7 통합 흡수) + 회귀 0건.** 매매 동작 변경 0. **사이클 49/50 운영 시정 (VCP/BFB) 이 사이클 49/50 번호 점유 → 본 작업은 사이클 51 로 재부여** (TaskID #29). 옵션 D 6번째/7장 중 HIGH #1 의 첫 단계. 후속 단계 (stale_manager / swing_manager / settlement_manager) 는 별도 사이클로 분리 — `_boot()` 분리만으로도 단일 책임 명시화 + 모듈 라인 -6.5% 감소. 빈 디렉토리 충돌 위험 영구 해소. | refactor-review 옵션 D (LOW→MEDIUM→HIGH) 6번째 카드. HIGH #1 scheduler 분해의 첫 단계 — 가장 응집도 높은 단일 책임 (`_boot()` 토큰 사전 발급 + 잔고 + 매크로 + DB 포지션 복구 + KIS 잔고 보완 + PENDING 정리 + 미체결 복구 + stock_master eager) 추출. 사이클 48 stale_tracker 패턴 검증 완료 후 답습 — 회귀 위험 최저 (디렉토리/파일 충돌 0, import 변경 0). 옵션 A (사용자 선택, 사이클 49 team-leader stall + 한도 도달 후 메인 세션 직접 진행). 후속 단계는 운영 안정성 우선 — KRX 메인 시간 회피 + 사이클 1회당 1 모듈 분리 권고 |
| 2026-05-31 | 사이클 49 — VCP Pullback "마지막 폭 0.0%" 결함 시정 (운영 funnel 확정) | **운영 진단**: `vcp_breakout` 가 사이클 48 BFB/VCP 0건 매매 결함 전면 시정 후에도 추가 30일 연속 0건 매매 잔존. `strategy_funnel_snapshots` 5/26~5/29 4영업일 누적 33/33 종목이 step 6 (Pullback 점진 수축) 에서 "마지막 폭 ≈ 0.0%" 동일 사유로 탈락 (삼성전자우/POSCO홀딩스/삼성SDI/SK텔레콤/LIG넥스원/엔씨소프트/이오테크닉스/대주전자재료 등 우량주). **Root cause 2개** — (a) `_check_pullback_sequence` 의 단순 swing 검출이 chrono 끝부분 rising 중이면 `j=k=n-1` → `high==low` → `high > low` 가드로 마지막 pullback 누락 → `base["last_pullback_pct"]` 미설정 → funnel reason 의 `base.get("last_pullback_pct", 0)` 디폴트가 "0.0%" 표시 (운영자 오인). (b) 등호 포함 `>=`/`<=` swing 검출이 한국 KRX 평탄 우량주의 1원 단위 미세 변동도 swing 으로 인식 → 회수 2~4회 범위 위반 빈발. **시정** (`src/engine/strategies/vcp_breakout.py`): (1) **ATR threshold swing 검출 (ZigZag 변형)** — 신규 파라미터 `min_swing_atr_mult=0.5` `DEFAULT_PARAMS` 추가. 베이스 ATR (구간 평균 high-low) × 0.5 미만 변동은 노이즈로 무시. ATR 산출 실패 시 종가 평균의 0.3% 폴백. running_max/running_min 추적 + threshold 이상 반전 시에만 swing 확정. (2) **마지막 swing 미완성 포함** — state machine ('undefined'/'up'/'down') 으로 끝까지 진행. `state=='down'` 중 끝나면 마지막 pivot_high → running_min 의 진행 중 pullback 도 마지막 pullback 으로 포함. (3) **strict 점진 수축** — `curr >= prev` 유지하되 노이즈 필터 후엔 동일 폭 swing 거의 발생 안 함. (4) **funnel reason 정확성** — False 반환 경로에서도 `base["last_pullback_pct"]` 에 실제 마지막 swing 폭 (또는 swing 0개면 명시적 0.0) 기록. 신규 회귀 가드 `tests/unit/engine/strategies/test_cycle49_vcp_pullback_width_fix.py` 6 케이스 (마지막 swing 미완성 / 노이즈 필터 파라미터 / 전형적 VCP 3회 점진 수축 통과 / "0.0%" 디폴트 제거 / DEFAULT_PARAMS 신규 키 / strict 점진 수축 회귀). 백엔드 1748 PASS (1742→+6) + 회귀 0건 (다른 5 전략 + scan/매매 흐름 무영향 — 매수 진입 임계만 시정). 명세 동기화: `_workspace/00_leader_trading_rules.md` 6-F 조정 시퀀스 사이클 49 절 + `DEFAULT_PARAMS` `min_swing_atr_mult` 추가. PR #16 (브랜치 `claude/vcp-ema-copy-fix`, 카피 정합화) 무관 별도 신규 브랜치 `cycle49/vcp-pullback-width-fix` 에서 진행. | 사용자 1.17M 자본 매매 critical — 사이클 48 시정 후에도 VCP 단독 0건 매매 잔존 ("마지막 폭 ≈ 0.0%" 결함 5 영업일 연속 재현). 사이클 41 funnel 진단 ("Pullback 9→0 새 결함 위치 영구 기록") 의 종결 — 진단만 했고 시정 안 된 채 누적. ZigZag indicator 표준 0.5×ATR 임계 채택 (미네르비니 본인도 noise filter 권장). 진입 빈도 정상화 목표 (월 0건 → 월 5~10건 합리적). 운영 권고: 다음 영업일(2026-06-01) 09:30 funnel snapshot step 6 통과 카운트 > 0 확인. 첫 거래 발생 시 손절(-7%) / 베이스 하단 / ATR 트레일링 / 50일 EMA 이탈 4중 청산 정상 발화 tester 검증. 진입 빈도 과다 시 `min_swing_atr_mult` 0.7~1.0 상향 검토 (AI 자문 PARAM_RANGES 추가는 후속). |
| 2026-05-27 | 사이클 48 — BFB/VCP 0건 매매 결함 전면 시정 + ScanMonitor TZ (운영 DB 확정) | **운영 진단**: `bull_flag_breakout` / `vcp_breakout` 가 배포 이후 단 한 건도 매매 못함 (`trade_history` 0건 + `daily_log_reports` 매일 signals=0). **(1) BFB 유니버스 시간무관화** `src/engine/strategies/bull_flag_breakout.py`: `acml_vol`(당일 누적 — 07:50 boot 시 0) → volume-rank (`FHPST01710000`, blng 0/1/3 합집합) `prdy_vol × 전일종가` 로 교체 (VB/LTV 동일 산식). "눌림목 돌파 유니버스 0종목" 매일 아침 결함 제거. **(2) BFB Pole 임계 완화**: `pole_min_return` 20→15, `pole_max_red_ratio` 0.30→0.45 (한국 일일 ±30% 환경 + 음봉 30% 교집합 0 차단, 돌파순간+거래량 2배 컷 유지). **(3) VCP 추세필터 EMA 하향** `src/engine/strategies/vcp_breakout.py`: `ema_long` 200→120, `ema_mid` 150→60 (KIS `fetch_daily_candles` 단일호출 100일 한도 내 정배열 계산 가능 — 200EMA 형식만 두던 사이클 33 `effective_ema_long` 자동축소 결함 정직화), `last_pullback_max` 0.08→0.12. `effective_ema_long` 안전망 가드 보존. **(4) scheduler** `src/engine/scheduler.py`: 장중 재prepare 안전망에 BFB/VCP 추가. **(5) 프론트 TZ** `frontend/src/components/ScanMonitor.tsx`: `formatRunAt` `timeZone='Asia/Seoul'` 누락 시정 — 해외/UTC 컨테이너에서 07:48 boot 이 "0시 48분" 오표시되던 버그. `formatRunAt` export + 회귀 가드 (`__tests__/ScanMonitor.formatRunAt.test.ts`). 신규 회귀 가드 (`test_cycle48_bfb_vcp_zero_trade_fix.py` + frontend 2). 백엔드 1731 PASS (1724→+7) / 프론트 162 PASS (160→+2) / tsc 0. **매도/손절/Trailing/익일청산 로직 미변경 — 매수 진입 임계만 완화.** **(6) P1 구독 배선 편입 (PR #15 codex 리뷰 후속)** `src/engine/scheduler.py` + `scanner.py`: BFB/VCP 매수 신호는 (donchian 폴링과 달리) `risk.on_tick`(WebSocket tick) 으로만 평가되는데, 후보가 구독되는 유일한 소스 `_collect_breakout_tickers()` 가 VB/LTV 만 순회 → BFB/VCP 후보 미구독 → tick 미수신 → 매수 평가 영영 안 됨 (유니버스/임계 시정만으로는 0건 지속). `_collect_breakout_tickers()` 4 전략 편입 (`_scan_loop` extra / `_collect_presubscribe_tickers` / `_build_priority_groups` breakout 자동 전파) + `_build_subscription_source_counts()` + scanner 구독완료 로그 `bfb/vcp` 키. BFB/VCP 는 VB/LTV 와 동일 breakout 그룹 LOW+bypass_limit=False (HIGH 41슬롯 보장 불변). 신규 회귀 가드 `test_cycle48_bfb_vcp_subscription_wiring.py` 4 케이스. 백엔드 1731→1735 PASS / 프론트 162 PASS / tsc 0. 리뷰 nit 동반 정리 (테스트 TZ delete 분기 / BFB docstring 15·45 / VCP 라벨·docstring 50·60·120 일반화, 리턴키 ema150/ema200 레거시 유지 / 미사용 import). 커밋 `b1b7dcd` (1~5) + 후속 커밋 (6+nit). 상세 명세 `_workspace/00_leader_trading_rules.md` 사이클 48 절 (line 2386~). | 사용자 1.17M 자본 매매 critical — 2개 전략이 가동 이후 매수 신호 0 = 매일 미매수 결함 누적. 운영 DB 로 "시장 자연 0" 이 아닌 코드 결함임을 확정 (BFB boot 시점 `acml_vol=0` 유니버스 공집합 + 임계 교집합 0 / VCP KIS 100일 한도로 200EMA 계산 불가). 사이클 33/40 의 부분 완화 + 사이클 39/41 funnel 진단 누적의 종결. 진입 빈도 증가 가능성 → 운영 권고: 첫 며칠 낮은 비중으로 승률/손절률 관찰 후 상향. 진짜 200EMA 분할 fetch 인프라는 운영 1주 후 별도 검토 (사이클 33 권고 유지) |
| 2026-05-22 | 사이클 48 — refactor-review 카드 #2 (MEDIUM): Stale 추적 dict 통합 (`StaleTrackerState` 데이터클래스) | **신규 모듈** `src/engine/stale_tracker.py`: `StaleTrackerState(dataclass)` 7 필드 (`retry_count` / `last_resubscribe_at` / `force_retry_history` / `silent_inactive_first_seen` / `silent_inactive_recovery_count` / `universe_excluded_today` / `last_ccnl_cache`) + `reset_daily()` 7 필드 일괄 clear. `src/engine/scheduler.py::TradingScheduler.__init__`: 사이클 17/24/28/29-R1/32/37/45 누적 7 필드 → `self._stale_state = StaleTrackerState()` 통합. **호환 layer 7 property (getter + setter)**: `_stale_retry_count` / `_stale_last_resubscribe_at` / `_stale_force_retry_history` / `_silent_inactive_first_seen` / `_silent_inactive_recovery_count` / `_universe_excluded_today` / `_last_ccnl_cache` — 기존 `self._stale_*` 직접 접근/할당 패턴 모두 보존 + dict 인스턴스 동일성 보장 (race 차단). `_ensure_stale_state()` 헬퍼 — `__new__` 인스턴스 graceful 자동 init (외부 test mock 호환). `_reset_daily_state` 7 줄 clear → `self._stale_state.reset_daily()` 1 줄 위임. 사이클 추가 시 신규 필드 누락 사고 차단. **사이클 45 TTL evict 정책 보존** (`last_ccnl_cache` 5분 + `force_retry_history` 60분 sliding window). 신규 회귀 가드 11 케이스 (`test_cycle48_stale_tracker_state.py` S-1~S-7: 데이터클래스 + 7 필드 default_factory + 독립 인스턴스 + reset_daily 일괄 clear + scheduler _stale_state 보유 + 호환 layer 7 property + reset_daily 위임 + dict 인스턴스 동일성 + 사이클 45 _evict_expired_ccnl + _prune_force_retry_history 보존). **inspect 회귀 6건 의미 갱신**: `test_cycle37_ccnl_cache.py` / `test_scheduler_stale_force_retry.py` / `test_universe_guard.py` 의 `__init__` 직접 필드 + `_reset_daily_state` `.clear()` inspect 검증 → 통합 `_stale_state` / `_stale_state.reset_daily()` 호환 OR 분기 추가. 백엔드 1724 PASS (1713→+11 신규) + 회귀 0건. 매매 동작 변경 0 (호환 layer 로 기존 접근 패턴 모두 동작). 사이클 49 (#1 HIGH scheduler 분해) 사전 준비 — 모듈 분리 첫 단계 (stale_tracker.py 추출). 커밋 `(별도)` | refactor-review 옵션 D (LOW→MEDIUM→HIGH) 5번째 카드. scheduler.py 의 stale 관련 7 dict/set 필드 산재 (사이클 28/29-R1/29-R2/32/37/45 누적) → 단일 데이터클래스 통합. 관계 불명시 → 사이클 추가 시 reset 누락 위험 차단. 사이클 49 scheduler 분해 사전 준비 (stale_tracker.py 분리). 사이클 45 TTL evict 정책 + `_reset_daily_state` 동행 clear 정책 모두 보존 |
| 2026-05-22 | 사이클 47 — refactor-review 카드 #3 (MEDIUM): Funnel hook DRY (`FunnelStage` 모듈 상수 + `_record_funnel_pipeline_step` 위임) | `src/engine/strategy_base.py`: `FunnelStage(frozen=True)` 데이터클래스 신규 (step_no, step_name) + `_record_funnel_pipeline_step(stage, survived, excluded=None, *, step_conditions=None)` 위임 헬퍼 (사이클 39 `_record_funnel_step` 호환 layer 보존). 3 전략 파일에 `FUNNEL_STAGES: tuple[FunnelStage, ...]` 모듈 상수 추가 (BFB 8단계 + VCP 8단계 + donchian 8단계 = 24 단계 정의 통합). 각 전략 `prepare()` 의 24 회 `_record_funnel_step(step_no=..., step_name=..., ...)` 호출 → `_record_funnel_pipeline_step(FUNNEL_STAGES[i-1], ...)` 위임 표준화. step_conditions 는 runtime 평가 (f-string 동적 문자열) 호출 시점 별도 인자 보존 — 사이클 41 동작 동일. **donchian step_name 정적화**: 사이클 41 `f"{donchian_period}일 신고가 돌파"` 동적 문자열 → 정적 `"신고가 돌파"` + step_conditions 통해 동적 값 노출 (CLAUDE.md `_workspace/00_leader_trading_rules.md` 동기 권고). 신규 회귀 가드 11 케이스 (`test_cycle47_funnel_pipeline_dry.py` D-1~D-6: 데이터클래스 + frozen + 필수 필드 + 위임 헬퍼 + step_conditions 호환 + excluded 호환 + 3 전략 FUNNEL_STAGES 8 단계 정합 + step_name 사용자 명세 + 회귀 가드 BFB 결과 동일성). 사이클 39+41 22 케이스 의미 갱신 없이 통과 (위임 layer 동작). 백엔드 1713 PASS (1702→+11 신규) + 회귀 0건. **운영 효과**: 24 회 반복 패턴 → FUNNEL_STAGES 모듈 상수 단일 정의. 새 전략 추가 시 funnel hook 누락 위험 차단. step_no/step_name 분산 → 모듈 상수 일관. 매매 동작 변경 0 (prepare 결과 + 퍼널 카운터 reset 시점 + 사이클 41 종목명 + 사유 hook 동작 모두 보존). 커밋 `(별도)` | refactor-review 옵션 D (LOW→MEDIUM→HIGH) 4번째 카드. BFB/VCP/donchian 각 `prepare()` 24 회 반복 _record_funnel_step 패턴 → FunnelStage 모듈 상수 + 위임 헬퍼로 DRY 통합. 새 전략 추가 비용 감소 + funnel hook 누락 위험 차단 + 단계 step_no/step_name 일관성 보장. 사이클 39+41 호환 layer 보존 |
| 2026-05-22 | 사이클 46 — refactor-review 카드 #6 (MEDIUM): 5분 emit task 통합 (`_session_health_loop` + `_heartbeat_metrics_emit_once`) | `src/realtime/websocket.py`: 사이클 42 `_heartbeat_metrics_loop` (인스턴스별 5분 task) 폐기 → `_heartbeat_metrics_emit_once` 단발 헬퍼 추출 (emit + 카운터 reset 로직 그대로). `_heartbeat_metrics_task` 인스턴스 필드 제거. `connect()` 의 task 생성 + `disconnect()` 의 task cancel 제거 (좀비 task 방지 + 단순화). `src/engine/scheduler.py`: `_session_health_task: asyncio.Task | None` 필드 + `_session_health_loop()` (5분 주기 task, `HEARTBEAT_METRICS_INTERVAL_SECS=300` 정합) + `_emit_heartbeat_metrics_all_sessions()` (메인 `kis_ws` + 보조 `kis_ws_pool._quotes` 일괄 호출 + 한 세션 실패 → 다른 세션 격리). `start()` 의 task 발화 (line 372) + stop()/finally 두 곳의 cancel tuple 에 `_session_health_task` 추가 (test_scheduler_stop_zombie_tasks 정합성 가드 9종 갱신). **사이클 42 회귀 가드 갱신**: `test_heartbeat_metrics_loop_emits_and_resets` 와 `test_heartbeat_metrics_log_format` 가 `_heartbeat_metrics_emit_once` 직접 호출 (loop 폐기 의미 갱신). `test_disconnect_cancels_metrics_task` 폐기 → 사이클 46 의미 갱신 (필드 자체 제거 확인). **`_stale_watcher_loop` (120s) 절대 보존** (사이클 24/29-R1/29-R3 안전망). `_report_tick_coverage` 는 `_scan_loop` (5분) 안에서 호출 — 중복 발화 차단. 신규 회귀 가드 11 케이스 (`test_cycle46_session_health_loop.py` H-1~H-8: 단발 헬퍼 존재 / 카운터 reset / loop 제거 / task 필드 제거 / connect 제거 / disconnect 제거 / scheduler task 존재 / 일괄 호출 / 메인+보조 일괄 / graceful 격리 / asyncio.CancelledError graceful). 백엔드 1702 PASS (1691→+11 신규) + 회귀 0건. **운영 효과**: 세션별 task N+1 개 → 통합 1개 task. 로그 포맷 동일 (사이클 42 호환). 메인+보조 인스턴스별 독립 카운터 (사이클 43 라벨 통일 보존). 매매 동작 변경 0. 커밋 `(별도)` | refactor-review 옵션 D (LOW→MEDIUM→HIGH) 3번째 카드. 사이클 42 인스턴스별 5분 task 분산 → scheduler 통합 호출 일원화. 좀비 task 위험 자체 제거 (task 필드 제거). 메인 + 보조 일관성 + 단순화. `_stale_watcher_loop` (120s) 와 `_report_tick_coverage` (5분 `_scan_loop` 내부) 보존 — 안전망 정책 + 중복 발화 차단 |
| 2026-05-22 | 사이클 45 — refactor-review 카드 #5 (MEDIUM): TTL 캐시 자동 evict (`_last_ccnl_cache` + `_stale_force_retry_history`) | **Phase 1 진단**: `_last_ccnl_cache` (TTL 5분, 사이클 37) 자동 evict 부재 — TTL hit 판정만 있고 만료 후 dict 제거 안 됨. `_stale_force_retry_history` (60분 sliding window, 사이클 29-R1) 부분 evict — 해당 ticker force_retry 진입 시 in-place evict 정상, 그러나 빈 list 잔존 + 다른 ticker stale 인 사이에 해당 ticker history 갱신 안 됨. **Phase 2 Green**: `src/engine/scheduler.py` 신규 헬퍼 2개. `_evict_expired_ccnl(now, ttl_secs=300) -> int` (TTL 만료 항목 일괄 제거, `_refresh_stale_ccnl_cache` 진입 시 자동 호출 + DEBUG `[ccnl_cache_evict] expired=N remaining=M`). `_prune_force_retry_history(ticker, now, window_secs=3600)` (sliding window 외 항목 제거 + 빈 list 시 dict 에서 자동 제거). 신규 회귀 가드 10 케이스 (`test_cycle45_cache_auto_evict.py` E-1~E-7: 헬퍼 존재 / TTL 만료 제거 / 이내 보존 / 빈 캐시 safe / _refresh 진입 자동 호출 / 빈 list ticker 제거 / window 내 보존 / 없는 ticker noop / 대량 pruning 빈 list 0 / 매매 동작 동일성 캐시 hit 정책 보존). 백엔드 1691 PASS (1681→+10 신규) + 회귀 0건. 매매 동작 변경 0 (캐시 hit/miss 정책 + cap 12회 비교 동일성 보존). 커밋 `(별도)` | refactor-review 옵션 D (LOW→MEDIUM→HIGH 순차) 2번째 카드. 영업일 중 stale watcher 가 더 이상 평가 안 하는 종목 (회복/R4 제외) 의 `_last_ccnl_cache` 영구 잔존 → 영업일 누적 메모리 누수. `_stale_force_retry_history` 도 빈 list 잔존 누수. 사이클 37 TTL 5분 정의 + 사이클 29-R1 cap 12회/시간 + `_reset_daily_state` 동행 clear (이중 안전망) 모두 보존. 영업일 중 cache size 안정 + 매일 일괄 reset |
| 2026-05-22 | 사이클 44 — refactor-review 카드 #7 (LOW): 종목명 lookup 단일 헬퍼 (`scanner.resolve_ticker_name`) | `src/engine/scanner.py:t()` 함수 직후에 `resolve_ticker_name(ticker: str \| None) -> str` 단일 헬퍼 추가 — 폴백 순서 `ticker_names` (동적, WS 시세 수신 시 갱신) → `STATIC_TICKER_NAMES` (정적, scanner 자기 파일 정규식 파싱) → `""` (graceful, 예외 전파 금지). `src/engine/strategy_base.py::_resolve_ticker_name` (사이클 41 호환 layer 보존) 가 신규 `scanner.resolve_ticker_name` 위임 — BFB/VCP/donchian funnel hook (사이클 41) 자동 동기. 빈 ticker/None → 빈 문자열 (예외 X). AttributeError/TypeError graceful (mock/import 순서 보호). 신규 회귀 가드 10 케이스 (`test_cycle44_resolve_ticker_name.py` N-1~N-7: 헬퍼 존재 / 동적 1순위 / 정적 폴백 / lookup miss / 빈 ticker / None / strategy_base 위임 / 동적 갱신 즉시 반영 / funnel hook 호환). 백엔드 1681 PASS (1671→+10 신규) + 회귀 0건. **다른 호출처 (condition.py 5곳 / order_engine.py 53곳 / scheduler.py 21곳) 마이그레이션은 별도 후속 카드** (회귀 위험 우려 — 본 사이클은 헬퍼 추가 + 사이클 41 호환 layer 위임만). 매매 동작 변경 0 (lookup 결과 동일성 보존). 커밋 `(별도)` | refactor-review 옵션 D (LOW→MEDIUM→HIGH 순차) 시작. 18+ 곳에서 4 경로 혼재 (`ticker_names` / `STATIC_TICKER_NAMES` / `_resolve_ticker_name` / `t()`). 분산 lookup → 단일 진입점. 사이클 41 funnel hook (BFB/VCP/donchian) 가 이미 `_resolve_ticker_name` 사용 중이라 호환 layer 위임만으로 자동 동기. 점진적 마이그레이션 — 매매 결과 동일성 절대 보존 |
| 2026-05-22 | 사이클 43 — 세션 라벨 통일 (1-based index `quote-N` → DB 라벨 ISA/sub/gold) | `src/realtime/websocket_pool.py::_session_label(ws)`: `_quotes` 인덱스 + `f"quote-{i}"` → `_quotes` 순회 후 `ws._label` 직접 반환 (graceful "unknown" 폴백). `get_session_status()`: `(q, f"quote-{i}")` → `(q, getattr(q, "_label", "") or "unknown")`. `disable_quote_session(label)`: `quote-` startswith + int parse → `_quotes` 순회 후 `_label` 매칭 (메인 noop 가드 절대 보존 + idempotent). 사이클 28 `get_subscriptions_by_session` 은 `_session_label` 재활용 → 자동 동기. UI `KisAccountPoolCard` 의 `sessionLabelBadge` 는 main 외 모두 회색 (분기 변경 0). 사이클 42 PINGPONG `[ws_heartbeat]` 와 라벨 일관 (이미 DB 라벨 사용 중). **회귀 갱신**: `test_websocket_pool_disable.py` / `test_websocket_pool.py::test_subscribe_low_priority_calls_quote_session` / `test_get_session_status_returns_main_and_quotes_breakdown` / `test_websocket_pool_session_grouping.py::test_get_subscriptions_by_session_main_and_quote` / `test_routes_subscriptions_pool.py::_build_session_mock` — mock 세션에 `_label` 명시 주입. 신규 회귀 가드 9 케이스 (`test_cycle43_session_label_unified.py` L-1~L-7: main 라벨 보존 / DB 라벨 반환 / 풀에 없는 세션 graceful / get_session_status DB 라벨 / disable_quote_session DB 라벨 매칭 + disconnect / main noop / 없는 라벨 idempotent / get_subscriptions_by_session DB 라벨). 백엔드 1671 PASS (1662→+9 신규) + 프론트 160 PASS + Vite 빌드 성공. 매매 동작 변경 0 (라벨 노출만 변경 — `_quotes[idx]` 인덱스 무변경 + 메인 라벨 `"main"` 절대 보존 + `_select_session` 라운드로빈 무영향). 커밋 `(별도)` | 사이클 42 ws_heartbeat 는 DB 라벨 사용 중 (label kwarg 주입) 이나 `[tick_coverage_session]` / `[stale_watcher_detail]` / `[priority_drop_pool]` / `[silent_inactive_force_reconnect]` / `/api/realtime/subscriptions` / `KisAccountPoolCard` 모두 `quote-1`/`quote-2`/`quote-3` 1-based index 사용 — 라벨 불일치로 운영자가 사용자 등록 라벨 (ISA/sub/gold) 과 quote-N 매핑 헷갈림. 사용자 결정: DB 라벨로 통일. `_quotes[idx]` 인덱스 자체는 변경 0 (label 반환 로직만 변경) → 사이클 7-C 풀 통합 / 사이클 28 stale 추적 / 사이클 42 PINGPONG 메트릭 모두 자동 동기 |
| 2026-05-22 | 사이클 42 — PINGPONG 가시성 강화 (Option L1 DEBUG echo + Option L2 [ws_heartbeat] 5분 통계) | `src/realtime/websocket.py`: `KisWebSocket.__init__` 에 `label: str = None` kwarg + 신규 필드 4개 (`_label` / `_pingpong_recv_count` / `_pingpong_last_at` / `_pingpong_window_start_at` / `_heartbeat_timeout_count` / `_heartbeat_metrics_task`). 메인은 `_label="main"` / 보조는 pool 주입 `quote-N`. **L1 (DEBUG)**: `_handle_raw` PINGPONG 분기에 `logger.debug("[ws_pingpong_echo] label=%s")` + 카운터 += 1 + `_pingpong_last_at = datetime.now(KST)` 갱신. **L2 (INFO 통계)**: `HEARTBEAT_METRICS_INTERVAL_SECS=300` 상수 + `_heartbeat_metrics_loop` 헬퍼 (5분 주기 `[ws_heartbeat] label=... window=... pingpong_recv=... avg_interval=... last_age=... heartbeat_timeout=...` INFO + `write_log` 영구 보존 + 카운터 reset + `asyncio.CancelledError` graceful). **Receive loop 통합**: `_receive_loop` `asyncio.TimeoutError` 분기에 `_heartbeat_timeout_count += 1` + warning 메시지에 label/count 포함. **Task lifecycle**: `connect()` 진입 직후 task 생성 (재연결 루프 내부 아닌 1회) + `disconnect()` 에서 task cancel + await 정리 (좀비 task 방지). `src/realtime/websocket_pool.py:147`: 보조 세션 생성 시 `label=label` 명시 주입. 회귀 11 신규 (P-1~P-8) + 백엔드 1662 PASS. 커밋 `(별도)` | ping-pong 메커니즘 자체는 정상 동작 (Layer 1/2/3 검증, 5/22 08:57 Layer 3 발화 확인) 이나 송수신 명시 로그 부재 → 운영 실시간 검증 도구 부재. 운영자가 5분 주기 INFO 로 송수신 빈도 + heartbeat timeout 누적 카운트 즉시 확인. DEBUG echo 는 폭주 방지 위해 LOG_LEVEL=DEBUG 일시 활성화 시만 노출. 메인+보조 세션 인스턴스별 독립 카운터 (label 분리). 매매 동작 변경 0 — 수신 분기 코드 추가만. write_log 실패 graceful — 다음 5분 사이클 자연 재시도 |
| 2026-05-22 | 사이클 41 — funnel 단계별 종목명 + 탈락 사유 정밀 추적 + UI 단계 조건 툴팁 | `src/engine/strategy_base.py`: `_resolve_ticker_name(ticker)` 헬퍼 (`scanner.ticker_names` → `STATIC_TICKER_NAMES` → "" graceful) + `_record_funnel_step` 시그니처 확장 (`survived: list[str \| dict]` + `step_conditions: str \| None` kwarg + survived 자동 dict 변환). BFB/VCP/donchian `prepare()` 8단계 hook 보강 — 각 단계 `step_conditions` 명시 + 탈락 사유 수치 포함 (BFB 음봉 비율 / VCP **Pullback 9→0 새 결함** 진단 핵심 / donchian 신고가 미달 등). `src/db/strategy_funnel.py::insert_snapshot`: `survived_tickers: list` (string + dict 둘 다 호환) + `step_conditions` 옵셔널 (DB JSONB 스키마는 변경 없음 — native dict 지원). `scheduler._auto_capture_funnel_snapshots` 가 `step_conditions` 전달. `frontend/src/api/strategy-funnel.ts`: `SurvivedTickerRow` / `SurvivedItem` 타입 + `ExcludedSampleRow.name?` + `FunnelSnapshot.step_conditions?` 확장. `frontend/src/pages/StrategyFunnel.tsx`: 단계명 옆 `조건` 툴팁 (`title` + `data-testid="funnel-step-conditions-..."`) + 통과/탈락 종목 dict/string 분기 + 종목명 별도 표시. 회귀 13 신규 (`test_cycle41_funnel_name_reason.py` R-1~R-9) + 사이클 39 의미 갱신 (string 자동 dict 변환) + 백엔드 1651 PASS + 프론트 160 PASS + Vite 빌드 성공. 커밋 `6035fd4` | 사용자 5/22 12:27 보고 — 사이클 39 VCP 키 매핑 fix 효과 확인 (EMA 113→31, base 31→9 정상 순서) + Pullback 9→0 새 결함 위치 식별. 종목명 + 수치 포함 사유 영구 기록으로 5/23 자동 snapshot 후 사이클 42+ 임계 조정 권고 도출 가능. 사이클 34 string 배열 형식 하위 호환 보존. 매매 동작 무영향 (hook 추가만, prepare 결과 무변경) |
| 2026-05-22 | 사이클 40 — BFB 폴 검출 음봉 임계 완화 (DB only, 코드 디폴트 보존) | DB `strategy_config.params` BFB `pole_max_red_ratio: 0.30 → 0.40`. 코드 `DEFAULT_PARAMS` 변경 없음 (회귀 가드 유지). VCP / donchian 는 사이클 39 단계별 자동 snapshot 1주 누적 후 사이클 41 별도 결정 | team-leader 시뮬레이션 (정규분포 약세장) — BFB 음봉 ≤30% 통과율 0/24 (강한 모멘텀 시나리오에서도 0%). ≤40% 완화 시 9/24 (37.5%) 통과 예상. 폴 +20% 본질 의도 (강한 상승 후 횡보) 보존. 시뮬 vs 5/22 24→0 실측 정합 → 시장 자연 0 가 아닌 임계 결함 명확. 사용자 명시 승인 후 시나리오 A 진행 |
| 2026-05-22 | 사이클 39 — BFB/VCP/donchian funnel 0 정밀 진단 + 단계별 자동 hook + VCP 프론트 키 매핑 시정 | `frontend/src/components/ScanMonitor.tsx` VCP_STAGES 키 매핑 시정 (`trend_pass`→`trend_filter_pass` / `contraction_pass`→`pullback_pass`) — 사용자 5/22 "EMA 0인데 base 9" 보고의 본질 결함 (백엔드 `_scan_stats` 키와 프론트 매핑 불일치). `src/engine/strategy_base.py::_record_funnel_step` 헬퍼 + `_funnel_steps` 필드 + cap 200/20. BFB/VCP/donchian `prepare()` 단계별 hook (1~8 단계, 결과 무변경). `scheduler._auto_capture_funnel_snapshots` (09:30 일일 1회 자동 발화 + DB `strategy_funnel_snapshots` 단계별 INSERT + graceful). `_reset_daily_state` 동행 reset. 회귀 9 신규 + 백엔드 1638 PASS + 프론트 160 PASS. 커밋 `51afd09` | 사이클 33 fix (BFB acml_vol + VCP fetch_days=100) 효과 확인 (5/22 BFB 30→24, VCP 113→113 정상화). 잔존 결함 식별: BFB 폴 검출 24→0 + VCP EMA 정렬 113→0. 단계별 ticker 캡처로 운영 1주 누적 후 시장 자연 vs 코드 결함 영구 판별 가능 |
| 2026-05-22 | 사이클 38 — LTV `DEFAULT_TRADABLE_BOARDS` 사용자 의도 복원 (`("pre_nxt","main","post_nxt")`) + `tradable_boards` 매수 진입 전용 정책 명문화 | `src/engine/strategies/long_tail_volatility.py:45-54`: 사이클 26 `("main",)` → 3보드 복원. `DEFAULT_PARAMS["tradable_boards"]` 자동 동기. `src/engine/session.py:70-79 _DEFAULT_TRADABLE_BOARDS["long_tail_volatility"]` 동기화 (3 보드). VB 디폴트 `("main",)` 절대 보존. **정책 명문화**: `src/engine/strategy_base.py::StrategyConfig` docstring 에 `tradable_boards` 매수 진입 전용 정책 명시 — 매도/손절/Trailing/익일청산/15:20 강제청산/상한가 손절 모니터링은 어떤 전략에서도 PRE/MAIN/POST 무관 항상 작동. `src/engine/risk.py::on_tick` line 88-99 매도 분기에 사이클 38 정책 주석. **테스트**: 신규 회귀 7 케이스 (`test_cycle38_ltv_default_restored.py` — D-1~D-7, 매도 분기 보드 가드 무관 회귀 가드 포함). 사이클 26 KRX ONLY 회귀 가드 3 케이스 폐기 (`test_long_tail_volatility_krx_only.py` 파일 삭제, 옵션 A — 사용자 의도와 어긋남). `test_long_tail_volatility_active_boards.py` line 113-133 의미 갱신 (MAIN+PRE_NXT 동시 활성 시 둘 다 visible). **문서**: `CLAUDE.md` 핵심 안전 규칙 1행 추가 + VB 디폴트 표기 사이클 26 동기화. `src/engine/strategies/CLAUDE.md` LTV 절 갱신 + 안전 규칙 2 항목 추가. `src/engine/CLAUDE.md` risk.py 보드 가드 절 사이클 38 명문화. 회귀 7 신규 - 3 폐기 = +4 + 백엔드 1629 PASS. | 사용자 정책 명확화 (2026-05-22): "`tradable_boards` 는 매수 진입 전용. 매도는 어떤 전략에서도 PRE/POST 무관 항상 작동". 사이클 26 (2026-05-20) 의 LTV `("main",)` 변경이 사용자 운영 의도 (연속 상한가 익일 청산 + 야간 매수) 와 어긋남. DB `strategy_config` 는 운영 중 `["pre_nxt","main","post_nxt"]` 보존되어 있어 운영 사고 0 — 코드 디폴트만 동기화. VB 디폴트는 그대로 (15:20 일괄 청산 + OVERNIGHT 결함 차단). 사이클 26 의 다른 변경 (보드 스케줄 3 보드 / 시세 채널 시간대별 / 종목 단위 원자 전환) 보존 |
| 2026-05-21 | 사이클 37 — stale 종목 UI 에 KIS 실제 last_cntg_hour 추가 (WS 구독 의심 진단) | `src/engine/scheduler.py` (`_last_ccnl_cache: dict[str, dict]` 필드 + `_refresh_stale_ccnl_cache(stale_tickers, cap=20)` 헬퍼 — `_scan_loop` 5분 주기 호출, r≥2 stale 사전 차단 + TTL 5분 + cap 20 + 보유 우선) + `_reset_daily_state` 동행 clear. `src/routes/realtime.py`: `tickers_detail` 응답에 `last_cntg_hour` / `today_volume` 신규 (캐시 미스 → null). `frontend/src/components/KisAccountPoolCard.tsx`: "KIS 체결" / "KIS 거래량" 컬럼 + `formatCntgHour` / `isWsSubscriptionSuspect` 헬퍼 (5분+ 차이 → amber "WS 의심" 배지). 회귀 12 신규 (9 unit + 3 contract) + 백엔드 1625 PASS + 프론트 160 PASS + Vite 빌드 성공. 커밋 `0c447f2` | 사이클 35 의 `last_tick` (WS 수신) 만으로 stale 진단 곤란. 두 시각 비교 (`last_tick` 오래됨 + `last_cntg_hour` 최근 → 우리 WS 구독 문제 / 둘 다 오래됨 → 종목 거래 없음 정상) 으로 본질 분기 가능. 사이클 32 R4 `inquire_ccnl` 함수 재활용 + Rate Limit 보호 (TTL + cap + R4 race 무해). 매매 동작 무영향 |
| 2026-05-21 | 사이클 36 (긴급) — `KST` NameError + backtest `pending` status 미처리 시정 | `src/engine/scheduler.py:540-549`: AI 자문 자동 적용 분기에 `from src.engine.scanner import KST_TZ as _AUTO_APPLY_KST_TZ` import + `datetime.now(_AUTO_APPLY_KST_TZ).date()` 사용 (다른 함수와 일관). `src/engine/backtest_engine.py:197/222`: `if status in ("running", "pending"): return None` — 외부 백테스트 서버 큐 대기 상태 정상 처리. `wait_for_result` 도 동일 일관성 (`pending`/`running` → timeout 명시 ExternalAPIError). 회귀 9 신규 + 백엔드 1613 PASS. 커밋 `cb672cc` | 2026-05-21 17:40 운영 사고: (1) 사이클 23 P3-1 AI 자문 자동 적용 도입 이후 매일 20:00 `NameError: name 'KST' is not defined` 로 자동 적용 실패. (2) 외부 백테스트 서버 (`http://43.202.187.5:3846/mcp`) 가 작업 큐 대기 중 `pending` 반환 시 unknown 분기 → ExternalAPIError → `backtest_runs status=failed` 잘못 기록 (17:40 6 job 중 3건 영향). 매매 안전성은 무영향 (자문/백테스트 보조 경로) |
| 2026-05-21 | 사이클 34 + 35 — 조건검색 funnel DB 추적 UI + 세션별 구독 종목 expand UI (병렬 발주) | **사이클 34** (funnel DB): migration `030_strategy_funnel_snapshots.sql` (UUID PK + target_date/strategy_id/step_no/snapshot_at UNIQUE + survived_tickers/excluded_sample JSONB cap 200/20 + 인덱스 2). `src/db/strategy_funnel.py`: CRUD (`insert_snapshot`/`list_snapshots`/`list_recent_by_strategy`). `src/routes/strategy_funnel.py`: `GET /api/strategy-funnel` / `GET /api/strategy-funnel/recent` / `POST /api/strategy-funnel/snapshot` (수동 trigger, 현재 최종 단계 `step_no=99` 만 저장 — 자동 hook 은 후속 사이클). `frontend/src/pages/StrategyFunnel.tsx`: 전략 dropdown + date picker + 단계별 expand + 수동 trigger 버튼. **사이클 35** (세션 UI): `src/routes/realtime.py` `tickers_detail` 응답 추가 (ticker/ticker_name/stale/last_tick/retries/last_resub, cap 200 + stale 우선 정렬). `frontend/src/components/KisAccountPoolCard.tsx` 세션별 expand 토글 + 종목 테이블 (사이클 28 `[tick_coverage_session]` 데이터 실시간 노출). 회귀 18 신규 (5+13) + 백엔드 1604 PASS + 프론트 160 PASS + Vite 빌드 성공. 커밋 `42b8837` + migration 030 Supabase 적용. | 사용자 보고 5/21 15:26 funnel 카운터 결함 진단 후 단계별 추적 영구화 필요 + 사이클 28~29 stale 진단을 UI 에서도 보고 싶음. donchian H-4 5/22 검증을 funnel snapshot 데이터로 수행 가능 |
| 2026-05-21 | 사이클 33 — 3 전략 funnel 0 결함 (BFB `prdy_vol` 필드 + VCP fetch 100일 한도) 시정 | 사용자 보고 5/21 15:26: donchian 111→0 / BFB 30→0 / VCP 113→0 funnel. **Phase 1 자문** (team-leader): BFB H-2 + VCP H-3 강 의심 (P0), H-1 부분봉 정상, H-4 시장 자연 0 ~70%. **Phase 2 진단 (KIS MCP 정본 교차)**: `src/engine/strategies/bull_flag_breakout.py:362` — KIS `FHKST01010100` 응답에 `prdy_vol` 필드 없음 → `acml_vol` 으로 변경. `src/engine/strategies/vcp_breakout.py:131` — `fetch_daily_candles` 단일 호출 100일 한도 (`src/api/CLAUDE.md` 명시). VCP `fetch_days=285` (`ema_long=200 + base_max=75 + 10`) 요청 → KIS 100일만 반환 → trend filter 진입 *전* continue (113→0). **Phase 3 시정**: `fetch_days = min(285, 100)` + `effective_ema_long = min(ema_long, available_len - uptrend_days - 5)` 자동 축소 (최소 30 이상) + `_check_trend_filter(candles, effective_ema_long=)` 시그니처 확장. 회귀 4 신규 + BFB prepare mock 갱신 + 백엔드 1586 PASS. 커밋 `2748b33` | 사용자 1.17M 자본 매매 critical — BFB / VCP 매수 신호 0 = 매일 미매수 결함 누적 위험. VCP EMA 길이는 KIS 100일 한도 내에서 자동 조정 (미네르비니식 의도 일부 양보, 운영 1주 후 multi-call 분할 fetch 인프라 검토 권고). H-4 donchian 은 5/22 운영 결과 모니터링 |
| 2026-05-21 | 사이클 32 (R4) — stale universe 가드 + KIS `inquire-ccnl` 최근체결시각 기록 | **신규 모듈** `src/api/quotation.py`: `inquire_ccnl(ticker: str, market: str = "J") -> dict \| None` (KIS `FHKST01010100` 주식현재가 시세, output[0] + today_volume 합산, graceful 빈/오류/예외 → None, 6자리 ticker 사전 가드, `kis_get_quote` 경유). **화이트리스트** `src/api/base.py::_QUOTE_ALLOWED_PATHS` 에 `inquire-ccnl` 추가 (시세성·민감 식별자 없음). **신규 헬퍼** `src/engine/scheduler.py::_evaluate_universe_guard(candidate_tickers)`: 사전 가드 (보유/익일청산/이미 제외/stale≤5 → KIS 호출 skip) + `inquire_ccnl` + `today_volume < UNIVERSE_LOW_VOLUME_THRESHOLD(=10_000)` 비교 → `_universe_excluded_today.add()` + `kis_ws_pool.unsubscribe()` + `[universe_excluded]` INFO + system_logs 영구. 종목 간 50ms sleep. `_collect_breakout_tickers` 필터링 + `_scan_loop` 5분 주기 통합 + `_reset_daily_state` 동행 clear (영구 블랙리스트 금지). 회귀 21 신규 (S1~S8 + U1~U10) + 백엔드 1582 PASS. 커밋 `d7b5fb9` | 사이클 28~29 후 메인 편중 73% → 5% 해소됐으나 stale 종목 자체 분산만 됨. 거래량 빈약한 중소형주 20여 종목 (주성엔지니어링/BHI/디알젬/하나마이크론 등) 영구 stale 로 41 슬롯 점유 + R1 force_retry KIS Rate Limit 부담 누적. 사용자 발주 추가 조건: 제외 시점 KIS `FHKST01010100` 으로 당일 최근체결시각 로그 기록 |
| 2026-05-21 | 사이클 31 (R6) — `risk.py:152` 사전 가드 침묵 가시화 (`[risk_silent_skip]` 1회/페어/일 emit) | `src/engine/risk.py::RiskManager.__init__`: `_risk_silent_skip_logged_today: set[tuple[str, str]]` 신규 필드. `on_tick` 의 `current_price > state.total_investment` skip 직전에 `(ticker, strategy_id)` 페어 emit cap INFO 추가 — 같은 페어 일일 1회만, 매 틱 폭주 차단. `scheduler._reset_daily_state` 동행 clear + AttributeError 방어 가드. 회귀 8 신규 + 백엔드 1561 PASS. 커밋 `e660897` | 09:13 VB 미매수 사고 디버깅 곤란 1차 원인. 대시보드 "돌파" 보고 매수 안 되는 이유를 코드 추적 + DB 분석 + Explore 2개 병렬 호출까지 거쳐야 진단 가능했음. VB 비중 0.5 상향 후에도 삼성전기/현대모비스/SK하이닉스 등 고가 종목 매일 발생 가능성 |
| 2026-05-21 | 사이클 30 — `_sync_orders_to_db` 중복 판정 키 dedupe 결함 + `(ticker, order_no, trade_type)` UNIQUE 인덱스 + 5/20 042700 DB 정리 | `src/db/trade_history.py`: 신규 `get_today_buy_trades_for_sync(ticker=None) -> list[dict]` / `get_today_sell_trades_for_sync(ticker=None) -> list[dict]` — **dedupe 없음** + CANCELLED 제외 + optional ticker filter. 기존 `get_today_buy_trades()` / `get_today_sell_trades()` 는 ticker 별 dedupe (포지션 복구용) 그대로 — docstring 의도 명시. `src/engine/scheduler.py:1989-2010 _sync_orders_to_db`: 호출 교체 (`_for_sync` 변형 사용) + strategy 매핑은 별도 dedupe 함수 유지. **migration 029** `029_trade_history_dedupe_unique.sql` (부분 UNIQUE 인덱스 `WHERE order_no IS NOT NULL AND != ''`, NULL/빈 order_no 는 제외 — 수동 매매 호환). 회귀 12 신규 (042700 핑퐁 사고 재현 4 + `_for_sync` 8) + 백엔드 1553 PASS. **DB 정리 SQL 실행 완료**: 042700 15건 + 475430 1건 + 053260/058430/092220/396300 4건 (옛 보정 INSERT PENDING) DELETE → 중복 잔존 0건 확인 → migration 029 적용 → `recompute_daily_performance()` 호출. 커밋 `389493c` | 사용자 보고 5/20 042700 19건 중 15건 중복 + 매매손익 8건 잡힘 (실거래 4건/손익 2건). 원인: `_sync_orders_to_db` 가 ticker dedupe 함수를 중복 판정 키 소스로 사용 → 같은 ticker 의 다른 `order_no` 가 가려져 매 재기동마다 핑퐁 INSERT (7회 재기동 = 14건 누적). DB 안전망 부재 (`(ticker, order_no, trade_type)` UNIQUE 없음). 자금 안전 critical — DB 정리는 SELECT 검증 후 사용자 명시 옵션 2 (DELETE + UNIQUE migration) 승인 후 진행 |
| 2026-05-21 | 사이클 29-R3 — K stale watcher 우선순위 분리 (사이클 25-B 패턴 확장, 메인 편중 73% → 5% 분산) | `src/engine/scheduler.py::_check_and_resubscribe_stale`: `high_tickers` 헬퍼 (매 호출 재계산) — `registry.all()` positions 합집합 ∪ `_pending_next_day_clear` ticker set (try/except 안전 폴백 — 전체 LOW). R1 force_retry 분기 (line 2790) + 1~5회 강제 재등록 분기 (line 2827) 양쪽에 `sub_priority = "HIGH" if ticker in high_tickers else "LOW"` + `sub_bypass = is_high` 적용. 회귀 8 신규 + 74 stale 누적 + 백엔드 1541 PASS. 커밋 `eb8359c` | 사이클 28 실측 main=25/quote-1=3/quote-2=4/quote-3=2 편중률 73%. K stale watcher (120s 주기) 가 모든 stale 종목을 HIGH 메인 강제로 누적시킨 결함. 사이클 25-B 가 `_resubscribe_stale_priority` (5분 주기) 만 분리했고 더 빈번한 K stale watcher 는 분리 누락. R3 후 편중률 5% 확인 (main=2/보조 합 34) — 보유 종목 손절 평가 race 무영향 (HIGH 분류 그대로) |
| 2026-05-21 | 사이클 29-R2 — `_detect_silent_inactive_sessions` 정의 `fresh==0` → `fresh_ratio<0.2` (20%) 완화 | `src/engine/scheduler.py:107`: `SILENT_INACTIVE_FRESH_RATIO_THRESHOLD = 0.2` 신규 상수. `_detect_silent_inactive_sessions` 분기 (line 2553~2575): `fresh_ratio = fresh_count / subscribed_count` (ZeroDivision 안전 가드) + `silent_suspect = fresh_ratio < THRESHOLD and subscribed_count >= MIN_SUBSCRIBED(=5)`. 3중 가드 구조 보존 (비율 + sub 최소 + 5분 지속) + 시간당 2회 cap 그대로 (LMS 위험 차단). `fresh==0` 케이스는 0 < 0.2 자동 호환 — 사이클 24 회귀 가드 보존. 회귀 9 신규 + 8 기존 + 백엔드 1533 PASS. 커밋 `a112946` | 사이클 28 실측 메인 `fresh=2/25 (8%) stale=23` 인데 사이클 24 `fresh==0` 정의로 강제 reconnect 미발화. R2 후 5분 30초 내 자동 감지 → `_ws.close()` → 보유 005935 시세 회복 경로 확보. R1 종목별 force_retry 와 R2 세션 reconnect race 무해 (R2 reconnect 후 종목 신규 등록 → R1 카운터 자연 clear) |
| 2026-05-21 | 사이클 29 (R1) — 영구 stale 무한 skip → 시간 기반 강제 재시도 (5분 cooldown + 시간당 12회 cap) | `src/engine/scheduler.py:88`: `STALE_FORCE_RETRY_AFTER_SECS=300` / `STALE_FORCE_RETRY_HOURLY_CAP=12` 신규 상수. `__init__` (line 196): `_stale_force_retry_history: dict[str, list[datetime]]` 신규 (60분 슬라이딩 윈도우). `_check_and_resubscribe_stale` 의 `retry > MAX_STALE_RETRIES (=5)` 분기 (line 2700-2787): 무한 skip → 시간 기반 분기 — `last_resub_age >= 300s` 또는 부재 시 강제 재시도 + `_stale_retry_count[ticker] = 0` 리셋 + history 등록. 시간당 12회 초과 → `[stale_force_retry_cap]` WARNING skip. `_reset_daily_state` 동행 clear. `[stale_force_retry] ticker=... retries=... last_resub_age=...s — 강제 재시도 + 카운터 리셋` INFO. 회귀 12 신규 + 49 기존 stale + 백엔드 1524 PASS. 커밋 `6d1f171` | 사이클 28 실측 2026-05-21 13:13~13:29 메인 22개 종목이 r=6→9 카운터만 증가하고 강제 재등록 8분간 0건. 보유 종목 005935(삼성전자우) 손절 평가 지연 위험. KIS LMS/앱키 정지 위험은 종목 단위 5분 cooldown + 시간당 12회 cap 이중 가드로 차단. 사이클 17 보강 (KIS 공식 "기등록한 사항을 재등록하지 않도록") 정신은 종목 단위 cooldown 으로 준수 |
| 2026-05-21 | 사이클 28 — stale 구독 추적 세션×종목 상세 로그 강화 (분산 로직 점검 인프라) | `src/realtime/websocket_pool.py`: `get_subscriptions_by_session() -> dict[str, set[str]]` 헬퍼 신규 (역인덱싱, 영속 dict 미추가). `src/engine/scheduler.py`: `_stale_last_resubscribe_at: dict[str, datetime]` 신규 필드 + `_build_session_subscription_view` + `_emit_stale_session_detail` 헬퍼 + 강제 재등록 직후 갱신 + `_reset_daily_state` 동행 clear. `_check_and_resubscribe_stale` 마지막에 `[stale_watcher_detail] session=main sub=22/41 fresh=8 stale=14 ratio=0.64 stale=[(009150,r=3,@09:12:45), ...]` 신규 prefix. `_report_tick_coverage` 에 `[tick_coverage_session] main sub=22/41 fresh=8 stale=14 (0.64) \| quote-1 sub=8/41 fresh=6 stale=2 (0.25)` 신규 prefix. 기존 `[stale_watcher]` / `[tick_coverage]` / `[stale_priority_resubscribe]` 보존 (모니터링 호환). 종목 리스트 20 cap + `...+N` overflow. 회귀 18 신규 + 73 기존 stale = 91/91 + engine·realtime 광범위 910/910 PASS. 커밋 `4ca527e` | 2026-05-21 09:13 사용자 VB 미매수 진단 사이클에서 09:35 첫 tick_coverage stale=16/30 노출. 종목별 추적·세션 분포·재구독 시각 부재로 영구 stale 무한 skip 결함 가시화 곤란. 향후 분산 로직 점검 인프라 동시 확보 |
| 2026-05-21 | 사이클 27 — 하네스 확장: `domain-expert` (데이/스윙 트레이더 자문) + `refactor-expert` (주기적 리팩토링) 합류 + KIS MCP (`mcp__kis-code-assistant__*`) 접근 명시 + KIS Code Assistant MCP 공식 저장소 (`koreainvestment/open-trading-api`) 안내 통합 | **A. 신규 에이전트 2명**: `.claude/agents/domain-expert.md` (opus, 데이/스윙 컨설턴트 — 신규 전략·파라미터·시장 미시구조·보드 행태·시장 레짐·KIS 거부 해석 자문, `_workspace/domain_consult/{topic}.md` 산출) + `.claude/agents/refactor-expert.md` (opus, 주기적 코드 품질 검토 — 중복/명명/모듈 비대화/dead code/아키텍처 드리프트, LOW/MEDIUM/HIGH 카드 단위 + 회귀 가드 + HIGH 카드는 domain-expert 행위 영향 평가 동반). **B. 신규 스킬 3개**: `.claude/skills/domain-consult/skill.md` (자문 요청 형식 + 자문 메모 구조 + 빈출 토픽 빠른 참조) + `.claude/skills/refactor-review/skill.md` (5 계명 + 검토 흐름 + 카드 구조 + 빈출 카드 유형) + `.claude/skills/kis-mcp-query/skill.md` (KIS 공식 저장소 출처 + stdio 설치 가이드 + 도구 일람 9개 + 공식 프롬프트 `kis_easy_code`/`kis_detailed_code` + 활용 패턴 4종). **C. 기존 에이전트 3개 갱신**: `backend-dev.md` / `tdd-engineer.md` / `tester.md` 에 KIS MCP 활용 절 추가 (`mcp__kis-code-assistant__search_domestic_stock_api` 1차 도구). **D. 오케스트레이터 갱신**: `auto-trading-orchestrator/skill.md` description 7 에이전트 반영 + 에이전트 구성 표 7명 + Phase 2.5 도메인 자문 트리거 6 조건 + **Phase 4.5 주기적 리팩토링 검토** 신규 (사이클 5회 누적 또는 명시 요청) + 데이터 흐름 다이어그램 + 에러 핸들링 4 항목 추가 + 후속 키워드 확장. **E. CLAUDE.md 하네스 섹션**: 라우팅에 `domain-consult` / `refactor-review` / `kis-mcp-query` 추가 + 모델 라우팅 표 (opus: team-leader, domain-expert, tdd-engineer, tester, refactor-expert / sonnet: backend-dev, frontend-dev) + 변경 이력 테이블 2 행. **F. README.md sync**: 에이전트 4명 → 7명 / 스킬 4개 → 9개 + 신규 항목 표 흡수. 사이클 27 의 매매 코드 본체 변경 0 — 하네스/문서만 | 사용자 요구 (2026-05-21): "현재 프로젝트 에이전트 중 도메인 전문가(데이/스윙 트레이더)가 없어. 리팩토링 전문가도 없고. 현재 구조에서 개발범위에 따라 도메인전문가의 조언을 얻도록 하고, 주기적으로 리팩토링 전문가에게 검토를 받도록 하자. 그리고 백엔드 개발자와 테스트 엔지니어, 리팩토링 전문가는 KIS MCP에 접근해서 질의/응답을 받을 수 있도록 하자." + 후속 "kis-mcp-query 는 KIS 공식 저장소 (`koreainvestment/open-trading-api`) 경로 안내 통합". 자율 결정: (a) team-leader 와 domain-expert 의 역할 분리 — team-leader = 정의/감독/분배 / domain-expert = 깊이있는 자문 (3 안 정렬 + 반례 + 트레이더 어법) / (b) refactor 5 계명 — 테스트 없는 리팩토링 금지 / CLAUDE.md 절대 깨지 말 것 우선 / 3회 반복 = 추상화 후보 / 주석 정책 보존 / 카드 단위 분할 / (c) KIS MCP 공식 저장소 stdio 방식 설치 — 개발 머신 간단, 운영 EC2 Docker+HTTP 권장 / (d) 공식 프롬프트 결과 즉시 채택 금지 — `kis_request()` / `settings.get_tr_id()` / `ApiResponse` 래퍼 컨벤션 변환 후 TDD Red 회귀 가드 통과 후에만 채택 / (e) 매매 코드 침범 0 — 하네스/문서만 변경, 사이클 17~26 보존 |
| 2026-05-20 | 사이클 26 — 매매 정책 KRX ONLY + 시세 채널 시간대별 전환 + 종목 단위 원자 전환 (TDD, 5 파일 변경 + 기존 8 파일 테스트 갱신 + 신규 회귀 가드 32 케이스, 매매 코드 본체 변경 0) | **A. 전략 보드** `src/engine/strategies/volatility_breakout.py` + `long_tail_volatility.py`: `DEFAULT_TRADABLE_BOARDS = ("pre_nxt","main")` → `("main",)` KRX ONLY. `DEFAULT_PARAMS["tradable_boards"]` 자동 동기. **B. 보드 스케줄** `src/engine/session.py`: `_BOARD_SCHEDULE` 5 보드 → 3 보드 (PRE/MAIN/POST). KRX_OPEN (08:30~09:00) 구간 제거 — PRE_NXT 단독 통합. KRX_AFTER (15:30~18:00) 구간 제거. MAIN 09:00~15:39:59 (갭 마진 10분 포함). POST_NXT 15:40~20:00. `_DEFAULT_TRADABLE_BOARDS` VB/LTV fallback MAIN 단독으로 동기. **C. 시세 채널** `src/engine/scanner.py`: `TICK_TR_ID` 하위 호환 보존 + `TICK_TR_ID_KRX="H0STCNT0"` + `TICK_TR_ID_NXT="H0NXCNT0"` 신규. `get_active_tick_tr_ids(now_t=None) -> set[str]` 함수 신규 — 6 구간 시간대별 분기 (PRE_NXT/{H0NXCNT0} / KRX사전마진/{H0NXCNT0,H0STCNT0} / MAIN/{H0STCNT0} / 종가흡수마진/{H0STCNT0} / NXT사전마진/{H0STCNT0,H0NXCNT0} / POST_NXT/{H0NXCNT0}). **D. 시간 상수** `src/engine/scheduler.py`: `TIME_KRX_MAIN_OPEN_PRESUBSCRIBE=time(8,59,10)` + `TIME_POST_NXT_OPEN_PRESUBSCRIBE=time(15,39,10)` + `TIME_POST_NXT_OPEN=time(15,40)` 신규. `ATOMIC_TRANSITION_*` 3 상수 신규. **E. 원자 전환** `src/engine/scheduler.py::TradingScheduler._atomic_board_transition(ticker, stale_tr_id, new_tr_id, ack_timeout_secs=2.0)` 신규 — (1) unsubscribe (2) _subscriptions_acked ACK polling (timeout 2s → WARNING [atomic_transition_ack_timeout] 강행) (3) subscribe(HIGH/LOW auto) (4) 50ms sleep. `_board_transition_loop(stale, new, tickers, priority_groups)` 신규 — HIGH(positions/ndc) 우선, 전체 순회, [board_transition_complete] INFO. **회귀 가드 신규 32**: `tests/unit/engine/strategies/test_volatility_breakout_krx_only.py` 3 / `test_long_tail_volatility_krx_only.py` 3 / `tests/unit/engine/session/test_board_schedule_cycle26.py` 6 / `tests/unit/engine/scheduler/test_tick_tr_id_by_time.py` 6 / `test_board_transition_presubscribe.py` 4 / `test_atomic_board_transition.py` 6 / `test_confirm_open_prices_main_only.py` 2 / `test_post_nxt_open_time.py` 2. **기존 테스트 갱신 8**: `test_session.py` KRX_OPEN/KRX_AFTER 보드 제거 반영 (3 케이스) / `test_vb_force_clear_at_15_20.py` pre_nxt 제거 반영 (1) / `test_volatility_breakout.py` tradable_boards/prev_price 반영 (2) / `test_volatility_breakout_active_boards.py` (1) / `test_long_tail_volatility_active_boards.py` (1) / `test_volatility_breakout_board_stop_loss.py` PRE_NXT 손절 board=None fallback 반영 (1) / `tests/integration/test_vb_board_stop_loss_fallback.py` 동일 반영 (1). 백엔드 1462→**1494 passed** (+32, 회귀 0). `src/engine/strategies/CLAUDE.md` + `src/engine/CLAUDE.md` + `src/realtime/CLAUDE.md` sync | SK하이닉스(000660) NXT 애프터 시가 결함 (2026-05-20 15:30~15:36: `stck_oprc` REST 폴백 NXT 시가 미반영) + VB/LTV PRE_NXT 매수 OVERNIGHT 위험 + 메인 세션 부담 과중(사이클 24/25-B 부분 해소). 사이클 26 결정: (1) VB/LTV KRX ONLY — 신규매수 MAIN 단독. PRE_NXT 갭상승 위험 제거 + SK하이닉스 결함 자연 해소. (2) 보드 스케줄 3 보드 단순화. (3) 시세 채널 PRE/POST=H0NXCNT0 MAIN=H0STCNT0 명확 분리. (4) 종목 단위 원자 전환 — unsub→ACK→sub 순차, 50ms sleep, HIGH 우선. 자금 안전 절대 원칙 보존 — 매매 코드 본체(risk.on_tick/order_engine) 변경 0, VB 15:20 `_force_clear_main_only` 보존, 사이클 17~25 보존 |
| 2026-05-20 | 사이클 25-B — VB/LTV 후보 stale 재구독 LOW 강등 (TDD, 1 파일 변경 + 회귀 신규 1 파일 + 기존 1 파일 갱신, 매매 코드 침범 0) | **`src/engine/scheduler.py`**: `_resubscribe_stale_priority()` 에 positions/next_day_clear 판별 로직 추가. `high_tickers` = `registry.all()` positions 합집합 ∪ `_pending_next_day_clear` ticker set. `ticker in high_tickers` → HIGH+bypass_limit=True (기존 보존). 그 외 후보 → LOW+bypass_limit=False (25-B 신규). 이중 안전망: 사이클 24 자동 reconnect + 후보 LOW 강등으로 메인 부담 원천 차단. **회귀 가드 (신규 6)**: `tests/unit/engine/test_breakout_candidate_low_priority.py` (후보 stale→LOW / positions stale→HIGH / ndc stale→HIGH / subscribe_filtered_stocks breakout→LOW 기존 / subscribe_filtered_stocks positions→HIGH 기존 / 혼합 분리). 기존 `test_scan_loop_stale_priority.py` 갱신 (Case A: "모든 stale→HIGH" → 25-B "positions/ndc 없는 후보→LOW" 재정의). 백엔드 1456→**1462 passed** (+6, 회귀 0). 영향 인덱스 재생성 (84 모듈 195 테스트). `src/engine/CLAUDE.md` + `src/realtime/CLAUDE.md` + `src/engine/strategies/CLAUDE.md` sync | 2026-05-20 14:58 운영 결함: 메인 세션 sub=11 fresh=0 stale=11 silent inactive + VB/LTV 후보 8종목(036930/005935/439960/010170/253840/142280/066430/011000) 메인 집중. 근본 원인: `_resubscribe_stale_priority` 가 모든 stale 에 HIGH+bypass_limit=True 재등록 → 후보 stale 되면 메인 승격 → 메인 과부하 → silent inactive. `subscribe_filtered_stocks(priority_groups=...)` 의 breakout 그룹은 이미 LOW 이지만, stale 회복 경로에서 HIGH로 덮어써지는 결함. 매매 코드 침범 0, push 사용자 명시 승인 후 |
| 2026-05-20 | 사이클 24 — 메인 세션 silent inactive 자동 감지 + 강제 reconnect (TDD, 1 파일 변경 + 회귀 신규 1 파일, 매매 코드 침범 0) | **A. `src/engine/scheduler.py`**: 신규 상수 `SILENT_INACTIVE_MIN_SUBSCRIBED=5` / `SILENT_INACTIVE_PERSIST_SECS=300.0` / `SILENT_INACTIVE_RECOVERY_CAP_PER_HOUR=2` / `SILENT_INACTIVE_RECOVERY_WINDOW_SECS=3600.0`. 신규 인스턴스 변수 `_silent_inactive_first_seen: dict[str, datetime]` / `_silent_inactive_recovery_count: dict[str, list[float]]`. 신규 헬퍼 `_detect_silent_inactive_sessions() -> list[str]` (3중 가드: fresh=0 + sub>=5 + 5분 지속). 신규 헬퍼 `_force_reconnect_session(label)` (메인: `kis_ws._ws.close()` / 보조: `kis_ws_pool._quotes[idx]._ws.close()` + 시간당 2회 cap + 영구 로그). `_stale_watcher_loop` 에 silent inactive 감지 + force reconnect 단계 추가. `_reset_daily_state()` 에 `_silent_inactive_first_seen.clear()` + `_silent_inactive_recovery_count.clear()` 추가. 모듈 레벨 `from src.realtime.websocket_pool import kis_ws_pool` 추가 (패치 대응). **회귀 가드 (신규 8)**: `test_session_silent_inactive_recovery.py` (5분 미달/도달/fresh 부분/sub<5/메인 reconnect/보조 reconnect/시간당 cap/cap 만료). 백엔드 1448→**1456 passed** (+8, 회귀 0). 영향 인덱스 재생성 (84 모듈 194 테스트). `src/engine/CLAUDE.md` + `src/realtime/CLAUDE.md` sync | 2026-05-20 14:58 운영 결함: 메인 세션 sub=11 ack=11 fresh=0 stale=11 (4분 안정 후도 회복 안 됨). 8 종목 (036930/005935/439960/010170/253840/142280/066430/011000) 영구 stale. 컨테이너 재기동으로 회복 안 됨 — KIS 측 메인 appkey 세션 silent inactive 지속. K stale watcher (사이클 17 보강) 의 종목별 재등록은 세션 자체 결함이라 무효. 자율 결정: (a) 동일 `_stale_watcher_loop` 통합 (별도 task 신설 금지) / (b) 3중 가드 — fresh=0 + sub>=5 (위양성 차단) + 5분 지속 (단발 끊김 즉시 close 차단) / (c) 시간당 2회 cap — KIS LMS + 앱정보 이용중지 위험 사전 차단 / (d) `_ws.close()` 만 호출 — connect() 재연결 루프 + `_restore_subscriptions_after_reconnect` 자연 발화 / (e) `_reset_daily_state()` 통합. 자금 안전 절대 원칙 보존 — 매매 코드 침범 0. push 보류 — 사용자 명시 승인 후 KRX 메인 시간 외 권장 |
| 2026-05-20 | 사이클 23 — 3 전략 (BFB/VCP/donchian) 파라미터/UI 최적화 + AI 자문 자동 적용 (TDD, 9 영역 묶음, 매매 코드 신호 본체 변경 0) | **P1-1** `recommendation_engine.py` PARAM_RANGES 9 키 (VCP 4 + P2 신규 5) + INT_PARAMS 3 키. **P1-2** `bull_flag_breakout.py::_empty_scan_stats` `min_trade_amount_failed` 키 + `_scan_universe` 거래대금 미달 카운터. **P1-3** `vcp_breakout.py::_empty_scan_stats` `mcap_pass` 키 + `_scan_universe` 시총 통과 카운터. **P2-1** `bull_flag_breakout.py` `breakout_retention_minutes=3` + `_breakout_first_seen` dict + `check_buy_signal` 첫 돌파→대기→충족→BUY 분기 + `_reset_daily_state()` override. **P2-2** `donchian_swing.py` `breakout_fail_n_days=5` + `_breakout_high` dict + `check_exit_signal` 시간 기반 청산 분기 (기존 ATR/하드 보존). **P2-3** `donchian_swing.py` `max_breakout_extension_pct=3.0` + `check_buy_signal` 과열 추격 가드. **P2-4** `donchian_swing.py` `box_contraction_period=10` + `max_box_volatility_pct=5.0` + `prepare()` 박스 수축 필터 + `box_contraction_pass` scan_stats 키. **P3-1+2** `recommendation_engine.py::auto_apply_recommendations(target_date)` 신규 (감액만 + 50% cap + `_CONSERVATIVE_KEYS` 보수적 파라미터 자동 적용 + 4종 영구 로그 + `status='applied_auto'`). `scheduler.py` TIME_RECOMMENDATION 직후 호출. `parameter_recommendations.py::list_pending_by_date` 신규. `parameter_recommendations.py::update_recommendation_status` `applied_auto` 허용. **P3-3** `system_config.py` `get/set_auto_apply_enabled()` 기본 False + `system_integrations.py` `GET/PUT /api/integrations/auto-apply` + `models/system_integrations.py` `AutoApplyRequest/AutoApplyStatus` + `frontend` `IntegrationToggleCard.tsx` 4번째 토글 `data-testid="toggle-auto-apply"` + `api/integrations.ts::getAutoApply/setAutoApply` + `types/integrations.ts::IntegrationKey` 'auto-apply' 추가. **마이그** `supabase/migrations/028_auto_apply_status.sql` `applied_auto` CHECK 제약 추가. **회귀 가드 36 케이스** (신규): `test_param_ranges_vcp.py` 12 / `test_bull_flag_breakout_min_trade_failed.py` 3 / `test_vcp_breakout_mcap_pass.py` 2 / `test_bull_flag_breakout_retention.py` 4 / `test_donchian_swing_fail_n_days.py` 3 / `test_donchian_swing_extension_cap.py` 2 / `test_donchian_swing_box_contraction.py` 3 / `test_auto_apply_recommendations.py` 5 / `test_system_config_auto_apply.py` 2 + 프론트 `IntegrationToggleCard.auto_apply.test.tsx` 2. **기존 회귀 수정**: `test_bull_flag_breakout.py` fixture `retention_minutes=0` 추가 (retention=3 기본값 호환). 백엔드 1412→**1448 passed** (+36, 회귀 0) / 프론트 158→**160 passed** (+2, 회귀 0). 영향 인덱스 재생성. `src/engine/strategies/CLAUDE.md` + `src/engine/CLAUDE.md` + `src/db/CLAUDE.md` + `frontend/CLAUDE.md` sync | 운영자 보고: BFB/VCP/donchian 거래 0건 또는 신호 부족. P1+P2: 가짜 돌파 차단(retention) + 과열 추격 금지(extension_cap) + 박스 수축 확인(box_contraction) + 시간 기반 청산(fail_n_days) + 운영 가시성(scan_stats). P3: AI 자문 weight 감액 자동 적용 (50% cap + 보수적 파라미터) — 거래 부진 전략 자동 보호. 자동 적용 기본 false, 운영자 명시 토글 ON 후 작동. push 보류 — 사용자 명시 승인 후 KRX 15:30+ 또는 익일 07:50 전 권장 |
| 2026-05-20 | 사이클 22 — Dockerfile `.token_cache` 디렉토리 권한 영구 보강 (TDD, 1 파일 변경, 매매 코드 침범 0) | `Dockerfile` prod 스테이지 `RUN adduser ... && chown -R appuser:appuser /app` 1줄에 `mkdir -p /app/.token_cache` (chown *전*) + `chmod 755 /app/.token_cache` (chown *후*) 추가. 순서: mkdir → chown → chmod → USER appuser 불변. **회귀 가드 (신규 3)**: `tests/integration/test_dockerfile_token_cache_perms.py` 3 케이스 (A: `mkdir -p /app/.token_cache` 라인 존재 정규식 매칭 / B: `mkdir` 가 `chown -R appuser:appuser /app` *앞* 순서 / C: `USER appuser` 가 `mkdir`/`chown` *뒤* 순서). 정적 텍스트 파싱 (pathlib.Path.read_text + re), docker build 미실행. 백엔드 1409→**1412 passed**(+3, 회귀 0). 영향 인덱스: 백엔드 84 모듈 184 테스트. `CLAUDE.md`(루트) Docker 절 사이클 22 1행 + `src/auth/CLAUDE.md` 사이클 22 절 4행 | 2026-05-20 13:20:46 사이클 20 (35ebe3a) push 후 운영 결함: 컨테이너 `appuser` 가 `.token_cache/quote_sub.json` / `.token_cache/quote_gold.json` 쓰기 거부 (`PermissionError [Errno 13]`). 근본 원인: `docker-compose.prod.yml` 의 `./.token_cache:/app/.token_cache` bind mount 가 호스트에서 디렉토리 신규 생성 시 root:root 소유 → `Dockerfile:17-19` 의 `USER appuser` 가 쓰기 거부. 사이클 20 핫픽스 A 로 `docker exec -u root chown` 임시 해결했으나, 재배포 / 신규 EC2 호스트마다 재발 위험. 자율 결정: (a) Dockerfile mkdir → chown → USER 순서 영구화 — Docker bind mount 가 컨테이너 측 권한 우선 매칭하므로 빌드 시점 디렉토리 보장이면 호스트 권한 자동 매칭 / (b) `chmod 755` 명시 — chown 이후 권한 확정 / (c) docker build 자체는 회귀 미실행 (CI 부담) — 정적 텍스트 파싱 3 케이스로 영구 가드 / (d) tests/integration 분류 — DB 미접속 + Docker 미실행 단순 파싱이라 integration 의미는 약하나 운영 환경 가드라 분류 보존. **매매 코드 변경 0** — token.py / scheduler.py / risk.on_tick / order_engine 무관. 사이클 17/18/6/19/20/21 보존. push 권장: 핫픽스 영구화라 컨테이너 재기동 위험 직접 차단 — 본 커밋은 immediate push 권장 |
| 2026-05-20 | 사이클 21 — UI 정리: 구독현황 KisAccountPoolCard 통합 + 전략별 필터링 단계 강화 (TDD, 4 영역 묶음, 매매 코드 변경 0) | **A. 백엔드 scan_stats**: `src/engine/strategies/volatility_breakout.py::_empty_scan_stats()` 9 키 + `prepare()` 단계별 카운트 + `get_scan_stats()` 메서드. `src/engine/strategies/long_tail_volatility.py` 10 키 (`consecutive_limit_pass` 포함). `src/engine/scanner.py::scan_filter_stats` 모듈 전역 dict 7 키 + `scan_stocks()` 단계별 누적 (상한가 30%+ 분기 추가 — `check_buy_signal` 30% 가드와 이중 안전망). `src/engine/strategies/momentum.py::MomentumStrategy.get_scan_stats()` 가 모듈 dict 사본 반환. **B. 프론트 ScanMonitor 인프라 제거**: `tick-coverage-badge`/`tick-coverage-progress`/`stale-context-label`/`stale-list-toggle`/수동 재구독 버튼/끊김 종목 펼치기 모두 제거. **C. 프론트 KisAccountPoolCard 통합**: `frontend/src/utils/stale-context.ts` 신규 공용 헬퍼 (`getKstMinutes`/`getStaleContextByKstMinutes`/`STALE_CONTEXT_META`/`formatLastTickKst`). `KisAccountPoolCard.tsx` 에 `stale-context-label`/`pool-resubscribe-button`/`pool-stale-list-toggle`/`pool-stale-row-{ticker}` 추가. **D. 프론트 5 전략 깔때기**: `ScanFunnelBars` 컴포넌트 추출 + VB(8)/LTV(9)/MOMENTUM(6)/BFB(8)/VCP(8) STAGES 정의. donchian SWING_STAGES 패턴 동일. `scan_stats` 미반영 시 fallback "아직 스캔 전". **회귀 가드 (신규 21)**: `tests/unit/engine/strategies/test_volatility_breakout_scan_stats.py` 3 / `test_long_tail_volatility_scan_stats.py` 3 / `test_momentum_scan_stats.py` 3 / `frontend/src/components/__tests__/KisAccountPoolCard.stale_integration.test.tsx` 6 / `ScanMonitor.funnel.test.tsx` 6. **제거 (14)**: `ScanMonitor.test.tsx` (tick_coverage / J2 stale 9 케이스 — KisAccountPoolCard 로 이전) + `ScanMonitor.stale_context.test.tsx` 5 케이스. **빌더 보강**: `tools/test_impact/build_index_frontend.mjs::parseImports` 에 `existsSync` 안전망 — 삭제 파일 git ls-files 호환. 백엔드 1400→**1409 passed** (+9, 회귀 0) / 프론트 ~160→**158 passed** (+12 신규, -14 제거, 회귀 0). 영향 인덱스: 백엔드 84 모듈 183 테스트 / 프론트 58 모듈 35 테스트. `src/engine/strategies/CLAUDE.md` 공통 패턴 절 사이클 21 5행 + `frontend/CLAUDE.md` ScanMonitor 사이클 21 깔때기 6행 + KisAccountPoolCard 사이클 21 끊김 통합 5행 | 사용자 요구 (2026-05-20): "조건검색현황 하단의 구독현황과 끊김 종목은 전략별로 흩어질 필요 없음. 조건검색에는 필터링 부분만 강조. 어떤 조건을 통해 필터링 되었는지 (모범사례: donchian-swing). 전략별 필터링 데이터를 자세하게 정리해서 반영하고 구독현황은 상단의 KIS 시세풀에 겹치는 부분을 제외하고 반영." 트레이더 관점 진단: ScanMonitor 가 (1) 전략별 필터링 깔때기 + (2) WS 구독 인프라 두 책임을 동시에 가져 가시성이 흐림. KisAccountPoolCard 는 세션별 fresh/stale 카운트만 노출 (끊김 종목 미노출). donchian_swing 만 8단계 깔때기 노출, VB/LTV/momentum 은 로그만. 자율 결정: (a) ScanFunnelBars 단일 컴포넌트로 5 전략 재사용 — donchian SWING_STAGES 패턴 동형 / (b) momentum 은 prepare 없어 모듈 전역 dict (scanner.scan_filter_stats) — 강결합 회피 / (c) scan_stocks 상한가 30%+ 분기 추가 — `check_buy_signal` 30% 가드와 이중 안전망 (매매 안전성 강화, 매수 신호 변경 0) / (d) utils/stale-context.ts 공용 헬퍼 — ScanMonitor 와 KisAccountPoolCard 양쪽 재사용 (DRY) / (e) ScanMonitor.test.tsx 인프라 케이스 14개 삭제 — 동일 케이스가 KisAccountPoolCard.stale_integration 로 이전됨 (회귀 가드 보존) / (f) 옵셔널 타입 가드 — 백엔드 미반영 시점에도 fallback "아직 스캔 전" 안전 노출. 매매 코드 변경 0 — risk.on_tick / order_engine / Position 무관, prepare() 카운터 추가만. 사이클 17/18/6/19/20 보존. 프론트 옵셔널 호환. push 보류 — 사이클 20 (35ebe3a) 와 함께 익일 07:50 _boot 전 권장 |
| 2026-05-20 | 사이클 20 — 토큰 발급 분당 1개 한도 위반 차단 (TDD, 3 영역 묶음, 자금 안전 절대 원칙 보존) | **A. 캐시 영속화**: `docker-compose.yml`/`docker-compose.prod.yml` 양쪽 `./.token_cache:/app/.token_cache` 디렉토리 볼륨 마운트. `.gitignore` `.token_cache/` + 구 `.token_cache_quote_*.json` 호환. **B. 모듈 전역 직렬화**: `src/auth/token.py` `_GLOBAL_ISSUE_LOCK`(asyncio.Lock lazy create) + `_LAST_ISSUE_AT`(time.monotonic) + `_ISSUE_GAP_SECS=61.0` + `_get_global_issue_lock()`/`reset_global_issue_state()`. `issue()` 가 lock 안에서 `_LAST_ISSUE_AT` gap 체크 → 미달 시 `asyncio.sleep(wait_secs)` → KIS POST → 성공 후 `_LAST_ISSUE_AT=time.monotonic()` 갱신 (실패 시 미갱신 → 재시도 가능). `_TOKEN_CACHE_DIR=.token_cache/` 디렉토리 단위 (`main.json`/`quote_<safe-label>.json`) + 구 경로 `_LEGACY_MAIN_CACHE_PATH`/`_LEGACY_QUOTE_CACHE_PREFIX` 자동 마이그레이션 + 구 경로 unlink. **C. boot 사전 순차 발급**: `src/engine/scheduler.py::TradingScheduler._preissue_all_tokens()` 신규 — 메인 `get_token()` → `kqa.list_accounts(active_only=True)` 순회 → 보조 N 매니저 순차 `get_token()`. 보조 발급 실패 try/except 흡수 + 메인 흐름 영향 0. `_boot()` 진입 초입에 `await self._preissue_all_tokens()` 호출. **회귀 가드 (신규 7)**: `tests/unit/auth/test_token_global_serialization.py` 5 케이스 (A Lock 직렬화 / B gap 강제 / C gap 충분 → sleep 0 / D 캐시 hit → issue() skip / E gather race 직렬화). `tests/unit/engine/test_boot_preissue_tokens.py` 2 케이스 (F 메인+보조 N 순차 / G 보조 실패 graceful). `src/auth/CLAUDE.md` 사이클 20 절 + Multi-Account boot 사전 순차 발급 절. `src/engine/CLAUDE.md` `TIME_BOOT` 표에 `_preissue_all_tokens()` 1행. `CLAUDE.md`(루트) Docker 절 토큰 캐시 영속화 1 문단. 백엔드 1393→**1400 passed**(+7, 회귀 0). 영향 인덱스: 백엔드 84 모듈 180 테스트 | 2026-05-20 10:40:31 boot 직후 메인 + 보조 sub/gold/ISA 4 매니저 거의 동시 `/oauth2/tokenP` 발급 시도 → KIS 분당 1개 한도 위반 → 일부 매니저 403 Forbidden → VB 일봉 fetch 8+ 종목 영향 + 매수 후보 prepare 불완전. 결함 3 계층: (1) `./logs:/app/logs` 만 마운트 → `.token_cache*.json` 미영속화 → 컨테이너 재기동마다 4 매니저 모두 신규 발급 / (2) `issue()` 에 모듈 전역 lock 부재 + rate-limit gap 0 / (3) boot 시점 사전 순차 발급 부재 (lifespan + scheduler._boot lazy 호출 비동기 산개). 자율 결정: (a) 디렉토리 단위 마운트 — 개별 파일 마운트는 race + chmod 이슈 / (b) 호환 fallback — 구 `.token_cache.json` 자동 마이그레이션 → 운영 중단 0 / (c) `_ISSUE_GAP_SECS=61.0` — KIS 60s 한도 + 1s 마진 / (d) `_LAST_ISSUE_AT` HTTP 성공 *후* 갱신 — 실패 시 재발급 시도 가능 / (e) lock 안 sleep — 다음 매니저는 자연 대기, KIS 부담 0 / (f) `_preissue_all_tokens()` 진입 초입 호출 — 캐시 hit 시 0초, miss 시 보조 3개 × 60s = 3분 boot 지연. 07:50 _boot 라 KRX 영향 0 / (g) 보조 실패 try/except — 매니저 1개 KIS 403 시 나머지 진행. 자금 안전 절대 원칙 보존 — 메인 매매 흐름 영향 0 (캐시 hit 시 lock 거치지 않음). 사이클 17/18/6/19 보존. push 보류 — boot 변경이 큰 작업이라 익일 07:50 boot 전 권장 |
| 2026-05-20 | 사이클 19 핫픽스 — `risk.on_tick` 보유 분기 `_selling` 가드 1줄 추가 (TDD, 매매 코드 1 파일 1 분기) | `src/engine/risk.py` 보유 분기 `check_exit_signal` 호출 *전* `if ticker in self.order_engine._selling: continue` 추가, `tests/unit/engine/test_risk_on_tick_selling_skip.py` 신규 3 케이스(A: `_selling` 비어있음 → exit 정상 평가 + STOP_LOSS 시 execute_sell / B: `_selling` 에 ticker → exit 호출 0 + execute_sell 0 / C: 매도 진행 종목 + 다른 보유 종목 ticker 단위 격리), `src/engine/CLAUDE.md` risk.py 절 사이클 19 1행 추가 | 2026-05-20 08:41:33~08:41:40 042700 한미반도체 VB 손절 매도 1주 시장가 발사 1회 정상, 그러나 7초 18+ 행 `[volatility_breakout] 변동성돌파 손절` INFO 로그 폭주. 근본 원인: `risk.on_tick:84-88` 보유 분기에 `_selling` 검사 부재 → 매도 발사 후 체결통보 도착 전까지 매 tick `check_exit_signal` 호출 → strategy 내부 `logger.info` 폭주. `order_engine.execute_sell:442-445` 의 `_selling` 가드는 정상 동작(중복 매도 차단됨), 본 핫픽스는 *신호 평가 자체 skip* 으로 로그/이벤트 루프 부담 동시 해소. 6 전략 공통 적용 — `volatility_breakout`/`momentum`/`long_tail_volatility`/`donchian_swing`/`bull_flag_breakout`/`vcp_breakout` 의 `check_exit_signal` 분기 호출 0회. 사이클 17/18/6/빌드핫픽스 보존, VB POST_NXT 변경 0, NXT/매수 흐름 영향 0. 백엔드 1390→**1393 passed**(+3, 회귀 0). 영향 인덱스: 백엔드 84 모듈 178 테스트. 운영 효과: 분당 ~600+ 손절 신호 평가/로그 → 매도 발사 후 0 |
| 2026-05-20 | 사이클 6 통합 마감 — 로그 메뉴 검색 박스 + 등급별 retention 정책 (TDD, 옵션 B 통합 1 사이클, 매매 코드 침범 0) | **백엔드 (A)**: `src/db/system_logs.py::purge_old_logs()` 신규 — INFO 2일 / WARNING+ 30일 cutoff 등급별 분리 DELETE + 1회 cap 100,000 + WHERE 누락 RuntimeError 방어 + `[log_retention] info_deleted=N high_deleted=M elapsed_ms=K` INFO 1행. `_purge_by_cutoff(cutoff_iso, level_filter)` 내부 헬퍼 (단일/다중 등급 분기 — `eq` vs `in_`). 상수 `INFO_RETENTION_DAYS=2`/`HIGH_RETENTION_DAYS=30`/`HIGH_LEVELS=("WARNING","ERROR","CRITICAL")`/`MAX_PURGE_BATCH=100_000`/`SEARCH_DEFAULT_LIMIT=200`/`SEARCH_MAX_LIMIT=1000`. **백엔드 (B)**: `src/db/system_logs.py::search_logs(q, *, level=None, start=None, end=None, limit=200)` 신규 — `ilike("message", "%q%")` substring 매칭 + level/start/end 동시 조건 + limit clamp 1~1000 + 빈 q ValueError + 반환 `{logs, total, has_more}`. `src/routes/logs.py::GET /api/logs/search` 신규 (q `min_length=1` 422 / limit 1~1000 422). `src/engine/scheduler.py::_run` settlement 흐름에 `purge_old_logs()` 1회 호출 추가 — `_log_analysis_engine` *후* + `_reset_daily_state()` *전*. 예외 graceful (`[log_retention_skip]` INFO + 다음 사이클 재시도). **프론트 (C)**: `frontend/src/api/logs.ts::searchLogs(p)` 신규 + `SearchPayload/SearchParams` 타입. `frontend/src/components/SystemLogsTab.tsx` 검색 박스 추가 (`system-logs-search-input` / `system-logs-search-button` / `system-logs-search-clear`) + 검색 모드 토글 (페이징/자동 새로고침 비활성, 200건 초과 시 `system-logs-search-has-more` 안내 amber 배너). 검색 결과 0건 → "검색 결과가 없습니다." 분리 메시지. **회귀 가드 (신규 30)**: `tests/unit/db/test_system_logs_retention.py` 7 케이스 (A 반환 dict / B INFO 2일 cutoff / C HIGH 30일 + level 필터 / D INFO eq / E cap / F cutoff None RuntimeError / G [log_retention] 로그). `tests/unit/db/test_system_logs_search.py` 11 케이스 (A ilike / B level+q / C ALL skip / D start/end / E limit clamp / F 반환 dict has_more / G 빈 q ValueError). `tests/contract/test_routes_logs_search.py` 7 케이스 (A 200 정상 / B q 누락 / C 빈 q / D limit>1000 / E limit=0 / F ApiResponse / G level/start/end 전달). `frontend/src/components/__tests__/SystemLogsTab.test.tsx` +5 케이스 (검색 호출 + q 인자 / 0건 메시지 / 결과 렌더 + 초기화 노출 / 초기화 → 페이징 모드 복귀 / has_more 안내). 백엔드 1365→**1390 passed** (+25, 회귀 0) / 프론트 155→**160 passed** (+5, 회귀 0). 영향 인덱스: 백엔드 84 모듈 177 테스트 / 프론트 57 모듈 33 테스트. `src/db/CLAUDE.md` purge_old_logs/search_logs 1행씩 + `src/routes/CLAUDE.md` /api/logs/search 1행 + `src/engine/CLAUDE.md` settlement 흐름 표 1행 + `frontend/CLAUDE.md` Logs 페이지 검색 박스 1문단 + `_workspace/00_leader_trading_rules.md` 사이클 6 통합 1행 | 사이클 6 (2026-05-17) 1차 완료 후 사용자 신규 요청 2건 통합 마감 (옵션 B): 1) system_logs 폭증 — INFO 등급 (구독/시세 etc) 30일 누적 시 supabase 부하 + UI 응답 저하 / 2) 키워드 검색 부재 — 운영자가 OPSP0002/nxt_downgrade 등 특정 사고 흔적 추적 어려움. 자율 결정: (a) retention 시점 settlement 흐름 통합 (07:50 _boot 가 아닌 20:10) — log_analysis 가 system_logs 를 *읽은 후* 정리 (분석 데이터 보존) / (b) INFO 2일 = 모멘텀/VB 회귀 검토 영업일 + 1일 마진 / WARNING+ 30일 = 사고 추적 + 자문 metrics 영구화 호환 / (c) cap 100,000 = supabase 단일 트랜잭션 부하 흡수 + 잔여분 다음 사이클 자연 흡수 / (d) WHERE None RuntimeError = 전체 DELETE 사고 절대 차단 (방어 코드) / (e) 검색 ILIKE substring = 운영자 익숙한 SQL 패턴 + KIS 거부 코드(APBK0918 등) 부분 매칭 / (f) limit 기본 200/최대 1000 = 페이징 부재 보완 + has_more 안내로 키워드 좁히기 유도 / (g) start/end ISO 직접 받기 = 검색 시 정밀 범위 (분/초 단위) 가능 / (h) 검색 모드 페이징 비활성 = limit 200 1회 응답으로 UI 단순화. 매매 코드 침범 0 — `risk.on_tick`/`order_engine`/전략 6개 변경 0. 사이클 18 보존 (OPSP backoff/K stale watcher/5xx dedupe/UI 컨텍스트). KIS API 호출 0 (DB 모듈만). push 보류 — 사용자 별도 승인 시점 19:30~20:00 NXT 진정 후 또는 익일 07:50 _boot 전 권장 |
| 2026-05-19 | 사이클 18 — ISA 5xx 폭주 정리 + UI 끊김/돌파 컨텍스트 (TDD, 3 영역 묶음, 운영 매매 흐름 무관) | **백엔드 (A)**: `src/api/base.py` 5xx WARNING dedupe (`_QUOTE_5XX_DEDUPE_WINDOW=60.0` + `_quote_5xx_dedupe` dict + `_quote_5xx_dedupe_lock` + `_record_5xx_for_dedupe`/`_emit_5xx_dedupe_summary`) + 라벨 선택 직후 fast 5xx 80%+ 메인 fallback 강제 (`_LABEL_FALLBACK_5XX_RATIO_THRESHOLD=0.8`, `fast_fallback` 메트릭). `src/services/quote_session_health.py` FAST_WINDOW (60s/80%/10회) 임계 + `_fast_window_*` 카운터 + `get_recent_5xx_ratio(label)` API. `src/engine/scheduler.py::_5xx_dedupe_summary_loop` 60s 주기 task. **백엔드 (B-1)**: `src/routes/realtime.py::get_subscriptions` 응답에 `last_tick_map: dict[ticker, ISO_KST\|null]` 추가. **백엔드 (C-1)**: `src/engine/strategy_registry.py::get_strategies_status()` 응답에 `tradable_boards: list[str]` 추가 (params 우선 → DEFAULT_TRADABLE_BOARDS fallback). **프론트 (B-2/3)**: `ScanMonitor.tsx` 끊김 시간대 컨텍스트 라벨 (`stale-context-label`, KRX 메인 빨강 / PRE_NXT 노랑 / 그 외 회색) + 끊김 펼치기 (`stale-list-toggle` + `stale-row-{ticker}` HH:MM:SS) + `useQuery(['realtime-subscriptions'])` 활용. `api/realtime.ts::SubscriptionsResponse.last_tick_map?` 추가. **프론트 (C-2)**: "돌파" 라벨 분기 — 활성 보드 ∩ tradable_boards 교집합 ∅ 시 회색 "돌파 (대기 — {보드라벨})". `types/trading.ts::StrategyInfo.tradable_boards?` 옵셔널 추가. **회귀 가드 (신규 23)**: `tests/unit/api/test_quote_pool_5xx_dedupe.py` 5 / `tests/unit/services/test_quote_session_health.py` +6 / `tests/unit/api/test_quote_pool_label_skip.py` 5 / `frontend/src/components/__tests__/ScanMonitor.stale_context.test.tsx` 5 / `frontend/src/components/__tests__/ScanMonitor.breakout_label.test.tsx` 5. **기존 갱신**: `tests/unit/engine/test_scheduler_stop_zombie_tasks.py::expected_members` 7→8 (사이클 18 `_5xx_dedupe_summary_task` 추가 동기). 백엔드 1349→**1365 passed** (+16, 회귀 0) / 프론트 145→**155 passed** (+10, 회귀 0). 영향 인덱스: 백엔드 84 모듈 174 테스트 / 프론트 57 모듈 33 테스트. `src/api/CLAUDE.md` 사이클 18 절 + `src/routes/CLAUDE.md` last_tick_map/tradable_boards 1행씩 + `frontend/CLAUDE.md` ScanMonitor 컨텍스트 3행 + `_workspace/00_leader_trading_rules.md` 사이클 18 섹션 | 2026-05-19 사이클 17 옵션 A 안정화 직후 운영 진단 3 결함 단일 PR fix: (1) ISA HTTP 500 폭주 (분당 ~30 행 WARNING / 3회 재시도 backoff 응답 지연 수 초) → dedupe (60s 첫 emit + summary 1행) + 80%+ 즉시 메인 fallback (수 초 → ms). 분당 ~30 → 2 행. (2) UI "끊김 19종목" 시스템 결함 오해 → 시간대 컨텍스트 (KRX 메인=빨강 결함 / PRE_NXT=노랑 관찰 / NXT 애프터=회색 정상) + 종목별 last_tick HH:MM:SS 펼치기. (3) 한화에어로스페이스(012450) 16:43 "돌파" 표시되나 매수 0건 (VB POST_NXT 보드 가드 정상이나 UI 불명확) → "돌파 (대기 — 프리/메인)" 회색 라벨 + title 툴팁. 자율 결정: (a) dedupe 키 `(path, label, status)` 3-tuple — 같은 라벨도 path 다르면 별도 추적 / (b) summary count>=2 만 INFO (1 은 첫 WARNING 으로 노출) / (c) fast 임계 80%/1분/10회 — 단발 5xx 폭주 premature 차단 / (d) `get_recent_5xx_ratio` read-only API / (e) disabled 라벨 sanity guard (1.0, FAST_MIN_CALLS) / (f) stale 컨텍스트는 SessionTracker 시간대와 동일 정책 / (g) 끊김 펼치기 KisAccountPoolCard 와 동일 큐 staleTime 5s / (h) tradable_boards 미존재 → 빨강 fallback (백엔드 미반영 시점 호환). 매매 코드 침범 0 — `risk.on_tick` / `order_engine` / 전략 파일 변경 0. 사이클 17 옵션 A / 사이클 16 AES / 사이클 15-A delta-only / 사이클 11 60s TTL 보존. VB `DEFAULT_TRADABLE_BOARDS` 변경 0 (POST_NXT 추가 없음, UI 라벨만 분기). 응답 키 추가만 0 제거. 프론트 옵셔널 `?`. push 보류 — 사용자 별도 승인 시점 19:30~20:00 NXT 진정 후 또는 익일 07:50 _boot 전 권장 |
| 2026-05-08 | TDD 하네스 초기 구성 — tdd-engineer 에이전트 + tdd-cycle/test-impact-index 스킬 + pytest/vitest/Playwright 인프라 + AST 기반 영향 인덱스 | `.claude/agents/`, `.claude/skills/`, `tests/`, `frontend/src/test/`, `e2e/`, `tools/test_impact/`, CI workflow | 사후 검증만 있던 기존 하네스에 Red→Green 사이클을 강제해 매매 시스템 회귀 위험을 차단 |
| 2026-05-08 | Phase B — 전략 엔진 단위 테스트 + Python 3.9 호환 패치 | `tests/unit/engine/**` (107건 신규), `src/api/base.py` 등 7개 파일에 `from __future__ import annotations` | 4개 전략(momentum/VB/LTV/donchian) + strategy_base/registry + session 의 신호·청산·가드 회귀 테스트 영구화. 영향 인덱스 매핑 모듈 5→19개로 확대 |
| 2026-05-08 | Phase C — order_engine + risk 통합 테스트 + 인덱스 conftest 전파 | `tests/integration/{conftest,test_buy_flow,test_sell_flow,test_chegyeol_race,test_buy_block_low_funds,test_risk_on_tick,test_multi_strategy_block}.py` (26건 신규), 추가 `from __future__ import annotations` 11개 파일, `eval_type_backport` dev 의존성, `tools/test_impact/build_index.py` conftest 전파 | 매수/매도 흐름·체결통보 race·잔고부족 락·on_tick 보드/자금 가드·전략 간 중복차단 회귀 테스트. 매핑 모듈 19→29개로 확대 |
| 2026-05-08 | Phase D — 시간 기반 스케줄러 통합 테스트 + scheduler_env fixture | `tests/integration/{test_force_clear_1520,test_next_day_clear,test_confirm_open_prices,test_reset_daily_state,test_auto_start,test_presubscribe}.py` (30건 신규), `conftest.py` scheduler_env fixture 추가 | 15:20 KRX 메인 강제청산(POST_NXT 보존)·익일 NXT 프리 청산(30s 안정화)·보드별 시가 확정·일일 상태 리셋·DB auto_start 우선 폴백·사전구독 합집합. 매핑 모듈 29→34개로 확대 |
| 2026-05-08 | Phase E — FastAPI 19+ 엔드포인트 계약 테스트 + 프론트엔드 컴포넌트/훅/API 단위 테스트 | `tests/contract/{conftest,test_routes_*}.py` (52건 신규), `frontend/src/{components,contexts,api}/__tests__/*.test.{ts,tsx}` (30건 신규), routes/models 11개 파일 `from __future__ import annotations`, root `package.json` js-yaml, `build_index_frontend.mjs` untracked 파일 포함 | TestClient 격리(lifespan 미실행) + 싱글톤 scheduler 모킹·trading/strategies/balance/performance/history/recommendations/log-reports/logs 라우트 계약·ConfirmModal/InfoTooltip/TradingStatusContext·6개 API wrapper 응답 unwrap. 백엔드 매핑 34→48/54(89%), 프론트 0→15/39(38%) |
| 2026-05-08 | Phase F — Playwright E2E 스모크 + CI 커버리지 게이트 60% + 회귀 패턴 정립 | `e2e/{playwright.config.ts,fixtures/api-mocks.ts,trading-flow,settings,history,recommendations}.spec.ts`, root `package.json` Playwright, `pyproject.toml` fail_under=60 + omit 정의, `.github/workflows/ci.yml` E2E job + coverage 게이트, `_workspace/regression/README.md` + `_workspace/red/_behaviors.md` | 5개 E2E 시나리오 통과(7.3s), pytest-cov 도입 baseline 60.31%, 회귀 등록 절차 + 행위 카탈로그 영구화 |
| 2026-05-09 | 모델 라우팅 분리 — 계획·검증=opus / 일반 구현=sonnet / 명령어=haiku | `.claude/agents/{team-leader,tdd-engineer,tester}.md` → `model: opus`, `.claude/agents/{backend-dev,frontend-dev}.md` → `model: sonnet`, `.claude/skills/auto-trading-orchestrator/skill.md` TeamCreate `model` 동기화(backend/frontend → sonnet), CLAUDE.md "모델 라우팅" 섹션 추가 | 작업 유형별 비용·속도 최적화 — 계획/검수에는 추론 품질, 일반 구현에는 균형, 명령어 작성에는 속도/비용 우선 |
| 2026-05-09 | 기본 프롬프트 수행 루트로 `team-leader` 우선 진입 명시 | `CLAUDE.md` "기본 프롬프트 수행 루트" 섹션 추가 | 모든 도메인 작업이 트레이더 관점의 검수·분배를 거치도록 일관된 진입점 강제 — 단순 질의·즉시 점검만 메인 세션 직접 응답 허용 |
| 2026-05-09 | 루트 CLAUDE.md 슬림화 — 변경 이력은 본 문서로 분리, 전략/체결통보/포지션/안전장치/DB 상세는 `src/engine`·`src/realtime`·`src/db` CLAUDE.md로 위임 | `CLAUDE.md`(244→축약), `docs/HARNESS_CHANGELOG.md`(신규) | 루트 문서 비대화 → 컨텍스트 비용 증가. 진실의 원천을 디렉토리별로 한 곳에 두고 루트는 진입점/하네스/배포만 유지 |
| 2026-05-15 | 신규 추세추종 전략 2종 추가 — `bull_flag_breakout`(눌림목 돌파, 폴/플래그 + 측정된 이동 익절) + `vcp_breakout`(미네르비니식 VCP, 베이스 + 변동성 수축) | `src/engine/strategies/{bull_flag_breakout,vcp_breakout}.py` 신규, `src/engine/scheduler.py` registry 등록 + import, `_workspace/00_leader_trading_rules.md` 6-E/6-F 섹션, `supabase/migrations/018_register_bull_flag_and_vcp.sql`, `tests/unit/engine/strategies/test_{bull_flag,vcp}_breakout.py` (50건 신규, Red→Green), `frontend/src/components/{TradeHistoryGrid,TradePnLGrid}.tsx` + `frontend/src/utils/strategyInfo.ts` UI 매핑, `tests/contract/conftest.py` 신규 전략 자금 0 격리, `src/engine/CLAUDE.md`·`CLAUDE.md` 다중 전략 표 갱신. `Position._MULTIDAY_STRATEGIES` 에 `vcp_breakout` 추가 — frozenset 교체. 신규 전략 모두 `enabled=false`/`weight=0` 디폴트(011 마이그레이션 패턴 — 검증 후 Settings 에서 수동 활성화) | 모멘텀(+29% 폭발) 후속 정리 진입로(눌림목) + donchian 정공법 보강(VCP 베이스 확인) — TDD Red→Green 사이클로 50/50 신규 테스트 통과, 백엔드 684+104, 프론트 74 회귀 무영향. 운영 활성화는 사용자 Settings 수동 토글로만 |
| 2026-05-15 | 백테스트 통합 사이클 Phase 0 (`4cb2cd7`) — TIME_RECOMMENDATION 19:50 → 20:00 이동 (자문 시점만 변경, TIME_NXT_POST_BUY_STOP=19:50 NXT 매수 중단은 그대로 유지) | `src/engine/scheduler.py` 상수 + 분기 이동(자문 호출을 TIME_NXT_POST_CLOSE 분기로 동기 순차 배치), `tests/integration/test_recommendation_time_change.py` (5건 신규 Red→Green: 상수값/매수 중단 보존/순서 보장/20:00 분기 호출/19:50 분기 부재), `src/engine/CLAUDE.md`·`_workspace/00_leader_trading_rules.md` 시간 상수 표 갱신, `_workspace/red/phase-0-recommendation-time-shift.md` 행위 분해 | Phase 3 백테스트 검증(외부 MCP 서버) 합류 시 자문 ↔ 백테스트 enqueue 시점 정합성 사전 확보. 영향 테스트 48개 전체 + 백엔드 전체 793개 회귀 0건 |
| 2026-05-15 | 백테스트 통합 사이클 Phase 1 (`7aa40a2`) — MCP 클라이언트 + 헬스체크 라우트 | `src/services/{mcp_client,exceptions}.py` 신규 (Streamable HTTP/SSE, 싱글톤 + 세션 ID 재사용 + 421 자동 재초기화 + asyncio.Lock + 타임아웃 분리), `src/routes/backtest.py::GET /api/backtest/mcp/health`, `frontend/src/api/backtest.ts::getMcpHealth`, `src/config.py` 환경변수 3종(`KIS_MCP_URL`/`KIS_MCP_ENABLED`/`BACKTEST_TIMEOUT_SECS`), `.env.example` 동기화, `tests/unit/services/test_mcp_client.py` 16 케이스(respx) + `tests/contract/{test_routes_backtest,test_backtest_mcp_health}.py` + `tests/integration/test_mcp_client_lifecycle.py`, `_workspace/00_leader_trading_rules.md` "외부 백테스트 서버 통합" 섹션 | 외부 stock-manager 백테스트 서버 graceful 통합 기반 — `KIS_MCP_ENABLED=false` 시 ConfigError raise → 자문 단계가 try/except 로 흡수. 운영 EC2 IP 화이트리스트 이미 통과 |
| 2026-05-15 | 백테스트 통합 사이클 Phase 2 (`bc0144b`) — BacktestEngine + 6 전략 YAML DSL + 마이그 019 | `src/engine/backtest_engine.py` (singleton + submit/poll/wait_for_result + 응답 unwrap 헬퍼), `src/engine/backtest_yaml.py` 6 전략 분류 — (a) momentum/VB/donchian YAML 빌더 / (b) LTV/bull_flag/vcp `BacktestNotSupportedError`, `src/models/backtest.py` (BacktestMetrics 8 메트릭 + diff 헬퍼), `supabase/migrations/019_backtest_runs.sql` + `src/db/backtest_runs.py` CRUD, `tests/unit/engine/{test_backtest_engine,test_backtest_yaml}.py` + `tests/unit/db/test_backtest_runs.py` | 외부 YAML DSL 표현 가능 3종에 한해 submit, (b) 폴백 3종은 명시 raise → Phase 4-bis 로컬 어댑터 분리 진입 게이트. DEFAULT_SYMBOLS 5 종목으로 외부 서버 캐시 적중률 확보 |
| 2026-05-15 | 백테스트 통합 사이클 Phase 3 (`ce3bdda`) — 20:00 자문 ↔ 백테스트 통합 + 마이그 020 | `src/engine/recommendation_engine.py::_enqueue_backtest_jobs / _backtest_poll_loop / _spawn_backtest_poll_task` (fire-and-forget + 24h timeout + 중복 task 가드), `supabase/migrations/020_parameter_recommendations_backtest.sql` 신규 `backtest_summary JSONB` 컬럼, `src/db/parameter_recommendations.py::update_backtest_summary / list_recommendations_pending_backtest` + `src/db/backtest_runs.py::list_by_date`, `tests/integration/test_recommendation_backtest_flow.py` 2 케이스 + `tests/unit/engine/{test_recommendation_backtest_hook,test_backtest_poll_loop}.py` + `tests/unit/db/test_parameter_recommendations_backtest.py` | 자문 INSERT 보존 + 백테스트 fire-and-forget 별도 task — settlement 20:10 race 무관. 6 전략 × 2 kind=12 backtest_runs row 영속, (a) 6 완료시 backtest_summary 동봉 |
| 2026-05-15 | 백테스트 통합 사이클 Phase 4 (`985300a`) — Recommendations UI 백테스트 카드 | `frontend/src/components/recommendations/BacktestComparisonCard.tsx` 신규 (8 메트릭 좌/우 비교 + 차이값 칩 + 로딩 분기 + (b) 폴백 라벨), `frontend/src/pages/Recommendations.tsx` 자산 배정 카드 위 조건부 렌더 (`backtest_summary != null`), `frontend/src/types/recommendations.ts` BacktestSummary 타입 추가, `frontend/src/components/__tests__/BacktestComparisonCard.test.tsx` RTL 케이스 (로딩/정상/(b) 폴백/차이 컬러 컨벤션), `frontend/CLAUDE.md` Recommendations 페이지 항목 갱신 | 운영자 가시성 — 자문 검증 결과를 차이값 컬러 칩으로 즉시 판단. `max_drawdown` 부호 컨벤션은 `diffSignInverted` 매개변수로 future-proof — Phase 5b 실측 후 확정 |
| 2026-05-16 | 백테스트 통합 사이클 Phase 5 — 회귀 가드 + 운영 모니터링 가이드 | `tests/integration/test_backtest_disabled_and_graceful.py` 3 케이스 신규 (KIS_MCP_ENABLED=false 모든 row skipped / 외부 서버 다운 → (a) failed + 자문 보존 / 24h timeout 미완료 row failed), `docs/backtest-monitoring.md` 신규 (활성화 절차 / Phase 5b 검증 단계 / 트러블슈팅 / MDD 부호 컨벤션 확정 절차 / 응답 키 매핑 검증 체크리스트 / 회귀 가드 매트릭스), `CLAUDE.md` 다중 전략 표 하단 "외부 백테스트 서버 통합 사이클" 2 문단 명시, `frontend/CLAUDE.md` Recommendations 섹션 BacktestComparisonCard 1 문단 추가 | 사용자가 2026-05-18(월) 20:00 첫 실 발화 후 Phase 5b 검증을 혼자 수행할 수 있도록 진단 절차 영구화. 백엔드 859 + 프론트 85 회귀 0건 |
| 2026-05-16 | 백테스트 통합 사이클 Phase 6 (보강) — 사후 검증 결함 3건 fix + 응답 키 확정 | `src/services/mcp_client.py::_extract_mcp_content` 신규 (MCP Streamable HTTP 2겹 래핑 unwrap — `{"content":[{"type":"text","text":"<JSON>"}]}` → `success/data` 평탄화), `call_tool` 마지막에 호출 추가 (회귀 보존), `initialize()` 세션-id 누락 로그 WARNING → DEBUG 다운그레이드 (stateless 외부 서버 정상), `src/engine/backtest_engine.py::_normalize_metrics` + `_NESTED_METRIC_MAP` 신규 (외부 응답 `data.result.metrics.{basic,risk,trading}` 3단 중첩 → 8 키 평탄화: `total_return → total_return_pct` / `annual_return → cagr` / `profit_loss_ratio → profit_factor` / `total_orders → total_trades`), `_extract_metrics` 가 `data.metrics` → `data.result.metrics` 두 경로 폴, `scripts/verify_mcp_response_schema.py` Section [6] BacktestMetrics 평탄화 결과 + Section [7] donchian YAML 외부 검증 보강, `tests/unit/services/test_mcp_client_unwrap.py` 9 케이스 + `test_mcp_client_session_log.py` 2 케이스, `tests/unit/engine/test_backtest_engine_unwrap.py` 5 케이스 + `test_backtest_engine_nested_metrics.py` 4 케이스 + `test_backtest_yaml_donchian_compat.py` 6 케이스, `docs/backtest-monitoring.md` Section 5-6 Phase 6 실측 결과 표 + 매핑 영구 명시, `_workspace/00_leader_trading_rules.md` Phase 6 1행 추가 | Phase 5 사후 검증으로 발견된 Critical 2건(unwrap 누락 / nested metrics 평탄화 누락) + Low 1건(세션 로그 노이즈) fix. 실측 외부 서버 응답: `max_drawdown` 양수 절대값 / `profit_loss_ratio` → `profit_factor` / `total_orders` → `total_trades` 컨벤션 영구 명시. donchian_swing YAML 외부 `validate_yaml_tool` 통과 → (a) 분류 유지 (`_FALLBACK_STRATEGIES` 변경 없음). 백엔드 888 passed(862→+26) / 프론트 85 회귀 0. Phase 5b 월요일 20:00 토글 안전 |
| 2026-05-17 | 자문 시스템 개선 사이클 Phase A — LTV `stop_loss_hits=0` 결함 진단 (진단만, 패치 없음) | `_workspace/red/phase-a-ltv-stop-loss-hits.md` 신규 진단 보고서 (root cause + 5/15 trade_history 실측 SQL + 후속 fix 방향 권고) | 5/15 첫 자문 metrics 분석에서 `stop_loss_hits=0` + `max_loss_pct=-7.554%` 모순 발견. root cause: `recommendation_metrics.compute_metrics()` 가 `current_params.get("stop_loss_rate")` 만 참조 — LTV는 `intraday_stop_loss`/`overnight_stop_loss` 분리 키 사용 → `None` 폴백 → 분기 영영 skip. metrics 결함(분류 a) 확정, execution 결함(분류 b) 아님. 후속 fix 는 별도 사이클로 분리 |
| 2026-05-17 | 자문 시스템 개선 사이클 Phase B — `PARAM_RANGES` 화이트리스트 7 키 확장 (TDD) | `src/engine/recommendation_engine.py::PARAM_RANGES` 7 키 추가(`k_value_krx_main/k_value_nxt_pre/k_value_nxt_post` 각 (0.5, 2.0) + `donchian_period` (10, 60) + `long_ma_period` (20, 120) + `volume_multiplier` (1.0, 5.0) + `atr_trail_mult` (1.0, 5.0)), `INT_PARAMS` 에 `donchian_period`/`long_ma_period` 추가, `tests/unit/engine/test_recommendation_param_ranges.py` 9 케이스 신규 (Red 7 → Green 9: 신규 키 등록·범위 정합성·정상 통과·범위 밖 거부 WARNING·INT 캐스트·기존 키 회귀 보존), `_workspace/00_leader_trading_rules.md` PARAM_RANGES 명세 동기화, `src/engine/CLAUDE.md` recommendation_engine 섹션 갱신 | 5/15 4 전략 `code_review_notes` 가 강하게 권고한 키들 — 다음 자문 사이클(5/18 월 20:00) 부터 OpenAI 가 보드별 K값/donchian 기간/거래량 멀티플/ATR 트레일 자동 튜닝 가능. `min_prdy_rate` 는 사이클 이전부터 등록되어 있어 중복 추가하지 않음(8→7 키 확장). 백엔드 897 passed(888→+9) / 프론트 영향 없음 |
| 2026-05-17 | 자문 시스템 개선 사이클 Phase A2 — LTV `stop_loss_hits=0` 결함 fix (TDD) | `src/engine/recommendation_metrics.py::_normalize_stop_loss_rate(params)` 헬퍼 신규 (3 키 후보 수집 `stop_loss_rate/intraday_stop_loss/overnight_stop_loss` → 음수만 인정 → `min(candidates)` = 절대값 큰 = 가장 보수적 → 후보 없으면 0.0), `compute_metrics()` 의 `_safe_float(current_params.get("stop_loss_rate"))` 단일 키 참조를 `_normalize_stop_loss_rate(current_params)` 로 교체, `tests/unit/engine/test_recommendation_metrics_ltv_stop_loss.py` 8 케이스 신규 (Case A LTV 분리 키 / Case B 단일 키 회귀 / Case C 부재 0.0 / Case D 5/15 LTV 064400 -3.030% 통합 / Case E intraday only / Case F 혼합 + 양수/None edge), `src/engine/CLAUDE.md` Phase A2 1행 + `_workspace/00_leader_trading_rules.md` 자문 섹션 1행 + 진단 보고서 상단 Resolved 마크 | Phase A 진단 후속 fix — LTV 분리 키(`intraday_stop_loss`/`overnight_stop_loss`) 가 `stop_loss_rate` 단일 키 참조에서 영영 skip 되던 결함 해소. 다른 5 전략(momentum/VB/donchian/bull_flag/vcp) 의 `stop_loss_rate` 단일 키 회귀 보존. 5/15 발화된 LTV `parameter_recommendations.metrics.stop_loss_hits=0` 은 소급 재계산 안 함 — 5/18 월 20:00 첫 자문부터 정상. 백엔드 905 passed(897→+8) / 프론트 영향 없음 |
| 2026-05-17 | 자문 시스템 개선 사이클 2 — 시장 레짐 필터 + dkstock.cloud 매크로 연동 (TDD, 옵션 1b+2b+3a+4a) | `src/services/dkstock_client.py` 신규(JWT Bearer + 401 자동 refresh + 24h 메모리 캐시 + DKSTOCK_REGIME_ENABLED 가드), `src/engine/market_regime.py` 신규(MarketRegime 싱글톤 + 복합 임계 매수 가드 + cash_usage_ratio_from_regime), `src/db/market_regime_snapshots.py` 신규(CRUD), `src/routes/market_regime.py` 신규(GET /current, GET /history, PUT /auto-adjust), `src/models/market_regime.py` 신규(Pydantic), `src/config.py` DKSTOCK_* 4 필드 추가, `src/db/system_config.py::get/set_auto_regime_adjust` 추가 + `cash_usage_ratio` 범위 [0.5, 1.0] → [0.0, 1.0] 확장, `src/engine/scheduler.py::_boot()` 에 `_refresh_market_regime_and_persist()` + `_resolve_cash_usage_ratio()` 헬퍼 추가, `src/engine/risk.py::on_tick()` 매수 분기 직전 `get_current_regime().is_buy_allowed(...)` 가드 + `_maybe_emit_regime_block` 분당 INFO 로그, `src/main.py` market_regime router 등록, `supabase/migrations/022_market_regime_snapshots.sql` + `023_cash_usage_ratio_range.sql` (적용 보류), `.env.example` DKSTOCK 4 변수, `frontend/src/components/MarketRegimeCard.tsx` 신규(red/gray/blue 배지 + 매수 가드 amber 배너 + auto-regime-adjust 토글 ConfirmModal), `frontend/src/types/market_regime.ts` + `frontend/src/api/market_regime.ts` 신규, `frontend/src/pages/Dashboard.tsx` ControlPanel 직하 삽입, `tests/unit/services/test_dkstock_client.py` 8 케이스, `tests/unit/engine/test_market_regime.py` 11 케이스, `tests/unit/db/test_market_regime_snapshots.py` 4 케이스, `tests/unit/db/test_system_config_auto_regime.py` 3 케이스, `tests/integration/test_boot_market_regime.py` 3 케이스, `tests/unit/engine/test_risk_regime_guard.py` 4 케이스, `tests/contract/test_routes_market_regime.py` 4 케이스, `frontend/src/components/__tests__/MarketRegimeCard.test.tsx` 7 케이스, `_workspace/00_leader_trading_rules.md` 사이클 2 섹션 + `src/engine/CLAUDE.md` market_regime + risk 가드 + `frontend/CLAUDE.md` MarketRegimeCard | 운영 graceful 정책: `DKSTOCK_REGIME_ENABLED=false` 기본 → 외부 호출 0건/매수 가드 비활성/cash_usage_ratio 자동 갱신 비활성. fetch 실패/timeout/토큰 만료 시 empty regime → 매수 가드 비활성 + 운영자 수동 ratio 보존. 매도/손절 영향 0건 — 보유 종목 청산은 `check_exit_signal` 분기에서 정상 작동. `auto_regime_adjust=false` 토글 시 수동값 그대로. 백엔드 958 passed(921→+37) / 프론트 100 passed(93→+7). 5/18 월 20:00 자문 사이클 1 검증 완료 후 EC2 `.env` 4 변수 추가 + 마이그 022/023 적용 + DKSTOCK_REGIME_ENABLED=true 토글로 활성화 |
| 2026-05-17 | 자문 시스템 개선 사이클 3 — VB 보드별 손절 분리 (TDD, 옵션 2 — 전략 내부 `_resolve_active_board()` 조회) | `src/engine/recommendation_engine.py::PARAM_RANGES` 에 `stop_loss_main`/`stop_loss_pre_nxt` `(-15.0, 0.0)` 2 키 추가(`stop_loss_post_nxt` 는 VB POST_NXT 미사용 — 사이클 3-B 재검토), `src/engine/recommendation_metrics.py::_normalize_stop_loss_rate(params)` 후보 키 3→5 확장(`stop_loss_main`/`stop_loss_pre_nxt` 추가, `min(candidates)` 정책 유지), `src/engine/strategies/volatility_breakout.py::_get_stop_loss_for_board(params, board)` staticmethod 헬퍼 신규(우선순위: 보드별 키 음수 → top-level `stop_loss_rate` fallback) + `check_exit_signal()` 손절 분기에서 `_resolve_active_board()` 호출 후 헬퍼로 임계 결정(시그니처 보존 `(ticker, current_price, open_price)` — 옵션 2 채택으로 risk.on_tick 영향 0), `supabase/migrations/024_vb_board_stop_loss_defaults.sql` 신규(VB strategy_config.params.stop_loss_rate → stop_loss_main/stop_loss_pre_nxt 멱등 자동 복사, 적용 보류), `tests/unit/engine/strategies/test_volatility_breakout_board_stop_loss.py` 6 케이스(A main 단독 / B pre_nxt 단독 / C top-level fallback / D 보드별 우선 / E 활성 보드 미감지 fallback / F 5/15 운영값 회귀), `tests/unit/engine/test_recommendation_metrics_board_stop_loss.py` 9 케이스(보드별 정규화 + 회귀 5 + compute_metrics 통합), `tests/unit/engine/test_recommendation_param_ranges_board_stop_loss.py` 4 케이스(등록 / 정상 통과 / 범위 밖 WARNING), `tests/integration/test_vb_board_stop_loss_fallback.py` 3 케이스(마이그 전/후/운영자 차별화), `_workspace/00_leader_trading_rules.md` 사이클 3 섹션 + `_workspace/cycle3b_ltv_board_stop_loss_spec.md` 신규(LTV 보드 × 시간 모드 4 조합 명세 placeholder) | VB `_resolve_active_board()` 가 None 반환 시(테스트 환경 / SessionTracker 미동작) top-level `stop_loss_rate` fallback 으로 graceful 회귀 0건. 5/18 첫 발화 영향 없음 (코드만 적용 + 마이그 보류). LTV/momentum/donchian/bull_flag/vcp 변경 0. PRE_NXT 노이즈 흡수 운영 효과는 5/19~ 데이터로 측정 후 사이클 3-B(LTV) 발의 결정. 백엔드 980 passed(958→+22) / 프론트 영향 없음. 운영 활성화: 마이그 024 적용 → Settings 에서 보드별 차별화 → 다음 자문부터 OpenAI 가 보드별 손절 추천 가능 |
| 2026-05-17 | 자문 시스템 개선 사이클 4 — 매크로 레짐 → AI 자문 user_payload 통합 (TDD) | `src/engine/market_regime.py::MarketRegime.is_empty()` 인스턴스 메소드 + `_classify_vix()` / `_classify_fear_greed()` 헬퍼 + `to_advisor_dict()` 12 키 반환(`regime/regime_desc/cycle_phase/vix/vix_level/fear_greed_score/fear_greed_label/buffett_ratio/buy_blocked/block_reason/cash_min_recommended/stock_max_recommended`. raw/cash_min 원본 등 내부 필드 제외). VIX 임계: low(<15) / normal(15~25) / elevated(25~35) / high(≥35). FG 임계: 극공포(<15) / 공포(15~35) / 중립(35~65) / 탐욕(65~85) / 극탐욕(≥85), `src/engine/recommendation_engine.py::_call_openai()` user_payload 분기 추가(`get_current_regime()` 호출 → `regime is not None and not regime.is_empty()` 시 `market_regime` 키에 `to_advisor_dict()` 동봉) + SYSTEM_PROMPT 끝에 매크로 컨텍스트 활용 가이드 1 문단 추가(defensive/neutral/aggressive 분기 + buy_blocked=True 시 매수 임계 변경 권고 무용 + weight_reasoning/code_review_notes 매크로 영향 명시 권장), `tests/unit/engine/test_recommendation_market_regime_payload.py` 27 케이스(A empty regime 키 미포함 회귀 / B None regime 키 미포함 / C defensive 활성 시 키 포함 / D 12 키 검증 / E VIX 분류 9 / F FG 분류 11 / G raw/cash_min 원본 제외 / H SYSTEM_PROMPT 키워드 / I 사이클 1 8 필드 회귀 보존) + `tests/integration/test_recommendation_with_market_regime.py` 3 케이스(A 비활성 자문 정상 / B 활성 defensive payload / C fetch 실패 graceful). autouse fixture 로 모듈 싱글톤 `set_current_regime(empty)` 매 테스트 후 reset — risk_on_tick 등 후속 테스트의 buy_blocked 가드 오염 차단. `_workspace/cycle4_market_regime_advisor_payload.md` 명세 신규 | 운영 graceful: `DKSTOCK_REGIME_ENABLED=false` 기본 상태 → 모듈 싱글톤 empty → 자문 user_payload 에 `market_regime` 키 미포함 → 사이클 1 8 필드 회귀 보존. 5/18 월 20:00 첫 자문 발화는 본 변경 영향 없음 (자문 호출 정상). 5/19 이후 DKSTOCK_REGIME_ENABLED=true 활성화 시 OpenAI 가 defensive/neutral/aggressive 레짐 + VIX/fear_greed 분류 + buy_blocked 상태를 인지해 손절률·position_ratio·daily_loss_limit·매수 임계를 동적 권고. 매매 실행 흐름 영향 0 (recommendation_engine 만 변경 — scheduler/risk/order_engine 무관). 5/15 자문 row 소급 재계산 안 함. 백엔드 1010 passed(980→+30) / 프론트 영향 없음. 활성화 절차 없음 — 자동 통합, DKSTOCK_REGIME_ENABLED=true 토글만 필요 |
| 2026-05-17 | 자문 시스템 개선 사이클 5 — 외부 통합 토글 Settings UI (DB 우선 / .env fallback / 활성화 즉시 백그라운드 fetch trigger, TDD) | `src/db/system_config.py::get/set_dkstock_regime_enabled` + `get/set_kis_mcp_enabled` 헬퍼 4개 추가(기본 None 반환 — 호출자가 .env fallback / 5% 단위 보정 없는 bool JSONB), `src/services/dkstock_client.py::_check_enabled_async` + `src/services/mcp_client.py::_check_enabled_async` 신규(DB 우선 / 예외 시 .env fallback graceful), login/refresh/_get/call_tool/list_tools/initialize/health_check 진입점들이 async 가드로 갈아끼움, `src/engine/backtest_engine.py::is_enabled_async()` 신규 + `run_for_strategy/poll/wait_for_result` 가 DB 우선 분기로 가드, `src/routes/system_integrations.py` 신규 6 라우트(GET/PUT × 3 토글) + `_refresh_market_regime_and_persist_safely` 백그라운드 task — `dkstock-regime` 활성화 직후 `asyncio.create_task` fire-and-forget + 비활성화 시 메모리 regime empty reset, `src/models/system_integrations.py` 신규(IntegrationToggleStatus `{enabled, source: db|env, env_value, db_value}` + IntegrationToggleRequest), `src/main.py` router 등록, `supabase/migrations/025_external_integration_toggles.sql` 신규(`dkstock_regime_enabled`/`kis_mcp_enabled` `{"value": false}` 멱등 INSERT, 적용 보류 — auto_regime_adjust 는 사이클 2 023 기존 키 활용), `frontend/src/components/IntegrationToggleCard.tsx` 신규(3 토글 단일 카드 + 분리 행 + source 배지 DB/env + ConfirmModal 이중 확인 + dkstock-regime 활성화 후 3s `fetch-progress-{key}` 인라인 진행 표시 + marketRegime queryKey invalidate), `frontend/src/api/integrations.ts` + `frontend/src/types/integrations.ts` 신규, `frontend/src/pages/Settings.tsx` CashUsageRatioCard 직하 IntegrationToggleCard 삽입, `tests/unit/db/test_system_config_integrations.py` 8 케이스(get 부재 None / set true/false / round-trip × 2 토글) + `tests/unit/services/test_dkstock_client_db_toggle.py` 6 케이스(DB true override env false / DB false override env true / DB None env fallback × 2 / 즉시 반영 / DB 예외 fallback) + `tests/unit/services/test_mcp_client_db_toggle.py` 6 케이스 동일 패턴 + `tests/unit/engine/test_backtest_engine_db_toggle.py` 6 케이스(`is_enabled_async` 3 케이스 + `run_for_strategy` ConfigError/허용/DB fallback) + `tests/contract/test_routes_system_integrations.py` 10 케이스(GET 4 + PUT 4 + 백그라운드 fetch 검증 + 500 에러) + `frontend/src/components/__tests__/IntegrationToggleCard.test.tsx` 6 케이스(3 토글 노출 / source 배지 / ConfirmModal / PUT API / API 에러 graceful / fetch progress) | 운영 graceful: DB 미설정 시 .env 환경변수로 fallback — Phase 1 / 사이클 2 운영자 영향 0. 활성화 즉시 fetch 는 백그라운드 task — API 응답은 즉시 반환, fetch 실패도 toggle 성공 유지. 운영 매매 흐름 미침범 (scheduler/order_engine/risk 영역 침범 0). 자율 결정: 3 토글을 단일 카드 + 분리 행으로 통합 (운영자 시야 집중). `dkstock-regime` 활성화는 매수 가드 + cash_usage_ratio 자동 조정 영향 가능 → ConfirmModal 이중 확인 강제. 5/17 사용자 EC2 검증 (실측: `regime=defensive vix=18.43 fg=76.0 buy_blocked=True cash_min=75`) 후속 — 5/17 휴장이라 `_boot()` 미발화 시에도 운영자가 Settings 토글로 즉시 매크로 fetch trigger 가능. 백엔드 1046 passed(1010→+36) / 프론트 106 passed(100→+6) / 영향 인덱스 갱신(백엔드 모듈 79·테스트 125 / 프론트 모듈 50·테스트 26). 운영 활성화 절차: (1) 마이그 025 Supabase 콘솔 적용 / (2) Settings → 외부 통합 → `매크로 레짐` ON / (3) `auto-regime-adjust` 검토 / (4) `.env` `DKSTOCK_REGIME_ENABLED=true` 는 운영자 결정 — DB 토글이 우선이라 제거해도 무방, 그대로 두면 fallback 안전망. 안전 평가: 5/18 자문 + 토글 활성화 동시 진행 시 자문 흐름은 user_payload 에 매크로 동봉 정상(사이클 4 통합), backtest_summary 도 정상 — 백테스트 통합은 ctor 시점 `_enabled` 가 아닌 매번 `is_enabled_async()` 호출이라 DB 갱신 즉시 반영 |
| 2026-05-17 | 자문 시스템 개선 사이클 6 — 로그 메뉴 신설 + 년/월/일 필터 (탭 통합, 매매 침범 0) | `src/db/system_logs.py::get_logs()` 시그니처 확장(`from_date/to_date/page/size` 키워드 추가, 응답 `{items, total, total_pages}` dict — limit 단독 호출 하위 호환 보존, supabase-py `count="exact"` 활용), `src/routes/logs.py` 신규 쿼리 파라미터(`from_date`/`to_date`/`page`/`size`) + 422 가드(`from_date>to_date`), `tests/unit/db/test_system_logs_filter.py` 8 케이스(KST `T00:00:00+09:00`/`T23:59:59.999999+09:00` 필터 / page→range / total_pages ceil / 빈 결과 / level+date 동시) + `tests/contract/test_routes_logs.py` 9 케이스(default dict / legacy limit+level / 422 limit·page·size·from>to / page+size / total/total_pages), `frontend/src/pages/Logs.tsx` 신규(탭 컨테이너 + `useSearchParams` `?tab=system\|daily-report` 동기화 + 알 수 없는 값 system fallback + role=tab/tabpanel + aria-selected), `frontend/src/components/SystemLogsTab.tsx` 신규(date input 2개 + 적용 버튼 + 레벨 토글 5개 + 페이징 1-base + 자동 새로고침 3s 조건부 — `fromDate===today && toDate===today && page===1` 만 활성 + KST 강제 `Intl.DateTimeFormat('en-CA', timeZone: 'Asia/Seoul')` 으로 today 계산·`toLocaleString` `timeZone: 'Asia/Seoul'` 으로 시각 표시 + `from_date>to_date` 클라이언트 가드 메시지), `frontend/src/components/DailyReportTab.tsx` 신규(기존 `pages/LogReports.tsx` 본문 추출 — JSX 동일, `formatDateTime` export 유지), `frontend/src/api/logs.ts` 신규(`fetchLogs(filter)` + `LogEntry/LogsPayload/LogsFilter` 타입), `frontend/src/App.tsx` 메뉴 "/log-reports 일일 로그 분석" → "/logs 로그" + `/log-reports` 라우트는 `<Navigate to="/logs?tab=daily-report" replace />` 북마크 호환, `frontend/src/pages/Dashboard.tsx` `<LogViewer />` import + JSX 제거(컴포넌트 파일 자체는 보존), `frontend/src/pages/LogReports.tsx` 삭제(`git rm`), `frontend/src/pages/__tests__/Logs.test.tsx` 5 케이스(기본/?tab=daily-report/탭 클릭 전환/garbage→system/두 탭 헤더 visibility) + `frontend/src/components/__tests__/SystemLogsTab.test.tsx` 9 케이스(today 기본/날짜 변경 재호출/level 인자/페이지 next/total_pages=1 disabled/빈 결과/from>to 가드/KST 시각/jsdom TZ=UTC 회귀), `frontend/src/__tests__/AppShell.test.tsx` 메뉴 라벨 "로그" 갱신, `frontend/src/pages/__tests__/LogReports.formatDateTime.test.ts` import 경로 `../../components/DailyReportTab` 갱신(회귀 가드 보존), `frontend/src/test/handlers.ts` `/logs` 기본 핸들러 추가(unhandled request 방지), `tests/contract/conftest.py::fake_get_logs` 새 시그니처(`from_date/to_date/page/size`) 흡수 + dict 응답(list/dict 양쪽 입력 호환), `tools/test_impact/manual_overrides.yaml` `src/routes/logs.py` + `src/db/system_logs.py` → contract/unit 테스트 매핑(contract URL 호출은 정적 import 분석 불가능) | 운영자가 5/18 자문 사이클 사고 추적 시 `/logs?tab=system&from_date=YYYY-MM-DD&to_date=YYYY-MM-DD&level=ERROR` 으로 즉시 검색 가능. 매매 코드 침범 0(`src/engine/`/`src/api/`/`src/realtime/` 무수정), 자문 흐름(`recommendation_engine` 20:00) 결합점 0(읽기 전용 조회). 자율 결정: (1) 단일 페이지 → 탭 통합(운영자 학습 비용 0) / (2) URL 쿼리 상태 보존(북마크/새로고침) / (3) 자동 새로고침은 오늘+page1 한정(과거 검색/페이징 중 폴링 차단) / (4) KST 강제(en-CA 포맷 + `toLocaleString` `timeZone: 'Asia/Seoul'` — 도커 빌드 UTC 환경 시각 어긋남 차단, L3 컨벤션) / (5) `LogReports.tsx` 삭제 + App.tsx 라우트 inline `Navigate`(lazy import 부담 제거). 백엔드 1060 passed(1046→+14: unit 8 + contract 6) / 프론트 119 passed(106→+13: Logs 5 + SystemLogsTab 9 - 사용 안 함 LogViewer 회귀 보존). 영향 인덱스 갱신(백엔드 모듈 79·테스트 126 / 프론트 모듈 53·테스트 28). 운영자 안내: 메뉴 "일일 로그 분석" 이 "로그" 안의 두 번째 탭으로 이동 |
| 2026-05-17 | 자문 시스템 개선 사이클 1 — 비중조절 사유 분리(`weight_reasoning`) + UI 카드 순서 변경 (TDD) | `supabase/migrations/021_weight_reasoning.sql` 신규(ALTER TABLE ADD COLUMN `weight_reasoning TEXT` ≤1000자, recommended_weight null 이면 null), `src/engine/recommendation_engine.py::_validate_recommendations()` 5-tuple `(validated_params, reasoning, weight, notes, weight_reasoning)` 로 확장 + SYSTEM_PROMPT 4번째 항목 명시 + `WEIGHT_REASONING_MAX_LEN=1000` / `WEIGHT_REASONING_FALLBACK="(사유 미제공)"` 상수, `generate_recommendations()` 의 unpacking + `insert_recommendation` 호출부 갱신, `src/db/parameter_recommendations.py::insert_recommendation(weight_reasoning=None)` kwarg 추가, `src/models/recommendation.py::RecommendationItem.weight_reasoning` 필드 추가, `frontend/src/types/recommendations.ts` 타입 동기화, `frontend/src/pages/Recommendations.tsx` 자산 배정 카드를 **최상단**(BacktestComparison 위)으로 이동 + 자산 배정 카드 내부에 `data-testid="weight-reasoning-{id}"` amber 영역(`bg-amber-50 border-amber-200 max-h-32 overflow-y-auto whitespace-pre-wrap`) 추가, `tests/unit/engine/test_recommendation_weight_reasoning.py` 11 케이스(시그니처/정상/누락 fallback/null 정리/1000자 truncate/비-str/빈문자열/5/15 LTV fixture/1000자 보존/양쪽 null/묵시적 누락) + `tests/unit/db/test_parameter_recommendations_weight_reasoning.py` 4 케이스(round-trip/null/J4 회귀/kwarg default) + `frontend/src/pages/__tests__/Recommendations.weightReasoning.test.tsx` 5 케이스(A amber / B null / C weight null / D DOM 순서 / E 1000자 overflow) + `Recommendations.cardOrder.test.tsx` 2 케이스(전체 카드 순서 / weight 없을 때 fallback), `_workspace/00_leader_trading_rules.md` 사이클 1 섹션 + `src/engine/CLAUDE.md` recommendation_engine 사이클 1 1행 + `frontend/CLAUDE.md` Recommendations 카드 순서 1행 | J4(2026-05-12) 통합 `reasoning` 에 묻혀 있던 비중 변경 사유를 별도 필드 + UI amber 영역으로 분리해 5/18 월 20:00 첫 자문 검수 시 가독성 향상. 자산 배정 카드를 최상단으로 이동 — 운영 의사결정 흐름이 비중 → 백테스트 → 로직 → 파라미터 순서로 자연스럽게 흐름. 5/15 발화된 row 영향 없음(소급 재계산 안 함, null 그대로). graceful fallback `(사유 미제공)` — LLM 사유 누락 시 영구 저장. 운영 매매 흐름 미침범. 백엔드 921 passed(905→+16) / 프론트 93 passed(86→+7) / 영향 인덱스 갱신(백엔드 107/72 + 프론트 24/44) |
| 2026-05-17 | 자문 시스템 개선 사이클 7-A — KIS 다중 계좌 인프라 (DB + 라우트 + 토큰 multi, TDD, 시세/매매 흐름 변경 0) | `supabase/migrations/026_kis_quote_accounts.sql` 신규(UUID PK / label UNIQUE / app_key·app_secret TEXT / kis_env CHECK in('real','vts') / active 부분 인덱스 / 적용 보류 — 사용자 명시 승인 후 Supabase 콘솔 수동 실행), `src/models/kis_quote_account.py` 신규(`KisQuoteAccount` 응답 모델 — `app_secret_masked` 만, 평문 필드 자체 부재 / `mask_secret()` 8자리 미만은 `****` 통일 길이 누출 차단 / `KisQuoteAccountCreate` `field_validator` strip / `KisQuoteAccountUpdate` active+label 부분 갱신만), `src/db/kis_quote_accounts.py` 신규(`list_accounts/get_account/get_account_by_label/insert_account/update_account/delete_account` + 토큰 매니저 전용 `get_credentials_for_token_manager(label)` 평문 노출 함수 분리 / `LabelConflictError` exception / 사전 label 충돌 검사 + DB UNIQUE 메시지 검출 이중 안전망 / `asyncio.to_thread` 일관), `src/auth/token.py` multi-account 확장(`TokenManager.__init__(*, app_key, app_secret, base_url, cache_path, label)` 키워드 주입 + 미지정 시 `settings.kis_*` fallback / `issue/revoke/get_approval_key/build_headers/cache` 헬퍼들이 `self.app_key` 사용으로 통일 / `_TOKEN_CACHE_PATH` 메인 캐시 보존 + `_QUOTE_TOKEN_CACHE_PREFIX` 라벨별 격리 / `_quote_token_managers: dict[str, TokenManager]` + `asyncio.Lock` 싱글톤 / `get_token_manager(label=None)` async lazy 발급 / `reset_quote_token_managers()` 테스트 헬퍼 / `_resolve_base_url('real'/'vts')` 도메인 분기 — 메인 매니저 `token_manager` 인스턴스 변경 0), `src/routes/kis_quote_accounts.py` 신규(GET 목록 + POST 등록 201 + PUT active/label 부분 갱신 + DELETE / 409 label 중복 / 422 빈 값 / 404 미존재 ID / 응답에 app_secret 평문 절대 노출 안 함), `src/main.py` router 등록, `tests/unit/db/test_kis_quote_accounts.py` 8 케이스(A insert→list 마스킹 / B active_only 필터 / C get_by_id+get_by_label / D update active false / E delete / F label 중복 / G kis_env CHECK 위반 / H 빈 값 거부), `tests/unit/auth/test_token_manager_multi.py` 6 케이스(A 메인 매니저 동일 인스턴스 / B DB 자격증명 로드 / C 싱글톤 / D 미등록 ValueError / E 메인·보조 격리 / F 메인 매니저 회귀 보존), `tests/contract/test_routes_kis_quote_accounts.py` 10 케이스(A 빈 목록 / B POST 201 마스킹 / C 409 label 중복 / D 422 빈 label / D' 422 빈 app_key / E GET 마스킹 보존 / F PUT active 토글 / G DELETE / H PUT 404 / H' DELETE 404), `src/auth/CLAUDE.md` + `src/db/CLAUDE.md` + `src/routes/CLAUDE.md` + `src/models/CLAUDE.md` 사이클 7-A 1행 추가, `_workspace/00_leader_trading_rules.md` 사이클 7-A 섹션 신규 | **시세/매매 흐름 변경 0** — 본 사이클은 인프라만. 보조 계좌 등록·조회·토큰 매니저 lazy 발급만 가능, 실제 시세 수신/매매 활용 0(7-B/7-C 사이클에서). 자금 안전 원칙: 매매/잔고/체결통보(H0STCNI0) 코드 경로(`src/api/order.py`/`src/api/balance.py`/`src/realtime/`) 무수정 — 영원히 메인 계좌 단일 보장. 자율 결정: (1) app_secret 마스킹 형식 `****` + 마지막 4자리(8자리 미만은 길이 정보 누출 차단 위해 `****` 통일) / (2) 토큰 매니저 싱글톤 위치는 모듈 전역 dict + `asyncio.Lock`(과거 `token_manager` 모듈 전역 인스턴스 패턴과 일관) / (3) PUT 라우트는 active/label 만 — app_key/app_secret 수정 시 보안 감사 추적성 위해 삭제 후 재등록 패턴 / (4) `get_credentials_for_token_manager(label)` 평문 노출 함수를 별도 분리 — 토큰 매니저 lazy 초기화 이외 호출 경로 차단. 백엔드 1084 passed(1060→+24: DB 8 + 토큰 6 + 라우트 10) / 프론트 영향 없음. 영향 인덱스 갱신(백엔드 모듈 82·테스트 129). 5/18 자문 + 사이클 7-A 운영 안전성 평가: 본 변경은 KIS 호출 경로 0 추가 — 메인 매니저만 사용 → 자문/매매/시세 흐름 무영향. 후속 사이클 7-B/7-C 진입 전 확인: (a) 보조 계좌 KIS Developers 발급 — 별도 앱 등록 후 app_key/app_secret 운영 환경 별도 보관 / (b) Supabase RLS 정책 활성화 (service_role + authenticated 외 SELECT 차단) / (c) `.token_cache_quote_*.json` 파일 추가 발생 — `.gitignore` 갱신 권장. 운영 활성화 절차: (1) 마이그 026 Supabase 콘솔 수동 적용 / (2) curl POST 또는 SQL INSERT 로 보조 계좌 등록(UI 는 7-D 사이클) / (3) 7-B WebSocketPool 사이클 진입 후 실제 시세 수신 분배 시작 |
| 2026-05-18 | 자문 시스템 개선 사이클 7-C — REST 시세성 호출 풀 + scanner 분배 명시화 + scheduler 보조 connect + K stale watcher 풀 통합 (TDD, 매매/잔고 메인 단일 절대 보장) | `src/api/base.py` 확장(~200 LOC: `kis_get_quote`/`kis_post_quote` public + `_request_via_quote_pool` 본체 + `QuotePoolPathError` 가드 + `_QUOTE_ALLOWED_PATHS` 5 path 화이트리스트 + `_select_quote_label()` 라운드로빈 + `_quote_semaphores[label]=Semaphore(18)` per-label 격리 + `_quote_request_metrics` 격리 dict / `get_quote_request_metrics`/`reset_quote_request_metrics` / 보조 매니저 lazy(`get_token_manager(label)`) + 발급 실패 graceful 메인 fallback / `[kis_rejection_quote]` prefix 거부 로깅 분리), `src/api/condition.py` 6 함수 위임(`is_market_open`/`next_trading_day`/`_fetch_fluctuation_rank`/`inquire_stock_basics`/`_fetch_stock_detail_and_cache`/`_fetch_daily_candles_and_cache` 모두 `kis_get` → `kis_get_quote` + import 정리), `src/engine/scanner.py` priority 명시 분배(`kis_ws.subscribe(...)` → `kis_ws_pool.subscribe(priority='HIGH'|'LOW', bypass_limit=...)` — positions/next_day_clear=HIGH+bypass / breakout/momentum/swing=LOW), `src/realtime/websocket_pool.py` 라이프사이클 추가(~120 LOC: `start(dispatch_message)` DB lazy connect 멱등 `_started` + `stop()` + `unsubscribe_in_pool(tr_id, tr_key)` 분배 추적 제거 + 세션 unsubscribe + `_quote_connect_tasks` cancel 정리), `src/realtime/websocket.py` `KisWebSocket.__init__(*, token_manager=None)` 선택적 매니저 주입(메인 기본 보존) + `connect()` 의 `get_approval_key()` 위임, `src/engine/scheduler.py` boot 통합(`kis_ws.connect` 직후 `kis_ws_pool.start(dispatch_message=...)` graceful 호출) + `_check_and_resubscribe_stale()` 풀 헬퍼 위임(`resend_subscribe_for_ticker` / `unsubscribe_in_pool` + `subscribe(priority='HIGH', bypass_limit=True)`), `tests/unit/api/test_quote_pool.py` 18 케이스 신규(보조 0 fallback / 라운드로빈 / 매니저 lazy / 매니저 실패 graceful / path forbidden×5 + allowed×5 / kis_get_quote 위임 / 메트릭 격리 / 5xx 재시도 / 매매·잔고 spy 가드), `tests/unit/api/test_condition_quote_routing.py` 7 케이스 신규(6 함수 위임 + order/balance 모듈 import 가드), `tests/unit/engine/test_scanner_priority_dispatch.py` 8 케이스 신규(HIGH/LOW 명시 / bypass_limit / 중복 dedup), `tests/integration/test_stale_watcher_pool.py` 5 케이스 신규(풀 헬퍼 / 분배 추적 / >3 강제 재등록 / >6 skip / fresh clear), `tests/integration/test_scheduler_boot_quote_sessions.py` 4 케이스 신규(보조 0 회귀 / N connect / 실패 graceful / 체결통보 메인 단일), `tests/unit/realtime/test_websocket_priority.py` + `tests/unit/engine/test_scanner_priority_order.py` + `tests/integration/test_stale_watcher.py` 풀 fixture 통합(`_ticker_to_session.clear()` + 풀 헬퍼 spy 위임), `tests/unit/api/test_condition_cache.py` + `tests/unit/api/test_condition_stock_basics.py` mock 경로 `kis_get` → `kis_get_quote` 정리, `src/api/CLAUDE.md` + `src/engine/CLAUDE.md` + `src/realtime/CLAUDE.md` + `_workspace/00_leader_trading_rules.md` 사이클 7-C 섹션 추가 | **자금 안전 절대 원칙 보존** — 매매(`place_order`/`cancel_order`) / 잔고(`get_balance`/`get_buyable`) / 체결조회(`get_daily_orders`) / 체결통보(H0STCNI0/H0STCNI9) 영원히 메인 단일. `src/api/order.py` / `src/api/balance.py` 변경 0 + 코드 가드(`QuotePoolPathError` 화이트리스트 외 path 즉시 raise) + 문서 + 모듈 import 가드 테스트 3중. 외부 호출자 인터페이스 100% 보존 — `fetch_daily_candles(ticker, days)` / `fetch_stock_detail(ticker)` / `subscribe_filtered_stocks(...)` 시그니처 변경 0. 보조 0개 회귀 보존 — 5/18 자문 영향 0(보조 등록 안 됐을 때 메인 only 자연 동작). 자율 결정: (1) `kis_request_quote` 위치는 `base.py` 확장 — 별도 파일 분리 안 함(Rate Limit/재시도/메트릭 로직 공유, 단일 진실의 원천), (2) per-label `Semaphore(18)` — 메인 20 보다 보수적 여유, (3) 메트릭 격리 dict — 메인 `_request_metrics` 와 완전 분리해 시세 풀 호출이 메인 카운터에 누출되지 않음, `by_label: defaultdict(int)` 로 `main`(fallback) / `quote-N` 별 분배 가시화, (4) 보조 토큰 매니저 발급 실패(`ValueError` 또는 일반 예외) → 메인 fallback graceful — 호출자 catch 불필요, (5) `unsubscribe_in_pool` 을 `unsubscribe` 와 분리한 이유: 강제 재등록 시 라운드로빈 재선택 의도 명시(분배 추적 dict 에서 제거 → 다음 `subscribe` 가 새 세션 선택 가능). 백엔드 1172 passed(1130→+42: api unit 25 + scanner unit 8 + integration 9) / 2 skipped 그대로 / 프론트 영향 없음(7-D 사이클 예정). 영향 인덱스 갱신(백엔드 모듈 83·테스트 138). 5/18 자문 + 보조 0개 운영 안전성: 본 사이클 코드 배포되어도 `kis_quote_accounts` 활성 행 0개면 메인 only 작동 → 자문/매매/시세 흐름 무영향. 후속 사이클 7-D 진입 전 확인: (a) Settings UI 에 `kis_quote_accounts` 등록 화면 추가(라벨 + app_key + app_secret + active 토글 + 마스킹 표시), (b) `/api/realtime/subscriptions` 의 `sessions` 배열 + `get_quote_request_metrics().by_label` 대시보드 노출, (c) 사이클 7-A 마이그 026 운영 적용(Supabase 콘솔 수동) 후 첫 보조 계좌 등록 — 운영 환경에서 분배 작동 검증, (d) 5/18 자문 직후 코드 배포 권장(거래일 중간 배포는 WebSocket 재연결 race 가능). 운영 활성화 절차: 보조 0개 유지로 코드 배포 → 5/18 자문 무영향 확인 → 7-D Settings UI + 모니터링 → 첫 보조 계좌 등록 → 다음 영업일 `_boot` 부터 분배 시작 |
| 2026-05-18 | 자문 시스템 개선 사이클 7-D — Settings UI 보조 계좌 관리 + Dashboard 시세 풀 모니터링 (프론트 only, TDD, 운영 매매 흐름 무관) | `frontend/src/types/kis-quote-accounts.ts` 신규(`KisQuoteAccount` / `KisEnv` / `KisQuoteAccountCreateInput` / `KisQuoteAccountUpdateInput` / `KisQuoteAccountsListResponse` — 백엔드 1:1, **app_secret 평문 필드 부재**), `frontend/src/api/kis-quote-accounts.ts` 신규(`listAccounts(activeOnly)` / `createAccount(payload)` / `updateAccount(id, payload)` / `deleteAccount(id)` — 사이클 7-A 라우트 4종 매핑, 응답에 마스킹만 노출), `frontend/src/api/realtime.ts` 확장(`SubscriptionSession` / `SubscriptionSessionTickers` / `SubscriptionsResponse` 타입 + `getSubscriptions()` — 사이클 7-B sessions 배열 활용), `frontend/src/components/KisQuoteAccountsCard.tsx` 신규(~340 LOC — Settings 카드: 표 7컬럼 / 등록 폼 / kis_env 라디오 real·vts / app_secret type=password autocomplete=new-password / 클라이언트 검증 빈값+label 형식 `^[A-Za-z0-9\-]+$` 거부 → POST 미발사 / ConfirmModal 이중 확인 — 등록·active 토글·삭제 모두 / 등록 성공 시 secret state 즉시 클리어 평문 잔존 차단 / `axios.isAxiosError` 분기 409/422 한글 메시지 / KST `Intl.DateTimeFormat` 등록일 표시 / app_key 마지막 4자리 마스킹 표시 — 평문은 백엔드 응답 그대로 보존하되 UI 는 마스킹만 노출 / queryKey `['kis-quote-accounts']` staleTime 30s), `frontend/src/components/KisAccountPoolCard.tsx` 신규(~200 LOC — Dashboard 카드: 새로고침 버튼 + 자동 30s 폴링(`refetchInterval`) / 총 슬롯 사용률 진행바 — 80% 이상 amber·미만 emerald / 세션별 표 7컬럼: label 배지(main=blue 강조 + "(체결통보)" 표기) / connected 배지(emerald/red) / subscribed/limit / 미니 진행바 / fresh·stale·재연결 / 보조 0개 시 `pool-no-secondary-note` 안내 "보조 세션 없음 (메인 only)" / 에러 시 `pool-error-message` graceful), `frontend/src/pages/Settings.tsx` `IntegrationToggleCard` 직하 `KisQuoteAccountsCard` 삽입(+3 LOC), `frontend/src/pages/Dashboard.tsx` `MarketRegimeCard` 직하 `KisAccountPoolCard` 삽입(+4 LOC — 시장 상태 → 인프라 상태 위계), `frontend/src/components/__tests__/KisQuoteAccountsCard.test.tsx` 10 케이스 신규(A 빈 목록 / B 1개 행+마스킹 / C 등록 폼+ConfirmModal+POST / D 409 label 중복 에러 / E 빈값 클라이언트 검증 / F active 토글 PUT / G 삭제 DELETE / H 평문 부재 / I secret 폼 클리어 / J password type 강제), `frontend/src/components/__tests__/KisAccountPoolCard.test.tsx` 6 케이스 신규(PA 메인 only + 안내 / PB 메인+보조 2 3행 / PC disconnect red 배지 / PD 슬롯 100% 진행바 / PE 500 graceful / PF 새로고침 즉시 재조회), `frontend/CLAUDE.md` Settings(KisQuoteAccountsCard) + Dashboard(KisAccountPoolCard) 명세 1행 추가, `_workspace/00_leader_trading_rules.md` 사이클 7-D 섹션 신규(운영자 사용 가이드 — KIS Developers 발급 → UI 등록 → _boot 자동 활성화 → Dashboard 분배 확인 → 트러블슈팅) | **운영 매매 흐름 무관** — 프론트엔드 UI 만 변경, 백엔드 라우트는 사이클 7-A 의 기존 라우트(`/api/integrations/quote-accounts/*`) 활용 — 백엔드 코드 변경 0 (1172 passed 그대로). 자금 안전 원칙: (1) app_secret 평문 잔존 시간 최소화 — 폼 제출 성공 시 secret state 즉시 클리어 + `type=password` 강제 + `autocomplete=new-password` / (2) 응답 마스킹 의존 0 — UI 가 `app_secret_masked` 만 참조(백엔드 실수로 평문이 와도 표시 안 됨, 회귀 가드 H/I/J) / (3) ConfirmModal 이중 확인 의무 — 등록·토글·삭제 모두 (사이클 5 컨벤션 동일) / (4) 등록·삭제는 다음 _boot(07:50) 부터 효력 — 운영자가 재기동 시점 의도적 통제. 자율 결정: (1) KisAccountPoolCard 위치 Dashboard(MarketRegimeCard 직하) — Settings 가 아닌 이유는 운영자가 매매 시간 중 분배 상태를 즉시 확인하는 모니터링 카드 성격(설정 변경이 아닌 가시화) / (2) 새로고침 버튼 + 자동 30s 폴링 — Trading Status 5s 폴링과 별개 queryKey 로 부하 격리, 30s 가 분배 변동 추적에 충분 / (3) 메인 행 강조 — `(체결통보)` 표기로 사이클 7-B 절대 원칙 시각화 / (4) 진행바 80% 임계 amber — 슬롯 가득 차기 전 운영자 인지 / (5) 등록 폼 검증 client-side 우선 — 422 왕복 비용 절감 + 즉시 피드백 / (6) "(선택)" `GET /api/integrations/quote-pool/metrics` 라우트 신설은 본 사이클에서 생략 — `/api/realtime/subscriptions` 의 sessions 만으로도 분배 가시화 충분, REST 메트릭은 별도 사이클 또는 운영자 요청 시 추가. 백엔드 1172 passed (변경 없음) / 프론트 119→135 passed(+16: KisQuoteAccountsCard 10 + KisAccountPoolCard 6) / TypeScript 컴파일 clean. 영향 인덱스 갱신(프론트 모듈 53→57·테스트 28→30, 백엔드 변경 없음). 5/18 자문 + 사이클 7-D 운영 안전성: 본 변경은 프론트엔드 UI 만 — `_boot`/`scheduler`/`order_engine`/`risk` 영역 침범 0. 보조 계좌 등록 안 한 상태로도 카드는 정상 렌더(빈 목록 안내 / 메인 only 안내) — 회귀 0. 운영자 사용 가이드: (1) KIS Developers 보조 계좌 5개 발급(`https://apiportal.koreainvestment.com/` → 내 API 키 관리 → 신청 5회), (2) Settings → 보조 KIS 시세 계좌 카드 → label/app_key/app_secret 입력 → 등록 ConfirmModal 확인 → 표 1행 추가 → 5회 반복, (3) 다음 영업일 07:50 `_boot` 자동 활성화 또는 즉시 활성화 위해 `POST /api/trading/stop`→`/start` 재기동, (4) Dashboard "KIS 시세 풀" 카드에서 메인 + quote-1~5 = 6행 표 + 총 슬롯 246 확인, (5) disconnect 배지(red) 시 `system_logs` `[pool_start]`/`[token]` 로그로 토큰 발급 실패 추적. 사이클 7 시리즈(A/B/C/D) 마무리 — 백엔드 인프라(A: DB+토큰 / B: WebsocketPool / C: REST 풀+lifecycle) + 프론트엔드 UI(D) 완성. 후속 권장: (a) 마이그 026 운영 적용 — Supabase 콘솔 수동, (b) KIS Developers 보조 5계좌 발급 — 사용자 액션, (c) Settings UI 등록 → 다음 _boot 부터 시세 풀 분배 시작, (d) 5/18 첫 자문 사이클 무영향 검증 후 5/19 거래일 진입 시 분배 모니터링 |
| 2026-05-17 | 자문 시스템 개선 사이클 7-B — WebsocketPool 시세 분배 (메인 + 보조 N 세션 풀, TDD, 옵션 2 — kis_ws 메인 인스턴스 재사용 + 풀 신설) | `src/realtime/websocket_pool.py` 신규 (~280 LOC, `WebsocketPool` 클래스 + `kis_ws_pool` 싱글톤 + `QuoteSessionExecutionNoticeError` + `_enforce_main_only_execution_notice` 헬퍼 — 메인은 기존 `kis_ws` 재사용으로 인스턴스 동일성 유지 + handler/scheduler/scanner 의 참조 분기 결함 차단 / 분배 정책: HIGH→메인 bypass_limit=True 절대 보장 / LOW→보조 라운드로빈 + 가득·disconnect 세션 건너뜀 + 모두 가용 없으면 메인 fallback / 중복 ticker→메인 우선·보조에 있는데 HIGH 들어오면 메인 승격 + 보조 unsubscribe + `[pool_promote]` INFO / 체결통보(H0STCNI0/H0STCNI9)→무조건 메인 강제 + 보조 직접 호출 시 RuntimeError / 모든 세션 가득→drop + `[priority_drop_pool] tr_key=... priority=LOW reason=all_sessions_full main=N/41 quotes=M` / 호환 property `_subscriptions`/`_subscriptions_acked`/`_ws`/`_reconnect_count` — 합집합 또는 메인 기준), `src/routes/realtime.py::GET /api/realtime/subscriptions` 응답 확장(`kis_ws_pool` 호출로 변경 + `sessions: [{label, subscribed, acked, fresh, stale, limit, ws_connected, reconnect_count, tickers}]` 배열 추가 + `total/acked/fresh_60s/stale_60s` 합집합 카운트 + `limit = MAX_SUBSCRIPTIONS × len(sessions)` — 보조 0개 시 sessions 길이 1 기존 호환), `tests/unit/realtime/test_websocket_pool.py` 25 케이스 신규(분배 알고리즘 / 중복 / 체결통보 강제 / drop / get_subscribed_tickers / get_session_status / unsubscribe / 보조 0개 회귀 / round_robin 슬롯 가득 건너뜀 / default priority LOW), `tests/unit/realtime/test_pool_safety_rules.py` 8 케이스 신규(E1 메인 bypass×2 / E2 세션 독립 / F1 각 세션 독립 발화 / K stale 분배 추적×2 / 메인 단일 체결통보 가드 RuntimeError×2), `tests/integration/test_pool_distribution.py` 6 케이스 신규(5 세션 100 종목 분배 / 총 슬롯 246 drop / disconnect graceful / HIGH bypass 메인 가득 / 보조 0개 회귀 / 100 종목 통합 조회), `tests/contract/test_routes_subscriptions_pool.py` 7 케이스 신규(sessions 배열 / 보조 0개 길이 1 / 보조 3개 길이 4 / 필수 키 / total=sum / acked=sum / limit 확장), `src/realtime/CLAUDE.md` 사이클 7-B 섹션 신규(자금 안전·분배 정책·운영 활성화·안전 규칙 멀티 세션·라우트 응답 확장·회귀 가드), `_workspace/00_leader_trading_rules.md` 사이클 7-B 섹션 추가 | **외부 호출자 인터페이스 100% 보존** — `scanner.subscribe_filtered_stocks` / `scheduler.kis_ws.*` / `routes/realtime.py` 의 `kis_ws` import 그대로 작동. 메인 인스턴스 자체를 풀이 재사용하므로 기존 호출자가 `kis_ws._subscriptions` set 직접 참조하는 패턴도 회귀 0건. 자금 안전 절대 원칙: (1) 체결통보 H0STCNI0/H0STCNI9 → 메인 단일 강제 + 보조 시도 RuntimeError, (2) 매매/잔고/체결조회 → 변경 0 (사이클 7-A 가드 그대로), (3) 보조 세션 0개 시 메인 only 회귀 보존 → 5/18 자문 영향 0. 자율 결정: (1) 옵션 2 채택 — 풀의 `_main` 을 기존 `kis_ws` 모듈 변수 그대로 재사용해 인스턴스 동일성 유지(handler.py 등 다른 모듈의 참조 분기 결함 차단). 옵션 1(`kis_ws` 자체를 `WebsocketPool` 으로 교체)은 인스턴스 동일성 잃어 회귀 위험 / (2) 분배 추적 dict `_ticker_to_session` 은 ticker→KisWebSocket 인스턴스 참조 보유 — label 비교 대신 `is` 비교로 라벨 변경 안전 / (3) 중복 ticker 처리: 메인 우선 + 보조→HIGH 승격 (보조 unsubscribe + 메인 add) / (4) 라우트 `limit` 의미 확장 — MAX_SUBSCRIPTIONS × 세션 수(총 용량 표현). 메인 단독 41 의미는 sessions[0].limit 로 분리 / (5) `get_session_status` 의 `tickers` 키는 sorted subscribed/acked 만 (fresh/stale 은 라우트 레벨에서 ticker_last_tick 비교 후 카운트). 백엔드 1130 passed(1084→+46: unit 33 + integration 6 + contract 7) / 프론트 영향 없음 (사이클 7-D 에서). 영향 인덱스 갱신 필요(백엔드 새 모듈 `src/realtime/websocket_pool.py` 등록). 5/18 자문 + 사이클 7-B 운영 안전성: 보조 계좌 0개 상태에서 풀은 메인 only 작동 — 자문 흐름·매매 흐름 영향 0. 5/18 월 09:00 거래일 진입 시 풀 코드는 배포되어 있되 `kis_quote_accounts` 테이블 미등록이면 사이클 7-A 직후와 동일 동작. 후속 사이클 7-C 진입 전 확인: (a) `scheduler._boot()` 가 풀의 보조 세션 connect() 호출 추가 (시세 흐름 변경 시작 시점) / (b) `scanner.subscribe_filtered_stocks` 가 풀의 `subscribe(priority=...)` 로 명시 분기 (현재는 `kis_ws.subscribe` 호출이 풀 메인으로 자연 routing 됨) / (c) K stale watcher 가 `kis_ws_pool.resend_subscribe_for_ticker` 사용으로 갈아끼움 — 분배 추적 정합성 유지. 운영 활성화 절차: 보조 계좌 0개 유지로 코드만 배포 → 5/18 자문 무영향 확인 → 7-C 사이클에서 보조 세션 connect() 통합 |
| 2026-05-18 | 자문 시스템 개선 사이클 8 — 매수 가드 4 모드(OFF/WARN/SOFT/HARD) + 4 임계값 조정 Settings UI (TDD, 옵션 1c+2a+3a+4a) | `supabase/migrations/027_buy_block_mode.sql` 신규(5 키 멱등 INSERT — `buy_block_mode='HARD'` / `buy_block_vix_threshold=25.0` / `buy_block_fg_high_threshold=85.0` / `buy_block_fg_low_threshold=15.0` / `buy_block_regime_defensive_enabled=true`, 적용 보류 — 사용자 명시 승인 후 콘솔 수동 실행), `src/db/system_config.py::get_buy_block_mode / set_buy_block_mode` (4 모드 외 ValueError) + `BuyBlockThresholds` Pydantic + `get_buy_block_thresholds / set_buy_block_thresholds(vix_threshold=, fg_high_threshold=, fg_low_threshold=, defensive_enabled=)` 부분 갱신 헬퍼 + `_get_float_or_default / _get_bool_or_default` 내부 헬퍼(JSONB `{"value":...}` + 과거 직저장 호환), `src/engine/market_regime.py::BuyBlockState` dataclass 신규(mode/blocked/soft_multiplier/reasons) + `_db_get_buy_block_mode / _db_get_buy_block_thresholds` 위임 진입점(테스트 monkeypatch 용) + `MarketRegime._collect_reasons()` 헬퍼(4 임계 OR 다수 사유 수집, defensive_enabled toggle 분기) + `MarketRegime.get_buy_block_state()` async(4 모드 분기 — OFF: 평가 자체 스킵 / HARD: blocked=True / WARN: blocked=False + reasons 보존 / SOFT: blocked=False + soft_multiplier=0.5), `MarketRegime.is_buy_allowed()` 동기 API 사이클 2 회귀 가드 보존, `src/engine/risk.py::on_tick` 매수 분기 직전 `get_buy_block_state()` await + 4 모드 분기(HARD continue / WARN `[buy_block_warn] strategy=... reasons=...` 1행 / SOFT `soft_multiplier` 전달 / OFF 비활성) + 사이클 2 `[regime_block]` 1분 주기 로그는 HARD 분기에서 그대로 보존, `src/engine/order_engine.py::execute_buy(soft_multiplier=1.0)` kwarg 추가 + `calc_buy_quantity` 결과에 `max(1, int(qty * multiplier))` 적용 + `[buy_block_soft]` 1행 INFO(축소 시), `src/models/system_integrations.py::BuyBlockMode / BuyBlockThresholdsModel / BuyBlockStatusResponse / BuyBlockUpdateRequest` 신규(Pydantic Field ge/le 로 422 자동: VIX [10,50] / FG_high [50,100] / FG_low [0,50]), `src/routes/system_integrations.py::GET /api/integrations/buy-block` + `PUT /api/integrations/buy-block`(부분 갱신, 422/500/200) + `_build_buy_block_status()` 헬퍼(현재 mode+thresholds+state 메모리 regime 평가), `frontend/src/types/integrations.ts::BuyBlockMode/BuyBlockThresholds/BuyBlockState/BuyBlockUpdateRequest` 타입 추가, `frontend/src/api/integrations.ts::getBuyBlock / setBuyBlock` 추가, `frontend/src/components/IntegrationToggleCard.tsx::BuyBlockSection` 신규(~250 LOC — 모드 select + ConfirmModal 모드별 안내 메시지 / 4 임계 슬라이더 + defensive 체크박스 + 저장 버튼(ConfirmModal 없이 즉시) / `buy-block-reasons` 사유 리스트 / `buy-block-soft-multiplier` SOFT 안내 / `buy-block-error` 500 graceful), `tests/unit/db/test_system_config_buy_block.py` 17 케이스 신규(모드 4종 round-trip × 4 + 모드 외 ValueError × 8 + 부분 갱신 / 전체 갱신 / defensive 단독 / 기본값), `tests/unit/engine/test_market_regime_buy_block_state.py` 10 케이스 신규(DB 미설정 HARD / HARD blocked / SOFT 0.5 / WARN allow / OFF disable / defensive_enabled=false / 4 사유 / 임계 적용 / empty graceful / is_buy_allowed 회귀), `tests/unit/engine/test_risk_buy_block_modes.py` 11 케이스 신규(HARD skip / HARD allow / WARN 로그 / SOFT multiplier kwarg × 2 / OFF 비활성 / 4 모드 청산 무관 parametrize 4 / 사이클 2 회귀), `tests/contract/test_routes_buy_block.py` 9 케이스 신규(GET 응답 구조 / SOFT 응답 / PUT mode-only / PUT thresholds-only / defensive=false / 422 모드 / 422 VIX / 422 FG_high / 422 FG_low), `frontend/src/components/__tests__/IntegrationToggleCard.test.tsx` +7 케이스(I8-A select+4 슬라이더 / I8-B reasons / I8-C 모드 변경 ConfirmModal+PUT / I8-D 임계 슬라이더 PUT / I8-E defensive 체크박스 PUT / I8-F SOFT multiplier 표시 / I8-G 500 graceful), `_workspace/00_leader_trading_rules.md` 사이클 8 섹션 신규 + `src/engine/CLAUDE.md` market_regime + risk.on_tick 4 모드 분기 + `src/db/CLAUDE.md` system_config 신규 5 키 + `src/routes/CLAUDE.md` buy-block GET/PUT + `frontend/CLAUDE.md` IntegrationToggleCard BuyBlockSection | **현재 동작 회귀 보존 100%** — 기본값 `buy_block_mode='HARD'` + 기본 임계(25/85/15/true) = 사이클 2 단일 HARD 분기 동작 동일. 마이그 027 미적용 / DB 미설정 / 환경변수 없음 모두 코드 디폴트로 fallback → 회귀 0건. 자율 결정: (1) 라우트 위치 — 기존 `src/routes/system_integrations.py` 확장(사이클 5 컨벤션). 별도 `buy_block.py` 파일 분리 안 함 (3 토글 + buy-block 모두 운영 통합 토글 성격) / (2) SOFT multiplier 적용 layer — OrderEngine.execute_buy `soft_multiplier` kwarg(risk.py 가 1.0 미만이면 전달, OrderEngine 이 `max(1, int(...))` 보장). risk.py 에서 직접 `calc_buy_quantity` 곱하기 보다 OrderEngine 이 최종 quantity 결정의 단일 진실 / (3) `is_buy_allowed()` 동기 API 보존 — `to_advisor_dict.buy_blocked` 호환성 + 회귀 가드용 / (4) ConfirmModal — 모드 변경만 이중 확인(매매 흐름 직접 영향), 임계값 변경은 즉시 적용(덜 위험). 자금 안전 원칙: (1) 매도/손절 4 모드 모두 무관 — `check_exit_signal` 분기는 가드 진입 전 / (2) SOFT 최소 1주 보장 — `max(1, int(qty * 0.5))` 으로 0 으로 떨어지는 결함 차단 / (3) DB 우선 + .env fallback 없음 — 운영 가변 설정(DB 미설정 시 코드 디폴트 안전) / (4) 외부 fetch 실패 / DKSTOCK 비활성 → empty regime → reasons=[] → blocked=False (graceful). 5/17 사용자 실측 케이스(`regime=defensive vix=18.43 fg=76.0`) 해결: SOFT 모드 or `defensive_enabled=false` 선택으로 매수 잠금 완화 가능 + 진성 위기(VIX>30+defensive) 시 HARD 유지. 백엔드 1219 passed(1172→+47: DB 17 + market_regime 10 + risk 11 + contract 9) / 2 skipped 그대로 / 프론트 142 passed(135→+7) / TypeScript 컴파일 clean. 영향 인덱스 갱신(백엔드 모듈 83 / 프론트 모듈 57). 5/18 자문 + 사이클 8 운영 안전성: 본 사이클 배포 후에도 HARD 기본값 + 기본 임계 = 사이클 2 동작 100% 보존 → 5/18 월 20:00 첫 자문 영향 0. 운영자가 명시적으로 IntegrationToggleCard 매수 가드 영역에서 모드/임계 조정 시에만 동작 변경. 운영 활성화 절차: (1) 마이그 027 Supabase 콘솔 수동 적용(권장, 미적용도 코드 디폴트로 안전 — 적용 시 운영자가 즉시 조정 가능) / (2) Settings → 외부 통합 → 매수 가드 영역 → 5/17 케이스 권장: 모드 SOFT 또는 defensive 체크박스 해제 / (3) `[buy_block_warn]` `[buy_block_soft]` `[regime_block]` 3 prefix 로그로 가드 동작 추적 |
| 2026-05-19 | 사이클 17 보강 — KIS OPSP0002 공식 답변 반영 (K stale watcher 재SEND 폐기 + OPSP backoff 300s) | **수정**: `src/engine/scheduler.py` (`_check_and_resubscribe_stale` 의 1~5회 `pool.resend_subscribe_for_ticker` 재SEND 분기 완전 폐기 → 첫 stale 즉시 `pool.unsubscribe_in_pool` + `pool.subscribe(priority='HIGH', bypass_limit=True)` 강제 재등록. 6회 초과 → skip. 상수 `MAX_STALE_RETRIES=5` 신규 + 기존 `STALE_FORCE_REREGISTER_AFTER=5` 회귀 가드 의미만 보존) / `src/realtime/websocket.py` (OPSP0002 backoff 60s → 300s. `_handle_raw` 의 `_opsp_backoff_until[(tr_id, tr_key)] = time.time() + 300.0` 변경 + INFO `[ws_opsp_backoff until=+300s]`). **기존 갱신**: `tests/unit/engine/test_stale_watcher_thresholds.py` 7 → 9 케이스 (`test_force_reregister_fires_on_first_stale` / `test_force_reregister_continues_until_max_retries` / `test_skip_after_max_stale_retries` / `test_max_stale_retries_is_5` 갱신·신규, 트래픽 산정 보존) / `tests/unit/realtime/test_ws_subscribe_backoff.py` (case A `test_opsp0002_registers_backoff_60s` → `test_opsp0002_registers_backoff_300s` 갱신, case C 만료 시각 검증값 60s → 300s 정책 반영) / `tests/integration/test_stale_watcher.py` K-2/K-3/K-6 갱신 (1~5회 즉시 강제 재등록 / 6회 이상 skip 의미 반영, 재SEND 0건 보장) / `tests/integration/test_stale_watcher_pool.py` D-1/D-2/D-3/D-4/D-6 갱신 (재SEND 0건 보장 + 강제 재등록 priority=HIGH bypass_limit=True 검증). 백엔드 1345 → **1349 passed**(+4 신규 = stale_watcher_thresholds 7→9 + 신규 max_stale_retries 1 + opsp 300s 갱신 1, 회귀 0). `src/engine/CLAUDE.md` `_stale_watcher_loop()` 절 갱신(1~5회 재SEND 분기 폐기 + 즉시 강제 재등록 명시) + 사이클 17 보강 1행 추가 / `src/realtime/CLAUDE.md` OPSP0002 backoff 60s → 300s 명시 | **KIS 공식 답변 (2026-05-19) 반영**. 인용: "등록이 1회만 될 수 있도록 기 요청된 목록을 관리하여 기등록한 사항을 재등록하지 않도록 부탁드립니다. (당사 서버 부담 시 LMS + 앱정보 이용중지 처리)". 현 시스템의 KIS 답변 위반 재SEND 경로 두 곳 차단: (1) K stale watcher 의 1~5회 `pool.resend_subscribe_for_ticker` (같은 종목 같은 세션에 SUBSCRIBE 재발송) → 무한 루프 + LMS 위험 / (2) OPSP0002 후 60s backoff → `_scan_loop` 5분 주기와 만료 시각 race → 또 OPSP0002. 자율 결정: (a) 재SEND 분기 완전 폐기 — `pool.resend_subscribe_for_ticker` 함수 자체는 보존(미사용·향후 운영 도구 후보), 호출자만 0 / (b) 첫 stale 즉시 강제 재등록 (unsubscribe + subscribe HIGH) — KIS 정상 "신규 등록" 패턴 + 5/12 silent inactive 사고 안전망 보존 / (c) `MAX_STALE_RETRIES=5` — 6회 이상이면 영구 stale 의심 (거래정지·이상 종목) skip + `_scan_loop` 위임 / (d) OPSP backoff 60s → 300s — `_scan_loop` 5분 주기 ≥ backoff 만료 보장, 같은 사이클 내 재시도 차단 / (e) `bypass_limit=True` 검사 skip 보존 — HIGH (보유/익일청산) 손절 우선. 매매 흐름 영향 0 — `risk.on_tick` / `order_engine.execute_buy/sell` 무변경. 안전 원칙 보존: Codex `1350aa8`+`84372cb` (handler/websocket try/except), 사이클 16 AES 키 격리, 사이클 15-A delta-only, 사이클 14-C ACK 정합성, 사이클 13-D `_resubscribe_stale_priority`, 사이클 17 본체 (380e50f) + MAX SUBSCRIBE OVER (ae2f46a). 예상 효과: 분당 KIS 부담 회피 = 재SEND 0건 + backoff 300s 로 OPSP0002 시간당 0~2 회 회복. 운영 활성화: push → EC2 자동 배포 → 다음 `_boot` 부터 정상 흐름. 사용자 명시 옵션 A (사이클 17 본체+MAX SUBSCRIBE OVER 위에 보강 합쳐서 push 승인) |
| 2026-05-19 | 사이클 17 — 사이클 15-B/C 전체 롤백 + OPSP0002 backoff 가드 신규 (시세 수신 복구 + 단순화) | **삭제**: `src/engine/stream_pool_manager.py` (294 LOC) + `src/engine/near_signal_monitor.py` (258 LOC) + `tests/unit/engine/test_stream_pool_manager.py` (11 케이스) + `tests/unit/engine/test_near_signal_monitor.py` (12 케이스) + `tests/unit/engine/test_scheduler_near_signal_integration.py` (5 케이스) + `tests/contract/test_routes_stream_status.py` (5 케이스) + `frontend/src/pages/StreamStatus.tsx` (165 LOC) + `frontend/src/pages/__tests__/StreamStatus.test.tsx` (7 케이스). **수정**: `src/engine/scheduler.py` (`_near_signal_loop` 메서드 ~100 LOC 제거 + `_near_signal_task` 인스턴스 변수 + start() 발화 + finally/stop() cancel 루프 멤버 제거 + `_build_priority_groups` `near_signal_mode` 분기 제거) / `src/config.py` (`Settings.near_signal_mode` 필드 제거) / `src/routes/realtime.py` (`get_stream_status` 엔드포인트 제거) / `src/realtime/websocket.py` (`KisWebSocket.__init__` 에 `_opsp_backoff_until: dict[tuple[str, str], float] = {}` + `subscribe()` 진입 시 `bypass_limit=False` 이고 `time.time() < until` 면 send skip + DEBUG `[ws_subscribe_backoff]` + `_handle_raw` OPSP0002 분기에 `self._opsp_backoff_until[(tr_id, tr_key)] = time.time() + 60.0` 기록 + INFO `[ws_opsp_backoff until=+60s]`) / `frontend/src/App.tsx` (StreamStatus lazy import + `/stream` 라우트 + navItems "시세수신현황" 메뉴 제거) / `frontend/src/api/realtime.ts` (`StreamEntry`/`StreamStatusResponse` 타입 + `getStreamStatus()` 함수 제거). **신규**: `tests/unit/realtime/test_ws_subscribe_backoff.py` 5 케이스(A OPSP0002 후 backoff 등록 + 값 ≈ now+60 / B 유효 시간 내 send skip / C 만료 후 정상 send / D 다른 종목 격리 / E 정상 SUBSCRIBE SUCCESS 후 미진입). **기존 갱신**: `tests/unit/engine/test_scheduler_stop_zombie_tasks.py::test_stop_tuple_equals_finally_tuple_members` 의 `expected_members` 8 → 7 환원 (`_near_signal_task` 제거). 백엔드 1369 → **1345 passed**(-24 = 삭제 28 - 신규 5 + 1 갱신, 회귀 0) / 프론트 152 → **145 passed**(-7 StreamStatus.test.tsx 삭제, 회귀 0). `src/engine/CLAUDE.md` `_near_signal_loop` 절 + `_build_priority_groups` 분기 절 삭제 + 사이클 17 롤백 1행 / `src/realtime/CLAUDE.md` 사이클 17 OPSP0002 backoff 가드 1행 추가 / `src/routes/CLAUDE.md` `/api/realtime/stream-status` 행 삭제 / `frontend/CLAUDE.md` `## StreamStatus (/stream)` 절 삭제 | **2026-05-19 15:15:20 KST `tick_coverage subscribed=3 fresh=0 stale=3 ratio=0.0%` 운영 사고 대응**. Codex PR #14 (`1350aa8`+`84372cb`) 가 malformed payload receive loop 죽음(#0)을 잡았지만 잔여 결함 (#4 NEAR_SIGNAL_MODE 분기, #6 사이클 15-B/C race, #2 OPSP backoff 부재) 해결 필요. 사용자 설계 사상: "simple is best — 단일 데이터 흐름 + 상태 최소화. 신규 모듈 추가 금지". 사이클 15-B/C 의 `stream_pool_manager`/`near_signal_monitor`/`_near_signal_loop` 는 단순화 원칙 위반 + `_scan_loop` + `_stale_watcher_loop` + `_resubscribe_stale_priority` 와 동시에 같은 종목에 promote/demote → KIS OPSP0002 폭주 → 시간당 30+ 회 → tick_coverage 0% 결함의 직접 원인. 본 사이클이 전체 롤백. OPSP0002 차단은 `_handle_raw` 의 60s backoff 단순 흡수 (`_opsp_backoff_until` dict + `subscribe()` 진입 시 검사). 자율 결정: (a) backoff 60s — KIS 정상 패턴 회복까지 충분, `_scan_loop` 5분 주기와 비교해 보수적 / (b) `bypass_limit=True` 는 backoff 검사 skip — HIGH (보유/익일청산) 손절 우선 보장, 매매 안전성 핵심 / (c) `_subscriptions` set 도 add 안 함 — 다음 자연 재시도 시 일관 동작 / (d) DEBUG `[ws_subscribe_backoff]` + INFO `[ws_opsp_backoff]` — DEBUG 는 5분 주기 정상 송신 노이즈 차단 / (e) 사이클 15-A delta-only 보존 — `_scan_loop` + scanner `already_in_pool` 가드 그대로 / (f) Codex `1350aa8`+`84372cb` (handler/websocket try/except 가드 + 4 회귀 가드) 보존 — receive loop 죽음 차단 핵심 / (g) 사이클 16 AES 키 격리 가드 보존 — `is_main` + `_EXECUTION_NOTICE_TR_IDS`. 매매 흐름 영향 0 — `risk.on_tick` / `order_engine.execute_buy/sell` 무변경. NXT 애프터(15:30 이후) 시간대 push 권장. 운영 활성화 절차: push → EC2 자동 배포 → 다음 `_boot` 부터 정상 흐름 회복 (보조 task 7개 + OPSP0002 backoff 가드). 5/19 영향 평가: tick_coverage 80%+ 회복 / OPSP0002 거부 시간당 0~2 / 코드 LOC 변경 0 → -622 / WS 등록 경로 1중 (`_scan_loop` 5분 delta) + K 재발송 안전망 |
| 2026-05-19 | 사이클 16 — 체결통보 AES 키 격리 가드 (사이클 7-C 잠재 결함 fix) | `src/realtime/websocket.py::KisWebSocket.__init__(*, is_main: bool = True)` 인자 추가 + `_EXECUTION_NOTICE_TR_IDS = frozenset({"H0STCNI0", "H0STCNI9"})` 모듈 상수 + `_handle_raw` SUBSCRIBE SUCCESS AES iv/key 저장 분기에 이중 가드: `self.is_main` + `tr_id in _EXECUTION_NOTICE_TR_IDS` 모두 만족 시에만 `set_aes_keys()` 호출. `src/realtime/websocket_pool.py::start()` 가 보조 세션 생성 시 `KisWebSocket(token_manager=manager, is_main=False)` 명시. 자체 인스턴스 변수 `self.aes_iv`/`self.aes_key` 보존 (디버깅용). 회귀 가드: `tests/unit/realtime/test_aes_keys_main_only.py` 6 케이스(is_main default True / False 명시 / 메인+체결통보 저장 / 메인+시세 skip / 보조+시세 skip / 보조+체결통보 이중 안전망). 백엔드 1363 → **1369 passed**(+6 신규, 회귀 0). `src/realtime/CLAUDE.md` 사이클 16 1행 | 사이클 7-C 풀 통합 (메인 + 보조 N 세션) 도입 시점부터 잠재 결함. KIS 명세 (`docs/kis/domestic-stock-realtime.md`): 모든 SUBSCRIBE SUCCESS 응답에 `output.iv`/`output.key` 포함. 같은 connect 내 SUBSCRIBE SUCCESS = 같은 키 (단일 세션 환경 무해). 메인 vs 보조 = 별도 connect = 다른 키 → 보조 시세 SUBSCRIBE SUCCESS 의 AES 키가 메인 체결통보 키 덮어씀 → 체결통보 (H0STCNI0/H0STCNI9) 복호화 실패. 2026-05-19 11:20:23~11:22:31 운영 사고 11건 발견 (당일 매도 trade_history 0건 → 매매 결과 실 영향 0, KIS HTS ID 공유 환경의 다른 계좌 체결통보 push 일 가능성 강함). 잠재 위험: 우리 매도 체결통보 push 와 보조 SUBSCRIBE SUCCESS race 겹치면 복호화 실패 + 누락 → trade_history PENDING 잔존. 사이클 7-B 가 체결통보 *구독* 만 차단(`QuoteSessionExecutionNoticeError`) — AES 키 저장 격리는 누락. 본 사이클이 사이클 7-B "체결통보 메인 단일 강제" 정책의 완전 격리 — 구독 차단 + AES 키 저장 차단 이중 안전망. 자율 결정: (a) 이중 가드 — `is_main` + `tr_id` 둘 다 (각 단독으로도 안전이지만 둘 다 만족 시에만 저장 = 가장 보수적) / (b) `set_aes_keys` 시그니처 무변경 — 호출자(websocket.py) 가 가드 적용, 호환성 100% 보존 / (c) DEBUG `[aes_key_skip]` 로그 — INFO 로 노출하면 5분 주기 시세 SUBSCRIBE SUCCESS 마다 발화해 운영 노이즈. 매매 영향 0 (메인 체결통보 흐름 변경 0). KRX 메인 시간대 push 가능 — 보조 세션 키 덮어쓰기 차단만 |
| 2026-05-19 | 사이클 15-B-2 — scheduler 통합 (`_near_signal_loop` + `near_signal_mode` env 토글) | `src/config.py::Settings.near_signal_mode: bool = False` 신규 — `.env NEAR_SIGNAL_MODE=true` 토글. `src/engine/scheduler.py::_build_priority_groups()` 분기 추가 — `near_signal_mode=True` 시 `momentum`/`breakout` 빈 list (후보 종목 _scan_loop 등록 안 함, `near_signal_monitor` 가 60s 주기로 임박 종목만 promote). `src/engine/scheduler.py::_near_signal_loop()` 신규 ~100 LOC — 60s 주기 task: (1) `scan_stocks()` momentum 후보 + `near_signal_monitor.collect_near_signals(registry)` / (2) `stream_pool_manager.reset_cycle()` + `expire_cooldowns()` / (3) 임박 종목 promote `kis_ws_pool.subscribe(LOW)` + `mark_promoted` / (4) 임박 list 빠진 ws_active 종목 demote `kis_ws_pool.unsubscribe` + `mark_demoted` (positions/next_day_clear 보호) / (5) `[near_signal_loop] ws=N rest=M dropped=K` INFO. `start()` 에서 task 발화 + `__init__` `_near_signal_task=None` + finally + stop() 8번째 task cancel 멤버 추가. 회귀 가드: `tests/unit/engine/test_scheduler_near_signal_integration.py` 5 케이스(mode False 보존 / True 빈 list / positions 보존 / task 속성 / cancel 루프 8 멤버) + 기존 `tests/unit/engine/test_scheduler_stop_zombie_tasks.py::test_stop_tuple_equals_finally_tuple_members` 갱신 (7→8 멤버). 백엔드 1358 → **1363 passed**(+5 신규, 기존 1건 갱신, 회귀 0). `src/engine/CLAUDE.md` _near_signal_loop 절 추가 | 사용자 설계서 11장 11.B (백엔드 모듈 구현) 완성. 자율 결정: (a) env var 토글 — 기본 False 로 사이클 15-A 동작 100% 보존, 운영자가 `.env NEAR_SIGNAL_MODE=true` 갱신 + 재기동 시점 결정. KRX 메인 시간 외 권장 (5/19 push 시 KRX 메인 중이라 기본 false 안전) / (b) `_near_signal_loop` 항상 실행 — mode=False 면 단순 sleep loop. 토글 즉시 활성화 가능 (재시작 불필요는 미지원, 다음 _boot 부터) / (c) 첫 발화 5s 후 — `_scan_loop` race 차단 / (d) 본체 예외 try/except — 다음 사이클 자연 재시도. 매매 영향: `near_signal_mode=True` 활성화 시 후보 종목이 _scan_loop 에서 빠짐 → near_signal_monitor 가 임박 시점 promote 까지 ~60s 갭 — 갭률 큰 폭발 돌파 일부 누락 가능 (수용 가능 — 정상 흐름은 임박 후 매수). 매매 위험 평가: 기본값 False 안전. push 후 운영자가 명시 활성화 시점 결정. 후속 사이클 15-D (필요 시): near_signal_mode DB 토글 (system_config 키) — UI 즉시 토글 |
| 2026-05-19 | 사이클 15-C-2 — 시세수신현황 프론트 페이지 (매매 영향 0) | `frontend/src/api/realtime.ts` 에 `getStreamStatus()` + `StreamEntry` / `StreamStatusResponse` 타입 추가. `frontend/src/pages/StreamStatus.tsx` 신규 ~165 LOC — REST/WS/탈락 3 탭 + 30s 폴링 + 수동 새로고침 + 빈 카테고리/에러 graceful. 컬럼: 종목 / 전략 (한글 라벨) / 사유 / 등록 후 경과 / 남은시간 (ws=min_hold / dropped=cooldown). 탭 라벨에 카운트 `(N)` 표기. `frontend/src/App.tsx` 라우터 `/stream` + navItems "시세수신현황" 메뉴 추가. lazy import 로 초기 번들 분리. 회귀 가드: `frontend/src/pages/__tests__/StreamStatus.test.tsx` 7 케이스 (ws 기본 탭 / REST 탭 / 탈락 탭 cooldown 컬럼 / 빈 카테고리 / 탭 카운트 / 새로고침 refetch / 500 에러). 프론트 145 → **152 passed**(+7 신규, 회귀 0). `frontend/CLAUDE.md` StreamStatus 절 신규 | 사용자 설계서 제6장 (시세수신현황 UI) + 제7장 (가시화) 그대로 구현. 자율 결정: (a) 기본 탭 = `ws` — 운영자가 가장 관심 있는 활성 매수 후보 즉시 노출 / (b) 한글 전략 라벨 매핑 — `STRATEGY_LABELS` dict 로 사용자 보기 친화 / (c) 30s `refetchInterval` — Dashboard `KisAccountPoolCard` 와 동일 주기 (Trading Status 5s 와 별개 queryKey) / (d) lazy import — 초기 번들 분리 (사이클 6 Logs / Recommendations 패턴) / (e) `time` 표시 — secs > 60 시 "M분 S초" / 그 외 "X초" 한글. 사이클 15-B-1 코어 + 15-C-1 endpoint 가 빈 응답 (호출자 없음) 단계라 현재 UI 도 빈 응답 노출. 사이클 15-B-2 (scheduler 통합) 후 실데이터 노출 |
| 2026-05-19 | 사이클 15-C-1 — `GET /api/realtime/stream-status` 엔드포인트 (매매 영향 0) | `src/routes/realtime.py::get_stream_status` 신규 — `stream_pool_manager.snapshot()` 위임 + 호출 직전 `expire_cooldowns()` 자동 전이 (cooldown 만료 → rest_watch 즉시 반영). 응답 스키마 사용자 설계서 제6장: `{rest:[...], ws:[...], dropped:[...]}` 각 `{ticker, strategy, reason, since_secs, ...remaining_secs}`. 회귀 가드: `tests/contract/test_routes_stream_status.py` 5 케이스(빈 / ws_active / rest_watch / cooldown dropped / cooldown 만료 자동 전이). 백엔드 1353 → **1358 passed**(+5 신규, 회귀 0). `src/routes/CLAUDE.md` 1행 추가 | 매매 흐름 영향 0 — 프론트 UI (15-C-2) 가 사용할 단독 endpoint. `stream_pool_manager` 모듈 싱글톤 직접 위임 → DB / KIS API 호출 없음. 운영자 진단 가시화 (REST 관찰 / WS 활성 / cooldown 카테고리). 사이클 15-B-2 (scheduler 통합) 가 stream_pool_manager 를 활성 사용 시 endpoint 가 실데이터 노출. 현 단계 (15-B-1 코어 + 15-C-1 endpoint) 만으로는 빈 응답 (호출자 없음). 자율 결정: (a) 응답 시점 `expire_cooldowns()` 호출 — UI 가 새로고침 시 cooldown 만료 즉시 반영 (별도 task X) / (b) 빈 응답 시 200 OK + 빈 list (404 아님) — 운영자가 풀 비활성 / 활성 구분 가능 / (c) `/api/realtime/stream-status` URL — 기존 `/api/realtime/subscriptions` 와 별개 카테고리 (subscriptions=풀 슬롯, stream-status=종목별 상태) |
| 2026-05-19 | 사이클 15-B-1 — REST+WS 혼합 풀 매니저 (코어 모듈 단독, 통합 X) | `src/engine/stream_pool_manager.py` 신규 ~240 LOC — `StreamPoolManager` 클래스 + 모듈 싱글톤 `stream_pool_manager`. 상태 모델 `rest_watch` / `ws_active` / `cooldown` + `StreamSlot` dataclass + 안정성 정책 (`MIN_WS_HOLD_SECS=180s` / `WS_REJOIN_COOLDOWN_SECS=300s` / `MAX_PROMOTE_PER_CYCLE=5` / `MAX_DEMOTE_PER_CYCLE=5`) + 우선순위 8 단계 (positions > next_day_clear > donchian_swing > momentum > VB > LTV > BFB > VCP). API: `mark_promoted/mark_demoted/mark_rest_watch/expire_cooldowns/can_promote/can_demote/reset_cycle/snapshot/get_state/get_slot/list_by_state`. positions/next_day_clear `is_protected_from_demote=True`. `time.monotonic()` 주입 가능 (테스트). `src/engine/near_signal_monitor.py` 신규 ~200 LOC — 5 전략 임박 판단 함수: `calc_distance_pct(target, current)` / `extract_target_price(strategy_id, info)` / `check_breakout_strategy` (VB/LTV/BFB/VCP 4종, 0.3%/0.4% distance) / `check_momentum_strategy` (change_rate >= 28.5%) / `check_donchian_strategy` (시간 기반 09:05~09:30) / `collect_near_signals(registry, ...)` 통합 worker. 시간 윈도우: BFB 09:05~13:00 / VCP 09:05~14:30 / DONCHIAN 09:05~09:30. 회귀 가드: `tests/unit/engine/test_stream_pool_manager.py` 11 케이스(promote/demote 전이 / cooldown / protected / 배치 제한 / snapshot / reset) + `tests/unit/engine/test_near_signal_monitor.py` 12 케이스(calc_distance_pct / extract_target_price 3종 / breakout 식별 / 거리 미달 skip / 시간 윈도우 / momentum / donchian 2종). 백엔드 1330 → **1353 passed**(+23 신규, 회귀 0). 영향 인덱스 86 모듈 / 170 테스트 | **매매 흐름 영향 0** — 단독 모듈만, scheduler 통합 X (15-B-2 후속). 사용자 설계서 제5장 (임박 기준 6 전략) + 제4장 (안정성 정책) + 제3장 (상태 모델 3 단계) 그대로 구현. 자율 결정: (a) 작업 분할 — 15-B-1 (코어, 매매 영향 0) + 15-B-2 (scheduler 통합, 매매 영향 큼) — 사용자 자리 비움 + KRX 메인 시간대 (10:46 push) 안전 우선 / (b) `MIN_WS_HOLD_SECS=180s` 사용자 설계서 그대로 / (c) `time.monotonic()` 주입 — fake_clock 테스트 가능 (사이클 11 `buy_block_state` 캐시 패턴 재사용) / (d) `extract_target_price` 5 전략 분기 — VB/LTV (boards.main 우선), BFB (flag_high), VCP/donchian (target_price 직접), momentum (별도 — change_rate 사용). 후속 사이클 15-B-2 (scheduler 통합 + `_scan_loop` 옵션 B): near_signal_monitor task 60s 주기 시작 + priority_groups 변경 (후보 종목 제외, 보유/익일청산만) + risk.on_tick 영향 평가. 후속 사이클 15-C (시세수신현황 UI): `/api/realtime/stream-status` + 프론트 메뉴 |
| 2026-05-19 | 사이클 15-A — KIS 정상 패턴 준수 (이벤트 기반 시세 구독, 운영 시세 회복) | `src/engine/scheduler.py::_scan_loop()` 의 `unsubscribe_all() + subscribe_filtered_stocks()` 전체 재구독 → `_delta_unsubscribe_dropped(new_set)` (빠진 종목만 unsubscribe) + 기존 subscribe 호출 유지. 신규 메서드 `_delta_unsubscribe_dropped(new_set)` ~50 LOC. `OrderEngine.__init__` 직후 scheduler 가 `_pending_next_day_clear_provider` lambda 주입 — `_handle_sell_fill` 전량 체결 분기 끝에 `_unsubscribe_if_no_other_strategy(ticker)` 신규 헬퍼 ~40 LOC 호출 (a) `registry.is_ticker_held_by_any` False + (b) `_pending_next_day_clear` 부재 + (c) 모든 전략 `get_scanned_tickers()` 부재 통과 시 `kis_ws_pool.unsubscribe(TICK_TR_ID, ticker)`. `src/engine/scanner.py::subscribe_filtered_stocks` LOW 분기 + breakout overflow 분기에 `already_in_pool = set(kis_ws_pool.get_subscribed_tickers())` 가드 — 이미 풀에 구독 중인 LOW 종목은 SEND skip (HIGH 종목 = positions/next_day_clear 는 always 호출, promote 보존). 회귀 가드: `tests/unit/engine/test_scan_loop_delta_only.py` 5 케이스(LOW skip / 신규 LOW SEND / delta_remove / HIGH always / HIGH 신규) + `tests/unit/engine/test_order_engine_unsubscribe_on_sell.py` 4 케이스(다른 전략 X → unsubscribe / 다른 전략 보유 / pending_next_day_clear / 다른 전략 _scanned_tickers). 백엔드 1280 → **1330 passed**(+9 신규 + 기존 41 회복, 회귀 0). `src/engine/CLAUDE.md` `_scan_loop` + 체결 후처리 절 갱신 | KIS 공지 (2026-05-18): "비정상 케이스 2 — 데이터 구독 후 데이터 수신여부 검증 없이 무한 등록/해제 반복 처리" → IP/앱키 일시 차단 가능. 우리 시스템이 정확히 이 패턴(매 5분 ~48 종목 전체 unsubscribe + 재구독, 시간당 ~1,200 메시지) → KIS 측 silent inactive 발화 의심. 2026-05-19 운영자 보고: VB 079550 LIG넥스원 09:14 돌파 신호 누락 (09:00:21 시가 확정 15/24=37.5% 미달 + 매 5분 unsubscribe/resubscribe 영향). fix 후 매 5분 KIS 측 등록/해제 메시지: 전체 ~96건 → 변경분 (5~10건 미만) — 시간당 ~1,200 → ~50건 미만 (96% 감소). KIS 정상 패턴 "연결>구독>데이터수신>불필요종목 구독해제>연결종료" 준수. 매도 체결 후 즉시 unsubscribe 로 5~10분 영구 잔존 결함 차단. 자율 결정: (a) HIGH 종목은 always 호출 — 풀 promote (보조→메인) 보존 / (b) `_pending_next_day_clear_provider` lambda 주입 — OrderEngine ↔ scheduler 결합도 최소화 / (c) `_unsubscribe_if_no_other_strategy` 3 가드 — 다른 전략 보유/익일청산/스캔 후보 통과 시만 / (d) 매도 체결 hook 만 추가 — `_drain_pending_next_day_clear` / `_force_clear_main_only` 청산도 결국 `_handle_sell_fill` 거치므로 단일 hook 으로 모두 커버. 매매 코드 침범 최소 — `place_order` / `cancel_order` 발사 경로 무변경. 사이클 9/13/14-A/14-C 안전망 보존 (K stale watcher / `_resubscribe_stale_priority` / ACK 정합성). 5/19 KRX 메인 시간대 외 (15:30 이후 또는 익일 boot 전) push 권장. 후속 사이클 15-B (REST+WS 혼합 풀 매니저) + 15-C (시세수신현황 UI) 는 별도 분리 |
| 2026-05-18 | 사이클 14-E — 컨테이너 재시작 빈발 진단 (코드 변경 없음, 운영 가이드만) | supabase MCP 로 5/18 6시간 `[pool_start]` 발생 시각 6회 추적 + git log 시각 매칭. 결과: **컨테이너 재시작 4회(13:13 / 13:23 / 14:11 / 14:44) 모두 GitHub Actions 자동 배포 트리거 정상 동작**. push 시각 → pool_start 지연 1~5분 (EC2 git pull + docker compose up --build -d). 운영 장애 아님. 재시작 직전 `[kis_quote_accounts] list 실패` 28회 ERROR 는 supabase HTTP/2 keep-alive 가 컨테이너 종료 시 끊김 → 재시작 후 첫 호출 stale connection 결함의 정상 trace, 이미 사이클 14-D 의 60s TTL 캐시로 차단. `docs/HARNESS_CHANGELOG.md` 1행 추가, 코드 무변경, 회귀 가드 무변경 | 운영 가이드 (사이클 14-E 결과물): (1) KRX 메인 시간(09:00~15:30) 중 빈번한 push 자제 — `_scan_loop` 5분 race 가능 + 매수 평가 흐름 일시 중단 / (2) NXT 애프터(15:30~20:00) 또는 익일 07:50 `_boot` 전 push 권장 / (3) 사이클 14-A(가시성) + 14-D(캐시) + 14-C(ACK 정합성) 배포 후엔 재시작 race 영향 최소화 — `[tick_coverage]` 풀 실측치 + `[kis_quote_accounts] list 실패` 0~1회 + `_subscriptions vs _acked` 격차 자동 차단. 사이클 14-E task 종결 — 별도 후속 사이클 불필요 |
| 2026-05-18 | 사이클 14-C — `_subscriptions` 정합성 가드 (in-flight ACK race 차단, 운영 진단 중 발견) | `src/realtime/websocket.py::_handle_raw` SUBSCRIBE SUCCESS 분기에 `(tr_id, tr_key) in self._subscriptions` 가드 1줄 추가 + DEBUG `[ws_ack_orphan]` 로그. 정상 흐름(subscribe→SEND→KIS ACK) 영향 0, orphan ACK (unsubscribe 직후 도착한 SUBSCRIBE SUCCESS in-flight) 만 `_subscriptions_acked.add` 차단. 회귀 가드: `tests/unit/realtime/test_ack_orphan_guard.py` 3 신규 케이스(orphan ACK 차단 / 정상 ACK 보존 / orphan + AES 저장 분기 보존) + 기존 `test_case_n6_subscribe_success_regression` 의 `_subscriptions.add` 선행 추가(정상 흐름 명시화). 백엔드 1277 → **1280 passed**(+3 신규, 회귀 0) | 2026-05-18 KST ~15:01 운영 endpoint 응답 `GET /api/realtime/subscriptions` 의 `sessions[main].subscribed=0 / acked=30` 격차 race 진단 결과 확정. 진짜 race 시나리오: T0(`subscribe`→SEND) → T1(`_scan_loop` 5분 주기 `unsubscribe_all` → `unsubscribe`→`_subscriptions.discard`+`_subscriptions_acked.discard`) → T2(KIS 가 T0 SEND 의 SUBSCRIBE SUCCESS ACK 지연 도착) → T3(기존 코드: 무조건 `_subscriptions_acked.add` → 격차). fix 후: orphan ACK 는 `_subscriptions` 부재 → 무시. KIS 측은 unsubscribe 처리 → 격차 자연 차단. AES iv/key 저장 분기는 단일 글로벌이라 정합성 가드 무관 (별도 분기 보존). 자율 결정: (a) 가드 위치 — `_handle_raw` 안 SUBSCRIBE SUCCESS 분기 1줄(가장 좁은 fix point) / (b) 로그 레벨 DEBUG — 정상 race 케이스 (5분마다 수회) 라 INFO/WARNING 노이즈 차단, 운영자 진단 시 로그 레벨 변경 / (c) 회귀 N6 케이스 갱신 — fixture 명시 `_subscriptions.add` 선행 추가, 의도된 동작 정합화. 자금 안전: 매매/시세/체결 코드 침범 0. 본 fix 는 운영 가시성 (`_acked` 카운트 정확성) 개선이며 매매 결과 영향 0 |
| 2026-05-18 | 사이클 14-D — `list_accounts` 60s TTL 메모리 캐시 (운영 안정성, 사이클 14-A 후속) | `src/db/kis_quote_accounts.py` 모듈 전역 `_LIST_CACHE_TTL=60.0` + `_list_cache` / `_list_cache_expires_at` dict + `invalidate_list_cache()` 함수 + `import time` 추가. `list_accounts(active_only)` 진입에 `time.monotonic()` TTL 비교 → fresh hit 분기 (DB 호출 0). 만료/cache miss 분기에서만 DB fetch + 성공 시 캐시 저장. DB 예외 + 캐시 있음 → stale 반환 (graceful), 캐시 없음 → 빈 리스트 (사이클 7-A 회귀 보존). `insert_account` / `update_account` / `delete_account` 끝에서 `invalidate_list_cache()` 호출 — 운영 토글 즉시 반영. 회귀 가드: `tests/unit/db/test_kis_quote_accounts_cache.py` 7 신규 케이스(TTL fresh / TTL 만료 / invalidate 명시 / 첫 호출 / DB 예외 + stale / DB 예외 + 빈 / active_only 키 분리). 백엔드 1270 → **1277 passed**(+7, 회귀 0). `src/db/CLAUDE.md` 1 행 갱신 | 2026-05-18 KST 13:13~14:44 운영 사고: system_logs `[kis_quote_accounts] list 실패` ERROR 28회/시간 누적. 호출처 2곳 — (1) `routes/kis_quote_accounts.py:42` Settings/Dashboard 폴링 30s (`KisQuoteAccountsCard` + `KisAccountPoolCard`), (2) `realtime/websocket_pool.py:112` `_pool_start` boot 1회. 컨테이너 재시작 race + supabase HTTP/2 stale connection 결함 동시 발화로 DB 호출 다발 실패. graceful 폴백 `[]` 동작이라 매매 안전성 영향 0 이지만 ERROR 로그 + DB 부하. 사이클 11 `MarketRegime.get_buy_block_state()` 60s TTL 캐시 패턴 100% 동일 적용. 자율 결정: (a) 모듈 전역 캐시 — `MarketRegime` 인스턴스 캐시와 달리 `list_accounts` 가 모듈 함수라 인스턴스 슬롯 없음. dict 분리 (active_only=True/False) / (b) DB 예외 + stale 반환 정책 — 운영 안정성 (Settings UI 가 화면 빈 list 표시되지 않음). 매매 안전과 무관 (시세 풀 분배는 boot 시점 1회 결과 사용) / (c) INSERT/UPDATE/DELETE 직후 명시 invalidate — TTL 60s 기다리지 않고 운영자 변경 즉시 반영. 5/18 운영 즉시 영향: 다음 EC2 배포부터 분당 list 호출 ~2회 → 1회 미만, ERROR 28회/시간 → ~0 예상. 매매/시세/체결 코드 침범 0 |
| 2026-05-18 | 사이클 14-A hot fix — `_report_tick_coverage()` 풀 통합 (사이클 11 짝궁 누락 fix, 운영 진단 중 발견) | `src/engine/scheduler.py::_report_tick_coverage()` source `from src.realtime.websocket import kis_ws` + `kis_ws.get_subscribed_tickers()` → `from src.realtime.websocket_pool import kis_ws_pool` + `kis_ws_pool.get_subscribed_tickers()` (사이클 14-A 코멘트 3줄 추가). 사이클 11 에서 `scanner.get_scan_status()` 만 풀 통합되고 짝궁 메서드 `_report_tick_coverage()` 가 함께 갱신되지 않은 누락 결함 — 2026-05-18 KST 12:57~14:25 운영 사고에서 풀 25 종목 구독 (`[stale_watcher] subscribed=25 stale=18`) 중인데 system_logs `[tick_coverage] subscribed=0 fresh=0 stale=0` 출력 → 운영자가 "전부 끊김"이라고 오인. fix 후 풀 실측치 정상 노출. 회귀 가드: `tests/unit/engine/test_report_tick_coverage_pool.py` 3 신규 케이스(A 풀 합집합 25 → subscribed=25 / B 메인 0+보조 25 운영 사고 재현 → subscribed=25 / C 풀 0 회귀 → subscribed=0) + 기존 5 케이스(`test_tick_coverage.py` 3 + `test_observability_logs.py` 2) `patch.object(kis_ws, ...)` → `patch.object(kis_ws_pool, ...)` mock target 동기 갱신. 백엔드 1267 → **1270 passed**(+3 신규, 회귀 0). 매매 코드(`order_engine.py`/`risk.py::on_tick`/전략 파일) 침범 0, 사이클 11 / 13-E 인프라 보존, 응답 로그 포맷/임계/system_logs prefix 100% 보존. 자율 결정: (a) `_resubscribe_stale_priority` source(`scanner.ticker_last_tick`) 와 본 함수 source(`kis_ws_pool.get_subscribed_tickers()`) 분리 — `_resubscribe_stale_priority` 가 보유 last_tick 기반 stale 종목 회복하던 path 와 독립이라 풀 통합 후에도 영향 0 / (b) 기존 5 케이스는 mock target 만 갱신(기능 변경 없음) — `kis_ws_pool` 의 내부 구현이 메인+보조 합집합이므로 `kis_ws.get_subscribed_tickers` 단독 mock 으로는 새 source 잡지 못해 fail. mock target `kis_ws_pool.get_subscribed_tickers` 로 갱신 시 회귀 0. KRX 메인 시간대 배포 가능 — 본 함수는 5분 주기 로그 함수, 매매 평가 경로와 분리 | (1) 운영자 가시성 회복 — 풀 25 / 메인 0 인 운영 상태(보조 ISA 세션 1개에 모든 LOW 종목 집중)에서 `[tick_coverage] subscribed=0` 표시되어 "전부 끊김"이라고 오인하던 결함. fix 후 풀 실측치 노출 / (2) 후속 사이클 14-B 진단 (ISA 보조 silent inactive 72% 의 진짜 원인 — B1 ACK 미수신 / B2 KIS 측 silent / B3 메인 ws disconnect 가설 좁히기) 의 base — 풀 실측치가 정확하게 보여야 `_resubscribe_stale_priority` 효과 측정 가능 / (3) ScanMonitor 측 현재가 수신 문제는 본 사이클로 직접 해결 안 됨(silent inactive 가 진짜 원인) — 사이클 14-B 후속. 본 사이클은 가시성 fix + 후속 진단 base. plan: `/Users/koscom/.claude/plans/hashed-munching-eagle.md` 사이클 14 Phase 1 |
| 2026-05-18 | 자문 시스템 개선 사이클 9 — KIS 차단 회피 안전망 (stale watcher 트래픽 완화 + `QuoteSessionHealthMonitor` 자동 비활성 + `base.py::_request_via_quote_pool` 통합 + `WebsocketPool.disable_quote_session` 신규, TDD, 운영 매매 흐름 무관) | `src/engine/scheduler.py` 상수 2 변경(`STALE_WATCHER_INTERVAL_SECS` 30 → **120** / `STALE_FORCE_REREGISTER_AFTER` 3 → **10** / `STALE_FRESHNESS_SECS=60` 보존 — 5/12 사고 대응 의도. 분기 자동 확장: 11~20회 force, 21회 초과 skip), `src/services/quote_session_health.py` 신규 ~220 LOC(`QuoteSessionHealthMonitor` 싱글톤 + `health_monitor` 모듈 변수 — `_consecutive_failures` / `_window_total` / `_window_failures` / `_window_start` / `_disabled_labels` + `asyncio.Lock` / `MAX_CONSECUTIVE_FAILURES=5` + `WINDOW_SECS=300` + `MAX_FAILURE_RATE=0.5` + `MIN_CALLS_FOR_RATE=10` / `record_success(label)` consecutive 0 reset + `_evaluate_rate_only` 호출(누적 비율 임계 race 차단) / `record_failure(label, reason)` consecutive +=1 + `_evaluate` 호출 / 메인 라벨 "main" 추적 자체 skip — 안전 가드 / `_auto_disable(label, reason)` (1) `[quote_session_disabled]` 영구 ERROR 우선 (2) `_pool_disable_quote_session(label)` 풀 제거 (3) `_db_get_by_label` + `_db_update_account(active=False)` DB / DB 실패 graceful — `[quote_session_health_db_fail]` WARNING + 메모리 비활성은 보존 / `_disabled_labels` set 가드 두 번째 호출 idempotent / 외부 의존 wrapper 함수 4종(`_db_get_by_label`/`_db_update_account`/`_pool_disable_quote_session`/`_write_system_log`)로 monkeypatch point 노출), `src/api/base.py::_request_via_quote_pool` 통합 3 지점(보조 매니저 발급 실패 → `record_failure(label, "token_issue_fail")` / 5xx HTTPStatusError + `actual_label != "main"` → `record_failure(label, f"http_{status}")` / 성공 `rt_cd=="0"` + `actual_label != "main"` → `record_success(label)` / 네트워크 에러 + KIS rt_cd!=0 비즈니스 거부는 기록 안 함 — 헬스와 무관), `src/realtime/websocket_pool.py::disable_quote_session(label)` 신규 ~50 LOC("quote-N" 라벨 → 1-based index 변환 → 해당 세션 `disconnect()` 예외 swallow + `_quotes` 제거 + `_ticker_to_session` 담당 ticker 정리 + `_round_robin_idx=0` reset / 메인 라벨 "main" noop / 없는 label / 형식 불일치 / 두 번째 호출 모두 noop 안전 가드 / `[pool_disable]` INFO 1행), `tests/unit/engine/test_stale_watcher_thresholds.py` 7 케이스 신규(A INTERVAL=120 / B FORCE_AFTER=10 / B' FRESHNESS=60 보존 / C 11회 stale → force(`unsubscribe_in_pool`+`subscribe(HIGH,bypass)`) / D 10회까지 resend만 / E fresh 회복 retry clear / F 분당 트래픽 250 미만 + 임계 분기 후 30 미만), `tests/unit/services/test_quote_session_health.py` 10 케이스 신규(A 토큰 발급 실패 consecutive +=1 / B 5xx +=1 / C 5회 → `_auto_disable` 발화 / D 성공 0 reset / E 5분 window 58% rate → 비활성 / F window 만료 reset / G min_calls 미달 skip / H 풀 헬퍼 호출 / I 영구 로그 / J DB 실패 graceful), `tests/unit/realtime/test_websocket_pool_disable.py` 5 케이스 신규(A quote-1 제거 / B ticker 정리 / C 없는 label noop / D 메인 label noop / E idempotent), `tests/contract/test_quote_session_auto_disable.py` 4 케이스 신규(A 5xx 5회 → 전체 비활성 / B TFR 60% → 자동 비활성 / C 메인 라벨 안전 가드 / D idempotent), `tests/integration/test_stale_watcher.py` + `tests/integration/test_stale_watcher_pool.py` 임계 가정 동기 갱신(3→10, 6→20 케이스 5건), `src/engine/CLAUDE.md` K 절 사이클 9 갱신 + `src/api/CLAUDE.md` quote_pool 절 health_monitor 통합 추가 + `_workspace/00_leader_trading_rules.md` 사이클 9 섹션 신규(4 항목 변경 + 운영자 가이드 + 영구 로그 추적 SQL) + `docs/kis/rate-limits.md` 6절 KIS 차단 회피 안전망 신규 | **운영 매매 흐름 무관** — WebSocket / 시세 REST 안전망만 보강. 매수/매도/체결통보/잔고 코드 경로 변경 0. K stale watcher 5/12 운영 사고(fresh=1/stale=26) 대응 의도 보존(FRESHNESS=60 그대로). 자율 결정: (1) `quote_session_health` 위치 — `src/services/` 채택(DB + WebsocketPool cross-cutting 모니터, api/realtime/ 어느 곳에도 비대칭, 기존 `dkstock_client`/`mcp_client` 와 같은 인프라성 모듈군). (2) 호출 통합 지점 3개 — 보조 매니저 발급 실패 / 5xx HTTPStatusError / 성공 응답. 네트워크 에러는 클라이언트 측 이슈 가능해 추적 제외. KIS rt_cd!=0 비즈니스 거부도 헬스와 무관해 제외. (3) success 시점 sliding window 평가(`_evaluate_rate_only`) 추가 — failure 누적 후 success 가 와도 누적 비율 임계는 발화해야 늦은 결정 race 차단. consecutive 임계는 record_failure 전용. (4) `disable_quote_session` label → 1-based index 변환 — 기존 `_session_label` 규약("quote-N") 과 일관. 메인 라벨은 무조건 noop. (5) DB 실패 graceful — `[quote_session_health_db_fail]` WARNING + 메모리 비활성은 그대로(트래픽 차단 우선). (6) 운영자 수동 재활성 — Settings UI active=true 토글 → **다음 영업일** _boot 부터 풀 재참여(당일 즉시 재참여 미지원 — 보안: 동일 토큰 패턴 차단 + 운영자 진단 시간 확보). 백엔드 1245 passed(1221→+24: stale watcher 7 + health 10 + pool disable 5 + contract 4. 통합 임계 가정 갱신은 신규로 카운트 안 함) / 2 skipped 그대로 / 프론트 영향 없음. 영향 인덱스 갱신(백엔드 모듈 84·테스트 146). 5/18 _boot(07:50) + 사이클 9 운영 효과: (a) stale watcher 트래픽 분당 800 → 200 미만 회복(보조 5개 가정) + 실제 임계 분기로 1/10 이하 → KIS 비정상 케이스 2(무한 등록/해제) 직접 매핑 차단. (b) 보조 세션 1개가 토큰 발급 실패/5xx 지속 시 5회 또는 5분 50% 실패율 감지 즉시 자동 비활성 → 나머지 4개로 자연 라운드로빈 + 풀 전체 안정성 회복. (c) 메인 라벨 "main" 안전 가드로 절대 비활성 안 됨 → 매매 흐름(체결통보/잔고) 차단 결함 0. 운영자 사용 가이드: (1) 자동 비활성 발생 시 `system_logs WHERE message LIKE '[quote_session_disabled]%'` 로 사유 확인 / (2) 외부 원인 진단 (KIS 측 장애 / 토큰 만료 / 계정 issue) / (3) Settings UI 에서 active=true 토글 / (4) 다음 영업일 07:50 자동 재참여 또는 긴급 시 컨테이너 재기동. 후속 권장: (a) Dashboard "KIS 시세 풀" 카드(사이클 7-D)에 health 메트릭 노출 별도 사이클 / (b) 운영자가 `[quote_session_disabled]` 빈도 모니터링 후 임계(5회 / 50%) DB 토글 가변화 별도 사이클 |
| 2026-05-18 | 사이클 11 — UI 시세 카운트 풀 전체 가시화 + `buy_block_state` 60s TTL 캐시 (TDD, 운영 매매 흐름 무관) | `src/engine/scanner.py::get_scan_status()` source 4 변수(`subscribed_set`/`acked_set`/응답 키) 메인 단독 → `kis_ws_pool.get_subscribed_tickers()` / `get_acked_tickers()` 풀 합집합 위임. 응답 키 100% 보존. `src/engine/market_regime.py` `import time` + `BUY_BLOCK_CACHE_TTL=60.0` 상수 + `MarketRegime` dataclass `_buy_block_cache` / `_buy_block_cache_expires_at` 필드(`compare=False, repr=False`) + `invalidate_buy_block_cache()` 메서드 + `get_buy_block_state()` 진입 시 `time.monotonic()` 캐시 hit 분기 + 만료/DB-ok 케이스만 캐시 저장(폴백 분기 영구 오염 차단). `src/routes/system_integrations.py::set_buy_block` PUT 응답 끝에 `get_current_regime().invalidate_buy_block_cache()` graceful 호출 — 운영 토글 즉시 반영. `tests/unit/realtime/test_websocket_pool_subscribed_tickers.py` 5 케이스(메인 only / 보조 only / 합집합 / dedupe / SEND-vs-ACK 분리) + `tests/unit/engine/test_scanner_pool_count.py` 4 케이스(풀 전체 / 메인 only 회귀 / fresh-stale 분류 / 응답 키 회귀) + `tests/unit/engine/test_market_regime_buy_block_cache.py` 6 케이스(TTL fresh / TTL 만료 / invalidate / 첫 호출 / 일관성 / DB 예외 폴백 미저장). `src/engine/CLAUDE.md` market_regime/scanner 절 1 행씩 추가 + `src/realtime/CLAUDE.md` 사이클 11 절 신규 | 2026-05-18 09:00 KRX 진입 운영 점검 중 2 결함 확인 — (A1) 사이클 7-C 풀 통합 후 보조 세션(quote-1, ISA 계좌) 31 종목 구독 중인데 ScanMonitor `subscribed_count=0` 표시 (`scanner.get_scan_status()` 가 `kis_ws._subscriptions` 메인 단독만 카운트하던 결함). 운영자가 "조건검색현황에 현재가가 표시가 되지 않고 있어" 라고 인지한 실 원인. (D) `risk.on_tick` 매수 신호 평가 직전 `get_buy_block_state()` 호출이 매번 supabase 5 키 fetch → 분당 ~1,800 DB 쿼리. supabase-py HTTP/2 stale connection 으로 매번 ERROR 로그 누적 (graceful 폴백 동작이라 매매 영향 0, DB 부하만). 본 사이클은 두 결함 단일 PR 안전 fix — (A1) scanner 가 풀 헬퍼 위임으로 보조 종목 가시화, (D) 인스턴스 캐시 60s TTL 로 DB 쿼리 180배 감소 + 운영자 PUT 시점 즉시 무효화. 매매 코드 무수정(`order_engine.py` / `risk.py::on_tick` 변경 0, `get_buy_block_state()` 인터페이스 동일). 백엔드 1263 passed(1248→+15) 회귀 0건. 운영 즉시 영향 — 다음 EC2 배포부터 적용 |
| 2026-05-18 | 사이클 13-E hot fix — K stale watcher 진입점 풀 통합 (운영 진단 중 발견) | `src/engine/scheduler.py::_check_and_resubscribe_stale()` 진입점 `subscribed = kis_ws.get_subscribed_tickers()` (메인 단독) → `subscribed = kis_ws_pool.get_subscribed_tickers()` (풀 전체). 사이클 7-C 풀 통합 시 진입점 갱신 누락 결함 — 보유 0 + 시세는 보조 quote-1(ISA) 에 집중된 운영 상황(2026-05-18 13:52, total=26/fresh=8/stale=18)에서 메인이 빈 채라 `if not subscribed: return` 으로 K watcher 본체 영원히 발동 안 하던 결함. 사이클 13 B(`STALE_FORCE_REREGISTER_AFTER 10→5`) 실효성 회복 + 005930 등 KIS WS silent inactive 종목 강제 재등록 경로 정상화. 회귀 가드: `tests/integration/test_stale_watcher_pool.py` D-6 신규(메인 비어있어도 풀 종목 stale 처리 — 사이클 13-E 핵심 결함 회귀 가드) + 기존 5 케이스 monkeypatch 풀 헬퍼로 갱신 + `tests/unit/engine/test_stale_watcher_thresholds.py` `pool_mock.get_subscribed_tickers` 추가(3 케이스 보호) + `tests/integration/test_stale_watcher.py` `_patch_kis_ws` 헬퍼 풀 갱신. 백엔드 1266 → **1267 passed**(+1 신규 D-6, 회귀 0). 운영 관측: 컨테이너 시작 후 30분 stale 71% 정체 + `[stale_watcher]` 로그 0건 → fix 후 120s 주기로 발동 + 5회×120s=10분 stale 누적 후 `unsubscribe_in_pool + subscribe(HIGH, bypass)` 강제 재등록 = KIS 슬롯 리셋 발화 기대. 매매 코드(`order_engine.py`/`risk.py::on_tick`/전략 파일) 침범 0, 사이클 9 KIS 차단 회피 정책 보존(임계 분기 그대로), 사이클 13 B+D 실효성 회복. 자율 결정: (a) 헬퍼 위치는 `_check_and_resubscribe_stale` 진입점 1줄 변경(2192~2195) — 본체 내부 풀 위임은 사이클 7-C 에서 이미 적용 / (b) 회귀 가드는 D-6 신규 케이스로 "메인 empty + 풀 non-empty" 회귀 차단 — 기존 5 케이스는 fix 후에도 작동 확인용 monkeypatch 갱신 / (c) `pool_mock.get_subscribed_tickers = lambda: list(sch_mod.kis_ws.get_subscribed_tickers())` 패턴 — 기존 5 케이스의 메인 mock 을 풀이 그대로 위임받도록 |
| 2026-05-18 | 사이클 13-D 보충 — `_scan_loop` stale 우선 재구독 (TDD, 운영 매매 흐름 무관) | `src/engine/scheduler.py::_resubscribe_stale_priority(cap=10)` 헬퍼 신규 — `scanner.ticker_last_tick` 기준 `> STALE_FRESHNESS_SECS`(60s) 종목을 결정적 정렬 후 상위 10개에 `kis_ws_pool.subscribe(priority='HIGH', bypass_limit=True)` 즉시 재구독 (50ms sleep, 종목별 예외 격리). `_scan_loop` 가 통합 구독 직후 `_report_tick_coverage()` 직전에 try/except 로 호출 — 본체 예외 흡수해 다음 사이클 자연 재시도. INFO 로그 `[stale_priority_resubscribe] count=N tickers=[...]` + `system_logs` fire-and-forget. 회귀 가드: `tests/unit/engine/test_scan_loop_stale_priority.py` 3 케이스(C-A stale 5 + fresh 3 → stale 5만 호출 / C-B 전체 fresh → skip / C-C stale 15 → cap=10). `src/engine/CLAUDE.md` K 절 사이클 13-D 1문장 보강 | 사이클 13 직전 커밋(`5a9f5c2`)이 A(BFB/VCP 가시화) + B(K 임계 단축) 만 반영하고 D 누락된 상태로 push. D 추가 동기: K watcher 가 120s 주기로 발화하고 `_scan_loop` 는 300s 주기라 WS silent inactive 회복 시간이 최대 120~600s 까지 늘어진다. `_scan_loop` 통합 구독 직후 1행 추가로 **5분 주기의 별도 자연 회복 경로** 생성. cap=10 + 50ms sleep 으로 KIS Rate Limit 보호 (초당 ~20 req 한도의 10% 미만). 매매 코드(`order_engine.py`/`risk.py::on_tick`/전략 파일) 침범 0, 사이클 9 임계 보존, 사이클 11 last_tick 패턴 재사용(`risk.on_tick` 단일 진입점이 메인/보조 모두 갱신 → scanner 글로벌 dict 가 풀 합집합). 백엔드 1263 → **1266 passed**(2 skipped, +3 신규 케이스). 자율 결정: (a) 명세의 "또는 `scanner.ticker_last_tick` 풀 전체 합집합 사용" 옵션 채택 — `kis_ws_pool.get_ticker_last_tick_merged()` 헬퍼는 실제로 풀에 미존재이며, 풀 통합 후 last_tick 글로벌 dict 가 메인/보조 통합 갱신이라 추가 헬퍼 불필요 / (b) 헬퍼 위치는 `_check_and_resubscribe_stale` 다음(stale 도메인 그룹 일관성) / (c) 호출 분기는 `_report_tick_coverage` 직전(통합 구독 직후 시세 미수신 종목 즉시 회복 의도 유지) / (d) HIGH + bypass_limit=True 보유 종목과 동일 우선순위 — `_subscriptions` 한도 영향 0. 운영 즉시 영향: 다음 EC2 배포부터 `_scan_loop` 마다 시세 무수신 종목 자동 재구독 1행 노출 |
| 2026-05-18 | 사이클 13 — BFB/VCP ScanMonitor 가시화 + stale 회복 강화 (TDD, 운영 매매 흐름 무관) | `frontend/src/components/ScanMonitor.tsx::BREAKOUT_KEYS` 4종 확장(`bull_flag_breakout`/`vcp_breakout` 추가) + `BREAKOUT_LABELS` "눌림목 돌파"/"VCP 변동성 수축" 라벨 추가 — BFB/VCP 도 `isBreakout` 분기로 운영시간 안내 + 타겟 가격 테이블 재사용 (둘 다 MAIN only 단일 보드). `src/engine/scheduler.py::STALE_FORCE_REREGISTER_AFTER` 10 → **5** 단축 (사이클 9: 3 → 10; 사이클 13: 10 → 5). `STALE_WATCHER_INTERVAL_SECS=120` / `STALE_FRESHNESS_SECS=60` 보존. 분기 자동 축소: 6~10회 force, 11회 초과 skip. `frontend/src/components/__tests__/ScanMonitor.bfb_vcp.test.tsx` 3 케이스 신규(C13-A 전체 탭 카운트 카드 / C13-B BFB 타겟 가격 테이블 / C13-C VCP 타겟 가격 테이블). `tests/unit/engine/test_stale_watcher_thresholds.py` 임계 가정 의미 갱신(10→5, 케이스 수 7 보존). `tests/integration/test_stale_watcher.py` + `tests/integration/test_stale_watcher_pool.py` 임계 가정 동기 갱신 5건. `src/engine/CLAUDE.md` K 절 사이클 13 갱신 + `frontend/CLAUDE.md` ScanMonitor BFB/VCP 1행 + `_workspace/00_leader_trading_rules.md` 사이클 13 섹션 신규 | (1) BFB/VCP 운영자 가시성 결함 회복 — 2 신규 전략(2026-05-15) 이 백엔드 등록됐는데 ScanMonitor UI 에 BREAKOUT_KEYS 2종 한정으로 노출되지 않던 결함. 분기 그대로 재사용해 매매 코드 침범 0. (2) stale 회복 강화 — 10회 × 120s = 20분 stale 누적 후에야 강제 재등록 발화하던 사이클 9 임계가 단발 silent inactive 회복에 너무 느림. 5회 × 120s = **10분** 으로 단축. 분당 추가 트래픽 ~20 req (KIS 한도 18 req/s = 1080/분 의 2%) 수준이라 무시 가능. 사이클 9 health monitor 가 5xx 누적 감지 자동 비활성으로 안전망. 자율 결정: (a) BFB/VCP 도 MAIN only 단일 보드라 `isBreakout` 분기 재사용 — 별도 BFB/VCP 전용 컴포넌트 분리 안 함 (DRY) / (b) 사이클 9 회귀 가드 의미 갱신 채택(케이스 수 7 보존, 임계 가정만 10→5) — 별도 파일 분리 안 함 / (c) 백엔드 응답 키 무변경 — ScanMonitor 는 기존 `strategies[key]` 만 사용 (BFB/VCP 도 이미 `get_scan_stats()` 보유). 자금 안전: (a) 매매 코드(order_engine.py/risk.py::on_tick/전략 파일) 침범 0 / (b) stale 임계 분기 자동 축소(*2=20→10 / 6~10회 force) — `STALE_FORCE_REREGISTER_AFTER` 변경 1줄로 모든 분기 자동 반영 / (c) 분당 트래픽 worst-case 200 보존(사이클 9 회피 정책 침범 0). 백엔드 1263 passed(2 skipped) — 사이클 11 동일(stale watcher 임계 갱신은 신규 케이스 아님) / 프론트 145 passed(142→+3 BFB/VCP). 5/18 KRX 메인 시간 중 본 fix 적용 위험 0 (매매 무수정 + 응답 키 보존). 후속 권장: 사이클 14 — VCP 다중 호출 발의 (별도 평가) |
