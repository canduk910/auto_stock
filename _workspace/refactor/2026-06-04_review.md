# 리팩토링 리뷰 — 2026-06-04

> 사용자 요청 (사이클 49→58 누적 10 사이클, refactor-review 5회 임계 *2배 초과*) 후속 재검토.
> 이전 메모 (`_workspace/refactor/2026-06-03_review.md`) 잔여 4 카드 재평가 + 사이클 56~58 신규 코드 (sell_rejection V-1 알람 / daily_emit_cap / safe_write_log / log_analysis V-2 메타) 5 영역 일괄 평가.
> 5 계명 준수 (테스트 없는 리팩토링 금지 / CLAUDE.md 절대 규칙 우선 / 3 회 반복 = 추상화 후보 / 주석 정책 보존 / 카드 단위 분할).

## 검토 범위

- 검토 사이클: 49 → 50 → 51 → 52 → 53/53.1 → 54 → 55 → 56 (A~E) → 57 (V-1 매도 거부 폭주 알람) → 58 (V-2 OpenAI 메타). **10 사이클 누적**.
- 검토 디렉토리:
  - `src/engine/` (scheduler, order_engine, sell_rejection, daily_emit_cap, log_analysis_engine, boot_manager, stale_tracker, risk)
  - `src/db/system_logs.py` (`safe_write_log` 신규)
  - `src/api/base.py` (KIS REST)
- 활용 도구: `wc -l`, `grep -c "except Exception"`, Python AST 함수 라인 측정, 이전 메모 카드 status 추적, KIS MCP (영역 1 카드 #6 의존 — 본 검토 단계에서는 *발의 보류* 권고).
- KIS MCP: 본 검토에서 KIS API 정본 호출은 카드 #6 채택 *전* 사전 의뢰 영역. 메모 단계에서는 *사실 기록* 에 한정.

## 누적 통계

### 모듈 라인 수 (2026-06-03 → 2026-06-04 사이클 56~58)

| 파일 | 라인 (2026-06-03) | 라인 (2026-06-04) | 변화 | 비고 |
|---|---|---|---|---|
| `src/engine/scheduler.py` | 3,879 | **3,880** | +1 | 사이클 56-D 위임 호출 1줄 추가 (사실상 무변동) |
| `src/engine/order_engine.py` | 1,151 | **1,151** | 0 | 사이클 56-C/E 마이그레이션 (라인 수 동일, 줄당 의미 강화) |
| `src/engine/sell_rejection.py` | 271 | **368** | +97 | 사이클 57 V-1 (폭주 알람) 추가 (`_maybe_emit_burst_alarm` + `_emit_burst_alarm` + ALARM_* 3 상수) |
| `src/engine/daily_emit_cap.py` | — | **105** | +105 | 사이클 56-A 신규 |
| `src/engine/log_analysis_engine.py` | 462 (추정) | **529** | +67 | 사이클 58 V-2 (`_OpenAIMeta` + `_OPENAI_PRICING` + `_compute_cost_usd` + `_call_openai` 분리) |
| `src/db/system_logs.py` | 222 (추정) | **304** | +82 | 사이클 56-E `safe_write_log` 헬퍼 (+ docstring) |

### scheduler.py 함수 라인 top 12 (2026-06-04 측정, AST 기반)

| 함수 | 라인 | 책임 도메인 | 분리 후보 |
|---|---|---|---|
| `start()` | 378 | lifecycle (boot 후 task 등록 + 시간 분기 dispatch) | 보존 (이전 검토에서 *비권고* 사유 동일) |
| `_check_and_resubscribe_stale` | **221** | **stale_manager** | ★ 카드 #2 |
| `_swing_buy_poll_loop` | 156 | **swing_manager** | ★ 카드 #4 |
| `_execute_next_day_clear` | 150 | **settlement_manager** | ★ 카드 #3 |
| `_run_swing_rest_poll_once` | 118 | **swing_manager** | ★ 카드 #4 |
| `_evaluate_universe_guard` | 116 | **stale_manager** | ★ 카드 #2 |
| `_confirm_breakout_open_prices` | 106 | breakout 시가 확정 (별도 책임) | 두기 (단일 호출 + 보드 명시 의무) |
| `_resubscribe_stale_priority` | 100 | **stale_manager** | ★ 카드 #2 |
| `_build_session_subscription_view` | 98 | stale 진단 (read-only) | stale_manager 동행 |
| `_settle` | 93 | **settlement_manager** | ★ 카드 #3 |
| `_refresh_stale_ccnl_cache` | 93 | **stale_manager** | ★ 카드 #2 |
| `_auto_capture_funnel_snapshots` | 93 | funnel hook | 두기 (단일 책임 + 09:30 자동 발화 한정) |

> **변경 사항**: 2026-06-03 메모의 `_check_and_resubscribe_stale` 230L → 2026-06-04 측정 221L (–9L). 사이클 56-D 직접 clear → `risk_manager.reset_daily_state()` 위임으로 stale 분기 단축. 분해 후보 응집도 그대로.

### 광범위 except 카운트 (사이클 56-E 마이그레이션 후)

| 파일 | `except Exception` | 변화 | 비고 |
|---|---|---|---|
| `src/engine/scheduler.py` | **110** (변화 없음) | 0 | 카드 #2/#3/#4 분해와 동시 시정 |
| `src/engine/order_engine.py` | **15** | −3 | 사이클 56-E 마이그레이션 4 곳 — `safe_write_log` 내부 흡수 |
| `src/engine/recommendation_engine.py` | 30 | 0 | OpenAI graceful 정당화 |
| `src/api/base.py` | 14 | 0 | KIS MCP 검증 후 좁히기 카드 (#6) |
| `src/realtime/websocket_pool.py` | 11 | 0 | KIS 명세 외 ws library 변형 흡수 |
| `src/engine/boot_manager.py` | 8 | 0 | 외부 fetch graceful |
| `src/realtime/websocket.py` | 5 | 0 | WebSocket lifecycle |
| `src/engine/risk.py` | 3 | 0 | regime 폴백 안전 정책 |
| `src/engine/sell_rejection.py` | **0** | 0 | 신규 모듈 — 의도된 RuntimeError 가드 1 곳만 |
| `src/engine/daily_emit_cap.py` | **0** | 0 | 신규 모듈 — except 없음 |
| `src/engine/log_analysis_engine.py` | 2 | 0 | OpenAI graceful 1 + import 1 |

> **검토 시점 (영역 3) 의 "30+ 광범위 except" 진단은 오측이었음**. 실제 측정 15 곳. 사용자 요청의 "Line 142, 154, 162, 171, 193, 338, 515, 647, 764, 793, 808, 821, 896, 919, 1083, 1120, 1151" 중 사이클 56-E 마이그레이션 후 실제 잔존은 L146/174/196/341/477/648/765/777/794/820/895/918/1082/1119/1150 = 15 곳. 본 메모는 *실측* 기반.

### 사이클 56-E 마이그레이션 결과 (영역 2 평가)

이전 메모 카드 #5 후속. order_engine.py 14 곳 권고 중 **4 곳 마이그레이션** (L152/L168/L511/L803), **10 곳 잔존**. 잔존 10 곳 분류:

| 위치 | 패턴 | 분류 | 마이그레이션 가능성 |
|---|---|---|---|
| L146 | `stock_master.get` 실패 → `logger.exception` + return base | 의도된 fallback (`logger.exception` 보존 가치 — exc_info 자동 포함) | **유지 권고** (safe_write_log 는 INFO/WARNING graceful 용도, exception 흔적 명시는 다른 패턴) |
| L174 | `_log_stale_async` 내부 stale 체크 실패 → `logger.debug` | graceful skip 의도 명확 | **마이그레이션 가능** (단, async task 내부 — fire-and-forget 의도 동일) |
| L196 | `[nxt_downgrade]` write_log 실패 → `logger.debug` | graceful skip 의도 명확 | **마이그레이션 가능** ★ |
| L341 | `[market_order_preconvert_pre_nxt]` 사전 평가 실패 → swallow + 폴백 변경 | 의도된 fallback (변수 재할당 포함) | **유지 권고** (`safe_write_log` 범위 외) |
| L477 | `execute_buy` 외곽 — pending_buys 정리 + re-raise | 의도된 예외 전파 (`raise` 동반) | **유지 권고** (graceful 아님) |
| L648 | `stock_master.upsert_one` 사후 보강 실패 → `logger.exception` | 의도된 logger.exception | **유지 권고** |
| L765 | `_pending_next_day_clear` 등록 실패 → `logger.debug` | graceful skip 의도 명확 | **마이그레이션 가능** (단, 내부 try 와 다중 중첩 — 비용 ↑) |
| L777 | `execute_sell` 외곽 KIS API 예외 외 → `logger.warning` + retry | 의도된 fallback | **유지 권고** |
| L794 | `delete_position` 실패 → `logger.exception` | 의도된 logger.exception | **유지 권고** |
| L820 | `get_balance` 조회 실패 → `logger.debug` | graceful skip 의도 명확 | **마이그레이션 가능** (단, safe_write_log 범위 외 — KIS API graceful) |
| L895 | `_pending_next_day_clear_provider()` 호출 실패 → `pending = set()` | 의도된 fallback (변수 재할당) | **유지 권고** |
| L918 | `_unsubscribe_if_no_other_strategy` 외곽 → `logger.exception` | 의도된 logger.exception | **유지 권고** |
| L1082 | `_cancel_after_wait` task → `logger.exception` | 의도된 logger.exception (background task 안전망) | **유지 권고** |
| L1119 | `_cancel_and_reorder` task → `logger.exception` | 의도된 logger.exception (background task 안전망) | **유지 권고** |
| L1150 | `cancel_remaining` → `logger.exception` | 의도된 logger.exception | **유지 권고** |

→ **10 곳 중 마이그레이션 가능 3 곳** (L174 / L196 / L765). 단 L820 은 KIS API 영역으로 카드 #6 와 통합 후보. **나머지 12 곳은 의도된 예외 전파/logger.exception/fallback 으로 정당화**.

### Emit cap 통합 후 상태 (영역 1 카드 #1 종결 검증)

| 위치 | 자료구조 | 상태 |
|---|---|---|
| `risk.py` | `_risk_silent_skip_logged_today: DailyEmitCap[tuple[str, str]]` | ✅ 사이클 56-D 마이그레이션 완료 + `RiskManager.reset_daily_state()` 캡슐화 |
| `order_engine.py:93` | `_nxt_downgrade_logged_today: DailyEmitCap[str]` | ✅ 사이클 56-C 마이그레이션 완료 (호환 layer `add/clear/__contains__` 경유) |
| `sell_rejection.py:92` | `_logged_today: DailyEmitCap[str]` | ✅ 사이클 56-B 마이그레이션 완료 (호환 layer `clear` 경유) |
| `order_engine.py:1137` | 코드 코멘트 `# 사이클 54 유지 (사이클 56 통합 예정)` | ⚠ **잔존** — 사이클 56-C 종결 후 코멘트 정리 누락 |

→ **카드 #1 후속 nit**: order_engine.py:1137 코드 코멘트 "사이클 56 통합 예정" 영구 제거 미반영. 사이클 56-C 가 *완료* 했음에도 *예정* 으로 남아있어 차기 검토자 혼동 유발. **카드 #7 (LOW)** 으로 발의.

---

## 권고 카드 목록

### 카드 #2 (재발의) — scheduler 분해 후속: stale_manager 추출 (HIGH ★★★) — 우선순위 1

> **상태 (2026-06-04 17:00 KST)**: **Phase 2-A1 (LOW) 완료** — domain-expert 자문 옵션 B 3 단계 분할 전체 채택 → 사이클 60 = A1 단독 진행. 5 함수 (~275L) + 5 상수 + 18 회귀 가드 PASS (I1 hotfix 영구 가드 포함). scheduler.py 3,880 → 3,605L.
>
> **상태 (2026-06-05 13:15 KST 갱신)**: **Phase 2-A2 (MEDIUM) 완료** — 사이클 61. 4 함수 314L + 5 상수 추가 이주 (누적 9 함수 + 10 상수). 사이클 60 lazy import 빚 청산 + `sys.modules.get` 패턴 도입. 회귀 가드 20 케이스 (HIGH 3: C cap dict / I universe 보유 / J universe 익일청산). 백엔드 1862 → **1882 PASS** (+20, 회귀 0, flakiness 0 3 회 반복). scheduler.py 3,605 → 3,315L (누적 −870L, −21%). stale_manager.py 358 → 731L.
>
> **잔여 A3 (HIGH)**: 사이클 63 — `_check_and_resubscribe_stale` (221L) + `_resubscribe_stale_priority` (100L) = K stale watcher 핵심. **주말 push 의무 + 월요일 첫 _boot 1h tester verify** (사이클 60 도메인 자문 Q5/Q7).
>
> **상태 (2026-06-06 토 18:00 KST 갱신)**: **Phase 2-A3 (HIGH) 완료 → refactor #2 종결**. 사이클 63. 2 함수 322L 추가 이주 (`_check_and_resubscribe_stale` 222L + `_resubscribe_stale_priority` 100L). 누적 9 → **11 함수** (상수 0 추가). domain-expert 옵션 A 전부 적용 — **Q4 = B 직접 호출** (사이클 60 답습하지 않는 *유일 영역*) + Q2 try/except 4 중 가드 보존. scheduler.py 3,315 → **3,007L** (-308L). stale_manager.py 731 → **1,076L**. **누적 scheduler.py 감소**: 사이클 51 직전 ~4,185 → 3,007 = **-1,178L, -28%**. 회귀 가드 29 케이스 (12 파일, HIGH 11 = 38% 전수 PASS). 백엔드 1915 → **1944 PASS** / 전체 **2111 PASS** / 회귀 0 / flakiness 0 / coverage 81.06%. 사이클 29 005935 사고 패턴 차단 영속 (H 4 freezegun PASS).
>
> **신규 위험 (tester 발견, 사이클 61)**: stale_manager.py 731L 비대화 진행 중 — 향후 sub-module 분해 후보 3 종 (`cap_per_hour` / `fresh_ratio_threshold` / `universe_guard`) refactor-expert 검토 대상 등록 권고 (별도 사이클, A3 완료 후 재평가).
>
> **사이클 63 사후 신규 카드 발의 (domain-expert §Q5-3 + §Q5-2)**: 카드 #5 (HIGH) cap=10 결함 시정 + 카드 #14 (MEDIUM) sub-module 분해 (3+1 청사진) — 본 메모 아래 별도 섹션.

- **위험 등급**: HIGH (모듈 간 경계 + WebSocket 4 중 안전망 hot path)
- **재평가 결과**: 이전 메모 카드 #2 와 위험 등급/권고 동일. 사이클 56~58 변경이 *영향 없음* 확인:
  - 사이클 56-D 의 `risk_manager.reset_daily_state()` 위임은 risk 모듈에 한정 → stale_manager 와 무관
  - 사이클 57 V-1 (sell_rejection 알람) 는 sell_rejection 내부 — stale 영역 무관
  - 사이클 58 V-2 (log_analysis_engine 메타) 는 log_analysis 한정 — stale 영역 무관
  - `_check_and_resubscribe_stale` 230L → 221L (–9L) 미세 변동만
- **현 상태**: 분리 후보 11 함수 누적 ~1,000L+ (scheduler.py 의 26%). 응집도 변화 없음. 모두 `_stale_state: StaleTrackerState` (사이클 48) + `kis_ws_pool` 협업
- **권고**: `src/engine/stale_manager.py` 신규 모듈 (사이클 51 boot_manager 패턴 답습 — `scheduler` 인자 + 2 줄 wrapper 위임)
- **회귀 가드**: 위임 1회 + 호출 순서 보존 + 5 상수 (`MAX_STALE_RETRIES` / `STALE_FORCE_RETRY_AFTER_SECS` / `STALE_FORCE_RETRY_HOURLY_CAP` / `SILENT_INACTIVE_FRESH_RATIO_THRESHOLD` / `UNIVERSE_LOW_VOLUME_THRESHOLD`) 동일성 + WebSocket 4 중 안전망 행위 가드 4 종 (이전 메모 동일)
- **선행 의뢰** (HIGH 의무):
  - **domain-expert** — WebSocket 4 중 안전망 호출 시점 영향 평가 (K stale watcher force_retry timing + silent inactive `_force_reconnect_session` timing + `_reset_daily_state` 동행 reset 보장)
  - **tdd-engineer** — 위임 1회 / 호출 순서 / 5 상수 동일성 회귀 가드 사전 작성
- **안전 규칙 충돌**: WebSocket 4 중 안전망 (체결통보 / NXT 좀비 / `_reset_daily_state` 동행) 절대 보호
- **예상 효과**: `scheduler.py` 3,880 → ~2,880 (−1,000L, −26%). stale 사이클 추가 비용 −50%
- **우선순위 1 사유**: 라인 감소 효과 최대 + 이전 메모 1순위 그대로 + 사이클 56~58 무관

---

### 카드 #3 (재발의) — scheduler 분해 후속: settlement_manager 추출 (HIGH ★★★) — 우선순위 2

- **위험 등급**: HIGH (정산 + 익일청산 hot path + `_reset_daily_state` 동행 25+ 필드)
- **재평가 결과**: 이전 메모 카드 #3 와 위험 등급/권고 동일. 사이클 56-D 의 `risk_manager.reset_daily_state()` 위임이 `_reset_daily_state` 동행 25+ 필드 매트릭스에 *1 필드* 추가 (이미 사이클 56 backend-dev 가 반영). 사이클 57 V-1 의 `_sell_rejection.reset_daily()` 는 사이클 55 시점 이미 반영. **카드 #3 권고 동일성 유지**.
- **현 상태**: 4 함수 ~376L (`_execute_next_day_clear` 150 + `_drain_pending_next_day_clear` ~50 + `_settle` 93 + `_reset_daily_state` 81 + helpers ~2)
- **권고**: `src/engine/settlement_manager.py` 신규 모듈. **카드 #2 stale_manager 채택 *후* 발주** (의존성 — `_reset_daily_state` 가 stale_state.reset_daily 호출 후 분해 시 중복 변경 회피)
- **회귀 가드**: 위임 1회 + 호출 순서 + **`_reset_daily_state` 동행 25+ 필드 일괄 reset 단위 테스트** (한 줄 누락 = 전일 상태 잔류 사고 직결) + 익일 청산 30s 안정화 가드
- **선행 의뢰** (HIGH 의무):
  - **domain-expert** — (a) `unsubscribe_all() → AI자문 → settlement → log_analysis → purge_old_logs → _reset_daily_state` 순차 의존성 보존, (b) `_pending_next_day_clear` 와 `stock_master.nxt_tradable=False` race 없음, (c) cap dict (nxt_downgrade / sell_rejection / risk_silent_skip) 동행 clear
  - **tdd-engineer** — `_reset_daily_state` 25+ 필드 동행 reset 가드 + 30s 안정화 가드
- **안전 규칙 충돌**: 익일 청산 / `_reset_daily_state` / NXT 좀비 차단 / 15:20 일괄청산 절대 보호
- **예상 효과**: `scheduler.py` ~2,880 → ~2,504 (−376L). 정산 흐름 단일 모듈

---

### 카드 #4 (재발의) — scheduler 분해 후속: swing_manager 추출 (MEDIUM ★★) — 우선순위 3

- **위험 등급**: MEDIUM (donchian_swing 단일 전략 한정 + `_swing_rest_poll_loop` CLAUDE.md 절대 규칙 보존)
- **재평가 결과**: 이전 메모 카드 #4 와 동일. 사이클 56~58 무관.
- **현 상태**: `_swing_buy_poll_loop` 156L + `_run_swing_rest_poll_once` 118L + `_sleep_chunked` ~134L (nested) ≈ ~400L
- **권고**: `src/engine/swing_manager.py` 신규 모듈. 카드 #2/#3 *완료 후 마지막* 분해
- **회귀 가드**: `_swing_rest_poll_loop` wrapper 보존 + 09:30~15:20 시간 가드 + 60s 폴링 주기
- **선행 의뢰**: domain-expert *경량 자문* — donchian 멀티데이 손절 영향 없음
- **안전 규칙 충돌**: `_swing_rest_poll_loop` 제거 금지 — wrapper 보존
- **예상 효과**: `scheduler.py` ~2,504 → ~2,104 (−400L). 사이클 51 + #2 + #3 + #4 누적 3,880 → ~2,104 (−45%)

---

### 카드 #6 (재발의 + 보강) — KIS API except 좁히기 + safe_write_log 통합 통합 카드 (MEDIUM ★★, KIS MCP 의존) — 우선순위 4

- **위험 등급**: MEDIUM (KIS 응답 변형 흡수 영역 — 좁히기 시 KIS 거부 코드 누락 위험)
- **재평가 결과 (영역 1 + 영역 3 통합)**: 이전 메모 카드 #6 와 *영역 2 잔여 10 곳* + *영역 3 KIS API 영역* 가 모두 동일한 의도 — "graceful 정당화된 broad except 만 safe_write_log 로 흡수 + 의도 미식별 영역은 KIS MCP 검증 후 좁히기". **본 검토에서 영역 1+2+3 통합 카드로 재정의**.
- **현 상태**:
  - `src/api/base.py` 14 곳 `except Exception` — KIS REST graceful (사이클 33 `prdy_vol` 누락 같은 silent swallow 결함 위험)
  - `src/engine/order_engine.py` 잔존 10 곳 중 **3 곳 마이그레이션 가능** (L174 / L196 / L765 — 영역 2 표 참조)
  - `src/engine/order_engine.py` L820 `get_balance` 조회 실패 — KIS API graceful 영역 (KIS MCP 검증 영역과 겹침)
- **권고**: 카드 발의 *보류* — KIS MCP 정본 거부 코드 분류 후 좁히기 정당화 *선행 필요*. **분할 사이클 제안**:
  - **사이클 N-A (LOW)**: `src/engine/order_engine.py` L174 / L196 / L765 3 곳 추가 `safe_write_log` 마이그레이션 (KIS MCP 무관 — 단순 graceful skip)
  - **사이클 N-B (MEDIUM, KIS MCP 의존)**: `src/api/base.py::_request` 14 곳 → `except (KisApiError, httpx.HTTPError, asyncio.TimeoutError, json.JSONDecodeError)` 명시. 나머지 `Exception` 은 re-raise (예측 못 한 결함 가시화). **KIS MCP 사전 조사** 후 진행
  - **사이클 N-C (MEDIUM)**: `src/engine/order_engine.py` L820 `get_balance` 호출 분기 — KIS MCP 정본 응답 시리즈 합성 후 좁히기
- **회귀 가드** (현시점 작성 불가 — 카드 채택 후 tdd-engineer 가 KIS MCP 정본 응답 시리즈 합성):
  - 기존 `_request` 5xx/4xx/network/retries 메트릭 보존
  - 사이클 56+ 신규 KIS 거부 코드 발견 시 *바로 가시화*
- **선행 의뢰**:
  - **tdd-engineer** — KIS MCP 정본 응답 시리즈 합성 + 회귀 가드 사전 작성 (의무)
  - **domain-expert** — KIS 거부 코드 분류 (APBK0918 / APBK1943 / APBK3013 + 미식별)
- **안전 규칙 충돌**: `[kis_rejection]` system_logs 영구 저장 정책 보존
- **예상 효과**: 사이클 33 같은 silent swallow 결함 영구 차단 + 미식별 결함 traceback 즉시 가시화 + 운영 디버깅 시간 단축

---

### 카드 #7 (신규, LOW ★) — order_engine.py:1137 코드 코멘트 "사이클 56 통합 예정" 영구 제거

- **위험 등급**: LOW (코멘트만 — 코드 행위 영향 0)
- **현 상태**: `src/engine/order_engine.py:1137`
  ```python
  self._nxt_downgrade_logged_today.clear()  # 사이클 54 유지 (사이클 56 통합 예정)
  ```
  사이클 56-C 가 *완료* 했음에도 코멘트는 *예정* 으로 남아있음. 차기 검토자 혼동 유발 (refactor-expert 본인이 1차 발견).
- **권고**: 코멘트 1줄 갱신 — `# 사이클 54 + 사이클 56-C DailyEmitCap 마이그레이션 완료`. 또는 더 짧게 코멘트 제거 (변수명 자체가 자기설명적, CLAUDE.md "well-named identifiers already do that" 정책 따름)
- **회귀 가드**: 불필요 (LOW, 코멘트만)
- **선행 의뢰**: 없음
- **안전 규칙 충돌**: 없음
- **예상 효과**: 빚 해소 + 차기 검토자 혼동 차단. 1줄 변경

---

### 카드 #8 (신규, MEDIUM ★★) — sell_rejection.py V-1 알람 헬퍼 분리 (`_emit_burst_alarm` 추출)

- **위험 등급**: MEDIUM (사이클 57 V-1 신규 모듈 — 회귀 가드 8 케이스 통과)
- **현 상태**: `src/engine/sell_rejection.py` 368L 단일 클래스 `SellRejectionTracker`. 사이클 57 V-1 추가로 *2 책임 혼재*:
  - **R-1 단일 정책 (사이클 55)**: 4 분류 거부 진입 게이트 + TTL 관리
  - **V-1 폭주 알람 (사이클 57)**: 10분 5건 초과 CRITICAL 알람 + 30분 cooldown
- **분리 가치 평가** (3 회 반복 = 추상화 후보 임계 미충족):
  - 알람 패턴이 *다른 모듈* 에서 재사용될 가능성? 현재 sell_rejection 단일 사용. **2 회 이하 반복**
  - 향후 매수 거부 폭주 (V-1 후속 발의 가능성)? 사용자 미요청
  - **5 계명 #3 적용**: "3 회 반복 = 추상화 후보, 2 회 이하 = 두기" — **2 회 이하** + 미래 가정 추상화
- **권고**: 카드 발의 *하지 않음*. **비권고 사항으로 분류**. 단일 모듈 368L 단일 책임 (sell 거부 정책) 유지. 향후 매수 거부 알람 추가 시 (3 회째) 그때 분리 권고

---

### 카드 #9 (신규, LOW ★) — log_analysis_engine.py `_OpenAIMeta` 위치 보존

- **위험 등급**: LOW (사이클 58 신규 — 회귀 가드 통과)
- **현 상태**: `src/engine/log_analysis_engine.py:48-56` `_OpenAIMeta` dataclass (5 필드) + `_OPENAI_PRICING` 6 모델 + `_compute_cost_usd` + `_call_openai`. 모두 `log_analysis_engine` *내부 사용 한정*.
- **분리 가치 평가**:
  - `_OpenAIMeta` 가 다른 모듈에서 사용? `recommendation_engine` 도 OpenAI 호출 — **잠재 재사용 1 곳**. 현재 2 회 등장 (log_analysis 신규 + recommendation 가능). **2 회 이하**
  - `_OPENAI_PRICING` 도 동일. recommendation_engine 도 cost 계산 가치
  - 분리 시 모듈 신규 (`src/engine/openai_meta.py`) — *추상화 비용 > 효과*. 카드 #8 과 동일 논리
- **권고**: 카드 발의 *하지 않음*. **비권고 사항으로 분류**. 단, **사이클 N+ 에서 recommendation_engine 도 같은 메타 수집 요구가 발생하면** (3 회째 반복) 그때 `src/engine/openai_meta.py` 로 분리 권고. **잠재 의도** 만 기록.

---

### 카드 #10 (신규, LOW ★) — sell_rejection.py / order_engine.py 지역 import 패턴 표준화

- **위험 등급**: LOW (import 위치만)
- **현 상태**:
  - `src/engine/order_engine.py:87` `from src.engine.sell_rejection import SellRejectionTracker` (`__init__` 내부)
  - `src/engine/order_engine.py:92` `from src.engine.daily_emit_cap import DailyEmitCap` (`__init__` 내부)
  - `src/engine/order_engine.py:505` `from src.engine.sell_rejection import is_krx_main_hours, is_nxt_session_hours` (함수 내부)
  - 같은 모듈 (`order_engine.py`) 상단 line 29 `from src.engine.daily_emit_cap import DailyEmitCap` *이미* 존재 — line 92 의 지역 import 와 **중복**
- **분류**:
  - `sell_rejection` import 3 회 등장 (L87 init + L505 함수 + 상단 0) → 상단 import 추가 후 지역 import 제거 정당화
  - `daily_emit_cap` 의 L92 지역 import 는 *상단 L29 와 중복* → 즉시 제거 가능 (행위 동일)
  - circular import 위험: `sell_rejection` 은 `safe_write_log` + `daily_emit_cap` 만 import — `order_engine` 와 무순환. **상단 import 안전**
- **권고**: 카드 발의 *조건부 (LOW)*. **3 회 미만이지만 *중복 제거* 는 즉시 권고 가능**:
  - L92 `from src.engine.daily_emit_cap import DailyEmitCap` 1 줄 제거 (L29 와 중복, 행위 영향 0)
  - L87 `SellRejectionTracker` + L505 `is_krx_main_hours, is_nxt_session_hours` 상단 통합 import 권고 (회귀 가드 0 — 그냥 import 위치 이동)
- **회귀 가드**: 단위 테스트 1 케이스 — import 가능성만. 또는 기존 사이클 55/57 회귀 가드 통과로 충분
- **선행 의뢰**: 없음
- **안전 규칙 충돌**: 없음 (circular import 없음 확인됨)
- **예상 효과**: 코드 가독성 + import 단일 위치 정책 일관성 (CLAUDE.md "coding conventions" 보강)

---

## 잔여 카드 재평가 매트릭스

| 이전 카드 (2026-06-03) | 위험 등급 (2026-06-03) | 위험 등급 (2026-06-04) | 변경 | 사유 |
|---|---|---|---|---|
| #1 DailyEmitCap 통합 | MEDIUM | ✅ **종결** | — | 사이클 56-A~D 종결. 단 카드 #7 (코멘트 nit) 신규 발의 |
| #2 stale_manager | HIGH | HIGH | 동일 | 사이클 56~58 영향 없음 — 카드 #2 재발의 우선순위 1 유지 |
| #3 settlement_manager | HIGH | HIGH | 동일 | 사이클 56-D 의 `risk_manager.reset_daily_state()` 위임은 25+ 필드 동행 매트릭스에 *반영 완료* |
| #4 swing_manager | MEDIUM | MEDIUM | 동일 | 사이클 56~58 영향 없음 |
| #5 safe_write_log | LOW | ✅ **부분 종결 + 영역 2 추가 발의 가능** | — | 사이클 56-E 4 곳 종결. 영역 2 잔여 10 곳 중 3 곳 (L174/L196/L765) 추가 마이그레이션 권고 — **카드 #6 통합 카드** 1단계로 합류 |
| #6 KIS API 좁히기 | MEDIUM | MEDIUM | 동일 | 영역 2+영역 3 통합 카드로 *재정의*. KIS MCP 의존 사실 동일 |

### 신규 카드 (2026-06-04 추가)

| 신규 카드 | 위험 등급 | 우선순위 |
|---|---|---|
| #7 code comment cleanup (order_engine:1137) | LOW | 즉시 발주 가능 (1줄) |
| #10 지역 import 표준화 + L92 중복 제거 | LOW | 즉시 발주 가능 (3줄 내외) |

### 비권고 사항 (검토 후 카드 미발의)

| 영역 | 사유 |
|---|---|
| **카드 #8** sell_rejection V-1 알람 분리 | 2 회 이하 반복 (sell 단일 사용) + 미래 가정 추상화 — 5 계명 #3 위반. 매수 거부 알람 추가 시 (3 회째) 재검토 |
| **카드 #9** log_analysis `_OpenAIMeta` 분리 | 2 회 이하 반복 (log_analysis 단일 사용) + recommendation_engine 잠재 재사용은 미실현 — 5 계명 #3. 잠재 의도만 기록 |
| **잔여 12 곳 broad except** (order_engine.py) | 영역 2 표 참조 — `logger.exception` (자동 exc_info) / 의도된 예외 전파 / fallback 변수 재할당 — 모두 정당화. 좁히기 시 회귀 위험 증가 |
| **`start()` 378L 분할** | lifecycle 단일 의도 + 순차 의존성 강함. 이전 메모와 동일 (변경 없음) |
| **`recommendation_engine.py` 30 except 좁히기** | OpenAI/외부 fetch graceful 다수 — 일괄 좁히기는 비용 과도. 결함 발생 시 case-by-case |
| **`websocket_pool.py` / `websocket.py` 16 except** | KIS 명세 외 ws library 예외 — KIS MCP 로 검증 불가. 좁히기 시 연결 끊김 silent 위험 |
| **`scheduler.py` 110 except** | 카드 #2/#3/#4 모듈 분해와 동시 시정 시 효율 — 분해 *전* 좁히기는 의도 혼재 |
| **`boot_manager.py` 8 except** | 사이클 51 분해 시 원본 패턴 보존 — 외부 fetch graceful 정당화 |
| **`risk.py:125` regime 폴백** | HARD fallback 안전 정책 — 좁히기 시 매수 차단 회귀 위험 |
| **`_reset_daily_state` 81L 분할** | 25+ 필드 동행 clear 단일 의도 — 카드 #3 분해 시 *함수 자체는 이동만* |
| **`_check_and_resubscribe_stale` 221L 분할** | 카드 #2 stale_manager *내부* 함수 — 모듈 이동 후 별도 사이클에서 분할 권고 |
| **stale dict / TTL evict / 5분 emit task / 종목명 lookup / funnel hook** | 사이클 44/45/46/47/48 종결 |
| **사이클 56-A~D + 사이클 56-E + 사이클 57 V-1 + 사이클 58 V-2** | 모두 회귀 가드 통과 + 사이클별 1 의도 — 본 사이클 재평가 결과 *카드 발의 영역 없음* (단 카드 #7 #10 nit 만) |

---

## 후속 의뢰

| 대상 | 영역 | 의뢰 사항 | 우선순위 |
|---|---|---|---|
| **team-leader** | 7 카드 (재발의 4 + 신규 3) 채택/보류/폐기 결정 | 본 메모 기반 사용자 의사 반영. 권장 순서: **#7 #10 (LOW 즉시 정리) → #2 (HIGH 우선) → #3 (HIGH 후속) → #4 (MEDIUM) → #6 (KIS MCP 검증 후)** | 즉시 |
| **tdd-engineer** | 카드 #2 (stale_manager) 회귀 가드 사전 작성 | 위임 1회 / 호출 순서 / 5 상수 동일성 / WebSocket 4 중 안전망 행위 가드 | 카드 #2 채택 *전* (Red-first) |
| **tdd-engineer** | 카드 #3 (settlement_manager) 회귀 가드 | `_reset_daily_state` 25+ 필드 동행 clear 가드 (사이클 56-D 후 +1 필드) + 익일 청산 30s 안정화 가드 | 카드 #3 채택 *전* |
| **tdd-engineer** | 카드 #6 (KIS API 좁히기) KIS MCP 정본 응답 시리즈 합성 | kis-mcp-query 스킬 활용 — KIS 거부 코드 매트릭스 + respx + freezegun | 카드 #6 채택 *전* |
| **domain-expert** | 카드 #2 행위 영향 평가 | stale_manager 분해가 WebSocket 4 중 안전망 호출 시점/순서에 미치는 영향 | 카드 #2 채택 *전* (HIGH 의무) |
| **domain-expert** | 카드 #3 행위 영향 평가 | 정산 흐름 (`unsubscribe_all → AI자문 → settlement → log_analysis → purge_old_logs → reset`) 순차 의존성 보존. 사이클 56-D risk_manager.reset_daily_state() 위임 반영 후 25+ 필드 매트릭스 정합성 | 카드 #3 채택 *전* (HIGH 의무) |
| **domain-expert** | 카드 #4 경량 자문 | `_swing_rest_poll_loop` 분해가 donchian 멀티데이 손절 평가 영향 없음 확인 | 카드 #4 채택 *전* |
| **domain-expert** | 카드 #6 KIS 거부 코드 분류 검토 | APBK0918 / APBK1943 / APBK3013 + 미식별 코드 예외 분류 정합성 | 카드 #6 채택 *전* (HIGH 의무) |

---

## 통합 추상화 후보 (메모 보존 — 사이클 N+ 재검토)

본 사이클에서는 *카드 미발의*. **3 회 반복 임계 도달 시** 재검토:

1. **`_OpenAIMeta` + `_OPENAI_PRICING` + `_compute_cost_usd`** → `src/engine/openai_meta.py` 분리. **트리거**: recommendation_engine 도 메타 수집 요구 발생 시 (3 회째). 사이클 58 단계에서는 미발의.
2. **알람 폭주 차단 패턴** (sell_rejection V-1: 10분 5건 + 30분 cooldown) → `src/engine/burst_alarm.py` 분리. **트리거**: 매수 거부 알람 / WebSocket 결함 알람 등 2 회째 발생. 사이클 57 V-1 단계에서는 미발의.
3. **safe_execute 헬퍼** (write_log + 임의 async fire-and-forget graceful) → `src/utils/async_safe.py` 확장. **트리거**: `safe_write_log` 외 다른 fire-and-forget 패턴이 3 회 이상 동형으로 등장 시.
4. **scheduler 분해 4 단계 완성 후** (#2/#3/#4 종결) — `scheduler.py` ~2,104L 까지 감소 후 **추가 분해** 검토. 후보: `_swing_buy_poll_loop` (156L), `_confirm_breakout_open_prices` (106L), `_auto_capture_funnel_snapshots` (93L). 단 각각 단일 책임 + 비교적 작은 라인 → *3 분해 후 재평가*.

---

## 절대 안전 규칙 보호 (사전 격리)

CLAUDE.md 14+ 종 절대 규칙 + 사이클 55+ 신규 4 종:
- **사이클 52** OrderEngine 진입 차단 게이트 (`SellRejectionTracker.is_blocked`)
- **사이클 55** SellRejectionTracker 2단계 TTL (KRX 메인 5분 / NXT 다음 09:00)
- **사이클 56** DailyEmitCap 호환 layer (`add/clear/__contains__/__eq__`)
- **사이클 57** V-1 알람 임계 상수 (`ALARM_WINDOW_SECONDS=600 / ALARM_THRESHOLD=5 / ALARM_COOLDOWN_SECONDS=1800`)

각 카드의 *안전 규칙 충돌* 영역 사전 명시. 카드 #2/#3 (HIGH) 는 분해 *전후* 행위 동일성 통합 테스트 필수 — domain-expert 자문 *없이는 채택 금지*.

---

## 운영 시간 권고

- 본 검토 메모 작성 자체는 영향 없음
- **카드 #7 #10 (LOW)**: 비영업일 / 익일 07:50 _boot 전 push 권장 — 1~3줄 변경, 회귀 위험 0
- **카드 #2 #3 (HIGH, WebSocket 4 중 안전망 / 정산 hot path)**: *주말* 또는 *공휴일 전일* push 안전. KRX 메인 시간 (09:00~15:30) 절대 회피
- **카드 #4 (MEDIUM)**: NXT 애프터 (15:30~) 또는 익일 07:50 _boot 전 push 권장
- **카드 #6 (MEDIUM, KIS MCP 의존)**: KIS MCP 검증 + tdd-engineer 회귀 가드 작성 후 NXT 애프터 push

---

## 다음 단계 (사용자 결정)

| 옵션 | 진행 카드 | 예상 사이클 | 비고 |
|---|---|---|---|
| **A** | **#7 + #10 (LOW 즉시 정리)** | 사이클 59 (1 사이클) | 코드 코멘트 + 지역 import 정리 — 1~3 줄 변경, 회귀 위험 0, 발주 즉시 |
| **B** | **#2 (stale_manager HIGH)** | 사이클 60~61 | 이전 메모 1순위 그대로 — domain-expert 자문 필수 |
| C | #3 (settlement_manager HIGH) | 사이클 62 | 카드 #2 후속 |
| D | #4 (swing_manager MEDIUM) | 사이클 63 | scheduler 분해 완성 |
| E | #6 (KIS API 좁히기 + 영역 2 잔여 3 곳 + KIS MCP) | 사이클 60+ | tdd-engineer + domain-expert 사전 의뢰. **사이클 N-A (LOW, KIS MCP 무관 3 곳) 만 먼저 추출 가능** |
| **F** | **#7 + #10 → #2 → #3 → #4 → #6 순차** | 사이클 59 → 65 (2~3 주) | 우선순위 권고 — LOW 정리 → HIGH 분해 → MEDIUM KIS MCP |
| G | 메모만 보존 | — | 본 메모 영구 보존, 채택 보류 |

### refactor-expert 권장 순서 (team-leader 참고)

**Phase 1 (즉시, 사이클 59)**: #7 + #10 LOW 정리 → 1 사이클 내 종결 + 빚 해소
**Phase 2 (사이클 60~61)**: #2 stale_manager HIGH — scheduler.py 최대 라인 감소 (−26%)
**Phase 3 (사이클 62)**: #3 settlement_manager HIGH — 정산 흐름 단일 모듈
**Phase 4 (사이클 63)**: #4 swing_manager MEDIUM — scheduler 분해 완성
**Phase 5 (사이클 64+, KIS MCP 검증 후)**: #6 통합 카드 (영역 2 잔여 3 곳 + KIS API 14 곳 + L820)

---

## 부록 — 사이클 49→58 누적 변경 통계

```
git log --oneline -10 (사이클 49→58 + Copilot 리뷰 nit)
e2ed99d 사이클 58: daily_log_reports OpenAI 메타 컬럼 (V-2)
f23418e 사이클 57: 매도 거부 폭주 실시간 알람 (V-1)
e96cab7 사이클 56: DailyEmitCap 통합 + safe_write_log (refactor #1 + #5)
e82cf2f 사이클 55: SellRejectionTracker 단일 정책 객체 (R-1)
5aff5fd 사이클 54: NXT 다운그레이드 로그 emit cap (B-3)
9733c7e 사이클 53 + 53.1: log_analysis 데이터 수집 결함 시정 (B-2 + B-4)
3c96fdf 사이클 52: 매도 좀비 폭주 차단 (B-1) — 진입 게이트 + 문서 동기화
e0b30e3 refactor(engine): _boot 본체 boot_manager 형제 모듈 추출 (사이클 51)
b8531dd test(bfb): 사이클 50 테스트 빌더 dead code 제거 (Copilot 리뷰 nit)
09574cc 사이클 50: BFB 폴/플래그 funnel 단계별 사유 정밀화 + 실측 계측 (단계 1)
3d897c1 fix(vcp): Pullback 검출 결함 시정 (사이클 49)
```

- **신규 모듈**: `daily_emit_cap.py` (105L) / `sell_rejection.py` (368L = R-1 271L + V-1 +97L) / `boot_manager.py` (305L)
- **신규 헬퍼**: `safe_write_log` (system_logs.py +82L) / `_OpenAIMeta` + `_compute_cost_usd` (log_analysis_engine.py +67L)
- **scheduler.py 라인 변화**: 3,906 (2026-05-22) → 3,879 (2026-06-03) → 3,880 (2026-06-04). **사이클 56~58 사실상 무변동**. 카드 #2~#4 채택 시 ~2,104 까지 감소 가능 (−45%)
- **백엔드 테스트**: 1819 (사이클 56) → 1822 (사이클 57 V-1) → 1844 (사이클 58 V-2) PASS


---

## 사이클 60 사후 신규 카드 (2026-06-04 17:00 KST 발의)

### 카드 #11 (신규, MEDIUM ★★) — 보드 전환 ↔ K stale watcher race mutex (domain-expert Q6 발견)

- **위험 등급**: MEDIUM (사이클 26 보드 전환 시점 ↔ K stale watcher 동시 발화 race 잠재)
- **현 상태**: 사이클 26 (보드 전환) 도입 후 K stale watcher (120s 주기) 와 명시적 lock 없음. domain-expert (사이클 60 Phase 2-A 자문 Q6) 가 발견. 본 사이클 60 Phase 2-A1 의 범위 *밖* — 사이클 26 도입 시점 잠재 위험 가시화
- **권고**: `_board_transition_lock: asyncio.Lock` 신설 + 보드 전환 / K stale watcher / `_scan_loop` 모두 lock 경유 진입
- **회귀 가드**: 보드 전환 진행 중 K stale watcher 차단 + 큐 적재 (priority 보존)
- **선행 의뢰** (HIGH 의무): domain-expert — boards (PRE_NXT/MAIN/POST_NXT) 전환 timing + K stale watcher race 의 실제 발생 빈도 + 사이클 26 이후 사고 영향 점검
- **안전 규칙 충돌**: K stale watcher 우선순위 분리 (HIGH+bypass=True 보장)
- **예상 효과**: 잠재 race 영구 차단 + 보드 전환 atomicity

### 카드 #12 (신규, LOW ★ → MEDIUM 잠재) — 14 모듈 logger 이름 일관성 risk-review

- **위험 등급**: LOW (코드 행위 영향 0) ~ MEDIUM (운영 logging.yaml 설정 의존)
- **현 상태**: `src/engine/*.py` 16 모듈 중 14 모듈이 `__name__` logger 사용. stale_manager (사이클 60 hotfix 후) 만 `"src.engine.scheduler"` 명시 binding. 사이클 51 boot_manager 도 `__name__` 사용 (회귀 가드 없어 결함 가시화 안 됨)
- **잠재 위험**: 운영 logging.yaml 가 `src.engine.scheduler` 로거 한정 핸들러 (콘솔/파일/외부 모니터링) 설정한 경우 14 모듈의 prefix 로그 전부 누락
- **권고**: 운영 logging.yaml 정독 → (a) 한정 핸들러 사용 안 함 → 카드 폐기 (현 상태 안전), (b) 한정 핸들러 사용 → 14 모듈 모두 `"src.engine.scheduler"` 명시 binding 일괄 마이그레이션 또는 logging.yaml 핸들러 확장
- **회귀 가드**: 14 모듈 각각 logger.name 검증 (사이클 60 I1 패턴 답습 — `test_logger_is_scheduler_named`)
- **선행 의뢰**: backend-dev (운영 logging.yaml 정독) + refactor-expert (마이그레이션 vs 핸들러 확장 trade-off)
- **안전 규칙 충돌**: 사이클 28 G1 명세 (외부 모니터링 grep 호환 완전 보존) 일관성
- **예상 효과**: 운영 로그 누락 잠재 위험 영구 차단 + 사이클 51/60/61/62 모듈 분해 패턴 일관

---

## 사이클 60 Phase 2-A1 회고 (2026-06-04 17:00 KST)

- **계획 vs 실측**:
  - scheduler.py 라인 감소: 예상 -296L → 실측 **-275L** (-7.1%) — wrapper docstring 간결화
  - stale_manager.py: 예상 350~400L → 실측 358L (범위 내)
  - 회귀 가드: 예상 17 케이스 → 실측 **18** (I1 hotfix 영구 가드 추가)
  - StaleTrackerState 필드 수: 명세 "6 필드" → 실측 **7 필드** (G-1 정정 + `dataclasses.fields()` 자동 비교 가드)
  - PASS 카운트: 예상 1861 → 실측 **1862** (+1 I1)
- **flakiness 발견 (tester 카테고리 분리 측정 회수)**:
  - backend-dev Green 의 1861 PASS 일괄 측정 = caplog 상태 누수로 4 FAIL 가림
  - 사이클 58 V-2 회귀 패턴 답습 = 카테고리 분리 측정 의무화의 효과 입증
  - logger `__name__` → `"src.engine.scheduler"` hotfix 1줄 + I1 영구 가드 도입 (사이클 61 A2 / 62 A3 동일 결함 영구 차단)
- **domain-expert 자문 7 항목 적용 효과**:
  - Q4 옵션 B 3 단계 분할 채택으로 사이클 60 위험 LOW 한정 (HIGH 11 함수 일괄 이동 회피)
  - Q5 NXT 애프터 push 준수로 운영 영향 0
  - Q6 별개 카드 #11 발견 = 단일 사이클 단일 의도 원칙 보존
  - Q7 본질 차이 인지 (영구 hot path 360 회/일 vs boot 1회성) → A3 진입 시 별도 자문 + 주말 push 의무 인계
- **다음 사이클 권고 우선순위**:
  1. **사이클 61 = Phase 2-A2 MEDIUM** (4 함수 ~365L, 익일 새벽 push) — 카드 #2 진행분
  2. 사이클 62 = Phase 2-A3 HIGH (2 함수 ~321L, 주말 push 의무) — 카드 #2 완료
  3. 사이클 63 = 카드 #11 (Q6 보드 전환 mutex) 또는 카드 #12 (14 모듈 logger 일관성)
  4. 사이클 64+ = 카드 #3 settlement_manager (Phase 2-A3 완료 후 의존성 재평가)

---

## 사이클 62 사후 신규 카드 (2026-06-05 16:20 KST 발의)

### 카드 #13 (신규, MEDIUM ★★) — 거래대금 동행 필터 (가격 X 거래량) — domain-expert Q7 자문

- **위험 등급**: MEDIUM (작전주 차단 보강 — 가격 필터 단독 불충분)
- **현 상태**: 사이클 62 가격 필터 완료. 디폴트 OFF. 운영 1~2 영업일 데이터 축적 후 가격 필터 단독 효과 측정 가능
- **자문 발견 (Q7)**: 가격만으론 작전주 차단 불충분. 예: 5,000원 통과 + 거래대금 50억 (정상 5천만~수억) = 작전 신호 — 가격 필터로 못 잡음
- **권고**: `system_config` 추가 키 (`price_filter_min_trade_amount` 등) + `risk.on_tick` 분기 보강. 사이클 62 분기 *내부* 에 거래대금 추가 가드 (`scanner.ticker_acml_vol * cmp_price` 계산)
- **회귀 가드**: 사이클 62 38 케이스 패턴 답습 + 거래대금 fallback 검증 (실시간 누적 거래량 미확보 시 graceful 통과)
- **선행 의뢰** (MEDIUM 의무):
  - **domain-expert** — 작전주 거래대금 임계 (예: 10억? 30억? 100억?) 시장 미시구조 근거 + KOSPI/KOSDAQ 차별 + NXT 거래량 0 처리
  - **사이클 62 운영 1~2 영업일 데이터** — 가격 필터 단독 효과 측정 후 거래대금 보강 필요성 재확인
- **안전 규칙 충돌**: 사이클 38 명문화 (매수 진입 전용) 영속 + 사이클 62 Q3 가드 (시장가 폴백 무변경) 영속
- **예상 효과**: 작전주/이상 거래 차단 + 가격 필터 단독 결함 (저가 + 정상 거래량 통과 시 보호 0) 보완
- **사이클 분할 권고**: 가격 필터 (사이클 62) + 거래대금 필터 (사이클 63 또는 64) 분리 — 단일 사이클 단일 의도 원칙

### 사이클 62 회고 (2026-06-05 16:20 KST)

- **계획 vs 실측**:
  - 회귀 가드: 예상 31 → tester verify 보강 38 (+7) → 실측 **38** (자문 옵션 A 자동 정합)
  - DB 키: 2 → **3** (Q4 자문 mode 추가)
  - 위험 등급: MEDIUM (변경 없음, 자금 손실 위험 0)
  - PASS 카운트: 백 1882 → **1915** (+33), 프 160 → **167** (+7), 전체 **2082**
- **자문 옵션 A 채택 효과**:
  - **Q2 fallback** (team-leader 권고 반박): 갭상승 시점 작전주 회피 결함 영구 차단
  - **Q4 3 모드** (team-leader 권고 반박): 시장 신뢰 형성 패턴 (WARN 1주 → HARD 전환) — 운영자 임계 조정 편의
  - Q3 가드 (사용자 명시): order_engine 폴백 무변경 + E-4 AST 정적 회귀 가드 영속
- **사이클 60/61 패턴 영속 효과**:
  - 카테고리 분리 측정 + flakiness 3 회 반복 (사이클 58 V-2 + 사이클 60 hotfix 패턴) — 사이클 62 도 backend-dev mock 버그 1건 (`mocker.patch` → `monkeypatch.setattr`) 발견
  - logger 명시 binding (사이클 60 I1): 사이클 62 risk.py 는 기존 logger 그대로 — 사이클 60 I1 적용 영역 외
  - 사이클 56-D `RiskManager.reset_daily_state()` 캡슐화: cap 3 + 카운터 + reasons dict 일괄 reset (사이클 62 확장)
- **V12 신규 검증 영역 도입** (tester): 백엔드 `ApiResponse<T>` 래퍼 ↔ 프론트 `data.data` 추출 정합성 100% 확인 — 사이클 62 백엔드 + 프론트 단일 사이클 패턴의 영구 검증 항목 (사이클 63+ 신규 기능 사이클에도 적용 의무)
- **다음 사이클 권고 우선순위**:
  1. **사이클 63 = Phase 2-A3 HIGH** (`_check_and_resubscribe_stale` 221L + `_resubscribe_stale_priority` 100L) — 주말 push 의무 + 월요일 1h verify
  2. 사이클 64 = 카드 #13 거래대금 동행 필터 (사이클 62 운영 1~2 영업일 데이터 축적 후)
  3. 사이클 65 = 카드 #11 (보드 전환 mutex Q6) 또는 카드 #12 (14 모듈 logger 일관성)
  4. 사이클 66+ = 카드 #3 settlement_manager (Phase 2-A3 완료 후 의존성 재평가)

---

## 사이클 63 사후 신규 카드 (2026-06-06 토 18:00 KST 발의)

### 카드 #5 (신규, HIGH ★★★) — `_resubscribe_stale_priority` cap=10 결함 시정 (Q5-3 자문)

- **위험 등급**: HIGH (보유/익일청산 HIGH 종목이 cap=10 밖 잘림 가능 — KIS LMS chain 직접 영역)
- **현 상태 (사이클 63 K-2 PASS = 결함 confirm 영속)**:
  - `_resubscribe_stale_priority` 본체 L2748 `targets = stale_tickers[:cap]` (cap=10) → priority 분리 *전* 적용
  - 결함 시나리오: `stale_tickers` = [LOW 후보 1, ..., LOW 후보 9, HIGH 보유 1] sorted 순서일 때 `[:cap]` 적용 시 LOW 9 종목 우선 + HIGH 1 종목만 포함 → HIGH 종목 cap 밖 잘림
  - K-2 케이스 = 결함 영속 확인 (PASS = 결함 confirm)
- **권고 시정 안**:
  ```python
  # 1. priority 분리 *먼저*
  high_targets = [t for t in stale_tickers if t in high_tickers]
  low_targets = [t for t in stale_tickers if t not in high_tickers]
  # 2. cap 적용 — HIGH 먼저 보장 + LOW 잔여 cap
  targets = high_targets + low_targets[:max(0, cap - len(high_targets))]
  ```
- **회귀 가드**: K-2 케이스 갱신 (현재 PASS = 결함 confirm → Green 후 PASS = 시정 confirm) + 신규 케이스 (HIGH 11 종목 + LOW 11 종목 = 22 stale 시 HIGH 11 + LOW (cap-11) 보장 검증)
- **선행 의뢰**: domain-expert — 시정 안의 KIS 풀 API 인터페이스 영향 평가 (cap 초과 시 KIS LMS 리스크 평가)
- **안전 규칙 충돌**: K stale watcher 우선순위 분리 (사이클 29-R3) — HIGH+bypass=True 보장 영속
- **예상 효과**: 보유/익일청산 HIGH 종목 cap 잘림 결함 영구 차단 + 사이클 29 005935 사고 영역 보호 강화

### 카드 #14 (신규, MEDIUM ★★) — `stale_manager.py` 1,076L sub-module 분해 (Q5-2 자문)

- **위험 등급**: MEDIUM (행위 보존 리팩토링 + sub-module 경계 신중 설계 의무)
- **현 상태 (사이클 63 후)**:
  - `stale_manager.py` 1,076L = 사이클 60 A1 358 + 사이클 61 A2 373 + 사이클 63 A3 345
  - 11 함수 + 10 상수 단일 모듈 — 책임 4 영역 혼재
- **권고 sub-module 분해 (3+1 청사진, domain-expert 자문)**:
  | sub-module | 라인 (예상) | 책임 영역 |
  |---|---|---|
  | `stale_diagnostics.py` | ~350L | 진단 read-only (build_session_subscription_view / emit_stale_session_detail / refresh_stale_ccnl_cache / evict_expired_ccnl / prune_force_retry_history) — 사이클 60 A1 |
  | `stale_session_recovery.py` | ~250L | 세션 단위 silent inactive + force reconnect (detect_silent_inactive_sessions / force_reconnect_session / delta_unsubscribe_dropped) — 사이클 61 A2 일부 |
  | `stale_universe_guard.py` | ~150L | universe 가드 (evaluate_universe_guard) — 사이클 32 R4 |
  | `stale_watcher_core.py` | ~322L | K stale watcher 본체 (check_and_resubscribe_stale + resubscribe_stale_priority) — 사이클 63 A3 |
- **회귀 가드**: A1+A2+A3 누적 67 케이스 영속 + 신규 sub-module 경계 검증 (4 모듈 import sanity / 5 상수 re-export 호환 / property 7 쌍 layer 호환)
- **선행 의뢰**:
  - **domain-expert** — sub-module 경계 (4 영역) 의 행위 영향 평가 (특히 stale_watcher_core 가 stale_diagnostics 호출 시 Q4=B 직접 호출 패턴 영속)
  - **tdd-engineer** — 4 모듈 신규 import sanity + 위임 1 회 + re-export 호환 회귀 가드 사전 작성
- **안전 규칙 충돌**: K stale watcher 4 중 안전망 행위 보존 절대 + Q4=B 직접 호출 패턴 영속
- **예상 효과**: stale_manager.py 1,076L → 4 모듈 평균 ~268L (응집도 향상 + 차기 검토 비용 ↓)
- **사이클 분할 권고**: 사이클 65+ 단독 사이클 (카드 #5 cap=10 결함 시정 후) — 4 모듈 분해는 *단일 의도* 1 사이클로 가능

### 사이클 63 회고 (2026-06-06 토 18:00 KST)

- **계획 vs 실측**:
  - 회귀 가드: 예상 25 → 실측 **29** (G/H/I HIGH + J 60분 윈도우 +4)
  - 라인 감소: 예상 -315L → 실측 -308L (wrapper 추가 7L 차이)
  - 누적 scheduler.py: 사이클 51 직전 ~4,185 → 사이클 63 후 3,007 = **-1,178L, -28%**
  - stale_manager.py: 358 (A1) → 731 (A2) → 1,076 (A3) = +718L 누적
- **사이클 60 답습 안 함 유일 영역 (Q4=B 직접 호출)**:
  - 사이클 60 A1 = wrapper 위임 + sys.modules.get (간접)
  - 사이클 61 A2 = wrapper 위임 + sys.modules.get (간접)
  - 사이클 63 A3 = **직접 호출** + sys.modules.get (간접) — `emit_stale_session_detail(scheduler, ...)` 직접
  - 1 hop 단축 + lazy import 빚 1 건 청산 + 사이클 61 backend-dev Green `evict_expired_ccnl` 패턴 일관
- **flakiness 차단 패턴 영속 효과 (사이클 60 hotfix 발견)**:
  - backend-dev Green 단계 = 기존 patch 경로 깨짐 2 파일 자동 발견 (`test_scheduler_stale_force_retry.py` + `test_scan_loop_stale_priority.py`) → 즉시 시정
  - V10 flakiness 3 회 반복 = 35.54s / 35.47s / 36.62s (±1.15s 안정) — 사이클 58 V-2 + 사이클 60 hotfix 패턴 영구 절차화
- **V-A3 + V-1h-prep 신규 도입 (tester)**: HIGH 11 케이스 전수 우선 + 월요일 1h verify 시나리오 사전 명세 점검 = 영구 hot path 영역 안전성 검증 신규 분류
- **사이클 29 005935 사고 패턴 차단 영속**: H 4 케이스 freezegun 재현 PASS = T+18min 영구 잔류 + KIS LMS chain 차단 영구 보장
- **다음 사이클 권고 우선순위**:
  1. **사이클 64 = 카드 #5 (HIGH, cap=10 결함 시정)** — Q5-3 결함 confirm 후 즉시 시정 우선 (보유/익일청산 HIGH 종목 cap 잘림 위험)
  2. 사이클 65+ = 카드 #14 (MEDIUM, stale_manager.py 1,076L sub-module 분해 3+1) — 카드 #5 시정 후
  3. 사이클 66+ = 카드 #13 (MEDIUM, 거래대금 동행 필터) — 사이클 62 운영 1~2 영업일 데이터 축적 후
  4. 사이클 67+ = 카드 #11 (보드 전환 mutex Q6) 또는 #12 (14 모듈 logger 일관성)

---

## 사이클 64 사후 신규 카드 (2026-06-06 토 19:00 KST 발의)

### 카드 #15 (신규, LOW ★) — 액면분할/정정공시 prdy_clpr 즉시 invalidate (Q7-2 자문)

- **위험 등급**: LOW (사이클 64 graceful 통과 영속 → silent 영향만, 자금 손실 위험 0)
- **현 상태 (사이클 64 후)**:
  - `stock_master.raw.prdy_clpr` 24h TTL 캐시 (기존)
  - 사이클 64 가격 필터가 `prdy_clpr` 단독 사용 (Q2 자문)
  - 액면분할/정정공시 발생 시 (예: 액면 분할 1:5 → prdy_clpr 1/5 로 갱신) 24h TTL 만료 전까지 *구 prdy_clpr* 사용
- **영향**:
  - 액면 분할 후 즉시 가격 필터 범위 외 진입 (예: 5,000원 → 1,000원, 임계 min=2,000원 시 차단) → 보유 미해당, 신규 매수만 차단 (graceful)
  - 자금 손실 위험 0 (보유 종목은 사이클 64 옵션 D 3 중 안전망으로 절대 보호)
  - 사이클 32 R4 universe guard 와 일관 (24h TTL 영속)
- **권고 시정 안 (별개 사이클)**:
  ```python
  # stock_master.upsert_one(StockBasics) 호출 시 prdy_clpr 변동 감지 → scanner.invalidate_price_filter_cache_scanner()
  if existing and existing.raw.get("prdy_clpr") != stock.raw.get("prdy_clpr"):
      from src.engine import scanner
      scanner.invalidate_price_filter_cache_scanner()  # 다음 _scan_loop 5분 자연 delta
  ```
- **회귀 가드**: stock_master.upsert_one 시 prdy_clpr 변동 감지 → invalidate 1회 호출 검증
- **선행 의뢰**: 없음 (LOW)
- **안전 규칙 충돌**: 사이클 64 Q7-1 자동 unsubscribe 0 발화 영속 (invalidate 만 호출, unsubscribe 직접 호출 금지)
- **예상 효과**: 액면분할/정정공시 즉시 반영 (24h TTL 대신 즉시) — silent 영향 영구 차단
- **사이클 분할 권고**: 사이클 67+ (사이클 65 거래대금 필터 + 사이클 66+ 카드 #5 cap=10 결함 + 사이클 67+ 카드 #14 sub-module 분해 후)

### 사이클 64 회고 (2026-06-06 토 19:00 KST)

- **계획 vs 실측**:
  - 회귀 가드: team-leader 20 → 도메인 24 → tdd-engineer 실측 26 → tester verify 결함 + hotfix → 최종 **28 케이스** (+2 = H-2 + G-3 hotfix 영구 가드)
  - 라인 변경: scheduler.py 3,007 → 3,020 (+13L, hotfix + reset 동행) / risk.py -178L (가격 필터 폐기) / scanner.py +250L (신규 영역)
  - PASS 카운트: 백엔드 1944 → 1939 (사이클 62 -34 + 사이클 64 +28 + hotfix +2 -1 = 정합)
  - 사이클 62 폐기 9 파일 + 33 케이스 + 프론트 F-5 = 34 정리
- **사용자 의도 재정의 패턴**:
  - 사이클 62 결정 후 1 일만에 위치 변경 — 처음 AskUserQuestion 시 사용자 = "scanner 조건검색 직후" 선택했으나 team-leader 1차 권고로 `risk.on_tick` 으로 구현됨
  - 사이클 64 = 사용자 진정 의도 (WS 구독 *전*) 회복
  - **교훈**: team-leader 1차 권고 vs 사용자 명시 의도 우선 — 향후 의도 재정의 시 *cleanup 비용* 인지 (사이클 62 38 케이스 → 사이클 64 28 신규 + 34 폐기 = 62 cleanup 비용)
- **사이클 60 hotfix 패턴 영속 효과 (사이클 60/61/63 후 4 번째)**:
  - tester 카테고리 분리 측정 + flakiness 3 회 반복 = 사이클 64 결함 1건 발견 (HIGH 운영 가시화)
  - backend-dev 2 줄 hotfix + tdd-engineer H-2 + G-3 영구 가드 (AST 3 중) = silent 결함 영구 차단
  - 사이클 60 패턴 답습 효과 = 누적 4 사이클 결함 발견 (사이클 58 V-2 / 사이클 60 hotfix / 사이클 64 hotfix)
- **사이클 38 명문화 + 사이클 32 R4 universe guard 패턴 답습 효과**:
  - Q1 옵션 D 3 중 안전망 = 사이클 32 R4 보유/익일청산 보호 패턴 답습
  - 사이클 38 명문화 = 사이클 64 scanner 단계 = 매수 진입 전용 영속 (매도/익일청산/손절 영향 0)
- **다음 사이클 권고 우선순위**:
  1. **사이클 65 = Q7-5 거래대금 동행 필터 (HIGH 즉시 발주 확정)** — 사이클 62 갭상승 회피 효과 영구 폐기 위험 보강
  2. 사이클 66+ = 카드 #5 (HIGH) cap=10 결함 시정 (사이클 63 발견)
  3. 사이클 67+ = 카드 #14 (MEDIUM) stale_manager.py 1,076L sub-module 분해
  4. 사이클 68+ = 카드 #15 (LOW) Q7-2 액면분할 prdy_clpr invalidate
  5. 사이클 69+ = 카드 #13 (MEDIUM) 거래대금 동행 필터 (사이클 65 와 통합 또는 별개)
  6. 사이클 70+ = 카드 #11/#12 보드 전환 mutex / 14 모듈 logger

---

## 사이클 65 사후 신규 카드 (2026-06-06 토 22:00 KST 발의)

### 카드 #16 (신규, MEDIUM ★★) — 사이클 64+65 후보 풀 폭축 역설 risk 2주 회고 (Q6-4 자문)

- **위험 등급**: MEDIUM (운영 데이터 통계적 유의성 확보 후 임계 미세 조정)
- **현 상태 (사이클 65 후)**:
  - 사이클 64 가격 필터 + 사이클 65 거래대금 필터 양쪽 활성화 시 후보 풀 *과도 축소* 위험
  - 사이클 64 시뮬: 사이클 28 실측 73% 편중 → 추정 17~18% 차단 (가격 필터 단독)
  - 사이클 65 활성화 시: 추가 차단 가능 (거래대금 임계에 따라)
  - 양쪽 활성화 = scanner 후보 풀이 *너무* 축소되어 매매 신호 발생 0건 위험
- **권고 시정 안 (2주 회고 후 결정)**:
  1. 운영 2주 후 `[price_filter_scanner_daily_summary]` + `[trade_amount_filter_scanner_daily_summary]` 일일 집계 통계 분석 (system_logs DB 쿼리)
  2. 차단 비율 측정 (가격 차단 / 거래대금 차단 / 양쪽 차단 / 정상 통과)
  3. 신호 발생 빈도 측정 (사이클 65 활성화 전후 비교)
  4. 사용자 결정: 임계 미세 조정 (예: 가격 max 1,000,000 → 1,500,000 완화 / 거래대금 min 5억 → 3억 완화) 또는 한쪽 비활성화
- **회귀 가드**: 본 카드 = 운영 분석 카드 (코드 변경 0). 분석 결과 기반 신규 사이클 발의 시 추가 가드 작성
- **선행 의뢰** (MEDIUM): domain-expert — 후보 풀 폭축 임계 (예: 정상 후보 풀의 50% 이하 = 위험 신호) + 통계적 유의성 기준
- **안전 규칙 충돌**: 사이클 38 명문화 영속 (매수 진입 전용) + 사이클 32 R4 보호 영속
- **예상 효과**: 사이클 64+65 운영 데이터 기반 임계 미세 조정 + 후보 풀 폭축 결함 영구 차단
- **사이클 분할 권고**: 사이클 67+ (운영 2주 후 활성화 + 데이터 축적, 카드 #5 + 카드 #14 후순위)

### 사이클 65 회고 (2026-06-06 토 22:00 KST)

- **계획 vs 실측**:
  - 회귀 가드: team-leader 23 → 도메인 28 → tdd-engineer 실측 31 함수 (28 케이스, B-5/G-1/H-2 함수 분리)
  - 라인 변경: scanner.py +234L (5 함수 + 4 모듈 전역 + 4 hook + ticker_market_info 신규 키) / scheduler.py +10L (emit + reset) / system_config.py +57L (TradeAmountFilter + 2 함수) / routes/system.py +60L
  - PASS 카운트: 백엔드 1939 → 1970 (+31 = 신규 26 + 사이클 64 vacuous PASS 5 진정한 PASS 전환)
  - 사이클 64 vacuous PASS 5 진정한 PASS 전환 = 부가 효과 (사이클 65 hook 추가로 사이클 64 G-2 + H-2 호출 발생)
- **사이클 64 패턴 100% 답습 효과 (사이클 60 hotfix 영구 가드 패턴 영속)**:
  - logger 명시 binding (`logging.getLogger("src.engine.scheduler")`) — 사이클 60 I1 영구 가드 답습
  - 카테고리 분리 측정 + flakiness 3 회 (사이클 58 V-2 + 사이클 60 hotfix 패턴) = 결함 0건 발견
  - sys.modules.get (사이클 61 도입) + AST 정적 가드 (사이클 60/61/63/64 답습)
  - H-2 NO_TRY (사이클 64 hotfix 영구 가드) = silent 결함 영구 차단
  - G AST keyword 의무 (사이클 64 G-2 답습)
- **domain-expert 자문 6 사이클 연속 옵션 A 채택 영속 효과**:
  - 사이클 55 R-1 / 60 / 62 / 63 / 64 / 65 모두 옵션 A — *team-leader 1차 권고 반박 영역 모두 채택* 패턴
  - 사이클 65 Q1 (0~5,000억 → 0~100억 반박) + Q2 (옵션 A → 옵션 C 통합 폴백 반박) = team-leader 1차 권고 100% 반박 채택
- **사이클 62 폐기 cleanup 비용 회수 효과** (사이클 64 회고 인계):
  - 사이클 62 38 케이스 → 사이클 64 28 + 사이클 65 28 = 56 신규 (장기 누적 효과)
  - 사이클 62 폐기 cleanup 비용 회수 = 사이클 64 + 65 의 합산 효과 (사이클 62 위치 변경 + 사이클 65 보강)
- **Q2 옵션 C 통합 폴백 핵심 발견 (domain-expert)**:
  - team-leader 1차 권고 = 옵션 A 단독 (`stock_master.raw` 만) — KIS MCP §8 검증 결과 CTPF1002R 응답 필드 확정 불가 → 사실상 100% graceful 통과 = 필터 무용
  - 도메인 반박 = 옵션 C 통합 폴백 — scanner 가 이미 `fetch_rising_stocks::fetch_stock_detail` 호출 + `acml_tr_pbmn` 보강 중 (KIS 호출 0건 추가)
  - 신규 키 `ticker_market_info["trade_amount_raw"]` 추가 = 코드 정독으로 발견된 *기존 인프라 재활용*
- **다음 사이클 권고 우선순위**:
  1. **사이클 66 = 카드 #5 (HIGH)** cap=10 결함 시정 (사이클 63 발견, 영구 hot path 영역)
  2. 사이클 67+ = 카드 #14 (MEDIUM) stale_manager.py 1,076L sub-module 분해 (3+1 청사진)
  3. 사이클 67+ = **카드 #16 (MEDIUM, 신규 발의)** Q6-4 후보 풀 폭축 역설 risk 2주 회고
  4. 사이클 68+ = 카드 #15 (LOW) Q7-2 액면분할 prdy_clpr invalidate
  5. 사이클 69+ = 카드 #11/#12 보드 mutex / 14 모듈 logger
