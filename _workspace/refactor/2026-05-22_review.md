# 리팩토링 리뷰 — 2026-05-22

> 사이클 28~43 누적 16 사이클 종료 후 사용자 요청: "현재 프로젝트 시스템 리뷰 + 중복 제거 + 메모리 최적화 + KIS API 표준 점검"
> refactor-review 스킬 5 계명 (테스트 없는 리팩토링 금지 / CLAUDE.md 절대 규칙 우선 / 3 회 반복 = 추상화 후보 / 주석 정책 보존 / 카드 단위 분할) 준수.

## 검토 범위
- 검토 사이클: 28~43 (5/21~5/22 누적 16 사이클, 백엔드 1495→1671 PASS, +176 회귀)
- 검토 디렉토리: `src/engine/` (scheduler/order_engine/strategies), `src/realtime/`, `src/api/`, `src/db/`, `frontend/`
- 활용 도구: `wc -l` / `grep` / `Agent(Explore)` 정독 + 통계 / KIS MCP (카드 #4 사전 조사 영역)

## 누적 통계 요약

### 모듈 비대화
| 파일 | 라인 수 | 80L+ 함수 | 최대 함수 |
|---|---|---|---|
| **scheduler.py** | **3,906** | **21** | `start()` 373L / `_boot()` 274L / `_check_and_resubscribe_stale()` 231L |
| order_engine.py | 998 | 2 | `execute_buy/sell()` 280L+ |
| donchian_swing.py | 760 | 다수 | funnel hook 24 회 |
| vcp_breakout.py | 752 | 다수 | funnel hook 24 회 |
| bull_flag_breakout.py | 714 | 다수 | funnel hook 12 회 |
| websocket.py | 622 | 1 | `_is_rejection_response()` |
| websocket_pool.py | 602 | 1 | `_enforce_main_only_execution_notice()` |

### 중복 패턴 카운트
- 종목명 lookup 4 경로 (`ticker_names` / `STATIC_TICKER_NAMES` / `_resolve_ticker_name` / `t()`): 18+ 곳
- KIS REST `output[...]` 직접 인덱싱: 11+ 곳
- `datetime.now(KST_TZ)` / KST 처리: 43 곳 (모듈별 import 일관 — 사이클 36 통일)
- `_record_funnel_step` 호출: BFB+VCP+donchian × 8 단계 = **24 회**
- stale 추적 dict: scheduler.py 에 **7 필드** 산재

### 메모리 누수 위험
- `_stale_force_retry_history` — 60분 sliding window manual evict 검증 필요 (MEDIUM)
- `_last_ccnl_cache` — TTL 5분 자동 evict 분기 검증 필요 (MEDIUM)
- 그 외 8 dict/set 모두 `_reset_daily_state` 동행 clear 확인됨 (LOW)

### 5분 주기 emit task
3 개 분산 — 일일 ~936 KB system_logs 누적
- `_stale_watcher_loop()` (120s)
- `_report_tick_coverage()` (300s)
- `_heartbeat_metrics_loop()` (300s, 사이클 42 신규)

---

## 권고 카드 7 장

### 카드 #1 — scheduler.py 모듈 분해 (HIGH ★★★)
- **위험 등급**: HIGH (모듈 간 경계 + 행위 영향 가능)
- **현 상태**: `src/engine/scheduler.py` 3,906 L / 21 개 80L+ 함수 / 5 개 책임 혼재 (boot / stale watcher / swing poll / settlement / universe guard)
- **권고**:
  - `scheduler.py` (메인 진입 + 시간 분기 + lifecycle)
  - `scheduler/boot_manager.py` (`_boot()` + `_preissue_all_tokens` + 사전 검증)
  - `scheduler/stale_manager.py` (`_check_and_resubscribe_stale` + force_retry + universe_guard + ccnl cache)
  - `scheduler/swing_manager.py` (`_swing_buy_poll_loop` + `_run_swing_rest_poll_once`)
  - `scheduler/settlement_manager.py` (`_settle` + `_execute_next_day_clear` + `_drain_pending_next_day_clear`)
- **회귀 가드**: 기존 1671 PASS 전체 + 신규 통합 테스트 필요
- **선행 의뢰** (HIGH 의무): domain-expert — 분해가 매매 안전성 영향 평가 (체결통보 / 익일청산 / `_reset_daily_state` 호출 시점 보존 검증)
- **안전 규칙 충돌 사전 점검**: 체결통보 구독 / `_reset_daily_state` / 익일 청산 / WebSocket 4중 안전망 호출 시점 변경 금지
- **예상 효과**: 각 모듈 < 600L, 책임 분리, 사이클 변경 비용 감소, 가독성 향상

### 카드 #2 — Stale 추적 dict 통합 (MEDIUM ★★)
- **위험 등급**: MEDIUM (모듈 내부 구조)
- **현 상태**: `src/engine/scheduler.py:186-225` 에 7 개 stale 필드 산재 (사이클 28/29-R1/29-R2/32/37 누적). 관계 불명시 → 사이클 추가 시 reset 동행 누락 사고 위험
- **권고**: `StaleTrackerState` 데이터클래스 추출
  ```python
  @dataclass
  class StaleTrackerState:
      retry_count: dict[str, int]
      last_resubscribe_at: dict[str, datetime]
      force_retry_history: dict[str, list[datetime]]
      silent_inactive_first_seen: dict[str, datetime]
      silent_inactive_recovery_count: dict[str, list[float]]
      universe_excluded_today: set[str]
      last_ccnl_cache: dict[str, dict]

      def reset_daily(self) -> None:
          """모든 dict/set 일괄 clear (one-liner)."""
  ```
- **회귀 가드**: 사이클 28~37 stale 회귀 49 케이스 + 신규 통합 단위
- **선행 의뢰**: 카드 #5 TTL 캐시 검증 *우선* (의존)
- **안전 규칙 충돌**: `_reset_daily_state` 동행 clear 보존 의무 (절대)
- **예상 효과**: 상관관계 명시 + reset 누락 사고 차단 + 사이클 44+ 신규 추적 필드 추가 비용 감소

### 카드 #3 — Funnel hook DRY (MEDIUM ★★)
- **위험 등급**: MEDIUM (전략 내부)
- **현 상태**: BFB / VCP / donchian 각 8 단계 = **24 회 `_record_funnel_step` 반복** (사이클 39+41 누적). step_no / step_name / step_conditions 상수가 코드 중 분산
- **권고**: `StrategyBase._run_funnel_pipeline(stages: list[FunnelStage])` 헬퍼
  ```python
  @dataclass
  class FunnelStage:
      step_no: int
      step_name: str
      step_conditions: str
      filter_fn: Callable[[Ticker], tuple[bool, str | None]]  # (passed, reason)
  ```
- **회귀 가드**: 사이클 39+41 회귀 22 케이스 + 신규 통합 (각 전략 8 단계 일관성)
- **안전 규칙 충돌**: 퍼널 카운터 초기화 시점 (`20:10 정산 전 수집` + `_reset_daily_state` 후 reset) 보존
- **예상 효과**: 24 회 반복 → 3 stages 정의로 축소, 새 전략 추가 시 funnel hook 자동 적용, 단계 추가/변경 시 한 곳에서

### 카드 #4 — KIS 응답 파싱 통일 (HIGH ★★★, KIS MCP 검증 후)
- **위험 등급**: HIGH (KIS 응답 변형 영향 가능)
- **현 상태**: `output[...]` 직접 인덱싱 11+ 곳. 사이클 33 `prdy_vol` 누락 사고 잠재 위험
- **사전 조사** (KIS MCP):

  | TR_ID | 용도 | 우선순위 |
  |---|---|---|
  | FHKST01010100 | 주식현재가 시세 (BFB `_scan_universe`) | 1 |
  | FHKST03010100 | 일봉 차트 (VCP `fetch_daily_candles`) | 1 |
  | FHPST01700000 | 등락률 순위 (BFB + momentum) | 2 |
  | FHKST01010300 | 주식현재가 체결 (사이클 32 R4 `inquire_ccnl`) | 2 |
  | CTPF1002R | 종목 기본정보 (`stock_master`) | 3 |

- **권고**: `src/models/kis_responses.py` 에 pydantic 모델 통합
  ```python
  class FHKST01010100Output(BaseModel):
      stck_prpr: int
      acml_vol: int     # 사이클 33 fix 후 정확 키
      lstn_stcn: int
      prdy_vrss: int
      # ... (KIS MCP 정본 검증 후 키 확정)
  ```
- **회귀 가드**: respx + KIS MCP 정본 응답 시리즈 합성
- **선행 의뢰**: tdd-engineer — 5 TR_ID 응답 시리즈 회귀 가드
- **안전 규칙 충돌**: TR_ID 실전↔모의 자동 변환 보존 (`settings.get_tr_id()`)
- **예상 효과**: 사이클 33 같은 결함 영구 차단, 응답 변경 시 단일 모델 갱신만

### 카드 #5 — TTL 캐시 자동 evict 검증 (MEDIUM ★★)
- **위험 등급**: MEDIUM (메모리 누수)
- **현 상태**:
  - `_last_ccnl_cache` (TTL 5분) — `src/engine/scheduler.py:3456` 등록, `:3863` daily clear. **5분 경과 시 자동 제거 분기 검증 필요**
  - `_stale_force_retry_history` (60분 sliding window) — 매 호출 manual evict 추정. 실제 evict 로직 검증
- **권고**: 코드 추적 후 자동 evict 부재 시 추가
  ```python
  def _evict_expired_ccnl(self, now: datetime, ttl_secs: float = 300) -> None:
      cutoff = now - timedelta(seconds=ttl_secs)
      self._last_ccnl_cache = {
          t: c for t, c in self._last_ccnl_cache.items()
          if c["fetched_at"] > cutoff
      }
  ```
- **회귀 가드**: freezegun 으로 TTL 만료 시뮬레이션 + 5/22 운영 데이터 검증
- **안전 규칙 충돌**: 캐시 hit/miss 정책 변경 금지 (daily clear 우선)
- **예상 효과**: 영업일 중 캐시 폭주 차단 (운영 1주 누적 검증 필수)

### 카드 #6 — 5 분 주기 emit task 통합 (MEDIUM ★★)
- **위험 등급**: MEDIUM (task lifecycle)
- **현 상태**:
  - `_stale_watcher_loop` (120s)
  - `_report_tick_coverage` (300s, 사이클 28)
  - `_heartbeat_metrics_loop` (300s, 사이클 42)
  - 일일 ~936 KB system_logs 누적
- **권고**: 단일 `_session_health_loop()` (300s) 로 5 분 주기 통합. 120s `stale_watcher` 는 보존 (재시도 빈도 우선)
- **회귀 가드**: 사이클 28/42 회귀 + emit 순서 일관성
- **안전 규칙 충돌**: 사이클 24 silent_inactive 시간당 2 회 cap 보존
- **예상 효과**: 로그 정렬 + sleep 중복 제거 + emit 순서 일관 + system_logs row 약 30% 감소 예상

### 카드 #7 — 종목명 lookup 단일 헬퍼 (LOW ★)
- **위험 등급**: LOW (명명/단일 헬퍼)
- **현 상태**: 18+ 곳에서 `ticker_names` + `STATIC_TICKER_NAMES` + `_resolve_ticker_name` + `t()` 4 경로 혼재
- **권고**: `scanner.py` 에 `resolve_ticker_name(ticker) -> str` 단일 헬퍼 (사이클 41 `_resolve_ticker_name` 확장)
- **회귀 가드**: 기존 lookup 결과 동일성
- **예상 효과**: 4 경로 → 1 경로, 일관성

---

## 비권고 사항 (검토 후 카드 미발의)

| 영역 | 사유 |
|---|---|
| `order_engine.execute_buy/sell` 280L+ | 매수/매도 race 가드 + 체결통보 매핑 + 시장가 폴백 등 *순차 의존성* 강해 분할 시 회귀 위험 큼. CLAUDE.md 절대 규칙 다수 보호 |
| `websocket._is_rejection_response` 557L | 거절 키워드 판별 — KIS 응답 변형 흡수 영역. 분할 시 일관성 깨지기 쉬움 |
| `websocket_pool._enforce_main_only_execution_notice` 541L | 체결통보 메인 강제 안전 규칙 — *절대* 위반 금지 영역 |
| 시간/KST 처리 43 곳 | 모듈별 import 일관 (사이클 36 통일). 단일 헬퍼 폐기 필요 없음 |
| 테스트 파일 242 개 | 사이클별 분산 정렬 — 갱신 폭주 신호 없음 |

---

## 후속 의뢰

| 대상 | 영역 | 의뢰 사항 |
|---|---|---|
| **tdd-engineer** | 카드 #4 KIS 응답 회귀 가드 | 5 TR_ID 정본 응답 시리즈 — respx + freezegun 합성 |
| **tdd-engineer** | 카드 #2 stale 통합 회귀 | 49 케이스 의미 갱신 + 신규 reset 정합성 |
| **domain-expert** | 카드 #1 scheduler 분해 | 매매 안전성 영향 평가 (체결통보 / 익일청산 / WebSocket 안전망 호출 시점) |
| **domain-expert** | 카드 #4 KIS 모델 통합 | 응답 키 누락 시 매매 영향 평가 |
| **team-leader** | 7 카드 채택/보류/폐기 결정 | 본 메모 기반 사용자 의사 반영 |

---

## 절대 안전 규칙 보호 (사전 격리)

CLAUDE.md 14 종 절대 규칙 — 본 검토에서 카드 발의 사전 배제:
- 체결통보 구독 / uvicorn 단일 워커 / 주문번호 매핑 / 체결통보 race 가드 / `_reset_daily_state` / 익일 청산 / NXT 매도 좀비 / 시장가 거부 폴백 / KIS 거부 영구 저장 / WebSocket 4 중 안전망 / NXT 거래가능 사전 판별 / 종목코드 6 자리 / 1주 폴백 / VB 15:20 일괄청산 / donchian REST 폴링 / KST 강제

각 카드 *안전 규칙 충돌* 영역 사전 명시.

---

## 다음 단계 (사용자 결정)

| 옵션 | 진행 카드 | 예상 사이클 | 비고 |
|---|---|---|---|
| A | #5 + #6 | 44 | MEDIUM 즉각, 위험 낮음 + 효과 즉각 |
| B | #4 (KIS MCP 검증 후) | 44~45 | 미래 KIS 변경 결함 영구 차단 |
| C | #1 (scheduler 분해) | 44~47 | HIGH, domain-expert 자문 필수, 큰 작업 |
| **D** | **전체 7 카드 순차** | **44~50 (1~2 주)** | **LOW (#7) → MEDIUM (#5/#6/#3/#2) → HIGH (#1/#4)** ← **선택** |
| E | 메모만 보존 | 사이클 44 종료 | refactor-review 메모만 영구 보존, 채택 보류 |

---

## 진행 상황 (옵션 D)

| 사이클 | 카드 | 상태 | 회귀 / 신규 테스트 | 비고 |
|---|---|---|---|---|
| 44 | #7 LOW 종목명 lookup 단일 헬퍼 | **완료** | 백엔드 1681 PASS (+10 신규) | `scanner.resolve_ticker_name` + `_resolve_ticker_name` 위임 layer. condition.py/order_engine.py/scheduler.py 의 18+ 호출처 마이그레이션은 별도 후속 카드 (회귀 위험) |
| 45 | #5 MEDIUM TTL 캐시 자동 evict | **완료** | 백엔드 1691 PASS (+10 신규) | `_evict_expired_ccnl` (사이클 37 TTL 5분 정책 보존) + `_prune_force_retry_history` (사이클 29-R1 sliding window 정책 보존). 빈 list 자동 제거. `_reset_daily_state` 동행 clear 이중 안전망 |
| 46 | #6 MEDIUM 5분 emit task 통합 | 발주 대기 | — | — |
| 47 | #3 MEDIUM Funnel hook DRY | 발주 대기 | — | — |
| 48 | #2 MEDIUM Stale 추적 dict 통합 | 발주 대기 | — | — |
| 49 | #1 HIGH scheduler 분해 | 발주 대기 (domain-expert 자문) | — | — |
| 50 | #4 HIGH KIS 응답 파싱 통일 | 발주 대기 (KIS MCP 검증) | — | — |
