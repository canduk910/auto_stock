# 리팩토링 리뷰 — 2026-06-14 (사이클 130)

## 검토 범위

- **검토 사이클**: 사이클 68~129 (62 사이클 누적, CLAUDE.md "5 사이클 누적" 기준 12.4배 초과)
- **이전 refactor-review 종결 사이클**: 67 (`stale_manager.py` 1,099L → facade 96L + 4 sub-module 분해, 카드 #14 종결)
- **사용자 결정**: Q1=A (refactor-review 단독) / Q2=E (refactor-expert 자율) / Q3=A (HIGH 카드만 domain-expert) / Q4=A (권고만 + 사이클 131+ Green 별도)
- **검토 시 활용한 도구**: grep + wc -l + Read + CLAUDE.md 인용

## 검토 대상 정량 데이터

| 파일 | 라인 | 사이클 67 기준 | 누적 사이클 |
|------|------|---------------|------------|
| `src/engine/scheduler.py` | **3,497L** | 3.18배 초과 (최대) | 122/126/127/129 |
| `src/engine/scanner.py` | **2,681L** | 2.44배 초과 | 126/127/128/129 +600L |
| `src/db/stock_master.py` | 597L | - | 124/128/129 |
| `src/routes/stock_master.py` | 372L | - | 90/126/127/129 4 POST |
| `src/engine/refresh_progress.py` | 195L | 사이클 127 신규 | - |
| AST 가드 (`tests/unit/ast/`) | **49 파일** | 사이클 67 이후 ~40+ 신규 | DRY 검토 영역 |

## 권고 카드 목록

---

### 카드 #21 — scheduler.py 4 task loop 패턴 헬퍼 추출

- **위험 등급**: **MEDIUM**
- **현재 상태**:
  - `src/engine/scheduler.py:2639-2705` `_full_universe_load_task_loop` (~67L)
  - `src/engine/scheduler.py:2707-2777` `_stock_master_daily_load_task_loop` (~71L)
  - `src/engine/scheduler.py:2779-2843` `_stock_master_basics_refresh_task_loop` (~65L)
  - `src/engine/scheduler.py:2845-2910+` `_stock_master_master_load_task_loop` (~70L)
  - **합계 ~273L, 4 함수 = 거의 동일한 lifecycle 패턴 100% 답습**
  - 동일 구조 (사이클 122 답습 → 사이클 126 답습 → 사이클 129 답습):
    1. lazy import (`from src.engine.scanner import _X_once` + `from src.engine.X_metrics import record_X, flush_X`)
    2. start() 직후 즉시 1회 실행 try/except (사이클 106 lifecycle race 차단 패턴)
    3. `record_X(summary)` + `flush_X_collector()` + `logger.info("[X_summary] ...")` emit
    4. while 루프 `_wait_until(TIME_X)` + CancelledError break + Exception graceful + `asyncio.sleep(60)`
  - 차이점은 **이름 + summary 키 차이만** (의미 변경 없음)
- **권고**:
  - **헬퍼 함수 1 개 추출**: `_run_periodic_load_task(task_name: str, wait_time: time, once_fn: Callable, record_fn: Callable, flush_fn: Callable, log_prefix: str, summary_keys: tuple[str, ...])` 형태
  - 또는 **데이터 클래스 + dispatch**: `@dataclass class PeriodicLoadTaskSpec` 정의 + `_run_periodic_load_task(spec)` 단일 헬퍼
  - 4 task loop 본체 = 각각 3~5줄로 축소 (spec 정의 + 헬퍼 호출)
  - **lifecycle race 차단 영속 + emit 영속 + collector 영속 + summary 키 영속 모두 보존** (행위 보존 의무)
- **회귀 가드** (기존 + 신규):
  - 기존: 사이클 106 lifecycle race 차단 + 사이클 122 daily task + 사이클 126 basics task + 사이클 129 master task 회귀 테스트 (모두 통과 유지 의무)
  - 신규: 헬퍼 함수 단위 테스트 5+ (CancelledError / Exception graceful / `_wait_until` 호출 검증 / record+flush 호출 검증 / summary 키 누락 가드)
  - 신규: AST 가드 — 4 task loop 본체에 동일 패턴 lazy import + while + try/except 영구 보존 (헬퍼 우회 차단)
- **영속 의무 매트릭스**:
  - 사이클 78 G-AST1 (flush 호출 사이트 영속) → 헬퍼 내부로 이주 시 AST 가드 갱신 의무
  - 사이클 79 G-AST2 (`task_attrs` 튜플 4 위치 영속) → 변경 0 (`stop()` task 멤버 이름 보존)
  - 사이클 88 G-REJECT (graceful 영속) → 헬퍼 내부 try/except 영속
  - 사이클 106 lifecycle race 차단 (start() 즉시 1회 + while) → 헬퍼 내부 구조 영속
  - 사이클 122/126/127/129 운영 prefix (`[X_summary]`) 영속
- **예상 효과**:
  - scheduler.py ~273L 영역 → ~80L 축소 추정 (-193L, -71%)
  - 다음 task loop 추가 (예: 사이클 131+ 새로운 종목 마스터 영역) 비용 = 80L → 5L
  - 4 task loop 일관성 보장 (한 곳 시정 시 4 task 일괄 효과)
- **선행 의뢰**: 없음 (MEDIUM = refactor 단독 평가)

---

### 카드 #22 — routes/stock_master.py 4 POST fire-and-forget 패턴 헬퍼 추출

- **위험 등급**: **LOW**
- **현재 상태**:
  - `src/routes/stock_master.py:46-100` `_run_X_background` 4 wrapper (~55L)
  - `src/routes/stock_master.py:103-223` 4 POST 라우트 핸들러 (~120L)
  - **합계 ~175L, 8 함수 = 거의 동일한 fire-and-forget 패턴 100% 답습**
  - 동일 구조 (사이클 127 답습 → 사이클 129 답습):
    1. `_run_X_background(force: bool)`: lazy import + try/except + `_rp.is_running` + `finish_progress("failed")` 안전망
    2. POST 라우트: `_rp.is_running("X")` 가드 + 409 + `background_tasks.add_task` + 동일 응답 schema
  - 차이점은 **task_key + once 함수 이름 + log prefix 만**
- **권고**:
  - **헬퍼 모듈 추가**: `_make_refresh_endpoint(task_key: TaskKey, once_fn: Callable, log_prefix: str)` 팩토리 함수
  - 또는 dispatch dict: `TASK_REGISTRY: dict[TaskKey, tuple[Callable, str]]` + 단일 `_run_background(task_key, force)` + 단일 `_make_endpoint(task_key)` 패턴
  - 4 POST 라우트 본체 = 각각 5줄로 축소
- **회귀 가드** (기존 + 신규):
  - 기존: 사이클 127 라우트 회귀 테스트 14 케이스 + 사이클 129 master 라우트 (모두 통과 유지)
  - 신규: 헬퍼 팩토리 단위 테스트 3+ (TaskKey 미지원 / 409 가드 / fire-and-forget 등록)
  - 신규: AST 가드 — `TASK_REGISTRY` 영속 + 4 task_key 전수 등록 영속 (신규 task_key 누락 silent 결함 차단)
- **영속 의무 매트릭스**:
  - 사이클 84 L-2 POST 화이트리스트 (4 라우트 영속, AST 영속)
  - 사이클 90 패턴 (asyncio.Lock 폐기 → state 기반 가드 영속)
  - 사이클 127 fire-and-forget BackgroundTasks 영속
  - 사이클 129 4 task_key 확장 영속
- **예상 효과**:
  - routes/stock_master.py ~175L 영역 → ~60L 축소 추정 (-115L, -66%)
  - 다음 task_key 추가 (예: ETF 마스터 / 업종 코드) 비용 = 60L → 3줄 (TASK_REGISTRY 등록만)
- **선행 의뢰**: 없음 (LOW = refactor 단독 평가)

---

### 카드 #23 — scanner.py 모듈 분해 (2,681L → 4 sub-module)

- **위험 등급**: **HIGH**
- **현재 상태**:
  - `src/engine/scanner.py` **2,681L** = 사이클 67 `stale_manager.py` 1,099L 기준 2.44배 초과
  - 책임 영역 식별 가능 (사이클 67 4 sub-module 분해 패턴 답습 가능):
    1. **scanner_filters** (가격 + 거래대금 필터, ~600L): L55~L515 `_apply_price_filter` / `_apply_trade_amount_filter` / `_collect_protected_tickers_for_scanner` / 상수 영역 + emit 영역
    2. **scanner_subscribe** (구독/스캔, ~700L): L517~L1230 `scan_stocks` / `subscribe_filtered_stocks` / `unsubscribe_all` / `_scan_pool_eager_refresh_loop` / STATIC_TICKER_NAMES
    3. **scanner_universe** (전체 universe 적재, ~700L): L1232~L2007 `_fetch_market_cap_page` / `_full_universe_load_once` / `_full_universe_load_krx_primary` / `_full_universe_load_kis_fallback`
    4. **scanner_master** (마스터 + basics + daily, ~700L): L2008~L2681 `_stock_master_daily_load_once` / `_stock_master_basics_refresh_once` / `_stock_master_master_load_once` / `_emit_stock_master_age_warning` / `market_cap_master_to_millions` / `validate_market_cap_consistency` / `_is_master_blocked_for_entry` / `get_market_cap_millions`
  - facade `scanner.py` (~100L) — re-export only (`__all__` 정의)
- **권고**:
  - **사이클 67 4 sub-module + facade 패턴 100% 답습** (검증된 분해 방식)
  - **logger 명시 binding** 4 sub-module 모두 `logging.getLogger("src.engine.scanner")` 동일 (사이클 60 I1 영속)
  - **모듈 의존 방향 (단방향)**: filters ← subscribe ← universe ← master (master 가 최상위)
  - **lazy import 영속** (사이클 127/129 `from src.engine.scanner import ...` 호출 패턴 영속)
  - **테스트 patch 경로 적응** (사이클 67 경험: 본체 모듈 네임스페이스 직접 호출 시 facade patch 비효과 → 테스트 patch 경로 갱신 의무)
- **회귀 가드** (기존 + 신규):
  - 기존: 사이클 100+ scanner 영역 전수 회귀 (모두 통과 유지)
  - **HIGH 위험 영역**: scanner 단계 = 매수 진입 *전* 영역 (사이클 38 명문화) — 행위 보존 의무 절대
  - 신규: AST 가드 — facade `__all__` 21+ 항목 영속 + 4 sub-module 파일 영속 + 단방향 import 영속
  - 신규: import sanity 테스트 (4 sub-module + facade 모두 import 가능 영속)
  - 신규: facade patch 안전선 AST (사이클 67 G-16 답습) — `patch("src.engine.scanner.X")` → X facade export 검증
- **영속 의무 매트릭스**:
  - 사이클 38 명문화 (scanner 단계 = 매수 진입 전용)
  - 사이클 81 G-AST1 (raw 영역 보호 — master 별도 컬럼 분리 영속)
  - 사이클 88 G-REJECT (graceful 영속)
  - 사이클 109 화이트리스트 영속 (market-cap path)
  - 사이클 122/126/127/129 task 패턴 영속
- **예상 효과**:
  - scanner.py 2,681L → facade ~100L (-2,581L, -96%) — **사이클 67 1,099L → 96L 패턴 답습**
  - 모듈 책임 명확화 → 다음 변경 비용 감소 (영역별 격리)
  - 회귀 위험 증가 (HIGH 위험 영역 = 매매 안전성 영역과 인접)
- **선행 의뢰**: **domain-expert 행위 영향 평가 (Q3=A 영속)** — scanner 분해가 매수 진입 *전* 영역만 한정되는지 정밀 검증 + risk/order/realtime/auth 영역 변경 0 검증 + lazy import 영속 검증

---

### 카드 #24 — stock_master metrics 3 모듈 (basics/daily/universe) 공통 헬퍼 추출

- **위험 등급**: **LOW**
- **현재 상태**:
  - `src/engine/stock_master_metrics.py` (146L) — universe collector
  - `src/engine/stock_master_basics_metrics.py` (74L) — basics collector
  - `src/engine/stock_master_daily_metrics.py` (78L) — daily collector
  - **합계 298L, 3 모듈 = 거의 동일한 collector 패턴 (record + flush 페어)**
  - 차이점: prefix 이름 + summary 키 + 누적 합산 방식만
  - master 영역은 metrics 모듈 없음 (사이클 129 신규 — 일관성 부재)
- **권고**:
  - **공통 collector 헬퍼 모듈 추가**: `src/engine/collectors/load_collector.py` 또는 `src/engine/stock_master_load_collector.py`
  - `LoadCollector` 클래스 또는 팩토리 함수 `make_load_collector(prefix: str, summary_keys: tuple[str, ...]) -> tuple[record_fn, flush_fn]`
  - 3 기존 모듈 = 헬퍼 호출 wrapper 형태로 축소 (~20L 추정)
  - master 영역 metrics 모듈 신규 추가 (일관성 보강, 카드 #21 헬퍼 추출과 동행 권고)
- **회귀 가드** (기존 + 신규):
  - 기존: 사이클 74 collector 패턴 + 사이클 89/108/122/126 metrics 회귀 (모두 통과 유지)
  - 신규: 헬퍼 단위 테스트 5+ (record 누적 / flush 비움 / threading safety / 빈 collector flush graceful)
- **영속 의무 매트릭스**:
  - 사이클 74 collector 패턴 영속
  - 사이클 78 G-AST1 (flush 호출 사이트 영속, 카드 #21 헬퍼 영역 영속)
- **예상 효과**:
  - 3 모듈 298L → ~80L 축소 추정 (-218L, -73%)
  - master 영역 metrics 추가 시 비용 = 80L → 5L
  - 4 작업 일관성 보장
- **선행 의뢰**: 없음 (LOW = refactor 단독 평가)

---

### 카드 #25 — AST 가드 49 파일 DRY 검토 + 헬퍼 모듈

- **위험 등급**: **LOW**
- **현재 상태**:
  - `tests/unit/ast/` **49 파일** (사이클 67 이후 ~40+ 신규)
  - 동일 패턴 다수 발견:
    - `_count_calls(node, func_name)` (사이클 78/89/101 등)
    - `_extract_function(tree, name)` (사이클 79/83/93 등)
    - `_iter_string_literals(tree)` (사이클 81/94/108 등)
    - `_find_imports(tree, target_module)` (사이클 67/110 등)
  - 신규 AST 가드 추가 시 동일 헬퍼 반복 작성 (3+ 회 반복 영역)
- **권고**:
  - **AST 헬퍼 모듈 신규**: `tests/unit/ast/_ast_helpers.py` (테스트 디렉토리 내부, production code 영향 0)
  - 공통 헬퍼 5~10개 정의 (count_calls / extract_function / iter_strings / find_imports / 등)
  - 기존 49 파일 = 점진 마이그레이션 (한 사이클당 5~10 파일씩, 회귀 위험 낮음)
  - **사이클 76 답습 패턴** (api retry helper DRY)
- **회귀 가드** (기존 + 신규):
  - 기존: 49 AST 가드 케이스 모두 통과 유지 (단순 헬퍼 이주, 행위 보존)
  - 신규: 헬퍼 자체 단위 테스트 5+ (각 헬퍼별 정확성)
- **영속 의무 매트릭스**:
  - 사이클 67/78/79/81/83/89/93/94/101/108/110 등 AST 가드 의미 영속
  - 테스트 디렉토리 내부 변경 = production code 무관
- **예상 효과**:
  - AST 가드 ~3,000L 추정 → ~1,500L 축소 추정 (-50%)
  - 다음 AST 가드 추가 비용 = 50~80L → 10L
- **선행 의뢰**: 없음 (LOW = refactor 단독 평가)

---

### 카드 #26 — scheduler.py 추가 분해 검토 (3,497L)

- **위험 등급**: **HIGH**
- **현재 상태**:
  - `src/engine/scheduler.py` **3,497L** = 사이클 67 분해 후에도 최대 비대화 영역
  - 카드 #21 (4 task loop 헬퍼 추출) 적용 후에도 잔존 ~3,300L
  - 책임 영역 식별 가능 (참고만):
    - `_boot()` 영역 = 사이클 51 `boot_manager.py` 이미 분리 (변경 0)
    - K stale watcher 영역 = 사이클 67 `stale_manager.py` 이미 분리 (변경 0)
    - 잔존 영역 = `_scan_loop` / `_settle` / `_session_loop` / `_resolve_*` / `_drain_pending_*` / `run_daily` / `start/stop/connect` / lifecycle hook 4 task / `_session_loop` / `_eager_refresh_*` / `_api_recovered_collector_loop` / `_reset_daily_state` / 시간 상수 30+ 개
- **권고**:
  - **사이클 131+ 후속 사이클 발의** (사이클 130 = 권고만, Q4=A 영속)
  - 분해 청사진 후보:
    - `scheduler_lifecycle.py` (~500L): start/stop/connect/_running 관리
    - `scheduler_loops.py` (~800L): _scan_loop / _session_loop / _api_recovered_collector_loop
    - `scheduler_boards.py` (~400L): _confirm_breakout_open_prices / _drain_pending_next_day_clear / _execute_next_day_clear / _board_transition_loop / _force_clear_main_only
    - `scheduler_eager.py` (~300L): _eager_refresh_* / _collect_presubscribe_tickers
    - `scheduler_settlement.py` (~300L): _settle / _reset_daily_state
    - facade `scheduler.py` (~200L) — TradingScheduler 클래스 + re-export
  - **HIGH 위험 영역**: 매수/매도/익일청산/15:20 강제청산/체결통보 lifecycle 영역 인접 → 사이클 67 `stale_manager.py` 분해 패턴 답습 의무
- **회귀 가드** (기존 + 신규):
  - 기존: scheduler 영역 전수 회귀 (모두 통과 유지)
  - **HIGH 위험 영역**: CLAUDE.md "절대 깨지 말 것" 영속 (체결통보 구독 / uvicorn 단일 워커 / 주문번호 매핑 / 익일 청산 30s 안정화 / NXT 좀비 차단 / WebSocket 4 중 안전망 / 15:20 일괄청산)
  - 신규: AST 가드 — facade `__all__` 영속 + 단방향 import 영속 + lazy import 영속
  - 신규: import sanity + facade patch 안전선 (사이클 67 G-16 답습)
- **영속 의무 매트릭스**:
  - 사이클 51 boot_manager 분리 영속 (변경 0)
  - 사이클 67 stale_manager 분리 영속 (변경 0)
  - 사이클 78/79 G-AST1/G-AST2 영속 (flush 호출 + task cancel)
  - 사이클 106 lifecycle race 차단 영속
  - CLAUDE.md "절대 깨지 말 것" 8 영역 영속
- **예상 효과**:
  - scheduler.py 3,497L → facade ~200L (-3,297L, -94%) — **사이클 67 1,099L → 96L 패턴 답습**
  - 모듈 책임 명확화 → 다음 변경 비용 감소
  - **회귀 위험 매우 높음** (매매 hot path 인접) — 다중 사이클 분할 의무
- **선행 의뢰**: **domain-expert 행위 영향 평가 (Q3=A 영속)** — scheduler 분해가 매매 hot path (체결통보 / 주문 / 익일청산 / 15:20 강제청산) 영역과 격리 가능한지 정밀 검증 + WebSocket 4 중 안전망 영역 영향 평가

---

## 비권고 사항

### 비권고 #N1 — refresh_progress.py (195L) 추가 분리

- **사유**: 사이클 127 신규 모듈, 단일 책임 명확 + 195L 임계 미달성 + 사이클 129 TaskKey 4 확장 영속. 미래 가정으로 추상화 만들지 않음 (5 계명 #3 영속).

### 비권고 #N2 — `db/stock_master.py` (597L) 분해

- **사유**: 임계 (800L) 미달성 + 단일 책임 (stock_master CRUD) 명확 + 사이클 124/128/129 확장은 동일 책임 영역. 사이클 67 임계 (1,099L) 기준 절반 수준.

### 비권고 #N3 — `Position` / `Signal` / `StrategyBase` 영역

- **사유**: 매매 안전성 영역 직접 인접 (HIGH 위험). 사이클 38 명문화 + CLAUDE.md "절대 깨지 말 것" 영속. refactor-review 영역 외.

### 비권고 #N4 — risk.py / order_engine.py / realtime/ / auth/ 영역

- **사유**: 사이클 38 명문화 영속 + CLAUDE.md "절대 깨지 말 것" 8 영역 영속. refactor-review 영역 외 (HIGH 위험 + 매매 hot path 직접).

---

## 후속 의뢰

### tdd-engineer 회귀 가드 추가 영역 (사이클 131+ Green 시점)

- 카드 #21 헬퍼 단위 테스트 5+ (CancelledError / Exception graceful / lifecycle race 차단)
- 카드 #22 헬퍼 팩토리 단위 테스트 3+ (TaskKey / 409 / fire-and-forget)
- 카드 #23 import sanity + facade patch 안전선 + AST 가드 다수
- 카드 #24 collector 헬퍼 단위 테스트 5+
- 카드 #25 AST 헬퍼 자체 단위 테스트 5+
- 카드 #26 다중 사이클 분할 (3~5 사이클) + HIGH 회귀 가드 30+

### domain-expert HIGH 카드 행위 영향 평가 (Q3=A 영속)

- **카드 #23** (scanner.py 분해) — 매수 진입 *전* 영역 격리 검증
- **카드 #26** (scheduler.py 추가 분해) — 매매 hot path 영역 격리 검증

### team-leader 카드별 채택/보류/폐기 결정 요청

다음 6 카드에 대한 사용자/team-leader 결정 의무:
- 카드 #21 (scheduler 4 task loop 헬퍼) MEDIUM
- 카드 #22 (routes/stock_master 4 POST 헬퍼) LOW
- 카드 #23 (scanner.py 분해) HIGH — **domain-expert 평가 필수**
- 카드 #24 (metrics 3 모듈 공통 헬퍼) LOW
- 카드 #25 (AST 가드 49 파일 DRY) LOW
- 카드 #26 (scheduler.py 추가 분해) HIGH — **domain-expert 평가 필수 + 다중 사이클 분할 의무**

---

## 사이클 131+ 추천 우선순위

| 순위 | 카드 | 위험 | 사유 |
|------|------|------|------|
| 1 | **#22** (routes 4 POST 헬퍼) | LOW | 가장 작은 영역 + 가장 명확한 패턴 + 즉시 효과 검증 가능. refactor 친밀도 회복 (62 사이클 누적 임계 후 첫 Green) |
| 2 | **#24** (metrics 3 모듈 헬퍼) | LOW | master metrics 모듈 부재 일관성 결함 해소 + 카드 #21 사전 준비 |
| 3 | **#21** (scheduler 4 task loop 헬퍼) | MEDIUM | 카드 #24 완료 후 자연 발주 + 패턴 일관성 |
| 4 | **#25** (AST 가드 DRY) | LOW | 점진 마이그레이션 (한 사이클 5~10 파일) — 회귀 위험 최소 |
| 5 | **#23** (scanner.py 분해) | HIGH | domain-expert 사전 평가 필수 + 다중 사이클 분할 (4 sub-module = 4 사이클 예상) |
| 6 | **#26** (scheduler.py 추가 분해) | HIGH | 매매 hot path 인접 + 다중 사이클 분할 (5 sub-module = 5+ 사이클 예상) + 카드 #21 완료 후 자연 발주 |

---

## 누적 라인 영역 감소 예상

| 카드 | 감소 라인 | 비고 |
|------|-----------|------|
| #21 | -193L | scheduler.py ~273L → ~80L |
| #22 | -115L | routes/stock_master.py ~175L → ~60L |
| #23 | -2,581L | scanner.py 2,681L → facade ~100L (4 sub-module 분해) |
| #24 | -218L | metrics 3 모듈 298L → ~80L |
| #25 | -1,500L 추정 | AST 가드 ~3,000L → ~1,500L |
| #26 | -3,297L | scheduler.py 3,497L → facade ~200L |
| **합계** | **-7,904L 추정** | **사이클 67 -1,003L (1,099L → 96L) 대비 7.9배 영역** |

---

## 회귀 위험 영역

| 카드 | 매매 안전성 영역 | 영속 의무 매트릭스 | 평가 |
|------|----------------|-------------------|------|
| #21 | 무영향 (lifecycle hook 영역) | 사이클 78/79/88/106/122/126/127/129 | 회귀 위험 낮음 |
| #22 | 무영향 (라우트 영역) | 사이클 84/90/127/129 | 회귀 위험 최소 |
| #23 | 매수 진입 *전* 영역 한정 | 사이클 38/81/88/109/122/126/127/129 | **회귀 위험 중간 + domain-expert 평가 의무** |
| #24 | 무영향 (collector 영역) | 사이클 74/78 | 회귀 위험 낮음 |
| #25 | 무영향 (테스트 영역) | 49 AST 가드 의미 영속 | 회귀 위험 최소 |
| #26 | **매매 hot path 인접** | CLAUDE.md "절대 깨지 말 것" 8 영역 + 사이클 51/67/78/79/106 | **회귀 위험 매우 높음 + domain-expert 평가 의무 + 다중 사이클 분할 의무** |

---

## 사이클 130 종결 산출물 (Q4=A 영속)

- 권고 카드 6 장 산출 완료 (HIGH 2 / MEDIUM 1 / LOW 3)
- HIGH 카드 2건 = domain-expert 평가 위임 의무 (사이클 131+ Green 발주 *전*)
- Green 실행 0 (사이클 131+ 별도 발주)
- 매매 안전성 무영향 영속 (행위 보존 가정 + 검토만)
- commit/push 보류 (사용자 명시 승인 전 영구 영속)
