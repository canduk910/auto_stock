# 2단계 설계 카드 — 접수 후 PENDING 영속화를 매수까지 4곳 → 1곳

> 진행 규약 정본: [`2026-09-20_execute_sell_plan.md`](2026-09-20_execute_sell_plan.md)
> 1단계 카드: [`2026-09-20_step1_card.md`](2026-09-20_step1_card.md) · 관문 3인:
> [`domain`](2026-09-20_step1_gate_domain.md) · [`tester`](2026-09-20_step1_gate_tester.md) · [`tdd`](2026-09-20_step1_gate_tdd.md)
> 기준 소스: `src/engine/order_engine.py` **3,147 라인** ·
> sha `e1beeb1fffab6ca5193d6954e9164e75f1d0d16eaaaedbcbb7d3fef1328dfa2e`
> (cycle333 `42644e0` 반영본 — 1단계 이후 cycle329·330·331·332·333 이 더 들어갔다)

---

## 2단계 — 매수 주 경로·폴백의 **접수 후 PENDING 영속화**를 매도와 같은 코어 헬퍼로 올려, 그 규칙을 **한 곳에서만 고치게** 한다

🔴 **경계(cycle327 ⓑ)는 이 단계에서 매수로 넘기지 않는다.** 두 축을 **층으로 분리**한다 —
「무엇을 영속화하는가」는 4경로 공용 코어 헬퍼가, 「접수 후 경계를 누가 닫는가」는
호출자 층이 갖는다. 매수 경계 부재는 **결함이고 그렇게 명시하며**, 별도 승인 사이클(2B)로 뺀다.

---

## 왜 이것이 다음 수인가

### 0계명 순위별 기여

| 순위 | 지금 | 이 단계 뒤 |
|---|---|---|
| **1. 수정 시 누락 위험** | 「선행 체결통보 체크 → skip 로그 → `TradeRecord` 조립 → race 흡수 INSERT」가 **4곳**에 손으로 복제 (매수 주 `:1356-1379` · 매수 폴백 `:1471-1493` · 매도 2곳은 1단계가 이미 헬퍼 안) | **1곳**. 그 규칙을 바꿀 때 손대는 자리 4 → 1 |
| **2. 이해 비용** | `execute_buy` = **466줄 · CC 45 · 관심사 27개**. 폴백 블록 최심 중첩 = 들여쓰기 **6단**(`:1490`) | 관심사 2개(⑯·㉓)가 하나의 이름을 갖는다. 폴백 블록 최심 **6단 → 5단**. ⚠️ 함수 전체 최대 중첩은 **6 그대로**(최심점은 GTP 프로브 `:1237` 이고 무접촉) |
| **3. 변경 위험** | 대상 구간이 결합한 `self` 상태 = **2개**(`_completed_orders` · `_insert_pending_or_absorb_race`). 미커버 줄 **0**(아래 실측) | 결합 2개 유지. 새 그물 12건 추가 |
| **4. 중복** | 같은 22~24줄 블록 **4회** 반복(3회 규칙 충족, 그리고 cycle327 실사고가 바로 이 흩어짐에서 났다) | 0회 |
| 5. 줄 수 | — | **보고하지 않는다** |

### 제친 후보와 이유 (1단계 카드의 후보 3~7 을 현재 소스에 다시 대고 판단)

| 후보 | 제친 이유 |
|---|---|
| **3. 폴백 경로 전체(⑰a~⑰g) → `_handle_sell_market_disallowed`** | 중첩이 가장 깊은 덩어리이지만 **행위 결합이 가장 크다** — `register_market_order_disallowed`(30초 TTL) · `_pending_next_day_clear` 등록 · `_bump_after_exit_fails_and_maybe_giveup` · ETP 관측 · `[after_exit_division]` 이 한 블록에 얽혀 있고, tester 관문이 실측한 **N3·N4 오귀인 사슬**(접수 성공한 주문을 「폴백 모두 거부」로 적고 익일청산 큐에 넣는다)이 정확히 이 블록이다. 그물이 이 블록의 **분기 조합**을 다 덮는지 먼저 재야 한다 |
| **4. APBK0400 잔고 재대조 5분기(⑮)** | 「판정 순수 함수 + 집행」 분리는 좋은 수이지만, `_selling` 유지/해제가 분기마다 **정반대**(`sell_qty_partial_locked` 는 의도적 유지, `market_closed` 는 discard)라 순수화가 그 비대칭을 지우기 쉽다. 0계명 1순위 기여도 낮다(중복 아님) |
| **5. 최종 실패 후처리(⑲) → `_reconcile_sell_insufficient`** | `test_cycle185_cluster1_ast.py` 의 positions 제거 site 집합 `{_handle_sell_fill, execute_sell}` **재조준 선행**이 필요하다. 그 가드는 "포지션을 지우는 자리는 정확히 둘" 이라는 구조 불변식이라, 재설계가 카드 본문보다 크다 |
| **6. 주문사양 결정(④~⑧) → `_resolve_sell_order_plan`** | `execute_sell_pre_nxt_preconvert` **frozen segment 재핀 승인 선행**(`test_cycle287_ast_scope._FROZEN_SEGMENTS`) |
| **7. 매핑 5종 공통화** | `test_cycle291_ast_scope::test_a10c` 가 `src.count("self._order_exchange[") == 5` 를 단언한다. 추출하면 5→1 이 되어 **"등록 5곳 누락 감시자" 가 죽는다**. 가장 마지막 |

**그리고 2단계만이 선결 조건을 이미 충족했다** — 1단계 관문이 남긴 「매수 축 대칭 단언 부재」를
**cycle333 A3 가 닫았다**(아래에서 충분성을 검증했다).

---

## 현재 상태 (실측)

### 대상 구간 2곳

| | 위치 | 들여쓰기 | 구조 |
|---|---|---|---|
| **A — 매수 주 경로** | `src/engine/order_engine.py:1356-1379` | col 13 (블록 1단) | `try:`(`:1275`) 본문 안 |
| **B — 매수 폴백** | `src/engine/order_engine.py:1471-1493` | col 21 (블록 3단), 최심 `:1490` col 25 | `except KisApiError as e:`(`:1386`) → 중첩 `try:`(`:1414`) 안 |

두 블록은 **선행 체크 분기 + `TradeRecord` 조립 + `_insert_pending_or_absorb_race` 호출**로
매도 헬퍼 본문(`:1018-1041`)과 **구조가 같다.** `TradeRecord` 9필드 중 실제로 다른 것은
`trade_type`·`price`·`quantity`·`strategy`·`order_no` 뿐이고, `profit_loss` 는 매수가 생략·매도가
`0` 명시인데 **모델 기본값이 `0`**(`src/models/trade.py:31`)이라 **동일 값**이다.
`ticker_name` 파생식도 3곳 모두 같다(폴백만 지역 변수 `:1479` 를 한 번 거칠 뿐).

### `execute_buy`(`:1049-1514`)가 다루는 관심사 27개 — 대상은 ⑯과 ㉓

① 자기 전략 중복 차단 `:1065` ② 전략 간 중복 가드 `:1069` ③ 15:30~16:00 휴식 컷 `:1081`
④ 잔고락·low_funds cooldown `:1090`~`:1098` ⑤ 매수가능금액 캐시·조회 `:1101-1120`
⑥ 수량 산출 + soft_multiplier `:1124-1136` ⑦ 수량 0 cooldown `:1138` ⑧ max_buy_qty 클램프 `:1149-1163`
⑨ `pending_buys`/`pending_buy_amounts` 등록 + funnel `:1165-1168` ⑩ 거래소 라우팅 `:1173`
⑪ PR-F 프리장 사전 지정가 변환 + GTP 승격 + 카나리아 `:1187-1273` ⑫ 발사 `:1280-1289`
⑬ `record_price` 결정 `:1294-1296` ⑭ 주문번호 매핑 5종 + `_pending_buy_orders` `:1297-1317`
⑮ LLM 매수평가 훅 `:1324-1354` **⑯ 접수 후 영속화 `:1356-1379`** ⑰ 접수 로그 + 캐시 무효화 `:1381-1384`
⑱ insufficient_cash `:1389-1395` ⑲ 시장가불가/GTP 폴백 게이트 `:1403-1411`
⑳ 폴백 pending 재등록 + 발사 `:1413-1425` ㉑ 폴백 매핑 5종 `:1429-1440` ㉒ 폴백 LLM 훅 `:1447-1469`
**㉓ 폴백 접수 후 영속화 `:1471-1493`** ㉔ 폴백 성공 로그·캐시·return `:1495-1500`
㉕ 폴백 거부 후처리 `:1501-1509` ㉖ 미분류 재전파 `:1510` ㉗ 일반 예외 rollback + raise `:1511-1514`

### 대상 구간이 결합한 `self` 상태 (2개 — 매도와 동일)

- `self._completed_orders` (읽기 + `discard`)
- `self._insert_pending_or_absorb_race` (호출)

`pending_buys` · `pending_buy_amounts` · `_order_*` 5종 · `_pending_buy_orders` ·
`cached_buyable_at` · `llm_buy_gate` 는 **대상 구간 안에서 한 번도 쓰이지 않는다.**
→ **헬퍼 밖에 남는다.** (아래 「매수 쪽에만 있는 것」 전수 확인)

### 덮는 테스트 · 미커버 줄

```bash
python -m pytest -q -p no:randomly \
  tests/unit/engine/test_order_engine_buy.py \
  tests/unit/engine/test_cycle271_execute_buy_fill_during_insert.py \
  tests/unit/engine/test_cycle276_order_time_hook.py \
  tests/unit/engine/test_cycle291_pre_nxt_gtp.py \
  tests/unit/engine/test_cycle331_buy_fill_from_pending.py \
  tests/integration/test_chegyeol_race.py \
  --cov=src.engine.order_engine --cov-report=term-missing
→ 185 passed
Missing: … 1264-1265, 1532-1533, …
```

🔴 **`1266-1531` 이 Missing 목록에 한 줄도 없다** — 대상 A(`1356-1379`)·B(`1471-1493`)는
**미커버 줄 0 · 미커버 분기 0**이다(부분 분기 71건 중에도 그 범위는 없다).
진행 규약 §5 의 「미커버 구간을 건드려야 하는데 회귀를 못 세운다」에 해당하지 않는다.

| 파일::케이스 | 덮는 것 |
|---|---|
| `test_cycle271_…::test_c3_1_when_order_no_already_in_completed_orders_then_pending_insert_skipped`(`:486`) | **A** skip 분기 + `_completed_orders` 소비 + 매핑 보존 |
| 〃 `::test_c3_2_fallback_when_order_no_already_completed_then_pending_insert_skipped`(`:500`) | **B** skip 분기 |
| 〃 `::test_c5_1_when_full_fill_during_insert_then_marker_emitted_once` | **A** `[buy_fill_during_insert]` 1행 |
| 〃 `::test_c5_2_…`(`:533`) | **B** 같은 마커 |
| 〃 `::test_c7_1_when_unique_violation_without_fill_then_still_raises`(`:563`) | **A** 증거 없는 `UniqueViolationError` **전파**(`pytest.raises`) |
| 〃 `::test_c7_2_when_non_unique_violation_with_fill_evidence_then_still_raises`(`:586`) | **A** 증거 있어도 non-Unique 는 **전파**(`pytest.raises`) |
| `test_order_engine_buy.py::test_buy_pending_record_carries_order_price_and_quantity`(`:664`, **cycle333 A3**) | **A** record 7필드 값 |
| 〃 `::test_buy_fallback_pending_record_carries_fallback_price_symmetrically`(`:691`, **cycle333 A3**) | **B** record 7필드 값 |
| `tests/integration/test_chegyeol_race.py` | A skip 분기 통합 |

---

## 🔴 선행 조건 1 — cycle333 A3 로 충분한가 : **값 축은 충분, 문구 축은 부족**

`tests/unit/engine/test_order_engine_buy.py:664`·`:691` 을 읽고 매도 시나리오 J 와 1:1 대조했다.

| 축 | 매도(시나리오 J) | 매수(cycle333 A3) | 판정 |
|---|---|---|---|
| record 6필드(`order_no`/`ticker`/`trade_type`/`status`/`strategy`/`quantity`) | 주·폴백 **대칭** | 주·폴백 **대칭** | ✅ 충족 |
| `price` 판별력 | `4475` vs `4500` vs `10` + 전제 봉인 | `4500`(주) / `step_up(4500,5)`(폴백) vs `10` + **전제 봉인 2줄**(`:701-702`) | ✅ 충족 |
| **skip 분기 WARNING 문구** | `test_sell_pending_skip_warning_distinguishes_market_and_fallback` 이 **주·폴백 쌍**으로 봉인 | 🔴 **리포 전체 0건** (`grep "PENDING INSERT 생략" tests/` = 매도만) | ❌ **미충족** |
| **접수 후 예외 전파·상태 롤백** | 주 = `test_sell_does_not_refire_on_generic_insert_error` · 폴백 = 시나리오 I | 주 = `test_c7_1`/`c7_2`(전파만 단언, `pending_buys` 는 안 본다) · 폴백 = 🔴 **0건** | ❌ **미충족** |

**결론 — cycle333 A3 는 「인자 4개가 뒤바뀌는」 실패 모드는 닫았지만,
2단계가 실제로 건드리는 나머지 두 축(문구·예외 전파)은 아직 무방비다.**
그 둘을 **현행 행위 핀**(J-BUY-1~4, 아래 「선행 의뢰」)으로 먼저 세운다 — 1단계에서
team-lead 가 시나리오 I 를 먼저 세운 것과 같은 역할이다.

### 실측 — 지금 매수 축이 무엇을 하는지 (격리 하네스, repo 무접촉)

`scratchpad/test_zz_buy_boundary_probe.py`(리포 밖)로 `insert_trade` 를 `TimeoutError` 로 터뜨렸다.

```
[MAIN] raised=TimeoutError  place_order=1  pending_buys=[]  pend_amounts={}
       order_qty={'BUY-000001': 999}  pend_orders=['BUY-000001']
       cached_buyable_at=<비0>  is_ticker_blocked_for_buy=False   🔴
[FB]   raised=TimeoutError  place_order=2  pending_buys=['012200']  pend_amounts={...}
       is_ticker_blocked_for_buy=True
[SKIP-FB] '폴백 응답보다 체결통보 선행 — PENDING INSERT 생략: 테스트종목(012200)'   ← 주문번호 없음
```

---

## 바꿀 것 — 🔴 **행위 보존이다. 결함 시정은 이 카드 밖(2B)으로 뺀다**

### 판정: tdd 관문의 경고는 **결함 지적으로서 옳다. 그러나 그 시정은 2단계가 아니다**

관문은 「2단계는 **행위 보존으로 승인받으면 안 된다**(예외 전파가 바뀐다)」고 적었다.
그 전제는 **"매수 2곳을 `_persist_sell_pending_after_send` 와 같은 헬퍼(=경계 포함)로 올린다"** 이다.
이 카드는 그 전제를 채택하지 않는다 — **경계를 뺀 코어 헬퍼**를 공유하고 경계는 호출자 층에 둔다.
그러면 예외 전파가 **한 글자도 바뀌지 않는다**(아래 근거 절).

그리고 관문이 지목한 것은 **실재하는 결함**이다. 그 판정을 명시한다.

> ### 🔴 매수 접수 후 실패의 상태 롤백은 **결함이다** (행위 변경이 아니라 결함 시정 대상)
>
> **① 정본 규칙이 이미 그렇게 말한다.** 루트 `CLAUDE.md`:
> 「**주문이 나간 뒤의 실패로 재발사 금지 (cycle327)** — `place_order` 가 성공하면 그 주문은
> 이미 거래소에 있다. 그 뒤의 어떤 예외도 「발사 실패」가 아니므로 재시도 루프로 되돌리지 않는다.」
> 문장은 매도 경로를 서술하지만 **근거(주문은 이미 거래소에 있다)는 축과 무관**하다.
> 매수는 재시도 루프가 없는 대신 **상태 롤백**으로 같은 오류를 범한다 —
> `:1512-1513` 의 `pending_buys.discard` + `pending_buy_amounts.pop` 은 **발사 실패의 정리**인데
> 발사 성공 뒤에도 실행된다.
>
> **② 피해가 실측된다(위 격리 하네스).**
> - `is_ticker_blocked_for_buy = False` → **같은 종목 재매수 가능**(`registry` 의 "주문중" 축이 사라진다)
> - `pending_buy_amounts` 소멸 → `_calc_used_funds()` 과소계상 → **예산 이중 사용**.
>   노출 창은 ms 가 아니다 — 미체결 LIMIT(프리장 `step_up(5)` · 폴백 5호가)은 **하루 종일** 안 채워질 수 있다
> - `raise` → `risk.on_tick`(`src/engine/risk.py:749`, **try 없음**) 관통 → 그 틱의 뒤 전략 청산 평가 소멸
>   + WS 재연결(cycle229 실측: 매수 9건이 매일 15:30 재연결 유발)
> - `cached_buyable_at` 미무효화(`:1384` 미도달) → 최대 60초 stale 캐시
>
> **③ 폴백은 구조가 매도 폴백과 같다.** `:1414` 중첩 `try` 의 핸들러는 `except KisApiError as e2`
> **하나뿐**이라 non-`KisApiError` 는 형제 핸들러(`:1386`·`:1511`)가 못 받고 `execute_buy` 를 관통한다
> — 실측으로 확증했다(`[FB] raised=TimeoutError`). 1단계 tester 관문의 N2 와 같은 사슬이다.
>
> **④ 게다가 구조적으로 더 나쁜 잠복 경로가 하나 있다.** 접수 후 구간에서 `KisApiError` 가 새면
> 주 경로는 `:1386` → `is_market_order_disallowed` 게이트 → **`:1418` 에서 두 번째 매수 주문을 발사**하고,
> 폴백은 `:1501` 이 「지정가 폴백도 거부」로 오분류해 **접수 성공한 주문에 cooldown 을 건다.**
> 오늘은 그 구간에 KIS 호출이 없어 도달 불가이지만, **구조는 열려 있다** — cycle327 이 매도에서 닫은
> 것과 같은 모양이다.
>
> **⑤ 그러나 리팩토링 커밋과 섞지 않는다.** 진행 규약 §3(1단계=1커밋=1의도) + cycle327 을
> 리팩토링과 분리한 선례. **2B 로 뺀다**(승인 면적 절).

### (1) 코어 헬퍼 신설 — `_insert_pending_or_absorb_race`(`:870-908`) 바로 뒤

```python
    #: (side, path) → 선행 체결통보 skip 로그의 **접두 수식어**. 관측 전용이라
    #: 조회가 실패해 행위를 바꿀 수 없어야 한다 — `.get(key, "")` 는 총함수다.
    _PENDING_SKIP_PREFIX = {
        ("buy",  "market"):   "매수 ",
        ("buy",  "fallback"): "폴백 ",        # 🔴 현행 문구 보존(아래 ⚠️)
        ("sell", "market"):   "매도 ",
        ("sell", "fallback"): "매도 폴백 ",
    }
    #: `trade_type` → 마커 축. 🔴 방향이 계약이다 — **행위값(trade_type)에서 관측값(side)을
    #: 파생**시킨다. 반대로 하면 `side` 오타 하나가 `trade_history` 의 매수/매도를 뒤집는다.
    _PENDING_SIDE_BY_TRADE_TYPE = {TradeType.BUY: "buy", TradeType.SELL: "sell"}

    async def _persist_pending_after_send(
        self, *, trade_type: TradeType, ticker: str, order_no: str,
        strategy_id: str, record_price: int, quantity: int, path: str,
    ) -> None:
        """접수 후 PENDING 영속화 — **매수·매도 4 경로 공용**.

        🔴 **이 함수는 경계를 닫지 않는다(예외를 그대로 전파한다).**
        접수 후 경계(cycle327 ⓑ)는 **호출자 층**의 책임이다 — 매도는
        `_persist_sell_pending_after_send` 가 닫고, 매수는 **아직 닫지 않는다**
        (그 부재가 결함이고 별도 사이클 소관이다). 여기에 `try` 를 들이면 매수 두
        경로의 예외 전파가 조용히 바뀐다 — `test_g2a_3` 이 그것을 구조로 막는다.
        """
        side = self._PENDING_SIDE_BY_TRADE_TYPE.get(trade_type, "")
        if order_no in self._completed_orders:
            self._completed_orders.discard(order_no)
            logger.warning(
                "%s응답보다 체결통보 선행 — PENDING INSERT 생략: %s (주문번호: %s)",
                self._PENDING_SKIP_PREFIX.get((side, path), ""), t(ticker), order_no,
            )
        else:
            record = TradeRecord(
                ticker=ticker,
                ticker_name=t(ticker).split("(")[0] if "(" in t(ticker) else "",
                trade_type=trade_type,
                price=record_price,
                quantity=quantity,
                profit_loss=0,                      # 체결 확정 시 계산
                status=TradeStatus.PENDING,
                strategy=strategy_id,
                order_no=order_no,
            )
            await self._insert_pending_or_absorb_race(
                record, side=side, ticker=ticker, order_no=order_no,
                strategy_id=strategy_id, path=path,
            )
```

### (2) 매도 헬퍼는 **경계 래퍼**로 얇아진다 — `:994-1047`

docstring(cycle327 ⓑ 설명)은 **한 글자도 바꾸지 않는다.** 본문만 위임으로 바뀐다.

```python
    async def _persist_sell_pending_after_send(
        self, *, ticker: str, order_no: str, strategy_id: str,
        record_price: int, quantity: int, path: str,
    ) -> None:
        """🔴 cycle327 ⓑ — 호출 시점에 **주문은 이미 나갔다**. (기존 docstring 전문 유지)"""
        try:
            await self._persist_pending_after_send(
                trade_type=TradeType.SELL, ticker=ticker, order_no=order_no,
                strategy_id=strategy_id, record_price=record_price,
                quantity=quantity, path=path,
            )
        except Exception:
            logger.exception(
                "[sell_post_send_error] ticker=%s order_no=%s strategy=%s path=%s "
                "— 주문은 접수됐다. 재발사하지 않고 체결통보·동기화에 맡긴다.",
                ticker, order_no, strategy_id, path,
            )
```

### (3) 호출부 A — `:1356-1379` 대체

LLM 훅(`:1324-1354`) **뒤**, 접수 로그(`:1381`) **앞**. 그 사이에 다른 문장을 끼우지 않는다.

```python
            await self._persist_pending_after_send(
                trade_type=TradeType.BUY,
                ticker=ticker,
                order_no=result.order_no,
                strategy_id=strategy.strategy_id,
                record_price=record_price,
                quantity=quantity,
                path="market",
            )
```

### (4) 호출부 B — `:1471-1493` 대체

폴백 LLM 훅(`:1447-1469`) **뒤**, 폴백 성공 로그(`:1495`) **앞**.
`record_price=fallback_price` · `path="fallback"` · `order_no=result.order_no`.

### 🔴 의도된 유일한 문구 변경 1건

**매수 폴백** skip WARNING 이 주문번호를 얻는다.

```
전: 폴백 응답보다 체결통보 선행 — PENDING INSERT 생략: 테스트종목(012200)
후: 폴백 응답보다 체결통보 선행 — PENDING INSERT 생략: 테스트종목(012200) (주문번호: BUY-000001)
```

- **접두 수식어 4개는 전부 byte 동일**이다(`_PENDING_SKIP_PREFIX` 가 현행 4 리터럴을 그대로 담는다).
  바뀌는 것은 매수 폴백에 ` (주문번호: %s)` 가 붙는 것 **하나**뿐이고, 이것은 **정보가 느는 방향**이다
  — 지금 그 줄은 어느 주문이 skip 됐는지 말하지 않는다.
- ⚠️ **21:30 리포트 `top_patterns` 파급** — WARNING 이라 `log_analysis_engine._aggregate_log_patterns`
  집계에 오른다. `_normalize_message` 가 종목코드·숫자를 마스킹해도 **꼬리가 키에 들어간다** →
  **배포일 전후의 그 패턴 문자열을 직접 비교하지 않는다**(1단계의 폴백 꼬리 통일과 같은 계열).
  건수는 영향 없다.
- ⚠️ `("buy","fallback") → "폴백 "` 이 다른 셋과 어긋나는 것(`"매수 폴백 "` 이 아니다)은
  **현행 문구를 보존한 결과**다. 정규화는 문구 변경이 1건 더 느는 선택이므로 **리뷰어 판단에 맡긴다**
  — 이 카드는 기본값으로 보존을 택했다.

---

## 행위 보존의 근거 / 행위 변경의 범위

### 행위 변경의 범위 = **0** (위 문구 1건 제외)

| 축 | 근거 |
|---|---|
| **예외 전파** | 코어 헬퍼에 `try` 가 **없다**. 매수 두 호출부에서 난 예외는 추출 전과 **같은 핸들러**(주 = `:1511 except Exception` → discard + `raise` / 폴백 = 형제 핸들러가 못 받아 `execute_buy` 관통)로 흐른다. 매도는 래퍼의 `except Exception` 이 그대로 받는다. `BaseException`(`CancelledError`)은 전후 모두 비흡수 |
| **주문번호 매핑이 `place_order` 응답 직후 동기 영역인가** | **그렇다.** 매핑 블록(`:1297-1317` · `:1429-1440`)은 한 줄도 움직이지 않고, 새 `await` 는 매핑 **뒤**에 놓인다. `test_cycle291_ast_scope::test_a10c` 의 `self._order_exchange[` / `self._order_division[` **5곳 카운트도 불변** |
| **최초 양보점** | 불변. `await coro` 는 첫 진짜 suspension 까지 동기로 구동하고, 코어 헬퍼 앞부분(`side` 파생 → `_completed_orders` 판정 → `TradeRecord` 조립)에 `await` 가 0건이므로 **최초 suspension 은 여전히 `await insert_trade`** 다. 래퍼 한 겹이 더 붙어도 같다 |
| **A-ATOMIC 구간** | 무접촉. `test_cycle276_ast_order_hook::_atomic_bounds` 가 잡는 구간은 `calc_buy_quantity`(`:1124`) ~ 첫 `pending_buys.add`(`:1165`) 이고 대상은 `:1356` 이후다. `_ATOMIC_SEGMENT_SHA` 불변 |
| **LLM 훅 순서 계약(cycle276 C7/C8)** | 의미 보존. 훅은 여전히 매핑 **뒤** · 선행 판정과 INSERT **앞**이고, 훅 끝 ~ 헬퍼 호출 사이 `await` 0건이다. 가드는 **앵커만** 헬퍼 호출로 재조준한다(아래) |
| **`_insert_pending_or_absorb_race` 본체·`_FILL_DURING_INSERT_MARKER`** | 무접촉. `side` 값도 `"buy"`/`"sell"` 그대로 |
| **record 9필드** | 동일. `profit_loss` 는 매수 생략 ↔ 모델 기본값 `0` 이라 같은 값 |
| **`pending_buys`/`pending_buy_amounts`/`cached_buyable_at`/`_pending_buy_orders`** | 대상 구간 안 참조 **0건** — 전부 헬퍼 밖에 남는다 |
| **`_selling` add/discard** | 매도 축 무접촉(1단계에서 이미 확인, 대상 구간 참조 0건) |

### `_FROZEN_SEGMENTS` 와 겹치지 않는가

`tests/unit/ast/test_cycle287_ast_scope.py` 의 3 항목을 현재 소스에 대고 확인했다(전부 초록):

```
execute_buy_prf                  앵커 :1187-1188 ~ :1273   ← 대상 A(:1356) 밖
execute_sell_pre_nxt_preconvert  execute_sell 안            ← 무접촉
cycle286_nxt_evidence            execute_sell 안            ← 무접촉
```

코어 헬퍼는 `:908` 뒤에 삽입되므로 `execute_buy_prf` 가 아래로 밀리지만 **앵커가 문자열 매칭**이라
라인 이동은 무해하다.

---

## 🔴 `test_g328_0c` 를 무엇으로 대체하는가 — **대체하지 않는다. 의미가 살아나고, 거울을 하나 더 세운다**

1단계 카드는 「`test_g328_0c`(헬퍼가 `execute_buy` 에서 안 불린다)는 2단계에서 **반드시 뒤집히거나
삭제된다**」고 적었다. **그 전제는 "매수가 매도 헬퍼를 부른다" 였고, 이 카드는 그렇게 하지 않는다.**

- `test_g328_0c` 의 **단언은 그대로 참이다** — 매수는 `_persist_sell_pending_after_send`(경계 래퍼)를
  부르지 않는다. 그리고 이제 그 단언이 **더 중요해진다**: "매수가 실수로 매도 경계·매도 마커·
  `매도 ` 접두를 물려받지 않는다" 를 재는 자리가 된다.
- ⚠️ **docstring 은 반드시 다시 쓴다** — "2단계에서 반드시 뒤집히거나 삭제된다"는 문장이 **거짓이 되고**,
  그대로 두면 다음 사람이 낡은 가드로 오해해 지운다.
- 그리고 **거울 두 개를 새로 세운다**(아래 G-2A-1·G-2A-2). 그중 **G-2A-2 가 이 단계에서
  가장 중요한 신규 가드**다 — `execute_sell` 이 코어 헬퍼를 **직접** 부르게 되는 순간
  cycle327 ⓑ 경계가 매도에서 사라지기 때문이다.

---

## 검증 방법 (돌연변이)

### 신규 구조 가드 (Red, `tests/unit/ast/test_cycle328_sell_pending_helper.py` 확장 또는 신규 파일)

| # | 단언 | 닫는 돌연변이 |
|---|---|---|
| **G-2A-1** | `execute_buy` 가 **코어 헬퍼**를 정확히 2회 호출하고 `_persist_sell_pending_after_send` 를 **0회** 호출 | 매수가 매도 래퍼를 부름(마커·접두·경계가 통째로 매도가 된다) |
| **G-2A-2** 🔴 | `execute_sell` 이 **래퍼**를 정확히 2회, **코어 헬퍼를 0회** 직접 호출 | 매도가 코어를 직접 부름 → **cycle327 ⓑ 경계 소멸**(무증상, 행위 테스트로는 폴백+`KisApiError` 조합에서만 드러난다) |
| **G-2A-3** 🔴 | 코어 헬퍼 본문에 `ast.Try` **0건** | 코어에 `except Exception` 삽입 → **매수 두 경로의 예외 전파가 조용히 바뀐다**(= 승인 없이 행위 변경) |
| **G-2A-4** | 4 호출부의 `trade_type` 리터럴이 `TradeType.BUY`×2(execute_buy) · `TradeType.SELL`×1(래퍼) | 축 뒤바뀜 |
| **G-2A-5** | `_PENDING_SIDE_BY_TRADE_TYPE` / `_PENDING_SKIP_PREFIX` 조회가 `[...]` 인덱싱이 아니라 `.get(...)` | `test_g328_4` 의 논리를 그대로 확장 — 관측 조회 실패가 INSERT 를 통째로 건너뛰게 하지 않는다 |

### 기존 가드 재조준 (tests/ 만 — 승인 불필요)

| 파일::케이스 | 지금 | 재조준 후 (의미 보존) |
|---|---|---|
| `test_cycle328_…::test_g328_0b` | 래퍼 2회/`execute_sell` | **무변경** ✅ |
| 〃 `::test_g328_0c` | 래퍼 0회/`execute_buy` | **단언 무변경**, docstring 재작성 |
| 〃 `::test_g328_1` | 매핑 → 래퍼 호출 순서·사이 `await` 0 | 진입점 집합을 **{래퍼, 코어}** 로 넓히고 `execute_buy`(2곳)까지 확대 = **4 호출부 전수** |
| 〃 `::test_g328_2` | 래퍼 안 probe 앞 `await` 0 | **코어 헬퍼**로 재조준 |
| 〃 `::test_g328_3`/`3b` | 래퍼의 `try` 1개·핸들러 1개·`Exception` | **무변경** ✅ (래퍼가 1문 `try` 라 오히려 더 조여진다) |
| 〃 `::test_g328_4` | 래퍼 안 `.get()` | **코어 헬퍼**로 재조준(G-2A-5 와 합칠 수 있다) |
| `test_cycle276_ast_order_hook::test_c1_7` | 훅 < `_completed_orders` probe(`execute_buy` 안) | probe 가 코어로 이동 → 앵커를 **코어 헬퍼 호출 lineno** 로. 「probe 가 코어의 첫 분기」는 `test_g328_2` 가 이미 보장 |
| 〃 `::test_c1_8` | 훅 < `_insert_pending_or_absorb_race`(`execute_buy` 안) | 같은 앵커로 재조준. 「코어 안에서 probe < insert」 절을 1줄 추가 |
| 〃 `::test_c1_9` | 훅 끝 ~ insert 사이 `await` 0 | 같은 앵커. ⚠️ 헬퍼 호출 자체가 `ast.Await` 이므로 **`< call.lineno` 엄격 부등호**로 자기 자신을 제외한다(`test_g328_1` 선례) |
| 〃 `::test_c1_6`·`c2_2`·`c3_1`·`c3_2`·`c4_1`·`c4_2` | — | **무변경** ✅ (매핑 순서·훅 개수·A-ATOMIC sha·`place_order` kwargs·import 증가분 전부 무접촉) |
| `test_cycle291_ast_scope::test_a10c` | `_order_exchange[` 5곳 | **무변경** ✅ |

### 행위 돌연변이 (재조준 뒤 반드시 붉어지는지 확인)

| # | 깨는 것 | 붉어져야 할 것 |
|---|---|---|
| M-B1 | 호출부 A 의 `record_price`↔`quantity` 맞바꿈 | `test_buy_pending_record_carries_order_price_and_quantity`(cycle333 A3) |
| M-B2 | 호출부 B 의 `record_price=fallback_price` → `record_price` | `test_buy_fallback_pending_record_carries_fallback_price_symmetrically` |
| M-B3 | 호출부 B 의 `path="fallback"` → `"market"` | **J-BUY-3**(문구 쌍) — 지금은 **아무것도 붉어지지 않는다** |
| M-B4 | 호출부 A 의 `trade_type=TradeType.BUY` → `SELL` | cycle333 A3 2건 + G-2A-4 |
| M-B5 | 코어에 `except Exception: pass` 삽입 | G-2A-3 + **J-BUY-1/J-BUY-2**(전파 핀) — 지금은 `test_c7_1`/`c7_2` 만 붉어지고 폴백 축은 **무방비** |
| M-B6 | `execute_sell` 이 코어를 직접 호출 | **G-2A-2** — 행위로는 1단계 tester 관문의 N2·N3·N4 가 필요하고, 그건 표본 의존이다 |
| M-B7 | `_PENDING_SIDE_BY_TRADE_TYPE` 를 `side` 인자로 되돌리고 호출부 하나에 `"sell"` 오타 | cycle333 A3(`record.trade_type`) + `[buy_fill_during_insert]` 마커 부재 |
| M-B8 | 코어 맨 위에 `await asyncio.sleep(0)` | `test_g328_2`(재조준본) + `test_c1_9` |

---

## 이 단계가 건드리지 않는 것

- `execute_sell` **본문** — 한 줄도(호출부 2곳은 래퍼를 그대로 부른다)
- `handle_execution_notice` · `_handle_buy_fill` · `_handle_sell_fill` · `_resolve_pending_buy_owner`
  · `_cancel_and_reorder` · `_emit_fill_qty_src` · `_emit_ordered_qty_mismatch` — **cycle329·331·332 산출물 전부 무접촉**
- 주문번호 매핑 5종 등록 블록 4곳 + `_pending_buy_orders` 등록
- `llm_buy_gate.observe_order` 훅 2곳(`:1324-1354` · `:1447-1469`) — 호출·인자·감싼 `try` 전부 무접촉
- `pending_buys` / `pending_buy_amounts` / `cached_buyable_at` 생명주기 — **헬퍼 밖 그대로**
- A-ATOMIC 구간(`:1124-1165`) · PR-F/GTP 블록(`:1187-1273`, frozen segment)
- `except KisApiError` 분류 블록(`:1386-1510`)의 **분기 구조·순서**(대상 B 는 그 안의 `try` **본문** 3줄뿐)
- `_insert_pending_or_absorb_race` 본체 · `_FILL_DURING_INSERT_MARKER`
- `scheduler.py` · `risk.py` · `realtime/**` · `auth/**` · `api/order.py` · DB 스키마 · 프론트
- 매매 파라미터 · `DEFAULT_PARAMS` · 운영 DB

---

## 승인 면적

| 항목 | 값 |
|---|---|
| **8영역 접촉** | `src/engine/order_engine.py` **1파일**(이미 `_APPROVED_EIGHT_AREA_PINS` 등재 — 키 집합 변경 없음, **값만 갱신**) |
| **sha 재핀 지점** | **14곳** = 전체 파일 sha 핀 **10 파일** + 메타가드 **4**<br>핀 10 = `test_cycle222a3_ast_followup_fixes` · `test_cycle223_ast_donchian_exit_fix` · `test_cycle223f_ast_manual_apply_safeguard` · `test_cycle226_zero_breakout_defense`(이상 `_PIN_GUARD_FILES` 자매 4, **넷 전부 같은 값**) + `test_cycle274_ast_llm_gate` · `test_cycle278_ast_catalog_guards` · `test_cycle282_ast_purity` · `test_cycle290_ast_scope` · `test_cycle294_ast_stage3` · `test_cycle297_ast_scope`<br>메타가드 4 = `test_cycle223g3_ast_guard_sees_staged`(`test_g3_8`·`g3_9a`·`g3_9b`) + `test_cycle276_ast_order_hook::test_c6_4c` |
| 현재 값 | `e1beeb1fffab6ca5193d6954e9164e75f1d0d16eaaaedbcbb7d3fef1328dfa2e` (10/10 일치 실측) |
| **문서** | `src/engine/CLAUDE.md` 규칙 ⑤(`_persist_sell_pending_after_send` 계약·`test_g328_*` 목록) 갱신 필요 → `/sync-docs`(Phase 4.8). ⚠️ `src/**` 이라 **배포 모드 full** (이미 `order_engine.py` 로 full) |
| **domain-consult 선행** | **불필요**(이 카드 범위 한정) — 진입·청산·수량·사이징·손절 규약·`DEFAULT_PARAMS` 를 건드리지 않고 행위 변경이 0 이다. **2B 는 필요** |
| **롤백 수단** | 1커밋 revert + 장외 배포. 파라미터 다이얼 없음(1단계와 동일 판단 — 예외 경계에 스위치를 달지 않는다) |
| **영향 인덱스** | `order_engine.py` 의 `direct_tests` = **1,067 = 백엔드 전부**, `transitive_tests` 0. **선택력 0 이므로 "영향 테스트만" 을 시간 절약 근거로 인용하지 않는다** |
| 스위트 규모 | 전체 **11,505 collected** · AST **1,835 collected**(2026-09-21 01:0x 실측) |

### 단계 종료 조건

- 전체 스위트 초록 + AST 가드 초록
- sha 재핀 10 파일(자매 4는 **같은 값**) → ⚠️ **재핀 뒤 전체 스위트 재실행**(주석 한 줄이 핀 수십 곳을 깬다 — 2026-09-18 CI red 사고)
- `python tools/test_impact/build_index.py` (신규 테스트 **파일**을 만들 경우 필수)
- 관문 3인 **순차**(1단계 사고 재발 방지 — 쓰기가 섞이는 리뷰어는 `isolation: "worktree"` 또는 순차, 리뷰어가 도는 동안 전체 스위트를 돌리지 않는다)

---

## 🔴 선행 의뢰 — 현행 행위 핀 4건 (tdd-engineer, **오늘 새벽에 landable**)

전부 **현재 코드에 대해 초록**이다(1단계의 시나리오 I 와 같은 역할). `tests/` 단독이라
`compose_up_changed.sh` **none 모드** = 빌드 없는 `up -d` → **배포 위험 0**, CI 는 정상 통과.

| # | 파일 | 단언 |
|---|---|---|
| **J-BUY-1** | `tests/unit/engine/test_order_engine_buy.py` | 주 경로 접수 후 `insert_trade` 실패(`TimeoutError`) → `pytest.raises` · `place_order` **1회** · `pending_buys` **비었다** · `pending_buy_amounts` **비었다** · `_order_qty[order_no]` **남아 있다** (실측값과 일치) |
| **J-BUY-2** | 〃 | **폴백** 접수 후 `insert_trade` 실패 → `pytest.raises` · `place_order` **2회** · `pending_buys` **유지** · `block_low_funds` **미등록**. 🔴 이 축은 **현재 리포 전체에 회귀 0건**이다 |
| **J-BUY-3** | 〃 | skip 분기 WARNING **쌍** — 주 경로 문구에 `"폴백"` **없음** + 폴백 문구에 `"폴백"` **있음**. ⚠️ caplog 은 `levelno >= WARNING` + `"PENDING INSERT 생략" in msg` 로 한정(CI 루트 로거 DEBUG 대비) |
| **J-BUY-4** | 〃 | 주 경로 skip 문구에 `order_no` 포함 (폴백은 **현재 미포함**을 명시 단언 → 2단계에서 이 한 줄이 **의도적으로 붉어지고** 변경이 눈에 보인다) |

**J-BUY-1·2 는 결함을 「고정」하는 핀이다** — 2B 가 그 둘을 다시 쓰게 되며, 그때 붉어지는 2건이
「지금 무엇을 바꾸는가」의 정확한 명세가 된다. 핀 없이 2B 를 하면 무엇이 바뀌었는지 아무도 못 본다.

---

## ⏱ 일정 판단 — **오늘 새벽에 배포까지 끝낼 수 없다. 범위를 줄인다**

지금 **2026-09-21(월) 01:0x KST**, 자동 매매 **07:45** 시작. `order_engine.py` 변경 = **full 모드 =
backend 재생성**. 보유 포지션이 있으면 **09:00~15:30 push 금지(D6)** 라 다음 장외 창은
**15:30~16:00(30분)** 또는 **21:35~익일 07:45** 뿐이다.

1단계 domain 관문의 **부대조건**이 이 상황을 정확히 지목했다 —
「월요일 새벽에 배포하고 07:45 부팅을 맞으면 **문제가 났을 때 되돌릴 창이 15:30까지 없다**」.
남은 6시간 45분에 Red(가드 5 + 행위 4) → Green → 재핀 14 → 전체 스위트 2회 → **관문 3인 순차** →
커밋 → CI → 배포 → 실측을 넣는 것은 비현실적이다(1단계는 관문이 **그물 구멍 4개**를 찾아
도중에 시나리오 J 를 세우게 만들었다).

### 권고 — 2분할

| 창 | 하는 것 | 배포 영향 |
|---|---|---|
| **지금 ~ 07:45** | 이 카드 + **선행 의뢰 J-BUY-1~4**(현행 행위 핀, 현재 코드에 초록) | `tests/` + `_workspace/` 단독 = **none 모드**(빌드 없음) · CI 초록 |
| **21:35 이후** (또는 15:30~16:00 이 넉넉하면 그때) | Green + 재핀 + 관문 3인 + 배포 + 실측 | full 모드. 21:35~ 창은 익일 07:45까지 열려 있어 **롤백 여유가 가장 크다**. ⚠️ **20:00~21:35 은 금지**(20:00 자문 · 20:05 metrics · 20:30 일봉 적재 · 21:30 정산) |

🔴 **AST Red 가드(G-2A-1~5)는 지금 넣지 않는다** — Green 없이는 붉어 CI 를 깨뜨린다. Green 과 같은 커밋.

---

## 다음 단계 후보

**2B(별도 사이클, 승인 + `domain-consult` 선행 — 행위 변경)**
매수 접수 후 경계 `_persist_buy_pending_after_send` 를 세워 `[buy_post_send_error]` 를 남기고,
`:1512-1513` 의 상태 롤백을 **발사 전 실패에만** 적용한다. 이 카드의 층 분리 덕에 **래퍼 한 개(≈12줄)
추가 + 호출부 2줄 교체**로 끝난다(= 다음 변경이 싸졌다). 다시 쓸 테스트는 `test_c7_1`·`test_c7_2`
+ J-BUY-1·J-BUY-2 **정확히 4건**이고, 그 4건이 곧 변경 명세다.
🔴 2B 를 먼저 하면 안 되는 이유 = 지금 매수 축엔 문구·전파 그물이 **0건**이라 무엇이 바뀌었는지 못 잰다.

3. 폴백 경로 전체(⑰a~⑰g) → `_handle_sell_market_disallowed` — 중첩 최심. **선행 = N2·N3·N4 사슬의 분기 조합 그물**
4. APBK0400 잔고 재대조 5분기(⑮) → 판정 순수 함수 + 집행
5. 최종 실패 후처리(⑲) → `_reconcile_sell_insufficient` — ⚠️ `test_cycle185_cluster1_ast` 재조준 선행
6. 주문사양 결정(④~⑧) → `_resolve_sell_order_plan` — ⚠️ frozen segment 재핀 승인 선행
7. 주문번호 매핑 5종 공통화 — ⚠️ `test_a10c` 의 **의미를 보존하는 가드 재설계 선행**. 가장 마지막

---

## 보고할 한 문장

이 단계가 끝나면 「주문이 나간 뒤 무엇을 어떻게 장부에 적는가」라는 규칙을
**매수·매도 네 경로 통틀어 한 곳에서만 고치면 된다.**
그리고 매수에 **접수 후 경계가 없다는 사실**이 처음으로 코드에서 눈에 보이게 된다 —
지금은 466줄 함수 두 군데에 묻혀 있다.
