# 리팩토링 리뷰 — 2026-08-09

## 검토 범위
- **트리거**: 사용자 수동 `/refactor-review` (범위 미지정 → 최근 ~10 사이클이 착지한 영역 + 만성 비대화로 확정)
- **최근 사이클**(git log): VCP/BFB 확대 유니버스(d698a48~60a6814) · 성장경로 로드맵 · 예산 불변식 가드 · BREAKOUT cap 제거 · 레짐 관찰 정직화 · market_op 소켓 가드 · NXT 프리장 게이트 · VCP/BFB 진단 대시보드 · H-1 트레일링 복구 · 섹터 컬럼 · kojiro Σ리스크 캡
- **대상 모듈**(라인 수 실측):
  - engine: scheduler.py **4230** / scanner.py **2978** / order_engine.py 1414(8영역) / recommendation_engine.py 1126 / log_analysis_engine.py 818
  - strategies: vcp 1450 · bfb 1379 · vb 1139 · kojiro 1125 · donchian 1110 · ltv 885
  - db: system_config 811 · stock_master_daily 742 · trade_history 710 · stock_master 710
  - frontend: StockMaster.tsx 1442 · ScanMonitor.tsx 1227 · Settings 745 · Recommendations 681 · IntegrationToggleCard 624
- **방법**: 5개 refactor-expert 관점 병렬 분석(AST 마스킹 해시 대조 + grep 소비처 실증 + pyflakes) + HIGH 카드 2건 domain-expert 행위 영향 평가
- **8영역 확정**: `risk.py` · `order_engine.py` · `scheduler.py` · `session.py` · `strategy_registry.py` · `api/order.py` · `realtime/` · `auth/` (+ scanner.py 매수경로). **8영역 접촉 카드는 전부 HIGH + 위임 분해 방식만 + domain-expert 평가 첨부**.

---

## 핵심 진단
1. **최근 사이클이 VCP/BFB 에 평행 인프라를 대량 복제** — P1 청산복구·확대·191/213 쿨다운·H-1 이 만든 헬퍼가 VCP/BFB 2벌이 아니라 **3~6전략**으로 퍼짐. 이미 base 승격된 `_apply_high_since_buy_from_candles`(H-1)와 **동일 클래스**의 잔여 중복 다수.
2. **scheduler.py 재비대**(사이클 67 분해로 3007L → 현재 4230L, +1223L) — 정당한 응집이 아니라 **위임 모듈이 있는데도 신규 task 를 본체에 직접 작성한 드리프트**(데이터적재 task 군 122/126/129/171/C1 + kojiro 스윙 폴루프 + H0UNMKO0 구독 149/214).
3. **프론트 횡단 유틸 부재** — P/L 색상 8곳·KST 포맷 13파일 각자 재구현(정본은 frontend/CLAUDE.md 컨벤션 1곳인데 코드는 분산).
4. **확대·정정 이전 스테일 주석 잔존**(sync-docs 3건과 동일 클래스) — "220일"·죽은 import·죽은 scan_stats 키.

---

## 권고 카드 — Group A: 전략 헬퍼 base/scanner 승격 (8영역 diff 0)

> 공통 안전 근거: 승격 대상은 **전략 파일 + strategy_base.py + scanner(매수경로 밖)** 뿐. order_engine 의 `on_position_closed` 등 **호출 site 는 불변**(정의만 이동). prepare 헬퍼는 매수 진입 *전*(사이클 38). FREEZE(kojiro)·다크런치(VCP/BFB)는 실행 로직 byte 보존으로 행위 불변. 로그 접두사는 `_HIGH_RECOVER_LABEL` ClassVar 선례(H-1)로 보존. **중복 정의 0건 AST 가드**(H-1 `test_no_duplicate_helper_definition_in_strategy_files` 미러)를 각 카드에 동반.

### 카드 A1 — `_apply_price_filter_in_prepare` 를 StrategyBase 로 승격 ⭐최대 실익
- **위험**: LOW(5전략) / MEDIUM(kojiro 포함 시)
- **현상태**: `vb:463` · `ltv:515` · `donchian:603` · `bfb:753` · `vcp:905` — 5벌 **byte-identical**(로그 접두사 1토큰만 상이, 각 ~50L). `kojiro:601` 동일 로직 압축표기(6번째). scanner 의 `_apply_price_filter`(구독 전 필터)와는 **별개 소스** — prepare 판은 어디에도 미추출. **프로젝트 최대 prepare 중복(~250~300L)**.
- **권고**: `StrategyBase._apply_price_filter_in_prepare(tickers)` 단일 정의 + `_PREPARE_LOG_PREFIX: ClassVar[str]`. get_price_filter 단일 source·protected early-return·bfdy_clpr graceful 전부 위임.
- **회귀가드**: 기존 cycle148(VB) + cycle151(LTV/donchian/BFB/VCP factory 파라미터화). ⚠️ **cycle151 의 소스레벨 AST 체크**(전략 소스파일에서 `_LABELS` 문자열 존재 assert)는 base 이동 시 **의미 전환**(hasattr 은 상속 통과하나 소스 AST 존재 검사 깨짐) → 인수인계 명시. 추가 = 중복정의 0건 AST + **kojiro 편입 시 kojiro 전용 회귀**(cycle151 미포함).
- **예상효과**: ~250L 순감. 가격 필터 정책 6곳 드리프트 구조 차단.
- **권장**: **1차 5전략만, kojiro 후속**(FREEZE 엄정성 — log 접두사 변경 회피).

### 카드 A2 — `_apply_master_block_filter_in_prepare` wrapper 를 StrategyBase 로 승격
- **위험**: LOW
- **현상태**: `vb:439` · `ltv:491` · `donchian:579` · `bfb:729` · `vcp:881`(+kojiro:578) — 본체는 이미 `scanner.apply_master_block_filter`(사이클 157) 추출됨, **여기 중복은 wrapper 자체**(~13~23L). 로그 접두사만 상이.
- **권고**: wrapper 를 base 단일 정의(A1 과 동일 ClassVar). scanner 헬퍼는 불변.
- **회귀가드**: cycle157(5전략 hook) + cycle155(13키). cycle157 소스 hook 존재 assert = 의미 전환. 추가 = kojiro 가드 + 중복정의 0건.
- **예상효과**: ~65~115L 순감. **A1 과 한 사이클로**(동일 회귀 세트·의미 전환 공유).

### 카드 A3 — `_refine_cooldown_business_days` 를 StrategyBase 로 승격
- **위험**: LOW
- **현상태**: `vcp:1299` · `bfb:1191` · `ltv:134` · `vb:1117` — **4벌 byte-identical**(접두사만, 각 ~10L). `add_business_days` + `_cooldown_until[ticker]` + `reentry_cooldown_days` 만 사용(duck-typing 성립).
- **권고**: `StrategyBase._refine_cooldown_business_days(ticker)` 승격.
- **회귀가드**: cycle191/201/213 쿨다운 AST 가드(호출 site 검사 — 정의 이동은 호출 불변이라 통과 예상, **재실행 검증 필수**). `_cooldown_until` daily-reset 0건(G-191-NO-DAILY-RESET) 보존.
- **예상효과**: ~30L 순감.

### 카드 A4 — `register_cooldown_after_exit` + on_position_closed 쿨다운 스케줄링 tail base 화
- **위험**: MEDIUM (BFB override divergence)
- **현상태**: `register_cooldown_after_exit` 3벌 동일(vcp:1288·ltv:123·vb:1106) + **BFB(:1177)만 `_breakout_first_seen.pop` 1행 추가**. on_position_closed 말미 `create_task`+`RuntimeError`+`coro.close()` 6줄 블록이 VCP/BFB/LTV 반복.
- **권고**: base `register_cooldown_after_exit` 공통 3줄 승격 + BFB override(`super()` + pop). 스케줄링 tail 을 base `_schedule_cooldown_refine(ticker)` 헬퍼 추출(각 on_position_closed 1줄 호출). **on_position_closed 본체는 전략별 정리가 달라 승격 금지, 헬퍼 호출만**.
- **회귀가드**: 쿨다운 AST 3종이 on_position_closed 내 호출 ≥1 검사 → 호출은 각 전략에 남겨야 함(정의만 이동). ⚠️ LTV `was_limit_up` 면제(`ltv:155`)는 전략 고유 — 절대 base 승격 금지. 추가 = base 헬퍼 단위 + LTV 면제 계약 회귀.
- **예상효과**: ~25L 순감 + 쿨다운 배선 규약 단일화.

### 카드 A5 — `_rederive_entry_atr` 를 StrategyBase 로 승격 (`_HIGH_RECOVER_LABEL` 선례 미러)
- **위험**: LOW
- **현상태**: `vcp:1264` · `bfb:1229` · `donchian:787` — 3벌 **실행 본문 byte-identical**(차이=타입힌트·로그 접두사·한글 예외 메시지, 각 ~16L).
- **권고**: `StrategyBase._rederive_entry_atr(ticker, pos, candles, atr_period)` + `_ENTRY_ATR_REDERIVE_LABEL: ClassVar` — **이미 승격된 `_apply_high_since_buy_from_candles`/`_HIGH_RECOVER_LABEL`(strategy_base.py:506,586)과 완전 동형**. `self._atr`(카드 A6)·`_entry_atr` 스탬프 커플링 불변식 보존.
- **회귀가드**: donchian turtle / bfb restart / cycleC vcp. 추가 = 중복정의 0건 AST. kojiro 미보유(Wilder 경로) → 상속돼도 호출 0.
- **예상효과**: ~32L 순감.

### 카드 A6 — SMA `_atr` static 을 StrategyBase 로 승격 (kojiro Wilder 이원화 봉인)
- **위험**: LOW
- **현상태**: `vcp:795` · `bfb:633` · `donchian:464` — 3벌 byte-identical(각 ~12L).
- **권고**: `StrategyBase._sma_atr(highs,lows,closes,period)` static. **네이밍 `_sma_atr` 명시**로 kojiro Wilder 혼동 방지.
- **회귀가드**: 추가 = 중복정의 0건 + **"kojiro 는 base `_sma_atr` 미사용" AST 가드**(ATR 이원화 영구 봉인, CLAUDE.md "재사용 금지" 명시). ⚠️ kojiro 절대 편입 금지.
- **예상효과**: ~24L 순감. **A5 와 묶어 처리**(같은 base 이동·ATR 선례 정합). 단독 발의 후순위.

### 카드 A7 — 터틀 sizing 계열(`_turtle_buy_quantity` + turtle calc_buy_quantity) base 승격
- **위험**: MEDIUM (매수 수량 = 리스크 직결 경로)
- **현상태**: turtle `calc_buy_quantity` 3벌 동일(vcp:1216·bfb:1129·donchian:1058). `_turtle_buy_quantity` vcp/bfb 동일, donchian 은 후보 ATR 키(`atr` vs `atr14`)만 상이. `compute_unit_qty_guarded` 위임·`_entry_atr` 스탬프·`_apply_budget_limit` 경유 동일.
- **권고**: base 승격 + `_CANDIDATE_ATR_KEY: ClassVar`("atr14"/donchian="atr").
- **회귀가드**: `_turtle_buy_quantity` 참조 1파일뿐(커버리지 얇음) → **tdd-engineer 선행 회귀 필수**. ⚠️ **A-GATE AST 가드**(7전략 모든 calc_buy_quantity return 이 `_apply_budget_limit` 경유)가 전략파일별 검사인지 확인 후라야 승격 — base 이동 시 스캔 대상 조정 필요. 추가 = 3전략 수량 동등성 골든 테스트.
- **예상효과**: ~60L 순감. **회귀 위험이 A1~A6 보다 높아 후순위, 선행 가드 없이 발의 보류**.

### 카드 A8 — `_scan_universe` 종목필터 루프 → scanner 공통 헬퍼
- **위험**: MEDIUM
- **현상태**: vb/ltv/donchian/bfb/vcp/kojiro **6벌 byte-identical**(6자리 isdigit + ETF 제외 + `prdt_abrv_name` 폴백 + `ticker_names` 갱신 + append, 각 ~12L).
- **권고**: `scanner.filter_valid_universe_rows(rows)` 신규 헬퍼로 **루프만** 흡수. ⚠️ `_scan_universe` 전체 추출 금지(list_by_filter 인자·0종목 ERROR·graceful 블록이 전략별 상이).
- **회귀가드**: cycle108(3전략) + cycle170(stage counts). 추가 = 헬퍼 전용 단위(6자리/ETF/폴백/ticker_names 갱신) tdd 선행. 매수 후보 반환 원소·순서 불변(cycle170 G-A-1 정신).
- **예상효과**: ~60L 순감 + ETF/좀비차단 규칙 단일 진실원.

### 카드 A9 — `_fetch_one` 일봉 fetch 클로저 → StrategyBase 병렬 fetch 헬퍼
- **위험**: MEDIUM (HIGH 인접 — 매수 target 경로)
- **현상태**: 6전략 구조 동일(`get_recent_daily_normalized` + gather). 차이 = days/min_required 값·로그.
- **권고**: `StrategyBase._gather_daily_candles(tickers, *, days, min_required, log_label)`. days/min_required **명시 인자 유지**(사이클 173 "None 의존 금지" 계약).
- **회귀가드**: ⚠️ **cycle173 이 각 전략 소스에서 `get_recent_daily_normalized` 호출을 AST assert** → base 이동 시 **5건 의미 전환**. 추가 = kojiro min_required=80 값 가드 + **domain-expert 동등성 재확인 첨부**(락/신선도 게이트 경유 불변). donchian min_required=63 절대 하향 금지.
- **예상효과**: ~40L 순감 + min_required 하향 실수(silent 왜곡) 단일 지점 통제.

### 카드 A10 — `get_scan_stats`/`get_scanned_tickers` base 기본구현 (optional)
- **위험**: LOW
- **현상태**: 6전략 1행 동일(momentum 만 별도 — scanner 모듈 dict).
- **권고**: base 기본구현(momentum override 유지).
- **예상효과**: ~12~20L. **실익 marginal — 단독 금지, 다른 카드 곁다리로만**.

---

## 권고 카드 — Group B: 8영역/대형 모듈 위임 분해

> 위임 패턴 정본(사이클 51/60/61/63/67): **scheduler 인자 first-arg + 2줄 wrapper + facade/모듈 함수 + logger 명시 `getLogger("src.engine.scheduler")`(운영 grep 이력 보존) + lazy import(`from src.engine import X` 또는 `sys.modules.get`, 순환 import 차단)**.

### 카드 B1 — 저녁 데이터적재 task 8종을 `data_load_tasks.py` 로 위임 분해 ⭐재비대 1순위
- **위험**: MEDIUM (매수 hot path 무관 — 16:00~20:00 데이터 계층)
- **현상태**: `scheduler.py:3045-3511` **459L**. full_universe/daily/basics/master/financial/evening_funnel/purge/scan_pool_eager 8종 task loop. **대부분 이미 `run_periodic_task_loop` 위임 facade**(본체=import+헬퍼 호출).
- **권고**: 신규 `data_load_tasks.py` 로 본체 이관, scheduler 는 `async def _X_task_loop(self): await data_load_tasks.X(self)` 2줄 wrapper. `capture_funnel_snapshots`(이미 모듈 함수)도 동반. TIME_* 상수·count polling 게이트는 lazy 참조.
- **회귀가드**: daily 15·basics 13·master 10·financial 10·full_universe 54·evening_funnel 6. **wrapper 메서드명 + task_attrs 4위치(사이클 79 G-AST2) + expected_members(zombie 가드) 보존**. patch 경로 적응(`data_load_tasks.X`). 추가 = facade patch 안전선 AST(사이클 67 G-16).
- **선행의뢰**: tester — 16:00→16:10→16:20→16:30→16:40 순서 의존성(count polling 가드) 보존.
- **예상효과**: scheduler −420L. 재비대 1순위 제거 + 신규 데이터 task 재유입 구조 차단.

### 카드 B2 — scanner 의 stock_master 적재 로직을 `stock_master_loader.py` 로 분리
- **위험**: MEDIUM (scanner **매수경로 아님** — 16:00+ 데이터 계층)
- **현상태**: `scanner.py:1437-2978` ~1436L(48%). full_universe_load_krx/kis · daily/basics/financial/master_load_once · fetch_market_cap_page 등.
- **권고**: 신규 `stock_master_loader.py` 로 적재 함수군 이관 + scanner **re-export 호환**(import 경로 무변경 → B1 참조 불변). `_is_master_blocked_for_entry`/`apply_master_block_filter`(매수 진입 전 차단)는 경계 판단 후 — 매수경로 인접이면 잔류.
- **회귀가드**: **핵심 = `git diff scanner.py(매수경로) = 0`**. re-export 로 import 유지 시 대부분 통과. patch 경로 적응.
- **선행의뢰**: tester — 매수경로(scan_stocks/subscribe_filtered_stocks/priority_groups) byte 보존 + 16:00 task 정상 발화.
- **예상효과**: scanner −1200L(2978→~1750). 적재 변경이 매수경로 파일 미접촉.

### 카드 B3 — recommendation_engine 백테스트 오케스트레이션을 `backtest_orchestration.py` 로 분리
- **위험**: MEDIUM (20:00 AI자문 계층, 8영역 아님, offline)
- **현상태**: `recommendation_engine.py:515-967` ~450L. enqueue/poll/emit_pending 5함수가 AI자문 핵심과 혼재.
- **권고**: 신규 모듈로 5함수 이관, `generate_recommendations` 의 호출은 lazy import. `_backtest_poll_loop_running` 전역도 동반.
- **회귀가드**: backtest 32 + poll 5. 추가 = `_emit_pending_summaries` 단위 tdd 선행.
- **선행의뢰**: tester — 20:00 자문 INSERT 보존 + MCP 다운 graceful(summary=null).
- **예상효과**: −440L(1126→~690).

### 카드 B4 — [HIGH] kojiro/donchian 스윙 폴루프 → `swing_poll_loops.py`
- **위험**: **HIGH** (매수 실행 + 멀티데이 손절 평가 hot path)
- **현상태**: `scheduler.py:2631-2944` ~330L. `_swing_buy_poll_loop`(161L)·`_run_swing_rest_poll_once`(116L)·`_swing_rest_poll_loop`(35L). `_SWING_POLL_STRATEGIES` 순차 double-buy 차단.
- **권고**: `stale_watcher_core.py`(HIGH hot path 위임 선례) 답습. record/flush collector(사이클 74)도 동반.
- **domain-expert 평가**: **NEEDS_GUARDS_FIRST (GO with guards)** — 위임 대상은 순수 오케스트레이션(시간게이트·후보순회·게이트체크·시세fetch·카운터), 실제 의사결정(execute_buy/on_tick/check_buy_signal)은 이동 대상 밖 → **행위 보존**. 단 3함정:
  - (A) `patch("src.engine.scheduler.fetch_stock_detail")`(rest poll) 이 이관 시 무력화 — reach-back vs 경로갱신 **명시 결정** 필요(buy poll 은 함수-로컬 import 라 영향 없음, 이 비대칭 카드에 명시).
  - (B) 콜렉터 record/flush 는 cycle78/102 AST 가 scheduler.py 잔존 강제 → **콜렉터는 scheduler 잔류 + 이동 함수는 lazy reach-back**. 순진하게 옮기면 가드 깨짐 / top-level import 면 순환 import 크래시(둘 다 fail-loud).
  - (C) **루프 통째 이동 필수** — 전략 단위 sub-delegation 하면 미래 `asyncio.gather` 병렬화 시 순차 double-buy 가드가 깨질 씨앗.
- **필수 선행 가드**(RED→GREEN, 이동 전): ① buy-poll 순차 double-buy end-to-end(A pending 등록→B skip) ② rest-poll 손절 전달 불변식(held −7% → on_tick 정확 호출, candidates 미호출) ③ 60s 윈도우 게이트(09:29:59 밖/09:30:01 안/15:20:01 밖 freezegun) ④ 콜렉터 배선 정합(cycle78/102 GREEN 확인) ⑤ namespace patch 경로 결정→고정.
- **함정 E**: 이동 중 "누락된 것 같다"며 유령 레짐 게이트(`get_buy_block_state`) 복원 절대 금지(사이클 I 제거, CLAUDE.md 603행).
- **예상효과**: scheduler −310L. kojiro 튜닝 사이클 변경 범위 축소.
- **착수 조건**: 8영역 diff 0(strategies/risk/order_engine 0줄) + FREEZE 무충돌 + NXT 애프터/익일 boot 전 배포.

### 카드 B5 — [HIGH] 보드 전환·사전구독 → `board_transition.py`
- **위험**: **HIGH** (WebSocket 구독 우선순위 + 돌파 시가 확정)
- **현상태**: `scheduler.py:1533-2009` ~470L. confirm_breakout_open_prices(106L)·atomic_board_transition(89L)·collect_breakout/swing/presubscribe·build_priority_groups·resolve_open_price.
- **domain-expert 평가**: **NEEDS_GUARDS_FIRST** — 강제 로직(bypass_limit·41-cap·2-pass·BFB→VCP→VB→LTV 순서)은 **전부 이관 대상 밖**(scanner.subscribe_filtered_stocks / run_daily 의 board= 명시) → 위임은 행위 미변경. **그러나 현재 카드 명세 그대로면 아니오** — 4대 위험:
  - (A) **cycle214형 silent import 사망**: `kis_ws_pool` 은 `scheduler.py:49` module-level, 본문 18회 전역 참조. 신규 모듈이 `from ...websocket import kis_ws_pool`(잘못) 착지 시 cycle214 재현. → **import path AST 가드 + 런타임 delattr 회귀**(cycle214 `test_market_op_subscribe_import_path_guard` 패턴) board_transition 용 신설.
  - (B) **patch-path vacuous PASS**: `test_atomic_board_transition.py` 가 `monkeypatch.setattr(sched_mod,"kis_ws_pool",mock)` → 이관 시 mock 미스로 실 unsubscribe/subscribe 나가거나 조용히 GREEN. 이관과 동시 patch 경로 적응 필수.
  - (C) **인벤토리 누락 `_emit_breakout_open_confirm`**(scheduler:1640) — confirm 본체가 호출하나 카드 목록에 없음. `_confirm_breakout_open_prices_if_pending`(:2509, 잔류)도 confirm 을 cross-module 호출. 누락 시 AttributeError/로그 소실.
  - (D) **`_board_transition_loop`/`_atomic_board_transition` 고아 의심** — grep 결과 src/ 전체 production 호출 **0**(정의 + CLAUDE.md 문서뿐). TIME_KRX_MAIN_OPEN_PRESUBSCRIBE(08:59:10)/TIME_POST_NXT_OPEN_PRESUBSCRIBE(15:39:10)도 정의만·run_daily 미사용. cycle 26 설계 문서가 stale, 실제 채널 전환은 subscribe 시점 TR_ID 선택으로 대체 추정.
- **필수 선행 가드**: ① import-path AST + delattr 회귀(최우선) ② board= 경계 전달 end-to-end(08:00/09:00:05/15:30 각 board kwarg assert — 현재 함수 직접호출만 검증) ③ confirm→drain 순서 가드(현재 부재) ④ `_collect_breakout_tickers` BFB→VCP→VB→LTV 순서 assert.
- **선결 조건**: (1) **`_board_transition_loop` 고아 최종 확정**(team-leader/tester 가 handler.py/session.py H0NXMKO0 경로까지 훑어 라이브 호출 0 확인 — 고아면 이관 아닌 **삭제 후보** 별도 판정, 죽은 코드를 새 모듈로 옮기면 board_transition.py 가 태어나자마자 dead surface) (2) `_emit_breakout_open_confirm` 인벤토리 편입 (3) **부분 이관**: 시가확정군(confirm+emit+resolve_open_price)만 1차, 채널전환군(atomic/loop)은 고아면 삭제/보류.
- **예상효과**: scheduler −450L(단 고아 삭제 시 축소). B1~B5 누적 시 scheduler ≈ 3050L(사이클 67 수준 복귀).

### 카드 B6 — `IMMEDIATE_FRESH_SKIP_HOURS` 죽은 함수-로컬 import 3건 제거
- **위험**: LOW
- **현상태**: `scheduler.py:3091,3133,3495` 에서 import 하나 미사용(세 task 는 신선도 게이트 미대상 — docstring 명시). 실사용은 basics:3208·master:3267 2곳뿐.
- **권고**: 세 import 에서 심볼만 제거. **B1 채택 시 자연 흡수 → 단독 폐기**.
- **예상효과**: −3L + 게이트 대상/미대상 혼동 제거.

---

## 권고 카드 — Group C: 프론트 (매매 8영역·KIS·주문 무관, HIGH 없음)

> 공통 제약: **testid 보존 / MSW·e2e Playwright LIFO(사이클 80 hotfix#3) 정합 / KST 강제(사이클 68) / `npm run build`(tsc TS6133 미사용 import) 통과**.

### 카드 C1 — P/L 색상 헬퍼 → `utils/pnlColor.ts`
- **위험**: LOW
- **현상태**: 이익 `#FF3333`/손실 `#3366FF`(정본=frontend/CLAUDE.md) **8곳 재구현** — class `profitColor` byte-identical 3벌(PerformanceCard:16·BalanceTable:48·Strategies:85) + hex 상수 3벌(ProfitChart:20·Recommendations:29·DailyReportTab:70). (`text-red-600` shade 계열 TradePnLGrid/BreakoutCandidateMonitor 2곳은 shade 상이 → **제외**).
- **권고**: `utils/pnlColor.ts`(`PROFIT_HEX`/`LOSS_HEX`/`NEUTRAL_HEX` + `pnlColorClass`). `#FF3333` 정확 계열 **6곳만** 위임(순수). `text-red-600` 미접촉.
- **회귀가드**: 기존 6 + 추가 1(순수함수 3분기). className 만 변경(출력 동일) → testid/MSW/KST 무관.
- **예상효과**: 색상 8→1 진실원. ~20L.

### 카드 C2 — KST 포맷 헬퍼 → `utils/stale-context.ts` 확장
- **위험**: MEDIUM
- **현상태**: `Intl.DateTimeFormat('...',{timeZone:'Asia/Seoul'})` **13파일** 각자 구현(로케일 ko-KR/en-CA/en-GB/en-US 제각각). 이미 `stale-context.ts` 가 `getKstMinutes`/`formatLastTickKst` 선례.
- **권고**: `getKstTodayIso()`·`formatKstDateTime`·`formatKstTime`·`formatKstHm` 추가. **출력 문자열 정확 일치 사이트만 순차 위임(1파일씩)**. 단일 함수 강제 금지(포맷 상이).
- **회귀가드**: 기존 다수(ScanMonitor.formatRunAt.test 등 포맷 직접검증). 추가 4(헬퍼별). ⚠️ **KST 강제 최우선** — 공통 헬퍼 결함이 전 timestamp 전파 → 위임 전 사이트별 출력 스냅샷 동등성 필수. `getHours()`류 로컬타임 절대 금지.
- **예상효과**: KST 13→1 진실원. ~50L.

### 카드 C3 — StockMaster 4 refresh mutation 팩토리 통합
- **위험**: MEDIUM
- **현상태**: `StockMaster.tsx:729-841` 4벌 near-identical fire-and-forget useMutation(차이=mutationFn·toast·메시지·timeout). retry:false+409분기+invalidate 4회 반복.
- **권고**: `makeRefreshMutation({...})` 팩토리. 4 toast state 유지(버튼 독립 피드백).
- **회귀가드**: cycle128/129(버튼+onError). testid `refresh-{basics,daily,master}-button` 보존. "KIS API 일시 결함"(106)·409(127) 문자열 보존.
- **예상효과**: ~67L.

### 카드 C4 — StockMaster.tsx 1442L 상수·서브컴포넌트 분리
- **위험**: MEDIUM
- **현상태**: 상수(FIELD_LABELS:123·CATEGORY_KEYS:90·HIGHLIGHT_KEYS:74…) + 포맷터 8개 + DetailModal/DailyTab/CollapsiblePre + 본체 ~808L.
- **권고**: 상수+포맷터 → `pages/stockMaster/constants.ts`, 서브컴포넌트 → 하위 파일.
- **회귀가드**: cycle128/129/166 + G-169-AST(source scan `before_raw`/`after_raw` 0건). ⚠️ **사이클 124 UI 동기화 영구 가드가 경로 `StockMaster.tsx::FIELD_LABELS` 명시** → 상수 이동 시 frontend/CLAUDE.md 8단계 절차 + G-169-AST 경로 동반 갱신. **선행: team-leader 가이드 경로 문구 갱신 승인**.
- **예상효과**: 본체 1442→~800L.

### 카드 C5 — ScanMonitor 5 STAGES → Record 맵 + 단일 렌더 루프
- **위험**: LOW
- **현상태**: `ScanMonitor.tsx:46-130` 6 STAGES 배열 + `449-491` 5블록 near-identical 분기.
- **권고**: `STRATEGY_STAGES: Record<string,{stages,testId,title,theme}>` + 단일 분기. ⚠️ **동일 파일 내 상수화 유지**(cycle175/178 정적 source scan 이 ScanMonitor.tsx 경로 직접 읽음 → 별도 파일 분리 금지). key/label 문자열 = 백엔드 `_scan_stats` 키 정합, 절대 변경 금지.
- **회귀가드**: funnel/cycle175/cycle178 + testid `{vb,ltv,momentum,bfb,vcp}-scan-funnel` 보존.
- **예상효과**: 렌더 5→1. ~30L.

### 카드 C6 — ScanMonitor isVbLtv 인라인 IIFE(242L) → `VbLtvTargetTable.tsx`
- **위험**: MEDIUM
- **현상태**: `ScanMonitor.tsx:909-1150` ~242L 인라인 IIFE(VB/LTV 타겟표). isVcpOrBfb→BreakoutCandidateMonitor, isKojiro→KojiroMonitor 는 이미 위임 — isVbLtv 만 거대 인라인.
- **권고**: `components/VbLtvTargetTable.tsx` 추출(선례 2건 답습). ⚠️ CLAUDE.md "VB/LTV 경로 byte 동일 보존" — activeBoardCode 결정·사이클 18 "돌파"/"돌파(대기)" 라벨(매매 컨텍스트) **byte-identical 이동**. props 전달, 로직 이동 금지.
- **회귀가드**: boards/activeBoard/breakout_label/bfb_vcp + 라벨 보존.
- **예상효과**: ScanMonitor 1227→~985L. 3탭 전부 서브컴포넌트 대칭.

### 카드 C7 — 억/조원 환산 → `utils/koreanMoney.ts` (도메인 정책 통일 선행)
- **위험**: LOW-MEDIUM
- **현상태**: 억/조 환산 4곳+ 분산(StockMaster:218/233/245 · TradeAmountFilterCard:43 · paramLabels:192). ⚠️ **미묘한 차이 실재**(toFixed(0) vs Math.round vs toFixed(1)).
- **권고**: `koreanMoney.ts`(formatEok/formatJoEok). **출력 차이로 순수 아님 → 억 표시 정책 확정 후 위임, 불가 시 보류**.
- **회귀가드**: cycle166(억/조 정정) + TradeAmountFilterCard. **선행 = 억/조 경계값 스냅샷**(통일 시 출력 변경 위험). 사이클 166 "백만원→억원 100배 silent" 재발 방지.
- **예상효과**: 통일 성공 시 ~20L. **정책 확정 없이 착수 금지**.

---

## 권고 카드 — Group D: LOW dead code/주석 정리 (8영역 diff 0)

### 카드 D1 — 죽은 `write_log` 함수-로컬 import 5개 제거 (사이클 72 호출 제거 잔재)
- **위험**: LOW
- **현상태**: `stale_diagnostics.py:167` · `stale_session_recovery.py:140,230` · `stale_universe_guard.py:64` · `stale_watcher_core.py:123` — 전부 "사이클 72 write_log 제거" 주석이 자백, 호출 0(logger.* → _DbLogHandler 위임 전환됨).
- **권고**: 5개 죽은 import 삭제. 함수-로컬이라 monkeypatch seam 아님, noqa 없음.
- **예상효과**: 5L + "로그하는 함수처럼 보이나 안 함" 오독 제거.

### 카드 D2 — `is_event_blocking` 죽은 import + 스테일 docstring 정정
- **위험**: LOW
- **현상태**: `market_operation_monitor.py:30` import 하나 소비 0(record_market_op_event 는 `_is_code_active` 인라인). docstring:54 "is_event_blocking()으로 통합 판정" 이 코드와 불일치.
- **권고**: import 제거 + docstring 실제 로직으로 정정. **함수 자체는 api/market_operation.py:110 에서 살아있음(미삭제, 계약 불변)**.
- **예상효과**: 죽은 import 1 + docstring 정합.

### 카드 D3 — 모듈-레벨/지연 죽은 import ~14 심볼 일괄 정리 (F401, noqa/재export/8영역 제외)
- **위험**: LOW
- **현상태**(pyflakes + 소비처 0 실증): scheduler.py:19 date/:21 get_daily_orders/:23 token_manager/:30 MarketBoard(재export 소비 0)·:3851 update_trade_status·:3856 TradeType/TradeStatus / log_analysis_engine.py:514 stock_master(위임 전환) / recommendation_engine.py:32 COMPARE_METRIC_KEYS / boot_manager.py:21 date / kis_quote_accounts.py:23 timezone/:24 Any/:29 mask_secret / stock_master_daily.py:37 timedelta.
- **권고**: 미사용 심볼만 제거. ⚠️ **제외**: scheduler.py:103 stale_manager 상수(재export seam, 테스트 scheduler 경유 import) + noqa:F401 asyncio 4곳·KST 5곳(monkeypatch seam / L-3 KST 가드).
- **예상효과**: ~14 심볼 제거.

### 카드 D4 — 미사용 로컬 변수 2건 제거
- **위험**: LOW
- **현상태**: `kojiro.py:856` `info = self._candidates.get(ticker)`(미참조) / `boot_manager.py:113` `initial_count = cnt`(미참조). pyflakes.
- **권고**: 삭제. 순수 죽은 할당 = 행위 보존. kojiro FREEZE 무관(출력 불변).
- **예상효과**: 2L.

### 카드 D5 — VB/LTV `_empty_scan_stats` 죽은 키 3종 백엔드 제거
- **위험**: LOW
- **현상태**: `volatility_breakout.py:54-56` + `long_tail_volatility.py:47-49` 의 `price_filtered`/`mcap_pass`/`trade_amount_pass` 초기화만·증가 0(사이클 108 제거, 프론트는 178 정리). VCP `mcap_pass`·momentum scan_filter_stats 는 **살아있음 — 미접촉**.
- **권고**: VB/LTV 죽은 키 3종 제거.
- **회귀가드**: VB/LTV scan_stats 9-키 계약 테스트 3파일 **lockstep 갱신 필요**(additive 아님) + xfail 갱신 → **tdd-engineer 선행**.
- **예상효과**: 죽은 키 6 + 프론트-백엔드 계약 정합.

### 카드 D6 — VCP "일봉 220일" 스테일 주석 8지점 정정 (사이클 196 이후)
- **위험**: LOW (주석 전용)
- **현상태**: 확정치 = retention 230달력일≈154영업일 / backfill target 120(scanner:2104) / prepare cap 100. "220" 잔존: `stock_master_daily.py:600,606,607` · `vcp_breakout.py:190,196,197,198,278`.
- **권고**: "220일" → 현행값 정정, 핵심 논지("VCP 는 DB 깊이 무관 100 cap") 유지. retention 상수 미변경(주석만). **방금 sync-docs 3건과 동일 클래스**.
- **예상효과**: 8지점 정정 + 운영자/AI 220일 lookback 오독 제거.

---

## 비권고 사항 (검토했으나 발의하지 않음)

| 항목 | 사유 |
|---|---|
| `_effective_setup`(vcp/bfb 2벌) · `_ema`(vcp/donchian 2벌) | **2회 반복 = 두기**(계명 #3). 3번째 부재 |
| `_refresh_position_setup_from_candles`+`_rederive_*_levels` | 2벌 + **구조 상이**(VCP 단일값 vs BFB 4키 루프). CLAUDE.md "BFB §3 `.get()` 방어+키 결손 미발화" 계약 위반 우려 |
| `recompute_high_since_buy` | CLAUDE.md **"base 승격 금지"** 명시(전략별 fetch 소스·일수 상이). 공통 `_apply_high_since_buy_from_candles` 는 이미 base 승격 완료 |
| `get_targets_status` | 후보 진단 필드 전략별 상이 + **VB 호환 5키 scheduler(8영역) 소비 계약** |
| `_scan_universe` 전체(루프 밖) | list_by_filter 인자·0종목 ERROR·graceful 전략별 상이(byte-identical 아님). 카드 A8 은 루프만 |
| funnel `_reset/record_funnel_*` | **이미 base 추출됨**(사이클 47/170). FUNNEL_STAGES 전략별 단계수/라벨 상이 = 정당 |
| `_apply_quant_filter`/`_apply_rs_rsi_observe`(VB) | **VB 단독 1회** = 두기. 3번째 도입 시 A1 패턴 흡수 |
| `_empty_scan_stats` 통합 | 키가 전략별 정당 상이(VB 9/donchian 10/LTV +consecutive) |
| `prev_idx` 가드 | 단일 표현식 + 후속 처리 전략별 상이. 1행 헬퍼 실익 낮음 |
| useQuery retry:1 공통 훅 | **AST 가드가 리터럴 강제**(`_ast_useQuery_retry_required.test.ts`) + main.tsx 전역 default 이미 존재(런타임 이득 0). 흡수 시 가드 다수 fail = 순비용 음수 |
| PriceFilter/TradeAmount/CashUsage 카드 완전 통합 | testid 매트릭스 컴포넌트별 하드코딩(e2e LIFO·MSW·AST) + 분기 상이 → 흡수 이득 < 회귀 위험. 독립성이 e2e 안정성 기여 |
| `scheduler.start()`(482L) | **정당한 응집** — _boot→체결통보 구독(절대규칙)→task lifecycle 단일 시퀀스. 분해 회귀 위험 > 이득 |
| order_engine execute_buy/sell/_handle_*_fill | **8영역 핵심** — 주문번호 매핑 동기영역·race 가드·NXT 좀비·폴백이 원자적. 함수 길이 자체는 결함 아님 |
| `_execute_next_day_clear`(206L) | 익일청산 **절대규칙**(30s 안정화·폴백 금지·on_tick 즉시청산 금지) + 사이클 142 시정 촘촘. 안전 우선 제외 |
| `order_engine soft_multiplier` 제거 | vestigial 확인되나 **8영역 미접촉 우선**(CLAUDE.md 이미 vestigial 명문화) |
| `risk.py:18 Optional` 미사용 | **8영역** → 미발의 |
| kojiro `max_units_per_stock`/`max_units_total` | CLAUDE.md **"피라미딩 배선 예정 inert 봉인"** = 문서화된 의도적 존속 |
| `buy_block_mode` / noqa asyncio·KST seam / `symbol`·`stock_code` 명명 | 표시 전용 소비 / 의도적 monkeypatch seam / 소비처 0(ticker 이미 표준) |

---

## 후속 의뢰
- **tdd-engineer 선행 회귀 가드**: A1/A2 kojiro 회귀 · A7 터틀 수량 골든+A-GATE 스캔 대상 · A8 scanner 헬퍼 단위 · A9 kojiro min_required + cycle173 AST 5건 의미 전환 계획 · **B4 5종 가드**(double-buy/손절전달/윈도우/콜렉터/patch경로) · **B5 4종 가드**(import-path AST+delattr/board=경계/confirm→drain/collect 순서) · C7 억/조 경계값 · D5 9-키 계약 lockstep.
- **domain-expert (완료)**: B4/B5 행위 영향 평가 = 둘 다 **NEEDS_GUARDS_FIRST**(위임 자체는 행위 보존, 선행 가드 필수). A9 는 이관 시 동등성 재확인 첨부 권장.
- **team-leader 결정 요청**: 카드별 채택/보류/폐기 + **B5 선결 = `_board_transition_loop` 고아 최종 확정**(handler.py/session.py H0NXMKO0 경로 훑기 → 고아면 이관 아닌 삭제 판정) + C4 사이클 124 가이드 경로 갱신 승인.

## 권장 처리 순서
1. **1차 (LOW·무위험, 회귀가드 추가 0)**: D1·D2·D3·D4·D6 + C1·C5 + A5+A6(묶음). 즉시 착수 가능.
2. **2차 (LOW·최대 실익)**: A1+A2(한 사이클, kojiro 제외·cycle151/157 의미 전환) → A3. scheduler 무관 순감 ~360L.
3. **3차 (MEDIUM, 8영역 무관 위임)**: B1(재비대 1순위) → B6(B1 흡수) → B3 → C3 → C6 → C2. 1주 운영 후 B2.
4. **4차 (MEDIUM, tdd 선행)**: A4 → A8 → A9 → D5 → C4(가이드 승인) → C7(정책 확정).
5. **5차 (HIGH, 별도 사이클·domain 완료·가드 선행)**: A7 → B4 → B5. **NXT 애프터(15:40~)/익일 07:50 boot 전 배포, 장중(09:00~15:30) push 금지**.

**공통 안전 원칙**: 전 카드 **8영역 diff 0**(승격=전략파일+strategy_base+scanner非매수경로 / 위임=scheduler+신규모듈, 호출 site 불변). 위임 패턴 = 사이클 51/67 답습(scheduler 인자 + 2줄 wrapper + lazy reach-back + logger 명시 binding). FREEZE(kojiro)·다크런치(VCP/BFB) 실행 로직 byte 보존. Refactor 중 RED 즉시 되돌림.
