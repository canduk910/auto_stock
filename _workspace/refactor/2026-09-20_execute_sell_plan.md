# `execute_sell` 재귀 리팩토링 — 진행 규약

사용자 지시 (2026-09-20) —

> "매도실행을 재귀리팩토링으로 여러단계에 걸쳐서 리팩토링하자.
>  실제 행위기준으로 로직이나 코드를 분리해서 가독성과 구조를 대폭 개선하자.
>  또, 공통기능이 여러군데 흩어져있다면 한군데에 모아서 수정 시 누락이 발생하지 않도록 하자.
>  리팩토링 에이전트의 지침에도 리팩토링의 최우선은 소스길이가 아니라 기술부채 해소 임을 명시하자.
>  마지막으로 작업 진행은 메인 오케스트레이터와 리팩토링 에이전트가 진행하고
>  리팩토링 중간중간과 최종결과 과정에서 다른 개발자들의 코드리뷰를 받도록 하자."

---

## 0. 왜 지금인가

| 조건 | 값 |
|---|---|
| 대상 | `order_engine.py::execute_sell` — **711줄 · CC 90 · 중첩 8** |
| 위치 | 이 리포에서 **가장 복잡한 함수**, 그리고 **손절을 집행**하는 함수 |
| 그물 | 커버리지 **94%**(미커버 40줄) · 74파일 33,514줄이 이 함수를 호출 |
| 창 | **주말 = 장 마감**. 평일은 8영역 + 장중 배포 금지로 손댈 수 없다 |
| 보유 | 11종목(kojiro 6 · donchian 3 · BFB 2) — 월요일 07:45까지 거래 없음 |

⚠️ 미루면 안 하게 된다(사용자 지적). 그리고 평일에는 **못 한다.**

## 1. 🔴 성공의 정의 — 줄 수가 아니다

리팩토링 지침 0계명(이 지시로 명문화)의 순위를 그대로 쓴다.

| 순위 | 목표 | 측정 |
|---|---|---|
| 1 | 수정 시 누락 위험 ↓ | 한 규칙을 바꿀 때 **손대야 하는 파일 수** |
| 2 | 이해 비용 ↓ | CC · 중첩 · **한 함수가 다루는 관심사 수** |
| 3 | 변경 위험 ↓ | 커버리지 · 결합된 `self` 상태 수 |
| 4 | 중복 ↓ | 3회 이상 반복 패턴 |
| 5 | 줄 수 | 위 넷의 **결과로만** |

**보고 금지 문구** — 「N줄 줄였다」.
**보고할 문구** — 「이 규칙은 이제 한 곳에서만 고치면 된다」 · 「관심사 4개가 각각 제 이름을 갖게 됐다」.

## 2. 진행 주체와 관문

```
[메인 오케스트레이터]  단계 정의 · 승인 확인 · 커밋/배포 · 최종 판정
        │
        ├─ [refactor-expert]  단계별 설계 카드 산출 (행위 보존 · 0계명 순위)
        │
        ├─ [backend-dev]      Red→Green 실행 (카드 1장 = 커밋 1개)
        │
        └─ 🔴 코드리뷰 관문 — **매 단계마다 + 최종에 한 번 더**
             ├─ tester          통합·경계면 회귀 0 · 행위 보존 증명
             ├─ domain-expert   매매 행위 영향 0 인가 (손절 경로다)
             └─ tdd-engineer    그물이 이 변경을 판정할 수 있는가(미커버 구간 여부)
```

**관문 통과 조건** — 세 리뷰어가 **전부** "행위 보존"을 확인해야 다음 단계로 간다.
하나라도 "확인 못 함"이면 그 단계는 **되돌리고** 회귀 테스트를 먼저 세운다.

## 3. 단계 규약

- **1단계 = 1커밋 = 1의도.** 묶으면 회귀 추적이 불가능하다.
- 각 단계 후 **전체 스위트**(11,100+)와 **AST 가드**(1,797)를 돌린다.
- `order_engine.py` 는 8영역이라 sha 핀 재핀이 따라온다 — 단계마다 1회.
- **되돌리기 = 그 단계 커밋 하나 revert.** 그래서 단계를 작게 자른다.

## 4. 절대 건드리지 않는 것 (「절대 깨지 말 것」)

- **주문번호 매핑 5종** 등록은 `place_order` 응답 직후 **동기 영역**, `await insert_trade` **전**
- **체결통보 선행 race 가드**(`_completed_orders` + UPDATE 0건 보정 INSERT)
- **NXT 좀비 차단** — `is_market_closed_rejection` 이면 positions 보존 + `_selling` **discard**
- **시장가 거부 → 지정가 5호가 폴백 1회** 규약
- `_selling` 의 add/discard **전 경로** — 하나라도 빠지면 stale `_selling` = 손절 마비

## 5. 중단 기준

다음 중 하나라도 나오면 **그 단계에서 멈추고 사용자에게 보고**한다.

- 미커버 구간을 건드려야 하는데 회귀 테스트를 못 세운다
- 행위 보존을 증명할 수 없다(리뷰어 셋 중 하나라도 "확인 못 함")
- 추출이 위 4절의 순서·생명주기를 바꾼다
- 월요일 07:45 가 가까워진다 — **부팅 전에 끝나지 않으면 되돌리고 중단**

---

## 진행 기록

(단계별로 append)
