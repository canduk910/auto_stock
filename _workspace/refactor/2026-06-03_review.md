# 리팩토링 리뷰 — 2026-06-03

> 사용자 요청 (사이클 49→55 누적 7회, refactor-review 5회 임계 *2배 초과*) 후속 검토.
> 검토 범위 3 영역 사전 명시: ① emit cap 통합 / ② scheduler 분해 후속 단계 / ③ 광범위 except 절 누적.
> refactor-review 스킬 5 계명 (테스트 없는 리팩토링 금지 / CLAUDE.md 절대 규칙 우선 / 3 회 반복 = 추상화 후보 / 주석 정책 보존 / 카드 단위 분할) 준수.

## 검토 범위

- 검토 사이클: 49 (VCP Pullback 시정) → 50 (BFB funnel 정밀화) → 51 (`_boot` boot_manager 추출) → 52 (sell 좀비 차단 B-1) → 53 + 53.1 (log_analysis 결함 B-2/B-4) → 54 (NXT 다운그레이드 emit cap B-3) → 55 (SellRejectionTracker R-1)
- 검토 디렉토리: `src/engine/` (scheduler, risk, order_engine, boot_manager, sell_rejection, stale_tracker), `src/api/base.py`, `src/realtime/websocket*.py`
- 활용 도구: `wc -l`, `grep -n`, Python 함수 라인 측정 스크립트, 이전 검토 메모 (`_workspace/refactor/2026-05-22_review.md`) 후속 의뢰 흐름 추적
- KIS MCP: 본 검토에서 영역 ③ (`is_market_closed_rejection` 등 거부 코드 분류) 카드 *발의에 의존* 하지만, 실제 호출은 카드 채택 후 backend-dev/tdd-engineer 가 통일 작업 시 수행 — 본 메모 단계에서는 거부 helper 4종 (`is_market_closed_rejection`/`is_market_order_disallowed`/`is_insufficient_quantity`/`is_insufficient_cash`) 의 *기존 코드 사실* 기록에 한정

## 누적 통계

### 모듈 라인 수 (사이클 49→55 변화)

| 파일 | 라인 (2026-05-22) | 라인 (2026-06-03) | 변화 | 비고 |
|---|---|---|---|---|
| `src/engine/scheduler.py` | 3,906 | **3,879** | -27 | 사이클 51 boot 분해 (-265) 후 사이클 52~55 신규 코드 (+238) — 순 감소는 미미 |
| `src/engine/order_engine.py` | 998 | **1,151** | +153 | 사이클 52 (B-1 진입 차단) + 사이클 54 (NXT cap) + 사이클 55 (R-1 reconciliation) 누적 |
| `src/engine/risk.py` | (포함된 위치 다름) | **249** | — | 사이클 31 R6 emit cap 기존 |
| `src/engine/sell_rejection.py` | — | **271** | +271 | 사이클 55 신규 |
| `src/engine/boot_manager.py` | — | **305** | +305 | 사이클 51 신규 |
| `src/engine/stale_tracker.py` | — | (사이클 48) | — | 이전 검토 카드 #2 산출물 |

### scheduler.py 함수 라인 top 10 (사이클 49→55 후)

| 함수 | 라인 | 책임 도메인 | 분리 후보 |
|---|---|---|---|
| `start()` | 378 | lifecycle (boot 후 task 등록 + 시간 분기 dispatch) | 시간 분기만 추출 가능. start 본체는 lifecycle 책임 — 보존 |
| `_check_and_resubscribe_stale()` | 230 | **stale_manager** | ★ 후보 |
| `_execute_next_day_clear()` | 150 | **settlement_manager** (익일청산 발사부) | ★ 후보 |
| `_sleep_chunked()` (nested) | 134 | swing helper | swing_manager 동행 |
| `_run_swing_rest_poll_once()` | 118 | **swing_manager** | ★ 후보 |
| `_evaluate_universe_guard()` | 116 | **stale_manager** | ★ 후보 |
| `_resubscribe_stale_priority()` | 100 | **stale_manager** | ★ 후보 |
| `_build_session_subscription_view()` | 98 | stale 진단 (read-only) | stale_manager 동행 |
| `_settle()` | 95 | **settlement_manager** | ★ 후보 |
| `_refresh_stale_ccnl_cache()` | 93 | **stale_manager** | ★ 후보 |

### Emit cap 패턴 카운트 (영역 ①)

| 위치 | 자료구조 | 정책 | reset 호출 위치 |
|---|---|---|---|
| `risk.py:41` | `_risk_silent_skip_logged_today: set[tuple[str,str]]` | 페어별 1회/일 | scheduler 가 *직접 risk_manager 내부 필드 clear* (`scheduler.py:3833`, AttributeError 가드) |
| `order_engine.py:91` | `_nxt_downgrade_logged_today: set[str]` | ticker별 1회/일 | `OrderEngine.reset_daily_state()` 가 내부 clear (사이클 54 명시 "사이클 56 통합 예정") |
| `sell_rejection.py:75` | `_logged_today: set[str]` | ticker별 1회/일 + discard 으로 만료 후 재발 가능 | `SellRejectionTracker.reset_daily()` 가 통합 4 필드 clear (사이클 55 R-1) |

→ **3 곳 모두 동형 패턴**, 단 (a) 키 타입 차이 (tuple vs str), (b) reset 호출 위치/추상화 깊이 차이가 통합 헬퍼 인터페이스에 영향.

### 광범위 except 누적 (영역 ③)

| 파일 | `except Exception` 수 | 비고 |
|---|---|---|
| `src/engine/scheduler.py` | **110** | 압도적 — 모듈 분해 카드(영역 ②)와 동시 시정 시 효율 |
| `src/engine/recommendation_engine.py` | 30 | 20:00 AI자문 — OpenAI/외부 fetch graceful 다수 |
| `src/engine/order_engine.py` | 18 | 사이클 52~55 신규 (write_log 실패 graceful 다수) |
| `src/api/base.py` | 14 | KIS REST graceful |
| `src/realtime/websocket_pool.py` | 11 | WebSocket lifecycle graceful |
| `src/engine/boot_manager.py` | 8 | 사이클 51 추출 시 원본 패턴 보존 |
| `src/realtime/websocket.py` | 5 | WebSocket graceful |
| `src/engine/risk.py` | 3 | `regime.get_buy_block_state()` 폴백 등 |
| **합계 (검토 범위)** | **199** | — |

---

## 권고 카드 목록

### 카드 #1 — emit cap 헬퍼 `DailyEmitCap` 통합 (MEDIUM ★★) — ✅ **사이클 56-A~D 종결 (2026-06-04)**

> **종결 요약** — 4 단계 모두 완료. 신규 `src/engine/daily_emit_cap.py` (89L) + 3 곳 마이그레이션 (sell_rejection / order_engine / risk). RiskManager 외부 직접 clear 비대칭 해소 (`reset_daily_state()` 신규 + scheduler 위임). G-1~G-4 회귀 가드 +11 케이스. 백엔드 1804 → 1819 PASS (회귀 0). 사이클 54 코드 코멘트 "사이클 56 통합 예정" 영구 제거.

- **위험 등급**: MEDIUM (모듈 내부 구조 / 단위 테스트로 회귀 가드 가능)
- **현 상태** (3 곳 동형):
  - `risk.py:41` `_risk_silent_skip_logged_today: set[tuple[str,str]]` (사이클 31 R6, 페어별 1회/일)
  - `order_engine.py:91` `_nxt_downgrade_logged_today: set[str]` (사이클 54 B-3, ticker별 1회/일)
  - `sell_rejection.py:75` `_logged_today: set[str]` (사이클 55 R-1, ticker별 1회/일, 만료 시 `discard` 으로 재발 허용)
  - 사이클 54 코드 코멘트 "R-1 범위 밖 — 사이클 56 emit cap 통합 시 tracker 위임 예정" (`order_engine.py:90`) — 통합 의도 코드 본문에 *명문화* 됨 (3 회 반복 = 추상화 후보 임계 충족)
- **권고**: `src/engine/emit_cap.py` 신규 모듈에 제네릭 `DailyEmitCap` 도입 + 3 곳 마이그레이션 *분할 사이클*

  ```python
  # src/engine/emit_cap.py
  from dataclasses import dataclass, field
  from typing import Generic, Hashable, TypeVar
  K = TypeVar("K", bound=Hashable)

  @dataclass
  class DailyEmitCap(Generic[K]):
      """1회/key/일 emit cap — _reset_daily_state 동행 reset() 호출 의무.

      만료 후 재발 허용은 mark_emitted 외부에서 discard 호출 (sell_rejection 패턴).
      """
      _emitted: set[K] = field(default_factory=set)

      def should_emit(self, key: K) -> bool:
          return key not in self._emitted

      def mark_emitted(self, key: K) -> None:
          self._emitted.add(key)

      def discard(self, key: K) -> None:
          """만료 후 재발 허용 (sell_rejection register_market_closed 패턴)."""
          self._emitted.discard(key)

      def reset(self) -> None:
          self._emitted.clear()
  ```
- **분할 사이클 제안** — 카드 1 장에 *변경 1 의도* 원칙 준수:
  - **사이클 56-A** (LOW): `DailyEmitCap` 헬퍼 도입 + 단위 테스트만 (호출처 0)
  - **사이클 56-B** (MEDIUM): `SellRejectionTracker._logged_today: set[str]` → `DailyEmitCap[str]` 으로 *내부 교체*. `should_emit_block_log/mark_block_logged/discard/reset_daily` API 보존 (호환 layer). 기존 사이클 55 회귀 가드 통과
  - **사이클 56-C** (MEDIUM): `OrderEngine._nxt_downgrade_logged_today` 마이그레이션 + `reset_daily_state()` 가 위임 호출. 코드 코멘트 "사이클 56 통합 예정" 제거
  - **사이클 56-D** (MEDIUM): `RiskManager._risk_silent_skip_logged_today: set[tuple[str,str]]` → `DailyEmitCap[tuple[str,str]]` 마이그레이션. `scheduler.py:3833` 의 *외부 직접 clear* 비대칭 해소 — `risk_manager.reset_daily()` 신규 메서드 추가 후 위임. AttributeError 가드 (기존 `try/except AttributeError`) 도 `risk_manager.reset_daily()` 호출로 일원화
- **회귀 가드**:
  - 기존: 사이클 52 회귀 8 시나리오 + 사이클 54 회귀 (NXT cap) + 사이클 55 회귀 (sell_rejection 통합) = 충분 (이미 PASS)
  - 추가 필요: `DailyEmitCap` 단위 테스트 5~7 케이스 (should_emit / mark_emitted / discard / reset / 제네릭 key 타입 검증)
- **선행 의뢰**: 없음 (MEDIUM, 단위 테스트로 회귀 가드 충분). domain-expert 자문 불필요
- **안전 규칙 충돌**: `_reset_daily_state` 동행 clear 보존 (절대) — D 단계의 외부 직접 clear 비대칭 해소가 *오히려 안전 규칙 강화*
- **예상 효과**:
  - 3 곳 동형 패턴 → 1 모듈로 단일화 + 새 emit cap 추가 비용 0
  - 사이클 56+ 새로운 INFO/WARNING cap 패턴 발생 시 *즉시 헬퍼 재사용* (4번째 등장 위험 사전 차단)
  - 코드 코멘트 "사이클 56 통합 예정" 영구 제거 → 빚 해소

---

### 카드 #2 — scheduler 분해 후속: stale_manager 추출 (HIGH ★★★)

- **위험 등급**: HIGH (모듈 간 경계 변경 + 매매 hot path 영향 가능 — WebSocket 4 중 안전망 호출 시점 보존 의무)
- **현 상태**: 사이클 51 boot 분해 완료 (`scheduler.py` 3,906 → 3,879, `boot_manager.py` 305L 신규). 이전 검토 카드 #1 의 후속 단계 (`stale_manager` / `swing_manager` / `settlement_manager`) 발주 대기 — 본 검토에서 *우선순위* 평가
- **분리 후보 클러스터 (stale_manager.py)** — *가장 응집도 높고 가장 큰 라인 감소 효과*:
  | 함수 | 라인 | 책임 |
  |---|---|---|
  | `_check_and_resubscribe_stale` | 230 | K stale watcher (사이클 17/28/29-R1/R3 4중 안전망의 K 분기) |
  | `_evaluate_universe_guard` | 116 | 사이클 32 R4 universe 자동 제외 |
  | `_resubscribe_stale_priority` | 100 | 사이클 25-B HIGH/LOW 우선순위 분리 |
  | `_build_session_subscription_view` | 98 | stale 진단 read-only view |
  | `_refresh_stale_ccnl_cache` | 93 | 사이클 37 ccnl 캐시 |
  | `_emit_stale_session_detail` | (cap 20) | stale 상세 emit |
  | `_detect_silent_inactive_sessions` | (사이클 29-R2 3중 가드) | 세션 단위 silent inactive 감지 |
  | `_force_reconnect_session` | (사이클 24) | 강제 reconnect 발화 |
  | `_evict_expired_ccnl` + `_prune_force_retry_history` | 29 + 33 | 사이클 45 TTL evict (보존) |
  | `_stale_watcher_loop` | 55 | 120s 주기 (이미 wrapping) |
  - **누적 라인**: ~1,000+ (scheduler.py 의 26%)
  - **응집도**: 모두 `_stale_state: StaleTrackerState` (사이클 48 산출물) 와 `kis_ws_pool` 협업에 집중
- **권고**: `src/engine/stale_manager.py` 신규 모듈 (사이클 51 `boot_manager` 패턴 답습)
  ```python
  # src/engine/stale_manager.py
  async def check_and_resubscribe_stale(scheduler) -> None: ...
  async def evaluate_universe_guard(scheduler, candidate_tickers: list[str]) -> None: ...
  async def resubscribe_stale_priority(scheduler, cap: int = 10) -> list[str]: ...
  # ... 나머지 함수
  ```
  - scheduler 본체는 *2 줄 wrapper* 위임 (사이클 51 패턴: `await stale_manager.check_and_resubscribe_stale(self)`)
  - `_stale_state` 직접 접근은 헬퍼 함수 첫 인자 `scheduler` 통해 그대로 유지 (StaleTrackerState 이미 분리됨 → 내부 결합 zero)
- **회귀 가드 매트릭스** (사이클 51 답습 + 안전 규칙 보강):
  - 모듈 callable 가드 — `from src.engine.stale_manager import check_and_resubscribe_stale` import 가능성
  - 위임 1회 가드 — scheduler 본체 wrapper 가 단순 위임만 수행 (logic 0)
  - 호출 순서 보존 가드 — `_scan_loop` (5분), `_stale_watcher_loop` (120s) 의 stale_manager 호출 순서 보존
  - **WebSocket 4 중 안전망 행위 가드** (CLAUDE.md 절대 규칙):
    1. K stale watcher 양쪽 분기 우선순위 분리 (HIGH/LOW + bypass=True/False) 보존
    2. silent inactive 3중 가드 (`fresh_ratio<0.2` + `subscribed_count>=5` + 5분 지속) 보존
    3. 시간당 cap 2회 (silent inactive) / 12회 (force_retry) 보존
    4. `_reset_daily_state` 동행 `_stale_state.reset_daily()` 위임 호출 보존
  - **MAX_STALE_RETRIES=5 / STALE_FORCE_RETRY_AFTER_SECS=300 / STALE_FORCE_RETRY_HOURLY_CAP=12 / SILENT_INACTIVE_FRESH_RATIO_THRESHOLD=0.2 / UNIVERSE_LOW_VOLUME_THRESHOLD=10_000** 상수 *값 동일성* 단위 테스트
  - 기존 회귀: 사이클 17/24/28/29-R1/R2/R3/32/37/48 누적 stale 케이스 60+ 개 (이미 PASS) 전부 GREEN 유지
- **선행 의뢰** (HIGH 의무):
  - **domain-expert** — *행위 영향 평가*: stale_manager 분해가 WebSocket 4 중 안전망 호출 시점/순서에 미치는 영향. 특히 (a) K stale watcher 의 force_retry 발화 timing, (b) silent inactive 감지 후 `_force_reconnect_session` 발화 timing, (c) `_reset_daily_state` 가 stale_state.reset_daily 와 ccnl/force_retry evict 동행 보장
  - **tdd-engineer** — 위임 1회/호출 순서/상수 동일성 회귀 가드 3 종 사전 작성
- **안전 규칙 충돌 사전 점검**: ★ WebSocket 4 중 안전망 (체결통보 구독 / NXT 좀비 차단 / `_reset_daily_state` 동행) 호출 시점 변경 절대 금지. 분해 *전후* 동일성을 통합 테스트로 검증
- **예상 효과**:
  - `scheduler.py` 3,879 → ~2,900 (약 -1,000L 감소, 25%)
  - stale 관련 사이클 추가 시 변경 비용 -50% (단일 모듈 내 집중)
  - 사이클 48 `stale_tracker.py` 와 짝 — *데이터 (state) + 로직 (manager) 분리* 완성

---

### 카드 #3 — scheduler 분해 후속: settlement_manager 추출 (HIGH ★★★)

- **위험 등급**: HIGH (정산 + 익일 청산 hot path — `_reset_daily_state` 호출 시점 보존 의무)
- **현 상태**:
  | 함수 | 라인 | 책임 |
  |---|---|---|
  | `_execute_next_day_clear` | 150 | 익일 청산 발사 (08:00 + 30s 안정화) |
  | `_drain_pending_next_day_clear` | (50+) | 09:00:05 KRX 시장가 일괄 청산 |
  | `_settle` | 95 | 20:10 정산 |
  | `_reset_daily_state` | 81 | 일간 상태 초기화 |
  - **누적 라인**: ~376
  - **응집도**: 모두 *정산 + 일일 cleanup* 단일 책임
- **권고**: `src/engine/settlement_manager.py` 신규 모듈 (boot_manager 패턴 답습). 단 *카드 #2 stale_manager 채택 이후* 발주 — `_reset_daily_state` 가 stale_state.reset_daily 호출 *후* 분해 시 중복 변경 회피
- **회귀 가드**:
  - 모듈 callable / 위임 1회 / 호출 순서 보존
  - **`_reset_daily_state` 동행 clear 의무 보존 가드** — strategy state 9 필드 + order_engine 7 dict + pending_next_day_clear + stale_state + risk_manager (카드 #1 D 적용 후) + scanner 글로벌 dict 5 + condition cache + backtest_poll_loop + auto_funnel_done — 총 25+ 동행 필드. 한 줄 누락이 전일 상태 잔류 사고 직결 → *단위 테스트로 25+ 필드 모두 reset 호출 가드*
  - **익일 청산 시가 수신 30s 안정화 (`NEXT_DAY_STABILIZE_SECS=30`) 보존 가드** — `_execute_next_day_clear` 의 30s 대기 + `_pending_next_day_clear` 보류 분기 행위 동일성
- **선행 의뢰** (HIGH 의무):
  - **domain-expert** — *행위 영향 평가*: 정산 + 익일청산 호출 흐름 변경 안전성. 특히 (a) `unsubscribe_all()` → AI 자문 → settlement → log_analysis → `_reset_daily_state` 의 *순차 의존성* 보존, (b) `_pending_next_day_clear` set 가 `stock_master.nxt_tradable=False` 판별과 race 없음, (c) `nxt_downgrade_logged_today` (카드 #1 D 후 reset_daily) 동행 clear
  - **tdd-engineer** — 위 회귀 가드 사전 작성
- **안전 규칙 충돌**: ★ 익일 청산 / `_reset_daily_state` / NXT 좀비 차단 / 15:20 일괄청산 — 모두 CLAUDE.md 절대 규칙. 분해 시 행위 동일성 통합 테스트 필수
- **예상 효과**:
  - `scheduler.py` ~2,900 → ~2,520 (카드 #2 후, 추가 -376L)
  - 정산 흐름 단일 모듈 — 사이클 53/53.1 같은 정산 결함 시정 비용 -40%

---

### 카드 #4 — scheduler 분해 후속: swing_manager 추출 (MEDIUM ★★)

- **위험 등급**: MEDIUM (donchian_swing 단일 전략 한정 + `_swing_rest_poll_loop` CLAUDE.md 절대 규칙 보존)
- **현 상태**:
  | 함수 | 라인 | 책임 |
  |---|---|---|
  | `_swing_buy_poll_loop` | (사이클 26~) | 09:30~15:20 60s 매수 폴링 |
  | `_swing_rest_poll_loop` | (사이클 26~) | 09:30~15:20 60s 매도 손절 평가 폴링 |
  | `_run_swing_rest_poll_once` | 118 | 단일 폴 실행 |
  | `_sleep_chunked` (nested) | 134 | 폴링 helper |
  - **누적 라인**: ~400
  - **응집도**: 모두 donchian_swing 멀티데이 전략 전용
- **권고**: `src/engine/swing_manager.py` 신규 모듈. 카드 #2/#3 *완료 후 마지막* 분해 — 단일 전략 한정이라 위험도 가장 낮음
- **회귀 가드**:
  - `_swing_rest_poll_loop` 제거 금지 (CLAUDE.md 절대 규칙) — 모듈 분해 후에도 wrapper 가 *반드시* 시간 분기에서 호출
  - 09:30~15:20 시간 가드 보존
  - 60s 폴링 주기 보존
- **선행 의뢰**: domain-expert *경량 자문* — `_swing_rest_poll_loop` 분해가 donchian 멀티데이 보유 손절 평가에 영향 없음 확인
- **안전 규칙 충돌**: `_swing_rest_poll_loop` 제거 금지 — wrapper 보존
- **예상 효과**:
  - `scheduler.py` ~2,520 → ~2,120 (카드 #2/#3 후, 추가 -400L)
  - 사이클 51 + 카드 #2/#3/#4 누적: `scheduler.py` 3,879 → ~2,120 (-45%) — 사이클 49→55 누적 빚 청산

---

### 카드 #5 — 광범위 except: `order_engine.py` graceful write_log 패턴 통일 (LOW ★) — ✅ **사이클 56-E 부분 종결 (2026-06-04)**

> **종결 요약** — 신규 `src/db/system_logs.py::safe_write_log(level, msg, *, debug_key)` 헬퍼 + `order_engine.py` **4 곳** 마이그레이션 (refactor-expert 권고 14 곳 중 graceful skip 의도 *명확한 4 곳만* — 나머지 10 곳은 의도된 예외 전파 / 단일 try 범위라 마이그레이션 범위 외). G-5 4 회귀 가드 케이스. 보수적 마이그레이션 — 잔여 10 곳은 향후 결함 발생 시 case-by-case 재검토.

- **위험 등급**: LOW (graceful logger.debug 위주 + 본 흐름 보존 — 행위 영향 0)
- **현 상태**: `src/engine/order_engine.py` 18 개 `except Exception` 중 *14 개가 동형 패턴*:
  ```python
  try:
      await write_log("INFO", "[some_prefix] ...")
  except Exception:
      logger.debug("[some_prefix] write_log 실패", exc_info=True)
  ```
  - 위치: L153 / L170 / L192 / L514 / L807 / L820 (write_log 폴백) + L763 (`_pending_next_day_clear` 등록 실패 — 동일 graceful 패턴)
- **권고**: `src/engine/order_engine.py` 모듈 하단 또는 `src/engine/_log_helpers.py` 에 `safe_write_log(level, msg, log_key)` 헬퍼 추출
  ```python
  async def safe_write_log(level: str, msg: str, *, debug_key: str) -> None:
      """write_log fire-and-forget — 실패 graceful (본 흐름 영향 0)."""
      try:
          await write_log(level, msg)
      except Exception:
          logger.debug("[%s] write_log 실패", debug_key, exc_info=True)
  ```
  - 14 곳 → `await safe_write_log("WARNING", f"[nxt_downgrade] ...", debug_key="nxt_downgrade")` 단일 호출
- **회귀 가드**: 기존 graceful 패턴 동일성 (단위 테스트 1 케이스 — write_log raise 시 본 흐름 영향 0 검증)
- **선행 의뢰**: 없음 (LOW)
- **안전 규칙 충돌**: 없음 (graceful 패턴 보존)
- **예상 효과**: 14 곳 → 14 줄 단축 (총 -42L) + 새로운 graceful write_log 추가 비용 0 + 정책 일관성

---

### 카드 #6 — 광범위 except: KIS API 호출 경로 좁히기 (MEDIUM ★★, KIS MCP 검증 후)

- **위험 등급**: MEDIUM (KIS 응답 변형 흡수 영역 — 좁히기 시 KIS 거부 코드 누락 위험)
- **현 상태**: `src/api/base.py` 14 개 `except Exception` — 대부분 *KIS REST 호출 graceful* (httpx/asyncio 예외 흡수). 의도된 예외 vs 누락 분류 필요. `KisApiError`/`httpx.HTTPError`/`asyncio.TimeoutError`/`json.JSONDecodeError` 만 catch + 나머지 re-raise 후보
- **권고**: 카드 발의 *보류* — KIS MCP 로 정본 거부 코드 분류 후 좁히기 정당화 *선행 필요*
  - **KIS MCP 사전 조사** (kis-mcp-query 스킬 활용):
    1. `mcp__kis-code-assistant__search_domestic_stock_api` — 주문/잔고/시세 API 응답 정본 거부 코드 매트릭스
    2. `mcp__kis-code-assistant__read_source_code` — KIS 공식 SDK 의 예외 처리 패턴 (re-raise 후보 식별)
  - 조사 후 좁히기 카드 사이클 분할:
    - **사이클 57-A**: `src/api/base.py::_request` 14 곳 → `except (KisApiError, httpx.HTTPError, asyncio.TimeoutError, json.JSONDecodeError)` 명시 — 나머지 `Exception` 은 re-raise (예측 못 한 결함 가시화)
    - **사이클 57-B**: `src/engine/order_engine.py` 잔여 4 곳 (graceful 외 L337/L473/L1150) 동일 분류
- **회귀 가드** (현시점 작성 불가 — 카드 채택 후 tdd-engineer 가 KIS MCP 정본 응답 시리즈 합성):
  - 기존 `_request` 5xx/4xx/network/retries 메트릭 회귀 (`get_request_metrics()`) 보존
  - 사이클 56+ 신규 KIS 거부 코드 발견 시 *바로 가시화* (현 broad except 는 silent swallow)
- **선행 의뢰**:
  - **tdd-engineer** — KIS MCP 정본 응답 시리즈 합성 + 회귀 가드 사전 작성 (의무 — 좁히기 카드 채택 *전*)
  - **domain-expert** — KIS 거부 코드 분류 검토 (예: APBK0918 / APBK1943 / APBK3013 + 미식별 코드의 예외 분류 정합성)
- **안전 규칙 충돌**: KIS 거부 응답 영구 저장 (`[kis_rejection]` system_logs) 정책 보존 의무 (절대) — 좁히기 후에도 비-KisApiError 예외도 *로그는 발생 후 re-raise* 패턴 보장
- **예상 효과**:
  - 사이클 33 (`prdy_vol` 누락) 같은 silent swallow 결함 영구 차단
  - 미식별 결함이 traceback 로 즉시 가시화 — 운영 디버깅 비용 감소
  - 사이클 56+ KIS 응답 변형 사고 대응 시간 단축

---

## 비권고 사항 (검토 후 카드 미발의)

| 영역 | 사유 |
|---|---|
| `start()` 378L 분할 | lifecycle (boot 후 task 등록 + 시간 분기 dispatch) 단일 의도 + *순차 의존성* 강해 분할 시 회귀 위험 큼. lifecycle 책임 단일 모듈 유지가 옳음. 시간 분기만 헬퍼로 빼는 마이너 변경은 *효과 대비 위험 비대칭* 으로 두기 |
| `recommendation_engine.py` 30 개 `except Exception` 좁히기 | OpenAI/외부 fetch graceful 다수 — 대부분 정당화 가능. 일괄 좁히기는 비용 과도 + 회귀 위험 LOW 영역. 사이클 56+ 결함 발생 시 *해당 위치만* 좁히기 권고 (proactive 미발의) |
| `websocket_pool.py` / `websocket.py` 16 개 except 좁히기 | WebSocket lifecycle (connect/disconnect/timeout) 변형 흡수 영역 — KIS 명세 외 ws library 예외 매트릭스가 KIS MCP 로 검증 불가. 좁히기 시 *연결 끊김 silent 결함* 위험 — 두기 |
| `scheduler.py` 110 개 except 좁히기 (영역 ③ 핵심) | 카드 #2/#3/#4 모듈 분해와 *동시 시정* 시 효율 — 분해 후 각 신규 모듈에서 좁히기 카드 별도 발의. 분해 *전* 좁히기는 변경 의도 혼재 + 회귀 추적 곤란 |
| `boot_manager.py` 8 개 except 좁히기 | 사이클 51 분해 시 원본 패턴 보존 — _boot 외부 fetch (KIS / dkstock / stock_master) 다수가 graceful 정당화. 분해 의도가 *행위 보존* 이었으므로 좁히기는 별도 카드 분리 |
| `risk.py:125` `except Exception` (regime fetch 폴백) | HARD fallback 안전 정책 — `BuyBlockState(mode="HARD", blocked=False)` 안전 폴백 동작 보존. 좁히기 시 *외부 fetch 실패에 매수 차단 회귀* 위험 — 두기 |
| `condition.py` / `scanner.py` / `recommendation_engine.py` 의 fire-and-forget 캐시 갱신 except | 캐시 미스 graceful 정당화 — 좁히기 시 *캐시 일시 결함이 본 흐름 영향* 위험 — 두기 |
| `_reset_daily_state` 81L 분할 | 25+ 필드 동행 clear 단일 의도 — 분할 시 *누락 사고 위험 증가*. 카드 #3 settlement_manager 분해 시 *함수 자체는 이동만* (분할 안 함) |
| `order_engine.execute_buy/sell` 280L+ 분할 | 사이클 52~55 누적으로 매수/매도 race 가드 + 체결통보 매핑 + 시장가 폴백 + sell_rejection 통합 등 *순차 의존성* 강해 분할 시 회귀 위험 큼. CLAUDE.md 절대 규칙 다수 보호 (이전 검토 카드와 동일) |
| `_check_and_resubscribe_stale` 230L 분할 | 카드 #2 stale_manager 추출 의 *내부* 함수 — 모듈 이동 후 별도 사이클에서 분할 권고. 본 사이클 56 에서는 *동시 변경 금지* (의도 혼재) |
| 종목명 lookup / Funnel hook / Stale dict / TTL evict / 5분 emit task 통합 | 사이클 44/45/46/47/48 에서 이미 완료 (이전 검토 카드 #2/#3/#5/#6/#7) — 회귀 0 |

---

## 후속 의뢰

| 대상 | 영역 | 의뢰 사항 | 우선순위 |
|---|---|---|---|
| **team-leader** | 6 카드 채택/보류/폐기 결정 | 본 메모 기반 사용자 의사 반영. 권장 순서: **#1 → #5 → #2 → #3 → #4 → #6** (난이도 + 위험 등급 + 의존성 순) | 즉시 |
| **tdd-engineer** | 카드 #1 (DailyEmitCap) 단위 테스트 5~7 케이스 | should_emit / mark_emitted / discard / reset / 제네릭 key 타입 (str / tuple[str,str]) 검증 | 카드 #1 채택 직후 |
| **tdd-engineer** | 카드 #2 (stale_manager) 회귀 가드 사전 작성 | 위임 1회 / 호출 순서 / 5 상수 동일성 / WebSocket 4 중 안전망 행위 가드 | 카드 #2 채택 *전* (Red-first) |
| **tdd-engineer** | 카드 #3 (settlement_manager) 회귀 가드 | `_reset_daily_state` 25+ 필드 동행 clear 가드 + 익일 청산 30s 안정화 가드 | 카드 #3 채택 *전* |
| **tdd-engineer** | 카드 #6 (KIS API 좁히기) KIS MCP 정본 응답 시리즈 합성 | kis-mcp-query 스킬 활용 — KIS 거부 코드 매트릭스 + respx + freezegun | 카드 #6 채택 *전* |
| **domain-expert** | 카드 #2 행위 영향 평가 | stale_manager 분해가 WebSocket 4 중 안전망 호출 시점/순서에 미치는 영향 (F1 / `_scan_loop` / K / `_resubscribe_stale_priority`) | 카드 #2 채택 *전* (HIGH 의무) |
| **domain-expert** | 카드 #3 행위 영향 평가 | 정산 흐름 (`unsubscribe_all` → AI자문 → settlement → log_analysis → reset) 순차 의존성 보존 검증 | 카드 #3 채택 *전* (HIGH 의무) |
| **domain-expert** | 카드 #4 경량 자문 | `_swing_rest_poll_loop` 분해가 donchian 멀티데이 보유 손절 평가에 영향 없음 확인 | 카드 #4 채택 *전* |
| **domain-expert** | 카드 #6 KIS 거부 코드 분류 검토 | APBK0918/APBK1943/APBK3013 + 미식별 코드 예외 분류 정합성 | 카드 #6 채택 *전* (HIGH 의무) |

---

## 절대 안전 규칙 보호 (사전 격리)

CLAUDE.md 14+ 종 절대 규칙 — 본 검토에서 카드 발의 사전 배제:
- 체결통보 구독 (H0STCNI0/H0STCNI9) / uvicorn 단일 워커 / 주문번호 매핑 / 체결통보 race 가드 / `_reset_daily_state` / 익일 청산 30s 안정화 / NXT 매도 좀비 차단 / 매수/매도 시장가 거부 폴백 / KIS 거부 영구 저장 / WebSocket 4 중 안전망 / NXT 거래가능 사전 판별 / 종목코드 6 자리 비대칭 / 1주 폴백 잔여 자금 기준 / VB 15:20 일괄청산 / donchian `_swing_rest_poll_loop` / `tradable_boards` 매수 진입 전용 (사이클 38 명문화) / KST 강제

각 카드의 *안전 규칙 충돌* 영역 사전 명시. 특히 카드 #2/#3 (HIGH) 는 분해 *전후* 행위 동일성 통합 테스트 필수 — domain-expert 자문 *없이는 채택 금지*.

---

## 운영 시간 권고

CLAUDE.md "로컬과 EC2 동시 실행 금지" + "KRX 메인 시간 (09:00~15:30) 중 빈번한 push 자제" 정책:
- 본 검토 메모 작성 자체는 영향 없음
- **카드 채택 후 사이클 발주는 NXT 애프터 (15:30~) 또는 익일 07:50 _boot 전 push 권장**
- 특히 카드 #2/#3 (HIGH, WebSocket 4 중 안전망 영향) 는 *주말* 또는 *공휴일 전일* push 가 안전

---

## 다음 단계 (사용자 결정)

| 옵션 | 진행 카드 | 예상 사이클 | 비고 |
|---|---|---|---|
| ~~A~~ | ~~#1 + #5~~ | ~~사이클 56~~ | ✅ **사이클 56 종결 (2026-06-04, 백엔드 1804→1819 PASS)** |
| **B** | **#2 (stale_manager)** | **사이클 57~58** | **HIGH, domain-expert 자문 필수, 큰 작업 — 사이클 51 boot_manager 패턴 답습. 잔여 카드 1순위 후보** |
| C | #3 (settlement_manager) | 사이클 59 | HIGH, 카드 #2 후속 |
| D | #4 (swing_manager) | 사이클 60 | MEDIUM, scheduler 분해 완성 |
| E | #6 (KIS API 좁히기, KIS MCP 검증) | 사이클 57+ | MEDIUM, tdd-engineer + domain-expert 사전 의뢰 |
| **F** | **잔여 4 카드 순차** | **57 → 60 (1~2 주)** | **HIGH (#2/#3) → MEDIUM (#4) + MEDIUM (#6 KIS MCP 분기)** — scheduler 분해 완성 |
| G | V-1 P1 분석 카드 발주 (refactor 카드 #2 전) | 사이클 57 | R-1 deque maxlen=20 인프라 직계 활용 — 매도 거부 폭주 실시간 알람. team-leader 우선 권고 |
| H | 메모만 보존 | — | 본 메모 영구 보존, 채택 보류 |

### 사이클 56 종결 후 잔여 카드 우선순위 (team-leader 재정렬)

**refactor 카드** (본 메모):
1. **#2 HIGH** — stale_manager 추출 (scheduler.py -1,000L, 사이클 51 패턴)
2. **#3 HIGH** — settlement_manager 추출 (-376L, 카드 #2 후속)
3. **#4 MEDIUM** — swing_manager 추출 (-400L)
4. **#6 MEDIUM** — KIS API except 좁히기 (KIS MCP 의존)

**분석 카드** (`_workspace/2026-06-01_log_analysis.md`):
- **V-1 P1** — 매도 거부 폭주 실시간 알람 (사이클 55 R-1 `deque maxlen=20` 인프라 위 hook)
- V-2 P2 / R-2 MEDIUM / R-3 MEDIUM / V-3 P2 / V-4 P3

**team-leader 다음 사이클 권고**:
- (a) **V-1 P1** — R-1 deque 인프라 즉시 활용, 매매 안전성 critical (사이클 52 매도 좀비 사고 후속 가시화)
- (b) **refactor 카드 #2 HIGH** — scheduler 분해 본격 진행, domain-expert 자문 동반
