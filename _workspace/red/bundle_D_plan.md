# 묶음 D 실행 계획 — ⑨A · ⑨B · B4 3~7단계 · B7 · J-1~J-4 · ⑦ 폴백 2곳

작성 2026-09-26(토) 22시 KST · team-leader · 계획 문서(코드 변경 없음)

## 0. 전제 — 이 계획이 서 있는 네 가지

1. **지금은 토요일 22시, 주말 배포 창이 월 07:45 까지 열려 있다.** 그래도 이번 주말에는 **매매 행위를 바꾸는 배포를 하지 않는다.**
   이유: 09-28(월)은 연휴 뒤 첫 거래일이고, 이번 주에 배포한 cycle364 S1·368·369·371 의 **첫 실측일**이다.
   변수를 하나 더 얹으면 월요일에 이상이 보여도 어느 사이클 탓인지 가를 수 없다. 배포 SHA 는 `825c6dd` 로 고정해 둔다.
2. D 묶음의 **행위 변경은 전부 사용자 승인 + `domain-consult` 선행**이다(루트 CLAUDE.md 「사용자 결정 항목」).
   09-25 의 「C 가능한 부분 마무리」 답은 D 를 덮지 않는다.
3. 8영역 `order_engine.py` 를 건드리는 커밋은 **주말 하나에 하나만** 둔다. 커밋마다 whole-file sha 핀 14곳을 다시 박고, 다른 카드의 줄 번호 앵커도 밀린다.
4. ⑨B 권고의 「F-A 와 같은 full 배포에 태운다」는 선택지는 **없어졌다.** F-A 는 cycle355 `6074c58` 로 09-25 에 이미 배포됐다.

배포 창(full = backend 재시작): 15:30~16:00 · 21:35~익일 07:45 · 주말·공휴일. **20:00~21:35 금지**, 보유 중 09:00~15:30 금지.
문서·테스트만 바꾸면 none 모드다(`src/**/*.md` 는 이미지에서 빠진다).

---

## 1. 지금 할 수 있는 순서 (이번 주말, 월 07:45 전)

| # | 무엇 | 8영역·scheduler | 자문 | 승인 | 배포 |
|---|---|---|---|---|---|
| S1 | **J-1 문서 정정.** 「16:00 뒤 `cancel_remaining` 이 남은 주문을 회수한다」는 약속을 지우고, 사실대로 적는다: 「회수 경로 없음. KRX 는 정규장 마감에 자동 취소한다. NXT 잔량은 20:00 까지 남는데, 그런 주문은 pre-NXT keep 이거나 `clock_mode=off` 일 때만 생긴다」. 고칠 곳 = `src/engine/CLAUDE.md:683` · `tests/unit/engine/test_cycle295_market_rest_cut.py:617` docstring. `order_engine.py:3090` 주석은 다음 8영역 사이클로 넘긴다 | 무접촉 | 불요 | 커밋·push 만 | none |
| S2 | **읽기 전용 실측 묶음 M.** 한 번 앉아서 ⑨A·⑨B·J-3·⑦ 넷의 숫자를 같이 뽑는다(아래 1.1) | 무접촉 | 불요 | 불요(조회만) | 없음 |
| S3 | (여력이 있을 때만) **B4-3 선행 그물**을 테스트 전용 커밋으로 만든다. ⑰ 폴백 분기 N2·N3·N4 재현 + `2084-2261` 분기 커버리지 + 돌연변이 1회. B4-4 대비로 `test_cycle236` 이 5분기의 `_selling` 유지·해제와 `continue`/`return` 을 모두 핀하는지도 확인하고, 빠진 것만 더한다 | 무접촉(테스트만) | 불요 | 커밋·push 만 | none |

**하지 않는 것:** ⑦ 관측 커밋(→ 09-28 21:35 이후, 2장 ②), 모든 8영역 커밋, `scheduler.py`, DB UPDATE(257720 PARTIAL 행 보정 포함).

### 1.1 실측 묶음 M — 내용과 주의

- **M-a KIS 주문내역**: `get_daily_orders(target_date=…)` 로 09-14~09-25 거래일마다 조회한다(TTTC0081R).
  - 날짜별 행 수를 센다. **100행이면 첫 쪽에서 잘린 것**이다. 역순 조회라 가장 오래된 08시대 행이 먼저 빠지므로, 그날은 「잘림」으로 표시하고 전칭 결론(「0건」·「없었다」)을 내지 않는다.
  - 자동취소 행(`tot_ccld_qty<ord_qty ∧ rmn_qty==0` 이면서 우리가 취소하지 않은 것)을 `ord_dvsn_cd`(27 과 그 외)·거래소·`ord_tmd`·`sll_buy_dvsn_cd_name` 별로 센다.
  - 073240 `0000149100` 행이 원래 `odno` 를 유지하는지, `sll_buy_dvsn_cd=="02"` 인지, `cncl_cfrm_qty`·`rjct_qty` 값이 무엇인지 확인한다.
  - 쓰임: ⑨A 의 가치와 잘림 위험, ⑨B O1.
- **M-b EC2 파일 로그**: DEBUG, 20일 보존(`src/main.py:105·121`). 🔴 **시한이 있다 — 09-14 파일이 10-04 무렵 사라진다.**
  - M-a 의 자동취소 `odno` 로 08:50:00 · 15:30 · 20:00 에 `체결통보 접수(미체결)` 프레임이 왔는지 본다.
  - 우리가 낸 취소의 확인 통보가 새 `odno` 로 오는지 본다. 그렇다면 자동취소(원 `odno`)와 가르는 값싼 판별자가 될 수 있다.
  - 쓰임: ⑨B O2.
- **M-c SQL(SELECT 만)**:
  - 09-14 이후 남아 있는 BUY PENDING 행 → ⑨A/⑨B O3
  - PARTIAL 행의 나이·전략별 분포와 체결수량 대비 저장수량 → J-3
  - `positions WHERE order_no=''` · `strategy_config` 의 enabled/weight · 30일치 `KIS 잔고 보완 복구` WARNING → ⑦-F1
  - 30일치 `[buy_post_send_error]`·`[buy_fill_fallback_orphan]` → ⑦-F2
- **주의**:
  - 원격 스크립트 첫 줄은 `export TZ=Asia/Seoul` 로 두고, 첫 수집 행을 눈으로 확인한다.
  - KIS 호출은 **캐시된 토큰만** 쓴다. 발급 호출은 금지다(분당 1개 한도).
  - 결과는 `_workspace/` 에 기록한다. 행의 정체를 확인하고 나서 합산한다.

---

## 2. 사이클 순서 (승인 뒤) — 한 줄 = 한 사이클

| 순서 | 사이클 | 무엇 · 왜 | 선결 | 8영역 / scheduler | 자문 | 09-28 전? |
|---|---|---|---|---|---|---|
| ① | **J-1** | = S1. 운영자가 걸린 주문을 방치하게 만드는 거짓 보증을 지운다. 함수를 배선하는 안은 **하지 않는다**(`pos.order_no` 는 매수 주문번호라 엉뚱한 주문을 취소한다) | 없음 | 무 / 무 | 불요 | **가능** |
| ② | **⑦-F1+F2 관측** | 부팅 복구의 폴백을 보이게 만든다. `[boot_recover_strategy_unknown] path=kis_supplement\|unfilled_order` WARNING · 기존 복구 로그에 `source=` · `:343`·`:425` 의 `except: pass` 를 로그로. 행위는 바뀌지 않는다. `_SRC_TREE_DIGEST` 재핀과 test_index 재생성이 같이 간다 | M-c | 무(`boot_manager.py`) / 무 | 불요 | 가능하지만 **09-28 21:35 뒤로 미룬다** — 배포 SHA 고정, 07:45 부팅에는 미체결 주문이 거의 없어 급하지 않다 |
| ③ | **⑦-F2 행위** | 부팅이 출처를 모르는 주문에 `_order_strategy="momentum"` 을 써서 **사람이 낸 주문에 30초 취소 타이머가 걸리는** 우회를 닫는다. 주문번호 기준으로 해석한다: `trade_history`(odno) → `llm_buy_evaluations` → 당일 BUY. 끝내 모르면 pending·`_order_qty` 등은 유지하되 `_order_strategy` 는 쓰지 않는다. 그러면 cycle331 경로가 소유를 정하고 타이머를 억제한다 | ②, 자문, 승인 | 무 / 무 | **필요**: 미상 주문의 소유, 30초 취소 여부, 사람 주문을 관리할지 | 불가 |
| ④ | **⑨A `buying_reconcile`** | 체결 0으로 끝난 매수(주로 LTV 프리장 GTP 08:50 자동취소)가 21:30 까지 예산·슬롯·종목을 묶는 것을 15분 sync 에서 푼다. 새 leaf + `scheduler.py` +6~8줄(3,781→약 3,789). 🔴 **양성 증거로만 해제한다**(자기 `odno` 행이 `rmn_qty==0 ∧ tot_ccld_qty==0`). 카드 원문의 「없으면 해제」는 첫 쪽 100행 잘림에 걸린다. 보류 사유: 주문번호 없음 · 행 없음 · 열린 주문 · 보유 · 최소 경과. await 뒤 재검증, 해제 때 해당 행 CANCELLED(유령 COMPLETED 방지). 적용 범위는 09:45~15:15 뿐이다(`_scan_loop`) | M-a, 자문, 승인 | 무 / **scheduler** | **필요**: 같은 날 재진입 허용 여부(momentum·VB·LTV 는 `_bought_today` 가 없다), 최소 경과 값과 출처, CANCELLED 표기 | 불가 |
| ⑤ | **⑦-F1 행위** | KIS 잔고 보완 복구가 **옛 왕복이나 CANCELLED 행의 전략**을 상속하는 것을 고친다. 증거 사슬 = 당일 `odno` → 열린 랏이 정확히 하나 → 비CANCELLED 최근 BUY → momentum + WARNING. `:358` 을 `registry.get(sid) or registry.get("momentum")` 으로 바꿔 보유를 버리지 않는다. 해석된 전략이 비활성이면 경보를 낸다 | ②, ③, 자문, 승인 | 무 / 무 | **필요**: 고아 보유의 주인, 수동·MTS 보유를 자동 관리할지, 비활성 전략일 때 | 불가 |
| ⑥ | **카드 E + J-4** (order_engine 주말 #1) | `pending_buy_amounts` 키를 `(ticker, order_no)` 로 바꾸고(승인됨), 발사 창에서 두 번째 부분체결의 수량이 버려지는 구멍 C 를 `pos.order_no==order_no` 정확 일치로 귀속한다. B-2 가드는 느슨하게 하지 않는다. ⑨A 가 먼저 착지했으면 그 pop 자리도 같이 바꾼다 | ③ 먼저 착지(아니면 E 를 ③ 위로 rebase), **09-28 `[buy_fill_fallback_held_conflict]` 판독**, 승인 | **8영역** / 무 | J-4 는 소(小) 자문 | 불가 |
| ⑦ | **B7 + J-2** (+J-3 호출부 · J-1 주석) (order_engine 주말 #2) | 부분 청산 주문이 전량 체결되면 **남은 보유까지 지우는** 결함을 고친다. 지금도 수동 부분 매도(`/api/trading/manual-sell`, MTS)가 실제로 밟는다. 부분 체결이면 `pos.quantity` 를 차감하고 저장한다. 재주문 직전에는 **registry 전체에서 종목으로** 포지션을 재조회하고, 의심스러우면 발사한다. 전략 한정 재조회는 `momentum` 기본값 함정에 걸린다 | 자문 1회(B7·J-2 공용), J-3 는 M-c 숫자 뒤 설계, 승인 | **8영역** / 무 | **필요** | 불가. 임시 방편 = manual-sell 이 `quantity < 보유` 이면 거부·경고(`routes/trading.py`, 비8영역, 사용자 결정) |
| ⑧ | **B4-3** (order_engine 주말 #3, 행위 보존) | 폴백 블록 ⑰ 을 `_handle_sell_market_disallowed` 로 뽑아낸다. 제어 신호는 enum 으로 돌려준다. 게이트 셋(tester → domain-expert → tdd-engineer)을 순차로 통과해야 한다 | S3 그물 커밋, 카드 앵커 갱신, 승인 | **8영역** / 무 | 게이트 안 domain-expert | 불가 |
| ⑨ | **B4-5** | 최종 실패 후처리 ⑲ 를 뽑아낸다. `test_cycle185_cluster1_ast` 는 「논리적 종료 자리 정확히 둘」 의미를 **재조준**하되 약화하지 않고, **같은 커밋**에 넣는다 | ⑧ | **8영역** / 무 | 게이트 | 불가 |
| ⑩ | **⑨B** 거래소 자동취소 통보 | M-b 결과로 범위를 다시 정한다. KRX 마감·20:00 프레임이 없으면 B 는 GTP 전용이고 ⑨A 로 충분할 수 있다. 여전히 필요하면: `handler.py` 관측 한 줄(이름 붙인 필드만, 0·1·17 PII 제외, 14핀) → 장중 C1~C5 → 자문 → TDD. 매수만 다루고, **양성 판별일 때만** 상태를 바꾸며, `_order_*` 맵은 유지한다 | M-b, ④ 결과, 8영역 승인, **장중 실측** | **8영역**(handler + order_engine) / 무 | **필요** | 불가 |
| ⑪ | **B4-4** | APBK0400 5분기를 순수 판정 함수로. 중복 제거가 없어 0계명 1순위 가치가 낮다. 기준을 엄격히 잡으면 **폐기** | ⑧⑨ | **8영역** | 게이트 | 불가 |
| ⑫ | **B4-6** | 주문사양 결정 블록 추출. **동결 구간 `execute_sell_pre_nxt_preconvert` 재핀은 사용자 결정**이다. 없으면 건너뛴다 | 사용자 결정 | **8영역** | 게이트 | 불가 |
| ⑬ | **B4-7** | 매핑 5곳 공통화. 금기 위험이 가장 크고 가치는 가장 작다. **기본은 하지 않는다.** 여섯 번째 `place_order` 자리가 생길 때 다시 본다 | `test_a10c` 의미 보존 재설계 | **8영역** | 게이트 | 불가 |

순서의 근거:
- ③ 을 ⑤ 보다 앞에 둔다. ③ 은 기존 안전 가드(30초 취소 억제, 고아 CRITICAL)를 **우회하는 실재 경로**를 닫고, ⑤ 는 대부분 진짜 수동 보유라 급하지 않다.
- ④ 를 ⑩ 보다 앞에 둔다. ④ 는 8영역도 장중 실측도 필요 없고, 결과에 따라 ⑩ 을 줄일 수 있다.
- 피라미딩 3단계가 닫혔으므로(cycle372) ⑥⑦ 의 급함은 피라미딩이 아니라 **수동 부분 매도**와 **장부 정확성**에서 나온다.

---

## 3. 대기 — 무엇을 기다리는지

| 항목 | 기다리는 것 |
|---|---|
| ②~⑬ 전부의 배포 | 09-28 실측이 끝난 뒤의 장외 창(가장 빠르면 09-28 21:35) |
| ③④⑤⑦⑩ | 사용자 승인 + `domain-consult` |
| ④ | `scheduler.py` 접촉 승인(라인 상한 3,900 안) |
| ⑥ J-4 | 09-28 `[buy_fill_fallback_held_conflict]` 판독. 서명 = 같은 `order_no` 의 `[buy_fill_strategy_from_pending]` 이 앞선다. 0건이고 E 도 보류면 더 미룬다 |
| ⑦ J-3 | M-c 숫자. 🔴 **09-28 판독 주의**: 부분체결 뒤 취소된 주문은 `[trade_status_update_miss] status=CANCELLED` 가 **행이 있는데도** 뜬다(PARTIAL 행이라 0건 매치). 「행 없음」으로 읽지 않는다 |
| ⑩ ⑨B | M-b 결과 → 8영역 관측 승인 → **거래일 장중** C1~C5. C3~C5 는 사용자가 MTS 로 1주 주문을 내 줘야 한다 |
| ⑪⑫⑬ | ⑧⑨ 착지, ⑫ 는 동결 구간 재핀 결정 |
| 257720 PARTIAL 행 보정 | DB UPDATE = 되돌리기 어려운 운영 조치라 별도 승인 |

## 4. 사용자에게 물을 것 (보고서 결정 카드 후보)

1. ⑦-F2·F1 행위 변경 착수(자문 뒤)
2. ⑨A: `scheduler.py` 접촉과 같은 날 재진입 정책
3. B7 임시 방편: manual-sell 이 부분 수량이면 거부할지 경고할지
4. ⑨B: `handler.py` 관측 한 줄(8영역)과 MTS 시험 주문 협조
5. B4-6 동결 구간 재핀
