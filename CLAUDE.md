# CLAUDE.md — 프로젝트 루트

KIS OpenAPI 기반 주식 자동매매시스템. FastAPI(백엔드) + React(프론트엔드) + Supabase(DB). 다중 전략 아키텍처.

> 디렉토리별 상세는 각 하위 `CLAUDE.md` 가 진실의 원천:
> `src/CLAUDE.md` · `src/engine/CLAUDE.md` · `src/engine/strategies/CLAUDE.md` · `src/api/CLAUDE.md` · `src/realtime/CLAUDE.md` · `src/db/CLAUDE.md` · `src/routes/CLAUDE.md` · `frontend/CLAUDE.md`
>
> 사이클별 변경 이력: [`docs/HARNESS_CHANGELOG.md`](docs/HARNESS_CHANGELOG.md)
> 시스템 흐름 도식: [`docs/architecture.md`](docs/architecture.md)

## 하네스: TDD-First Trading Team

**목표:** 모든 코드 변경을 Red→Green→Refactor 사이클로 강제하고, 변경 시 영향받는 테스트만 실행할 수 있는 정적 인덱스를 유지한다. *매매 의사결정의 깊이* 는 도메인 전문가의 사전 자문으로 보완하고, *코드 품질 드리프트* 는 리팩토링 전문가의 주기적 검토로 흡수한다.

### 기본 진입점 — `team-leader` 우선

사용자의 모든 요청은 1차로 `Agent({subagent_type: "team-leader"})` 로 라우팅한다. team-leader 가 트레이더 관점에서 해석 후 하위 에이전트에 분배한다.

- 코드 변경 → `auto-trading-orchestrator` 스킬 (TDD 사이클: `tdd-engineer` Red → `backend-dev`/`frontend-dev` Green → `tester` 검증)
- 매매 의사결정 자문 (신규 전략·파라미터·보드 행태·시장 레짐·KIS 거부 해석) → `domain-consult` 스킬 (`domain-expert` 에이전트) — Phase 2.5 명세 분해 *전* 또는 사이클 중 행위 영향 평가 시
- 주기적 리팩토링 검토 (사이클 5회 누적 또는 명시 요청) → `refactor-review` 스킬 (`refactor-expert` 에이전트) — Phase 4.5
- 단위/회귀 테스트 → `tdd-cycle` (백엔드 pytest+respx+freezegun / 프론트엔드 vitest+RTL+MSW)
- 영향 인덱스 → `test-impact-index`
- 통합/경계면/E2E/안전성 → `trading-test`
- KIS API 정본 스펙 (TR_ID·응답 구조·거부 코드) → `kis-mcp-query` 스킬 (backend-dev / tdd-engineer / tester / refactor-expert 공유)

**우회 허용 (메인 세션 직접 응답):** 단순 사실 질의, 단발 디버그/grep, 운영 환경 즉시 점검(EC2 SSH 등). 코드 변경 제안이 따라오면 다시 team-leader 로 인계.

### 모델 라우팅

| 작업 유형 | 모델 | 적용 |
|----------|------|------|
| 계획·검증·자문 (구현 계획, 테스트 설계, 검수, 안전성 검증, 도메인 자문, 리팩토링 검토) | **opus** | `team-leader`, `domain-expert`, `tdd-engineer`, `tester`, `refactor-expert` |
| 일반 구현 (코드 작성·리팩터·버그 수정) | **sonnet** | `backend-dev`, `frontend-dev` |
| 명령어 작성 (bash/슬래시/스크립트) | **haiku** | 메인 세션 단발 작업 — fork 또는 `claude-haiku-4-5-20251001` 위임 |

### 하네스 변경 이력

| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-04-22 | 초기 구성 (team-leader / backend-dev / frontend-dev / tdd-engineer / tester) | 전체 | TDD-First Trading Team 출범 |
| 2026-05-21 | `domain-expert` (데이/스윙 트레이더 자문) + `refactor-expert` (주기적 리팩토링) 합류, KIS MCP 접근 명시 (backend-dev / tdd-engineer / tester / refactor-expert) | 에이전트 7명 / 스킬 9개 | 매매 의사결정 깊이 보강 + 코드 품질 드리프트 흡수 + KIS 스펙 정본 통일 |
| 2026-05-21 | `kis-mcp-query` 스킬에 KIS 공식 저장소 경로 + 설치 가이드 + 공식 프롬프트 도구 (`kis_easy_code`/`kis_detailed_code`) 활용 안내 추가 | skills/kis-mcp-query | 공식 저장소 (koreainvestment/open-trading-api) 가 정본임을 명시, 비공식 fork 사용 차단 |
| 2026-05-21 | 사이클 28~37 (운영 진단 + 결함 시정 11 사이클) — 상세는 `docs/HARNESS_CHANGELOG.md` 참조. 코드 본체: stale 추적 강화 / force_retry / silent_inactive 비율 / 우선순위 분리 / `_sync_orders_to_db` 핑퐁 수정 / risk_silent_skip 가시화 / universe 가드 + `inquire_ccnl` / BFB acml_vol + VCP fetch 한도 / funnel DB + 세션 UI / KST NameError + backtest pending / KIS 체결시각 UI | `src/engine/scheduler.py` 외 다수 + 신규 라우트 `/api/strategy-funnel` + migration 029/030 | 09:13 VB 미매수 사고 진단을 시작점으로 stale 추적 → 결함 가시화 → 시정 + funnel DB 추적 → UI 강화. 백엔드 1625 PASS / 프론트 160 PASS / 매매 안전성 무영향 |
| 2026-05-22 | 사이클 38~43 (정책 명문화 + funnel 진단 + ping-pong 가시성 + 라벨 통일 6 사이클) — 상세는 `docs/HARNESS_CHANGELOG.md` 참조. 38(LTV 디폴트 PRE_NXT+MAIN+POST_NXT 복원 + `tradable_boards` 매수 진입 전용 정책 명문화) / 39(BFB·VCP·donchian funnel 단계별 자동 hook + VCP 프론트 키 매핑 시정 + 09:30 자동 snapshot) / 40(BFB 폴 음봉 임계 완화 DB only) / 41(funnel 단계별 종목명 + 탈락 사유 정밀 추적 + UI 조건 툴팁) / 42(PINGPONG echo DEBUG + `[ws_heartbeat]` 5분 통계) / 43(세션 라벨 `quote-1/2/3` → DB 라벨 `ISA/sub/gold` 통일) | `src/engine/strategies/long_tail_volatility.py` / `strategy_base.py` / `risk.py` / 3 전략 prepare / `src/realtime/websocket.py` / `websocket_pool.py` / `frontend/src/pages/StrategyFunnel.tsx` / `ScanMonitor.tsx` / `KisAccountPoolCard.tsx` | 백엔드 1671 PASS / 프론트 160 PASS / 매매 안전성 무영향. 사이클 33 BFB+VCP fix 효과 확인 (BFB 30→24, VCP 113→113), 사이클 41 Pullback 9→0 새 결함 위치 영구 기록. 사이클 42 운영 실측: 메인 PINGPONG 11~14s 간격 정상, 보조 시세 세션은 PINGPONG 미수신 (KIS 정책 추정). 사이클 43 라벨 통일로 `[ws_heartbeat]` / `[tick_coverage_session]` / `[stale_watcher_detail]` 모두 사용자 DB 라벨 (ISA/sub/gold) 일관 |
| 2026-06-11 | 사이클 108 — **Plan Phase A 완료 (VB/LTV/BFB stock_master 베이스 전환 + KIS volume-rank API 100% 폐기, 3 영역 통합)** (**43 사이클 연속 옵션 A 패턴 영속**). **사이클 104 인계 영역 영구 영속**: Q5=B (LOW 위험 자문 생략) + 사이클 107 raw 보강 의존성 해소 완료 영구 영속 → Plan Phase A 데이터 영역 활용 가능 → 사이클 108 = `stock_master.list_by_filter()` 신규 메서드 + VB/LTV/BFB `_scan_universe()` 전환 통합 영구 영속. **사용자 결정 (영구 영속)**: Q1=A 시총 영역 = `hts_avls` (KIS FHKST01010100, 백만원 단위) + Q2=A 통합 단일 사이클 (3 전략 동시 전환) + Q3=C 4 필터 (`min_market_cap` + `min_trade_amount` + `exclude_tickers` + `nxt_tradable`). **시정 (production 3 영역 통합)**: **영역 1** `src/api/condition.py::inquire_stock_basics` (+1L) — `hts_avls` 5번째 키 추가 (사이클 107 4 키 → 사이클 108 5 키 보강 영구 영속, CTPF1002R 기존 키 덮어쓰기 금지 영속 + 사이클 81 G-AST1 영속). **영역 2** `src/db/stock_master.py::list_by_filter` (+~60L) — 신규 메서드 (Supabase 영역 조회 + Python-side JSONB 필터링 = `raw.hts_avls × 1_000_000` 백만원 단위 환산 + `raw.acml_tr_pbmn` 거래대금 + 2배 buffer `limit × 2` + 필터 후 limit 적용). **영역 3** VB/LTV/BFB `_scan_universe()` 전환 (+~125L) — KIS `volume-rank` (FHPST01710000) 100% 폐기 → `stock_master.list_by_filter()` 100% 전환 (사이클 89 답습 + 4 필터 + ETF 키워드 제외 영속 + 6자리 ticker 영속 + funnel 카운터 영속 = `universe_candidates` + `universe_filtered` + `last_run_at`). **회귀 가드 52 PASS (사이클 108 격리 실행)**: 52 PASS / 0.20s / flakiness 0. **xfail 의미 전환 23건** (사이클 97 K-2 패턴 답습 영속 = 과거 폐기 계약 영구 보존). **백엔드 2354 → 2383 PASS + 141 XFAIL** (+29 신규 + 23 의미 전환) / **flakiness 0** (3 회 반복 동일) / 회귀 0. **영속 의무 매트릭스 영구 영속 (전수 영속)**: (1) 사이클 32 R4 universe guard 영속 + (2) 사이클 38 명문화 영속 (scanner 단계 = 매수 진입 전 후보 풀 영역 한정 + 매도/익일청산 hot path 무관) + (3) 사이클 65 H3 retry:1 영속 + (4) 사이클 78 G-AST1 + 79 G-AST2 영속 + (5) 사이클 81 G-AST1 영속 (`bfdy_clpr` + `hts_avls` 덮어쓰기 금지 영속) + (6) 사이클 88 G-REJECT 영속 + (7) 사이클 89 답습 패턴 영속 (KOSPI/KOSDAQ + ETF 제외 + funnel 카운터) + (8) 사이클 97 K-2 패턴 답습 (xfail 23 의미 전환 영속) + (9) 사이클 98 G-DOC1 영속 (KIS 정본 인용 의무 = `chk_inquire_price.py`) + (10) 사이클 101 영속 (`_full_universe_load_once` 자동 반영) + (11) 사이클 102 G-REJECT 영속 + (12) 사이클 106 영속 (lifecycle race 차단) + (13) 사이클 107 영속 (raw 보강 의존성 해소) + (14) CLAUDE.md "절대 깨지 말 것" 8 영역 영속. **매매 안전성 무영향 확정 영구 영속** (scanner 단계 = 매수 진입 전 후보 풀 구성 영역 한정 + 매도/손절/Trailing/익일청산/15:20 강제청산 hot path 무관). **운영 효과 영구 영속 (push + EC2 자동 배포 후)**: (a) **KIS API 호출 100% 절감 영구 영속** = VB 3 + LTV 3 + BFB 3 = 9 호출/일 → 0 호출 (사이클 폭주 차단) / (b) 신호 빈도 ±5% 이내 영속 (사이클 89 답습 + 동일 필터 적용) / (c) stock_master ~2,800 종목 영역 활용 영구 영속 (사이클 106 lifecycle race 차단 + 사이클 107 raw 보강 + 사이클 108 list_by_filter 통합) / (d) 운영자 통제 영역 영구 영속 (Plan Phase C UI 운영자 필터링 호환). **사이클 49→108 누적 63 사이클 + hotfix 14 영역**. **사이클 109+ 후속 카드 인계**: D+1 운영 실측 의무 (2026-06-12 금 09:00~10:00 측정 — VB/LTV/BFB `_scan_universe()` 정상 작동 + KIS volume-rank API 호출 0건 영구 영속 확인 + stock_master.list_by_filter() 정상 영역) / 카드 #21 (LOW 사이클 74 flaky stale_watcher logging caplog race) / #16 (MEDIUM Q6-3 2026-06-13 이후 후보 풀 폭축 회고) / auth 1.43x (LOW) / 사이클 100 인계 영역 3+4 (Plan Phase B). | `src/api/condition.py` (+1L `hts_avls` 5번째 키 추가) / `src/db/stock_master.py` (+~60L `list_by_filter` 신규 메서드) / `src/engine/strategies/volatility_breakout.py` + `long_tail_volatility.py` + `bull_flag_breakout.py` 3 전략 `_scan_universe()` 전환 (+~125L 합계) / `tests/unit/api/test_cycle108_*.py` + `tests/unit/db/test_cycle108_*.py` + `tests/unit/engine/strategies/test_cycle108_*.py` 신규 (52 PASS 영역 통합) / `_workspace/red/cycle108_plan_phase_a.md` Red 명세 영구 기록 / `_workspace/cycle108_verify_report.md` verify 보고서 / `src/engine/strategies/CLAUDE.md` 본문 사이클 108 영역 (VB/LTV/BFB stock_master 베이스 전환 명세 영구 영속) / `src/db/CLAUDE.md` 본문 사이클 108 영역 (`list_by_filter` 명세 + Python-side JSONB 필터링) / `src/api/CLAUDE.md` 본문 사이클 108 영역 (`inquire_stock_basics` `hts_avls` 5 키 영구 영속) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 108 행 / `docs/HARNESS_CHANGELOG.md` 사이클 108 행 영구 기록 | team-leader Phase 1 진단 (사이클 104 인계 Q5=B LOW 위험 자문 생략 영속 + 사이클 107 raw 보강 의존성 해소 완료 영구 영속 확인) + 사용자 결정 Q1=A (`hts_avls` 시총 영역) + Q2=A (통합 단일 사이클 3 전략) + Q3=C (4 필터) + tdd-engineer Red (52 케이스 3 영역 통합) + backend-dev Green (3 영역 통합 시정) + tester verify (백엔드 2383 PASS + flakiness 0 + 회귀 0 + 사이클 108 격리 52 PASS / 0.20s). 43 사이클 연속 옵션 A 패턴 영속. 매매 안전성 무영향 (scanner 단계 = 매수 진입 전 후보 풀 영역 한정 + 매도/익일청산 hot path 무관 + 사이클 38 명문화 영속). 영속 의무 매트릭스 전수 영속 (사이클 32/38/65/78/79/81/88/89/97/98/101/102/106/107 + CLAUDE.md 8 영역). 사이클 89 답습 패턴 영속 (KOSPI/KOSDAQ + ETF 제외) + 사이클 97 K-2 패턴 답습 (xfail 23 의미 전환 영속). 운영 효과 영구 영속: KIS API 호출 100% 절감 (9/일 → 0) + 신호 빈도 ±5% 이내 + stock_master ~2,800 종목 영역 활용 + 운영자 통제 영역 영구 영속 (Plan Phase C 호환). 사이클 109+ 후속 카드: D+1 운영 실측 의무 (2026-06-12 금) / 카드 #21 + #16 + auth 1.43x + 사이클 100 영역 3+4 (Plan Phase B). |
| 2026-06-11 | 사이클 107 — **stock_master raw 보강 (CTPF1002R + FHKST01010100 merge, Plan Phase A 의존성 해소)** (**42 사이클 연속 옵션 A 패턴 영속**). **Phase 1 진단 결정적 발견 (KIS MCP 정본 영구 영속 확정)**: CTPF1002R 응답 (67 컬럼) = 3 키 (`acml_tr_pbmn` 누적 거래 대금 + `lstn_stcn` 상장 주수 + `prdy_vol` 전일 거래량) **모두 부재 영구 영속** / FHKST01010100 (`inquire_price` 주식현재가 시세) = 3 키 모두 **존재 영구 영속** (`acml_tr_pbmn` + `lstn_stcn` + `acml_vol` 누적 거래량 (`prdy_vol` 영속 동등) + `prdy_vrss` 전일 대비). 사용자 결정 (영구 영속): **Q1=A `inquire_stock_basics` 본체 보강** = CTPF1002R 호출 *후* FHKST01010100 추가 호출 → 응답 merge → raw JSONB 통합 (CTPF1002R 기존 키 덮어쓰기 금지 영속). **시정 (production 1 파일)**: `src/api/condition.py::inquire_stock_basics` (+44L 순증) — `ctpf_output = data.get("output") or {}` (변수명 명시) + `await asyncio.sleep(0.05)` Rate Limit (사이클 83/91/97 답습) + `kis_get_quote(STOCK_PRICE_URL, "FHKST01010100", ...)` 추가 호출 + try/except graceful (`price_data = {}` fallback + `[inquire_stock_basics] FHKST01010100 호출 실패 graceful` 로그) + `merged_raw = dict(ctpf_output)` + for loop 4 키 조건부 merge (`acml_tr_pbmn` + `lstn_stcn` + `acml_vol` + `prdy_vrss`). **CTPF1002R 기존 키 덮어쓰기 금지 설계** (`bfdy_clpr` 영속 영구 영속 + 사이클 81 G-AST1 영속). **회귀 가드 12 신규 (HIGH 4 케이스)**: HIGH-1 양쪽 API 호출 영속 + CTPF 선행 순서 영속 (2 sub) + HIGH-2 raw 3 키 포함 + `bfdy_clpr` 보존 + 덮어쓰기 금지 (3 sub) + HIGH-3 graceful fallback (RuntimeError / KisApiError / CTPF 에러 전파, 3 sub) + HIGH-4 AST 영구 가드 (FHKST01010100 + inquire-price URL + 60줄 윈도우 + 3 키 merge, 4 sub). **백엔드 2315 → 2354 PASS** (+39 = 신규 12 + 기존 갱신 4 흡수) / **flakiness 0** (3 회 반복 동일) / 회귀 0. **영속 의무 매트릭스 영구 영속 (전수 영속)**: (1) 사이클 32 R4 universe guard 영속 + (2) 사이클 38 명문화 영속 + (3) 사이클 81 G-AST1 영속 (`bfdy_clpr` 영역 영구 영속 정합 + 덮어쓰기 금지 영속) + (4) 사이클 88 G-REJECT 영속 + (5) 사이클 98 G-DOC1 영속 (KIS 정본 인용 의무 = `chk_inquire_price.py`) + (6) 사이클 101 영속 (`_full_universe_load_once` → `inquire_stock_basics` 호출 영역 자동 반영) + (7) 사이클 102 G-REJECT 영속 + (8) 사이클 106 영속 (`_full_universe_load_task_loop` 영역 변경 0) + (9) CLAUDE.md "절대 깨지 말 것" 8 영역 영속. **매매 안전성 무영향 확정** (`inquire_stock_basics` = 종목 마스터 캐시 영역 한정 + 매수/매도 진입 직전 lazy 호출 + 매매 hot path 직접 영향 0 + Rate Limit 50ms sleep KIS LMS chain 안전). **운영 효과 영구 영속 (push + EC2 자동 배포 후)**: `inquire_stock_basics` 1회 호출 → stock_master.raw 에 `acml_tr_pbmn` + `lstn_stcn` + `acml_vol` 자동 포함 / 2,800 종목 × 2 KIS 호출 + 50ms sleep = 약 5분 소요 (사이클 101 `_full_universe_load_task_loop` 배치 시 적용) / 향후 Plan Phase A (사이클 108+) `list_by_filter(min_market_cap=, min_trade_amount=)` 데이터 영역 영구 영속 의존성 해소. **사이클 49→107 누적 62 사이클 + hotfix 14 영역**. **사이클 108+ 후속 카드 인계**: Plan Phase A (`list_by_filter` 신규 영역 구현) / D+1 운영 실측 의무 (2026-06-12 금 07:55 KST `[full_universe_load] 초기 실행 완료` emit + stock_master.raw 3 키 포함 영구 영속 확인) / 카드 #21 (LOW 사이클 74 flaky) / #16 / auth 1.43x / 사이클 100 인계 영역 3+4 (Plan Phase B). | `src/api/condition.py` (+44L `inquire_stock_basics` CTPF1002R + FHKST01010100 merge 영역 영구 영속) / `tests/unit/api/test_cycle107_inquire_stock_basics_merge.py` 신규 (12 케이스 HIGH-1~HIGH-4) / `_workspace/red/cycle107_stock_master_raw_enrichment.md` Red 명세 영구 기록 / `_workspace/cycle107_phase1_diagnosis.md` Phase 1 진단 영구 기록 (KIS MCP 정본 재검증 = CTPF1002R 3 키 부재 + FHKST01010100 3 키 존재 결정적 발견) / `_workspace/cycle107_verify_report.md` verify 보고서 / `src/api/CLAUDE.md` 본문 사이클 107 영역 (`inquire_stock_basics` 보강 명세 영구 영속) / `src/db/CLAUDE.md` 본문 사이클 107 영역 (stock_master raw 3 키 추가 영구 영속) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 107 행 / `docs/HARNESS_CHANGELOG.md` 사이클 107 행 영구 기록 | team-leader Phase 1 진단 결정적 발견 (CTPF1002R 3 키 부재 + FHKST01010100 3 키 존재 KIS MCP 정본 영구 영속 확정) + 사용자 결정 Q1=A (`inquire_stock_basics` 본체 보강) + tdd-engineer Red (12 케이스 HIGH 4) + backend-dev Green (1 파일 +44L 순증) + tester verify (백엔드 2354 PASS + flakiness 0 + 회귀 0). 42 사이클 연속 옵션 A 패턴 영속. 매매 안전성 무영향 (종목 마스터 캐시 영역 한정 + 매매 hot path 무관 + Rate Limit 50ms sleep KIS LMS chain 안전). 영속 의무 매트릭스 영구 영속 (사이클 32/38/81/88/98/101/102/106 + CLAUDE.md 8 영역 전수 영속). 사이클 81 G-AST1 영속 (`bfdy_clpr` 덮어쓰기 금지 설계) + 사이클 98 G-DOC1 영속 (KIS 정본 인용 의무 = `chk_inquire_price.py`). 운영 효과 영구 영속: stock_master.raw 에 `acml_tr_pbmn` + `lstn_stcn` + `acml_vol` 자동 포함 → Plan Phase A 데이터 영역 영구 영속 의존성 해소. 사이클 108+ 후속 카드: Plan Phase A (`list_by_filter` 신규 영역 구현) / D+1 운영 실측 의무 (2026-06-12 금 07:55 KST) / 카드 #21 + #16 + auth 1.43x + 사이클 100 영역 3+4 (Plan Phase B). |
| 2026-06-11 | 사이클 106 — **stock_master 갱신 영역 영구 영속 점검 (4 영역 통합 시정)** — (1) UI 안내 배너 영역 영구 영속 갱신 (사이클 94 무효 메시지 제거 + 신규 메시지 추가) + (2) "적재 실패" UI toast 영역 영구 영속 정밀화 ("KIS API 일시 결함" 명시) + (3) `_full_universe_load_task_loop` lifecycle race 영구 차단 (start() 직후 즉시 1회 + while 루프, 사이클 78/83 답습) + (4) UI 메시지 영역 영구 영속 현행화 (영역 1 흡수) (**41 사이클 연속 옵션 A 패턴 영속**). **사용자 보고 (verbatim)**: "종목마스터 갱신작업 점검이 금일 내로 완료되어야 할 것 같아. 현재 종목마스터의 상단에 있는 메시지는 이제 무효한거 아닌가? 점검해서 UI에서 지우고 프론트와 백엔드 모두 현행화하길 바래." **Phase 1 진단 결정적 발견**: 191 ticker = 사이클 101 `_full_universe_load_task_loop` 어제 (2026-06-10 수) 20:00:05 **미발화** 영역 영구 영속 확정 (root cause = start() 직후 → `_wait_until(20:00:05)` 영역 영구 영속 대기 중 → 20:10 _settle → stop() cancel 발화 = **lifecycle race**). UI 사이클 94 안내 배너 영역 영구 영속 = 사이클 95~101 영구 영속 후 무효 영역. **사용자 결정 (영구 영속)**: Q1=A scheduler.py `_full_universe_load_task_loop` 시정 = start() 직후 즉시 1회 + 다음 20:00:05 무한 루프 (사이클 78/83 답습 + is_stale 24h idempotency) + Q2=A UI 신규 메시지 영역 영구 영속 추가 ("매일 20:00 KRX/KOSDAQ 전 종목 일괄 적재 + 보유 종목 + 매수 후보 종목 5분 주기 자동 갱신") + Q3=A UI 적재 실패 영역 영구 영속 정밀화 ("KIS API 일시 결함 — 잠시 후 재시도"). **시정 (production 2 파일)**: **영역 1+4 (frontend-dev, UI 안내 배너 영역 영구 영속 갱신)**: `frontend/src/pages/StockMaster.tsx:468~479` (`stock-master-info-banner` testid 영역 영구 영속) — 사이클 94 영역 영구 영속 무효 메시지 ("각 전략의 후보 종목은 매 사이클...") 제거 + 신규 메시지 추가 ("매일 20:00 KRX/KOSDAQ 전 종목 일괄 적재 + 보유 종목 + 매수 후보 종목 5분 주기 자동 갱신"). **영역 2 (frontend-dev, "적재 실패" UI toast 영역 영구 영속 정밀화)**: `frontend/src/pages/StockMaster.tsx::refreshMutation.onError` 영역 — "적재 실패 — 잠시 후 재시도" → "KIS API 일시 결함 — 잠시 후 재시도" (사이클 76 `[api_retry_recovered_summary]` graceful 영역 영구 영속 정합). **영역 3 (backend-dev, `_full_universe_load_task_loop` lifecycle 시정)**: `src/engine/scheduler.py::_full_universe_load_task_loop` (+21/-14L 순증 +7L) — 기존 (사이클 101): while 루프 `_wait_until(20:00:05)` 후 호출 → lifecycle race 영역 영구 영속 / 시정: start() 직후 즉시 1회 실행 + while 루프 `_wait_until(20:00:05)` 무한 루프 (사이클 78/83 답습 + is_stale 24h TTL idempotency 영역 영구 영속 활용). 즉시 실행 블록: `CancelledError → return` / `Exception → graceful logger.exception`. while 루프 블록: `CancelledError → break` / `Exception → graceful logger.exception + asyncio.sleep(60)`. **회귀 가드 19 신규 회귀 가드 (영역 3 lifecycle: G-LC1~7 7 HIGH/MEDIUM/LOW + 영역 3 idempotency: G-ID1~6 6 HIGH/MEDIUM/LOW + 영역 1+2+4 UI: G-BANNER1/2/3 + G-TOAST1 6 sub-case)**. **백엔드 2315 → 2342 PASS** (+27 신규 = 사이클 106 lifecycle + idempotency 통합) / **프론트엔드 (이전 cycle) → 268 PASS** / **합계 2610 PASS** / **flakiness 0** (3 회 반복 동일 카운트) / 회귀 0 / 매매 안전성 무영향 확정. **영속 의무 매트릭스 영구 영속 확인 (8 영역 전수 영속)**: (1) 사이클 32 R4 universe guard 영속 + (2) 사이클 38 명문화 영속 + (3) 사이클 65 H3 retry:1 영속 + (4) 사이클 78 G-AST1 + 79 G-AST2 영속 (`_full_universe_load_task` stop 튜플 포함 영구 영속) + (5) 사이클 88 G-REJECT 영속 + (6) 사이클 101 영속 영구 확인 (`_full_universe_load_once` 함수 영역 변경 0) + (7) 사이클 102 G-REJECT 영속 + (8) CLAUDE.md "절대 깨지 말 것" 8 영역 영속. **매매 안전성 무영향 확정** (영역 1/2/4 = UI 가시화 영역 한정 + 영역 3 = lifecycle race 차단 영역 한정 + 매수/매도/익일청산/15:20 강제청산 hot path 무관). **사이클 49→106 누적 61 사이클 + hotfix 14 영역**. **운영 효과 영구 영속 (push + EC2 자동 배포 후)**: (a) `[full_universe_load] 초기 실행 완료 total=N kospi=K kosdaq=L` start() 직후 즉시 1회 emit + 매일 20:00:05 while 루프 정기 실행 영역 영구 영속 / (b) stock_master 191 ticker → **~2,800 ticker 정상화 영역 영구 영속** / (c) UI 안내 배너 = 사이클 94 무효 메시지 영구 제거 + 신규 메시지 영구 영속 / (d) "적재 실패" toast = "KIS API 일시 결함 — 잠시 후 재시도" 영구 영속 정밀화. **사이클 107+ 후속 카드 인계**: 운영 실측 의무 (D+1 2026-06-12 금 07:55 KST 시점 측정 — `[full_universe_load] 초기 실행 완료` emit 영구 영속 + stock_master ~2,800 ticker 정상화 영구 영속 + UI 신규 안내 배너 영구 영속 표시 + 사용자 토스트 정밀화 영구 영속 확인) / 카드 #21 (LOW 사이클 74 flaky stale_watcher logging caplog race) / #16 / auth 1.43x / 사이클 100 인계 영역 3+4 (Plan Phase B). | `src/engine/scheduler.py` (+21/-14L 순증 +7L `_full_universe_load_task_loop` start() 직후 즉시 1회 + while 루프 영역 영구 영속) / `frontend/src/pages/StockMaster.tsx` (L468~L479 안내 배너 영역 영구 영속 갱신 + refreshMutation.onError 영역 영구 영속 정밀화) / `tests/unit/engine/test_cycle106_full_universe_load_lifecycle.py` 신규 (G-LC1~7) / `tests/unit/engine/test_cycle106_full_universe_load_idempotency.py` 신규 (G-ID1~6) / `frontend/src/pages/__tests__/StockMaster.test.tsx` 갱신 (G-BANNER1/2/3 + G-TOAST1) / `_workspace/red/cycle106_stock_master_inspection.md` Red 명세 영구 기록 / `_workspace/cycle106_phase1_diagnosis.md` Phase 1 진단 영구 기록 / `_workspace/cycle106_verify_report.md` verify 보고서 / `src/engine/CLAUDE.md` 본문 사이클 106 영역 (lifecycle race 차단 + is_stale 24h TTL idempotency 활용 + 사이클 78/83 답습) / `frontend/CLAUDE.md` 본문 사이클 106 영역 (StockMaster.tsx 안내 배너 갱신 + toast 정밀화) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 106 행 / `docs/HARNESS_CHANGELOG.md` 사이클 106 행 영구 기록 | 사용자 보고 (191 ticker stock_master 영구 영속 결함 + UI 사이클 94 안내 배너 무효 영역) 즉시 시정 (4 영역 통합). 41 사이클 연속 옵션 A 패턴 영속. team-leader Phase 1 진단 (사이클 101 `_full_universe_load_task_loop` 20:00:05 미발화 lifecycle race 단일 근본 원인 확정) + tdd-engineer Red + backend-dev + frontend-dev Green 병렬 + tester verify (백엔드 2342 PASS + 프론트엔드 268 PASS + 합계 2610 PASS + flakiness 0 + 회귀 0). 매매 안전성 무영향 (lifecycle race 차단 + UI 가시화 영역 한정, 매매 hot path 무관). 영속 의무 매트릭스 8 영역 전수 영속 (사이클 32 R4 + 38 명문화 + 65 H3 retry:1 + 78 G-AST1 + 79 G-AST2 + 88 G-REJECT + 101 영속 + 102 G-REJECT + CLAUDE.md "절대 깨지 말 것" 8 영역). 운영 효과 영구 영속: start() 직후 즉시 1회 emit + stock_master 191 → ~2,800 ticker 정상화 + UI 신규 안내 배너 영구 영속 + toast 정밀화. 사이클 107+ 후속 카드: D+1 운영 실측 의무 (2026-06-12 금 07:55 KST) / 카드 #21 + #16 + auth 1.43x + 사이클 100 영역 3+4 (Plan Phase B). |
| 2026-06-11 | 사이클 104 — **Playwright E2E spec 신규 (사이클 103 신규 페이지 검증) + silent 결함 2 영구 시정** (**40 사이클 연속 옵션 A 패턴 영속**). **신규 spec 2**: `e2e/realtime-health.spec.ts` (8 케이스: H-RH1~H-RH4 HIGH 4 + M-RH5~M-RH6 MEDIUM 2 + L-NAV1 + L-MOB LOW 2) + `e2e/strategies.spec.ts` (9 케이스: H-ST1~H-ST4 HIGH 4 + M-ST5~M-ST7 MEDIUM 3 + L-NAV2 + L-ST8 LOW 2). **silent 결함 1 — `**/api/logs*` Playwright glob Vite 모듈 intercept (근본 원인 확정)**: `e2e/fixtures/api-mocks.ts` 의 `**/api/logs*` Playwright glob 이 `http://localhost:3000/src/api/logs.ts` Vite 모듈 요청 (resourceType='script') 을 intercept → JSON 반환 → MIME 타입 불일치 → `logs.ts` 모듈 로딩 실패 → `realtime-health.ts` 로딩 실패 → `RealtimeHealth.tsx` 동적 import 실패 → realtime-health.spec.ts 7/8 FAIL. **시정**: resourceType guard 추가 (`if (route.request().resourceType() === "script") return route.continue()`). **미래 동일 패턴 영구 차단**: Playwright glob 이 Vite 모듈 경로와 충돌하는 경우 resourceType guard 의무. **silent 결함 2 — settings.spec.ts 회귀 (사이클 103 내포 형식 핸들러 LIFO 우선)**: 사이클 103 api-mocks 에 추가된 `/api/strategies` GET 내포 형식 핸들러 (`{strategies: {momentum: ...}}`) 가 LIFO 우선으로 Settings.tsx `getStrategies()` 에 잘못된 응답 → `name = undefined` → "상한가 모멘텀" 미렌더 → settings.spec.ts 1/13 FAIL. **시정 3종**: (1) 사이클 103 내포 형식 핸들러 제거 + (2) 기존 플랫 형식 핸들러에 4 임계 params 추가 (`stop_loss_rate: -7.5` / `daily_loss_limit: -5.0` / `trailing_stop_rate: -2.0`) + (3) `frontend/src/pages/Strategies.tsx` 에 `data?.strategies ?? data` fallback 추가 (백엔드 플랫/내포 형식 양쪽 처리). **검증 결과**: e2e/realtime-health.spec.ts 8/8 PASS × 3회 + e2e/strategies.spec.ts 9/9 PASS × 3회 + 전체 e2e 30/30 PASS × 3회 (flakiness 0) + 기존 spec (settings/dashboard) 회귀 0. **영속 의무 매트릭스 전수 영속**: 사이클 80 hotfix #3/#4 Playwright LIFO 영속 + 사이클 89 한글 친숙 용어 영속 + 사이클 81 G-MOBILE-9 (9개 메뉴) 영속 + 사이클 65 H3 useQuery retry:1 영속. **매매 안전성 무영향** (e2e spec + api-mocks fixtures + Strategies.tsx fallback 영역 한정, production 매매 hot path 무관). **사이클 49→104 누적 59 사이클 + hotfix 14 영역**. | `e2e/realtime-health.spec.ts` 신규 (8 케이스, 380L) / `e2e/strategies.spec.ts` 신규 (9 케이스, 188L) / `e2e/fixtures/api-mocks.ts` 갱신 (`**/api/logs*` resourceType guard + 사이클 103 내포 형식 핸들러 제거 + momentum params 4 임계 추가) / `frontend/src/pages/Strategies.tsx` 갱신 (`data?.strategies ?? data` fallback + 관련 코드 주석) | E2E spec 신규 (사이클 103 신규 페이지 검증) + silent 결함 2 영구 시정. 40 사이클 연속 옵션 A 패턴 영속. tdd-engineer Red + frontend-dev Green (E2E spec + api-mocks 시정 + Strategies.tsx fallback) + tester verify (3회 반복 30/30 PASS flakiness 0). 매매 안전성 무영향. 사이클 80 hotfix #3/#4 Playwright LIFO 영속 + 사이클 89 한글 친숙 용어 영속 + 사이클 81 G-MOBILE-9 영속. 사이클 104+ 후속 카드: D+1 운영 실측 의무 (2026-06-12 금 09:00~10:00) / 카드 #21 + #16 + auth 1.43x + 사이클 100 영역 3+4. |
| 2026-06-11 | 사이클 103 — **4 영역 통합 시정 (운영 가시화 UI 4 카드 + 손절 임계 가시화 + momentum 로그 임계 동행 + strategy.py dead code 영구 폐기 + 영역 4 명세 동기화)** (**39 사이클 연속 옵션 A 패턴 영속**). **사이클 102.5 회고 결정적 사실 (영구 영속)**: 코미코(183300) 2026-06-11 10:03 매수 138,200원 → 13:21:30 STOP_LOSS 매도 138,200원. 단일 근본 원인 = DB `strategy_config.momentum.params.stop_loss_rate = -2.4%` (2026-06-03 22:11 KST 사용자 수동 apply, AI 자문 권고 reasoning = "승률 낮음 + 손절 빈도 19회 + 변동성 9.21 → 보수적 손절 임계"). 매도 시점 loss_rate = -2.604% ≤ -2.4% 만족 → momentum.py STOP_LOSS 분기 정상 작동. 사용자 의문 = 코드 DEFAULT `-7.5%` 가정 → 운영 영역 `-2.4%` 영구 영속 가시화 부족 → 오인. 결함 영역 = 아님 (코드 정상, 운영 가시화 영역 보강 의무). **사용자 결정**: Q10'=B 영역 1 = Strategies.tsx 각 전략 카드 영역 4 임계 영속 보강 + Q11'=C 영역 2 = momentum.py 로그 AST + 단위 테스트 통합 + Q12'=B 영역 3 = AST 영구 가드 신설 후 dead code 삭제 + Q13'=A 영역 4 = 사이클 103 명세 동기화 통합. **시정 (production 9 파일)**: **영역 0 (frontend-dev, 운영 가시화 UI 4 카드 신규 페이지)**: `frontend/src/types/realtime-health.ts` 신규 (33L) + `frontend/src/api/realtime-health.ts` 신규 (84L, `/api/logs/search` 기존 활용) + `frontend/src/pages/RealtimeHealth.tsx` 신규 (244L, 4 카드 = `[dispatch_drop_summary]` + `[callback_exception]` + `[stale_force_retry]` + `[ws_auto_restart]`) + `frontend/src/App.tsx` 갱신 (navItems 7→9 = 실시간 상태 + 전략 현황 2 추가) + MSW handler + Playwright LIFO 정합 (사이클 80 hotfix #3 영속). **영역 1 (frontend-dev, UI 손절 임계 가시화)**: `frontend/src/pages/Strategies.tsx` 신규 (233L) — 각 전략 카드 영역 4 임계 영속 (stop_loss_rate / daily_loss_limit / trailing_stop_rate / position_ratio). 데이터 = 기존 `GET /api/strategies` 영구 영속 (`strategy_registry.py:153 params: s.config.params`). 한글 라벨 + graceful (`params={}` 시 "—"). **영역 2 (backend-dev, momentum.py 손절 로그 임계 동행 emit)**: `src/engine/strategies/momentum.py` L127 1줄 변경 — Before: `"손절 신호: %s(%s) 매수가(%d) 대비 %.1f%% (현재가: %d)"` / After: `"손절 신호: %s(%s) 매수가(%d) 대비 %.1f%% (임계: %.1f%%, 현재가: %d)"` (+1 인자 `stop_loss`). 미래 동일 영역 운영자 오인 영구 차단. **영역 3 (backend-dev, strategy.py dead code 영구 폐기)**: `src/engine/strategy.py` L85-207 3 모듈 함수 영구 폐기 (-119L) — `check_buy_signal` (64L) + `check_stop_loss` (21L) + `check_next_day_clear` (34L). callsite 0건 영구 영속 확인 후 안전 폐기 (사이클 78 G-AST1 + 79 G-AST2 답습). AST 영구 가드: `tests/unit/ast/test_cycle103_ast_no_dead_strategy_funcs.py` (미래 재발 영구 차단). 보존 영역: `class Signal` + `class Position` + `class StrategyState` + 7 상수 + `calc_buy_quantity()` 영속. **영역 4 (team-leader, 명세 동기화)**: `_workspace/00_leader_trading_rules.md` momentum 손절 임계 명세 영역 동기화 — 명세 영역 `-7.5%` (DEFAULT) → DB 운영 영역 `-2.4%` (사용자 apply 영역, 2026-06-03 22:11 KST). 시계열 진화 영구 영속 명시: `-7.5` (DEFAULT) → `-4.5` (05-07) → `-3.8` (05-12) → `-3.2` (05-15) → `-2.8` (05-28) → `-2.4` (06-03). AI 자문 reasoning 영구 영속 명시. 향후 운영 영역 변경 시 본 명세 동기화 의무 추가. **회귀 가드 매트릭스**: AST 영구 가드 3 신규 (G-103-DEAD strategy.py 3 함수 영구 부재 AST + G-103-LOG momentum.py L127 임계 동행 emit AST + G-103-UI Strategies.tsx 4 임계 영속 AST). **백엔드 2312 → 2323 PASS** (+11) / **프론트엔드 245 → 265 PASS** (+20) / **합계 2588 PASS** / **flakiness 0** (3 회 반복 동일) / 회귀 0. **영속 의무 매트릭스 영구 영속 (10 영역)**: 사이클 38 명문화 (영역 2 매도 hot path 영속) / 사이클 41 StrategyFunnel 4 카드 패턴 / 사이클 65 H3 useQuery retry:1 / 사이클 68 KST / 사이클 75 G-AST5 api-mocks / 사이클 78 G-AST1 + 79 G-AST2 (영역 3 답습) / 사이클 80 hotfix #3/#4 Playwright LIFO / 사이클 85 G-AST-RT/G-AST-MOCK + M-9 / 사이클 89 한글 친숙 용어 / 사이클 102 G-REJECT (양 agent 일치). **CLAUDE.md "절대 깨지 말 것" 8 영역 영속**. **매매 안전성 무영향** (영역 0/1 = UI 가시화 영역 한정 + 영역 2 = 로그 메시지 1줄 보강 영역 + 영역 3 = dead code 영구 폐기 (callsite 0건 영구 영속 확인) + 영역 4 = 명세 동기화 영역 = 매수/매도/익일청산/15:20 강제청산 hot path 무관). **사이클 49→103 누적 58 사이클 + hotfix 14 영역**. **운영 효과 (push + EC2 자동 배포 후)**: (a) `/realtime-health` 페이지 = 4 카드 실시간 가시화 (사이클 102 신규 prefix 3 + 사이클 92 [ws_auto_restart]) / (b) `/strategies` 페이지 = 6 전략 각 4 임계 실시간 가시화 (사용자 오인 영역 영구 차단) / (c) momentum.py 손절 로그 = 임계 동행 emit (운영자 즉시 확인) / (d) strategy.py = dead code 영구 폐기 (코드 정합성 영속). **사이클 104+ 후속 카드 인계**: 운영 실측 의무 (D+1 2026-06-12 금 09:00~10:00) / 카드 #21 (LOW 사이클 74 flaky) / #16 / auth 1.43x / 사이클 100 인계 영역 3+4 (market_cap 페이징 + CTPF1002R 편입 + 매일 20:00 일괄 적재). | `frontend/src/types/realtime-health.ts` 신규 (33L) / `frontend/src/api/realtime-health.ts` 신규 (84L) / `frontend/src/pages/RealtimeHealth.tsx` 신규 (244L) / `frontend/src/pages/Strategies.tsx` 신규 (233L) / `frontend/src/App.tsx` 갱신 (navItems 7→9) / `frontend/src/test/handlers.ts` 갱신 (MSW handler) / `e2e/fixtures/api-mocks.ts` 갱신 (Playwright LIFO 정합) / `src/engine/strategies/momentum.py` (+1 인자 L127 임계 동행 emit) / `src/engine/strategy.py` (-119L 3 모듈 함수 영구 폐기, class Signal/Position/StrategyState + 7 상수 + calc_buy_quantity 보존) / `tests/unit/ast/test_cycle103_ast_no_dead_strategy_funcs.py` 신규 (G-103-DEAD) / `tests/unit/engine/strategies/test_cycle103_momentum_log_emit.py` 신규 (G-103-LOG) / `frontend/src/pages/__tests__/Strategies.test.tsx` 신규 (G-103-UI) / `_workspace/00_leader_trading_rules.md` momentum 손절 임계 명세 동기화 (시계열 진화 + AI 자문 reasoning 영구 영속) / `src/engine/CLAUDE.md` 본문 사이클 103 영역 (strategy.py dead code 폐기 + momentum.py 로그 임계 동행) / `frontend/CLAUDE.md` 본문 사이클 103 영역 (RealtimeHealth + Strategies 2 페이지 신규 + navItems 9개 영속) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 103 행 / `docs/HARNESS_CHANGELOG.md` 사이클 103 행 영구 기록 | 사이클 102.5 회고 결정적 사실 (코미코 STOP_LOSS 운영 가시화 부족 = 단일 근본 원인 운영 영역 -2.4% apply) 즉시 시정 (4 영역 통합). 39 사이클 연속 옵션 A 패턴 영속. team-leader Phase 1 진단 (코미코 사례 단일 근본 원인 확정) + tdd-engineer Red + backend-dev + frontend-dev Green 병렬 + tester verify. 매매 안전성 무영향 (UI 가시화 + 로그 보강 + dead code 폐기 + 명세 동기화 영역 한정, 매매 hot path 무관). 영역 3 dead code 영구 폐기 = 사이클 78 G-AST1 + 79 G-AST2 영속 패턴 답습 (callsite 0건 영구 영속 확인 후 안전 폐기 + AST 영구 가드 신설). 영역 4 명세 동기화 = 향후 운영 영역 변경 시 본 명세 동기화 의무 신설 (미래 운영자 오인 영구 차단). 영속 의무 매트릭스 10 영역 전수 영속 (사이클 38/41/65/68/75/78/79/80/85/89/102 + CLAUDE.md 8 영역). 운영 효과: /realtime-health + /strategies 2 페이지 신규 운영 가시화 + momentum 손절 로그 임계 동행 emit + strategy.py dead code 영구 폐기. 사이클 104+ 후속 카드: D+1 운영 실측 의무 (2026-06-12 금 09:00~10:00) / 카드 #21 + #16 + auth 1.43x + 사이클 100 영역 3+4. |
| 2026-06-11 | 사이클 102 — **시세 구독 영역 전면 재검토 시정 (3 영역 통합)** — 사이클 78 silent 결함 영구 확인 + 외부 의견 가시화 + force_retry 임계 상향 (**38 사이클 연속 옵션 A 패턴 영속**). **사용자 신규 요구**: "재구독 로직 자체가 실수가 아닌가 싶어. 시세구독 관련 부분을 전면 새로운 시각에서 재검토하고 리팩토링 전문가와 함께 현재 시스템을 개선해줘. 기존시스템에 개발해둔 로직이 아까워서 억지로 유지하려고 하지 말고 완전히 새로운 시각에서 판단하도록 해." **refactor-expert + domain-expert 병렬 자문 결과 일치 (영속 의무)**: 사이클 88 G-REJECT 영구 영속 (재구독 영역 전면 폐기 = 사이클 29 005935 LMS chain 사고 재현 위험) + 가시화 영역 강화 + 임계 상향. **사용자 결정**: Q71=A (사이클 78 silent 결함 의심 영역 우선) + Q72=F+가시화 (`last_ws_message_at` + dispatch + callback) + Q73=B (5분 → 10분 + 12회 → 6회). **시정 (production 4 파일)**: **영역 1 (HIGH 부차, 코드 변경 0)**: `scheduler.py::_api_recovered_collector_loop` (L2543~L2565) + `stop()` (L923~L928) 영속 영구 확인 + AST 영구 가드 5 신설 (G-78-VERIFY-1~5). **영역 2 (HIGH, 3 영역 통합 가시화)**: (a) `src/realtime/websocket.py` (+7L) `KisWebSocket.__init__` (L165) `_last_ws_message_at: dict[str, datetime]` 신규 인스턴스 변수 + `_handle_raw` (L609) 진입 시 `self._last_ws_message_at[self._label] = datetime.now(_KST_TZ)` update (사이클 16 _aes_iv 패턴 답습 + 사이클 88 G-REJECT-2 종목별 ticker_last_tick 영속 + 사이클 102 세션별 _last_ws_message_at 분리 = 책임 분리). (b) `src/realtime/handler.py` (+54L / -2L) 모듈 전역 `_silent_drop_count: dict[str, int]` 신규 (L59) + `_handle_tick` (L94~L113) `len<10` (L101) + `parsed is None` (L109) 양쪽 영역 graceful silent drop + `_silent_drop_count[ticker]+=1` 누적 + 신규 함수 `flush_silent_drop_count()` (L223~L238, 5분 collector flush `[dispatch_drop_summary] window=300s drops_total=N by_ticker={...}` 1행 emit + clear, 사이클 74 답습 + Q2 빈 윈도우 skip 영속) + `_handle_tick` (L121~L128) + `_handle_execution` (L178~L186) + `_handle_market_op` (L212~L220) 3 콜백 일관 `try/except` + `logger.exception("[callback_exception] handler=... ticker=...")` + **`raise` 영속 (사이클 88 G-REJECT-1 영구 영속 = 재연결 trigger 영역 영구 보존)**. (c) `src/engine/scheduler.py` (+4L) `_api_recovered_collector_loop` 영역 (L2567~L2571) `from src.realtime.handler import flush_silent_drop_count` import (L41) + try/except flush 호출 (사이클 78 G-AST1 영역 답습). **영역 3 (HIGH, 사이클 29 R1 패턴 답습)**: `src/engine/stale_diagnostics.py` (+2L / -2L) — L31 `STALE_FORCE_RETRY_AFTER_SECS = 300` → `600` (5분 → 10분) + L32 `STALE_FORCE_RETRY_HOURLY_CAP = 12` → `6` (12회 → 6회). 사유: KIS LMS chain 사고 안전 마진 2 배 확장 + 운영 실측 (예상 124~141건/일 → 50~70건/일). **회귀 가드 17 케이스 (6 파일 신규)**: `tests/unit/engine/test_cycle102_stale_summary_emit.py` (G-78-VERIFY-1/5, 2) + `tests/unit/ast/test_cycle102_ast_flush_call_sites.py` (G-78-VERIFY-2/3/4, 3) + `tests/unit/realtime/test_cycle102_last_ws_message_at.py` (G-WS-MSG1/2, 2) + `tests/unit/realtime/test_cycle102_dispatch_drop.py` (G-DISPATCH1~3, 3) + `tests/unit/realtime/test_cycle102_callback_exception.py` (G-CALLBACK1, 1) + `tests/unit/ast/test_cycle102_ast_callback_exception.py` (G-CALLBACK2, 1) + `tests/unit/engine/test_cycle102_force_retry_threshold.py` (G-THRESHOLD1/2/3 + G-LMS1 + G-PERSIST1, 5). **xfail 의미 전환 16건** (사이클 29/60/61/63 영역, 사이클 66 K-2 패턴 답습). **백엔드 2311 → 2312 PASS + 118 XFAIL + 2 skipped + 1 xpassed** (+1 순증) / **flakiness 0** (3 회 반복 동일) / 회귀 0. **영속 의무 매트릭스 영구 확인 (10 영역)**: 사이클 88 G-REJECT-1/2/3 영구 영속 (외부 LLM 단순 graceful 추천 거부, 재연결 trigger 영역 영구 보존) / 사이클 17 OPSP0002 backoff 300s 영속 / 사이클 29 R1/R2/R3 영속 (임계 상향만, 영역 폐기 0) / 사이클 32 R4 universe guard 영속 / 사이클 66 cap=10 priority 분리 영속 / 사이클 67 stale_manager 4 sub-module 영속 / 사이클 78 G-AST1 flush 호출 사이트 영속 (영역 1 영구 확인) / 사이클 79 G-AST2 task cancel 영속 / WebSocket 4중 안전망 (F1 + `_scan_loop` + K stale watcher + `_resubscribe_stale_priority`) 영속 / CLAUDE.md "절대 깨지 말 것" 8 영역 영속. **매매 안전성 무영향 확정** (가시화 + 임계 상향 영역 한정, 매수/매도/익일청산/15:20 강제청산 hot path 무관 + 사이클 88 G-REJECT-1 callback `raise` 영속 = 재연결 trigger 영역 영구 보존). **사이클 49→102 누적 57 사이클 + hotfix 14 영역**. **운영 효과 예상 (push + EC2 자동 배포 후)**: (a) `_last_ws_message_at` 세션별 가시화 → 추후 진단 영역 확장 가능 / (b) `[dispatch_drop_summary]` 5분 주기 emit → 미래 silent drop silent 결함 영구 차단 / (c) `[callback_exception]` 전수 가시화 + raise 영속 (재연결 trigger 영역 보존) / (d) `[stale_force_retry]` 124~141건/일 → 50~70건/일 영구 감소 (LMS chain 안전 마진 2 배 확장). **사이클 103+ 후속 카드 인계**: 운영 실측 의무 (D+1 2026-06-12 금 09:00~10:00 측정 — dispatch_drop / callback_exception / stale_force_retry 빈도 + last_ws_message_at 세션별 갱신 정확성) / 카드 #21 (LOW 사이클 74 flaky) / #16 / auth 1.43x / 사이클 100 인계 영역 3+4 (market_cap 페이징 + CTPF1002R 편입 + 매일 20:00 일괄 적재). | `src/realtime/websocket.py` (+7L L162~L165 `_last_ws_message_at` 인스턴스 변수 + L607~L609 `_handle_raw` update) / `src/realtime/handler.py` (+54L / -2L L56~L59 모듈 전역 `_silent_drop_count` + L101~L113 `_handle_tick` 2 silent drop 분기 + L121~L128 + L178~L186 + L212~L220 3 콜백 try/except + raise 영속 + L223~L238 신규 `flush_silent_drop_count()`) / `src/engine/stale_diagnostics.py` (+2L / -2L L29~L32 STALE_FORCE_RETRY_AFTER_SECS 300 → 600 + STALE_FORCE_RETRY_HOURLY_CAP 12 → 6) / `src/engine/scheduler.py` (+4L L41 import + L2567~L2571 flush 호출) / `tests/unit/engine/test_cycle102_stale_summary_emit.py` 신규 (G-78-VERIFY-1/5) / `tests/unit/ast/test_cycle102_ast_flush_call_sites.py` 신규 (G-78-VERIFY-2/3/4) / `tests/unit/realtime/test_cycle102_last_ws_message_at.py` 신규 (G-WS-MSG1/2) / `tests/unit/realtime/test_cycle102_dispatch_drop.py` 신규 (G-DISPATCH1~3) / `tests/unit/realtime/test_cycle102_callback_exception.py` 신규 (G-CALLBACK1) / `tests/unit/ast/test_cycle102_ast_callback_exception.py` 신규 (G-CALLBACK2) / `tests/unit/engine/test_cycle102_force_retry_threshold.py` 신규 (G-THRESHOLD1/2/3 + G-LMS1 + G-PERSIST1) / xfail 의미 전환 16건 (사이클 29/60/61/63 영역, 사이클 66 K-2 패턴 답습) / `src/realtime/CLAUDE.md` 본문 사이클 102 영역 (last_ws_message_at + dispatch silent drop + callback exception) / `src/engine/CLAUDE.md` 본문 사이클 102 영역 (force_retry 임계 상향 + flush 호출 사이트) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 102 행 / `docs/HARNESS_CHANGELOG.md` 사이클 102 행 영구 기록 | 사용자 신규 요구 (재구독 영역 전면 재검토) + refactor-expert + domain-expert 병렬 자문 일치 = 사이클 88 G-REJECT 영구 영속 (재구독 영역 전면 폐기 = 사이클 29 005935 LMS chain 사고 재현 위험) + 가시화 영역 강화 + 임계 상향. team-leader Phase 1 진단 + 양 agent 자문 + tdd-engineer Red + backend-dev Green (4 파일 통합 시정 +67L / -4L) + tester verify. 38 사이클 연속 옵션 A 패턴 영속. 매매 안전성 무영향 (가시화 + 임계 상향 영역 한정, 사이클 88 G-REJECT-1 callback raise 영속 = 재연결 trigger 영역 영구 보존). 영속 의무 매트릭스 10 영역 전수 영속 (사이클 17/29/32/66/67/78/79/88 + 4중 안전망 + CLAUDE.md 8 영역). 사이클 100 인계 영역 3+4 (market_cap 페이징 + CTPF1002R 편입 + 매일 20:00 일괄 적재) 사이클 103+ 영속. 운영 효과 예상: dispatch silent drop 가시화 + callback exception 전수 + stale_force_retry 124~141건/일 → 50~70건/일 영구 감소. 사이클 103+ 후속 카드: D+1 운영 실측 의무 (2026-06-12 금 09:00~10:00) / 카드 #21 + #16 + auth 1.43x + 사이클 100 영역 3+4. |
| 2026-06-11 | 사이클 100 — **UI prefix 정정 (count_eager_refresh_today 3 prefix OR 합산) + 주문 발주 시장 분기 영속 확인 (영역 2 변경 0)** (**37 사이클 연속 옵션 A 패턴 영속**). **사용자 보고 (D+1 운영 측정 영역)**: "오늘 자동 갱신 횟수 0" UI 영역 vs 백엔드 실측 614 영역 (`[universe_eager_refresh]` 244 + `[scan_pool_eager_refresh]` 230 + `[stock_master_bulk_refresh]` 140 = 614, 단일 grep 시 230 만 노출). **단일 근본 원인**: `src/db/stock_master.py::count_eager_refresh_today` (L169~194) = 단일 grep `[scan_pool_eager_refresh]` → 244+140 = 384 영역 누락 (~62% 노출). 사용자 요구 = (1) UI prefix 정정 (영역 1) + (2) 주문 발주 시장 분기 영속 확인 (영역 2, 적시 청산주문 영역) + (3+4 사이클 101 인계). 사용자 결정 (Q61=A 사이클 100 영역 1+2 응급 + 사이클 101 영역 3+4 신규 + Q65=C-1 3 prefix OR + Q64=매일 20:00 일괄 적재 사이클 101 영속). **시정 (production 1 파일)**: `src/db/stock_master.py::count_eager_refresh_today` (+13/-7L) — 단일 `ilike("%[scan_pool_eager_refresh]%")` 1회 → 3 prefix 순차 호출 + 합산 (사이클 89 `[universe_eager_refresh]` + 사이클 83 `[scan_pool_eager_refresh]` 영속 + 사이클 95 `[stock_master_bulk_refresh]`). **영역 2 (변경 0 영속 확인)**: `_strategy_exchange_async` (`src/engine/order_engine.py:128~197`) 사이클 13 영속이 매수 (L307) + 매도 (L543) 양쪽 호출 100% 통일 영속 + 사이클 55 R-1 `SellRejectionTracker` (is_blocked L505 + register_market_closed L611 + reset_daily 위임 L1134) 영속 + 사이클 31 R6/R7 익일 청산 자동 전환 영속 = 적시 청산 영역 영구 차단 영속. **회귀 가드 13 sub-case (8 매트릭스)**: HIGH 4 (G-UI1 3 prefix mock 합산 2 sub + G-UI2 AST 3 prefix 본체 등장 2 sub + G-MKT1 매수 영역 + execute_buy 호출 2 sub + G-MKT2 매도 영역 호출) + MEDIUM 2 (G-INT1 통합 384 응답 + G-REG1 SellRejectionTracker 3 sub) + LOW 2 (G-DOC1 사이클 98 chk_fluctuation 인용 영속 + G-PERSIST1 사이클 99 60 ticker 영속). **백엔드 2302 → 2315 PASS** (+13 신규) + 2 skip + 56 XFAIL + 1 XPASS / **프론트 245 PASS** 영속 / **합계 2560 PASS** / **flakiness 0** (3 회 47.87~48.46s 동일) / 회귀 0. **영속 의무 매트릭스 9 영역 전수 영속**: 사이클 13 영속 (영역 2 변경 0) / 55 R-1 SellRejectionTracker / 31 R6/R7 / 84 history trigger / 88 G-REJECT / 89 [universe_eager_refresh] / 83 [scan_pool_eager_refresh] / 95 [stock_master_bulk_refresh] / 99 60 ticker 영구 영속. **CLAUDE.md "절대 깨지 말 것" 8 영역 영속**. **매매 안전성 무영향** (DB 카운트 영역 단독 시정 + 영역 2 변경 0 + 매수/매도/익일청산/15:20 hot path 무관). **사이클 49→100 누적 55 사이클 + hotfix 14 영역**. **운영 효과 (push + EC2 자동 배포 후)**: UI 종목마스터 "오늘 자동 갱신 횟수" = 단일 prefix 대비 **~2.6배 증가 (실측 614/230)** + 사용자 가독성 영구 정합. **사이클 101 후속 카드 인계 (Plan 영속)**: 영역 3+4 통합 (market_cap FHPST01740000 페이징 KOSPI/KOSDAQ 분리 + CTPF1002R search_stock_info 67 컬럼 편입 + Q64 매일 20:00 일괄 적재 = KIS API 만으로 stock_master 전체 ~2,800 종목 완성 16x 확장 영역, Plan Phase B 권고) / domain-expert 자문 의무 (Q62=B 영역 3+4 분리 자문) / 카드 #21 (LOW 사이클 74 flaky) / #16 / auth 1.43x. | `src/db/stock_master.py` (+13/-7L count_eager_refresh_today 3 prefix OR) / `tests/unit/db/test_cycle100_count_eager_refresh_three_prefix.py` 신규 (G-UI1) / `tests/unit/ast/test_cycle100_ast_count_eager_refresh_three_prefix.py` 신규 (G-UI2) / `tests/unit/engine/test_cycle100_strategy_exchange_persistence_buy.py` 신규 (G-MKT1) / `tests/unit/engine/test_cycle100_strategy_exchange_persistence_sell.py` 신규 (G-MKT2) / `tests/integration/test_cycle100_count_eager_refresh_integration.py` 신규 (G-INT1) / `tests/unit/engine/test_cycle100_sell_rejection_persistence.py` 신규 (G-REG1) / `tests/unit/ast/test_cycle100_docstring_kis_chk_citation.py` 신규 (G-DOC1) / `tests/unit/engine/scanner/test_cycle100_60_ticker_persistence.py` 신규 (G-PERSIST1) / `_workspace/red/cycle100_ui_prefix_fix_market_branching_persistence.md` Red 명세 / `_workspace/cycle100_phase1_diagnosis.md` Phase 1 진단 (KIS MCP 정본 재검증 = market_cap 페이징 영역 결정적 발견) / `_workspace/cycle100_d1_operation_verification.md` D+1 운영 측정 영구 기록 / `_workspace/cycle100_verify_report.md` verify 보고서 / `_workspace/test_index.yaml` 영향 인덱스 갱신 (backend tests 468 → 476) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 100 행 | 사이클 99 D+1 운영 측정 (2026-06-11 목 08:01 KST) 시점 사용자 첨부 UI = "오늘 자동 갱신 횟수 0" 결함 영역 즉시 시정 + 사용자 요구 4 영역 통합 진단 (영역 1+2 응급 + 영역 3+4 사이클 101 인계). 37 사이클 연속 옵션 A 패턴 영속. team-leader Phase 1 진단 (KIS MCP 정본 재검증 결정적 발견 = market_cap FHPST01740000 페이징 지원 유일 API 영역) + tdd-engineer Red + backend-dev Green (1 파일 +6L 순증) + tester verify. 매매 안전성 무영향 (DB 카운트 영역 단독 + 영역 2 변경 0). 사이클 13 영속 (영역 2) + 사이클 55 R-1 + 31 R6/R7 + 99 60 ticker 영구 영속 + 98 G-DOC1 영속. 운영 효과: UI 카운트 ~2.6배 증가 (실측 614/230). 사이클 101 후속 카드: 영역 3+4 통합 (market_cap 페이징 + CTPF1002R 편입 + Q64 매일 20:00 일괄 적재 = stock_master 16x 확장 영역, Plan Phase B). | 사이클 100 영구 종결. |
| 2026-06-10 | 사이클 99 — **사이클 91 페이징 영역 영구 폐기** + 60 ticker 영구 영속 명문화 (KIS API 본질 한계 영구 확정)** (**36 사이클 연속 옵션 A 패턴 영속**). **사용자 보고**: 사이클 98 OPSQ2002 시정 후 "지금 새로고침" 토스트 = `universe 60 ticker 즉시 적재 완료 (3952ms)` 영속 (KOSPI 30 + KOSDAQ 30 = 사이클 89 도입 시점 영역과 동일). 3952ms 페이징 시도 영속이나 응답 영역 30 한도. **결정적 영구 확정**: **KIS API 전체 영역 (volume_rank + fluctuation) = 페이징 미지원 영구 확정** (운영 실증 사이클 96 + 사이클 98). 사이클 89/91/94/96/97/98 모든 시정 = KIS API 본질 한계 영구 무용. 사용자 결정 (Q58=C 60 ticker 영구 영속 + 사이클 91 페이징 영역 영구 폐기 + Q59=A 사이클 91 페이징 폐기 + 60 ticker 영구 영속 명문화). **시정 (production 1 파일 -53L 순감)**: `src/engine/scanner.py::_fetch_fluctuation` 페이징 영역 전수 폐기 — `accumulated: list[dict]` 변수 / `tr_cont` 인자 + 영역 / `for page in range(max_pages)` 루프 / `next_tr_cont = data.get("_response_headers", {}).get("tr_cont", "")` / `if next_tr_cont != "M": break` / `tr_cont = "N"` / `await _asyncio.sleep(0.05)` / `max_pages` 시그너처 인자 영구 폐기 + `top_n=30` 영구 영속 + 단일 호출 영역 + docstring 영역 KIS API 본질 한계 영구 명문화 (`KIS API + 페이징 + 미지원 + 60 ticker + chk_fluctuation.py 정본 인용`) + `fetch_top_500_universe()` `top_n=30` 갱신 + `[:30]` 슬라이싱 + `[:500]` 절단 폐기. **xfail 의미 전환 3건 신규 (사이클 66 K-2 패턴 답습)**: `test_cycle97_fluctuation_pagination_500::test_h2` (페이징 누적 영역 폐기) + `test_cycle97_rate_limit_2call::test_m2_rate_limit_sleep_between_pages` (sleep 영역 폐기) + `test_cycle97_scanner_upsert_chain::test_m3` (top_n=100 chain 폐기). **회귀 가드 5 케이스 (5 매트릭스)**: HIGH 3 (G-PURGE1 페이징 영역 5종 raw + AST 영구 부재 + G-PERSIST1 60 ticker mock 영속 + G-DEFAULT1 시그너처 `top_n=30` + `max_pages`/`tr_cont` 인자 영구 부재 AST) + MEDIUM 1 (G-DOC1 docstring KIS 본질 한계 + chk_fluctuation.py 정본 인용 영구 명문화) + LOW 1 (G-REG1 integration 2회 분리 호출 + 60 ticker mock). **백엔드 2300 → 2302 PASS** (+2 순증, +5 신규 - xfail 3 의미 전환) + 2 skip + 56 XFAIL (+3 신규) + 1 xpass / **프론트 245 PASS** 영속 / **합계 2547 PASS** / **flakiness 0** (3 회 반복 동일 카운트) / 회귀 0. **영속 의무 매트릭스 9 영역 전수 영속**: 사이클 88 G-REJECT (test_cycle95 흡수) / 89 ETF 제외 + 거래대금 정렬 / 93 chain / 95 unknown=0 / 98 G-DOC1 chk 인용 / 32 R4 / 38 명문화 / 64/65/81 graceful / KIS 정본 params 영속 (`FID_RANK_SORT_CLS_CODE="0"` 1자리 + `FID_INPUT_CNT_1=str(top_n)` + `FID_COND_SCR_DIV_CODE="20170"`). **CLAUDE.md "절대 깨지 말 것" 8 영역 영속**. **매매 안전성 무영향** (scanner.py 매도/체결 영역 0건 + 사이클 38 명문화 영속 + scanner 단계 매수 진입 전 + 매도/손절/Trailing/익일청산 hot path 무관). **silent 결함 영구 차단 26 회 누적** (사이클 91+94+96+97+98+99 통합). **사이클 49→99 누적 54 사이클 + hotfix 14 영역**. **운영 효과 (push + EC2 자동 배포 후)**: 사용자 UI "지금 새로고침" 클릭 → KIS fluctuation 2회 분리 호출 (단일 페이지) → **60 ticker 영구 영속 적재** (시스템 본질 한계 영속) + stock_master 적재 영역 점진 증가 (114 → 시간 경과 더, 사이클 95 unknown 합집합 + 사이클 93 chain 영속 + 매일 자동 갱신 + 보유 종목 사전 갱신 등 다양한 영역) + `[stock_master_bulk_refresh] universe=60 kospi=30 kosdaq=30 unknown=0` 영속. **사이클 100+ 후속 카드 인계 (Plan 영속)**: Plan Phase A/B/C (VB/LTV/BFB + donchian/VCP stock_master 베이스 + UI 운영자 필터링) — stock_master 적재 영역 (114+ 점진 증가) 활용 영역 / 익일 (2026-06-11 목) 07:55 운영 측정 (사이클 92~99 통합 첫 운영) / 카드 #21 (LOW 사이클 74 flaky) / #16 / auth 1.43x. | `src/engine/scanner.py` (-53L 순감, 페이징 영역 전수 폐기 + 단일 호출 + docstring 명문화) / `tests/unit/engine/scanner/test_cycle99_pagination_purged.py` 신규 (G-PURGE1) / `tests/unit/engine/scanner/test_cycle99_60_ticker_persistence.py` 신규 (G-PERSIST1) / `tests/unit/ast/test_cycle99_default_arg_persistence.py` 신규 (G-DEFAULT1) / `tests/unit/ast/test_cycle99_kis_limit_documentation.py` 신규 (G-DOC1) / `tests/integration/test_cycle99_fluctuation_single_call.py` 신규 (G-REG1) / `tests/unit/engine/scanner/test_cycle97_*.py` 4 파일 (xfail 의미 전환 + top_n 갱신) / `_workspace/red/cycle99_pagination_removal_60_ticker_persistence.md` Red 명세 + KIS 본질 한계 명문화 + 운영 실증 인용 / `_workspace/cycle99_verify_report.md` verify 보고서 / `_workspace/test_index.yaml` 영향 인덱스 갱신 (backend tests 463 → 468) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 99 행 | 사용자 보고 (60 ticker + 3952ms 영속 페이징 시도) = KIS API 본질 한계 영구 확정 (volume_rank + fluctuation 모두 페이징 미지원). team-leader 진단 + 사용자 결정 (Q58=C 60 영구 영속 + Q59=A 페이징 영역 폐기) + tdd-engineer Red + backend-dev Green (페이징 영역 전수 폐기 + docstring 명문화) + tester verify. 36 사이클 연속 옵션 A 패턴 영속. 매매 안전성 무영향. 사이클 91/97 페이징 영역 영구 폐기 (xfail 3 의미 전환 영속). silent 결함 영구 차단 26 회 누적. 운영 효과: 60 ticker 영구 영속 + stock_master 적재 점진 증가 (114+). 사이클 100+ 후속 카드: Plan Phase A/B/C (stock_master 적재 영역 114+ 활용) / 익일 운영 측정 / 카드 #21 + #16 + auth 1.43x. |
| 2026-06-10 | 사이클 98 — **KIS fluctuation API `FID_RANK_SORT_CLS_CODE` 1줄 silent 결함** 영구 시정 (사이클 97 도입 결함, 0 → 500 ticker 영속 회복)** (**35 사이클 연속 옵션 A 패턴 영속**). **사용자 보고**: 사이클 97 push 후 "지금 새로고침" 토스트 = `universe 0 ticker 즉시 적재 완료 (262ms)` 영속 = 사이클 97 시정 즉시 silent 결함. **결정적 발견 (Supabase MCP 운영 실증, 16:36 KST 사이클 97 push 직후)**: KIS 응답 `OPSQ2002: ERROR INVALID INPUT_FILED_SIZE [FID_RANK_SORT_CLS_CODE] [4]` = **단일 1줄 파라미터 값 길이 결함** (KIS 정본 `chk_fluctuation.py` main 호출 영역 = `"0"` 1자리 vs 사이클 97 = `"0000"` 4자리). **사이클 97 backend-dev 결함**: KIS docstring 영역 (`"0000": 등락률순` 거짓 안내) 인용 → KIS chk_fluctuation.py main 호출 영역 정본 실측 `"0"` 미인용 → 회귀 가드 영역도 `"0000"` 영구 검증 → 운영 영역 즉시 KIS 거부. 사용자 결정 (Q55=A 1줄 시정 + AST 영구 가드 + Q57=A 진단 직후 push NXT 애프터). **시정 (production 1 파일)**: `src/engine/scanner.py` (+4L) — L1531 `"FID_RANK_SORT_CLS_CODE": "0000"` → `"0"` 1줄 시정 + L1497~L1499 docstring 동행 시정 (chk_fluctuation.py 정본 인용 영역 명시, G-DOC1 영구 가드 영역). **회귀 가드 9 sub-case (5 매트릭스)**: HIGH 3 (G-FIX1 params `FID_RANK_SORT_CLS_CODE == "0"` 1자리 + G-AST1 scanner.py source `"0"` 영속 + 다자리 영구 차단 AST + **G-DOC1 KIS chk_*.py 정본 인용 의무 영구 가드 신규 패턴 신설** ±10줄 윈도우) + MEDIUM 1 (G-INT1 a 정상 응답 + b OPSQ2002 graceful 양쪽 통합 시나리오) + LOW 1 (G-REG1 사이클 97 H-3 영역 회귀 가드 갱신). **백엔드 2294 → 2300 PASS** (+6 신규) + 2 skip + 53 XFAIL + 1 xpass / **프론트 245 PASS** 영속 / **합계 2545 PASS** / **flakiness 0** (3 회 0.04s 동일) / 회귀 0. **영속 의무 매트릭스 9 영역 전수 영속**: 사이클 97 영역 영속 (사이클 98 = 1줄 시정만) / 사이클 32 R4 / 38 명문화 / 64/65/81 graceful / 88 G-REJECT / 89 ETF + 거래대금 정렬 (fluctuation 흡수) / 91 페이징 (fluctuation 적용) / 93 chain / 95 unknown=0. **CLAUDE.md "절대 깨지 말 것" 8 영역 영속**. **매매 안전성 무영향** (scanner 단계 매수 진입 전 + 사이클 38 명문화 + production 4L 변경 + 매도/손절/Trailing/익일청산 hot path 무관). **G-DOC1 신규 패턴 (미래 silent 결함 영구 차단)**: KIS chk_*.py main 호출 영역 = 실측 영역 (운영 정합) / KIS *.py docstring 영역 = 참고 영역 (간헐적 거짓 안내 영역 확정 = 사이클 97). `FID_RANK_SORT_CLS_CODE` 영역 ±10줄 윈도우 내 chk 인용 동행 의무. **silent 결함 영구 차단 25 회 누적** (사이클 91 + 94 + 96 + 97 + 98 통합). **사이클 49→98 누적 53 사이클 + hotfix 14 영역**. **운영 효과 (push + EC2 자동 배포 후)**: 사용자 UI "지금 새로고침" 클릭 → KIS fluctuation API 정상 응답 (OPSQ2002 영구 차단) → **500 ticker 정상 적재 (0 → 500)** + `[stock_master_bulk_refresh] universe=500 kospi=250 kosdaq=250 unknown=0` 영속. **사이클 99+ 후속 카드 인계 (Plan 영속)**: Plan Phase A/B/C (VB/LTV/BFB + donchian/VCP stock_master 베이스 + UI 운영자 필터링) / 익일 (2026-06-11 목) 07:55 운영 측정 (사이클 92~98 통합 첫 운영) / 카드 #21 (LOW 사이클 74 flaky) / #16 / auth 1.43x. | `src/engine/scanner.py` (+4L 1줄 시정 + docstring chk 인용) / `tests/unit/engine/scanner/test_cycle98_rank_sort_cls_code_fix.py` 신규 (G-FIX1) / `tests/unit/ast/test_cycle98_ast_rank_sort_cls_code.py` 신규 (G-AST1) / `tests/unit/ast/test_cycle98_ast_chk_citation_required.py` 신규 (G-DOC1 영구 가드 패턴 신설) / `tests/integration/test_cycle98_fluctuation_kis_api.py` 신규 (G-INT1 a + b 통합) / `tests/unit/engine/scanner/test_cycle97_fid_input_cnt_1_control.py` (G-REG1 갱신) / `_workspace/red/cycle98_fid_rank_sort_cls_code_fix.md` Red 명세 + KIS MCP 정본 + 운영 실증 OPSQ2002 인용 / `_workspace/cycle98_phase1_diagnosis.md` Phase 1 진단 / `_workspace/cycle98_verify_report.md` verify 보고서 / `_workspace/test_index.yaml` 영향 인덱스 갱신 (backend tests 459 → 463) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 98 행 | 사용자 보고 (0 ticker + 262ms 영역) = 사이클 97 즉시 silent 결함. team-leader Phase 1 진단 (Supabase MCP 운영 실증 OPSQ2002 직접 발견) + KIS MCP 정본 재검증 (chk_fluctuation.py main vs docstring 영역 차이점 확정) + tdd-engineer Red + backend-dev Green (1줄 시정) + tester verify. 35 사이클 연속 옵션 A 패턴 영속. 매매 안전성 무영향. G-DOC1 영구 가드 패턴 신설 = 미래 backend-dev docstring 거짓 안내 영역 인용 silent 결함 영구 차단 + KIS chk_*.py main 호출 영역 인용 의무 영구 가드. silent 결함 영구 차단 25 회 누적 (사이클 91+94+96+97+98 통합). 운영 효과: stock_master 0 → 500 ticker 정상 적재. 사이클 99+ 후속 카드: Plan Phase A/B/C / 익일 운영 측정 (사이클 92~98 통합) / 카드 #21 + #16 + auth 1.43x. |
| 2026-06-10 | 사이클 97 — **KIS volume_rank API 영구 폐기** + KIS fluctuation API (FHPST01700000) 영역 신규 도입 (사이클 89/91/94/96 영역 전수 폐기, 60 → 500 ticker 영속 회복)** (**34 사이클 연속 옵션 A 패턴 영속**). **사용자 보고**: 사이클 96 push 후 "지금 새로고침" 토스트 = `universe 60 ticker 즉시 적재 완료 (3438ms)` 영속 (KOSPI 30 + KOSDAQ 30 = 사이클 89 도입 시점 영역과 동일). **단일 근본 원인 영구 확정 (운영 실증 30분 7 사이클 + Supabase MCP)**: KIS `volume_rank` API (FHPST01710000) = **단일 페이지 30 ticker 한도 + `tr_cont` 헤더 "M" 영구 비반환 = 페이징 자체 미지원**. 사이클 89/91/94/96 모든 시정 = **KIS API 영역 자체 한도 영구 무용 확정**. **결정적 대안 (KIS MCP 정본 재검증)**: KIS `fluctuation` API (FHPST01700000) = **`fid_input_cnt_1` 사용자 제어 + `tr_cont` 페이징 영속** + 응답 키 `stck_shrn_iscd` (volume_rank `mksc_shrn_iscd` 와 영역 차별). 사용자 결정 (Q52=A KIS fluctuation 영역 도입 + Q53=A 사이클 89/91/94/96 전수 폐기 + Q54=A 진단 완료 직후 push KRX 메인 시간). **시정 (production 1 파일 영구 시정)**: `src/engine/scanner.py` (+113/-106 = 순 +7L) — `_FLUCTUATION_URL = "/uapi/domestic-stock/v1/ranking/fluctuation"` + `_FLUCTUATION_TR_ID = "FHPST01700000"` + `_FLUCTUATION_MARKET_INPUT_ISCD = {"kospi": "0001", "kosdaq": "0002"}` 신규 + `_fetch_fluctuation(market, top_n=250, max_pages=17)` 신규 함수 (KIS 정본 영역 정합 + 페이징 `tr_cont == "M" → "N"` 영속 + Rate Limit 50ms sleep 사이클 83 답습 + graceful KIS error 사이클 91 답습) + `fetch_top_500_universe()` 2회 분리 호출 (KOSPI + KOSDAQ) → `_fetch_fluctuation` 전환 + 응답 키 `mksc_shrn_iscd` → `stck_shrn_iscd` (KIS 정본 영역 차별) + 사이클 89 ETF 제외 + 거래대금 정렬 영속 + 사이클 95 unknown=0 영속 + `[stock_master_bulk_refresh] universe=N kospi=K kosdaq=L unknown=0 securities=M etf_excluded=E` emit 영속 + `import asyncio as _asyncio` 모듈 레벨 + `_VOLUME_RANK_URL` / `_VOLUME_RANK_TR_ID` / `_fetch_volume_rank` / `_MARKET_INPUT_ISCD` (사이클 96 도입) 전수 폐기. **xfail 의미 전환 53건 (사이클 66 K-2 패턴 답습)**: 사이클 89 (이미 xfail 영속) + 사이클 91 (5 신규 + 2 영속) + 사이클 94 (이미 xfail 영속) + 사이클 96 (8 신규) = 사이클 89/91/94/96 영역 전수 폐기 영구 보존. **회귀 가드 27 sub-case (10 매트릭스)**: HIGH 5 (H-1 `_FLUCTUATION_URL`/`_TR_ID` AST 3 sub + H-2 fluctuation 페이징 누적 500 4 sub + H-3 `fid_input_cnt_1` 사용자 제어 2 sub + H-4 `_VOLUME_RANK_*` 영역 영구 폐기 AST + H-5 AST `volume-rank` URL + `FHPST01710000` TR_ID 영구 차단) + MEDIUM 3 (M-1 emit 영속 6 sub + M-2 Rate Limit 50ms + 2회 호출 5 sub + M-3 `_scanner_upsert_loop` chain 5 sub) + LOW 2 (L-1 사이클 88 G-REJECT 3 sub + L-2 KST 2 sub). **백엔드 2300 → 2294 PASS** (+~ 신규, xfail 의미 전환 흡수 영역) + 2 skip + 53 XFAIL + 1 xpass / **프론트 245 PASS** 영속 / **합계 2539 PASS** / **flakiness 0** (백 3 회 49.24~49.47s 동일 카운트) / 회귀 0. **영속 의무 매트릭스 10 영역 전수 영속**: 사이클 32 R4 / 38 명문화 / 64/65/81 graceful / 88 G-REJECT (L-1 가드 흡수 + 확장) / 89 ETF 제외 + 거래대금 정렬 (fluctuation 영역 흡수) / 91 페이징 (fluctuation 적용) / 93 chain / 95 unknown=0 영속 / 96 KOSPI/KOSDAQ 분리 호출 (fluctuation 영역 흡수) / KIS LMS chain 차단 (사이클 17/29 + 사이클 18 dedupe + 사이클 76 collector + Rate Limit 50ms). **CLAUDE.md "절대 깨지 말 것" 8 영역 영속**. **매매 안전성 무영향** (scanner 단계 universe 적재 + 매수 진입 전 + 사이클 38 명문화 + 매도/손절/Trailing/익일청산 hot path 무관 + `risk.py` / `order_engine.py` / `scheduler.py` 변경 0). **silent 결함 영구 차단 24 회 누적** (사이클 91+94+96+97 통합 시정). **사이클 49→97 누적 52 사이클 + hotfix 14 영역**. **운영 효과 (push + EC2 자동 배포 후)**: 사용자 UI "지금 새로고침" 클릭 → KIS fluctuation API 2회 분리 호출 (KOSPI + KOSDAQ) × 각 17 페이지 페이징 누적 + 50ms sleep = ~1.7s × 2 = ~3.4s 소요 → **500 ticker 정상 적재 (60 → 500)** + `[stock_master_bulk_refresh] universe=500 kospi=250 kosdaq=250 unknown=0` 영속 + Q9 정상 발화. **사이클 98+ 후속 카드 인계 (Plan 영속)**: 사이클 95 Plan Phase A/B (VB/LTV/BFB + donchian/VCP stock_master 베이스 전환) + 사이클 96+ Phase C (UI 운영자 필터링) / 익일 (2026-06-11 목) 07:55 운영 측정 의무 (사이클 92~97 통합 첫 운영) / 카드 #21 (LOW 사이클 74 flaky) / #16 / auth 1.43x / #15 / #11/#12. | `src/engine/scanner.py` (+113/-106 = 순 +7L, KIS fluctuation 영역 신규 + KIS volume_rank 영역 영구 폐기) / `tests/unit/engine/scanner/test_cycle97_fluctuation_url_tr_id.py` 신규 (H-1) / `tests/unit/engine/scanner/test_cycle97_fluctuation_pagination_500.py` 신규 (H-2) / `tests/unit/engine/scanner/test_cycle97_fid_input_cnt_1_control.py` 신규 (H-3) / `tests/unit/engine/scanner/test_cycle97_volume_rank_deprecated.py` 신규 (H-4) / `tests/unit/ast/test_cycle97_ast_no_volume_rank.py` 신규 (H-5) / `tests/unit/engine/scanner/test_cycle97_emit_visibility_fluctuation.py` 신규 (M-1) / `tests/unit/engine/scanner/test_cycle97_rate_limit_2call.py` 신규 (M-2) / `tests/unit/engine/scanner/test_cycle97_scanner_upsert_chain.py` 신규 (M-3) / `tests/unit/engine/scanner/test_cycle97_g_reject_persistence.py` 신규 (L-1) / `tests/unit/engine/scanner/test_cycle97_kst_persistence.py` 신규 (L-2) / `tests/unit/engine/scanner/test_cycle91_*.py` 5 파일 + `tests/unit/ast/test_cycle91_ast_pagination_pattern.py` (xfail 의미 전환) / `tests/unit/engine/scanner/test_cycle94_*.py` 2 파일 (xfail 추가) / `tests/unit/engine/scanner/test_cycle95_*.py` 3 파일 (xfail 추가) / `tests/unit/engine/scanner/test_cycle96_*.py` 7 파일 + `tests/unit/ast/test_cycle96_ast_no_zero_market_code.py` (xfail 의미 전환) / `_workspace/red/cycle97_fluctuation_api_replacement.md` Red 명세 + KIS MCP 정본 인용 / `_workspace/cycle97_phase1_diagnosis.md` Phase 1 진단 영구 기록 / `_workspace/cycle97_verify_report.md` verify 보고서 영구 기록 / `_workspace/test_index.yaml` 영향 인덱스 갱신 (backend tests 449 → 459) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 97 행 | 사용자 보고 (60 ticker 영속 + 3438ms 소요 영역) = 사이클 91 페이징 영역 코드 정합 영속 + KIS API 영역 자체 한도 영구 확정. 단일 근본 원인 = KIS `volume_rank` 페이징 미지원 영역 (운영 실증 30분 7 사이클 + Supabase MCP). 결정적 대안 = KIS `fluctuation` API (`fid_input_cnt_1` 사용자 제어 + `tr_cont` 페이징 영속). 34 사이클 연속 옵션 A 패턴 영속. team-leader Phase 1 진단 (KIS MCP 정본 재검증 결정적 발견) + tdd-engineer Red + backend-dev Green + tester verify. 매매 안전성 무영향 (scanner 단계 + 사이클 38 명문화). 사이클 89/91/94/96 영역 전수 폐기 (xfail 53 의미 전환 영속) + 사이클 95 unknown=0 영속 흡수. KIS LMS chain 차단 영속. silent 결함 영구 차단 24 회 누적. 운영 효과: stock_master 60 → 500 ticker 정상 적재 (~3.4s 소요). 사이클 98+ 후속 카드: Plan Phase A/B/C / 익일 운영 측정 / 카드 #21 + #16 + auth 1.43x + #15 + #11/#12. |
| 2026-06-10 | 사이클 96 — **KIS API "0000" 응답 단일 페이지 30 한도** + 페이징 미지원 silent 결함 영구 시정 (사이클 94 도입 결함, 사이클 89 영역 복원 + 사이클 91 페이징 결합)** (**33 사이클 연속 옵션 A 패턴 영속**). **사용자 보고**: 사이클 95 시정 후에도 "지금 새로고침" 토스트 = `universe 29 ticker 적재 완료` 영속 (stock_master 80 영속, 운영 실증 30분간 7 사이클 emit = `universe=29 kospi=23~24 kosdaq=5~6 unknown=0 etf_excluded=1`). **단일 근본 원인 (가설 A+E 통합 확정)**: 사이클 94 commit `542d3e5` 가 `_MARKET_INPUT_ISCD = {"all": "0000"}` 단일화 영역으로 시정했으나, **KIS API `volume_rank` (FHPST01710000) "0000" 전체 호출 = 응답 단일 페이지 30 한도 + `tr_cont` 헤더 "M" 비반환 = 페이징 자체 영역 미지원**. 결과 = 단일 페이지 30 ticker 영속. 사이클 91 페이징 코드 (commit `9a0e96a`) 정합 영속 + 사이클 94 `_response_headers["tr_cont"]` 추출 정합 영속이나 = KIS API 영역 자체 한도. **사이클 89 영역 (`"0001"` KOSPI 업종 + `"0002"` KOSDAQ 업종 분리 호출) = KIS 정본 정합** (chk_volume_rank.py 영역). 사용자 결정 (Q50=D 사이클 89 영역 복원 + 사이클 91 페이징 결합 + Q51=A 진단 완료 직후 push KRX 메인 시간). **시정 (production 1 파일)**: `src/engine/scanner.py` (-5L 순감) — `_MARKET_INPUT_ISCD = {"kospi": "0001", "kosdaq": "0002"}` 복원 (사이클 89 영속) + `_fetch_volume_rank(market, top_n=250, max_pages=17)` 시그너처 (사이클 91 페이징 영속) + `fetch_top_500_universe()` 2회 분리 호출 (KOSPI + KOSDAQ) + post-split chain (`stock_master.get` + `_classify_market`) 폐기 + unknown=0 영속 명시 + `[stock_master_bulk_refresh] universe=N kospi=K kosdaq=L unknown=0 securities=M etf_excluded=E` emit 영속 + `from src.db import stock_master as _sm_mod` 불필요 import 제거. **xfail 의미 전환 20건 (사이클 66 K-2 패턴 답습)**: Red 명세 6건 + 폐기 계약 적응 14건 (사이클 94 도입 영역 전수 폐기 영속). **회귀 가드 21 sub-case (10 매트릭스)**: HIGH 5 (H-1 `_MARKET_INPUT_ISCD` 복원 3 sub + H-2 2회 호출 + 페이징 누적 ≥ 500 2 sub + H-3 post-split chain 폐기 2 sub + H-4 KOSPI/KOSDAQ 2회 분리 호출 2 sub + H-5 AST `"0000"` 영구 차단 3 sub) + MEDIUM 3 (M-1 emit unknown=0 + M-2 Rate Limit 50ms 2 sub + M-3 chain 영속 2 sub) + LOW 2 (L-1 사이클 88 G-REJECT 2 sub + L-2 KST 2 sub). **백엔드 2293 → 2300 PASS** (+7 신규) + 2 skip + 20 xfail (+13 의미 전환) + 1 xpass / **프론트 245 PASS** 영속 / **합계 2545 PASS** / **flakiness 0** (백 3 회 50.59~51.11s 동일 카운트) / 회귀 0. **영속 의무 매트릭스 9 영역 전수 영속**: 사이클 32 R4 / 38 명문화 (scanner 매수 진입 전용) / 64/65/81 graceful / 88 G-REJECT-1/2/3 (L-1 PASS) / 89 영역 복원 (`"0001"`/`"0002"` + ETF 제외 + 거래대금 정렬) / 91 페이징 (각 업종별 17 페이지) / 93 chain / 94 폐기 (xfail 20 의미 전환 영속) / 95 unknown=0 영속 (graceful 영역). **CLAUDE.md "절대 깨지 말 것" 8 영역 영속**. **매매 안전성 무영향** (scanner 단계 universe 적재 + 매수 진입 전 + 사이클 38 명문화 + 매도/손절/Trailing/익일청산 hot path 무관). **KIS LMS chain 차단 영속** (사이클 17 OPSP0002 backoff + 사이클 29 005935 패턴 + 사이클 18 60s WARNING dedupe 영역 + 사이클 76 collector + Rate Limit 50ms 영속). **silent 결함 영구 차단 23 회 누적** (사이클 91 + 94 + 96 통합 시정). **사이클 49→96 누적 51 사이클 + hotfix 14 영역**. **운영 효과 (push + EC2 자동 배포 후)**: 사용자 UI "지금 새로고침" 클릭 → 2회 분리 호출 (KOSPI 250 + KOSDAQ 250) × 각 17 페이지 페이징 누적 = ~34 KIS 호출 × 50ms sleep = ~1.7s 소요 → **500 ticker 정상 적재 (29 → 500)** + `[stock_master_bulk_refresh] universe=500 kospi=250 kosdaq=250 unknown=0` 영속 + Q9 정상 발화. **사이클 97+ 후속 카드 인계 (Plan 영속)**: 사이클 95 Plan Phase A/B (VB/LTV/BFB + donchian/VCP stock_master 베이스 전환) + 사이클 96+ Phase C (UI 운영자 필터링) / 익일 (2026-06-11 목) 07:55 운영 측정 의무 (사이클 92+93+94+95+96 통합 첫 운영) / 카드 #21 (LOW 사이클 74 flaky) / #16 / auth 1.43x / #15 / #11/#12. | `src/engine/scanner.py` (-5L 순감 = +55/-60 영역 복원 + 페이징 결합 + post-split 폐기) / `tests/unit/engine/scanner/test_cycle96_market_input_iscd_restored.py` 신규 (H-1) / `tests/unit/engine/scanner/test_cycle96_two_call_pagination_500.py` 신규 (H-2) / `tests/unit/engine/scanner/test_cycle96_post_split_removed.py` 신규 (H-3) / `tests/unit/engine/scanner/test_cycle96_kospi_kosdaq_separation_2call.py` 신규 (H-4) / `tests/unit/ast/test_cycle96_ast_no_zero_market_code.py` 신규 (H-5) / `tests/unit/engine/scanner/test_cycle96_emit_visibility_2call.py` 신규 (M-1) / `tests/unit/engine/scanner/test_cycle96_rate_limit_2call.py` 신규 (M-2) / `tests/unit/engine/scanner/test_cycle96_scanner_upsert_chain.py` 신규 (M-3) / `tests/unit/engine/scanner/test_cycle96_g_reject_persistence.py` 신규 (L-1) / `tests/unit/engine/scanner/test_cycle96_kst_persistence.py` 신규 (L-2) / `tests/unit/engine/scanner/test_cycle94_*.py` 5 파일 (xfail 의미 전환) + `tests/unit/ast/test_cycle94_ast_no_industry_code.py` (xfail) / `tests/unit/engine/scanner/test_cycle95_*.py` 4 파일 (xfail 의미 전환) / `_workspace/red/cycle96_resurrect_industry_code_with_pagination.md` Red 명세 영구 기록 / `_workspace/cycle96_phase1_diagnosis.md` Phase 1 진단 영구 기록 / `_workspace/test_index.yaml` 영향 인덱스 갱신 (backend tests 439 → 449) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 96 행 | 사용자 보고 (29 ticker 영속) 즉시 시정 (사이클 94 도입 KIS API "0000" 단일 페이지 한도 + 페이징 미지원 silent 결함 단일 근본 원인 확정). 33 사이클 연속 옵션 A 패턴 영속. team-leader Phase 1 진단 (KIS MCP 정본 + 운영 실증 30분 7 사이클) + 사용자 결정 + tdd-engineer Red + backend-dev Green + tester verify. 매매 안전성 무영향 (scanner 단계 + 사이클 38 명문화 + KIS LMS chain 차단). 사이클 89 영역 복원 (정본 정합) + 사이클 91 페이징 결합 (각 업종 17 페이지) = KIS 호출 ~34 + 50ms sleep = ~1.7s 소요. 사이클 94 폐기 영역 20 xfail 의미 전환 영속 (사이클 66 K-2 패턴 답습). 운영 효과: stock_master 29 → 500 ticker 정상 적재. 사이클 97+ 후속 카드: Plan Phase A/B/C / 익일 운영 측정 (사이클 92+93+94+95+96 통합) / 카드 #21 + #16 + auth 1.43x + #15 + #11/#12. |
| 2026-06-10 | 사이클 95 — **chicken-and-egg lock-in 영구 시정** (사이클 94 도입 결함) + UI 전일종가 컬럼 + 시장 한글 변환** (**32 사이클 연속 옵션 A 패턴 영속**). **사용자 보고**: 사이클 94 시정 push 완료 + 재기동 + 강제 적재 후에도 stock_master 78 ticker 영속. **결정적 진단 (단일 근본 원인)**: 사이클 94 도입 `_classify_market(sm_data)` 가 stock_master 부재 종목 = `None` 반환 → `fetch_top_500_universe()` post-split 영역에서 kospi/kosdaq 둘 다 미진입 = **graceful skip lock-in**. 결과 = KIS 응답 500 ticker 중 stock_master 적재된 78 ticker 만 분류 통과 + 422 ticker 영구 누락 → 다음 사이클 호출 시 다시 동일 78 만 통과 → 새 종목 영원히 stock_master 진입 불가 (**chicken-and-egg**). 사용자 결정 (Q43=1 unknown 합집합 (KIS 호출 0 증가) + Q44=A 3 영역 통합 단일 사이클 + Q45=A 진단 완료 직후 push). **시정 (production 2 파일)**: (1) `src/engine/scanner.py` (+9L) — `unknown: list[dict]` 신규 + `_classify_market` None 분기 = `unknown.append(row)` (사이클 89 `continue` 폐기) + `remaining = max(0, 500 - len(kospi_sorted) - len(kosdaq_sorted))` cap + `unknown_sorted = sorted(unknown, key=_trade_amount_key, reverse=True)[:remaining]` + `universe_rows = kospi_sorted + kosdaq_sorted + unknown_sorted` (500 cap 영속) + `[stock_master_bulk_refresh] universe=N kospi=K kosdaq=L unknown=U` emit 영역 확장 (운영 가시화) + `record_universe_refresh({"unknown": len(unknown_sorted), ...})` collector 키 추가. (2) `frontend/src/pages/StockMaster.tsx` (+9L) — `formatExchange` 헬퍼 정정 (`'02': 'KOSPI'` + `'03': 'KOSDAQ'` KIS CTPF1002R 정본 일치 + STK/KSQ backward compat) + list `<thead>` 전일종가 컬럼 추가 (시장 컬럼 다음) + `<tbody>` `formatExchange(item.excg_dvsn_cd)` 1줄 교체 + `formatPrice(item.raw?.bfdy_clpr)` 전일종가 셀 추가. **xfail 1 신규 (사이클 66 K-2 의미 전환)**: `test_cycle94_graceful_market_id_missing::test_m3` (사이클 94 "보수적 skip" 의도 → 사이클 95 unknown 합집합 의미 전환). **회귀 가드 25 케이스 (10 매트릭스)**: HIGH 5 (H-1 unknown 분기 2 sub + H-2 universe 500 lock-in 차단 3 sub + H-3 `_classify_market` graceful 3 sub + H-4 list 테이블 전일종가 컬럼 + formatPrice 3 sub + H-5 시장 한글 변환 2 sub) + MEDIUM 3 (M-1 `[stock_master_bulk_refresh] unknown=N` emit 2 sub + M-2 `_scanner_upsert_loop` chain 영속 3 sub + M-3 formatExchange 헬퍼 확장 2 sub) + LOW 2 (L-1 사이클 88 G-REJECT 영속 2 sub + L-2 KST 영속 3 sub). **백엔드 2276 → 2293 PASS** (+17 신규) + 2 skip + 6→7 xfail (+1 의미 전환) / **프론트 238 → 245 PASS** (+7 신규) / **합계 2538 PASS** / **flakiness 0** (백 3 회 56.47~64.02s 동일 카운트) / 회귀 0. **영속 의무 매트릭스 8 영역 전수 영속**: 사이클 88 G-REJECT-1/2/3 / 사이클 89 KOSPI 250 + KOSDAQ 250 cap (remaining 500 초과 차단) / 사이클 94 영역 1/2 (`"0000"` 단일화 + `_fetch_volume_rank` 단일 호출 + max_pages=17) / 사이클 93 `_scanner_upsert_loop` chain / 사이클 68 KST / 사이클 75 G-RT retry:1 / 사이클 80 hotfix #3 LIFO / 사이클 85 G-KST / H-POLLING / H-DETAIL 41 케이스. **CLAUDE.md "절대 깨지 말 것" 8 영역 영속**. **매매 안전성 무영향** (scanner 단계 매수 진입 전 + 사이클 38 명문화 영속 + KIS 호출 0 증가 + 매도/손절/Trailing/익일청산 hot path 무관). **chicken-and-egg lock-in 영구 차단**: 첫 호출에서 ~500 ticker upsert chain trigger + 다음 사이클부터 KOSPI/KOSDAQ 분류 자연 회복. **사이클 49→95 누적 50 사이클 + hotfix 14 영역**. **운영 효과 (push + EC2 자동 배포 후)**: 사용자 UI "지금 새로고침" 클릭 → 첫 적재 = unknown 영역 합집합 500 ticker → upsert chain → 다음 사이클부터 stock_master 적재 영역 확장 + KOSPI/KOSDAQ 분류 정확도 자연 회복 + UI 종목마스터 list 테이블 전일종가 컬럼 + 시장 KOSPI/KOSDAQ 한글 표기. **사이클 96+ 후속 카드 인계**: 사이클 95 Phase A/B (VB/LTV/BFB + donchian/VCP stock_master 베이스 전환, Plan 영속) 별개 사이클 / 사이클 96+ Phase C (UI 운영자 필터링 기능) / 익일 (2026-06-11 목) 07:55 운영 측정 (사이클 92+93+94+95 통합 효과) / 카드 #21 (LOW 사이클 74 flaky) / #16 / auth 1.43x / #15 / #11/#12. | `src/engine/scanner.py` (+9L unknown 영역 + emit 확장) / `frontend/src/pages/StockMaster.tsx` (+9L formatExchange 정정 + 전일종가 컬럼) / `frontend/src/pages/__tests__/StockMaster.test.tsx` (+7 H-4/H-5/M-3 sub-case) / `tests/unit/engine/scanner/test_cycle95_unknown_split.py` 신규 (H-1) / `tests/unit/engine/scanner/test_cycle95_universe_500_lock_in_break.py` 신규 (H-2) / `tests/unit/engine/scanner/test_cycle95_classify_market_graceful.py` 신규 (H-3) / `tests/unit/engine/scanner/test_cycle95_emit_visibility_unknown.py` 신규 (M-1) / `tests/unit/engine/scanner/test_cycle95_chain_with_unknown.py` 신규 (M-2) / `tests/unit/engine/scanner/test_cycle95_g_reject_persistence.py` 신규 (L-1) / `tests/unit/engine/scanner/test_cycle95_kst_persistence.py` 신규 (L-2) / `tests/unit/engine/scanner/test_cycle94_graceful_market_id_missing.py` (test_m3 xfail 의미 전환) / `_workspace/red/cycle95_chicken_and_egg_fix_ui.md` Red 명세 영구 기록 / `_workspace/cycle95_verify_report.md` verify 보고서 영구 기록 / `_workspace/test_index.yaml` 영향 인덱스 갱신 (backend tests 432 → 439) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 95 행 | 사용자 보고 (78 ticker 영속) 즉시 시정 (사이클 94 chicken-and-egg lock-in 단일 근본 원인 확정). 32 사이클 연속 옵션 A 패턴 영속. team-leader Phase 1 진단 + 사용자 결정 + tdd-engineer Red + backend-dev + frontend-dev Green 병렬 + tester verify. 매매 안전성 무영향 (scanner 단계 + 사이클 38 명문화 + KIS 호출 0 증가). chicken-and-egg lock-in 영구 차단 (첫 호출 upsert chain → 다음 사이클 자연 회복). UI 전일종가 + 시장 한글 변환 (사용자 가독성). 사이클 89 KOSPI/KOSDAQ cap 영속 (remaining 500 초과 차단). 사이클 96+ 후속 카드: Phase A/B (Plan 영속) / Phase C (UI 운영자 필터링) / 익일 운영 측정 / 카드 #21 + #16 + auth 1.43x + #15 + #11/#12. |
| 2026-06-10 | 사이클 94 — **FID_INPUT_ISCD 60 ticker silent 결함 영구 시정** (사이클 89/91 도입 시점 결함) + UI 안내 가이드 배너 (사용자 혼동 영역 해소)** (**31 사이클 연속 옵션 A 패턴 영속**). **사용자 보고**: 사이클 91 (페이징 누적) + 사이클 93 (호출 chain) 시정 push 완료 후에도 UI "지금 새로고침" 토스트 = `universe 60 ticker 즉시 적재 완료` 영속 → `fetch_top_500_universe()` 자체가 60 반환. **단일 근본 원인 (KIS MCP 정본 재검증 결정적 발견)**: KIS 정본 `FID_INPUT_ISCD = "0000"` (전체) — `examples_llm/domestic_stock/volume_rank/volume_rank.py` 영역. 우리 (사이클 89/91) = `"0001"` (KOSPI 업종) / `"0002"` (KOSDAQ 업종). KIS docstring: `"0000": 전체, 기타: 업종코드`. **업종코드 응답 = 단일 페이지 30건 한도** = KOSPI 30 + KOSDAQ 30 = **60 ticker 영속**. 사용자 결정 (Q37=C `_MARKET_INPUT_ISCD = "0000"` 단일화 + post-split + Q38=A UI 안내 + Q39=C 분할 단순 시정 사이클 94 + 아키텍처 재정의 사이클 95+ 분리 + Q40=A 전체 KRX ~2,400 적재 영속 사이클 95+ 영역 + Q41=A domain-expert 자문 사이클 95+ + **Q42=A `stock_master.excg_dvsn_cd` 컬럼 join post-split**). **시정 (production 3 파일)**: (1) `src/engine/scanner.py` — `_MARKET_INPUT_ISCD = {"all": "0000"}` 단일화 + `_classify_market(sm_data)` 헬퍼 신규 (`excg_dvsn_cd "02" = KOSPI / "03" = KOSDAQ / 기타 = None graceful`) + `_fetch_volume_rank(market="all", top_n=500, max_pages=17)` 시그너처 변경 + `fetch_top_500_universe()` 단일 호출 + post-split 재작성 (KOSPI 250 + KOSDAQ 250 = 500). (2) `src/models/stock.py` — `StockBasics.nxt_tradable` required → default False (보수적 KRX fallback). (3) `frontend/src/pages/StockMaster.tsx` — `stock-master-info-banner` UI 안내 배너 추가 (참고용 종목 마스터 + 실시간 매매 신호와 별개 영역 명시). **회귀 가드 19 케이스 (12 케이스 + sub-case)**: HIGH 6 (H-1 FID_INPUT_ISCD "0000" + H-2 페이징 17 페이지 500 ticker + H-3 post-split `_classify_market` + H-4 KOSPI 250 + KOSDAQ 250 split + H-5 UI 배너 4 sub + H-6 AST 업종코드 "0001"/"0002" 영구 차단 3 sub) + MEDIUM 4 (M-1 emit 가시화 + M-2 ETF 제외 500 + M-3 graceful market_id 부재 + M-4 scanner upsert chain 3 sub) + LOW 2 (L-1 KST 3 sub + L-2 G-REJECT 2 sub). **xfail 4 신규 (사이클 66 K-2 의미 전환 패턴 답습)**: `test_cycle89_fetch_top_500_universe::test_h1` + `test_cycle89_kospi_kosdaq_separation::test_h3` (2회 분리 호출 계약 폐기) + `test_cycle91_volume_rank_max_pages_guard::test_h3` (max_pages=15 디폴트 폐기) + `test_cycle91_fetch_top_500_universe_full::test_h4` (FID_INPUT_ISCD "0001"/"0002" 계약 폐기). **백엔드 2261 → 2276 PASS** (+15 신규) + 2 XFAIL → 6 XFAIL (+4 의미 전환) + 2 skip / **프론트 234 → 238 PASS** (+4 UI 배너 sub) / **합계 2514 PASS** / **flakiness 0** (백 3 회 56.20~61.91s 동일 카운트) / 회귀 0. **영속 의무 매트릭스 10 영역 전수 영속**: 사이클 32 R4 / 38 명문화 / 64/65/81 graceful / 88 G-REJECT-1/2/3 (graceful fallback 영속) / 89 KOSPI/KOSDAQ 분리 (post-split 영역으로 영속) / 90 L-2 + L-3 / 91 페이징 누적 (FID_INPUT_ISCD 단독 시정) / 92 자동 재기동 / 93 호출 chain (`_scanner_upsert_loop` 영속). **CLAUDE.md "절대 깨지 말 것" 8 영역 영속**. **매매 안전성 무영향** (scanner 단계 = 매수 진입 전 WS 구독 후보, 사이클 38 명문화 영속 + 매도/손절/Trailing/익일청산 hot path 무관 + nxt_tradable=False 기본 = KRX fallback 보수적 NXT 좀비 차단 영속). **silent 결함 영구 차단 22 회 누적** (사이클 91 영속). **사이클 49→94 누적 49 사이클 + hotfix 14 영역**. **운영 효과 (push + EC2 자동 배포 후)**: 사용자 UI "지금 새로고침" 클릭 → KIS volume_rank "0000" 17 페이지 누적 + post-split → **500 ticker 정상 적재** + `[stock_master_bulk_refresh] universe=500 kospi=250 kosdaq=250` 영속 + Q9 `[stock_master_bulk_refresh]` 정상 발화. **사이클 95+ 후속 카드 인계 (Plan 영속)**: **사이클 95 Phase A** VB/LTV/BFB stock_master 베이스 전환 (LOW 위험, 신호 무변화, KIS API 호출 100% 절감) / **사이클 95 Phase B** donchian/VCP 부분 전환 (MEDIUM 위험, 신호 -5~15% 감소, domain-expert 자문 의무) / **사이클 96+ Phase C** UI 운영자 필터링 기능 (종목 수동 제외/추가 + 일일 리포트, LOW 우선순위) / 익일 (2026-06-11 목) 07:55 운영 측정 (사이클 92 + 93 + 94 통합 효과) / 카드 #21 (LOW 사이클 74 flaky) / #16 / auth 1.43x / #15 / #11/#12. | `src/engine/scanner.py` (L1368~1559 `_MARKET_INPUT_ISCD` 단일화 + `_classify_market` 헬퍼 + `_fetch_volume_rank` 시그너처 + `fetch_top_500_universe` post-split 재작성) / `src/models/stock.py` (nxt_tradable default False 보수적) / `frontend/src/pages/StockMaster.tsx` (L463 UI 안내 배너 신규) / `frontend/src/pages/__tests__/StockMaster.test.tsx` (H-5 4 sub-case 신규) / `tests/unit/engine/scanner/test_cycle94_fid_input_iscd_fix.py` 신규 (H-1) / `tests/unit/engine/scanner/test_cycle94_volume_rank_pagination_500.py` 신규 (H-2) / `tests/unit/engine/scanner/test_cycle94_post_split_market_id.py` 신규 (H-3) / `tests/unit/engine/scanner/test_cycle94_kospi_kosdaq_split_500.py` 신규 (H-4) / `tests/unit/ast/test_cycle94_ast_no_industry_code.py` 신규 (H-6 3 sub) / `tests/unit/engine/scanner/test_cycle94_emit_visibility.py` 신규 (M-1) / `tests/unit/engine/scanner/test_cycle94_etf_exclusion_500.py` 신규 (M-2) / `tests/unit/engine/scanner/test_cycle94_graceful_market_id_missing.py` 신규 (M-3) / `tests/unit/engine/scanner/test_cycle94_scanner_upsert_chain.py` 신규 (M-4 3 sub) / `tests/unit/engine/scanner/test_cycle94_kst_persistence.py` 신규 (L-1 3 sub) / `tests/unit/engine/scanner/test_cycle94_g_reject_persistence.py` 신규 (L-2 2 sub) / `tests/unit/engine/scanner/test_cycle89_*.py` 2 파일 (xfail 마킹) / `tests/unit/engine/scanner/test_cycle91_*.py` 2 파일 (xfail 마킹) / `_workspace/red/cycle94_fid_input_iscd_fix.md` Red 명세 영구 기록 + KIS MCP 정본 인용 / `_workspace/test_index.yaml` 영향 인덱스 갱신 (backend tests 421 → 432) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 94 행 | 사용자 보고 (60 ticker 영속 결함) 즉시 시정 (사이클 91 페이징 시정 후에도 영속 = 별개 단일 근본 원인 KIS 정본 `FID_INPUT_ISCD = "0000"` 영역 결함 확정). 31 사이클 연속 옵션 A 패턴 영속. team-leader Phase 1 진단 (KIS MCP 정본 재검증) + Plan agent 결정적 발견 + Q42 신규 의제 (post-split 구현 영역) + domain-expert 자문 생략 (사이클 94 단순 시정 영역, 사이클 95+ 아키텍처 재정의 영역에서 자문 의무). 매매 안전성 무영향 (scanner 단계 + 매수 진입 전 + 사이클 38 명문화 영속 + nxt_tradable 보수적). 사이클 89/91 의미 전환 4 xfail = 사이클 66 K-2 패턴 답습 영속. 운영 효과: stock_master 60 → 500 ticker 정상 적재. 사이클 95+ 후속 카드 인계: Phase A (VB/LTV/BFB LOW 위험) + Phase B (donchian/VCP MEDIUM 위험 자문 의무) + Phase C (UI 운영자 필터링) / 익일 운영 측정 / 카드 #21 + #16 + auth 1.43x + #15 + #11/#12. |
| 2026-06-10 | 사이클 93 — **호출 chain broken silent 결함 영구 시정** (사이클 89 도입 시점 silent 결함, 사용자 보고 = stock_master 60 ticker 영속, **30 사이클 연속 옵션 A 패턴 영속**). **사용자 보고 chain**: 사이클 87 Phase 2 측정 (09:33 KST) 발견 Q3 `[scan_pool_eager_refresh]` 0건 + Q9 `[stock_master_bulk_refresh]` 0건 + Q11 stock_master 52→57 (+5) = `_boot` 단일 ticker 갱신만 정상 + 사용자 추가 보고 = stock_master 60 ticker 영속 유지. **결정적 진단 (가설 E 확정)**: `scanner.py:1616::_universe_eager_refresh_loop(candidates)` 모듈 함수 (stock_master upsert 담당, 24h TTL + 50ms sleep + `inquire_stock_basics` + `upsert_one`) = **`grep -rn` 결과 호출 0건**. 3 영역 모두 `fetch_top_500_universe()` 만 호출 + ticker list 받은 뒤 **버림** (`scheduler.py:2618` 개장 전 1회 + `scheduler.py:2630` 5분 주기 + `routes/stock_master.py:52` POST `/refresh-universe`). **사용자 결정 채택**: Q34=A 3 영역 통합 단일 사이클 + Q35=B 코드 정적 분석 + 사용자 EC2 SSH 수동 확인 + Q36=A **진단 완료 직후 push (KRX 메인 중)** (사용자 자율 결정, 매매 많지 않음). **시정 (production 2 파일)**: (1) `src/engine/scheduler.py` (+16L) — `_universe_eager_refresh_loop` self method 2 분기 (개장 전 + 5분 주기) `_scanner_upsert_loop = _universe_eager_refresh_loop` alias import + `await _scanner_upsert_loop(tickers)` 호출 추가 + try/except `logger.exception("[universe_eager_refresh] stock_master upsert 실패 graceful")`. (2) `src/routes/stock_master.py` (+7L) — POST `/refresh-universe` 라우트에 alias import + `await _scanner_upsert_loop(tickers)` 호출 + try/except pass (사이클 90 L-3 `[refresh_universe_now]` forbidden prefix + `logger.*` 직접 emit 0건 영속 동시 준수). **회귀 가드 5 케이스 (5 파일)**: HIGH 4 (G-CC1 scheduler 개장 전 1회 chain + G-CC2 scheduler 5분 주기 chain freezegun + G-CC3 POST 라우트 chain + G-CC4 graceful 영속 + ERROR 로그 영속) + LOW 1 (G-AST1 AST 영구 가드 = scheduler `_universe_eager_refresh_loop` 본체 + routes `refresh_universe_now` 본체 양쪽 `_scanner_upsert_loop` 호출 ≥1건 정적 검증, **미래 호출 chain broken silent 결함 영구 차단**). **백엔드 2255 → 2261 PASS** (+6 신규 = 5 케이스 + AST 2 sub) + 2 XFAIL + 2 skip / **프론트 234 PASS** 영속 / **합계 2495 PASS** / **flakiness 0** (백 3 회 53.51~67.56s ±26%, 일관 동일 결과) / 회귀 0. **영속 의무 매트릭스 9 영역 전수 영속**: 사이클 32 R4 universe guard / 사이클 38 명문화 / 사이클 88 G-REJECT-1/2/3 / 사이클 89 KOSPI/KOSDAQ 분리 + ETF 제외 / 사이클 90 L-2 화이트리스트 + L-3 forbidden prefix / 사이클 91 페이징 누적 / 사이클 92 자동 재기동 / WebSocket 4중 안전망 / KIS LMS chain 차단 (사이클 17 OPSP0002 backoff). **CLAUDE.md "절대 깨지 말 것" 8 영역 영속**. **매매 안전성 무영향** (stock_master 영역 + 사이클 32 R4 영속 + 매수/매도/익일청산/15:20 강제청산 hot path 무관). **silent 결함 영구 차단 22 회 누적** (사이클 60/64/65#1/65#2/65#3/66/67/68/72/73/77/77#2/78/79/80/80#2/80#3/80#4/81/83/91/**93**). **사이클 49→93 누적 48 사이클 + hotfix 14 영역**. **운영 효과 (push + EC2 자동 배포 후)**: stock_master `count_all` 60 → 점진 증가 (5분 주기 + 사용자 UI "지금 새로고침" 즉시 500+) + `[universe_eager_refresh] stock_master upsert 완료` INFO 정상 발화 + Q9 `[stock_master_bulk_refresh]` 0건 → ≥1건/분기 정상. **사이클 94+ 후속 카드 인계**: Q3 `[scan_pool_eager_refresh]` 사이클 83 영역 별개 정밀 검토 (`_scan_pool_eager_refresh_task` 호출 chain) / 사이클 87 Phase 3 측정 (10:00 자동 wakeup) / 익일 (2026-06-11 목) 07:55 운영 측정 (사이클 92 + 사이클 93 통합 효과) / 카드 #21 (LOW 사이클 74 flaky) / 사이클 96 외부 의견 재평가 / #16 / auth 1.43x / #15 / #11/#12. | `src/engine/scheduler.py` (+16L L2591~L2660 `_universe_eager_refresh_loop` 2 분기 chain + graceful) / `src/routes/stock_master.py` (+7L L34~L78 POST 라우트 chain + try/pass 사이클 90 L-3 영속) / `tests/unit/engine/test_cycle93_scheduler_upsert_chain.py` 신규 (G-CC1) / `tests/unit/engine/test_cycle93_scheduler_5min_upsert_chain.py` 신규 (G-CC2 freezegun) / `tests/unit/engine/test_cycle93_upsert_graceful.py` 신규 (G-CC4) / `tests/unit/routes/test_cycle93_route_upsert_chain.py` 신규 (G-CC3) / `tests/unit/ast/test_cycle93_ast_chain_required.py` 신규 (G-AST1 2 sub scheduler + route 양쪽) / `_workspace/red/cycle93_call_chain_broken.md` Red 명세 영구 기록 / `_workspace/cycle93_phase1_diagnosis.md` Phase 1 진단 영구 기록 / `_workspace/cycle87_dplus1_verification.md` 사이클 87 Phase 1/2 영구 기록 갱신 / `_workspace/test_index.yaml` 영향 인덱스 갱신 (backend tests 416 → 421) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 93 행 | 사용자 보고 stock_master 60 ticker 영속 결함 + 사이클 87 Phase 2 Q3/Q9 0건 silent 결함 = 단일 근본 원인 (가설 E 호출 chain broken) 영구 시정. 30 사이클 연속 옵션 A 패턴 영속. team-leader Phase 1 진단 (`_universe_eager_refresh_loop` 모듈 함수 호출 0건 grep 확정) + domain-expert 자문 생략 (사용자 즉시 시정 의무 + 단일 근본 원인 확정). 매매 안전성 무영향 (stock_master 영역 + 사이클 32 R4 영속 + 매매 hot path 무관). 사이클 89 도입 시점 silent 결함 영구 차단 = silent 결함 영구 차단 22 회 누적. AST G-AST1 = 미래 호출 chain broken silent 결함 영구 차단 패턴 신설. Q36=A 영속 = KRX 메인 중 즉시 push (사용자 자율 결정 + 매매 많지 않음). 운영 효과 즉시: stock_master 60 → 500+ 점진 증가 + 사용자 UI "지금 새로고침" 정상 작동. 사이클 94+ 후속 카드: Q3 사이클 83 영역 별개 정밀 검토 + Phase 3 10:00 자동 wakeup + 익일 07:55 운영 측정 + 카드 #21 (LOW) + 사이클 96 외부 의견 재평가 + #16 / auth 1.43x / #15 / #11/#12. |
| 2026-06-10 | 사이클 92 — **07:50 KIS API 강제 중단 충돌 영구 시정** (TIME_BOOT 07:55 + TIME_PRESUBSCRIBE 07:59 동행 이동 + MAX_RECONNECT 도달 시 자동 재기동 + cooldown/cap 영구 가드)** (사용자 운영 보고 = 장 초반 시세 미수신 + 전략 감시종목 미갱신 → 강제 재기동 후 정상, **29 사이클 연속 옵션 A 패턴 영속**). 사용자 보고 chain = 우리 매일 기동 07:45 vs KIS API 재기동 07:50 (모든 기존 접속 강제 중단) 정확히 동일 시점 충돌. **결함 chain 확정** (`src/realtime/websocket.py:30,174,207-210` 정밀 진단): `MAX_RECONNECT=5 + BACKOFF_BASE=1.0` = 1+2+4+8+16=31초 누적 → "최대 재연결 횟수 초과, 종료" → WS 영구 종료 → 4중 안전망 (F1 / `_scan_loop` / K stale watcher / `_resubscribe_stale_priority`) 모두 무력 (전제 `self._ws is not None` 불충족) → 사용자 수동 재기동만 유일한 회복 경로 = 사용자 보고 시나리오 100% 정확 일치. 사용자 결정 (Q28=E TIME_BOOT 07:50→07:55 + 자동 재기동 영구 가드 결합 + Q29=A TIME_AUTO_START 07:45 영속 + Q30=A MAX_RECONNECT 도달 시 `start()` idempotent + 60s cooldown + 시간당 3회 cap + Q31=C KIS 공식 명문 생략 + Q32=B TIME_PRESUBSCRIBE 07:55→07:59 동행 이동 domain-expert P1 권고 채택 + Q33=완료 즉시 push 매매 많지 않음 사용자 자율 결정). **domain-expert 자문 영속**: A1 `_boot()` 순차 영속 5분 마진 충분 / A2 idempotent + cooldown + cap 의무 / A3 MAX_RECONNECT=5 영속 (옵션 A/B 모두 LMS chain 직접 ↑ 위험) / A6 사이클 88 G-REJECT-1 위반 0 (자동 재기동 = 4중 안전망 *추가*, 대체 X). **시정 (production 2 파일)**: (1) `src/engine/scheduler.py` (+2L) — `TIME_BOOT = time(7, 55)` (KIS 07:50 강제 중단 후 5분 마진) + `TIME_PRESUBSCRIBE = time(7, 59)` (_boot 완료 후 1~2분 마진, race 회피). (2) `src/realtime/websocket.py` (+85L) — `import time as _time` (테스트 패치 정합, 기존 `time.monotonic/.time` 전체 동행 갱신) + 모듈 전역 상수 3 (`_AUTO_RESTART_COOLDOWN_SECS=60.0` + `_AUTO_RESTART_HOURLY_CAP=3` + `_AUTO_RESTART_WINDOW_SECS=3600.0`) + `__init__` 인스턴스 변수 2 (`_auto_restart_last_at` + `_auto_restart_history`) + `stop()` + `start()` 신규 메서드 (disconnect/connect 래퍼 + idempotent 진입점) + `_trigger_auto_restart()` 신규 (~50L, stop→start 순차 + 60s cooldown + 시간당 3회 cap + 1시간 슬라이딩 윈도우 prune + graceful) + `connect()` MAX_RECONNECT 분기에 `await self._trigger_auto_restart()` 1줄 추가. **운영 가시화 prefix 4 신설**: `[ws_auto_restart]` WARNING (재기동 발화) + `[ws_auto_restart_cooldown]` WARNING (60s 미경과) + `[ws_auto_restart_cap_exceeded]` ERROR (시간당 cap 도달, KIS LMS chain 차단) + `[ws_auto_restart_failed]` ERROR (재기동 자체 실패). **회귀 가드 14 케이스 (44 sub-case)**: HIGH 6 (H-1 TIME_BOOT/PRESUBSCRIBE AST + H-2 MAX_RECONNECT 도달 시 trigger + H-3 60s cooldown freezegun + H-4 시간당 3회 cap freezegun + H-5 idempotent (stop→start 순차) + H-6 사이클 88 G-REJECT-1/2/3 영속) + MEDIUM 5 (M-1 MAX_RECONNECT=5 영속 + M-2 graceful 실패 처리 + M-3 _boot/PRESUBSCRIBE race 차단 + M-4 토큰 24h 캐시 영속 + M-5 4중 안전망 영속) + LOW 3 (L-1 1시간 슬라이딩 윈도우 prune + L-2 TIME_AUTO_START 07:45 영속 + L-3 KST 영속). **백엔드 2211 → 2255 PASS** (+44 sub-case 신규) + 2 XFAIL + 2 skip / **프론트 234 PASS** 영속 / **합계 2489 PASS** / **flakiness 0** (백 3 회 ±0.10s) / 회귀 0. **영속 의무 매트릭스 9 영역 전수 영속**: 사이클 88 G-REJECT-1/2/3 (4중 안전망 대체 X, 추가만) / 사이클 17 OPSP0002 backoff / 사이클 29 R1/R2/R3 (silent_inactive + 종목 stale + priority) / 사이클 13-E-2 cooldown 패턴 답습 / 사이클 24 silent_inactive 2회 cap 답습 (사이클 92 = 3회 영역 보강) / 사이클 20 토큰 24h 캐시 / 사이클 67 stale_manager 4 sub-module / 사이클 38 명문화 (scanner 매수 진입 전용) / 사이클 64/65/81/89/91 scanner 영역 무영향 + 사이클 55 R-1 SellRejectionTracker 영속. **CLAUDE.md "절대 깨지 말 것" 8 영역 영속**. **매매 안전성 무영향** (시간 상수 영역 07:50 이전 코드 경로 영향 0 + 자동 재기동 = lifecycle 영역 매수/매도 hot path 직접 경로 없음 + 사이클 38 명문화 영속). **사이클 49→92 누적 47 사이클 + hotfix 14 영역**. **사이클 93+ 후속 카드 인계**: 익일 2026-06-11 (목) 07:45~08:10 운영 측정 의무 (07:55 _boot 발화 확인 + 07:50 발화 없음 확정 + 07:59 PRESUBSCRIBE 발화 + `[ws_auto_restart]` WARNING 0건 정상 시 + KIS 07:50 강제 중단 발생 시 07:55+31s = 08:01:31 이후 자동 재기동 발화 예상) / 사이클 87 D+1 운영 측정 영속 (Phase 2/3 09:33+ 자동 wakeup 예약) / 카드 #21 (LOW 사이클 74 flaky) / 사이클 96 외부 의견 재평가 / #16 / auth 1.43x / #15 / #11/#12. | `src/engine/scheduler.py` (+2L L51~L53 시간 상수 시정) / `src/realtime/websocket.py` (+85L import 정정 + 모듈 전역 3 + 인스턴스 변수 2 + stop/start 래퍼 + `_trigger_auto_restart` + MAX_RECONNECT 분기 1줄) / `tests/unit/engine/test_cycle92_*.py` 4 파일 신규 (H-1/M-3/M-4/L-2) / `tests/unit/realtime/test_cycle92_*.py` 9 파일 신규 (H-2~H-5/M-1/M-2/M-5/L-1/L-3) / `tests/unit/ast/test_cycle92_g_reject_persistence.py` (H-6) / `_workspace/red/cycle92_kis_750_force_disconnect.md` Red 명세 + 결함 chain + KIS MCP 정본 검증 결과 / `_workspace/cycle92_phase1_diagnosis.md` Phase 1 진단 영구 기록 / `_workspace/cycle92_domain_consult.md` 자문 메모 영구 기록 / `_workspace/cycle92_verify_report.md` verify 보고서 영구 기록 / `_workspace/cycle87_dplus1_verification.md` 사이클 87 Phase 1 baseline 영구 기록 / `_workspace/test_index.yaml` 영향 인덱스 갱신 (backend tests 410 → 416) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 92 행 | 사용자 운영 보고 (07:45 우리 기동 vs 07:50 KIS API 강제 접속 중단 충돌) 즉시 시정 (사용자 결정 Q28~Q33 + Q32 domain-expert P1 + Q33 완료 즉시 push). 29 사이클 연속 옵션 A 패턴 영속. team-leader Phase 1 진단 (결함 chain MAX_RECONNECT 31초 누적 → WS 영구 종료 → 4중 안전망 무력 확정) + domain-expert 자문 영속 (TIME_BOOT 07:55 + TIME_PRESUBSCRIBE 07:59 동행 + idempotent + cooldown/cap + G-REJECT-1 영속). 매매 안전성 무영향 (시간 상수 영역 + lifecycle 영역). 사이클 88 G-REJECT-1 영구 차단 가드 영속 (자동 재기동 = 4중 안전망 *추가*, 대체 X). 사이클 13-E-2 cooldown + 사이클 24 silent_inactive 2회 cap 답습 (사이클 92 = 3회 영역 보강). 사이클 20 토큰 24h 캐시 영속. 사이클 67 stale_manager / 사이클 38 / 사이클 64/65/81/89/91 영속. 운영 가시화 4 prefix 신설 = KIS LMS chain 영구 차단 + 운영 진단 가시화. 사이클 93+ 후속 카드: 익일 (2026-06-11 목) 07:45~08:10 운영 측정 의무 / 사이클 87 D+1 Phase 2/3 09:33+ 자동 wakeup 영속 / 카드 #21 (LOW 사이클 74 flaky) / 사이클 96 외부 의견 재평가 / #16 / auth 1.43x / #15 / #11/#12. |
| 2026-06-09 | 사이클 91 — **KIS volume_rank 페이징 누적 누락 silent 결함 영구 시정** (사이클 89 silent 결함 + 사용자 운영 실측 보고 60 ticker, **28 사이클 연속 옵션 A 패턴 영속**). 사용자 보고 = "60 티커로 나와.. 500티커가 되어야 정상" → KIS MCP 정본 검증 (`volume_rank.py` L101~L110) = **페이징 호출 의무** (응답 헤더 `tr_cont == "M"` → 재호출 `tr_cont="N"` → 누적). 사이클 89 구현 (`_fetch_volume_rank`) = 단일 호출 + slicing → 첫 페이지 ~30건 한도 = KOSPI 30 + KOSDAQ 30 = **60 ticker** 정확 일치. **시정 (production 2 파일)**: (1) `src/api/base.py` (+36L) `kis_get_quote` + `kis_post_quote` + `_request_via_quote_pool` 시그너처 `tr_cont: str = ""` 키워드 인자 추가 + 응답 헤더 `_response_headers["tr_cont"]` 주입 + AsyncMock coroutine graceful 폴백 (회귀 차단). (2) `src/engine/scanner.py` (+63L) `_fetch_volume_rank` 페이징 누적 루프 (`max_pages=15` 무한 루프 차단 + `tr_cont` 순환 + 50ms sleep + top_n 조기 종료 + graceful KIS error). **회귀 가드 8 케이스 (8 파일 신규)**: HIGH 4 (H-1 페이징 누적 mock 3 페이지 90 누적 + H-2 top_n=250 조기 종료 + H-3 max_pages=15 무한 루프 차단 + H-4 fetch_top_500_universe 통합 500 ticker 완전 달성) + MEDIUM 3 (M-1 페이지 간 50ms sleep + M-2 중간 페이지 KIS 실패 graceful 누적분 반환 + M-3 no_pagination legacy 환경 호환) + LOW 1 (L-1 AST `_fetch_volume_rank` 본체 `tr_cont`/`max_pages`/`asyncio.sleep`/`_response_headers` 정적 가드, silent 결함 영구 차단). **부수 시정**: `test_quote_pool_label_skip.py` 5건 회귀 (AsyncMock unawaited coroutine) → `asyncio.iscoroutine()` 감지 + `.close()` + 빈 문자열 폴백 graceful (production `httpx.Response.headers` 동기 경로 무영향). **백엔드 2199 → 2211 PASS** (+12 = 사이클 91 8 신규 + label_skip 5 회귀 복구 차감 후 순증) + 2 XFAIL + 2 skip / **프론트 234 PASS** 영속 / **합계 2445 PASS** / **flakiness 0** (백 3 회 50.14~50.70s ±0.6%) / 회귀 0. **영속 의무 매트릭스 7 영역 전수 영속**: 사이클 89 KOSPI/KOSDAQ 분리 + ETF 제외 + 거래대금 정렬 / 사이클 88 G-REJECT-1/2/3 / 사이클 84 L-2 + 사이클 90 L-2 화이트리스트 / 사이클 81 bfdy_clpr / 사이클 38 명문화 / 사이클 68 KST / 사이클 80 hotfix #3 LIFO. **CLAUDE.md "절대 깨지 말 것" 8 영역 영속**. **매매 안전성 무영향** (scanner 단계 종목 풀 보강 영역 + 사이클 38 명문화 + 매도/손절/익일청산/15:20 강제청산 hot path 무관 + KIS Rate Limit 안전 50ms sleep + 페이징 9~10회/시장 × 2 = ~25~30회 전체 호출 + 1~2분 소요). **silent 결함 영구 차단 21 회 누적** (사이클 60/64/65#1/65#2/65#3/66/67/68/72/73/77/77#2/78/79/80/80#2/80#3/80#4/81/83/**91**). **사이클 49→91 누적 46 사이클 + hotfix 14 영역**. **운영 효과 (push + EC2 배포 후)**: 사용자 UI "지금 새로고침" 버튼 클릭 → **1~2분 대기 → 60 ticker → 500+ ticker 정상 적재**. **사이클 92+ 후속 카드 인계**: 사이클 87 (D+1 운영 측정, 2026-06-10 수 09:00~ 별개) — 페이징 누적 500 ticker 달성 실측 + KIS 호출 빈도 25~30회 + 1~2분 소요 운영 확인 / 카드 #21 (LOW 부차) 사이클 74 stale_watcher logging flaky / 사이클 96 외부 의견 재평가 / #16 / auth 1.43x / #15 / #11/#12. | `src/api/base.py` (+36L tr_cont 시그너처 + 응답 헤더 주입 + AsyncMock graceful) / `src/engine/scanner.py` (+63L 페이징 누적 루프) / `tests/unit/engine/scanner/test_cycle91_*.py` 7 파일 신규 (H-1/H-2/H-3/H-4 + M-1/M-2/M-3) / `tests/unit/ast/test_cycle91_ast_pagination_pattern.py` 신규 (L-1, 5 sub-case) / `_workspace/red/cycle91_volume_rank_pagination.md` Red 명세 + KIS 정본 인용 / `_workspace/test_index.yaml` 영향 인덱스 갱신 (backend tests 394 → 410) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 91 행 | 사이클 89 silent 결함 (KIS volume_rank 페이징 누적 누락) + 사용자 운영 실측 보고 즉시 시정 (사용자 결정 A 즉시 발주). 28 사이클 연속 옵션 A 패턴 영속. team-leader 진단 (KIS MCP 정본 검증 의무) + domain-expert 자문 생략 (silent 결함 영구 시정 영역, 사이클 89 동일 영역). 매매 안전성 무영향 (scanner 단계 + KIS Rate Limit 50ms sleep + 페이징 누적). silent 결함 영구 차단 21 회 누적. AST L-1 영구 가드 = 미래 단일 호출 회귀 영구 차단. 사이클 89 KOSPI/KOSDAQ 분리 + ETF 제외 + 거래대금 정렬 영속. 사이클 88 G-REJECT / 사이클 84 L-2 + 사이클 90 L-2 / 사이클 81 / 사이클 38 / 사이클 68 / 사이클 80 hotfix #3 영속. 운영 효과: 사용자 UI "지금 새로고침" → 60 → 500+ ticker 정상 적재. 사이클 92+ 후속 카드: 사이클 87 D+1 운영 측정 + 카드 #21 (LOW 사이클 74 flaky) + 사이클 96 외부 의견 재평가 + #16 / auth 1.43x / #15 / #11/#12. |
| 2026-06-09 | 사이클 90 — **stock_master 500+ 수동 trigger API (POST /api/stock-master/refresh-universe) + UI "지금 새로고침" 버튼** (사이클 89 후속 = 장 종료 후 즉시 발화 영역 보강, **27 사이클 연속 옵션 A 패턴 영속**). 사용자 보고 = "장 종료 후라 시작이 안돼" → scheduler.start() 호출 안 됨 → `_universe_eager_refresh_task` 자동 발화 불가 → 즉시 적재 영역 보강 의무. 사용자 결정 (Q24=B 수동 trigger API + Q25=A asyncio.Lock + 409 Conflict + Q26=A stats 카드 상단 우측 + Q27=A 사이클 89 [stock_master_bulk_refresh] 영속 활용). domain-expert 자문 생략 (READ-ONLY POST + 매매 hot path 무관). **시정 백엔드** (`src/routes/stock_master.py` +37L): POST `/refresh-universe` 라우트 신규 + `_refresh_universe_lock = asyncio.Lock()` 모듈 전역 + `async with Lock` 패턴 + 409 Conflict locked 분기 + `fetch_top_500_universe()` 직접 호출 + `time.monotonic()` elapsed_ms + ApiResponse 래퍼 + GET `/{ticker}` 라우트에 `_POST_ONLY_PATHS` 가드 (FastAPI 동적 라우팅 POST only 차단). **사이클 84 L-2 AST 가드 갱신**: `_ALLOWED_POST_ROUTES = {"/refresh-universe"}` 화이트리스트 신설 (POST 1개 예외 허용 + 기타 POST/PUT/DELETE/PATCH 0건 영속). **시정 프론트** (5 파일 +95L): `frontend/src/types/stock-master.ts` (+9L `RefreshUniverseResult` 인터페이스) + `frontend/src/api/stock-master.ts` (+18L `refreshUniverseNow()` 함수 + ApiResponse `data.data` 추출) + `frontend/src/pages/StockMaster.tsx` (+48L stats 카드 상단 우측 "지금 새로고침" 버튼 + `useMutation` retry=false + 로딩 인디케이터 + 토스트 3종 (성공/409/5xx) + invalidateQueries 자동 갱신) + `frontend/src/test/handlers.ts` (+9L MSW POST mock) + `e2e/fixtures/api-mocks.ts` (+11L Playwright LIFO 정합 POST mock, wildcard *후* 등록). **회귀 가드 17 케이스** (백 9 신규 + 프 11 갱신/신규 + AST 2): HIGH 6 (H-1 POST 라우트 ApiResponse 정합 + H-2 asyncio.Lock + 409 Conflict + H-3 사이클 84 L-2 AST 갱신 + H-4 fetch_top_500_universe 호출 정합 + H-5 refreshUniverseNow API 정합 + H-6 UI 버튼 + useMutation 토스트 3종) + MEDIUM 6 (M-1 PUT/DELETE/PATCH/GET 405 + M-2 timing + M-3 Lock release + M-4 버튼 위치 + disabled + M-5 G-AST7 api-mocks 등록 영구 가드 + M-6 LIFO 정합 영속) + LOW 5 (L-1 KST + L-2 G-REJECT-1/2/3 영속 + L-3 emit 영속 + 2 영속 가드). **백엔드 2180 → 2199 PASS** (+19) + 2 XFAIL + 2 skip / **프론트 223 → 234 PASS** (+11) / **합계 2433 PASS** / **flakiness 0** (백 3 회 47.95~48.41s ±1% + 프 사이클 90 격리 17 PASS × 3 회 0.01s 일관) / 회귀 0. **부차 발견 (운영 영향 0)**: 백엔드 2차 측정에서 `test_scheduler_stale_logging_format::test_stale_watcher_emits_legacy_and_detail_lines` 1회 fail → 격리 단독 PASS = 사이클 74 stale_watcher logging caplog race flaky 추정 (사이클 90 영역 무영향 확정, 사이클 91+ 카드 #21 LOW 인계). **영속 의무 매트릭스 8 영역 전수 영속**: 사이클 38 명문화 (매수 진입 전용, POST refresh 매수 hot path 무관) / 사이클 64/65/81/89 scanner 단계 영속 / 사이클 84 L-2 화이트리스트 갱신 / 사이클 80 hotfix #3 LIFO 정합 / 사이클 88 G-REJECT-1/2/3 / 사이클 89 emit 영속 (자동/수동 구분 0) / 사이클 68 KST / 사이클 75 G-RT retry (useMutation retry=false 명시 = 25초 KIS 중복 호출 차단 영속). **CLAUDE.md "절대 깨지 말 것" 8 영역 영속**. **매매 안전성 무영향** (POST refresh = READ-ONLY 영역 KIS volume_rank 조회 + DB upsert + asyncio.Lock 도입 = 동시 KIS 호출 중복 차단 Rate Limit 보호). **사이클 49→90 누적 45 사이클 + hotfix 14 영역**. **사이클 91+ 후속 카드 인계**: 사이클 87 (D+1 운영 측정, 2026-06-10 수 09:00~ 별개) — 사이클 90 즉시 trigger 효과 동행 측정 (사용자가 push 후 UI 종목마스터 메뉴 → "지금 새로고침" 버튼 클릭 → stock_master 52 → 500+ 즉시 적재 가능) / 사이클 91+ 카드 #21 (LOW, 부차 발견) 사이클 74 stale_watcher logging caplog race flaky 가드 / 사이클 96 외부 의견 재평가 (#E1/#E2/#A/#B) / #16 / auth 1.43x / #15 / #11/#12. | `src/routes/stock_master.py` (+37L POST /refresh-universe + asyncio.Lock + 409 Conflict + GET /{ticker} 가드) / `tests/unit/ast/test_cycle84_ast_no_update_route.py` (L-2 갱신 화이트리스트 신설) / `tests/unit/routes/test_cycle90_*.py` 8 파일 신규 (H-1/H-2(2)/H-4(2)/M-1/M-2/M-3/L-1/L-3) / `tests/unit/ast/test_cycle90_g_reject_persistence.py` (L-2 신규) / `tests/unit/e2e_mocks/test_api_mocks_routes_registered.py` (M-6 갱신) / `frontend/src/types/stock-master.ts` (+9L) / `frontend/src/api/stock-master.ts` (+18L) / `frontend/src/pages/StockMaster.tsx` (+48L) / `frontend/src/test/handlers.ts` (+9L) / `frontend/src/api/__tests__/stock-master.test.ts` (H-5 3 갱신) / `frontend/src/pages/__tests__/StockMaster.test.tsx` (H-6 + M-4 6 갱신) / `frontend/src/components/__tests__/_ast_api_mocks_coverage.test.ts` (M-5 G-AST7 2 갱신) / `e2e/fixtures/api-mocks.ts` (+11L LIFO 정합) / `_workspace/red/cycle90_manual_refresh_universe_api.md` Red 명세 영구 기록 / `_workspace/cycle90_phase1_diagnosis.md` Phase 1 진단 영구 기록 / `_workspace/cycle90_verify_report.md` verify 보고서 영구 기록 / `_workspace/test_index.yaml` 영향 인덱스 갱신 (backend tests 385 → 394) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 90 행 | 사이클 89 후속 (장 종료 후 즉시 발화 영역 보강) — 사용자 보고 "장 종료 후라 시작이 안돼" 즉시 시정. 27 사이클 연속 옵션 A 패턴 영속. team-leader Phase 1 진단 (Q24~Q27) + domain-expert 자문 생략 (READ-ONLY POST). 매매 안전성 무영향 (READ-ONLY POST + scanner 영역 + asyncio.Lock Rate Limit 보호). 사이클 84 L-2 AST 가드 갱신 = POST 1개 예외 화이트리스트 영속 (READ-ONLY 의미 영속 + POST 명시 예외 1개) — 영속 의무 위반 0. 사이클 80 hotfix #3 LIFO 정합 + 사이클 89 emit 영속 + 사이클 88 G-REJECT 영속. 운영 효과 즉시: 사용자 UI 종목마스터 메뉴 "지금 새로고침" 버튼 클릭 → stock_master 52 → 500+ 즉시 적재 가능 (장 종료 후 + scheduler idle 영역 무관). 사이클 91+ 후속 카드 인계: 사이클 87 D+1 운영 측정 동행 / 카드 #21 (LOW 부차 발견) 사이클 74 stale_watcher logging flaky / 사이클 96 외부 의견 재평가 / #16 / auth 1.43x / #15 / #11/#12. |
| 2026-06-09 | 사이클 89 — **stock_master 500+ universe 확장 (KOSPI 250 + KOSDAQ 250) + UI hotfix 2 (종목명 표시 + 한글 라벨 + 영문/사이클 언급 제거)** (**26 사이클 연속 옵션 A 패턴 영속**). 사용자 요구 = "stock_master 수집 종목 500+ 확장 + 각 전략 필터링" + UI 결함 2 영역 (종목명 미표시 + 영문 필드명). team-leader Phase 1 진단 + domain-expert 자문 (Q19=B 거래대금 상위 500 + Q20=D 개장 전 1회 + 사이클 83 결합 + Q21=A KIS volume_rank + Q22=A 90일 retention + Q23=A 전략 독립 + A1 거래대금 정렬 + A2 KOSPI/KOSDAQ 분리 + ETF/리츠/SPAC 자동 제외). **시정 백엔드 (production 신규 1 + 갱신 3 + whitelist 1)**: (1) `src/engine/stock_master_metrics.py` 신규 — universe refresh collector + record/flush (사이클 74/78 답습) + KST 영속. (2) `src/engine/scanner.py` 신규 함수 5 (`fetch_top_500_universe` + `_fetch_volume_rank` + `_universe_filter_securities_only` + `_trade_amount_key` + `_universe_eager_refresh_loop`) + 상수 4 (`_VOLUME_RANK_URL` + `_VOLUME_RANK_TR_ID` + `_MARKET_INPUT_ISCD` + `_ALLOWED_PRODUCT_TYPE_CD`). (3) `src/engine/scheduler.py` `_universe_eager_refresh_task` lifecycle 3곳 (start + run_daily.finally + stop, 사이클 79 영구 가드 답습) + `_universe_eager_refresh_loop` 메서드 + `stop()` flush hook (사이클 78 답습). (4) `src/api/base.py` `/uapi/domestic-stock/v1/quotations/volume-rank` whitelist 추가. **KIS MCP 정본 검증** (사이클 81 답습): `function_name=volume_rank` / `api_name=거래량순위` / FHPST01710000 정합. **시정 프론트 (UI hotfix 2)**: hotfix #1 (`frontend/src/pages/StockMaster.tsx` +130L) — 종목명 텍스트 렌더 추가 + `FIELD_LABELS` 13 키 한글 매핑 (영문 `bfdy_clpr` → "전일종가" 등) + `CATEGORY_ICONS` 5 + `formatPrice/formatVolume/formatAmount/formatExchange` 포맷터 + boolean 플래그 (✓/✗ + 색상) + monospace 종목코드 / hotfix #2 — 사이클 언급 + 영문 용어 제거 (L448 "전일종가 정상 적재" ← "bfdy_clpr 보유 (사이클 81 정합)" + L460 "오늘 자동 갱신 횟수" ← "오늘 eager refresh"). **회귀 가드 19 케이스** (백엔드 17 신규 + 프론트 호환 갱신 2): HIGH 5 (H-1 거래대금 정렬 + H-2 ETF 자동 제외 + H-3 KOSPI/KOSDAQ 분리 + H-4 Rate Limit 50ms + H-5 후보 풀 폭증 영구 차단 mocking semantics 시정) + MEDIUM 7 (M-1 lifecycle + M-2 24h TTL + M-3 R4 universe guard 영속 + M-4 protected_tickers 영속 + M-5 trade_amount_filter 영속 + M-6 bfdy_clpr 영속 + M-7 emit 가시화) + LOW 5 (L-1 G-AST1 flush 호출 사이트 + L-2 G-AST2 task cancel 양쪽 추가 + L-3 KST + L-4 history trigger 영속 + L-5 G-REJECT-1/2/3 영속 가드). **기존 테스트 갱신**: `test_scheduler_stop_zombie_tasks.py::expected_members` 11종 → 12종 (사이클 79/83 영속 패턴 답습) + `test_cycle89_candidate_pool_protection.py` H-5 mocking semantics 시정 (`with patch(...)` 블록 내 모듈 attribute 직접 호출, 사용자 결정 옵션 A) + `frontend/src/pages/__tests__/StockMaster.test.tsx` `getByText` → `getAllByText` 2 줄 갱신. **백엔드 2161 → 2180 PASS** (+19 신규) + 2 XFAIL + 2 skip / **프론트 223 PASS** 영속 / **합계 2403 PASS** / **flakiness 0** (백 3 회 46.62~46.95s ±0.7% + 프 3 회 2.95~3.41s) / 회귀 0. **영속 의무 매트릭스 12 영역 전수 영속** (사이클 32 R4 / 38 명문화 / 64 protected_tickers / 65 trade_amount / 67 stale_manager / 78 flush / 79 G-AST1 / 81 bfdy_clpr / 83 scan_pool / 84 history trigger / 88 G-REJECT-1/2/3). **CLAUDE.md "절대 깨지 말 것" 8 영역 영속**. **매매 안전성 무영향** (scanner 단계 영역 한정 + 사이클 38 명문화 영속 + 매도/익일청산/15:20 강제청산/손절 영향 0 + UI 레이어 한정 hotfix). **사이클 49→89 누적 44 사이클 + hotfix 14 영역**. **사이클 90+ 후속 카드 인계**: 사이클 87 (D+1 운영 측정, 2026-06-10 수 09:00~ 별개) — 사이클 89 동행 측정 영역 추가 (`[stock_master_bulk_refresh]` + `[stock_master_universe_summary]` + stock_master 52→500+ + `[price_filter_scanner_skip]` 정상 발화 + `[risk_silent_skip]` SK스퀘어 폭주 감소) / 사이클 91+ (Q23 옵션 B 공용 헬퍼 검토, 3회 반복 추상화 규칙 = 사이클 74/78/89 충족 후보) / 사이클 96 외부 의견 재평가 (#E1/#E2/#A/#B) / #16 / auth 1.43x / #15 / #11/#12. | `src/engine/stock_master_metrics.py` 신규 (84L) / `src/engine/scanner.py` (+신규 함수 5 + 상수 4) / `src/engine/scheduler.py` (lifecycle 3곳 + 메서드 + flush hook) / `src/api/base.py` (whitelist 1) / `tests/unit/engine/test_scheduler_stop_zombie_tasks.py` (12종 갱신) / `tests/unit/engine/scanner/test_cycle89_*.py` 3 파일 (H-1/H-2/H-3) / `tests/unit/engine/test_cycle89_*.py` 9 파일 (H-4/H-5/M-1~M-7) / `tests/unit/ast/test_cycle89_*.py` 3 파일 (L-1/L-2/L-5) / `tests/unit/engine/test_cycle89_kst_consistency.py` (L-3) / `tests/unit/db/test_cycle89_history_trigger_persistence.py` (L-4) / `frontend/src/pages/StockMaster.tsx` (+130L hotfix #1 + L448/L460 hotfix #2) / `frontend/src/pages/__tests__/StockMaster.test.tsx` (`getAllByText` 2 줄 갱신) / `_workspace/red/cycle89_stock_master_500_universe.md` Red 명세 영구 기록 / `_workspace/cycle89_phase1_diagnosis.md` Phase 1 진단 영구 기록 / `_workspace/cycle89_domain_consult.md` 자문 메모 영구 기록 / `_workspace/test_index.yaml` 영향 인덱스 갱신 (backend tests 370 → 385) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 89 행 | 사용자 요구 (stock_master 500+ 확장 + 각 전략 필터링) + UI 결함 2 영역 (종목명 미표시 + 영문 필드명 + 사이클 언급 + 영문 용어) 통합 시정. 26 사이클 연속 옵션 A 패턴 영속. team-leader Phase 1 + domain-expert 자문 (Q19~Q23 + A1~A6) 전수 일치. KIS MCP 정본 검증 (volume_rank/FHPST01710000) = 사이클 81 답습 패턴. 매매 안전성 무영향 (scanner 단계 + UI 레이어 한정). 사이클 32 R4 / 38 명문화 / 64 / 65 / 67 / 78 / 79 / 81 / 83 / 84 / 88 G-REJECT 12 영역 영속. 운영 효과 예상 (push 후 다음 영업일): stock_master 적재 52 → 500+ / `[price_filter_scanner_skip]` 정상 발화 / `[risk_silent_skip]` SK스퀘어 폭주 감소 + UI 종목명 표시 + 한글 친숙 라벨 + 사이클 언급 0. 사이클 90+ 후속 카드 인계: 사이클 87 D+1 운영 측정 동행 (사이클 89 6 추가 영역) / 사이클 91+ Q23 옵션 B 공용 헬퍼 (3회 반복 추상화 규칙 충족) / 사이클 96 외부 의견 재평가 / #16 / auth 1.43x / #15 / #11/#12. |
| 2026-06-09 | 사이클 88 — **외부 LLM 의견서 영구 기록 + 영구 차단 AST 가드 3** (코드 변경 0, **25 사이클 연속 옵션 A 패턴 영속**). Plan `/Users/koscom/.claude/plans/radiant-marinating-pumpkin.md` 영속 (A 점진 흡수 + 전수 3 AST 가드 + 8 쿼리 채택). 사용자 첨부 타 LLM 의견서 (시세 수신 오류 + 구조 단순화 관점) → refactor-expert + domain-expert 병렬 검토 완료 (양 agent 일치 결론 = "구조 관찰 객관, 영속 시정 18 사이클 도메인 지식 부재"). **시정 (production 0 + 산출물 3)**: (1) `_workspace/external_llm_reviews/2026-06-09_realtime_review.md` 신규 디렉토리 + 통합 영구 기록 (외부 verbatim + refactor 검토 + domain 자문 + 양 agent 일치 + 사용자 결정 + 영구 차단 3 + 사이클 96 재평가 예약 7 영역). (2) `src/realtime/CLAUDE.md` §외부 LLM 검토 영구 기록 섹션 추가 (영속 보장 매트릭스 5 + 영구 차단 영역 3 + 채택 가능 후속 카드 + 참조). (3) `tests/unit/ast/test_external_llm_reject_patterns.py` 신규 (G-REJECT-1/2/3 영구 차단 AST 가드 3). **영구 차단 영역 3 (AST 가드)**: G-REJECT-1 (외부 R4 단일 restore 통합 차단 = 4중 안전망 영속 의무) + G-REJECT-2 (외부 R5 stale 전체 WS 단독 차단 = 종목+세션 2 계층 영속 + 사이클 29 005935 사고 재현 방지) + G-REJECT-3 (외부 R2 SubscriptionRegistry 단일 dict 통합 차단 = 4 dict 분리 영속 + orphan ACK race / KIS 41 한도 분산). **각 가드 docstring 영속**: 외부 의견 출처 + 영속 시정 사이클 + 위반 시 사고 시나리오 영구 기록. **백엔드 2158 → 2161 PASS** (+3 AST 신규) + 2 XFAIL + 2 skip / **프론트 223 PASS** / **합계 2384 PASS** / **flakiness 0** (3 회 45.64~45.96s ±0.7%) / 회귀 0. **영속 의무 매트릭스 전수 영속**: CLAUDE.md "절대 깨지 말 것" 8 영역 / WebSocket 4중 안전망 (F1 + `_scan_loop` + K stale watcher + `_resubscribe_stale_priority` 4 함수 정적 검증 PASS) / 종목+세션 2 계층 stale 하이브리드 (사이클 29 R1+R2) / 4 dict 분리 (`_subscriptions` / `_subscriptions_acked` / `_ticker_to_session` / `ticker_last_tick`) / HIGH bypass_limit=True + 보유/익일청산 절대 보호 (사이클 32 R4) / 사이클 38 명문화 / 사이클 67 stale_manager 4 sub-module 분할 영속. **매매 안전성 무영향** (production 코드 변경 0, 매매 hot path 영역 외). **사이클 49→88 누적 43 사이클 + hotfix 14 영역**. **사이클 87+ 후속 카드 인계**: 사이클 87 (D+1 운영 측정, 2026-06-10 수 09:00~10:00 별개) — Supabase MCP READ-ONLY 8 쿼리 (사이클 83/84/85 effect 3 + 외부 의견 검증 5) / 사이클 89~95 (1주 운영 실증, 자동, 추가 발주 없음) / 사이클 96 (2026-06-13 이후 외부 의견 재평가) — 카드 #E1 (MEDIUM SubscriptionRegistry read-only view ~80L) + #E2 (LOW docstring 매핑) + #A (MEDIUM stale 임계 완화 HIGH=60s 영속 LOW=300s 신규) + #B (LOW 15:25~15:30 KRX 동시호가 stale skip) 발주 결정 / #16 / auth 1.43x / #15 / #11/#12. | `_workspace/external_llm_reviews/2026-06-09_realtime_review.md` 신규 (디렉토리 + 통합 영구 기록 7 영역) / `src/realtime/CLAUDE.md` §외부 LLM 검토 영구 기록 섹션 추가 (L176~L207) / `tests/unit/ast/test_external_llm_reject_patterns.py` 신규 (G-REJECT-1/2/3 3 AST 가드) / `_workspace/refactor/2026-06-09_external_llm_review.md` 영구 기록 / `_workspace/refactor/2026-06-09_external_llm_domain_consult.md` 영구 기록 / `_workspace/test_index.yaml` 영향 인덱스 갱신 (backend tests 367→370) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 88 행 | 타 LLM 외부 의견서 (사용자 첨부, 시세 수신 오류 + 구조 단순화 관점) 처리 방침 결정 사이클 — Plan 영속 (A 점진 흡수). 25 사이클 연속 옵션 A 패턴 영속. team-leader Phase 1 진단 (사용자 결정 의제 없음 확정) + refactor-expert + domain-expert 양 agent 일치 결론 영속 (영속 시정 18 사이클 도메인 지식 부재 = 단순 통합 추천 영구 거부 명문화). 매매 안전성 무영향 (production 코드 변경 0). G-REJECT 3 AST 가드 = 미래 외부 LLM 동일 추천 재현 시 즉시 검출 영구 차단 + 영속 보장 매트릭스 5 영역 전수 영속 보장. 사이클 87+ 후속 카드 인계: 사이클 87 D+1 측정 (2026-06-10 수 별개) / 사이클 89~95 1주 운영 실증 / 사이클 96 (2026-06-13 이후) 외부 의견 재평가 + #E1/#E2/#A/#B 발주 결정 / #16 / auth 1.43x / #15 / #11/#12. |
| 2026-06-09 | 사이클 85 — **stock_master UI 메뉴 구축 프론트 단독 단계** (사이클 83 후속 3 단계 분할 2/3, **23 사이클 연속 옵션 A 패턴 영속**). 사용자 결정 (Q2=A 7번째 메뉴 `종목마스터` + Q10=A 단일 페이지 4 카드 + Q11=B 카테고리 분류 + 핵심 5 키 highlight + Q12=A `<pre>` collapsible + Q13=B 60s polling + Q14=C 사이클 85 push + 사이클 86 통합 검증 별개). domain-expert 자문 생략 (UI 레이어 한정 + 사이클 41/65/75/80/81 영속 패턴 답습). **시정 (production 신규 3 + 갱신 3 = 6 파일)**: (1) `frontend/src/types/stock-master.ts` 신규 (45L) — 5 interface (StockMasterStats / StockMasterListItem / StockMasterDetail / StockMasterHistoryItem / ScanPoolSummary). (2) `frontend/src/api/stock-master.ts` 신규 (70L) — 5 함수 (`fetchStats` / `fetchList` / `fetchScanPoolSummary` / `fetchDetail` / `fetchHistory`, ApiResponse `data.data` 추출 패턴 답습). (3) `frontend/src/pages/StockMaster.tsx` 신규 (330L, Q10=A 단일 페이지 4 카드) — 상태 카드 (count_all / bfdy_clpr_present 사이클 81 키 정합 가시화 / nxt_tradable / eager_refresh_today 사이클 83 emit 카운트) + list 테이블 (페이징 limit=100 refreshed_at DESC) + detail 모달 (Q11=B 카테고리 분류 기본/가격/거래/플래그/메타 + 핵심 5 키 bfdy_clpr/acml_vol/nxt_tradable/krx_halted/admin_item highlight) + history 테이블 (Q12=A changed_at DESC + change_type + before/after `<pre>` collapsible). useQuery 5종 모두 `retry: 1` (사이클 65 H3 + 사이클 75 G-RT 영속) + `refetchInterval: 60_000` (Q13=B). DetailModal `initialData` prop 추가 (list 클릭 시 highlight 즉시 렌더). (4) `frontend/src/App.tsx` (+4L) — `navItems` 6→7 (`종목마스터`/`/stock-master`) + 라우트 + `MobileMenuLabel` 매핑 + 모바일 햄버거 Drawer 7개 압축 (사이클 81 G-M5 영속). (5) `frontend/src/test/handlers.ts` MSW (+47L) — 5 라우트 mock handlers. (6) `e2e/fixtures/api-mocks.ts` Playwright (+35L, 사이클 80 hotfix #3 LIFO 정합) — wildcard `**/api/stock-master/**` *전* 등록 (fallback) + 5 구체 라우트 *후* 등록 (LIFO 우선 매칭). **회귀 가드 16 케이스 (5 파일 신규 + AST 영역 확장 4)**: HIGH 5 (G-AST-RT StockMaster.tsx TARGET_PAGES 확장 사이클 65 H3 + 사이클 75 G-RT3 답습 + G-AST-MOCK 5 endpoint api-mocks 등록 영구 가드 사이클 75 G-AST5 답습 + **G-AST-LIFO** wildcard *전* + 5 구체 *후* LIFO 정합 영구 가드 사이클 80 hotfix #3/#4 답습 + G-MOBILE-7 햄버거 Drawer 7개 메뉴 사이클 81 M-1~M-8 답습 + G-KST `Intl.DateTimeFormat(timeZone='Asia/Seoul')` 명시 + `getHours()` 0건 사이클 68 영속) + MEDIUM 7 (H-STATS / H-LIST / H-DETAIL / H-HISTORY / H-404 / H-422 / H-POLLING refetchInterval 60_000) + LOW 4 (L-API 5 함수 단위 + L-MENU 7번째 메뉴 라우트 + L-CYCLE83-EMIT count_eager_refresh_today 호출 정합 + L-CYCLE81-MOBILE 모바일 viewport 375px 영속). **프론트 196 → 223 PASS** (+27) / **백엔드 2151 → 2158 PASS** (+7 G-AST-LIFO 7 sub) + 2 XFAIL + 2 skip / **합계 2381 PASS** / **flakiness 0** (프론트 3 회 3.41~3.47s) / 회귀 0. **부수 발견** (사이클 86+ 인계 신규 카드 MEDIUM): 사이클 83 `test_g_ml1_periodic_task_clears_eager_refresh_collector` 가 full-suite 외부 DNS race 1건 발생 (단독 실행 시 0.15s PASS 재현, sandbox 환경 한정, **사이클 85 변경 영역 무관 확정 + 운영 무영향**) — 후속 카드 (mock 보강 또는 networkx isolation). **영속 의무 매트릭스 7 영역 전수 PASS**: 사이클 65 H3 useQuery retry:1 (5건 정확 grep) + 사이클 68 KST + 사이클 75 G-AST5 api-mocks 영역 확장 + 사이클 80 hotfix #3 Playwright LIFO + 사이클 81 M-1~M-8 햄버거 메뉴 7개 + 사이클 83 emit 영속 + 사이클 84 5 GET ApiResponse 정합. CLAUDE.md "절대 깨지 말 것" 8 영역 전수 영속. 매매 안전성 무영향 (UI 레이어 한정, READ-ONLY 5 GET 라우트만 호출, 매매 hot path 변경 0). **사이클 49→85 누적 40 사이클 + hotfix 11 영역**. **사이클 86+ 후속 카드 인계**: 사이클 86 (통합 검증) — Playwright `e2e/stock-master.spec.ts` 신규 + 운영자 1회 실제 실행 (bfdy_clpr_present 비율 95%+ + history INSERT trigger 실측 D+1 2026-06-10 수 09:00~10:00 동행) / 사이클 87+ (LOW) — 90일 retention cron `purge_old_stock_master_history()` (사이클 6 답습) / 사이클 85 부수 발견 (MEDIUM, 신규) — 사이클 83 메모리 leak 테스트 외부 DNS 영구 차단 (mock 보강) / 사이클 83 운영 측정 (D+1) / #16 / auth 1.43x / #15 / #11/#12. | `frontend/src/types/stock-master.ts` 신규 (45L) / `frontend/src/api/stock-master.ts` 신규 (70L) / `frontend/src/pages/StockMaster.tsx` 신규 (330L) / `frontend/src/App.tsx` (+4L, 7번째 메뉴 + Route + lazy import + MobileMenuLabel) / `frontend/src/test/handlers.ts` (+47L MSW 5 handler) / `e2e/fixtures/api-mocks.ts` (+35L LIFO 정합 wildcard + 5 구체) / `frontend/src/__tests__/AppShell.test.tsx` (PC 7개 갱신 + M-5 7개 갱신 + M-9 신규 + L-MENU + L-CYCLE81-MOBILE 신규) / `frontend/src/components/__tests__/_ast_useQuery_retry_required.test.ts` (G-AST-RT TARGET_PAGES 확장 +1L) / `frontend/src/components/__tests__/_ast_api_mocks_coverage.test.ts` (G-AST-MOCK G-AST6 신규 describe +25L) / `frontend/src/api/__tests__/stock-master.test.ts` 신규 (L-API 5 sub + H-404 + H-422 3 sub) / `frontend/src/pages/__tests__/StockMaster.test.tsx` 신규 (G-KST 2 + H-STATS + H-LIST 2 + H-DETAIL 2 + H-HISTORY + H-POLLING + L-CYCLE83-EMIT) / `tests/unit/e2e_mocks/test_cycle85_api_mocks_stock_master_lifo.py` 신규 (G-AST-LIFO 7 sub) / `_workspace/red/cycle85_stock_master_frontend.md` Red 명세 영구 기록 / `_workspace/cycle85_phase1_diagnosis.md` Phase 1 진단 영구 기록 / `_workspace/test_index.yaml` 영향 인덱스 갱신 (backend tests 366→367 / frontend modules 58→66 / frontend tests 34→41) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 85 행 | 사이클 83 후속 3 단계 분할 2/3 (프론트 단독) — stock_master UI 메뉴 구축 (사용자 요구 영속). team-leader Phase 1 진단 (5 의제 Q10~Q14) + domain-expert 자문 생략 (UI 레이어 한정). 23 사이클 연속 옵션 A 패턴 영속. 사이클 41 StrategyFunnel 4 카드 / 사이클 65 H3 retry:1 / 사이클 68 KST / 사이클 75 G-AST5 / 사이클 80 hotfix #3 LIFO / 사이클 81 M-1~M-8 햄버거 / 사이클 83 emit / 사이클 84 5 GET 라우트 ApiResponse 정합 영속 패턴 답습. G-AST-LIFO 영구 가드 = 미래 신규 라우트 추가 시 Playwright LIFO 위반 영구 차단 (사이클 80 hotfix 6 commit chain 답습). 매매 안전성 무영향 (UI 레이어 한정, READ-ONLY 5 GET 라우트). 부수 발견 (사이클 86+ 신규 카드 MEDIUM) = 사이클 83 메모리 leak 테스트 외부 DNS race (사이클 85 영역 무관 + 운영 무영향). Q14=C 영속 = 사이클 85 push + 사이클 86 (통합 검증) 별개 사이클. 사이클 86+ 후속 카드: 사이클 86 Playwright + 운영 실측 + 사이클 83 메모리 leak DNS 영구 차단 / 사이클 87+ retention cron / 사이클 83 운영 측정 (D+1) / #16 / auth 1.43x / #15 / #11/#12. |
| 2026-06-09 | 사이클 84 — **stock_master UI 메뉴 구축 백엔드 단독 단계** (사이클 83 후속 3 단계 분할 1/3, **22 사이클 연속 옵션 A 패턴 영속**). 사용자 결정 (Q1=B 신규 `stock_master_history` 테이블 + Q5=B 4 타입 INSERT/UPDATE/DELETE/TTL_REFRESH + Q6=A 전체 67 키 raw diff + Q7=B 90일 retention + Q8=A PostgreSQL trigger + Q9=B domain-expert 자문 생략). **시정 (production 5 파일)**: (1) `supabase/migrations/032_stock_master_history.sql` 신규 — 테이블 6 컬럼 + 인덱스 2종 + trigger 함수 PL/pgSQL (INSERT/UPDATE/TTL_REFRESH/DELETE 4 분기) + AFTER INSERT/UPDATE/DELETE trigger. **Supabase MCP `apply_migration` 운영 DB 적용 완료** (project `etaligxesjtjfkbntdve`, 사이클 30/31 답습). (2) `src/db/stock_master.py` (+97L) 헬퍼 4종 (`list_all` / `get_stats` / `list_history` / `count_eager_refresh_today`). (3) `src/routes/stock_master.py` 신규 5 GET 라우트 READ-ONLY (`/stats` + `/list` + `/scan-pool/summary` + `/{ticker}` 404 + `/{ticker}/history`, ApiResponse 래퍼 + 라우트 순서 정적 *전* 동적). (4) `src/main.py` (+2L) prefix `/api/stock-master`. (5) `src/routes/CLAUDE.md` 명세 추가. **회귀 가드 17 케이스 (16 파일 + 신규 디렉토리 `tests/unit/routes/`)**: HIGH 5 (M-1 migration 적용 + M-2~M-4 trigger 3 분기 + M-5 ApiResponse 정합) + MEDIUM 7 (H-1 페이징 + H-2 집계 + H-3 DESC + H-4 emit 카운트 + H-5 404 + H-6/H-7 422) + LOW 5 (L-1 KST + **L-2 AST READ-ONLY 영구 가드** PUT/POST/DELETE/PATCH 0건 + L-3 사이클 83 emit + L-4 사이클 72 dedupe 무영향 + L-5 사이클 32 R4 무영향). **백엔드 2120 → 2151 PASS + 2 XFAIL + 2 skip** (+31 = 신규 17 + 사이클 83 이월 14) / **flakiness 0** (3 회 49.27~49.71s ±0.4%) / tester verify Supabase MCP READ-ONLY = 테이블 + 인덱스 3종 + trigger 3 이벤트 활성 + 초기 history_count=0 + 운영 stock_master=29건. **영속 의무 매트릭스 전수 영속**: 사이클 31 R6 / 32 R4 / 38 명문화 / 64 Q1 옵션 D / 72 dedupe (trigger 가 `_DbLogHandler` 외부 영역) / 78 flush / 79 G-AST1 / 83 emit. CLAUDE.md "절대 깨지 말 것" 8 영역 전수 영속. 매매 안전성 무영향 (READ-ONLY GET + trigger INSERT). **운영 효과**: stock_master_history 가 모든 upsert 사이트 자동 추적 + 사이클 81 silent 결함류 미래 영구 차단 + 액면분할/상장폐지 자동 감지. **사이클 49→84 누적 39 사이클 + hotfix 11 영역**. **사이클 85+ 후속 카드 인계**: 사이클 85 (프론트 단독) 7번째 메뉴 `종목마스터` + `StockMaster.tsx` + 5 GET 라우트 호출 + retry:1 + 모바일 햄버거 7개 압축 / 사이클 86 (통합 검증) Playwright + e2e api-mocks LIFO + 운영 history INSERT 실측 / 사이클 87+ 90일 retention cron (사이클 6 답습) / 사이클 83 운영 측정 (2026-06-10 수 09:00~) / #16 / auth 1.43x / #15 / #11/#12. | `supabase/migrations/032_stock_master_history.sql` 신규 / `src/db/stock_master.py` (+97L) / `src/routes/stock_master.py` 신규 / `src/main.py` (+2L) / `src/routes/CLAUDE.md` / `tests/unit/db/test_cycle84_*.py` 9 파일 / `tests/unit/routes/test_cycle84_*.py` 4 파일 + `__init__.py` / `tests/unit/engine/test_cycle84_*.py` 2 파일 / `tests/unit/ast/test_cycle84_ast_no_update_route.py` / `_workspace/red/cycle84_stock_master_backend.md` Red 명세 / `_workspace/cycle84_phase1_diagnosis.md` + `_workspace/cycle84_verify_report.md` / `_workspace/test_index.yaml` (backend tests 349 → 366) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 84 행 | 사이클 83 후속 3 단계 분할 1/3 (백엔드 단독) — stock_master UI 메뉴 구축 (사용자 요구). team-leader Phase 1 진단 (5 의제 Q5~Q9) + domain-expert 자문 생략 (Q9=B). 22 사이클 연속 옵션 A 패턴 영속. Supabase MCP `apply_migration` 운영 DB 적용 완료. 매매 안전성 무영향. L-2 AST 영구 가드 = 미래 신규 라우트 추가 시 PUT/POST/DELETE/PATCH 0건 영구 차단 (Q9=B READ-ONLY 영구 보장). 운영 효과: stock_master_history 가 모든 upsert 사이트 자동 추적 + 사이클 81 silent 결함류 미래 영구 차단. 사이클 85+ 후속 카드 인계: 사이클 85 (프론트) / 86 (통합) / 87+ (cron) / 사이클 83 운영 측정 / #16 / auth 1.43x / #15 / #11/#12. |
| 2026-06-09 | 사이클 83 — **#82-A 옵션 1 (MEDIUM) `_scan_loop` 후보 풀 ticker `stock_master` eager refresh 영역 확장** (사이클 82 진단 인계 후속, **21 사이클 연속 옵션 A 패턴 영속**). 사이클 82 진단 결정 (`stock_master` 운영 적재 14건 한정 → `_apply_price_filter` 가 `bfdy_clpr <= 0` 시 graceful 통과 → 후보 풀 ~90% 가 가격필터 우회, 사이클 81 시정의 실효성이 stock_master 적재 영역에 결정적 의존). 사용자 결정 (Q1=B `subscribe_filtered_stocks` 진입점 hook + Q2=C 백그라운드 task 5분 주기 + Q3=B 24h TTL + 50ms sleep + Q6=B 사이클 84 push 분리). domain-expert 자문 (MEDIUM, 사용자 결정 전수 일치, 영구 가드 10 케이스 HIGH 3 + MEDIUM 4 + LOW 3, KIS RPS 20 안전). **시정**: `src/engine/scanner.py` 신규 함수 4 + 모듈 전역 4 (`_SCAN_POOL_EAGER_REFRESH_WINDOW_SECS=300` + `_SCAN_POOL_RATE_LIMIT_SLEEP_SECS=0.05` + 2 collector) + `subscribe_filtered_stocks` hook 1줄 + `invalidate_price_filter_cache_scanner()` 격리 + `src/engine/scheduler.py` `start()` task 생성 + `stop()` task_attrs (사이클 79 답습) + `run_daily()` finally + 마지막 flush (사이클 78 답습) + `_scan_pool_eager_refresh_loop()` 메서드 + `src/db/system_config.py` `_price_filter_memory_override` 인메모리 폴백 graceful. **회귀 가드 10 케이스 (4 파일)**: HIGH 3 (G-AST1 stop task_attrs + G-AST2 `tradable_boards` 0 참조 + G-LC1 asyncio lifecycle) + MEDIUM 4 (G-TT1 24h TTL + G-TT2 50ms sleep + G-FL1 stop flush + G-ML1 5분 collector clear) + LOW 3 (G-OP1 SK스퀘어 fixture + G-OP2 emit + G-RT1 R6 회귀). 백엔드 2110 → **2120 PASS** (+10) / flakiness 0 (3 회 49.84~50.11s) / tester verify 2316 PASS / 회귀 0 / 매매 안전성 무영향. **silent 결함 영구 차단 20 회 누적** (사이클 60/64/65#1/65#2/65#3/66/67/68/72/73/77/77#2/78/79/80/80#2/80#3/80#4/81/**83**). 사이클 49→83 누적 38 사이클 + hotfix 11 영역. **운영 효과 예상**: stock_master 14 → 150+건 / `_apply_price_filter` 통과율 97% → 30~50% / `[price_filter_scanner_skip]` 10~50건/일 회복 / SK스퀘어 R6 폭주 270/일 → 0건. D+1 (2026-06-10 수) 09:00~10:00 1h tester verify 의무. | `src/engine/scanner.py` / `src/engine/scheduler.py` / `src/db/system_config.py` / `tests/unit/ast/test_cycle83_ast_task_cancel_required.py` + `test_cycle83_ast_tradable_boards_no_reference.py` / `tests/unit/engine/test_cycle83_*.py` 8 파일 / `tests/unit/engine/test_scheduler_stop_zombie_tasks.py` (expected_members 11종) / `_workspace/red/cycle83_scan_pool_eager_refresh.md` + `_workspace/cycle82_c2_diagnosis.md` + `_workspace/cycle83_phase1_diagnosis.md` + `_workspace/cycle83_phase2_domain_consult.md` + `_workspace/cycle83_verify_report.md` + `_workspace/stock_master_ui_plan.md` + `_workspace/test_index.yaml` (339 → 349) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 83 행 | 사이클 82 진단 인계 후속 #82-A (MEDIUM 신규) 영구 시정 — 사이클 81 시정 실효성이 stock_master 적재 영역에 결정적 의존 → eager refresh 영역 확장. 21 사이클 연속 옵션 A 패턴 영속. team-leader Phase 1 진단 + domain-expert 자문. 매매 안전성 무영향 (사이클 38 명문화 + 사이클 31 R6 / 32 R4 / 64 Q1 옵션 D + 사이클 79 task cancel + 사이클 78 flush 영속). silent 결함 영구 차단 20 회 누적. Q6=B 영속 (사이클 84 push 분리, 사이클 83 push commit `6dfe070` 완료). 사이클 84+ 후속 카드: stock_master UI 메뉴 구축 (사이클 84/85/86 3 단계 분할) + 사이클 78 동행 측정 + #16 / auth 1.43x / #15 / #11/#12 / C-4 / C-5. |
| 2026-06-08 | 사이클 81 — **사이클 64 silent 결함 (price_filter 키 오타) + 사이클 65 거래대금 필터 폴백 폐기 + UI 모바일 메뉴 침범 시정** (사용자 보고 2 영역 동시, **20 사이클 연속 옵션 A 패턴 영속**). **영역 1 (HIGH, 매매 안전성)**: 사용자 보고 = SK스퀘어 (402340) 현재가 1,122,340원 (사용자 설정 `price_filter_max=500,000원` 2배 초과) 가 funnel "최종 prepared 12" 통과. team-leader 진단 후 domain-expert 자문 (사이클 64 Q2 재검토) + stock_master 입수 chain 정밀 진단 (사용자 추가 요구). **결정적 발견**: 사이클 64 Q2 자문 옵션 A 채택 시 KIS API 응답 키 명명 가정 오류 — `_apply_price_filter` (`src/engine/scanner.py:166`) 가 `basics.raw.get("prdy_clpr", 0)` 호출하나, **CTPF1002R 응답 정본 키 = `bfdy_clpr`** (**B**e**f**o**r**e**dy** **cl**ose **pr**ice = 전일종가). stock_master 67 키 전수 검증 (운영 실측) = `prdy_clpr` ✗ / `bfdy_clpr` ✓ → `_apply_price_filter` 가 `prdy_clpr=0` 영구 → 100% graceful 통과 → 가격필터 무용. 운영 실측 = `[price_filter_scanner_skip]` **0건 / 30일+** (사이클 64 배포 이후 전체) + `[risk_silent_skip]` 270건/일 (사이클 31 R6 마지막 안전망 발화) = LTV 매수 평가까지 도달 (`total_investment=120,414원` vs SK스퀘어 1,122,340원 = 사이클 31 R6 차단). **거래대금 필터 동일 결함**: `_get_acml_tr_pbmn` (L336) 2순위 폴백 `acml_tr_pbmn` 도 CTPF1002R 에 없음 (시세성 API 키). 1순위 (scanner `fetch_rising_stocks` 등락률 15%+ 보강) 단독 가능. **사용자 결정 채택**: C-1 (가격필터 1줄) + C-3 (거래대금 필터 폴백 폐기) 동시 시정 (domain-expert 권고). **시정 (`src/engine/scanner.py` -7L 순감)**: (1) L166 `prdy_clpr` → `bfdy_clpr` (1줄) + L188~198 로그 문자열 동행 (2줄) + L224 funnel excluded_sample 키 동행 (1줄) + L331~350 거래대금 필터 2순위 폴백 전체 폐기 (-9줄, `_sm_mod.get` + `acml_tr_pbmn` 분기 제거 + ImportFrom 제거). **회귀 가드 17 케이스 (4 파일)**: A 키 정정 검증 (2) + B above_max 차단 SK스퀘어 시나리오 (3) + C graceful 통과 영속 (2) + D 보유 보호 영속 (1) + E funnel + DailyEmitCap (2) + F 거래대금 폴백 폐기 (2) + AST G-AST1 `prdy_clpr` 0건 + `bfdy_clpr` ≥1 (2) + F-2 AST `acml_tr_pbmn` 폴백 0건 + stock_master 호출 0건 (2) + G 운영 실데이터 005930 fixture (1). 기존 사이클 64/65 mock 4 파일 raw 키 갱신 (`prdy_clpr` → `bfdy_clpr`) + `test_cycle65_get_acml_tr_pbmn_fallback.py::B-5-2` xfail 마킹 (사이클 66 K-2 의미 전환 패턴 답습, 폐기 기능 결함 영속 보존). **백엔드 2094 → 2110 PASS + 2 XFAIL + 2 skip** (+16 = 신규 17 - 폴백 케이스 1 xfail 전환) / **flakiness 0** (3 회 0.12~0.16s). 회귀 0 / 매매 안전성 무영향 (scanner 단계 매수 진입 전용, 사이클 38 명문화 영속 + 사이클 64 Q1 옵션 D 3중 안전망 영속 + 사이클 31 R6 / 32 R4 영속). **영역 2 (UI, frontend-dev 단독)**: iPhone 모바일 viewport 상단 헤더 메뉴 6개 (환경/보드/내역/AI자문/검색/추적) 가 컨텐츠 본문 overlap. 결함 원인 = `App.tsx::AppShell` `<div className="flex gap-4">` 반응형 클래스 전무 + 모바일 (375px) 메뉴 텍스트 세로 배치 + `<main>` padding-top 보상 없음. **시정 (옵션 A 햄버거 메뉴, 사용자 표준 패턴, `frontend/src/App.tsx` +53L 순증 110→179L)**: PC (sm: ≥640px) 가로 메뉴 + `whitespace-nowrap` + `shrink-0` / 모바일 (< sm) 햄버거 SVG 버튼 + `mobileOpen` state + Drawer + `MobileMenuLabel` 헬퍼 (useLocation 현재 경로 한글 레이블) + `data-testid="nav-sticky-wrapper"` + `aria-label`/`aria-expanded` ARIA. **회귀 가드 8 케이스 (`frontend/src/__tests__/AppShell.test.tsx` +82L)**: 기존 2 케이스 갱신 (testid + getAllByRole) + 신규 M-1~M-8 (햄버거 버튼 존재 / 초기 숨김 / 열기 / aria-expanded / 드로어 6 메뉴 / 클릭 후 닫힘 / 토글 닫힘 / main 독립 DOM 레벨). 프론트 188 → **196 PASS** (+8) / 회귀 0. UI 레이어 한정 매매 hot path 무영향. **CLAUDE.md "절대 깨지 말 것" 8 영역 영속** (체결통보 H0STCNI0/H0STCNI9 / uvicorn 단일 워커 / 주문번호 매핑 + race 가드 / `_reset_daily_state` / 익일 청산 30s 안정화 / NXT 매도 거부 좀비 차단 + SellRejectionTracker / WebSocket 4중 안전망 / KIS 거부 응답 + KST 강제 + 사이클 38 명문화). **silent 결함 영구 차단 19 회 누적** (사이클 60/64/65#1/65#2/65#3/66/67/68/72/73/77/77#2/78/79/80/80#2/80#3/80#4/**81**). **운영 효과 예상 (push 후)**: `[price_filter_scanner_skip]` 0건/30일+ → 정상 발화 (영업일 ~10~50건/일 영역, 작전주/초고가 종목 자동 차단) + `[risk_silent_skip]` SK스퀘어 폭주 270건/일 → 0건 (가격필터 정상화로 R6 우회 시나리오 자연 차단) + dup_factor 추가 감소 + UI 모바일 viewport 안정. **사이클 49→81 누적 36 사이클 + hotfix 11 영역**. **사이클 82+ 후속 카드 인계**: C-2 (HIGH 별개 사이클) `_scan_loop` 후보 풀 raw 보강 영역 확장 + C-4 (LOW) `[risk_silent_skip]` DailyEmitCap 폭주 검증 + C-5 (LOW) 운영 가시화 `[price_filter_scanner_pass_no_data]` emit + 사이클 78 `[swing_rest_poll_summary]` 실증 측정 (다음 영업일 2026-06-09 화요일 09:30~15:20) + #16 (MEDIUM Q6-3 2026-06-13 이후) + auth 1.43x. | `src/engine/scanner.py` (-7L 순감, L166 `prdy_clpr` → `bfdy_clpr` 가격필터 키 시정 + L188~198 로그 동행 + L224 funnel excluded_sample 키 동행 + L331~350 거래대금 필터 2순위 폴백 폐기 -9L + ImportFrom 제거) / `tests/unit/engine/scanner/test_cycle81_price_filter_key_fix.py` 신규 (10 케이스 A 2 + B 3 + C 2 + D 1 + E 2) / `tests/unit/engine/scanner/test_cycle81_trade_amount_filter_fallback_removal.py` 신규 (2 케이스 F-1 + F-3) / `tests/unit/ast/test_cycle81_ast_price_filter_key.py` 신규 (4 케이스 A-3 + A-3-BIS + F-2 + F-2-BIS AST 정적 가드) / `tests/unit/engine/scanner/test_cycle81_real_data_regression.py` 신규 (1 케이스 G-1 005930 fixture) / `tests/unit/engine/test_cycle64_price_filter_scanner_apply.py` + `test_cycle64_price_filter_scanner_emit_cap.py` + `test_cycle64_price_filter_scanner_protected.py` + `tests/integration/test_cycle64_price_filter_scanner_integration.py` (mock raw 키 `prdy_clpr` → `bfdy_clpr`) / `tests/unit/engine/scanner/test_cycle65_get_acml_tr_pbmn_fallback.py::B-5-2` xfail 마킹 (사이클 66 K-2 의미 전환 패턴 답습) / `frontend/src/App.tsx` (+53L 순증, AppShell 햄버거 메뉴 + Drawer + MobileMenuLabel + aria) / `frontend/src/__tests__/AppShell.test.tsx` (+82L, 기존 2 갱신 + 신규 M-1~M-8 8 케이스) / `_workspace/red/cycle81_price_filter_key_fix.md` Red 명세 영구 기록 / `_workspace/test_index.yaml` 영향 인덱스 갱신 (backend modules 97 / backend tests 339) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 81 행 | 사이클 64 시점 silent 결함 (Q2 자문 옵션 A KIS API 응답 키 명명 가정 오류) 영구 시정 — 단일 근본 원인 (`prdy_clpr` ≠ `bfdy_clpr`) 의 1 줄 시정. team-leader 진단 (보유/익일청산 raw 광범위 NULL 가설) + domain-expert 자문 (CTPF1002R 응답 정본 키 `bfdy_clpr` 발견) + stock_master 입수 chain 정밀 진단 (사용자 추가 요구) = 가설 (1) raw 광범위 NULL 거짓 알람 + 진짜 결함 (2) 키 오타 확정. 영역 2 UI 모바일 메뉴 침범 = frontend-dev 단독 시정 (App.tsx 햄버거 메뉴, 사용자 표준 패턴). 20 사이클 연속 옵션 A 패턴 영속. 매매 안전성 무영향 (scanner 단계 매수 진입 전용 + 사이클 38 명문화 + 사이클 64 Q1 옵션 D 3중 안전망 영속 + 사이클 31 R6 / 32 R4 영속). 운영 효과 예상: `[price_filter_scanner_skip]` 0건/30일+ → 정상 발화 + SK스퀘어 폭주 270건/일 → 0건. silent 결함 영구 차단 19 회 누적. 사이클 49→81 누적 36 사이클 + hotfix 11 영역. 후속 카드 사이클 82+ 인계: C-2 (HIGH) `_scan_loop` 후보 풀 raw 보강 영역 확장 + C-4 (LOW) `[risk_silent_skip]` DailyEmitCap 폭주 + C-5 (LOW) `[price_filter_scanner_pass_no_data]` emit + 사이클 78 `[swing_rest_poll_summary]` 실증 측정 (2026-06-09 화요일 09:30~15:20) + #16 / auth 1.43x / #15 / #11/#12 / Q6-2 옵션 B 헬퍼 영속. |
| 2026-06-08 | 사이클 80 hotfix 4 단계 통합 — **사이클 79 e2e fail → 사이클 65 hotfix #2 가드 잘못된 FIFO 가정 영구 시정 (Playwright LIFO 정합)** (**19 사이클 연속 옵션 A 패턴 영속**). 사이클 79 1차 + 재실행 모두 e2e fail (flaky 아닌 진짜 결함 확정) → **결함 chain 통찰**: 단일 근본 원인 (사이클 65 hotfix #2 시점 가드가 FIFO 가정으로 작성됨, 실제 Playwright = LIFO) 의 6 commit 진화. **hotfix #1 (`9d20012`)** Settings.tsx L43 + L692 useQuery retry:1 명시 + AST G-AST4 영역 확장 (TARGET_PAGES `Settings.tsx` 신규, 사이클 65 H1 답습) — trace `/api/strategies` 호출 발생 = retry 효과 부분 입증. **hotfix #2 (`c9c3b51`)** settings.spec.ts toBeVisible 10s → 20s (사이클 65 hotfix #3 답습) — 20s 도 fail = 단순 timeout 아닌 진짜 throw 확정. **hotfix #3 (`2f74673`) 근본 결함 시정 (옵션 C 정밀 조사 결과)**: trace console warning `<PriceFilterCard> component error` 발견 → trace resources/ sha 비교로 `/api/system/price-filter` mock 응답 = `{success: true, data: {}, message: ""}` (wildcard envelope({})) 확정 → **Playwright route 매칭 = LIFO** ("latest registered route wins") → `e2e/fixtures/api-mocks.ts` L291 wildcard `**/api/system/**` 함수 끝 등록이 가장 우선 매칭 → L196 price-filter / L201 trade-amount-filter 구체 라우트 무효화 → envelope({}) → min_price undefined → React controlled input throw → Error Boundary 없음 → 전체 페이지 unmount → `상한가 모멘텀` 미렌더 → toBeVisible 20s timeout. 시정: L291 wildcard 제거 + L190 wildcard 구체 라우트 *전* 등록 (LIFO 라 fallback 역할). e2e ✅ success. **hotfix #4 (`d65e3f2`)**: `tests/unit/e2e_mocks/test_api_mocks_routes_registered.py::test_e2e_api_mocks_specific_routes_registered_before_wildcard` 가 FIFO 가정 (`price_filter_pos < wildcard_pos`) 으로 작성됨 (사이클 65 hotfix #2 시점) → 사이클 80 hotfix #3 LIFO 시정과 충돌 → backend-test fail. 가드 시정: 함수명 `before_wildcard` → `after_wildcard` + assertion 방향 반전 (`>`) + docstring Playwright LIFO 명시 + 사이클 80 hotfix #3 시정 의도 영구 가드. **검증**: 사이클 80 hotfix #4 CI = backend-test ✅ + frontend-test ✅ + e2e ✅ + docker-build ✅ + backend-build ✅ + frontend-build ✅ = **6/6 job 전수 success**. 백엔드 2089 → 2094 PASS + 1 XFAIL + 2 skip (사이클 79 +5 G-CC1~CC3/G-LC1/G-AST1) / 프론트 187 → 188 PASS (+1 사이클 80 hotfix #1 G-RT 신규) / 합계 2284 PASS. **silent 결함 영구 차단 18 회 누적** (사이클 60/64/65#1/65#2/65#3/66/67/68/72/73/77/77#2/78/79/80/80#2/80#3/80#4). 변경 통계 (4 hotfix 누적): `frontend/src/pages/Settings.tsx` (+2L retry:1) + `frontend/src/components/__tests__/_ast_useQuery_retry_required.test.ts` (+39L TARGET_PAGES describe) + `e2e/settings.spec.ts` (+5L/-2L timeout 20s) + `e2e/fixtures/api-mocks.ts` (+16L/-4L LIFO 정합 wildcard 재배치) + `tests/unit/e2e_mocks/test_api_mocks_routes_registered.py` (+15L/-8L FIFO → LIFO 정합). 사이클 65 hotfix #1/#2/#3 영속 (변경 0) + 사이클 75 G-AST1~AST5 영속 + 사이클 77 hotfix 2단 영속 + 사이클 78/79 영속 + **사이클 17/18/29/38/55 R-1/66/67/68/72/73/74/75/76/77/78/79 모두 무영향**. CLAUDE.md "절대 깨지 말 것" 8 영역 영속 (변경 영역 = e2e fixtures + frontend retry + AST 가드 한정, production 무관). 운영 영향 0. **사이클 65 hotfix #2 시점 가드 가정 결함 영구 차단 = 미래 동일 패턴 재발 영구 차단** (Playwright LIFO 정합 AST 가드). 사이클 49→80 누적 35 사이클 + hotfix 11 영역. **사이클 81+ 후속 카드 인계**: 사이클 78 `[swing_rest_poll_summary]` 실증 측정 (다음 영업일 2026-06-09 화요일 09:30~15:20) + #16 (MEDIUM Q6-3 2026-06-13 이후 후보 풀 폭축 회고) + #15 (LOW Q7-2 액면분할 prdy_clpr invalidate) + #11/#12 보드 mutex / 14 모듈 logger + Q6-2 옵션 B 헬퍼 (사이클 71+) + auth 1.43x (LOW) 영속. | `frontend/src/pages/Settings.tsx` (+2L, L43 + L692 useQuery retry:1 명시) / `frontend/src/components/__tests__/_ast_useQuery_retry_required.test.ts` (+39L, TARGET_PAGES = ['Settings.tsx'] 신규 + it.each describe + 사이클 80 hotfix #1 영구 가드) / `e2e/settings.spec.ts` (+5L/-2L, toBeVisible timeout 10s → 20s + 주석 사이클 80 hotfix #2 답습 명시) / `e2e/fixtures/api-mocks.ts` (+16L/-4L, L291 wildcard 제거 + L190 wildcard 구체 라우트 *전* 등록 + 주석 Playwright LIFO 영구 가드 명시) / `tests/unit/e2e_mocks/test_api_mocks_routes_registered.py` (+15L/-8L, 함수명 `before_wildcard` → `after_wildcard` + assertion 방향 반전 + docstring Playwright LIFO + 사이클 80 hotfix #3 시정 의도 명시) / `frontend/CLAUDE.md` 사이클 80 hotfix 4 단계 통합 본문 추가 / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 80 hotfix 통합 행 | 사이클 79 1차 + 재실행 모두 fail (flaky 아닌 진짜 결함 확정) → 옵션 C 정밀 조사 (Playwright trace console error + resources sha 비교) → 단일 근본 원인 (사이클 65 hotfix #2 시점 가드 잘못된 FIFO 가정) 의 6 commit chain 영구 차단. Playwright LIFO 정합 AST 가드 신설 (사이클 80 hotfix #4 = `before_wildcard` → `after_wildcard` 함수명 변경 + assertion 반전 + docstring) = 미래 동일 패턴 재발 영구 차단 패턴 신설. 19 사이클 연속 옵션 A 패턴 영속 (55 R-1 / 60 / 62~69 / 72~80). 매매 안전성 무영향 (e2e + frontend retry + AST 가드 영역 한정, production 무관). 사이클 65 hotfix #1/#2/#3 + 사이클 75 G-AST1~AST5 + 사이클 77 hotfix 2단 + 사이클 78/79 모두 영속. silent 결함 영구 차단 18 회 누적. 후속 카드 사이클 81+ 인계: 사이클 78 [swing_rest_poll_summary] 실증 측정 (다음 영업일 2026-06-09 화요일 09:30~15:20) + #16 (MEDIUM Q6-3 2026-06-13 이후) / #15 (LOW 액면분할) / #11/#12 / Q6-2 옵션 B 헬퍼 / auth 1.43x 영속. |
| 2026-06-08 | 사이클 79 — 카드 **#20 (LOW) `_api_recovered_collector_task` cancel 영구 가드** (사이클 78 부차 발견 영속 시정). team-leader 진단 생략 (LOW + 단순 영역) + tdd-engineer Red → backend-dev Green 직행 = **18 사이클 연속 옵션 A 패턴 영속**. **부수 발견 (Red 단계 AST 수집)**: `scheduler.py` 내 `asyncio.create_task(...)` 패턴 attribute **10종** 도입 vs `stop()` + `finally` 블록 task_attrs 튜플 **9종** → 누락 = `_api_recovered_collector_task` (사이클 76 도입 시 누락 silent 결함). **시정 영역 2곳 (사이클 13-E-2 명세 답습 = 양쪽 동일 목록 통일)**: (1) `scheduler.py::stop()` task_attrs 튜플 (L866) `"_api_recovered_collector_task"` 1줄 추가 + (2) `scheduler.py::run_daily()` `finally` 블록 task_attrs 튜플 (L742) 동일 1줄 추가. **부차 hotfix**: `test_scheduler_stop_zombie_tasks.py::_extract_task_tuple_members` 정규식 `[^)]+` 이 주석 괄호 `(사이클 76 도입, ...)` 의 `)` 캡처 잘림 결함 동행 시정 (주석 괄호 → 대시 `—` 통일, 영구 차단). **회귀 가드 5 케이스 (3 파일)**: G-CC1 `stop()` AST `_api_recovered_collector_task` 참조 ≥1건 + G-CC2 `stop()` task_attrs 튜플 내 문자열 리터럴 + G-CC3 scheduler.py 전체 ≥2건 (stop + finally 양쪽) + G-LC1 asyncio mock `stop()` 호출 시 cancel 1회 + setattr None + **G-AST1 영구 가드** (`create_task` 패턴 attribute = stop task_attrs 튜플 차집합 = ∅ 정적 검증, 미래 신규 task 추가 시 cancel 목록 누락 영구 차단). **백엔드 2089 → 2094 PASS + 1 XFAIL + 2 skip** (+5 신규) / **flakiness 0** (3 회 0.15s) / 회귀 0 / 매매 안전성 무영향 (lifecycle 영역, 매매 hot path 무관). 사이클 17/29/38/55 R-1/66/67/68/72/73/74/75/76/77/78 모두 무영향. **사이클 78 부차 발견 영구 시정 완료**. **사이클 49→79 누적 31 사이클 + hotfix 7 영역**. | `src/engine/scheduler.py` (+2L, L742 finally + L866 stop() 양쪽 task_attrs 튜플에 `_api_recovered_collector_task` 추가) / `tests/unit/engine/test_scheduler_stop_zombie_tasks.py` (`expected_members` +1L + 주석 9종→10종 갱신 + 주석 괄호 → 대시 정규식 간섭 영구 차단) / `tests/unit/engine/test_cycle79_collector_task_cancel.py` 신규 (G-CC1~CC3, 3 케이스) / `tests/unit/engine/test_cycle79_task_lifecycle.py` 신규 (G-LC1, 1 케이스 asyncio mock) / `tests/unit/ast/test_cycle79_ast_task_cancel_required.py` 신규 (G-AST1, 1 케이스 영구 가드) / `_workspace/red/cycle79_task_cancel_guard.md` Red 명세 영구 기록 / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 79 행 | 사이클 78 부차 발견 (`stop()` task cancel 목록 `_api_recovered_collector_task` 누락) 영구 시정. team-leader 진단 생략 = 단순 영역 + LOW 위급도 (운영 중 `_running=False` → loop 자연 종료로 즉각 위험 낮음). 부수 발견 = 사이클 76 도입 시 silent 결함 (cancel 목록 누락 + 정규식 주석 간섭) 양쪽 영구 차단. AST G-AST1 = 미래 신규 task 추가 시 cancel 목록 누락 silent 결함 영구 차단 패턴 신설. 18 사이클 연속 옵션 A 패턴 영속. 매매 안전성 무영향 (lifecycle 영역). silent 결함 영구 차단 14 회 누적 (13 + 사이클 79 cancel 누락). 후속 카드 사이클 80+: 사이클 78 [swing_rest_poll_summary] 실증 측정 (다음 영업일 09:30~15:20) + #16 (MEDIUM Q6-3 2026-06-13 이후) / auth 1.43x / #15 (LOW 액면분할) / #11/#12 / Q6-2 옵션 B 헬퍼 영속. |
| 2026-06-08 | 사이클 78 — **사이클 74 도입 누락 silent 결함 시정** (flush 호출 사이트 0건 + 메모리 leak 영구 차단, **17 사이클 연속 옵션 A 패턴 영속**). team-leader Phase 1 + C 진단 (Supabase READ-ONLY) 후 사용자 결정 **A + B + C 채택**. **결정적 발견**: 카드 #19 (realtime_other 16.13x, 사이클 73 인계) = **거짓 알람 확정 → 종결** (배포 이전 데이터). 진짜 결함 = 사이클 74 commit (`0017fe0`) 가 `record_swing_rest_poll` + `flush_swing_rest_poll_collector` + `record_stale_watcher_check` + `flush_stale_watcher_collector` 함수 도입했으나 5분 주기 호출 사이트 누락 → `[swing_rest_poll_summary]` + `[stale_watcher_summary]` 영구 0건 + collector 무한 누적 (메모리 leak HIGH). 사이클 76 commit (`0d633a8`) 가 `_api_recovered_collector_loop` task 도입했으나 swing/stale flush 도 함께 호출해야 했는데 누락 (사이클 76 도 부분 시정). **C 진단 (Supabase READ-ONLY) 결과**: `[stale_watcher]` 개별 마지막 14:08:57 KST (사이클 74 배포 14:13 KST 이전) + `[swing_rest_poll]` 개별 마지막 14:09:27 KST = 모두 배포 후 emit 0건 = 사이클 74 collector 흡수 정상 작동 확정 = 카드 #19 거짓 알람 종결. **메모리 leak 위험 (HIGH 위급도)**: `_swing_rest_poll_collector: list[dict]` + `_stale_watcher_collector: list[dict]` 무한 누적 (60s/120s 주기 × 일일 ~720+360건). **시정 (옵션 1 = 사이클 76 task 재사용, `src/engine/scheduler.py` +26L)**: `_api_recovered_collector_loop` 본체 (L2469~L2480 영역) 에 `flush_stale_watcher_collector` import + swing/stale flush 4 줄 (try/except 각각) + `if not self._running: break` 를 flush 블록 *이후*로 이동 (sleep 후 무조건 1회 flush 보장, 사이클 42 답습) + `stop()` lifecycle hook (L875 영역, `unsubscribe_all()` 직전) 양쪽 flush try/except (Q5 사이클 74 답습 잔여 카운터 손실 방지). **회귀 가드 6 케이스 (3 파일)**: G-FL1~FL4 flush 호출 사이트 (G-FL1 `flush_swing_rest_poll_collector` 호출 ≥1건 + G-FL2 `flush_stale_watcher_collector` 호출 ≥1건 + G-FL3 `stop()` lifecycle hook 양쪽 flush + G-FL4 5분 주기 task 함수에 양쪽 flush co-located) + G-ML1 메모리 leak 차단 (5분 윈도우 종료 후 `len == 0` freezegun) + **G-AST1 영구 가드** (`record_*` 정의 모듈의 대응 `flush_*` 호출 사이트 ≥1건 정적 검증, 미래 신규 collector 추가 시 flush 호출 누락 영구 차단). **백엔드 2083 → 2089 PASS + 1 XFAIL + 2 skip** (+6 신규) / **flakiness 0** (3 회 0.21~0.22s) / 회귀 0. **사이클 17/18/29/38/55 R-1/66/67/68/72/73/74/75/76/77 모두 무영향** (로깅 영역 시정만, 매도/손절/Trailing/익일청산/15:20 강제청산 영향 0). **부차 발견 카드 #20 (LOW)**: `stop()` task cancel 목록 (L860~865) 에 `_api_recovered_collector_task` 누락 (좀비 task 위험, 운영 중 `_running=False` → loop 자연 종료로 즉각 위험 낮음, 본 사이클 범위 외). **운영 효과 예상 (push 후)**: `[swing_rest_poll_summary]` 5분 주기 emit 정상화 (0건 영구 → ~144건/day) + `[stale_watcher_summary]` 동일 (0건 → ~144건/day) + collector 메모리 누적 영구 차단 + dup_factor 추가 감소 (사이클 74 의도 완전 작동). **silent 결함 영구 차단 13 회 누적** (사이클 60/64/65#1/65#2/65#3/66/67/68/72/73/77/77#2/**78**). **사이클 77 hotfix 2단 통합 영속** (e2e flaky 영구 차단 + market-regime/current mock schema 정확 매칭). **카드 #19 (realtime_other 16.13x) 사이클 73 인계 = 사이클 78 C 진단으로 거짓 알람 확정 = 종결**. **사이클 79+ 후속 카드**: #20 (LOW 부차) `stop()` task cancel 영구 가드 + #16 (MEDIUM Q6-3 2026-06-13 이후 후보 풀 폭축 회고) / #21/#22 (LOW WS) / auth 1.43x. | `src/engine/scheduler.py` (+26L / -2L, `_api_recovered_collector_loop` 본체 swing/stale flush 4 줄 + `flush_stale_watcher_collector` import + `if not self._running: break` 위치 이동 + `stop()` lifecycle hook 양쪽 flush) / `tests/unit/engine/test_cycle78_flush_call_sites.py` 신규 (G-FL1~FL4, 4 케이스) / `tests/unit/engine/test_cycle78_memory_leak_prevention.py` 신규 (G-ML1, 1 케이스 freezegun) / `tests/unit/ast/test_cycle78_ast_flush_required.py` 신규 (G-AST1, 1 케이스 미래 신규 collector 누락 영구 차단) / `_workspace/red/cycle78_flush_missing.md` Red 명세 영구 기록 / `_workspace/test_index.yaml` 영향 인덱스 갱신 (백엔드 +3 = 332) / `src/engine/CLAUDE.md` 사이클 74 본문 *위*에 사이클 78 명세 추가 / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 78 행 | 사이클 78 C 진단 (Supabase READ-ONLY) 으로 카드 #19 거짓 알람 종결 + 사이클 74 도입 누락 silent 결함 (flush 호출 사이트 0건 = 메모리 leak HIGH) 확정 영구 시정. 17 사이클 연속 옵션 A 패턴 영속. team-leader 진단 단계에서 *진짜 결함 발견* = silent 결함 13 회 누적. 매매 안전성 무영향 (로깅 영역 시정만, 매도 hot path 영향 0). 사이클 17 KIS LMS chain / 사이클 29 005935 chain 진단 / 사이클 38 명문화 / 사이클 55 R-1 / 사이클 66 priority / 사이클 67 분해 / 사이클 68 KST / 사이클 72 dedupe / 사이클 73 R-1/R-2/S-1 / 사이클 74 WS sampling / 사이클 75 e2e flaky 영구 차단 / 사이클 76 api retry 5.80x / 사이클 77 hotfix 2단 모두 영속. 운영 효과 예상: `[*_summary]` 정상 emit + 메모리 leak 영구 차단. 사이클 49→78 누적 30 사이클 + hotfix 7 영역. 후속 카드 사이클 79+ 인계: #20 (LOW 신규 부차) `_api_recovered_collector_task` cancel 영구 가드 + #16 (MEDIUM Q6-3 후보 풀 폭축 회고, 2026-06-13 이후) + #15 (LOW 액면분할) + #11/#12 + Q6-2 옵션 B 헬퍼 영속. |
| 2026-06-08 | 사이클 77 hotfix 2단 통합 — **e2e flaky 영구 차단** (사이클 75 G-AST1 영역 한계 + mock schema 미일치 silent 결함 2 결함 영구 시정). **hotfix #1 (`909ee4c`)**: 사이클 76 CI fail (1차 + 재실행 모두 fail = flaky 아닌 진짜 결함 확정) — `/api/market-regime/current` + `/api/realtime/subscriptions` ECONNREFUSED (Dashboard 영역, 사이클 75 Settings 한정 매핑 영역 외). 시정: `e2e/fixtures/api-mocks.ts` +26L 4 라우트 추가 (market-regime/current + history + auto-adjust + realtime/subscriptions) + AST G-AST5 신설 신규 describe `_ast_api_mocks_coverage.test.ts` +24L (`REQUIRED_DASHBOARD_ENDPOINTS` literal + `it.each` + `isRouteRegistered` 검증, 사이클 75 G-AST1 Settings 한정 한계 영구 차단). **hotfix #2 (`c647f2a`)**: hotfix #1 CI fail (ECONNREFUSED 해결됐으나 `getByText('상한가 모멘텀') not found` toBeVisible 10s timeout) — mock schema 미일치 silent 결함 (사이클 77 hotfix #1 추가 시 `is_buy_allowed` + `raw` 잘못 추가 + 누락 5 필수 필드 `buy_blocked` / `block_reason` / `auto_regime_adjust` / `cash_usage_ratio` / `enabled`) → `MarketRegimeCard` 컴포넌트 throw → Suspense 무한 로딩 → settings 페이지 렌더 차단. 시정: `api-mocks.ts` +7/-2 (잘못된 필드 2 제거 + 누락 5 필수 필드 추가, `MarketRegimeCurrent` interface `frontend/src/types/market_regime.ts` 정확 매칭). realtime/subscriptions = `data.data ??` graceful fallback 영속 (시정 불필요). 검증: 프론트 187 PASS / 사이클 75 17 AST 가드 영속 / 회귀 0. 운영 영향 0 (e2e 환경 한정 시정). silent 결함 영구 차단 12 회 누적 (사이클 60/64/65#1/65#2/65#3/66/67/68/72/73/77/77#2). | `e2e/fixtures/api-mocks.ts` (hotfix #1 +26L + hotfix #2 +7/-2 = 합 +31L / -2L, 4 라우트 추가 + market-regime/current schema 정확 매칭 5 필수 필드) / `frontend/src/components/__tests__/_ast_api_mocks_coverage.test.ts` (+24L, G-AST5 신규 describe + `REQUIRED_DASHBOARD_ENDPOINTS` literal + `it.each` + `isRouteRegistered`) / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 77 hotfix 2단 행 | 사이클 73 1차 + 사이클 74 1차 + 사이클 76 1차/재실행 = flaky → 진짜 결함 확정 chain. 사이클 77 hotfix 2단 진화 = ECONNREFUSED 해결 (hotfix #1) → toBeVisible timeout 해결 (hotfix #2, mock schema 정확 매칭). 사이클 75 G-AST1 Settings 한정 영역 한계 영구 차단 = G-AST5 Dashboard 영역 매핑 확장. 매매 안전성 무영향 (e2e + frontend retry 영역 한정). 사이클 65 hotfix #1/#2/#3 영속 + 사이클 75 17 AST 가드 영속. 사이클 78 (별도 행) 추가 도입 = silent 결함 12 → 13 회 누적. |
| 2026-06-08 | 사이클 76 — 카드 #23 (MEDIUM) **api retry 5.80x dup 시정** (사이클 74 발견 신규 결함, **15 사이클 연속 옵션 A 패턴 영속**). team-leader Phase 1 + 사용자 결정 채택: **결정 1 = E** (사이클 18 60s WARNING dedupe + 사이클 74 5분 recovered collector 하이브리드) + **결정 2 = Y** (ERROR 보존 매트릭스 R2/R4/R7/R8 individual 영속, 사이클 29 LMS chain 진단 의무) + **결정 3 = Q** (domain-expert 자문 생략 — 사이클 18+74 100% 답습) + **Q1/Q2/Q4** (AST 영구 가드 + 빈 윈도우 skip + 메인/풀 dedupe state 분리). **사이클 74 운영 실증 잔존 dup**: `api` 5.80x (29/5) + `[api_retry_recovered]` 7.00x (14/2) + `[api_retry_exhausted]` 4.00x (4/1). **결함 사이트 2 영역 (Phase 1-A 확정)**: 영역 1 `_request` (메인 단일 L321~484, 사이클 18 dedupe **미적용** → 5.80x 주요인) + 영역 2 `_request_via_quote_pool` (시세 풀 L604~734, 사이클 18 dedupe 영속 — recovered/exhausted 만 신규). **시정 (옵션 E)**: **신규 함수 7 (`src/api/base.py` +150L)** = (1) `_record_request_5xx_for_dedupe(path, status) -> bool` 메인 5xx WARNING 60s dedupe (사이클 18 답습, Q4 분리) + (2) `_record_api_recovered(path, attempts)` + (3) `_record_quote_recovered(path, attempts)` (Q4 분리) + (4) `_flush_api_recovered_collector()` (`[api_retry_recovered_summary] window=300s total=N by_path={...}` 1행, Q2 빈 윈도우 skip) + (5) `_flush_quote_recovered_collector()` + (6) `_warn_http_status(status, attempt, max_retries, path)` (G-AST1 통과용 헬퍼 추출) + (7) 모듈 상수 `_REQUEST_5XX_DEDUPE_WINDOW=60.0` / `_API_RECOVERED_COLLECTOR_WINDOW=300.0`. **State 3 분리 (Q4)**: `_request_5xx_dedupe: dict[(path, status), ...]` (메인) ↔ `_quote_5xx_dedupe: dict[(path, label, status), ...]` (사이클 18 영속) + `_api_recovered_collector` ↔ `_quote_recovered_collector`. **신규 task (`src/engine/scheduler.py` +34L)**: `_api_recovered_collector_loop` 5분 주기 (사이클 42 `_heartbeat_metrics_loop` 답습) — `connect()` 시점 task 시작 + `disconnect()` cancel + await + 마지막 flush 1회 (Q5 사이클 74 답습). **ERROR 보존 매트릭스 4 영역 individual 영속 (변경 0)**: R2 `[api_retry_exhausted]` 5xx 최종 실패 (L495) + R4 네트워크 최종 실패 (L523) + R7 토큰 만료 최종 실패 (L563) + R8 `[kis_rejection]` (L590 메인 + L846 quote, CLAUDE.md "절대 깨지 말 것" 영속). **AST 영구 가드 3 신설**: G-AST1 (`_request` 직접 `logger.warning("HTTP ...")` 0건, 헬퍼 경유만) + G-AST2 (사이클 18 `_record_5xx_for_dedupe` 호출 영속) + G-AST3 (`[api_retry_recovered]` 직접 write_log 0건). **회귀 가드 15 케이스 (4 파일)**: G-MD1~MD4 메인 60s dedupe (freezegun) + G-RC1~RC4 5분 collector (freezegun) + G-ERR1~ERR4 ERROR 영속 + G-AST1~AST3. **백엔드 2068 → 2083 PASS + 1 XFAIL + 2 skip** (+15) / 카테고리 분리: cycle76 15 / api/ 146 / engine/ 1183 + 1 XFAIL / ast/ 9 / 전체 2083 / **flakiness 0** (3 회 0.11~0.12s). 회귀 0 / 매매 안전성 무영향 (KIS API base 로깅 영역 시정 + ERROR individual 영속). **사이클 17 OPSP0002 backoff (`websocket.py`) 영속** (변경 0) + **사이클 18 풀 dedupe 영속** (G-AST2 PASS, `_QUOTE_5XX_DEDUPE_WINDOW=60.0` 변경 0) + **사이클 38/55 R-1/66/67/68/72/73/74/75 모두 무영향**. CLAUDE.md "절대 깨지 말 것" 8 영역 영속. 변경 통계 3 파일 +207L (base.py +150 + scheduler.py +34 + test_base_retry_logging.py +23) + 신규 가드 4 파일 (cycle76 api 3 + ast 1). **silent 결함 영구 차단 10 회 누적** (사이클 60/64/65#1/65#2/65#3/66/67/68/72/73) — 사이클 76 = 합법적 retry 빈도 흡수 영역. **운영 효과 예상 (push 후 V-11 SQL 측정)**: `api` 5.80x → 1.0~1.2 + `[api_retry_recovered]` 7.00x → 1.0 (5분당 1행) + 사이클 74 1.29x 대비 추가 감소. **사이클 77+ 후속 카드**: #19 (HIGH `realtime_other` 16.13x 사이클 73 인계) / #20 (LOW collector shared 헬퍼 추출 사이클 74+76 답습 = 2회, 사이클 77 신규 영역 추가 시 3회 추상화 규칙) / #21 (LOW `[ws_opsp_backoff_state]`) / #22 (LOW `[ws_restore_after_reconnect]`) / auth 1.43x. | `src/api/base.py` (+150L, 신규 헬퍼 7 + state 3 + 모듈 상수 2 + `_request` 호출자 정정 `_warn_http_status` + `_record_api_recovered` 사용 + `_request_via_quote_pool` `_record_quote_recovered` 추가) / `src/engine/scheduler.py` (+34L, `_api_recovered_collector_loop` task 5분 주기 + connect/disconnect lifecycle) / `tests/unit/api/test_base_retry_logging.py` (+23L, 기존 recovered 테스트 collector 경유로 갱신) / `tests/unit/api/test_cycle76_request_5xx_dedupe.py` 신규 (G-MD1~MD4, 4 케이스 freezegun) / `tests/unit/api/test_cycle76_recovered_collector.py` 신규 (G-RC1~RC4, 4 케이스 freezegun) / `tests/unit/api/test_cycle76_error_preservation_matrix.py` 신규 (G-ERR1~ERR4, 4 케이스 사이클 29 LMS chain 진단 의무) / `tests/unit/ast/test_cycle76_ast_api_retry_helper.py` 신규 (G-AST1~AST3, 3 케이스) / `_workspace/red/cycle76_api_retry_aggregation.md` Red 명세 영구 기록 / `src/api/CLAUDE.md` 본문 사이클 76 명세 추가 / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 76 행 | 사이클 74 발견 신규 결함 시정 (api retry 5.80x dup) — 사이클 18 60s WARNING dedupe + 사이클 74 5분 recovered collector 하이브리드 (옵션 E) 채택. 15 사이클 연속 옵션 A 패턴 영속. team-leader Phase 1-A 식별 2 영역 (메인 미적용 + 풀 영속) + ERROR 보존 매트릭스 4 영역 individual 영속 (사이클 29 005935 LMS chain 진단 의무). 매매 안전성 무영향 (KIS API base 로깅 영역만, `_request` 호출자 매매 hot path 영향 0 + `[kis_rejection]` CLAUDE.md 영속). 사이클 17 OPSP0002 backoff + 사이클 18 풀 dedupe + 사이클 38/55 R-1/66/67/68/72/73/74/75 모두 무영향. **운영 효과 예상**: api 5.80x → 1.0~1.2 + recovered 7.00x → 1.0 + 사이클 74 1.29x 대비 추가 감소. push 후 V-11 SQL 측정 의무. 사이클 49→76 누적 28 사이클 + hotfix 5 영역. 후속 카드 사이클 77+ 인계: #19 (HIGH realtime_other 16.13x 사이클 73) / #20 (LOW collector shared 헬퍼 추출 = 사이클 74+76 2회 답습, 3회 추상화 규칙 충족 시 사이클 77+ 신규 영역 추가 시) / #21/#22 (LOW WS) / auth 1.43x (LOW) / #16 (MEDIUM Q6-3 2026-06-13 이후 후보 풀 폭축 회고) / #15 (LOW 액면분할) / #11/#12 / Q6-2 옵션 B 헬퍼 영속. |
| 2026-06-08 | 사이클 75 — 카드 #19' (HIGH) **e2e api-mocks flaky 영구 차단** (사이클 73 1차 + 사이클 74 1차 = flaky 2회 누적 확정). team-leader Phase 1 분석 후 사용자 결정 채택: **C** (api-mocks 7 endpoint + AST 영구 가드) + **X** (frontend/src/api/*.ts literal vs api-mocks 대조) + **Q4 채택** (`retry: 1` 영역 확장 IntegrationToggleCard / CashUsageRatioCard / KisQuoteAccountsCard) = **14 사이클 연속 옵션 A 패턴 영속**. **결함**: `e2e/playwright.config.ts` 명시 "백엔드 미가동 — page.route 모킹 응답" 정책이나 Settings 페이지 마운트 컴포넌트 (IntegrationToggleCard 4 토글 + BuyBlockSection + CashUsageRatioCard + KisQuoteAccountsCard) 호출 endpoint 7 group 미등록 → vite proxy 가 backend ECONNREFUSED → React Query 전역 default retry 3회 누적 → settings.spec.ts timeout 초과 race. **시정**: `e2e/fixtures/api-mocks.ts` +53L 7 라우트 추가 (`**/api/strategies/system/cash-usage-ratio` + `**/api/integrations/{dkstock-regime,kis-mcp,auto-regime-adjust,auto-apply,buy-block,buy-block/thresholds}` + `**/api/integrations/quote-accounts**`) + 3 컴포넌트 `retry: 1` 영역 확장 5 위치 (CashUsageRatioCard:27 + IntegrationToggleCard:190,469 BuyBlockSection+ToggleRow + KisQuoteAccountsCard:87) + IntegrationToggleCard.test.tsx I8-G `waitFor` timeout 1000→3000ms 보정 + `setupToggleStubs` auto-apply 핸들러 추가 (Q4 retry:1 직접 사이드이펙트). **AST 영구 가드 17 케이스 신설** (사이클 65 H3 / 사이클 72 G-6 / 사이클 73 G-6.R/G-6.S / 사이클 74 G-7/G-8 패턴 답습): vitest 15 (`_ast_api_mocks_coverage.test.ts` 신규 10 — frontend/src/api/*.ts literal vs api-mocks 대조 G-AST1~AST3 + Q4 G-AST4 + `_ast_useQuery_retry_required.test.ts` 확장 5 = 사이클 65 H3 2 영속 + 사이클 75 3 신규 G-RT1~RT3) + pytest 2 (`tests/unit/e2e_mocks/test_cycle75_api_mocks_routes_registered.py` 신규 — 7 endpoint group 정적 grep). 회귀 가드 17 케이스 / 5 파일 +65L. **백엔드 2066 → 2068 PASS** (+2 pytest AST) + 1 XFAIL + 2 skip / **프론트 172 → 185 PASS** (+13 vitest AST + I8-G timeout 보정 회귀 0) / **flakiness 0** (3 회 pytest 0.01s + vitest 0.4~0.5s) / 회귀 0. 카테고리 분리: cycle75 17 PASS / 사이클 65~74 AST 누적 225 PASS / 백엔드 전체 2068 / 프론트 전체 185 = **합계 2253 PASS + 1 XFAIL + 2 skip**. 사이클 65 hotfix #1/#2/#3 영속 (변경 0). 운영 영향 0 (e2e + frontend retry 영역 한정). **CLAUDE.md "절대 깨지 말 것" 8 영역 영속** (체결통보 H0STCNI0/H0STCNI9 / uvicorn 단일 워커 / 주문번호 매핑 + race 가드 / `_reset_daily_state` / 익일 청산 30s 안정화 / NXT 매도 거부 좀비 차단 + SellRejectionTracker / WebSocket 4중 안전망 + K stale watcher + 사이클 29-R3 priority 분리 + 사이클 66 cap / KIS 거부 응답 + KST 강제 사이클 65 H2/H2-bis + 68 통일 / 사이클 38 명문화 / donchian_swing _swing_rest_poll_loop / 사이클 72 dedupe / 사이클 73 R-1/R-2/S-1 / 사이클 74 sampling). **Playwright 실제 실행 운영자 1회 확인 권고** (`npx playwright install` 후 e2e/settings.spec.ts 실제 실행으로 ECONNREFUSED 0건 + BuyBlockSection 4 슬라이더 렌더링 확인). **사이클 76+ 후속 카드 인계**: #19 realtime_other 16.13x (사이클 73) / #20 collector shared 헬퍼 추출 (사이클 74) / #21 [ws_opsp_backoff_state] / #22 [ws_restore_after_reconnect] / #23 api retry 5.80x (사이클 74 발견) / auth 1.43x. | `e2e/fixtures/api-mocks.ts` (+53L, 7 endpoint group 추가 — cash-usage-ratio + 4 integrations 토글 + buy-block + buy-block/thresholds + quote-accounts) / `frontend/src/components/CashUsageRatioCard.tsx` (+1L, retry:1) / `frontend/src/components/IntegrationToggleCard.tsx` (+2L, BuyBlockSection L190 + ToggleRow L469 retry:1) / `frontend/src/components/KisQuoteAccountsCard.tsx` (+1L, retry:1) / `frontend/src/components/__tests__/IntegrationToggleCard.test.tsx` (+8L, I8-G waitFor timeout 3000ms + setupToggleStubs auto-apply 핸들러) / `frontend/src/components/__tests__/_ast_api_mocks_coverage.test.ts` 신규 (10 케이스 vitest AST G-AST1~AST4) / `frontend/src/components/__tests__/_ast_useQuery_retry_required.test.ts` 확장 (+3 케이스 G-RT1~RT3, 사이클 65 H3 2 영속) / `tests/unit/e2e_mocks/test_cycle75_api_mocks_routes_registered.py` 신규 (2 케이스 pytest AST) / `_workspace/red/cycle75_e2e_flaky_fix.md` Red 명세 영구 기록 / `frontend/CLAUDE.md` 사이클 75 retry 영역 확장 + AST 가드 명세 추가 / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 75 행 | 사이클 73 1차 + 사이클 74 1차 e2e fail (flaky 2회 누적 확인) — 진짜 결함 영역 확정 후 사이클 65 hotfix #2/#3 패턴 답습. team-leader Phase 1-A 식별 7 endpoint group 미등록 (Settings 페이지 마운트 컴포넌트 호출 vs api-mocks 등록 매핑 결과) + Q4 발견 (사이클 65 H1 retry:1 영역이 PriceFilterCard/TradeAmountFilterCard 2개에 한정, 3 컴포넌트 미적용 = 사이드이펙트 영역). 14 사이클 연속 옵션 A 패턴 영속 (사이클 55 R-1 / 60 / 62~69 / 72~75). AST 영구 가드 17 케이스 = 미래 신규 frontend useQuery endpoint 추가 시 api-mocks 누락 영구 차단 (G-AST1~AST3) + 미래 신규 카드 retry:1 누락 영구 차단 (G-RT1~RT3 + 사이클 65 H3 2 영속). 매매 안전성 무영향 (e2e + frontend retry 영역 한정 + production 코드 변경 0). 사이클 65 hotfix #1/#2/#3 영속 (변경 0). Playwright 실제 실행 운영자 1회 확인 권고 (간접 AST 가드 17 케이스로 영구 차단 보장). 사이클 49→75 누적 27 사이클 + hotfix 5 영역. 후속 카드 사이클 76+ 인계: #19 (HIGH realtime_other 16.13x 사이클 73) / #20 (MEDIUM collector shared 헬퍼 사이클 74) / #21 (LOW [ws_opsp_backoff_state]) / #22 (LOW [ws_restore_after_reconnect]) / #23 (MEDIUM api retry 5.80x 사이클 74 발견) / auth 1.43x (LOW) / #16 (MEDIUM Q6-3 2026-06-13 이후 후보 풀 폭축 회고) / #15 (LOW 액면분할) / #11/#12 / Q6-2 옵션 B 헬퍼 영속. |
| 2026-06-08 | 사이클 74 — **WebSocket 로그 sampling/aggregation/rate cap (옵션 E-1) 도입** (사이클 71/73 운영 실증 잔존 dup 영역 흡수, **13 사이클 연속 옵션 A 패턴 영속**). team-leader Phase 1 + refactor-expert 자문 (6 카드 + 5 결정 의제) 후 사용자 결정 채택: **R3 (전체) + 2-A/B/C C+E + X (5분 heartbeat 보존) + Q2-D/E/Q3/Q4/Q5 모두 옵션 A**. **사이클 71/73 운영 실증 잔존**: `[swing_rest_poll]` 5.00x (정확 60s × 5회 정상 반복) + `[stale_watcher]` 3.00x (120s × 30회 정상) + `realtime` 3.00x (WS 구독/ACK/해제 logger.info 빈도) = 합법적 반복이나 일일 INSERT 누적 ~8,600/day, Supabase 무료 tier 500MB 약 8개월 후 한계. **신규 함수 (6 파일 +197/-31)**: `src/realtime/websocket.py` +95L (`_ws_action_collector: dict[str, dict[str, list[str]]]` 인스턴스 변수 = tr_id → {SUBSCRIBE/UNSUBSCRIBE/ACK: list[tr_key], OPSP_ALREADY: int} + `_record_action(tr_id, tr_key, action)` (L289) + `_flush_ws_action_collector()` (L300, `[ws_action_summary] label=... tr_id=... window=300s +N SUBSCRIBE [...] -M UNSUBSCRIBE [...] ack=K opsp_already=L` 1행, 종목 cap 20 + overflow `...+N` 사이클 28 패턴 답습) + `_ws_action_metrics_loop` 5분 주기 task (사이클 42 `_heartbeat_metrics_loop` 답습) + `connect()` L166-168 task 시작 + `disconnect()` L228 cancel *전* 마지막 flush + L230-236 cancel + await 정리 + `_send_subscribe` L417 / SUBSCRIBE SUCCESS L524 / OPSP0002 ALREADY L490-493 직접 logger.info 제거 + collector 흡수) + `src/engine/stale_watcher_core.py` +51L (`record_stale_watcher_check(stats)` L46 + `flush_stale_watcher_collector()` L57, `[stale_watcher_summary] checks=N stale_total=M retried=K cap_blocked=L force_retried=J` 1행) + `src/engine/scheduler.py` +42L (`record_swing_rest_poll(stats)` L133 + `flush_swing_rest_poll_collector()` L142, `[swing_rest_poll_summary] polls=N candidates_avg=X max=Y total_held=Z elapsed_ms_avg=W` 1행). **AST 영구 가드 G-7/G-8-A/G-8-B 신설**: G-7 (`_send_subscribe` 직접 logger.info 0건) + G-8-A (`_run_swing_rest_poll_once` 영역 한정) + G-8-B (`check_and_resubscribe_stale` 영역 한정). **회귀 가드 23 케이스 (5 파일)**: G-SP1~SP4 swing_rest_poll aggregation freezegun (4 — 5분 윈도우 / 통계 정확 / reset / shutdown flush) + G-SW1/SW2/SW5 stale_watcher aggregation (3) + G-SW3 `[stale_watcher_detail]` individual 영속 (사이클 73 영속) + G-SW4 `[stale_force_retry_cap]` WARNING 영속 (사이클 66 K-10) + G-WS1~WS6 ws_action aggregation (6 — tr_id별 1행 + 다중 tr_id 분리 + ACK 흡수 + OPSP0002 흡수 + disconnect 직전 flush) + G-WS4 `[ws_subscribe_reject]` ERROR individual 보존 (사이클 73 R-2 영속) + G-ERR1~ERR5 ERROR 보존 매트릭스 5 (사이클 16 aes_key_skip / 24 silent_inactive / 42 heartbeat_timeout / 73 R-1 ws_reverify / 73 R-2 ws_subscribe_reject) + G-7/G-8-A/G-8-B AST 3. **ERROR 보존 매트릭스 7 prefix individual 영속 (사이클 29 005935 LMS chain 진단 의무)**: `[ws_subscribe_reject]` logger.error / `[silent_inactive_force_reconnect]` / `[ws_heartbeat]` heartbeat timeout / `[aes_key_skip]` / `[ws_reverify]` WARNING / `[stale_force_retry_cap]` / `[stale_priority_resubscribe_cap_exceeded]`. **백엔드 2043 → 2066 PASS + 1 XFAIL + 2 skip** (+23 신규) / 카테고리 분리: cycle74 23 PASS / realtime/ 171 PASS / engine/ 1183 PASS + 1 XFAIL / ast/ 6 PASS / 전체 2066 PASS / **flakiness 0** (3 회 cycle74 0.16s / unit/ ±2.4s). 회귀 0 / 매매 안전성 무영향 (로깅 영역 시정 + 운영 prefix logger.* 경로 영속 = stdout/file/DB 3중 발화 보장). **사이클 17 OPSP0002 backoff 영속** (`_opsp_backoff_until` dict 등록 행위 변경 0, collector 흡수만). **사이클 38 명문화 / 55 R-1 / 66 priority / 67 분해 / 68 KST / 72 dedupe / 73 R-1/R-2/S-1 모두 무영향**. flush 주기 5분 (사이클 42 `HEARTBEAT_METRICS_INTERVAL_SECS=300` 답습, Q4 옵션 A). 운영 효과 예상: `[swing_rest_poll]` ~720/day → 144/day (~80% 감소) + `[stale_watcher]` ~360/day → 144/day + WS 구독/ACK/해제 ~4,000/day → ~10/day (~95% 감소) + dup_factor 2.11 → 1.0~1.2. **사이클 75+ 후속 카드**: #19 (HIGH `realtime_other` 16.13x 후속) / #20 (MEDIUM collector 헬퍼 추출 `src/realtime/_log_aggregator.py` — 3회 반복 추상화 규칙 충족 후 사이클 76+) / #21 (MEDIUM `[ws_opsp_backoff_state]` 5분 emit) / #22 (LOW `[ws_restore_after_reconnect]` 1행 emit) / `auth` 1.43x 별개 카드. **월요일 (2026-06-09) 09:00~10:00 1h tester verify 시나리오 E 신규**: `[ws_action_summary]` / `[swing_rest_poll_summary]` / `[stale_watcher_summary]` 5분 주기 1행 emit 운영 실측 + V-9 SQL (`dup_factor → 1.0~1.2`) 측정 + 사이클 66 D + 사이클 67 A/B/C 결합. | `src/realtime/websocket.py` +95L (collector + record/flush + metrics_loop + lifecycle + 3 사이트 logger.info 제거) / `src/engine/stale_watcher_core.py` +51L (record/flush) / `src/engine/scheduler.py` +42L (record/flush) / `tests/unit/engine/test_cycle74_swing_rest_poll_aggregation.py` 신규 (G-SP1~SP4, 4 케이스) / `tests/unit/engine/test_cycle74_stale_watcher_aggregation.py` 신규 (G-SW1~SW5, 5 케이스) / `tests/unit/realtime/test_cycle74_ws_action_aggregation.py` 신규 (G-WS1~WS6, 6 케이스) / `tests/unit/realtime/test_cycle74_error_preservation_matrix.py` 신규 (G-ERR1~ERR5, 5 케이스) / `tests/unit/ast/test_cycle74_ast_aggregator_helper.py` 신규 (G-7/G-8-A/G-8-B, 3 케이스) / `tests/unit/engine/test_scheduler_stale_logging_format.py` (B3-1 직접 emit 0건 검증으로 전환, -8/+10) / `tests/unit/realtime/test_websocket_ack.py` (ACK 직접 INFO 0건 + collector 흡수 검증으로 전환, -5/+13) / `tests/unit/realtime/test_websocket_subscribe_rejection.py` (ALREADY INFO 직접 0건 + collector 흡수, -4/+10) / `_workspace/red/cycle74_sampling.md` Red 명세 영구 기록 / `_workspace/refactor/cycle74_sampling_advisory.md` refactor-expert 자문 메모 영구 기록 / `src/realtime/CLAUDE.md` 옵션 E-1 명세 + lifecycle 추가 / `src/engine/CLAUDE.md` swing/stale aggregation 명세 추가 / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 74 행 | 사이클 71/73 운영 실증 잔존 dup 영역 (합법적 반복) 흡수 = 사이클 42 `[ws_heartbeat]` 5분 통계 패턴 답습. team-leader Phase 1 + refactor-expert 자문 (옵션 E-1 채택 + ERROR 보존 매트릭스 7 prefix 영속 + Q2-D/E/Q3/Q4/Q5 모두 옵션 A) = 13 사이클 연속 옵션 A 패턴 영속. 사이클 29 005935 LMS chain 진단 의무 = ERROR/WARNING individual 보존 매트릭스 영구 가드 G-ERR1~ERR5 + G-SW3/SW4 + G-WS4 영속. 매매 안전성 무영향 (로깅 영역 + collector 흡수만 + 운영 prefix logger 경로 영속 stdout/file/DB 3중 발화 보장). 사이클 17 OPSP0002 backoff dict 등록 행위 무변경 (collector 흡수만 추가). 사이클 38 명문화 / 55 R-1 / 66 priority / 67 분해 / 68 KST / 72 dedupe / 73 R-1/R-2/S-1 모두 무영향. **운영 효과 예상**: `[swing_rest_poll]` ~80% 감소 + WS 구독/ACK/해제 ~95% 감소 + dup_factor 2.11 → 1.0~1.2. **월요일 (2026-06-09) 09:00~10:00 1h tester verify 시나리오 A/B/C/D/E 결합 측정** (사이클 66 D + 사이클 67 A/B/C + 사이클 74 신규 E = `_record_action` 5분 윈도우 emit 실측 + V-9 SQL dup_factor 측정). 사이클 75+ 후속 카드: #19 (HIGH `realtime_other` 16.13x) / #20 (MEDIUM collector 헬퍼 shared 추출, 사이클 76+) / #21 (MEDIUM `[ws_opsp_backoff_state]`) / #22 (LOW `[ws_restore_after_reconnect]`) / `auth` 1.43x 별개 카드. 사이클 49→74 누적 26 사이클 + hotfix 5 영역. |
| 2026-06-08 | 사이클 73 — 카드 #19 (HIGH) **WebSocket dup 핵심 영역 시정** (사이클 72 hotfix F-2 인계). team-leader Phase 1 분석 후 **옵션 C + G-6.전체 + 별개 카드 채택** = **12 사이클 연속 옵션 A 패턴 영속** (사이클 55 R-1 / 60 / 62 / 63 / 64 / 65 / 66 / 67 / 68 / 69 / 72 / 73). **사이클 72 운영 효과** (배포 후): dup_factor 3.72 → **1.34 (64% 감소)** 측정 완료. 잔존 1.34 = F-2 핵심 (`[swing_rest_poll]` 5.00x + `realtime` 2.40x). **결함 사이트 3건 (Phase 1-A 확정)**: **R-1** `src/realtime/websocket.py:380-393` `[ws_reverify]` (`logger.warning` L380 + `await write_log("WARNING", ...)` L386 양쪽) + **R-2** `src/realtime/websocket.py:512-529` `[ws_subscribe_reject]` (`logger.error` L512 + `await write_log("ERROR", ...)` L523 양쪽, `logger.error` 메시지에 prefix `[ws_subscribe_reject]` 부재 = G-6.R AST 가드 검출 누락 위험) + **S-1** `src/engine/scheduler.py:2319-2333` `[swing_rest_poll]` (`logger.info` L2319 + `await write_log("INFO", ...)` L2325 양쪽 = 5.00x 정확 일치). **시정 (옵션 C 옵션 A' 답습 + G-6 AST 가드 영역 확장)**: R-1 + R-2 + S-1 사이트 `await write_log(...)` 호출 try/except 블록 통째 제거 (-27L) + R-2 `logger.error` 메시지에 prefix `[ws_subscribe_reject]` 추가 (운영 가시화 + AST 가드 검출 영속) + `logger.warning/error/info` 단독 유지 → `_DbLogHandler` (`src/main.py:137-170`) 위임 단일 INSERT. **AST 영구 가드 2 신규 (G-6 영역 확장)**: **G-6.R** (`tests/unit/ast/test_cycle73_ast_realtime_no_logger_write_log_pair.py`, `src/realtime/` 디렉토리 전수 rglob 각 `write_log` 호출 ±10 줄 동시 `logger.*` 호출 0건 — 사이클 72 ±5 줄에서 multi-line logger 7줄 흡수 보강) + **G-6.S** (`tests/unit/ast/test_cycle73_ast_swing_rest_poll_no_logger_write_log_pair.py`, `scheduler.py::_run_swing_rest_poll_once` 함수 영역 한정). 사이클 72 G-6 (`src/` 전체) 영속 영역에서 영역 확장 + 정밀 검출 패턴 보강. **회귀 가드 7 케이스 (6 파일)**: G-RT1 (R-1 사이트 write_log 0건) + G-RT2 (R-2 사이트 write_log 0건) + G-SCH1 (S-1 사이트 write_log 0건) + G-6.R AST + G-6.S AST + G-RT3-A (R-1 `logger.warning` 호출 영속) + G-RT3-B (R-2 `logger.error` 호출 영속). **기존 27 케이스 갱신** (`test_websocket_subscribe_rejection.py` + `test_websocket_reverify.py` Case B) — `write_log` patch 제거 + `logger.error` prefix `[ws_subscribe_reject]` 반영. **사이클 73 매매 hot path 무영향** (positions 0 영역 10:30+ KRX 메인 진입 중). **운영 효과 예상 (push 후 V-8 SQL 측정 의무)**: dup_factor 1.34 → **1.0~1.2** (`[swing_rest_poll]` 5.00x → 1.00x, `realtime` 2.40x → ≤2.0x). 잔존 dup 영역 = WebSocket ACK/구독/해제 logger.info 빈도 자체 결함 + `auth` 1.43x = **사이클 74+ 후속 카드** (메시지 batching / rate cap / sampling/aggregation 패턴 도입). **silent 결함 영구 차단 10 회 누적** (사이클 60 / 64 / 65#1 / 65#2 / 65#3 / 66 / 67 / 68 / 72 / **73**). 백엔드 2036 → **2043 PASS + 2 skip + 1 XFAIL** (+7) / 카테고리 분리 측정: cycle73 7 / realtime/ 160 / engine/ 1174 + 1 XFAIL / ast/ 3 / 전체 2043 / **flakiness 0** (3 회 cycle73 0.04s + unit/ 44.9s ±2.1s) / 회귀 0 / CLAUDE.md "절대 깨지 말 것" 8 영역 영속 (체결통보 H0STCNI0/H0STCNI9 + uvicorn 단일 워커 + 주문번호 매핑 + race 가드 + `_reset_daily_state` + NXT 좀비 차단 + WebSocket 4중 안전망 + KIS 거부 응답 `[kis_rejection]`). | `src/realtime/websocket.py` (-19L 합, R-1 `_verify_subscriptions_after_reconnect` write_log 제거 -9L + R-2 `_handle_raw` write_log 제거 -10L + `logger.error` prefix 추가) / `src/engine/scheduler.py` (-10L, S-1 `_run_swing_rest_poll_once` write_log 제거) / `tests/unit/realtime/test_cycle73_ws_reverify_no_write_log.py` 신규 (G-RT1) / `tests/unit/realtime/test_cycle73_ws_subscribe_reject_no_write_log.py` 신규 (G-RT2) / `tests/unit/realtime/test_cycle73_logger_persistence.py` 신규 (G-RT3-A/G-RT3-B) / `tests/unit/engine/test_cycle73_swing_rest_poll_no_write_log.py` 신규 (G-SCH1) / `tests/unit/ast/test_cycle73_ast_realtime_no_logger_write_log_pair.py` 신규 (G-6.R, ±10 줄 + multi-line 7줄 흡수) / `tests/unit/ast/test_cycle73_ast_swing_rest_poll_no_logger_write_log_pair.py` 신규 (G-6.S, 함수 영역 한정) / `tests/unit/realtime/test_websocket_subscribe_rejection.py` + `test_websocket_reverify.py` Case B 갱신 (27 케이스 write_log patch 제거 + prefix 반영) / `_workspace/red/cycle73_ws_dup_fix.md` Red 명세 영구 기록 / `src/realtime/CLAUDE.md` E2/F1 본문 시정 명세 명시 / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 73 행 | 사이클 72 hotfix F-2 (WebSocket dup 핵심 영역) 즉시 인계 카드 #19 (HIGH) 완전 종결 — R-1/R-2/S-1 3 사이트 시정 + AST G-6 영역 확장. team-leader Phase 1-A 확정 (`[swing_rest_poll]` 5.00x = `logger.info` + `await write_log` 양쪽 INSERT 60s 주기 × 5 사이클 × 2 INSERT = 10/2 dup_factor 정확). 사이클 72 패턴 100% 답습 (옵션 A' 호출자 정정 + AST G-6 영역 확장) = 12 사이클 연속 옵션 A 패턴 영속. silent 결함 영구 차단 10 회 누적. R-2 `logger.error` prefix `[ws_subscribe_reject]` 추가 = 운영 가시화 보강 + G-6.R AST 가드 검출 영속 보장. AST G-6.R 정밀 검출 패턴 (±10 줄 + multi-line logger 7줄 흡수) = 사이클 72 G-6 ±5 줄 한계 보강. 매매 안전성 무영향 (로깅 영역 시정 + 운영 prefix logger 경로 영속 = stdout/file/DB 3중 발화 보장 = `_DbLogHandler` `src/main.py:172-174` root_logger 부착). 사이클 65 H2/H2-bis + 사이클 67 분해 + 사이클 68 KST + 사이클 72 dedupe 모두 무영향. **운영 효과 예상**: dup_factor 1.34 → 1.0~1.2 (push 후 V-8 SQL 측정). 후속 카드 사이클 74+ = **auth 1.43x 동행 시정 검토** (write_log dup 결함 아님 → 메시지 반복 패턴 분석) + **WebSocket ACK/구독/해제 logger.info 빈도 자체** (구독 100건 시 logger.info 100회 + DB 100건 INSERT → 운영 부담 측정 후 sampling/aggregation 패턴 도입) + **stale_manager.py 1,099L 사이클 67 종결 카드 #14 재발의 불요**. 사이클 49→73 누적 25 사이클 + hotfix 5 영역. 후속 카드 #16 (MEDIUM Q6-3 1주 후 2026-06-13 이후) / #15 (LOW Q7-2 액면분할) / #11/#12 / Q6-2 옵션 B 헬퍼 (사이클 71+) / D2/D3 (사이클 70+) / #20 (LOW RLS) / #21 (LOW 사이클 72/73 운영 배포 후 dup_factor 추이 모니터링) / 신규 사이클 74+ (auth dup + WS logger.info 빈도) 영속. |
| 2026-06-08 | 사이클 72 hotfix — system_logs **이중 INSERT 시정** (사이클 71 운영 실증 발견 silent 결함 #1, HIGH). 사용자 즉시 시정 지시 (매매 미발생 09:25 영역). **옵션 A' + D + AST G-6/G-7/G-8 채택** = 11 사이클 연속 옵션 A 패턴 영속. **결함 (사이클 71)**: 09:00~09:25 system_logs INSERT 205건 / distinct 71 = **평균 2.89x dup** (`[swing_poll]` 23x / `[stale_watcher]` 7.33x / `[stale_watcher_detail]` 확정적 2.00x). **Root cause**: `_DbLogHandler` (`src/main.py:137`) 가 `src.*` 모든 logger emit INSERT + 같은 emit 사이트가 `await write_log(...)` 양쪽 호출 → 이중 INSERT. **시정 (옵션 A' 호출자 정정 11+ 사이트)**: `realtime/websocket.py` (`[ws_heartbeat]`) + `engine/stale_watcher_core.py` 4 사이트 + `engine/stale_diagnostics.py` (`[stale_watcher_detail]`) + `engine/stale_session_recovery.py` 3 + `engine/stale_universe_guard.py` (`[universe_excluded]`) + `engine/scanner.py` (`[priority_drop]`) + G-6 AST 추가 검출 11 사이트 (`engine/boot_manager.py` `[cash_usage_ratio]` + `engine/scheduler.py` 6 사이트 L735/L842/L1730/L1825/L1834/L2657 등) `await write_log` / `await _write_log` / `asyncio.create_task(_write_log)` 호출 전수 제거. `logger.info/warning` 단독 유지 → `_DbLogHandler` 위임 단일 INSERT. **시정 (옵션 D dedupe 안전망)**: `src/main.py` `_DbLogHandler` 신규 `_DEDUPE_TTL_SECS = 0.5` + `_dedupe_cache: dict[str, float]` + `time.monotonic` 비교 + lazy evict (100 항목 cap) → 동일 메시지 500ms 내 중복 emit runtime 차단. **AST 영구 가드 3 신설**: G-6 (`src/` rglob `write_log` ±5 줄 동시 `logger.*` 0건) + G-7 (dedupe 구조 정적) + G-8 (사이클 65 H2-bis KST 영속 보강 2 케이스). **회귀 가드 18 케이스 (6 파일)**: G-A1~G-A11 (11) + G-D1~G-D3 freezegun (3) + AST 4. F-1 통합 테스트 2건 (`test_boot_cash_usage_ratio.py` + `test_boot_eager_refresh.py`) obsolete `calls.write_log` assertion 옵션 C 채택 제거 (사이클 72 의도 일치). 백엔드 2018 → **2036 PASS + 1 XFAIL + 2 skip** (+18) / flakiness 0 (3 회 0.21~0.30s) / 회귀 0 / 매매 안전성 무영향 (로깅 영역 시정, 운영 prefix 발화 logger 경로 영속). **사이클 65 H2/H2-bis 영속** (`system_logs.py:44` + `main.py:131` 변경 0). **사이클 67 stale_manager 분해 영향 0** (4 sub-module write_log 제거만, facade re-export 무영향, 27 PASS 영속). **사이클 68 KST 일관성 영속** (8 DB + 9 engine/api + `_kst.py` 67 케이스 PASS). **silent 결함 영구 차단 9 회 누적** (사이클 60/64/65#1/65#2/65#3/66/67/68/72). 변경 통계 production -65L / +30L 순감 -35L + 신규 회귀 가드 6 파일 + AST 가드 3. **F-2 (HIGH 운영) WebSocket dup 핵심 영역 사이클 73 카드 #19 인계** (운영 측정 dup 상위 4건 = `src.realtime.websocket` ACK / 구독 / 해제 / 5xx, G-6 AST 가드 영역 확장 검토). | `src/main.py` (+30 lines, `_DbLogHandler` 500ms TTL dedupe 캐시 신규) / `src/realtime/websocket.py` (-7 lines, `[ws_heartbeat]` write_log 제거) / `src/engine/stale_watcher_core.py` (-22 lines, 4 사이트) / `src/engine/stale_diagnostics.py` (-6 lines) / `src/engine/stale_session_recovery.py` (-18 lines, 3 사이트) / `src/engine/stale_universe_guard.py` (-9 lines) / `src/engine/scanner.py` (-3 lines + `[priority_drop]` logger.warning 격상) / `src/engine/boot_manager.py` (-3 lines, `[cash_usage_ratio]`) / `src/engine/scheduler.py` (-22 lines, 6 사이트) / `tests/unit/engine/test_cycle72_no_duplicate_insert_per_site.py` 신규 (11 케이스 G-A1~G-A11) / `tests/unit/main/test_cycle72_db_log_dedupe.py` 신규 (3 케이스 G-D1~G-D3 freezegun) / `tests/unit/ast/test_cycle72_ast_no_logger_write_log_pair.py` 신규 (1 케이스 G-6) / `tests/unit/main/test_cycle72_db_log_dedupe_structure_ast.py` 신규 (1 케이스 G-7) / `tests/unit/main/test_cycle72_insert_log_kst_persistence_ast.py` 신규 (2 케이스 G-8) / `tests/integration/test_boot_cash_usage_ratio.py` + `test_boot_eager_refresh.py` (F-1 obsolete assertion 제거, 옵션 C) / 기존 회귀 가드 5 파일 caplog 마이그레이션 (write_log fixture → caplog) / `_workspace/red/cycle72_dup_insert_fix.md` Red 명세 영구 기록 / `src/db/CLAUDE.md` 본문 사이클 72 hotfix 명세 추가 | 사이클 71 운영 hot path 실증 발견 silent 결함 (이중 INSERT 2.89x dup) 즉시 시정 — 사용자 결정 (매매 미발생 09:25 영역 안전). team-leader Phase 1 분석 후 옵션 A' (호출자 정정) + 옵션 D (dedupe 안전망) + AST 영구 가드 3 결합 채택 = 11 사이클 연속 옵션 A 패턴 영속. silent 결함 영구 차단 9 회 누적 (사이클 60→72). 매매 안전성 무영향 (로깅 영역 시정 + 운영 prefix logger 경로 영속) + 사이클 65 H2/H2-bis + 사이클 67 분해 + 사이클 68 KST 모두 무영향. F-1 통합 테스트 obsolete 옵션 C 제거 (사이클 72 의도 일치 + G-A 단위 가드로 회귀 차단 영속). F-2 (HIGH 운영) WebSocket dup 핵심 영역 사이클 73 카드 #19 인계 — 운영 dup_factor 1.00 목표 + G-6 AST 가드 영역 확장. 사이클 49→72 누적 24 사이클 + hotfix 5 영역. 후속 카드 #19 (HIGH 사이클 73 WebSocket dup) / #16 (MEDIUM Q6-3 1주 후) / #15 (LOW 액면분할) / #11/#12 / Q6-2 옵션 B 헬퍼 (사이클 71+) / D2 D3 (사이클 73+) / #20 (LOW RLS 활성 후보) / #21 (LOW 사이클 72 운영 배포 후 dup_factor 추이 모니터링) 영속. |
| 2026-06-08 | 사이클 69 — 카드 #18 (LOW) **영구 폐기** + TIMESTAMPTZ 사실 명문화 (Q4 채택, 미래 오해 차단). team-leader Supabase MCP `execute_sql` READ-ONLY 13 테이블 전수 점검 결과 **UTC 잔존 결함 = 0건** 확정 (system_logs 566,180 KST 07~23시 정상 + daily_log_reports 20 20:10 KST 정확 + parameter_recommendations 109 20:00 KST 정상 66건 + strategy_funnel_snapshots 660 09:30 KST 정상 258건). **Root cause 재해석**: PostgreSQL TIMESTAMPTZ = *내부 UTC + 표시 시 변환* 모델 → instant 자체 timezone 무관 절대 시점. DB DEFAULT `now()` (Supabase PG UTC) INSERT instant ≡ Python `datetime.now()` (컨테이너 `TZ=Asia/Seoul`) INSERT instant = **절대 시점 동일**. 사이클 65 hotfix H2/H2-bis + 사이클 68 17 모듈 시정 (`src/db/_kst.py` 헬퍼) = **표시 형식 `+09:00` ISO 일관화 + 호출자 정합성 보장** — *instant 정정이 아님*. **사이클 68 인계 가정 = 오해 (UTC 잔존 데이터 백필 필요)** → 본 사이클에서 진실 영구 명문화. **변경 영역 (코드 변경 0, 문서 명문화만)**: `src/db/CLAUDE.md` 신규 §"TIMESTAMPTZ 사실 명문화 (사이클 69)" + `docs/HARNESS_CHANGELOG.md` 사이클 69 행 + `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 69 행. 회귀 가드 0 (코드 변경 0). 백엔드 2018 PASS + 1 XFAIL + 2 skip 영속. 매매 안전성 무영향 + WebSocket 4중 안전망 + 사이클 67 stale_manager 분해 + 사이클 68 KST 일관성 모두 무영향. **Q2 (D2 3 모듈 UI strftime) + Q3 (D3 9 engine/api 인라인 `_KST` → 헬퍼 통일) 사이클 70+ 별개 인계**. **신규 카드 #20 후보 (Q5 발의)**: Supabase advisory 경고 = public 13 테이블 RLS 비활성 — server-side only (FastAPI ↔ service role key) + anon key 미배포로 실제 위험 0이나 운영 안전성 보강 카드 검토 영역. | `src/db/CLAUDE.md` (+19 lines, §TIMESTAMPTZ 사실 명문화 신규) / `docs/HARNESS_CHANGELOG.md` 사이클 69 행 / `CLAUDE.md` 루트 하네스 변경 이력 표 사이클 69 행 | 사이클 68 인계 카드 #18 (UTC 잔존 데이터 백필) = 사실 점검 결과 **불필요** 확정. team-leader Supabase MCP 13 테이블 전수 점검 후 root cause 재해석 (TIMESTAMPTZ 모델 특성). 사이클 68 가정 ("UTC 잔존") = 오해였음을 진실의 원천 (`src/db/CLAUDE.md` §TIMESTAMPTZ 사실 명문화 + `HARNESS_CHANGELOG.md`) 에 영구 기록 → 미래 동일 오해 재발 차단. 코드 변경 0, 문서 명문화만. 9 사이클 연속 옵션 A 패턴 영속 (사이클 55 R-1 / 60 / 62 / 63 / 64 / 65 / 66 / 67 / 68 / 69). Q2/Q3 사이클 70+ 인계 + Q5 신규 카드 #20 (RLS 활성) 검토 영역. 사이클 49→69 누적 21 사이클 + hotfix 4 영역. 후속 카드 #16 (MEDIUM Q6-3 1주 후) / #15 (LOW 액면분할) / #11/#12 / Q6-2 옵션 B 헬퍼 (사이클 71+) / D2 D3 (사이클 70+ Q2/Q3) / #20 (LOW RLS 활성 후보) 영속. |
| 2026-06-07 | 사이클 68 — 카드 #17 (LOW) 기타 INSERT 모듈 UTC → KST 일관성 (사이클 65 hotfix H2/H2-bis 인계 종결). team-leader Phase 1~4 분석 후 사용자 결정 채택: **A (HIGH 1 + MEDIUM 7 = 8 모듈 일괄)** + **2-B (`src/db/_kst.py` 공용 헬퍼 신규)** + **3-B (`src/` 전체 rglob AST 가드)** + **Q1 (LOW 9 모듈 `date.today()` → `today_kst()`)** + **Q3 (`stock_master.is_stale()` KST 비교 통일)** = **9 사이클 연속 옵션 A 패턴 영속** (사이클 55 R-1 / 60 / 62 / 63 / 64 / 65 / 66 / 67 / 68). 신규 모듈: `src/db/_kst.py` (35L, `KST` + `now_kst_iso()` + `today_kst()`). **시정 17 모듈**: 8 DB (stock_master / parameter_recommendations / log_reports / system_config 6 upsert / strategy_config / kis_quote_accounts 2 함수 / market_regime_snapshots / backtest_runs 2 함수) + 9 engine/api (api/balance / api/condition / engine/strategy_base / engine/strategy / engine/backtest_engine / engine/scheduler / engine/boot_manager / engine/strategies/long_tail_volatility / engine/strategies/volatility_breakout) + PR-C2 가드 갱신 (`tests/unit/api/test_condition_cache.py` 자정 race 패턴 유지). **silent 결함 차단**: `stock_master.is_stale()` UTC naive 비교 → KST aware (24h TTL 부정확 9시간 오차 영구 차단). **회귀 가드 15 케이스 (5 파일)**: G-1 헬퍼 모듈 export (3 sub-case) + G-2~G-9 8 모듈 KST 명시 (8) + G-10a/b AST 정적 (`datetime.utcnow()` 0건 + payload UTC 0건, 2 sub-case) + G-11 is_stale KST (1) + G-12 `date.today()` 0건 AST (1). **백엔드 2002 → 2017 PASS** (+15 신규) **+ 2 skipped + 1 XFAIL** (사이클 66 K-2 의미 전환 영속). 카테고리 측정: `tests/unit/db/test_cycle68_*.py` 15 PASS / `tests/unit/db/` 166 PASS / `tests/unit/engine/` 1,162 PASS + 1 XFAIL / `tests/integration/` 164 PASS / `tests/contract/` 146 PASS. flakiness 0 (3 회 ±0.5%) / 회귀 0 / 매매 안전성 무영향 (DB INSERT 영역 = 시세/매매 hot path 무관). **사이클 65 H2/H2-bis 영속** (변경 0): `system_logs.py:44` + `main.py:126` 인라인 패턴 유지 (행위 동일, 헬퍼 통일은 후속 사이클 인계 가능). **사이클 67 분해 영향 0**: stale_manager facade + 4 sub-module 무영향. **silent 결함 후보 D1 (LOW, 운영 무영향 → 사이클 69 인계)**: `strategy_base.py:34` `default_factory=date.today` 함수 참조가 G-12 정규식 `date\.today\s*\(\s*\)` 회피 — dataclass 인스턴스 생성 시 동일 결함이나 운영 `TZ=Asia/Seoul` 무영향. **사이클 69 카드 #18 (UTC 잔존 데이터 백필 migration) 자동 예약 (Q2 채택)**. | `src/db/_kst.py` 신규 35L / `src/db/stock_master.py` (G-2 upsert + G-11 is_stale, +12 lines) / `src/db/parameter_recommendations.py` (+1 line) / `src/db/log_reports.py` (+3 lines) / `src/db/system_config.py` (+9 lines, 6 upsert) / `src/db/strategy_config.py` (+2 lines) / `src/db/kis_quote_accounts.py` (+9 lines, 2 함수) / `src/db/market_regime_snapshots.py` (+6 lines) / `src/db/backtest_runs.py` (+8 lines, 2 함수) / `src/api/balance.py` (+5 lines, Q1) / `src/api/condition.py` (+6 lines, Q1) / `src/engine/strategy_base.py` (+6 lines, Q1) / `src/engine/strategy.py` (+8 lines, Q1) / `src/engine/backtest_engine.py` (+4 lines, Q1) / `src/engine/scheduler.py` (+6 lines, Q1) / `src/engine/boot_manager.py` (+5 lines, Q1) / `src/engine/strategies/long_tail_volatility.py` (+2 lines, Q1) / `src/engine/strategies/volatility_breakout.py` (+2 lines, Q1) / `tests/unit/api/test_condition_cache.py` (+15 lines, PR-C2 가드 갱신) / `tests/unit/db/test_cycle68_kst_helper_module_g1.py` 신규 (3 케이스) / `tests/unit/db/test_cycle68_kst_timestamp_per_module_g2_g9.py` 신규 (8 케이스) / `tests/unit/db/test_cycle68_kst_global_ast_guard_g10.py` 신규 (2 케이스) / `tests/unit/db/test_cycle68_stock_master_is_stale_kst_g11.py` 신규 (1 케이스) / `tests/unit/db/test_cycle68_date_today_kst_g12.py` 신규 (1 케이스) / `_workspace/cycle68_other_insert_modules_kst_audit.md` 신규 + `_workspace/red/cycle68_other_insert_modules_kst.md` Red 명세 / `src/db/CLAUDE.md` `_kst.py` 명세 추가 | 사이클 65 hotfix H2/H2-bis 인계 카드 #17 (LOW) 완전 종결 — system_logs 외 기타 INSERT/UPSERT 모듈 timestamp KST 일관성. 9 사이클 연속 옵션 A 패턴 영속 + Q1/Q3 동행 채택으로 silent 결함 17 모듈 일괄 차단. `_kst.py` 공용 헬퍼로 향후 신규 silent 결함 영구 차단 (G-10b AST 가드). 매매 안전성 무영향 (DB INSERT 영역). 사이클 67 stale_manager facade 분해 영향 0 (회귀 가드 27 PASS 영속). 사이클 49→68 누적 20 사이클 + hotfix 4 영역. 후속 카드 #18 (운영 UTC 백필 migration, 사이클 69 Q2 예약) + #16 (MEDIUM Q6-3 후보 풀 폭축 회고, 1주 운영 후) + #15 (LOW 액면분할 prdy_clpr invalidate) + #11/#12 (보드 mutex / 14 모듈 logger) + 옵션 B 헬퍼 (사이클 71+ Q6-2) 영속. |
| 2026-06-06 | 사이클 67 — refactor #2 후속 카드 #14 (MEDIUM) `stale_manager.py` 1,099L → **4 sub-module + facade 96L 분해** (사이클 63 발견 인계 카드 영속 종결). domain-expert 옵션 A 자문 (Q1~Q5 + Q6-1~Q6-3) 전부 적용 (**8 사이클 연속 패턴 영속**) + **Q4 유일 불일치 채택** (옵션 B facade patch 영속, 자문 실측 `patch("src.engine.stale_manager.*")` = 0건 강력 권고). **분해**: `stale_diagnostics.py` 357L (5 함수 + 4 상수 — 진단·CCNL 캐시·force_retry prune) / `stale_session_recovery.py` 274L (3 함수 + 5 상수 — silent inactive·세션 reconnect·delta unsubscribe) / `stale_universe_guard.py` 157L (1 함수 + 1 상수 — 보유/익일청산 보호) / `stale_watcher_core.py` 399L (2 함수 — **K stale watcher 본체 HIGH hot path**). facade `stale_manager.py` 96L (`__all__` 21 re-export only). `scheduler.py` 11 wrapper **변경 0** (Q5=A `from src.engine import stale_manager` lazy import 영속). **Q1=A facade / Q2=P1 모듈-레벨 정적 import (`stale_watcher_core` → `stale_diagnostics.emit_stale_session_detail`) / Q3=A 4 sub-module 모두 `logger = logging.getLogger("src.engine.scheduler")` 명시 (사이클 60 I1 영속) / Q4=B facade patch 영속 + G-16 AST 신설 / Q5=A wrapper 변경 0**. **사이클 29 005935 사고 영역 영속**: G-15 WARNING (`[stale_priority_resubscribe_cap_exceeded]`) + G-17 priority 분리 *후* cap AST. **사이클 66 시정 영속**: G-11 `high_targets`/`low_targets` 변수명 + G-12 try/except 4중. **회귀 가드 17 케이스 (6 파일, HIGH 6 = 35%)**: G-1~G-5 분해 (5) + G-6~G-9 의존성+logger (4) + G-10~G-13 사이클 63+66 영속 (4) + G-14~G-15 사이클 60+64 영속 (2) + **G-16 facade patch 안전선 (1, Q4=B)** + **G-17 priority 분리 *후* cap (1, 사이클 29)**. 백엔드 1985 → **2002 PASS + 1 XFAIL + 2 skipped** (+17 신규 G-1~G-17) / `tests/unit/engine/stale_manager/` 27 PASS / `tests/unit/engine/` 1,162 PASS + 1 XFAIL / flakiness 0 (3 회 0.24~0.39s + 36.34~36.56s, ±0.5%) / 회귀 0 / 매매 안전성 무영향 (행위 보존 + 외부 인터페이스 변경 0). **누적 scheduler.py 감소** (사이클 51 직전 ~4,185 → 사이클 67 후 3,007 영속): **-1,178L, -28%**. patch 경로 적응 3 건 (`test_C1` evict_expired_ccnl facade patch 비효과 → sub-module 직접 / `test_D2` 단일 파일 grep → 2 sub-module 합산 / `test_cycle66_*` 일부 적응). **G-12 라인 cap 마진 (정보, 결함 아님)**: stale_watcher_core 실측 399L (권고 ≤380L 대비 5% 초과) — 행위 보존 우선, 후속 사이클 71+ 옵션 B 헬퍼 (Q6-2 청사진 ~75L) 분리 시 추가 압축 가능. **무변경 영역**: 함수 본체 라인 단위 + 시그너처 (`emit_stale_session_detail(scheduler, ...)` Q4=B) + 상수 값 + `sys.modules.get` 패턴 (6건, 사이클 61 D-1 AST) + try/except 4중 (사이클 66 Q2) + 운영 prefix 전수 (`[stale_*]`) + WebSocket 4중 안전망 호출 시점/횟수 0 + silent inactive 시간당 2회 cap + universe 가드 보호. | `src/engine/stale_manager.py` 1,099 → 96L (facade) / `src/engine/stale_diagnostics.py` 신규 357L / `src/engine/stale_session_recovery.py` 신규 274L / `src/engine/stale_universe_guard.py` 신규 157L / `src/engine/stale_watcher_core.py` 신규 399L / `src/engine/scheduler.py` 변경 0 / `tests/unit/engine/stale_manager/test_cycle67_*.py` 6 파일 17 케이스 신규 / `tests/unit/engine/test_cycle60_phase2A1_stale_manager.py` test_C1 patch 경로 적응 / `tests/unit/engine/test_cycle63_phase2A3_dependency_direction.py` test_D2 grep 2 sub-module 합산 / `tests/unit/engine/stale_manager/test_cycle66_resubscribe_cap_priority_fix.py` patch 경로 일부 적응 / `src/engine/CLAUDE.md` 모듈 맵 + 본문 사이클 67 행 / `_workspace/cycle67_stale_manager_decomposition_design_card.md` + `_workspace/cycle67_stale_manager_decomposition_domain_consult.md` + `_workspace/cycle67_stale_manager_decomposition_domain_response.md` + `_workspace/red/cycle67_stale_manager_decomposition.md` 영구 기록 | refactor #2 후속 카드 #14 (MEDIUM) **완전 종결** — `stale_manager.py` 1,099L 모듈 비대화 해소 + 책임 분리 (진단/복구/유니버스/watcher 본체). domain-expert 옵션 A 자문 **8 사이클 연속 패턴 영속** + Q4 유일 불일치 채택 (자문 실측 데이터 강력 권고). G-16 (Q4=B facade patch 안전선) + G-17 (priority 분리 후 cap, 사이클 29 영구 차단) AST 가드 2 신설 — silent 결함 영구 차단 패턴 8 회 누적 검증 (사이클 60/64/65#1/65#2/65#3/66/67). Q6-2 (사이클 71+ 옵션 B 헬퍼 청사진) + Q6-3 (사이클 68+ 시점 분리) 채택. K stale watcher 본체 영역 영속 보장 (Q1/Q2/Q5 종합 회귀 위험 LOW) + scheduler.py 변경 0 + 외부 인터페이스 0 + 사이클 29 영역 G-15/G-17 AST 가드. **월요일 (2026-06-09) 09:00~10:00 1h tester verify 의무** (시나리오 A/B/C/D 결합). 사이클 49→67 누적 19 사이클 + hotfix 4 영역. 후속 카드 #16 (MEDIUM Q6-3 1주 후) / #17 (LOW) / #15 (LOW) / #11/#12 / 옵션 B 헬퍼 (사이클 71+ Q6-2) 영속. |
| 2026-06-06 | 사이클 66 — `_resubscribe_stale_priority` cap=10 결함 시정 (카드 #5 HIGH, 사이클 63 K-2 발견 결함 영속 시정). domain-expert 옵션 A 자문 Q1~Q5 + Q6-1~Q6-6 전부 적용 (**7 사이클 연속 패턴 영속**). 시정: `stale_manager.py:1023-1034` ~22L 교체 — priority 분리 *먼저* + try/except 4중 가드 통일 (`_check_and_resubscribe_stale` 본체 답습, Q2) + HIGH > cap 모두 보장 + `[stale_priority_resubscribe_cap_exceeded]` WARNING (운영 가시화, Q3) + `targets = high_targets + low_targets[:max(0, cap - len(high_targets))]`. **사이클 29 005935 사고 패턴 영구 차단** (HIGH 종목 cap 밖 잘림 8분 영구 잔류 + KIS LMS chain). 회귀 가드 11 케이스 (단일 파일, HIGH 4 = 36% = K-2 시정 confirm + K-3 HIGH > cap + K-4 분리 + K-8 try/except 4중 + K-10 WARNING + AST 정적). **K-2 의미 전환**: 사이클 63 K-2 (별도 파일, `test_cycle63_phase2A3_priority_cap.py`) = `@pytest.mark.xfail(strict=False)` 마킹 — 사이클 63 결함 confirm 영속 보존 (호환), 사이클 66 시정 완료로 자동 XFAIL 전환. 사이클 66 K-2 (신규 파일) = 시정 confirm assertion. 백엔드 1975 → **1984 PASS + 1 XFAIL** (+9 신규 + 1 의미 전환) / 회귀 0 / flakiness 0 (3 회 1145 ±0.18s) / coverage 80.95% (+0.02%). 매도 영향 0 (사이클 38 명문화 영속, K stale watcher = 시세 영역). 무변경 영역 7 (`sys.modules.get` / 풀 인터페이스 / `_stale_last_resubscribe_at` / `asyncio.sleep(0.05)` / INFO + `write_log` / HIGH↔bypass 매핑 / 함수 시그너처). | `src/engine/stale_manager.py` ~22L 교체 (L1023-L1057) + docstring Q6-4 의미 전환 명시 / `tests/unit/engine/stale_manager/test_cycle66_resubscribe_cap_priority_fix.py` 신규 (11 케이스, HIGH 4) / `tests/unit/engine/test_cycle63_phase2A3_priority_cap.py::test_K2` xfail 마킹 / `src/engine/CLAUDE.md` 사이클 66 본문 추가 / `_workspace/cycle66_cap10_fix_design_card.md` v2 + `_workspace/cycle66_cap10_fix_domain_consult.md` + `_workspace/cycle66_cap10_fix_domain_response.md` + `_workspace/red/cycle66_cap10_fix.md` 발주/자문/Red 명세 영구 기록 | 사이클 63 발견 결함 (K-2 PASS = 결함 confirm) 영속 시정 — 사이클 29 005935 사고 패턴 영구 차단. domain-expert 자문 옵션 A 전부 적용 (Q2 try/except 4중 + Q3 WARNING + Q6-4 docstring 의미 전환) = 7 사이클 연속 패턴 영속. K-2 의미 전환 = XFAIL 마킹으로 *과거 결함 영속 보존 + 시정 자동 가시화* 패턴 신규 도입. **사이클 66 = refactor #2 후속 후행 시정 (사이클 63 발견 영속 시정 의무)**. **월요일 (2026-06-09) 09:00~10:00 1h tester verify 시나리오 D 신규 (Q5)** — HIGH 인위 stale 주입 보유 0 + 측정 지표 3 (보장률 100% / WARNING 발화 / 회복 시간 ≤5분) 결합 효과 측정. 사이클 49→66 누적 18 사이클 + hotfix 4 영역. 후속 카드 #14/#16/#17/#15/#11/#12 영속. |
| 2026-06-06 | 사이클 65 hotfix — e2e settings.spec.ts FAIL 시정 (H1 useQuery retry) + **system_logs timestamp KST 강제 (H2 + H2-bis)** (사용자 신규 보고) + AST 영구 가드 4 (H3). **결정적 결함 발견**: `src/main.py::_insert_log_to_db` (`_DbLogHandler` 위임 경로, logger.* 호출 빈도 ↑) 가 system_logs INSERT 시 timestamp 미명시 → UTC 저장. H2 단독 (write_log) 시정 시 logger 경로 UTC 잔존 — H2-bis 로 영구 차단. 4 영역 hotfix: H1 (`PriceFilterCard.tsx` + `TradeAmountFilterCard.tsx` `retry: 1` 명시) + H2 (`system_logs.py::write_log` KST timestamp 1줄) + H2-bis (`main.py::_insert_log_to_db` KST timestamp 1줄) + H3 (AST 영구 가드 vitest 2 + pytest 2). 백엔드 1970 → **1972 PASS** (+2 pytest AST) / 프론트 170 → **172 PASS** (+2 vitest AST) / 전체 **2144 PASS** / 회귀 0 / flakiness 0 / coverage 80.93%. 매도 영향 0 (사이클 38 명문화 영속). | `frontend/src/components/PriceFilterCard.tsx` (+1) / `frontend/src/components/TradeAmountFilterCard.tsx` (+1) / `src/db/system_logs.py` (+1 line 44) / `src/main.py` (+1 line 126) / `frontend/src/components/__tests__/_ast_useQuery_retry_required.test.ts` (신규 vitest AST 2) / `tests/unit/db/test_system_logs_kst_timestamp.py` (신규 pytest AST 2) / `src/db/CLAUDE.md` (write_log + main.py KST 명세) / `frontend/CLAUDE.md` (useQuery retry 영구 가드) | 사이클 65 e2e 결함 = TradeAmountFilterCard 추가로 카드 수 임계 초과 + React Query 기본 retry 3회 누적 → 5초 timeout 초과 → settings.spec.ts FAIL. 사용자 추가 보고 (system_logs timestamp UTC) = 별도 결함 발견 → 통합 hotfix 진행 (사이클 53 KST `+09:00` 패턴 답습). tdd-engineer AST 가드 작성 중 `_DbLogHandler` 위임 경로 (`src/main.py:123`) 신규 결함 발견 = silent 영향 영역 (logger.* 모든 호출이 UTC 저장) → 직접 시정 (H2-bis). 사이클 60/64 hotfix 패턴 답습 (단위/통합/AST 3 중 silent 결함 차단). 카드 #17 (LOW) 기타 INSERT 모듈 UTC → KST 일관성 + 카드 #18 (LOW) 기존 UTC 잔존 데이터 변환 운영 카드 = 별개 사이클 인계. 사이클 49→65 누적 17 사이클 + hotfix 4 영역. 사이클 66 (카드 #5 cap=10 결함) 보류 → 사이클 65 hotfix 완료 후 재발주. |
| 2026-06-06 | 사이클 65 거래대금 동행 필터 — scanner 단계 작전주 차단 보강 (Q7-5 사이클 64 인계, 사이클 62 갭상승 회피 효과 폐기 보강). domain-expert 옵션 A 자문 Q1~Q5 + Q6-1~Q6-4 전부 적용 (**6 사이클 연속**). 사이클 64 패턴 100% 답습 (`_collect_protected_tickers_for_scanner` 100% 재사용 + 60s TTL + invalidate Q7-1 unsubscribe 0 + DailyEmitCap + daily_summary + AST keyword 의무 + 사이클 38 명문화). **Q1 디폴트 0 + UI 0~100억 step 1억 + 권장값 마커 1억/5억/10억** (team-leader 1차 5,000억 반박 채택). **Q2 옵션 C 통합 폴백 (HIGH 핵심)**: `ticker_market_info["trade_amount_raw"]` (신규 키, 원 단위) 1순위 + `stock_master.raw.acml_tr_pbmn` 2순위 + graceful — **KIS 호출 0건 추가** (scanner 가 이미 `fetch_rising_stocks::fetch_stock_detail` 보강 중). **Q3 순차 hook** + funnel `step_no=97`. **Q6-1 (HIGH) 09:00 race graceful 영속** (시스템 매매 무용 차단). **H-2 NO_TRY 답습** (사이클 64 hotfix 패턴). 회귀 가드 31 함수 (28 케이스 12 파일, HIGH 3). 백엔드 1939 → **1970 PASS** (+31 = 신규 26 + 사이클 64 vacuous PASS 5 진정한 PASS 전환) / 프론트 166 → **170 PASS** (+4) / 전체 **2140 PASS** / 회귀 0 / flakiness 0 (3 회 1136 ±0.1s) / coverage 80.93%. 매매 안전성 무영향 (디폴트 0 + 사이클 38 명문화 + 사이클 32 R4 보호 영속). **사이클 62 갭상승 회피 효과 폐기 보강 80~90%** (작전주 차단 + 정상 IPO/우량주 보존). 잔존 10~20% = 시스템 본질 한계. | `src/db/system_config.py` (TradeAmountFilter + 2 함수) / `src/engine/scanner.py` +234L (ticker_market_info `trade_amount_raw` 키 + 5 함수 + 4 모듈 전역 + subscribe_filtered_stocks 4 hook 추가) / `src/engine/scheduler.py` +10L (H-2 NO_TRY emit + reset 동행) / `src/routes/system.py` (GET/PUT + module-reference import) / `frontend/src/types/trade-amount-filter.ts` 신규 / `frontend/src/api/trade-amount-filter.ts` 신규 / `frontend/src/components/TradeAmountFilterCard.tsx` 신규 167L / `frontend/src/pages/Settings.tsx` (+5L) / `frontend/src/test/handlers.ts` (+14L) / `tests/unit/engine/scanner/test_cycle65_*.py` 6 파일 + `tests/unit/db/test_cycle65_*.py` + `tests/unit/routes/test_cycle65_*.py` + `tests/unit/engine/test_cycle65_scheduler_integration.py` + `tests/integration/test_cycle65_*.py` 2 + `frontend/src/components/__tests__/TradeAmountFilterCard.test.tsx` (28 케이스 신규) / `src/engine/CLAUDE.md` + `src/db/CLAUDE.md` + `frontend/CLAUDE.md` 동기화 | Q7-5 (사이클 64 자문) HIGH 즉시 발주 확정 인계 — 사이클 62 갭상승 회피 효과 폐기 위험 보강. 사이클 64 패턴 직답습 (HIGH 3 = C 2 + G 1) + Q2 옵션 C 통합 폴백 (KIS 호출 0건 핵심 발견) + Q6-1 09:00 race graceful (시스템 매매 무용 차단). 사이클 64 vacuous PASS 5 진정한 PASS 전환 (사이클 65 hook 추가 효과). **카드 #16 (MEDIUM, Q6-4)** 후보 풀 폭축 역설 risk 2주 회고 — 사이클 67+ 발의. 사이클 49→65 누적 17 사이클. 운영 2주 후 정량 측정 의무 (사이클 62 폐기 효과 회복 검증). |
| 2026-06-06 | 사이클 64 가격 필터 scanner 이전 + 단순화 (사용자 의도 재정의 — WebSocket 구독 *전* 종목 풀 차단, 사이클 62 매수 신호 판단용 위치 폐기). domain-expert 옵션 A 자문 Q1~Q6 + Q7-1~Q7-5 전부 적용. **mode 폐기** (3 모드 → 단순 임계 필터), **risk.on_tick 분기 폐기** (G3 -178L), `scanner.subscribe_filtered_stocks` 단일 hook (Q4 옵션 A). **Q1 옵션 D 3 중 안전망 (HIGH)**: `_collect_protected_tickers_for_scanner` 헬퍼 + early-return + AST keyword 가드 (사이클 32 R4 universe guard 답습). **Q2 prdy_clpr 단독** + graceful 통과 + KIS pre-fetch 비채택. **Q7-1 unsubscribe 0 발화** (KIS LMS chain 차단). **hotfix**: scheduler.py:638 사이클 62 폐기 메서드 호출 → scanner.emit_price_filter_scanner_daily_summary (2 줄 교체) + H-2/G-3 영구 가드 (AST). 회귀 가드 28 케이스 (HIGH 8 / 12 파일). 사이클 62 폐기 9 파일 + 33 케이스 + 프론트 1 케이스 = 34 정리. 백엔드 1944 → **1939 PASS** (사이클 64 +28 - 사이클 62 -34 + hotfix +2 -1 = 정합) / 전체 **2105 PASS** / 회귀 0 / flakiness 0 (3 회 1117 ±0.11s) / coverage 80.79%. 매매 안전성 무영향 (사이클 38 명문화 영속). | `src/db/system_config.py` (mode 폐기, 2 키) / `src/engine/risk.py` -178L (가격 필터 전체 폐기) / `src/engine/scanner.py` +250L (신규 영역) / `src/engine/scheduler.py` +13L (settle 직전 hotfix + reset 동행) / `src/routes/system.py` (mode 인자 제거 + extra="forbid" + scanner invalidate) / `frontend/src/components/PriceFilterCard.tsx` -88L (mode select 폐기) / `frontend/src/types/price-filter.ts` (mode 필드 제거) / `frontend/src/components/__tests__/PriceFilterCard.test.tsx` (mode 토글 폐기) / `tests/unit/engine/test_cycle64_*.py` 9 신규 + contract/integration 2 신규 + frontend 1 갱신 (28 케이스) / 사이클 62 9 파일 git rm / `src/engine/CLAUDE.md` + `src/db/CLAUDE.md` + `frontend/CLAUDE.md` 동기화 | 사용자 의도 재정의: 사이클 62 `risk.on_tick` (매수 신호 판단용) → scanner 단계 (WS 구독 *전* 종목 풀 차단) — 자원 절약 + 후보 풀 자체 축소 (사이클 32 R4 universe guard 보완). 사이클 60~63 답습 패턴: 카테고리 분리 측정 + flakiness 3 회 + sys.modules.get + AST 정적 가드. tester verify 결함 1건 (HIGH 운영 가시화) hotfix 2 줄 + H-2/G-3 영구 가드 (사이클 60 hotfix 패턴 답습). **사이클 65 거래대금 동행 필터 즉시 발주 확정** (Q7-5 HIGH — 사이클 62 갭상승 회피 효과 영구 폐기 위험 보강). **카드 #15 (Q7-2, LOW)** 액면분할 prdy_clpr invalidate 후속 발의. 사이클 49→64 누적 16 사이클. |
| 2026-06-06 | 사이클 63 Phase 2-A3 — refactor #2 stale_manager 추출 3단계 (HIGH, K stale watcher 핵심 2 함수 322L, refactor #2 **완료**). domain-expert 옵션 A 자문 (Q1~Q4 + Q5-1~Q5-3) 전부 적용 — Q4=B 직접 호출 (사이클 60 답습 안 함 *유일 영역*) + Q2 try/except 4 중 가드 보존. `_check_and_resubscribe_stale` 222L + `_resubscribe_stale_priority` 100L. 누적 9 → 11 함수 (상수 0 추가). scheduler.py 3,315 → 3,007L (-308L). stale_manager.py 731 → 1,076L (+345L). **누적 scheduler.py 감소** (사이클 51 직전 ~4,185 → 3,007): **-1,178L, -28%**. 회귀 가드 29 케이스 (12 파일, HIGH 11 = 38% 전수 PASS). 백엔드 **1915 → 1944 PASS** (+29) / 전체 **2111 PASS** / 회귀 0 / flakiness 0 (3 회 ±1.15s) / coverage 81.06%. 매매 안전성 무영향 (행위 보존). 사이클 29 005935 사고 패턴 차단 영속 (H 4 freezegun 재현 PASS). | `src/engine/stale_manager.py` +345L (731→1,076) / `src/engine/scheduler.py` -308L (3,315→3,007, 2 wrapper) / `tests/unit/engine/test_cycle63_phase2A3_*.py` 12 파일 신규 (29 케이스) / `tests/unit/engine/test_scheduler_stale_force_retry.py` + `test_scan_loop_stale_priority.py` patch 경로 수정 (A3 이주 영향) / `src/engine/CLAUDE.md` 모듈 맵 (11 함수) + 본문 사이클 63 행 + refactor #2 완료 표기 / `_workspace/test_index.yaml` 영향 인덱스 | refactor #2 stale_manager 3 단계 분할 **완료** (A1+A2+A3 = 11 함수 + 10 상수 누적 ~937L 이주). 사이클 51 boot_manager 답습 + Q4=B 직접 호출 = 사이클 60 패턴 답습하지 않는 *유일 영역* (HARNESS_CHANGELOG 명시 의무). sys.modules.get 패턴 + try/except 4 중 가드 보존 (`getattr` 폴백 silent 실패 위험 비채택). **주말 push 의무** (토 2026-06-06) + **월요일 (2026-06-09) 09:00~10:00 1h tester verify 의무** (시나리오 A/B/C — 사이클 60 §Q7 영속). **사이클 64+ 후속 카드 인계**: **#5 HIGH** Q5-3 cap=10 결함 시정 (priority 분리 *후* `[:cap]` 적용) + **#14 MEDIUM** Q5-2 stale_manager.py 1,076L sub-module 분해 (3+1 청사진). 사이클 49→63 누적 15 사이클 (refactor-review 임계 5회 *3 배 초과*). |
| 2026-06-05 | 사이클 62 가격 필터 — 매수 진용 가격대 차단 신규 기능 (백엔드 + 프론트 단일 사이클, MEDIUM, domain-expert 옵션 A 자문 전부 적용). `system_config` 3 키 (`price_filter_min` / `price_filter_max` / `price_filter_mode`, 디폴트 0/0/OFF). risk.on_tick 단일 진입점 (`check_exit_signal` 분기 *후*, `check_buy_signal` *전* — 사이클 38 명문화 영속). **3 모드** (HARD/WARN/OFF). **Q2 fallback**: `scanner.ticker_prev_close` 우선 + `current_price` fallback (갭상승 시점 결함 회피). 60s TTL 캐시 (사이클 56-E 답습). **Q3 가드**: 시장가 거부 5호가 폴백 시 필터 재평가 0건 (E-4 AST 정적 영속). 일일 집계 `[price_filter_daily_summary]` `_settle()` 직전. 신규 라우트 `GET/PUT /api/system/price-filter`. 신규 UI `PriceFilterCard` (9 testid + 권장값 툴팁 5,000/1,000,000). 회귀 가드 38 케이스 (백 33 + 프 5). 백엔드 1882 → **1915 PASS** (+33) / 프론트 160 → **167 PASS** (+7) / 전체 **2082 PASS** / coverage 80.96% / flakiness 0 (3 회 반복) / 회귀 0 / V12 백엔드↔프론트 정합 100%. **매도 영향 0** (사이클 38 명문화 영속 + Q3 가드). | `src/db/system_config.py` (PriceFilter Pydantic + 3 키 헬퍼) / `src/engine/risk.py` (on_tick 필터 분기 + 60s TTL 캐시 + emit 3종 + reset 확장) / `src/engine/scheduler.py` (`_settle` 직전 daily_summary) / `src/routes/system.py` (GET/PUT + invalidate 즉시 반영) / `frontend/src/types/price-filter.ts` 신규 / `frontend/src/api/price-filter.ts` 신규 / `frontend/src/components/PriceFilterCard.tsx` 신규 282L / `frontend/src/pages/Settings.tsx` (+3) / 테스트 11 파일 신규 (백 10 + 프 1) / `src/db/CLAUDE.md` + `src/engine/CLAUDE.md` + `frontend/CLAUDE.md` 동기화 | domain-expert 자문 Q1~Q6 + Q7~Q11 전부 적용 (옵션 A) — Q2 fallback / Q4 3 모드 (HARD 단일 반박, 시장 신뢰 형성) / Q5 즉시 + 60s TTL (5분 grace 금지). 사이클 60/61 hotfix flakiness 차단 패턴 영속 (카테고리 분리 측정 + 3 회 반복). 사이클 38 명문화 (`tradable_boards` 매수 진입 전용) 답습 + Q3 추가 가드 (order_engine 폴백 무변경). 매매 안전성 무영향 — 디폴트 OFF + 매수 차단만 (자금 손실 영역 아님). Q7 거래대금 동행 필터 = **사이클 63 별개 카드 #13** 발의. tester V12 백엔드 ApiResponse<PriceFilter> 래퍼 ↔ 프론트 `data.data` 추출 정합성 100% 확인. |
| 2026-06-05 | 사이클 61 Phase 2-A2 — refactor #2 stale_manager 추출 2단계 (MEDIUM, 4 함수 314L + 5 상수 추가 이주). 사이클 60 A1 패턴 답습. `_detect_silent_inactive_sessions` (62L, KIS LMS/앱키 정지 위험) + `_force_reconnect_session` (83L) + `_delta_unsubscribe_dropped` (52L) + `_evaluate_universe_guard` (117L, 보유/익일청산 보호). 5 상수 추가 (`STALE_FRESHNESS_SECS` + `SILENT_INACTIVE_*` 4). 누적 9 함수 + 10 상수. **사이클 60 lazy import 빚 청산** + `sys.modules.get` 패턴 (D-1 AST 가드 통과 + patch 호환). scheduler.py 3,605 → 3,315L (−290L). stale_manager.py 358 → 731L. 회귀 가드 20 케이스 (HIGH 3: C cap dict / I universe 보유 / J universe 익일청산). 백엔드 **1862 → 1882 PASS** (+20, 회귀 0) / coverage 80.66% / flakiness 0 (3 회 반복 확인). 매매 안전성 무영향 (행위 보존). 누적 scheduler.py 감소: 사이클 51 전 ~4,185 → 현 3,315 (**−870L, −21%**). | `src/engine/stale_manager.py` +373L (358→731) / `src/engine/scheduler.py` −290L (3,605→3,315) / `tests/unit/engine/test_cycle61_phase2A2_*.py` 12 파일 신규 (20 케이스) / `src/engine/CLAUDE.md` 모듈 맵 + 본문 사이클 61 행 / `_workspace/test_index.yaml` 영향 인덱스 갱신 | A1 완료 후 도메인 자문 옵션 B 3 단계 분할의 2 단계 — 사이클 60 패턴 직답습 (logger 명시 binding / re-export / 12 파일 분리). tester 카테고리 분리 측정 + flakiness 3 회 반복 = 사이클 58/60 hotfix 패턴 영구 절차화. A3 (사이클 63) = `_check_and_resubscribe_stale` 221L + `_resubscribe_stale_priority` 100L = WebSocket 4 중 안전망 핵심 — 주말 push 의무 + 월요일 1h verify. 신규 발견: stale_manager.py 731L 비대화 → 향후 sub-module 분해 (cap_per_hour / fresh_ratio / universe_guard) refactor-expert 검토 대상 등록 권고. |
| 2026-06-04 | 사이클 60 Phase 2-A1 — refactor #2 stale_manager 추출 1단계 (LOW, 5 함수 + 5 상수). domain-expert 자문 옵션 B 3 단계 분할 전체 채택 — A2/A3 별도 사이클 (61/62). `src/engine/stale_manager.py` 신규 358L (사이클 51 boot_manager 패턴 답습 — scheduler 인자 + 2 줄 wrapper 위임). scheduler.py 3,880 → 3,605L (−275L, −7.1%). hotfix 1건: logger `__name__` → `"src.engine.scheduler"` 명시 binding (사이클 28 회귀 가드 4 FAIL → 4 PASS + I1 영구 가드 신규). 백엔드 **1844 → 1862 PASS** (+18, 회귀 0) / coverage 80.62%. 매매 안전성 무영향 (행위 보존). | `src/engine/stale_manager.py` 신규 / `src/engine/scheduler.py` 5 상수 re-export + 5 wrapper / `tests/unit/engine/test_cycle60_phase2A1_stale_manager.py` 18 케이스 / `src/engine/CLAUDE.md` 모듈 맵 + 본문 사이클 60 행 / `_workspace/test_index.yaml` 영향 인덱스 | refactor-review 2026-06-04 카드 #2 (HIGH) 의 A1 단독 진행 — 사이클 51 boot_manager 답습 + 도메인 자문 (영구 hot path / KIS LMS/앱키 정지 chain 위험) 반영해 3 단계 분할 채택. tester 가 카테고리 분리 측정으로 flakiness (4 FAIL caplog 누수) 영구 차단 — 사이클 58 hotfix 패턴 답습. 별개 위험 식별: `src/engine/*.py` 14 모듈 `__name__` logger 사용 — 운영 logging.yaml 한정 핸들러 영향 잠재 (별도 risk-review 카드 후보). Q6 별개 카드 #11 (보드 전환 ↔ K stale watcher mutex) 본 사이클 종료 후 발의. |
| 2026-06-04 | 사이클 60 hotfix — CI backend-test 6시간 hang 영구 차단 (사이클 55+ 회귀). 사이클 55 R-1 Q3 `get_balance()` 호출 추가 후 unit 테스트 mock 누락 → `.env` 없는 CI 환경에서 무한 hang → GitHub Actions 6시간 cancel (run 5건 누적). `test_b1::S3a` + `test_order_engine_sell_fallback::D1` 두 위치에 `mock_get_balance` fixture 추가 + 영구 가드 도입 (pytest-timeout 60s + ci.yml `timeout-minutes: 15` 2중). 변경 통계 5 files / +40 / production code 무변경. 백엔드 **1844 passed in 47.04s** / coverage 80.38% / 회귀 0건. | `tests/unit/engine/test_b1_market_closed_zombie_block.py` / `tests/unit/engine/test_order_engine_sell_fallback.py` / `pyproject.toml` (`timeout = 60`) / `requirements-dev.txt` (`pytest-timeout>=2.3`) / `.github/workflows/ci.yml` (`backend-test.timeout-minutes: 15`) | 사이클 58 hotfix 와 유사 패턴 (production code 변경 → mock 갱신 누락). 영구 timeout 가드로 동일 결함 재발 시 1분 내 FAIL 보장. CI run 5건 = 30시간 wasted compute 회수. 향후 tester 체크리스트 *push 후 CI conclusion 확인 의무화* 권고. |
| 2026-05-31 | 사이클 49 — VCP Pullback "마지막 폭 0.0%" 결함 시정 (운영 funnel 확정). 사이클 48 BFB/VCP 전면 시정 후에도 VCP 단독 30일 연속 0건 매매 잔존 → 5/26~5/29 funnel 33/33 우량주 step 6 동일 사유 탈락 → `_check_pullback_sequence` 의 (a) 마지막 swing 미완성 누락 + (b) 등호 포함 노이즈 swing 폭주 root cause 시정. ATR threshold ZigZag (신규 `min_swing_atr_mult=0.5`) + state machine 마지막 swing 포함 + funnel reason 정확성 (`last_pullback_pct` 항상 기록). | `src/engine/strategies/vcp_breakout.py` (DEFAULT_PARAMS + `_check_pullback_sequence` 재설계) + 회귀 가드 `tests/unit/engine/strategies/test_cycle49_vcp_pullback_width_fix.py` 6 케이스 + `_workspace/00_leader_trading_rules.md` 6-F 명세 동기화 + `src/engine/strategies/CLAUDE.md` VCP 행 갱신 | 백엔드 1748 PASS (1742→+6) / 다른 5 전략 + scan/매매 흐름 무영향 — 매수 진입 임계만 시정. 사이클 41 funnel 진단의 종결 (진단만 했고 시정 안 된 채 누적). 다음 영업일(2026-06-01) 09:30 funnel snapshot step 6 통과 카운트 > 0 확인 + 첫 거래 발생 시 4중 청산 (손절/베이스 하단/ATR 트레일링/50일 EMA) 정상 발화 tester 검증 필요. PR #16 (카피 정합화) 무관 별도 브랜치 `cycle49/vcp-pullback-width-fix` |

### 테스트 실행

```bash
pip install -r requirements-dev.txt          # 1회
python -m pytest -q                          # 백엔드 전체
cd frontend && npm install && npm test       # 프론트엔드 전체
cd .. && npx playwright install && npx playwright test --config=e2e/playwright.config.ts  # E2E

# 영향 테스트만 (PR 빠른 피드백)
python tools/test_impact/build_index.py
node  tools/test_impact/build_index_frontend.mjs
pytest $(python tools/test_impact/affected.py origin/main --target=backend)
```

## 빌드 & 실행

```bash
# Docker (권장)
docker compose up --build                                 # 개발: 프론트 :3000, 백엔드 :8002
docker compose -f docker-compose.prod.yml up --build -d   # 프로덕션: Nginx :80

# 로컬
pip install -r requirements.txt && uvicorn src.main:app --host 0.0.0.0 --port 8001 --reload
cd frontend && npm install && npm run dev
```

## 환경 변수
`.env` 필수 (`.env.example` 참고).
- `KIS_ENV`: `vts`(모의) | `real`(실전)
- `KIS_APP_KEY_REAL/VTS`, `KIS_APP_SECRET_REAL/VTS`, `KIS_ACCOUNT_NO_REAL/VTS`
- `KIS_HTS_ID`: 실전 체결통보(H0STCNI0) 구독 키
- `SUPABASE_URL`, `SUPABASE_KEY`
- `AUTO_START`: 서버 기동 시 자동 매매 시작 (DB `system_config.auto_start` 우선, 매일 시작 전 재확인)
- `DKSTOCK_REGIME_ENABLED` (기본 false): dkstock.cloud 매크로 + 매수 가드 + cash_usage_ratio 자동 조정 활성화
- `KIS_MCP_ENABLED` (기본 false): 외부 백테스트 MCP 서버 활성화. 자문 직후 6 전략 × 2 kind = 12 job fire-and-forget

## 다중 전략 (요약)

6 전략: `momentum` / `volatility_breakout` / `long_tail_volatility` / `donchian_swing` / `bull_flag_breakout` / `vcp_breakout`.

상세 매수/청산/tradable_boards/exchange 는 **`src/engine/strategies/CLAUDE.md`** 참조.

### 새 전략 추가
1. `src/engine/strategies/` 에 `StrategyBase` 서브클래스 (`prepare/check_buy_signal/check_exit_signal/calc_buy_quantity`)
2. `src/engine/scheduler.py` `__init__` 에서 `registry.register()`
3. 필요 시 `scanner.py` 에 스캔 함수 추가
4. `strategies/CLAUDE.md` 표 + `_workspace/00_leader_trading_rules.md` 명세 추가

### 자금 관리
- 프론트 Settings → `PUT /api/strategies/weights` → `StrategyRegistry.allocate_funds()`
- `position_ratio` 는 **전략 할당 자금 기준** (순자산 × 전략비중 × position_ratio = 종목당 매수금액)
- 전략 간 동일 종목 중복 매수 방지: `registry.is_ticker_blocked_for_buy()` (보유/주문중/당일매도 통합 차단)
- **`cash_usage_ratio`**: `system_config.cash_usage_ratio` 키 — `_boot()` 가 `summary.net_asset × ratio` 로 `allocate_funds()` 호출. 범위 `[0.0, 1.0]`, 5% 단위, 기본 1.0. Settings 슬라이더로 조정 → **다음 영업일부터 반영**. `auto_regime_adjust=true` (기본) + `DKSTOCK_REGIME_ENABLED=true` 시 매크로 레짐 `cash_min` 기반 자동 갱신 (`clamp((100-cash_min)/100, 0.0, 1.0)`)

### 외부 통합 (백테스트 + 매크로 레짐)
- 20:00 AI 자문 INSERT 직후 외부 MCP 백테스트 (`http://43.202.187.5:3846/mcp`) — 6 전략 × 2 kind = 12 job fire-and-forget → `parameter_recommendations.backtest_summary` JSONB
- 매크로 레짐 (`dkstock.cloud`) — `regime/vix/fear_greed` 4 임계 OR 매수 가드 (4 모드: HARD/WARN/SOFT/OFF) + `cash_usage_ratio` 자동 조정
- 활성화 토글: `KIS_MCP_ENABLED` / `DKSTOCK_REGIME_ENABLED` (Settings UI 즉시 토글 가능). 외부 다운 시 graceful — 자문 INSERT 보존, summary=null, 매수 가드 비활성
- 운영 가이드: [`docs/backtest-monitoring.md`](docs/backtest-monitoring.md)

## 핵심 안전 규칙 (절대 깨지 말 것)

상세 메커니즘은 `src/engine/CLAUDE.md` · `src/realtime/CLAUDE.md` 참조. 여기서는 **금기**만:

- **체결통보 구독 (H0STCNI0/H0STCNI9) 제거 금지** — 미구독 시 포지션 등록·손절 불가
- **uvicorn 단일 워커 필수** — `--workers` 금지 (스케줄/포지션/WebSocket 중복)
- **주문번호 매핑** (`_order_qty/_order_strategy/_order_ticker/_pending_buy_orders`) 등록은 `place_order` 응답 직후 동기 영역, `await insert_trade` 진입 전 — 시장가 즉시체결 race 시 매핑 누락하면 기본값 "momentum" 으로 잘못 INSERT됨
- **체결통보 선행 race 가드** (`_completed_orders` set + UPDATE 0건 보정 INSERT) 제거 금지 — 시장가 즉시체결 + REST 응답 지연 시 trade_history 가 PENDING 영구 잔존
- **`_reset_daily_state()` 제거 금지** — 정산 후 미초기화 시 pending_buys/positions/sold_today 가 다음 날까지 잔류
- **익일 청산** 은 scheduler 에서 시가 수신 후 30s 안정화 처리 — `_pending_next_day_clear` 보류 후 09:00 KRX 시장가. `high_since_buy` 폴백 금지. on_tick 즉시 청산 금지
- **NXT 매도 거부 좀비 차단** — `is_market_closed_rejection` (APBK0918 + 장운영시간 외) 이면 `execute_sell` 이 positions(메모리/DB) 보존 + 재시도 중단. `is_insufficient_quantity` / `is_insufficient_cash` 와 분리. **사이클 55 R-1 (2026-06-03) `SellRejectionTracker.is_blocked()` 진입 게이트**: **2단계 TTL** — KRX 메인(09:00~15:30) 거부 = 5분 TTL (일시 장애 가정), NXT 시간대(08:00~09:00 / 15:30~20:00) 거부 = 다음 KST 09:00 TTL. `market_order_disallowed` 거부 = 30초 TTL (동일 tick 폭주 차단). NXT 폴백 실패 시 `_pending_next_day_clear` 익일 청산 자동 전환. `_reset_daily_state` 동행 clear (`_sell_rejection.reset_daily()` 4 필드 일괄 위임, `OrderEngine.reset_daily_state()` 캡슐화 보존). 호환 layer property `_market_closed_blocked` / `_market_closed_blocked_logged_today` 는 tracker 내부 dict/set 직접 노출 (is 동일성 보장)
- **매수/매도 시장가 거부 → 지정가 5호가 폴백 1회** — `is_market_order_disallowed` (msg1 키워드 `시장가매매불가` / `시장가호가불가` / `최유리/최우선지정가 주문만` / `지정가 및 최유리` — APBK1943/APBK3013) 매칭 시 `step_up(buy)/step_down(sell)` 으로 `LIMIT` 재시도. 매핑 동기 + race 가드 동일 규약
- **KIS 거부 응답 영구 저장** — `_request` 가 `rt_cd != "0"` 시 `system_logs` prefix `[kis_rejection]` + path/tr_id/msg_cd/msg1 + body 주요 키 (민감 키 마스킹) fire-and-forget
- **WebSocket 시세 보유·익일청산 우선 보장** — `MAX_SUBSCRIPTIONS=41` KIS 공식 한도. HIGH (보유/익일청산) `bypass_limit=True` 절대 보장. 후순위 drop 시 `[priority_drop]` INFO + WARNING `system_logs`. HIGH 단독 41 초과 ERROR
- **WebSocket 다중 안전망** — F1 (재연결 1회) + `_scan_loop` (5분) + K stale watcher (120s, 1~5회 즉시 강제 재등록 + 사이클 29-R1: 6회 초과 시 5분 cooldown 기반 시간 기반 force_retry + 시간당 12회 cap) + `_resubscribe_stale_priority` (5분 우선) 4중. K stale watcher 양쪽 분기에 우선순위 분리 (사이클 29-R3: positions/`_pending_next_day_clear` HIGH+bypass=True, 그 외 후보 LOW+bypass=False — 메인 편중 차단). `_subscriptions` ACK 정합성 가드 (orphan ACK race 차단)
- **세션 단위 silent inactive 자동 reconnect** — `_detect_silent_inactive_sessions` 3중 가드 (사이클 29-R2): `fresh_ratio < SILENT_INACTIVE_FRESH_RATIO_THRESHOLD(=0.2, 20%)` + `subscribed_count >= 5` + 5분 지속 → `_ws.close()` 강제 reconnect. 시간당 세션당 2회 cap (LMS/앱키 정지 위험 차단)
- **stale universe 가드** (사이클 32) — `_evaluate_universe_guard`: stale>5 + `today_volume < UNIVERSE_LOW_VOLUME_THRESHOLD(=10_000)` 종목 자동 unsubscribe + `_universe_excluded_today` 등록 + `[universe_excluded]` INFO + `inquire_ccnl` 으로 마지막 체결시각 로그. 보유/익일청산 절대 보호 + `_reset_daily_state` 동행 clear (영구 블랙리스트 금지)
- **`trade_history` 중복 INSERT 차단** (사이클 30) — `_sync_orders_to_db` 는 `get_today_buy_trades_for_sync()` / `get_today_sell_trades_for_sync()` 사용 (dedupe 없음 + CANCELLED 제외). DB 부분 UNIQUE 인덱스 `(ticker, order_no, trade_type)` 이중 안전망. 기존 `get_today_buy_trades()` 의 ticker dedupe 는 포지션 복구용 — 절대 sync 중복 판정에 사용 금지
- **NXT 거래가능 사전 판별** — `stock_master.nxt_tradable=False` 면 NXT/SOR → KRX 강제 다운그레이드 + `[nxt_downgrade]`. `_boot()` eager 사전 갱신 (보유 + `_pending_next_day_clear` 합집합). 거부 사후 보강 `stock_master.upsert_one(ticker, nxt_tradable=False)`
- **종목코드 형식 비대칭** — 진입은 6자리 숫자만 (`isdigit()`), 사후처리는 6자리 영숫자 (`isalnum()`) — ETF·신주인수권 자동매매 차단 + 좀비 포지션 방지
- **1주 폴백은 전략 잔여 자금 기준** — 6 전략 `calc_buy_quantity()` 가 `StrategyBase._fallback_one_share(current_price)` 공통 헬퍼. 잔여 = `total_investment - (positions buy_price×qty + pending_buy_amounts 합)`
- **VB 당일 15:20 일괄매도** — `DEFAULT_TRADABLE_BOARDS=("main",)` (사이클 26), POST_NXT 추가 금지. `_force_clear_main_only` 가 15:20 일괄 청산. 15:30 이후 호출은 시간 가드로 skip
- **`tradable_boards` 는 매수 진입 전용** (사이클 38, 2026-05-22 명문화) — 매도/손절/Trailing/익일청산/15:20 강제청산/상한가 손절 모니터링은 어떤 전략에서도 PRE/MAIN/POST 무관 항상 작동. `risk.on_tick` 의 `check_exit_signal` 분기는 `session_tracker.is_tradable` 검사 *전* 진입. LTV `DEFAULT_TRADABLE_BOARDS=("pre_nxt", "main", "post_nxt")` (사이클 38 사용자 의도 복원 — 연속 상한가 익일 청산 + 야간 매수)
- **donchian_swing `_swing_rest_poll_loop`** 제거 금지 — 09:30~15:20 60s REST 폴링으로 멀티데이 손절 평가 보강
- **모든 시각 데이터 KST 강제** — 백엔드 `_to_kst(iso)` 헬퍼 + `_today_kst_iso()` timezone 명시 (`+09:00`). 프론트 `Intl.DateTimeFormat(timeZone='Asia/Seoul')` 명시. `new Date(iso).getHours()` 브라우저 로컬타임 추출 금지
- 매매 파라미터 (`DEFAULT_PARAMS`) 변경 시 `_workspace/00_leader_trading_rules.md` 동기화

### 코딩 컨벤션
- Python: pydantic + async/await
- TS: 모든 API 응답은 `frontend/src/types/` 정의 사용
- API 응답 래퍼: `{ success: bool, data: T, message: str }` (`models/response.py` `ApiResponse`)
- KIS 호출은 반드시 `src/api/base.py::kis_request()` 또는 `kis_get_quote()` 경유 (Rate Limit·재시도·메트릭)
- TR_ID 는 `settings.get_tr_id()` 사용 — 하드코딩 금지

## DB 스키마 (Supabase)

마이그레이션: `supabase/migrations/`. CRUD 모듈 상세: `src/db/CLAUDE.md`.

| 테이블 | 용도 |
|--------|------|
| `trade_history` | 거래 내역 (status: PENDING/COMPLETED/PARTIAL/CANCELLED) |
| `daily_performance` | 일일 실적 (date+strategy 복합PK, TWR 누적, 실현손익 기준) |
| `positions` | 보유 포지션 영속화 (ticker PK) |
| `strategy_config` | 전략 설정 (strategy_id PK, params JSONB) |
| `system_config` | 시스템 설정 (auto_start, cash_usage_ratio, buy_block_mode + 4 임계값, dkstock_regime_enabled, kis_mcp_enabled 등) |
| `system_logs` | 시스템 로그 |
| `parameter_recommendations` | 20:00 AI자문 (target_date+strategy_id UNIQUE). `recommended_weight`/`code_review_notes`/`applied_weight`/`weight_reasoning`/`backtest_summary` JSONB |
| `daily_log_reports` | 20:10 일일 로그 분석 (target_date UNIQUE, metrics 에 api_metrics/strategy_funnel/by_ticker_pnl/by_hour_pnl/next_day_clear 포함). **사이클 58 V-2**: input_tokens/output_tokens/total_tokens/latency_ms/cost_estimate_usd 5 컬럼 추가 (migration 031, 모두 NULL 허용) |
| `stock_master` | KIS CTPF1002R 캐시 (ticker PK, 24h TTL). NXT 거래가능 사전 판별 |
| `backtest_runs` | 외부 MCP 백테스트 영속화 (`(target_date, strategy_id, params_kind)` UNIQUE. 6 전략 × 2 kind = 12 row/사이클) |
| `market_regime_snapshots` | dkstock.cloud 매크로 일일 스냅샷. `_boot()` 시점 1행. `buy_blocked`/`computed_cash_usage_ratio`/`raw_response JSONB` 영구 기록 |
| `kis_quote_accounts` | 보조 KIS 시세 수신 계좌 (UUID PK, label UNIQUE, active=true 부분 인덱스). `list_accounts()` 60s TTL 메모리 캐시 |
| `strategy_funnel_snapshots` | 사이클 34 — 전략별 조건검색 단계별 후보/탈락 종목 영구 추적. `(target_date, strategy_id, step_no, snapshot_at)` UNIQUE. `survived_tickers` JSONB cap 200 / `excluded_sample` JSONB cap 20. 수동 trigger `POST /api/strategy-funnel/snapshot` (현재 최종 단계 `step_no=99` 만, 자동 hook 은 후속 사이클) |

> **`trade_history` 부분 UNIQUE 인덱스 (사이클 30, migration 029)**: `uq_trade_history_ticker_order_no_type ON (ticker, order_no, trade_type) WHERE order_no IS NOT NULL AND order_no != ''`. `_sync_orders_to_db` 핑퐁 INSERT 영구 차단 + NULL/빈 order_no (수동 매매 사전 등) 호환.

## Docker / 배포

- `Dockerfile` / `frontend/Dockerfile` 멀티스테이지 (dev: hot-reload / prod: non-root + Nginx)
- `docker-compose.yml` (개발 hot-reload) / `docker-compose.prod.yml` (prod)
- **토큰 캐시 영속화 (사이클 20, 2026-05-20)**: `./.token_cache:/app/.token_cache` 디렉토리 볼륨 양쪽 compose 동일 마운트. KIS `/oauth2/tokenP` 분당 1개 한도 + 컨테이너 재기동 시 토큰 24h 유효 보존. `.gitignore` 등록 (`.token_cache/` + 구 `.token_cache_quote_*.json` 호환). 구 경로 `.token_cache.json` 존재 시 자동 마이그레이션
- **`.token_cache` 빌드 시점 권한 보장 (사이클 22, 2026-05-20)**: `Dockerfile` prod 스테이지가 `mkdir -p /app/.token_cache` → `chown -R appuser:appuser /app` → `USER appuser` 순서. 호스트 bind mount 가 root:root 로 생성되어 `appuser` 가 쓰기 거부되던 결함 영구 차단 (회귀 가드: `tests/integration/test_dockerfile_token_cache_perms.py`)
- `frontend/nginx.conf`: 정적파일 + `/api` → backend:8000 프록시
- 타임존 `TZ=Asia/Seoul`, vite 프록시 타겟은 `VITE_API_URL` 분기
- **EC2 t4g.small (ARM, ap-northeast-2)** 서비스 경로 `~/auto_stock/`
- 자동 배포: `git push origin main` → GitHub Actions 가 EC2 SSH → `git pull` + 재빌드 (`.github/workflows/deploy.yml`). push 시각 → pool_start 지연 1~5분
- GitHub Secrets: `EC2_HOST`, `EC2_USERNAME`, `EC2_SSH_KEY`
- **로컬과 EC2 동시 실행 금지** — KIS 동일 계정 동시 접속 충돌
- 운영 가이드: KRX 메인 시간 (09:00~15:30) 중 빈번한 push 자제 — `_scan_loop` 5분 race 가능. NXT 애프터 (15:30~) 또는 익일 07:50 _boot 전 push 권장

## 디렉토리 역할
- `src/auth/` — KIS OAuth 인증/토큰 (메인 + 보조 multi)
- `src/api/` — KIS REST (주문·잔고·조건검색·일봉) + 시세 풀 (`base.py::_request_via_quote_pool` + path 화이트리스트 가드). 사이클 32: `quotation.py::inquire_ccnl(ticker, market='J')` 신규 (FHKST01010100 주식현재가 시세, output[0] + today_volume 합산 + graceful None)
- `src/realtime/` — KIS WebSocket (시세·체결통보·H0NXMKO0) + WebsocketPool 멀티 세션 분배
- `src/engine/` — 매매 핵심 (전략·레지스트리·주문·리스크·스케줄러). `recommendation_engine.py` 20:00 AI자문 / `log_analysis_engine.py` 20:10 일일 분석 / `backtest_engine.py` + `backtest_yaml.py` / `market_regime.py`
- `src/engine/strategies/` — 6 전략 명세 (전용 CLAUDE.md)
- `src/services/` — 외부 서비스 클라이언트 (`mcp_client.py` 백테스트 MCP / `dkstock_client.py` 매크로 / `quote_session_health.py` 보조 세션 health monitor)
- `src/db/` — Supabase CRUD
- `src/routes/` — FastAPI 엔드포인트
- `src/models/` — Pydantic 모델
- `frontend/` — React 대시보드
