# CLAUDE.md — 프로젝트 루트

KIS OpenAPI 기반 주식 자동매매시스템. FastAPI(백엔드) + React(프론트엔드) + Supabase(DB). 다중 전략 아키텍처.

> 디렉토리별 상세는 각 하위 `CLAUDE.md` 가 진실의 원천:
> `src/CLAUDE.md` · `src/engine/CLAUDE.md` · `src/engine/strategies/CLAUDE.md` · `src/api/CLAUDE.md` · `src/realtime/CLAUDE.md` · `src/db/CLAUDE.md` · `src/routes/CLAUDE.md` · `frontend/CLAUDE.md`
>
> 사이클별 변경 이력 (verbatim): [`docs/HARNESS_CHANGELOG.md`](docs/HARNESS_CHANGELOG.md)
> 시스템 흐름 도식: [`docs/architecture.md`](docs/architecture.md)

## 하네스: TDD-First Trading Team

**목표:** 모든 코드 변경을 Red→Green→Refactor 사이클로 강제하고, 변경 시 영향받는 테스트만 실행할 수 있는 정적 인덱스를 유지한다. *매매 의사결정의 깊이* 는 도메인 전문가의 사전 자문으로 보완하고, *코드 품질 드리프트* 는 리팩토링 전문가의 주기적 검토로 흡수한다.

### 기본 진입점 — `team-leader` 우선

사용자의 모든 요청은 1차로 `Agent({subagent_type: "team-leader"})` 로 라우팅한다. team-leader 가 트레이더 관점에서 해석 후 하위 에이전트에 분배한다.

- 코드 변경 → `auto-trading-orchestrator` 스킬 (TDD 사이클: `tdd-engineer` Red → `backend-dev`/`frontend-dev` Green → `tester` 검증)
- 매매 의사결정 자문 (신규 전략·파라미터·보드 행태·시장 레짐·KIS 거부 해석) → `domain-consult` 스킬 (`domain-expert` 에이전트) — Phase 2.5 명세 분해 *전* 또는 사이클 중 행위 영향 평가 시
- 주기적 리팩토링 검토 (사이클 5회 누적 또는 명시 요청) → `refactor-review` 스킬 (`refactor-expert` 에이전트) — Phase 4.5
- 단위/회귀 테스트 → `tdd-cycle` (백엔드 pytest+respx+freezegun / 프론트엔드 vitest+RTL+MSW)
- 영향 인덱스 → `test-impact-index`
- 통합/경계면/E2E/안전성 → `trading-test`
- KIS API 정본 스펙 (TR_ID·응답 구조·거부 코드) → `kis-mcp-query` 스킬 (backend-dev / tdd-engineer / tester / refactor-expert 공유)

**우회 허용 (메인 세션 직접 응답):** 단순 사실 질의, 단발 디버그/grep, 운영 환경 즉시 점검(EC2 SSH 등). 코드 변경 제안이 따라오면 다시 team-leader 로 인계.

### 모델 라우팅

| 작업 유형 | 모델 | 적용 |
|----------|------|------|
| 구현 계획·검수·리팩토링 검토 | **fable** | `team-leader`, `tester`, `refactor-expert` |
| 테스트 설계·도메인 자문 | **opus** | `domain-expert`, `tdd-engineer` |
| 일반 구현 (코드 작성·리팩터·버그 수정) | **sonnet** | `backend-dev`, `frontend-dev` |
| 명령어 작성 (bash/슬래시/스크립트) | **haiku** | 메인 세션 단발 작업 — fork 또는 `claude-haiku-4-5-20251001` 위임 |

### 하네스 변경 이력 (요약)

각 행은 한 줄 요약. 사용자 보고 verbatim / 회귀 가드 / 영속 의무 매트릭스 / 운영 효과 등 상세는 [`docs/HARNESS_CHANGELOG.md`](docs/HARNESS_CHANGELOG.md) 참조.

| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-07-04 | 사이클 193 — 재시작 immediate run 신선도 게이트 (아침 프리마켓 burst 부하 완화, MEDIUM 운영 안정성 / 매매 무관) | `src/db/system_config.py` (+34L `get_task_last_success(task_label)` = `_get_string_or_none` 경유 189 retry 수혜 + `set_task_last_success(task_label, iso_ts)` = upsert, 쓰기 retry 미경유) / `src/engine/task_loop_helper.py` (+50L `IMMEDIATE_FRESH_SKIP_HOURS=20.0` 상수 + `immediate_skip_if_fresh_hours: float\|None=None` 게이트 — 지정 시 immediate 블록에서 마커 `0<=elapsed<hours*3600` 이면 once() skip + INFO, once() 성공 직후 마커 기록 graceful, **정기 while 발화는 게이트 무관 무조건 실행** = 188 정기 발화 복원 보존, None = 마커 접근 0건 회귀 보존) / `src/engine/scheduler.py` (+10L basics/master 2 wrapper 인자 + 미대상 3 task 제외 사유 주석, evening_funnel 범위 외) / `tests/unit/engine/test_cycle193_immediate_fresh_gate.py` 신규 (10) / `tests/unit/db/test_cycle193_task_marker_helpers.py` 신규 (3) / `tests/unit/ast/test_cycle193_ast_fresh_gate.py` 신규 (5 = 게이트 2 task + 미대상 4 부재) / 의미 전환 2 (cycle134 helper compact ≤150→≤200 + scheduler 라인 ≤4015→≤4025) / `_workspace/red/cycle193_immediate_run_freshness_gate.md` 신규 | 사이클 188 인계 (a) + 189 인계 (b). **배경**: `immediate_first_run=True` 가 재시작마다 무조건 풀런 — 7/3 배포 실측 basics 17분 + master 4분 즉시 실행 후 정기분과 이중 실행 = 프리마켓 burst = **187 잔존 read 실패 33건/일 공통 뿌리**. 사용자 결정 (AskUserQuestion) = 신선도 게이트 (catch-up 보존). **게이트 대상 = basics/master 2 task 만** — 커밋 전 검증 워크플로우(4차원 리뷰 + 적대 검증, 확정 4건)가 daily_load/full_universe/purge 는 자체 멱등/TTL(`skipped_fresh`/`skipped_ttl`)로 immediate 이미 저렴(benefit ~0) + 게이트 시 daily_load 후장 완결 off-by-one(MEDIUM) + populator self-heal 상실(MEDIUM) 위험을 확정 → 실제 burst 인 basics/master 로 범위 축소(사용자 재확인). **20h 산정** = 저녁 16:10 성공 → 익일 07:50 boot ≈15.7h → skip / 장애 시 ≈39h → catch-up. 오케스트레이션 = 메인 → AskUserQuestion → tdd-193 Red(18) → bdev-193 Green → 메인 검증 → 검증 워크플로우 → AskUserQuestion 범위 축소 → 메인 시정. **검증**: 격리 18 PASS ×2 + task loop 계열+db+ast 665 PASS/0 fail + **매매 안전성 8영역 diff 0 byte**. **커밋/푸시 보류**. **인계**: (a) D+1 = 익일 boot `immediate run skip — fresh` (basics/master 2행) + 187 read 실패 감소 확인 (b) 잔여 리뷰 LOW = purge SELECT retry 미경유(docstring 명문화 완료)/마커 empty-success 기록(정기발화 보완) (c) stale watcher 시간대 flaky 위생. |
| 2026-07-04 | 사이클 192 — stock_master_daily purge 루프 배치 전환 (날짜 슬라이스, HIGH 운영 안정성 / 매매 무관) | `src/db/stock_master_daily.py` (+60L/-21L — `PURGE_MAX_DATE_ITERATIONS=500` 신설 + `purge_old_rows` 날짜 슬라이스 루프 재구성: SELECT oldest bas_dd(protected 제외) → DELETE `returning="minimal"`+`count="exact"`+`.eq("bas_dd")`(protected 제외) → 누적, 예외 시 부분 누적 반환 + `type(exc).__name__` 계측(사이클 190 패턴), 시그니처/16:15 task/retention 230 불변, execute_with_retry 미경유 영속) / `tests/unit/db/test_cycle192_daily_purge_loop_batch.py` 신규 (12) / `tests/unit/ast/test_cycle192_ast_purge_loop.py` 신규 (5) / 의미 전환 2 (cycle150 G-150-DAILY-3 + cycle172 test_ret2 — bulk 체인 mock → 루프 형상, intent 보존) / `_workspace/red/cycle192_daily_purge_loop_batch.md` 신규 | 사이클 187~191 D+1 실측 검증 중 신규 발견. **결함**: `purge_old_rows` 단일 bulk DELETE (`delete().lt()`) 가 supabase-py 기본 `returning="representation"` 으로 47,924행 × raw JSONB 응답 시도 → 매 실행 실패 (**6/16 도입 이래 515건 = 전 실행 실패**, raw SQL 동일 DELETE 즉시 성공 = PostgREST 계층 결함 확정). cutoff 이전 47,924행 적체 + 매일 ~3.5K행 증가 = Supabase 용량 재초과 경로. 동일 계열 4번째 (사이클 6→150→175→192). **메인 세션 즉시 완화** = 사용자 승인 후 47,924행 raw SQL 드레인 (보유/익일청산 0 = 보호 예외 없음). 복합 PK 라 사이클 175 id 루프 직답습 불가 → 날짜 슬라이스. **핵심 가드** = SELECT 쪽 protected 제외 누락 시 never-drain 회귀 (P-3 HIGH). 오케스트레이션 = 메인 진단/드레인 → AskUserQuestion 2 → tdd-192 Red(17) → bdev-192 Green → 메인 검증. **Red 유효성**: stash 13 FAIL/4 PASS. **검증**: 격리 42 PASS ×2 flakiness 0 + db/ast 617 PASS/0 fail + scheduler 인접 18 PASS + **매매 안전성 8영역 diff 0 byte**. **커밋/푸시 보류**. **인계**: (a) D+1 = 7/7(월) 16:15 `deleted≈3575` 정상 발화 확인 (b) 187 잔존 read 실패 33건/일 (c) 191 쿨다운 실측 = 청산 대기. |
| 2026-07-04 | 사이클 191 — BFB/VCP 재진입 쿨다운 고아 배선 (영업일 2단계 등록, MEDIUM 매매 행위 — whipsaw 재매수 차단 최초 실효) | `src/api/condition.py` (+28L `add_business_days(base_date, n)` 신규 — 기존 CTCA0903R chk-holiday 1회 호출(~30일치)에서 n번째 `opnd_yn=Y` 개장일 반환, 실패/부족 시 `base+n+2` 달력일 fallback graceful, `next_trading_day` 패턴 답습·신규 TR 0) / `src/engine/strategies/bull_flag_breakout.py` (+26L net — `register_cooldown_after_exit` 즉시 근사 `today+days+2` 달력일 전환 + `_refine_cooldown_business_days` 신규(fire-and-forget, 성공 시 정확 N영업일 교체·실패 시 근사 유지) + `on_position_closed` 에 register+create_task 추가(`_partial_exit.pop` 사이클 185 계약 보존, create_task RuntimeError graceful)) / `src/engine/strategies/vcp_breakout.py` (+32L net — 동일 2단계 + `on_position_closed` **신설**) / `tests/unit/api/test_cycle191_add_business_days.py` 신규 (4) / `tests/unit/engine/strategies/test_cycle191_cooldown_wiring.py` 신규 (8) / `tests/unit/ast/test_cycle191_cooldown_ast.py` 신규 (3) / `_workspace/domain_consult/cycle191_reentry_cooldown_wiring.md` + `_workspace/red/cycle191_reentry_cooldown_wiring.md` 신규 | 사이클 185 인계 A-4 (사용자 백로그 1순위 선택). **결함**: `register_cooldown_after_exit`(BFB 3일/VCP 7일)와 매수 게이트(`cd_until >= today → NONE`)는 완비였으나 **production 호출 0건 고아** = 도입 이래 쿨다운 무발동, 청산 종목 당일 재돌파 즉시 재매수(whipsaw 무방비). domain-expert 자문 6의제 GO(`_workspace/domain_consult/`) — 배선=185 `on_position_closed` 훅(신호≠체결이라 check_exit_signal 시점 등록 NO) / 청산 유형 무구분(익절 후도 새 셋업 형성시간 > 쿨다운 = 기회손실 ~0) / 부분익절 잔량 보유 중 미발화 = registry 차단 정합 / 재시작 휘발 허용(영속화 인계) / multi-day 상태 일일 리셋 금지. **사용자 결정 2**: ① 무구분 일괄 ② 기간 = **영업일 2단계 등록**(사용자가 달력일+5 의 월요일 청산 4영업일 과잉 지적 → 재검토 → KIS MCP 확인 결과 CTCA0903R 이미 통합 + `next_trading_day` 기존재 = 영업일 환산 인프라 비용 ~0 확인 → domain 원권고(달력일+5) 대체. **BFB 3/VCP 7 값 불변** = DEFAULT_PARAMS 변경 0, `00_leader_trading_rules.md` "3영업일" 명세와 구현 최초 정합). 오케스트레이션 = 메인 스카우트 → domain-expert(dx-191) → AskUserQuestion 2 → KIS MCP 재검토 → tdd-191 Red(15) → bdev-191 Green → 메인 검증. **회귀 가드 15** (B-1~4 add_business_days 정확성/폴백 + C-1~8 배선·정정·게이트 최초 실효 end-to-end·루프 부재 graceful·185 pop 보존 + A-1 **G-191-NO-DAILY-RESET HIGH**(`_reset_daily_state`/`prepare` 의 `_cooldown_until` 접촉 0 영구 차단) + A-2 on_position_closed 내 register 호출 의무(고아 재발 차단) + A-3 order_engine 2 site 불변). **Red 유효성**: stash 시 B ImportError + C-1~6/8 + A-2 FAIL / C-7·A-1·A-3 PASS(불변식). **검증**: 격리 17 PASS ×2 flakiness 0 + strategies·api·ast+185 계약 921 PASS + engine 전수 1,838 PASS/0 fail + **매매 안전성 8영역 diff 0 byte**(변경 = condition.py 헬퍼 + strategies 2 — order_engine/게이트 본체 불변). 비용 = 청산당 KIS 1회(시세 풀 경유, 하루 수 건). **커밋/푸시 보류**. **인계**: (a) `_cooldown_until` 영속화(재시작 휘발 — 관찰 후 판단) (b) A-3 `_prev_price` cross-day (c) D+1 실측 = 청산 발생 시 `_cooldown_until` 등록 + refine 정정 로그 확인. |
| 2026-07-03 | 사이클 190 — `write_log` never-raise 전환 + 크래시 핸들러 예외타입 계측 (HIGH 운영 안정성, 매매 프로세스 크래시 클래스 영구 차단) | `src/db/system_logs.py` (`write_log` INSERT try/except Exception — 실패 시 `logger.debug("[write_log_failed]")` 단독(WARNING 이상 금지 = `_DbLogHandler` 재귀 차단, 사이클 56-E 답습), 시그니처/None 반환/KST payload 불변, `safe_write_log` 호환 유지) / `src/engine/scheduler.py` (크래시 핸들러 L920 1줄 — `"매매 프로세스 비정상 종료: {type(exc).__name__}: {str(exc)[:150]}"` 예외타입+요약 계측, `logger.exception`/사이클 146 KisApiError graceful 분기/finally 불변) / `tests/unit/db/test_cycle190_write_log_never_raise.py` 신규 (5) / `tests/unit/engine/test_cycle190_crash_handler_instrument.py` 신규 (3) / `tests/unit/ast/test_cycle190_ast_write_log_guard.py` 신규 (3) / `_workspace/red/cycle190_write_log_never_raise.md` 신규 | 7/3 아침 D+1 모니터링 중 발견 (사용자 "모니터링해보자" → "재검증" → "190 진행"). **운영 실증**: 7/3 07:59:00 `scheduler.py:677` start() "스윙 유니버스 비어있음" 분기의 bare `await write_log("INFO",...)` 가 Supabase HTTP/2 `httpcore.RemoteProtocolError(ConnectionTerminated)` 로 raise → 사이클 146 `KisApiError` graceful 분기 **미해당**(Supabase 예외) → L919 일반 except → finally task 7종 cancel + WS 3세션 종료 → **매매 시스템 종료** → 08:00:01 자동 재시작(다운 2.7분, 보유 0종목 실피해 0 + 아침 task 2벌 재실행 = 부하 2배). 재검증 = traceback 프레임(`scheduler.py:677`→`system_logs.py:56`) + "매매 프로세스 오류" ERROR 가 6/10~7/3 한 달 8회 상습(과거 건 원인은 traceback 소실 미확정) + L920 except 내부 bare write_log 2차 위험 사이트. **설계 결정 (AskUserQuestion)**: AST 스캔 = 직접 `await write_log` 72곳/나이브 미보호 21곳(order_engine 4 포함)이나 크래시 지점 L677 은 start() 거대 try 때문에 '보호됨' 오분류 = 로컬 보호 vs 파국적 외곽 try AST 구분 불가 + 72곳 전환은 기존 테스트 patch 타겟 대량 파괴 → **write_log 자체 never-raise** 채택(호출부 72곳 무변경, 단일 지점 영구 차단). **회귀 가드 11**(W-1/2 예외 5종 미전파 + W-3 debug 단독·WARNING 미발화 + W-4 KST payload 불변 + W-5 safe_write_log 보존 + H-1 예외타입 계측 + H-2 KisApiError 분기 불변 + H-3 150자 절단 + A-1 broad except·raise 0 + A-2 debug 단독·재귀 0 + A-3 계측 토큰). 의미 전환 0 (write_log raise 를 계약으로 단언한 기존 테스트 부재 확정). **검증**: Red 유효성 stash 12 FAIL/3 PASS + 격리 15 PASS + engine·db·ast 통합 2,424 PASS/1 fail — 잔존 1 = `test_cycle64_price_filter_scanner_system_config::test_A1` **기존 교차 디렉토리 오염**(190 stash 후에도 동일 재현 + 단독/db·ast 분리 실행 시 PASS = 190 무관, engine 테스트가 남기는 상태 오염 — 인계) + **매매 안전성 8영역 diff 0 byte**. **커밋/푸시 보류**. **인계**: (a) cycle64 test_A1 교차 오염 근원 격리(테스트 위생) (b) 과거 크래시 7건 원인 규명 불가 — L920 계측이 향후 커버 (c) 189 인계 지속. |
| 2026-07-02 | 사이클 189 — db read retry 확장 (F4) + reprepare WARNING DailyEmitCap (F1) (운영 안정성/관찰성, 매매 무관) | `src/db/kis_quote_accounts.py` (read 4함수 `list_accounts`/`get_account`/`get_account_by_label`/`get_credentials_for_token_manager` `asyncio.to_thread` → 사이클 187 `execute_with_retry` 경유, 쓰기·60s TTL 캐시 불변) / `src/db/system_config.py` (read 9함수 `get_cash_usage_ratio`/`get_auto_regime_adjust`/`_get_bool_or_none`/`get_buy_block_mode`/`_get_float_or_default`/`_get_bool_or_default`/`_get_int_or_default`/`_get_str_or_default_UNUSED`/`_get_string_or_none` 동일 경유, `_upsert`/`_set_*` 쓰기 미경유 영속) / `src/engine/scheduler.py` (+14L — `_reprepare_empty_logged_today: DailyEmitCap[str]` 필드 + `_reprepare_breakout_if_empty` WARNING logger+write_log 1회/전략/일 cap(getattr 후방호환 가드 = `__new__` 스텁 호환, 사이클 56-D 답습) + `_reset_daily_state` reset 배선 — **`strategy.prepare()` 재시도 행위는 cap 밖 불변** 사이클 48 회복 메커니즘 보존) / `tests/unit/db/test_cycle189_read_retry_expand.py` 신규 (6) / `tests/unit/ast/test_cycle189_ast_read_retry.py` 신규 (3) / `tests/unit/engine/test_cycle189_reprepare_emit_cap.py` 신규 (5) / `test_cycle134_task_loop_helper.py` 라인 임계 의미 전환 (≤3,990 → ≤4,015 = 188 +11L docstring + 189 +14L) / `_workspace/red/cycle189_db_read_retry_expand_and_reprepare_cap.md` 신규 | 출처 = 사용자 지시 "어제자 DB 로그분석결과 적용여부 정밀검증" → 7/1 `daily_log_reports` finding 5건 대조: F2 핵심=187 기반영, F3=기존 인계 일치, F5=부분 반영, **F4+F1 만 미반영 신규** → 사용자 "189 진행". F4 = `[kis_quote_accounts] list 실패` 11건/일(48h 실측 38건 = 187 이후 최대 ERROR 노이즈) + `[price_filter] get 실패 — 기본 0`(필터 silent 비활성) + `[system_config] get 실패`. F1 = BFB/VCP "후보 비어있음 재 prepare" WARNING **101건×4/일**(logger + write_log DB INSERT 무제한, 후보 0 = 정상 장세 가능·VCP 0/65 일상). 오케스트레이션 = 메인 스카우트 → red memo → tdd-189 Red(14, tdd-188 SendMessage 재사용 stall → 신규 스폰) → bdev-189 Green → 메인 검증 + 회귀 흡수 2건(reprepare 기존 테스트 3 = `TradingScheduler.__new__` 스텁 AttributeError → **production getattr 폴백**으로 흡수(cap 부재 시 기존 무제한 emit = 후방호환, 테스트 수정 0) + cycle134 라인 임계 의미 전환). **검증**: Red 유효성 stash 10 FAIL/4 PASS(불변식) + 격리 14 PASS ×2 flakiness 0 + db·ast 583 PASS + engine 전수 1,827 PASS/0 fail + **매매 안전성 8영역 diff 0 byte**(scheduler 변경 = reprepare 로그 cap 한정). 기존 폴백 기본값·graceful 전부 불변(retry 소진 → 기존 except). **커밋/푸시 보류**. **인계**: (a) 잔여 db read 모듈 점진 전환(positions/trade_history 등 저노이즈) (b) 아침 immediate run 부하 완화(내일 아침 burst 실측 후) (c) F3 잔고 500 fallback + funnel UPSERT `snapshot_at` 미갱신 quirk (d) F5 비활성 전략 0건 원인 리포트 자동 표기. |
| 2026-07-02 | 사이클 188 — `_wait_until(advance_if_passed=True)` never-return 회귀 시정 (정기 task 스케줄 발화 복원, 사이클 160 회귀 / 매매 무관) | `src/engine/scheduler.py` (`_wait_until` 재구성 — `target_dt` 를 while 루프 진입 *전* 호출 시점 1회 확정(지났으면 advance 모드만 `+1일`) + 루프 내 `now >= 확정 target_dt → return` 발화 경로 신설, 루프 내 target 재계산 폐기. 코드 실질 -2L + docstring 사이클 188 절 +12L. `break` 미사용 = cycle152 AST 가드 호환, naive `datetime.now()` 유지, 시그니처 불변) / `tests/unit/engine/test_cycle188_wait_until_advance_return.py` 신규 (7 = G-188-1/2/3/7 HIGH 발화 복원 + G-188-4/5/6 보존 불변식, 가상 시계 단조 전진 mock — fake sleep 이 가상 시각 전진, freezegun 금지 사이클 187 교훈) / `_workspace/red/cycle188_wait_until_advance_return.md` 신규 | 사이클 187 실측 검증(7/2) 중 발견. **결함**: 사이클 160 hotfix(6/17)가 advance 모드 return 경로를 만들지 않음 — 매 iteration "오늘 target" 재계산 → 도달 순간 `+1일`로 밀고 무한 sleep → 유일 탈출 = `_running=False`. production 유일 호출처 `task_loop_helper.py:114` → **모든 정기 task(16:00 일봉/16:10 basics/16:15 purge/16:20 저녁 funnel/16:30 master/20:00:05 universe) 정기 발화 2주간 0회**. 실측 = 7/1 정기 발화 0(전부 아침 boot immediate) + 6/30 13:31 재부팅 후 16:00 미발화 + funnel `is_provisional` 전부 아침 07:59 생성(저녁 캡처 무산). 매일 아침 run_daily 재시작 immediate run 이 D-1 데이터를 채워 silent — 부작용 = 무거운 full load 가 프리마켓 준비 구간(07:45~08:13)에 집중(187 RemoteProtocolError 아침 burst 공통 뿌리). 기존 스위트가 못 잡은 이유 = cycle160 ADVANCE-1/152 PAST-1 전부 고정 clock + `_running=False` 강제 탈출 = return 여부 미단언(Explore 전수 판정, **기존 테스트 깨짐 0/xfail 의미 전환 0**). run_daily phase 전환(15:20 강제청산/20:00 자문/20:10 정산) = default 모드 9곳 변경 0. 사용자 결정 = `_wait_until` 만 시정(아침 immediate 부하 완화는 인계). 오케스트레이션 = 메인 Explore → tdd-engineer Red(7) → backend-dev Green → 메인 검증. **검증**: Red 유효성 stash 4 FAIL(G-188-1/2/3/7)/3 PASS + 격리·인접(cycle160/152/134/158/106/122/171/zombie) 67 PASS + engine 전수 1,822 PASS/0 fail(xpassed 3 = 기존 96/123/153 마커, 188 무관) + **매매 안전성 8영역 diff 0 byte** 직접 검증. 사이클 152 폭주 차단 + 160 run_daily 즉시 break + 134 헬퍼 계약 + 106 immediate run 전부 영속. **커밋/푸시 보류**. **D+1 실측 의무**: push 후 당일 16:00 `[stock_master_daily_load_summary]` 정기 발화(2주 만의 첫) + 16:20 funnel 저녁 시각 `is_provisional` 생성 + 16:10/16:15/16:30/20:00 동반 확인. **인계**: (a) 아침 immediate_first_run 프리마켓 부하 완화(D+1 실측 후 사용자 결정) (b) `_wait_until`/run_daily naive now → KST 명시(LOW, MockDT 갱신 동반) (c) 187 인계 지속(kis_quote_accounts list retry 등, 48h 실측 38건 = 현 최대 ERROR 노이즈). |
| 2026-06-30 | 사이클 187 — Supabase connection 계열 retry 래퍼 (`execute_with_retry`, stock_master_daily read 4함수, 운영 안정성 / 매매 무관) | `src/db/supabase.py` (+44L `execute_with_retry(build, *, retries=1, op="")` — `_RETRY_EXCEPTIONS`(httpx RemoteProtocolError/ConnectError/ConnectTimeout/ReadError) 만 `retries`회 재시도 + 각 시도 사이 `asyncio.sleep(0.2)` + 소진 시 마지막 예외 raise(graceful 보존) + 비-retry 예외 즉시 전파 + `logger.warning("[supabase_retry]")` 단독(write_log 0, 사이클 72 이중 INSERT 차단), `build`=`.execute()` 포함 무인자 callable 멱등 SELECT 전용) / `src/db/stock_master_daily.py` (+7L net read 4함수 `get_recent_daily`/`count_all`/`count_by_ticker`/`max_bas_dd` `asyncio.to_thread` → `execute_with_retry` 경유, `max_bas_dd` ticker None/지정 2분기 build if/else, 기존 graceful except + 폴백값(`[]`/`0`/`None`) 불변, 쓰기 3함수 `upsert_daily`/`upsert_batch`/`purge_old_rows` 미경유=멱등 제외) / `tests/unit/db/test_cycle187_execute_with_retry.py` 신규 (6) / `tests/unit/db/test_cycle187_daily_read_retry.py` 신규 (4) / `tests/unit/ast/test_cycle187_ast_read_retry.py` 신규 (3) / `tests/unit/engine/strategies/test_bull_flag_breakout.py` 회귀 흡수 (get_recent_daily mock 1줄) / `_workspace/red/cycle187_supabase_execute_retry.md` 신규 | 사이클 180~186 반영분 **운영 로그 검증** 중 발견 (사용자 지시). Supabase MCP 6/29~6/30 system_logs 점검 → `get_recent_daily 실패 graceful` 68/`max_bas_dd 실패` 46 등 ERROR ~130/일, `logger.exception` 이라 DB 메시지에 사유 미부착 → **EC2 backend 컨테이너 traceback 직접 확인** = `httpx.RemoteProtocolError: Server disconnected` 확정. 진단 = 16:00 일봉 task 가 수백 종목 순회 → httpx pool 의 stale keep-alive HTTP/2 연결 재사용 → 첫 요청에서 서버 끊음 → DB 호출 **retry 부재**(KIS base.py 3회 vs Supabase to_thread 단발) → 1회 실패 = graceful 빈값 → KIS 폴백(사이클 173 prepare DB일봉 목적 부분 무력화 + 노이즈). **안전성**: RemoteProtocolError 90건/24h **전부 read 경로**, 매매 hot path 쓰기(save_position/insert_trade/체결통보) **0건 영향**(EC2 grep 확정). 사용자 결정 (AskUserQuestion 2) = ① stock_master_daily read 4함수만 ② read(SELECT)만 retry(쓰기 멱등 우려 제외). 오케스트레이션 = 메인 Supabase MCP + EC2 SSH 진단 → tdd-engineer Red(13) → backend-dev Green → tester 검증 → tdd-engineer 회귀 흡수. **회귀 흡수** (tester 발견 CI 차단): `test_bull_flag_breakout.py::test_prepare_populates_candidates_on_valid_setup` 가 `freeze_time` 안 BFB `prepare()` + DB 미mock → 실연결 `httpx.ConnectError` → 187 retry `asyncio.sleep(0.2)` 진입 → **freezegun 동결 `time.monotonic` → asyncio sleep 무한 대기 → 60s timeout**(stash 교차검증 pre-187 PASS/post-187 hang = 187 유발, 운영 영향 0). 시정 = `monkeypatch.setattr("src.db.stock_master_daily.get_recent_daily", AsyncMock(return_value=[]))` 1줄(patch 타겟 = source 모듈, BFB 가 normalized 를 함수-레벨 로컬 import). 광역 autouse 픽스처 **미적용 인계**(cycle173/123/125/158/163 DB·sleep 의도 가릴 위험). 인계 가이드 명문화 = `freeze_time` 안 prepare/stock_master_daily read 는 DB read mock 의무. 회귀 가드 13. **검증**: 격리 13 PASS × 2회 flakiness 0 + Red 유효성 stash 11 FAIL + 인접 db·ast 574 PASS + strategies 434 PASS/0 fail + 계약 4점 정합(비-retry 즉시 전파 / 쓰기 미경유 / max_bas_dd 양분기 SELECT 동일 / retry 로그 단일 INSERT). **매매 안전성 8영역 diff 0** (`git diff -- src/engine/risk.py src/engine/order_engine.py src/realtime/ src/auth/ src/api/order.py src/engine/session.py src/engine/scanner.py src/engine/strategy_registry.py` = 0 직접 검증). 사이클 173 prepare 어댑터(`get_recent_daily_normalized` → `get_recent_daily` 경유)는 retry 간접 수혜. 사이클 26/158/159 Supabase HTTP/2 race 보완(stagger=동시성, 187=연결 복원력). **커밋/푸시 보류**. **인계**: (a) 전 db read 공통 retry 전환(kis_quote_accounts list 등) (b) smdaily-1 max_bas_dd in-memory 판정 (c) 잔고 영속 500 fallback(KIS inquire-balance 500 동반 피크). |
| 2026-06-29 | 사이클 186 — 장운영상태(VI/거래정지/종목상태) UI + 서킷브레이커 휴리스틱 감지 (관찰성 전용, 매매 무관) | `src/engine/market_operation_monitor.py` (+93L `get_circuit_breaker_state()` + `get_market_op_state_summary()` 확장(`circuit_breaker`+`iscd_stat_active_count`) + `record_market_op_event` CB 계측 로그 DailyEmitCap + CB 상수 3 / 기존 VI·halt 추적·`is_ticker_stale_excluded` 불변) / `src/routes/realtime.py` (+52L `GET /api/realtime/market-operation` `get_market_operation`) / `frontend/src/types/market-operation.ts` + `frontend/src/api/market-operation.ts` 신규 / `frontend/src/pages/RealtimeHealth.tsx` (5번째 카드 "장운영상태") / `frontend/src/test/handlers.ts` + `e2e/fixtures/api-mocks.ts` mock / `tests/unit/engine/test_cycle186_market_op_cb_heuristic.py` 신규 (10) / `tests/unit/routes/test_cycle186_market_op_route.py` 신규 (3) / `frontend/src/pages/__tests__/RealtimeHealth.cycle186.test.tsx` 신규 (5) / `_workspace/red/cycle186_market_op_ui_cb.md` 신규 | 사용자 보고 "장운영상태 체크 UI 미구현 + VI/서킷브레이커 체크 여부?". 진단 = cycle 149 가 H0UNMKO0 로 VI/거래정지/종목상태를 백엔드 추적하나 **목적이 stale 회피뿐** → getter(`get_market_op_state_summary` 등) 소비처 0건 = 라우트/UI 노출 0 (운영자 화면 부재) + 서킷브레이커/사이드카 미인지. 승인 계획 `~/.claude/plans/hazy-prancing-cookie.md`. 메인 세션 KIS MCP 정본 확인(H0UNMKO0 `market_status_total` 10컬럼: TRHT_YN/TR_SUSP_REAS_CNTT/MKOP_CLS_CODE/ANTC_MKOP_CLS_CODE/MRKT_TRTM_CLS_CODE/DIVI_APP_CLS_CODE/ISCD_STAT_CLS_CODE/VI_CLS_CODE/OVTM_VI_CLS_CODE/EXCH_CLS_CODE) → **CB 전용 필드 없음** 확정 (CB 는 MKOP_CLS_CODE 시장 전체 공통 + TR_SUSP_REAS_CNTT 사유에 인코딩되나 **CB 코드값 KIS/docs/코드 미문서화** = 정식 감지 불가). Explore 3(라우트/프론트/매수가드) → AskUserQuestion 3 결정. **사용자 결정**: UI=RealtimeHealth 5번째 카드 / CB=**계측-우선 + 휴리스틱**(VI·거래정지 정식 + CB best-effort 휴리스틱 + raw MKOP/사유 노출 계측) / 가드=**표시만(매수 가드 X)**. **CB 휴리스틱**: `get_circuit_breaker_state()` suspected = (R) 거래정지 종목 `tr_susp_reas_cntt` 에 `_CB_REASON_KEYWORDS`("서킷"/"매매거래중단"/"circuit") 매칭 **OR** (W) `halted >= _CB_MIN_HALTED(5) AND halted/observed >= _CB_HALT_RATIO(0.8)`(전 시장 동시 거래정지). representative_mkop_cls_code(005930)+halt_reasons_sample 노출(계측). **false-positive 가드**: 소수 거래정지(halted=2/observed=10, 키워드 무) → suspected=False(개별 거래정지를 CB 로 오판 0, SAFETY 가드). CB 계측 로그 DailyEmitCap 1회/일. **표시만**: risk.on_tick/매매 로직 변경 0. **회귀 가드 18** (백엔드 monitor 10 + route 3 + 프론트 5): CB-KEYWORD/CB-RATIO/CB-NO-FALSE-POSITIVE(SAFETY)/SUMMARY-EXT/INSTRUMENT-CAP/**SAFETY-STALE-UNCHANGED**(`is_ticker_stale_excluded`+VI·halt 갱신 불변) + ROUTE-SCHEMA/DETAILS + FE-CARD/CB-BADGE/DETAIL/EXISTING-4. **검증**: 백엔드 cycle186 13 PASS × 2회 flakiness 0 + market_op 회귀(cycle149 등) 51 PASS + 프론트 RealtimeHealth 12 PASS(cycle186 5 + 기존 7) + tsc 0. **매매 안전성 8영역 diff 0** (`git diff -- src/engine/risk.py src/engine/order_engine.py src/realtime/ src/auth/ src/api/order.py src/engine/session.py src/engine/scanner.py src/engine/strategy_registry.py` = 0 직접 검증 — market_operation_monitor 신규 getter/휴리스틱 *추가만* → stale-skip 소비 불변, handler.py 미변경). 사이클 149 stale 회피 + 162 call-auction 영속. **커밋/푸시 보류**. **인계**: CB 정식 코드 감지 승격(실CB 관측 → MKOP_CLS_CODE/사유 포착 → 코드 기반) + 사이드카(프로그램매매 TR 별도) + CB→매수가드(regime buy_block 패턴, domain-expert 동반) + docs/kis H0UNMKO0 Response 명세 채우기. |
| 2026-06-29 | 사이클 185 — 클러스터 ① 전략 인스턴스 상태 일일 미리셋 시정 (strat-2~5 MEDIUM, transient 일일 리셋 배선 + 보유결합 매도 시점 정리) | `src/engine/strategy_base.py` (+18L `StrategyBase._reset_daily_state()` + `on_position_closed(ticker)` 기본 no-op 훅 2) / `src/engine/strategies/momentum.py` (+4L `_reset_daily_state` override `_prev_prdy_rate.clear()`) / `src/engine/strategies/bull_flag_breakout.py` (+4L `on_position_closed` `_partial_exit.pop`; 기존 `_reset_daily_state` L978 불변) / `src/engine/strategies/long_tail_volatility.py` (+4L `on_position_closed` `_limit_up_reached.discard`) / `src/engine/scheduler.py` (+7L `_reset_daily_state` 등록 순회 L3818 직후 `strategy._reset_daily_state()` try/except 배선) / `src/engine/order_engine.py` (+16L `_handle_sell_fill` 전량체결 `if pos:` 후 + `execute_sell` insufficient_qty reconciliation 2 site `on_position_closed` try/except 격리) / `tests/unit/engine/test_cycle185_cluster1_reset.py` 신규 (14) / `tests/unit/ast/test_cycle185_cluster1_ast.py` 신규 (4) / `_workspace/domain_consult/cycle185_position_closed_cleanup.md` + `_workspace/red/cycle185_cluster1_reset.md` 신규 | 출처 = 전체 코드리뷰 `_workspace/refactor/2026-06-27_full_review.md` cluster ① (strat-2~5) + 사용자 "3번째 작업" 지시. 승인 계획 `~/.claude/plans/hazy-prancing-cookie.md`. 메인 세션 Explore 3 병렬 → Plan → domain-expert(메커니즘 2) → tdd-engineer Red → backend-dev Green → 메인 세션 검증. **확정 결함**: `scheduler._reset_daily_state()`(20:10 정산 후) registry 순회(L3806)가 `strategy.state.*` 12필드만 clear, **전략 인스턴스 dict/set 필드 미참조** → 장수 싱글톤이라 영업일 가로질러 누적. cycle 180이 VB/LTV `_targets` 등을 prepare() 리셋으로 막았으나 4건 잔존. **핵심 통찰 = 두 버그는 고치는 방법이 다르다**: (A) **transient(매수 전 전용)** = 20:10 일괄 리셋이 정답 — strat-4 momentum `_prev_prdy_rate`(미리셋, momentum prepare=empty stub → 익일 첫 틱이 전일값 비교 → 갭상승 시 거짓 돌파 즉시 매수) + strat-3 BFB `_breakout_first_seen`(`_reset_daily_state` L978 존재하나 **호출처 0건 고아** → retention 가드 우회). (B) **보유결합(청산 모드)** = 20:10 일괄 리셋 **불가**(LTV 상한가 종목은 익일 NXT 프리 청산으로 밤새 보유 = DB 영속→07:50 boot 복구 + cycle 142 08:00 `_limit_up_reached.add` 재확립 → 일괄 리셋하면 보유 종목 청산 모드 깨짐) → **매도 체결 시점 정리**가 정답 — strat-2 LTV `_limit_up_reached`(재매수 종목 전일 상한가 모드 잔존 → 15:20 청산 누락 + 손절 -5% vs -3% 오적용) + strat-5 BFB `_partial_exit`(재진입 종목 measured-move 익절 영구 억제). **시정 (2 메커니즘, 메서드 추가 + 배선만, 기존 본체 0 변경)**: 메커니즘 1 = `StrategyBase._reset_daily_state()` no-op + momentum override + BFB 기존 호출됨 + scheduler 순회 배선(try/except graceful). 메커니즘 2 = `StrategyBase.on_position_closed(ticker)` no-op + LTV `discard`/BFB `pop` override + order_engine 2 site(`_handle_sell_fill` 주 + `execute_sell` reconciliation secondary, 각 try/except 격리). **domain-expert 조건부 GO** (`_workspace/domain_consult/cycle185_position_closed_cleanup.md`): flag 메모리 전용·매수 미게이트 + 포지션 제거 site 정확히 2곳(L793/L1169)이라 누설 구조적 봉쇄 + `_sync_positions_from_balance` additive-only라 secondary(L793)가 수동 HTS 매도 유일 catch-all + cycle 142 무충돌(매도 후에만 discard, 재확립은 보유 종목 = 시간 분리) + 부분 체결 시 full-fill 한정 hook 으로 잔량 청산모드 자연 보존. 신규 SAFETY 가드 = 부분체결 보존 2(LTV -5%/BFB 억제) + cycle142 시퀀스(freezegun) + 구조 불변식 AST(positions 제거 site={`_handle_sell_fill`,`execute_sell`} 고정 + `on_position_closed` 동반, 3번째 site 누락 영구 차단). **범위 = 엄격**(strat-2~5만, A-3 BFB/VCP `_prev_price`/A-4 cooldown 고아는 인계). **회귀 가드 18** (행위 14: 메커니즘1 4 + 메커니즘2 10 / AST 4). Red 유효성 = production 6파일 stash 시 13 FAIL(5 불변식 PASS). **검증**: cycle185 18 PASS × 2회 flakiness 0 + 인접(cycle147/161/163 22 + strategies 434 + engine 1805) 회귀 0. **매매 안전성 8영역 diff 0** (`git diff -- src/engine/risk.py src/realtime/ src/auth/ src/api/order.py src/engine/session.py src/engine/scanner.py src/engine/strategy_registry.py` = 0 직접 검증 — order_engine 은 매도 본류 0 변경, hook 1줄+격리만). 사이클 19/32 R4/38/142/147/161/163 영속. **커밋/푸시 보류**. **인계**: A-3(BFB/VCP `_prev_price` cross-day, LOW) + A-4(BFB/VCP `register_cooldown_after_exit` 고아 = 재진입 쿨다운 무력, MEDIUM 행위변경) + refactor(momentum `reset_next_day_clear_logged_today` ↔ `_reset_daily_state` 통합) + `_MULTIDAY_STRATEGIES` 코드 정본 `{donchian_swing}` vs CLAUDE.md `{donchian_swing, vcp_breakout}` 스테일 정정. |
| 2026-06-28 | 사이클 184 — 2-pass 슬롯 계산 풀 union 전환 (stale-2 MEDIUM, 메인 세션 단독 카운트 → 풀 총용량, 보조 세션 시 잔여슬롯/drop 오산 시정) | `src/engine/scanner.py` (+6L/-2L `subscribe_filtered_stocks` — HIGH 처리 후 LOW 루프 전 `_pool_session_count = len(kis_ws_pool.get_session_status())` + `_pool_total_slots = MAX_SUBSCRIPTIONS * _pool_session_count` 1회 계산 + 2-pass 슬롯 2곳 `MAX_SUBSCRIPTIONS - len(kis_ws._subscriptions)` → `_pool_total_slots - len(kis_ws_pool._subscriptions)`) / `tests/unit/engine/scanner/test_cycle184_pool_union_slots.py` 신규 (7) / `tests/unit/ast/test_cycle184_pool_slots_ast.py` 신규 (4) / `_workspace/red/cycle184_pool_union_slots.md` 신규 | 출처 = 사용자 "stale 전부" 지시 → scanner_stale 5건 중 마지막 (stale-2). domain-expert 불요 (HIGH bypass_limit=True 보유/익일청산은 슬롯 계산 *전* 무조건 subscribe = 영향 0, LOW 후보 슬롯 정확도만). 메인 세션 스카우트 → tdd-engineer Red → backend-dev Green → 메인 세션 검증. **확정 결함**: `subscribe_filtered_stocks` 2-pass LOW 후보 구독 슬롯 계산 2곳(1차 LOW 루프 + 2차 overflow 흡수 루프)이 `remaining = MAX_SUBSCRIPTIONS - len(kis_ws._subscriptions)` = **메인 세션 단독** 카운트. 풀(`kis_ws_pool`)이 LOW 후보를 보조 세션 라운드로빈 분배(`subscribe(bypass_limit=False)`)하면 메인 카운트 미증가 → `remaining=41-len(main)` 과대 → 과다 add (또는 메인 full+보조 여유 시 `remaining=0` → 과다 drop = 2-pass 가 막으려던 under-subscription 재현). 풀 총용량 = `MAX_SUBSCRIPTIONS × (1+보조수)`(websocket_pool.py:202 / route realtime.py:157). **시정 (scanner.py 단독, realtime/ 미변경)**: 기존 read-only accessor 재사용 — `kis_ws_pool.get_session_status()`(메인+보조 전 세션 리스트 len=1+N) + `kis_ws_pool._subscriptions`(websocket_pool.py:564 전 세션 합집합 property, **모든 tr_id** — `get_subscribed_tickers()`는 TICK-only 라 체결통보/장운영정보 undercount → property 사용 의무). 세션 수는 루프 중 불변이라 1회 계산. **보조 0개(n=1) 시 `_pool_total_slots=41` + `kis_ws_pool._subscriptions`==`kis_ws._subscriptions`(메인 단독, `_main is scanner.kis_ws` 싱글톤) → 현 동작 완전 동일 (회귀 0)** — 보조 세션 존재 시에만 동작 변경. **HIGH bypass 불변 (SAFETY)**: positions/next_day_clear 는 슬롯 계산 *전* `bypass_limit=True` 무조건 subscribe → 변경 영향 0 (사이클 32 R4 절대 보호). **회귀 가드 11** (행위 7 = REGRESS 3(보조 0/0/1 → 현재≡Green) + ACCURACY 2(보조1 메인full→흡수 / 보조2 union>메인) + SAFETY-HIGH 2(메인 full 에도 HIGH 전량+bypass) + AST 4(`remaining` 할당 노드 한정 = `len(kis_ws._subscriptions)` 잔존 0 + 풀 union 사용 + `get_session_status` 호출 + 탐지기 self-test, L1166 로그라인 제외 false-positive 차단)). **의미 전환 0건** (기존 2-pass 테스트 priority_order/dispatch/breakout/scan_loop_delta 전부 보조 0개 = 동작 불변). Red 유효성 = 현재 코드 5 FAIL(보조 N개 정확도 + AST). **검증**: cycle184 11 PASS × 2회 flakiness 0 + 2-pass 인접 29 PASS + 광역(scan/priority/subscribe/breakout) 516 PASS/0 fail. **realtime/ + 매매 안전성 영역 diff 0** (`git diff -- src/realtime/ src/engine/risk.py order_engine.py auth/ strategies/ api/order.py` = 0 직접 검증 — scanner.py 2-pass 만, 기존 pool accessor 재사용). 사이클 7-C 풀 분배 + 32 R4 + 15-A delta + WebSocket 4중 안전망 영속. **커밋/푸시 보류**. **stale 전부(stale-1~5) 종결** — 사이클 182(call-auction stale-1+5) + 183(scanner stale-3+4) + 184(stale-2). |
| 2026-06-28 | 사이클 183 — scanner KST 시각 + price filter 60s 캐시 경유 (stale-3 LOW + stale-4 LOW, 표시 시각 KST 강제 + scan당 DB read ≤4× → ≤1×) | `src/engine/scanner.py` (+5L/-3L 2곳 — L681 `_last_scan_time = datetime.now()` → `datetime.now(KST_TZ)` (stale-3 KST 강제, 바로 아랫줄 L685 이미 KST) + `_apply_price_filter` L150 `get_price_filter()` → `_get_price_filter_for_scanner()` 60s TTL 캐시 경유 + L151 오인 주석 정정 (stale-4)) / `tests/unit/engine/scanner/test_cycle183_stale3_stale4.py` 신규 (8) / `tests/unit/ast/test_cycle183_stale3_stale4_ast.py` 신규 (3 = 0-arg `datetime.now()` 0건 + `_apply_price_filter` 캐시 getter 경유) / cycle64/81 캐시 격리 fixture 5 파일 (의미 전환 0 = `invalidate_price_filter_cache_scanner()` 1줄, 단언 불변) / `_workspace/red/cycle183_stale3_stale4.md` 신규 | 출처 = 사용자 "stale 전부" 지시 → scanner_stale 5건 중 scanner.py 단독 LOW 2건. domain-expert 불요(매매 행위 무관 — stale-3=표시 문자열 / stale-4=DB read 횟수 최적화, 필터 결과·구독 집합 불변). 메인 세션 스카우트 → tdd-engineer Red → backend-dev Green → 메인 세션 검증. **stale-3**: `scan_stocks()` L681 `_last_scan_time = datetime.now().strftime("%H:%M:%S")` naive (서버 로컬, 바로 아랫줄 L685 는 `datetime.now(KST_TZ)` 사용 = 불일치). `get_scan_status()` 응답 `last_scan_time` 표시용. CLAUDE.md "모든 시각 KST 강제" 위반 → `datetime.now(KST_TZ)` (scanner.py:32 기정의). **stale-4**: `_apply_price_filter` (L150) 가 `get_price_filter()` 직접 호출 → 60s TTL 캐시(`_get_price_filter_for_scanner`, 정확히 이 용도로 존재) 우회. `subscribe_filtered_stocks` 가 `_apply_price_filter` 를 ≤4회 호출(tickers+extra+breakout/momentum+swing) → scan당 DB read ≤4× → 캐시 경유로 ≤1×. **정합성 보존**: PUT 즉시 반영은 `invalidate_price_filter_cache_scanner()` (사이클 64/148 PUT hook)가 담당 → scanner↔prepare 정합 영속. L151 오인 주석("subscribe_filtered_stocks 별도 최적화") 정정. **캐시 격리** (tdd-engineer 식별): 전환 후 `_apply_price_filter` 가 모듈 캐시 read → cycle64 apply/protected/emit_cap + cycle81 key_fix/real_data_regression 5 파일이 이전 테스트 캐시 hit 오염 가능 → autouse `invalidate_price_filter_cache_scanner()` fixture 추가 (단언 intent 불변 = 의미 전환 0, 결과는 캐시가 동일 PriceFilter 반환이라 불변). **회귀 가드 11** (행위 8 = stale-3 freezegun KST wall-clock + AST + stale-4 PERF 2(60s 내 1 DB read / scan당 ≤1) + CORRECT 5(invalidate 즉시반영 + 보유 early-return + min/max 차단 + 비활성 통과 + bfdy_clpr graceful) + AST 3). Red 유효성 = 현재 코드 6 FAIL. **검증**: cycle183 11 PASS × 2 flakiness 0 + cycle64/81 31 PASS + 광역(price_filter/scan/stale) 492 PASS/0 fail. **매매 안전성 영역 diff 0** (`risk/order_engine/realtime/auth/api/order/strategies/scheduler` = 0, 보유 절대 보호 사이클 32 R4 영속). 사이클 64/148 PriceFilter 단일 source + invalidate hook 영속. **커밋/푸시 보류**. **stale 잔여 인계**: stale-2(2-pass 슬롯 풀 union) = 사이클 184. |
| 2026-06-28 | 사이클 182 — call-auction 코드분기 시간창 게이트 (stale-1 HIGH/PLAUSIBLE + stale-5 LOW, 고착 코드 영구 True → 보유 종목 stale 탐지 무한 비활성 차단) | `src/engine/session.py` (+18L/-7L — `is_call_auction_now` 코드분기 `_last_nxt_mkop_code in ("110","121")` 무조건 True → `code AND 유효시간창`(110: 08:25~09:05 / 121: 15:15~15:35, 명목±5분) + `now or datetime.now(_KST)` KST 강제 + 모듈 상수 `_KST`) / `tests/unit/engine/test_cycle182_call_auction_time_gate.py` 신규 (23) / `tests/unit/ast/test_cycle182_call_auction_ast_gate.py` 신규 (4 = 코드분기 `time()` 동반 의무 + naive now 0건) / `tests/unit/engine/test_cycle162_call_auction_stale_skip.py` 의미 전환 3건 (G-162-E-1/E-2 xfail = stuck-110/121@MAIN 결함 박제 / E-10 freezegun 15:25 = 정당 동시호가 skip 보존) / `_workspace/domain_consult/cycle182_call_auction_time_gate.md` 신규 / `_workspace/red/cycle182_call_auction_time_gate.md` 신규 | 출처 = 전체 코드리뷰 base-1 다음 사용자 "stale 전부" 지시 → scanner_stale 5건(stale-1~5) 중 call-auction 그룹. 메인 세션 스카우트 → domain-expert + KIS MCP → tdd-engineer Red → backend-dev Green → 메인 세션 검증. **확정 결함 (stale-1)**: `SessionTracker.is_call_auction_now` (사이클 162 동시호가 stale 회피 hook) 코드 분기 L209 `if self._last_nxt_mkop_code in ("110","121"): return True` 가 시간창 게이트 없이 시간 무관 무조건 True. `_last_nxt_mkop_code` 는 세팅(L233 on_h0nxmko0)만 있고 **리셋/전환 처리 0건** (전수 grep) → KIS 가 15:20 "121"(장후 동시호가) push 후 종료/NXT 전환 코드를 미발신 시 "121" 영구 고착 → `is_call_auction_now()` 가 15:30~20:00 NXT 애프터(보유 NXT 거래 활발) 내내 True → stale_watcher 가 보유 종목 stale(WS 끊김) 탐지 일절 안 함 → **손절 틱 누락**. 다음 영업일 08:30 "110" 수신 전까지 지속. **domain-expert + KIS MCP 자문** (`_workspace/domain_consult/cycle182_call_auction_time_gate.md`): KIS MCP `market_status_total`(H0UNMKO0) = 컬럼 레이아웃만 검증, MKOP_CLS_CODE 코드 의미·push 트리거 **미문서화** → push 신뢰 불가 → **방식 A(시간창 게이트) 단독 채택** (방식 B 전환코드 리셋 단독 불가). 코드 분기를 명목창±5분으로 감싸 고착 코드를 자기 창 밖에서 구조적 무시. **신규 발견**: stuck-110(MAIN 09:00~15:20 전체 손절 피크 구간 stale OFF)이 사용자 보고 stuck-121(NXT)보다 **더 위험** — 게이트가 양쪽 봉쇄. 구조 취약점 3 확인(H0UNMKO0 005930 단일구독+실전한정 / websocket.py 가 H0UNMKO0 를 stale 안전망 명시 제외 / `_last_nxt_mkop_code` 리셋 0). **stale-5 (latent)**: L213 `now or datetime.now()` naive → `datetime.now(_KST)`. 현 호출자 `stale_watcher_core:130/196` 이미 tz-aware KST 전달이라 운영 영향 0(latent), None 폴백만 시정. tz-aware now.time()=wall-clock 반환 → naive time 경계 비교 무해(검증). **사이클 162 정합 (충돌 0)**: 진짜 동시호가창(08:30~09:00, 15:20~15:30)엔 코드+시간폴백 양쪽 True 유지 → 사용자 사고(15:21 폭주) 차단 영속. VTS(code="" 영구)=시간폴백 단독=162 동작 동일. **시정은 보유 stale 탐지를 *복원* → 매매 안전성 강화(약화 아님)**. **검증**: cycle182 26 PASS + cycle162 10 PASS/2 XFAIL(E-1/E-2)+E-10 freeze PASS × 2회 flakiness 0 + session/stale 인접 248 PASS/16 XFAIL/0 fail. Red 유효성 = 현재 코드 12 FAIL(stuck-121@16:00 / stuck-110@11:00 등). **매매 안전성 영역 diff 0** (`git diff -- src/engine/risk.py order_engine.py realtime/ auth/ scanner.py scheduler.py stale_watcher_core.py` = 0 직접 검증, session.py 1함수만). 사이클 38/135 grace/149 VI/162 영속. **커밋/푸시 보류**. **stale 잔여 인계**: stale-3(KST `_last_scan_time`)+stale-4(price filter 60s 캐시 우회) = 사이클 183 / stale-2(2-pass 슬롯 풀 union) = 사이클 184. |
| 2026-06-28 | 사이클 181 — 토큰만료 분기 msg_cd 화이트리스트 전환 (base-1 HIGH/CONFIRMED, EGW00120 예수금부족 오분류 → 토큰 재발급+주문 재전송 증폭 차단) | `src/api/base.py` (+38L/-2L — 모듈 frozenset 2개 `_TOKEN_EXPIRED_MSG_CODES`{EGW00121/00122/00123} + `_TOKEN_BRANCH_EXCLUDE_CODES`{EGW00120/APBK0919/APBK0918} + `_request`(메인)/`_request_via_quote_pool`(시세풀) 토큰 분기 조건 2곳 동일 교체, `"만료" in msg1` substring 폐기) / `tests/unit/api/test_cycle181_token_expiry_whitelist.py` 신규 (12 = FP 3 + TP 2 + EXHAUST + POOL 대칭) / `tests/unit/ast/test_cycle181_token_whitelist_ast.py` 신규 (3 = frozenset 엔트리 검사 + `"만료"` Compare 노드 0건) / `_workspace/domain_consult/cycle181_token_expiry_whitelist.md` 신규 / `_workspace/red/cycle181_token_expiry_whitelist.md` 신규 | 출처 = 전체 코드리뷰 `_workspace/refactor/2026-06-27_full_review.md` base-1 (HIGH, 권장 #4) + 사용자가 strat-1(180) 다음 항목 명시 선택. 메인 세션 스카우트(정본 확인) → 중첩 위임 stall 후 메인 세션 직접 오케스트레이션 (domain-expert + KIS MCP → tdd-engineer Red → backend-dev Green → 메인 세션 검증). **확정 결함**: `base.py:560`(`_request`)+`:866`(`_request_via_quote_pool`) 토큰 분기 `if "token" in msg1.lower() or "만료" in msg1:` 순수 substring. KIS `EGW00120`="기간이 만료된 code"(→"만료" 포함)인데 본 프로젝트가 EGW00120 을 **예수금부족 변형으로 활용** (`balance.py:99` `is_insufficient_cash` 화이트리스트 {APBK0919,EGW00120}). 매수(TTTC0012U)/매수가능조회(TTTC8908R) 예수금부족 거부(EGW00120) → "만료" 매칭 → `token_manager.issue()`(분당1개 한도, ≤61초 블록) + `attempt<MAX_RETRIES(3)` 시 동일 body `continue` 재전송 → **~122초 지연 + 불필요 토큰 2회 재발급(인증 chain) + 동일 매수 2회 재전송(중복 체결 위험)**. 권리만료/청약기간만료도 동일 오분류. **domain-expert + KIS MCP 자문** (`_workspace/domain_consult/cycle181_token_expiry_whitelist.md`): ① 화이트리스트 = access token 3종(EGW00121/00122/00123)만 — `issue()`(access token 재발급)가 올바른 복구인 코드만. session_key(EGW00124~126)도 제외(재발급으로 해소 불가 footgun, 1차 메모 "6종 대칭" 정정). ② hybrid 채택 = msg_cd 화이트리스트(주) + 영문 `"token"` 폴백(보험, 배제코드+"부족" 제외) — 토큰 3종 전부 영문 "token" 포함 vs EGW00120 은 "만료"만 → `"만료"` 절은 토큰 커버리지 0 + 오발화만 더하는 순수 부채 → 영구 폐기. 401 분기 신설 안 함(REST 토큰만료=HTTP200+rt_cd!=0). ③ 배제 가드 = base.py 인라인 frozenset(순환 import 회피, balance.py 직접 호출 불채택). ④ 증폭 기전 = `auth/token.py` `_ISSUE_GAP_SECS=61.0` 메인+풀 전역 직렬 락 → spurious issue N개 → ~N×61초 그리드락 = "아침 잔고500 halt" 정확한 기전, 시정 후 토큰 발급 O(N거부)→O(1)/일. ⑤ 중복 체결 = ~122초 토큰락 윈도우 중 예수금 회복 race → stale 가격 의도외 매수(semantic dup) + 예수금부족 정상처리(`block_buy 900s`) 최대 3재시도 지연 = wrong-handling, 시정 후 attempt-1 즉시 매수 락. **토큰 분기 본체 변경 0** (issue()+continue+`[api_retry_exhausted] last_status=token_expired` R7 ERROR 보존, 사이클 76 매트릭스). **검증**: cycle181 격리 15 PASS × 2회 flakiness 0 + api/ 디렉토리 전수 235 PASS + 토큰/retry 보존 매트릭스(cycle76 R2/R4/R7/R8 + base_retry_logging) 18 PASS / 의미 전환 0 (cycle76 단언은 `"token" in msg1.lower()` 만 검사 → 폴백 유지로 보존). Red 유효성 = 현재 코드 9 FAIL(FP-1 EGW00120 issue=3 등). **매매 안전성 8영역 diff 0** (`git diff -- src/engine/risk.py src/engine/order_engine.py src/realtime/ src/auth/ src/api/order.py` = 0 직접 검증 — base.py 는 8영역 외이나 주문 공통 래퍼이므로 토큰 분기 외 변경 0 + api 전수 회귀로 주문 재전송 행위 검증). **커밋/푸시 보류** (사용자 승인 대기). **사이클 182+ 인계**: 잔고 영속 500 fallback (MEDIUM, 아침 halt — base-1 이 증폭 경로 차단했으나 근본 fallback 은 별건) + rt 클러스터(rt-1/2/5 체결통보 다건 drop HIGH) + order-1(매수 폴백 except 분리 HIGH) + 클러스터①(strat-2~5 per-strategy reset 배선). |
| 2026-06-27 | 사이클 180 — VB/LTV `prepare()` 시작부 3-dict 리셋 (strat-1 HIGH/CONFIRMED, 전일 stale 종목 누적 매수 차단) | `src/engine/strategies/volatility_breakout.py` (+4L — `_reset_funnel_steps(VB_FUNNEL_STAGES)` 직후·첫 `_scan_universe()` 전 `_targets`/`_open_confirmed`/`_prev_price` clear 3줄) / `src/engine/strategies/long_tail_volatility.py` (+4L 동일) / `tests/unit/engine/strategies/test_cycle180_vb_ltv_prepare_reset.py` 신규 (13 = A 결함재현 8 + B SAFETY 5) / `tests/unit/ast/test_cycle180_prepare_reset_ast.py` 신규 (18 = C.7 clear-present 6 + C.8 forbidden-absent 4 + C.9 clear-position 2 + D.10 exit-no-side-effect 4 + no-risk-import 2) / `_workspace/domain_consult/cycle180_vb_ltv_prepare_reset.md` 신규 / `_workspace/red/cycle180_vb_ltv_prepare_reset.md` 신규 | 출처 = 전체 코드리뷰 `_workspace/refactor/2026-06-27_full_review.md` strat-1 (HIGH/CONFIRMED, 권장 #1) + 핸드오프 `funnel-vast-wolf.md`. 메인 세션 트레이더 스카우트 + 안전성 분석 (라인 인용 검증) → team-leader multi-agent TDD 오케스트레이션 (domain-expert → tdd-engineer Red → backend-dev Green → tester 검증, 단계별 별도 에이전트 = Red/Green 독립성). **확정 결함**: VB/LTV `prepare()` 가 장수 싱글톤 `_targets`/`_open_confirmed`/`_prev_price` 를 종목별 *할당만* + 시작부 미clear. `scheduler._reset_daily_state()` (20:10 유일 일일 리셋) 도 strategy.state.* + order_engine 추적 + scanner 글로벌 dict 만 비우고 **전략 인스턴스 3-dict 미참조** (직접 grep 확정) → 전일 universe 탈락 종목 영구 잔존 → `_scanned_tickers = list(_targets.keys())` → 구독 → 틱 → `check_buy_signal` 이 stale 전일 open_price/target_offset 로 매수 → "돌파 = 이전틱<기준가 AND 현재틱>=기준가" 절대규칙 *기준가 오염*. 형제 전략 (donchian/BFB/VCP) 은 이미 prepare 시작부 `_candidates={}` 리셋 → **5 전략 중 3 채택 컨벤션으로 VB/LTV 정합** (신규 패턴 아님, 회귀 기준선 낮음). **domain-expert 자문 5 의제 전부 GO**: ① empty-gate → 09:00 시가확정 미손실 (intraday 재prepare 2경로 boot660/reprepare2501 = `_targets` 빈 경우만 발화 → 잃을 confirmed 0 + 재할당 시 `_open_confirmed[t]={}` = clear 무관 종목별 리셋) ② `_limit_up_reached` 제외 **필수·절대** (clear 시 보유 상한가 익일청산 종목 15:20 강제청산 오편입 + -3% 손절 모드 격하 = 롱테일 핵심 수익구조 파괴) ③ 16:20 저녁 `immediate_first_run` prepare 빈 window = 유일 신규 미세 위험 (장중 EC2 재시작 시 수 초 매수 억제, negligible — KRX 메인 push 자제 정책 차단 + donchian/BFB/VCP 동일 window 사고 0) ④ clear 후 0건=미거래가 트레이더 정답 (전일 타겟 매수 = stale 진입 오류). **스코프 엄격**: `_limit_up_reached`(strat-2) / `_next_day_clear_pending`(strat-4) 절대 미참조 = 별건 + check_exit_signal/check_force_clear/risk/order_engine 변경 0. **회귀 가드 31** (AST 토큰 기반 = 사이클 167 source-scan false-positive 교훈, 날짜 의존 제거 = 사이클 176 교훈 `_valid_candles` 고정 2024): C.7 clear 존재 + C.9 첫 `_scan_universe()` 전 위치 + C.8 금지 리셋 0건 (미래 영구 차단) + D.10 청산경로 무side-effect + A 합성 시계열 결함재현 + B SAFETY. **Red 유효성 독립 재확인** (green revert 시 16 FAIL = A8+C.7×6+C.9×2, green 복구 +4L×2). **매매 안전성 8영역 diff 0** (`git diff -- src/engine/risk.py src/engine/order_engine.py src/realtime/ src/auth/ src/api/order.py` = 0 직접 검증, 청산 3-dict 미참조 = 사이클 38 명문화 영속). 격리 31 PASS × 3회 flakiness 0 + 인접 회귀 195 PASS (cycle143/148/156/157/158/170/173 xfail 3 의미 전환) + AST 전수 194 + 전략 전수 434 PASS. **커밋/푸시 보류** (사용자 승인 대기). **사이클 181+ 인계 (domain-expert 발견 별건 2)**: (a) `_limit_up_reached` 가 `_reset_daily_state`(20:10) 에도 미clear → 프로세스 생애 누적 latent 버그 (동일 ticker LTV 재매수 시 첫 check_exit_signal 상한가 모드 오진입, strat-2 영역) (b) donchian/vcp 2 xpassed (cycle173/157 의미 전환 xfail 마커 갱신 housekeeping). |
| 2026-06-26 | 사이클 179 — 거래량순위(volume-rank, FHPST01710000) dead code 폐기 (LOW, 행위 보존, 매매 무관) | `src/api/base.py` (`_QUOTE_ALLOWED_PATHS` 시세 풀 화이트리스트 `/quotations/volume-rank` path 제거 8→7 + docstring "6 path"→"7 path" 스테일 정정) / `src/engine/strategies/CLAUDE.md` (L24 스테일 문서 정정 — VB/LTV `_scan_universe` 거래량순위 현재형 서술 → 사이클 108 DB 전환 반영, L25 와 모순 해소) / `src/api/CLAUDE.md` (화이트리스트 8→7 path) / `tests/unit/api/test_cycle179_no_volume_rank_in_quote_allowlist.py` 신규 (3) | 사용자 질문 "변동성돌파 전략에서 굳이 거래량순위 TR을 쓸 이유 없는데?". ultracode 병렬 감사(Explore 3 + 종합) + 직접 grep 검증 = VB 는 사이클 108(2026-06-11)부터 거래량순위 미사용 (`_scan_universe` → `stock_master.list_by_filter` DB 단일 조회, KIS 호출 0건). 거래량순위 TR(FHPST01710000) = production 호출처 0건 완전 dead — src/ 잔존은 `base.py` 화이트리스트 path 1개 + 주석/문서뿐. 사용자 오해 뿌리 = (a) 사이클 178 직전까지 ScanMonitor 스테일 라벨 'blng 0/1/3 / 거래량순위' (b) `strategies/CLAUDE.md:24` 스테일 문서가 현재형 서술 + 바로 아랫줄 L25 사이클 108 폐기 명시 = 자기모순 (c) momentum 만 순위 API 사용하나 그것도 **등락률순위**(`/ranking/fluctuation` FHPST01700000, condition.py:299) = 거래량순위와 다른 TR (감사 에이전트 1건이 TR_ID 혼동 → 직접 grep 으로 교정). 사용자 판단 정확 + 이미 사이클 108 에 구현 완료 → 잔존 dead 자산(화이트리스트 path) + 스테일 문서만 정리. 매매 로직 변경 0 (VB 이미 DB 기반, momentum 등락률순위 의도된 사용). 가드 = frozenset 엔트리 검사 (source 텍스트 스캔 아님 — 사이클 167 주석 false-positive 교훈). 매매 안전성 무영향 — `git diff -- src/engine/ src/realtime/ src/auth/ src/api/order.py` = 0 (base.py 화이트리스트 1줄 제거만, 호출처 0건). 격리 41 PASS (179 신규 3 + 109/inquire-ccnl/97/108/routing 회귀 0) + api 디렉토리 223 PASS. **커밋(push 보류, 사용자 승인 대기)**. 인계: `base.py:741` `volume_rank` 페이징 헤더 주석 = live 기능 stale 예시 (무해, 선택 정리). |
| 2026-06-26 | 사이클 178 — VB/LTV ScanMonitor 죽은 단계 제거 + universe_candidates 정확 라벨 (관찰성 전용, 프론트 단독, 매매 무관) | `frontend/src/components/ScanMonitor.tsx` (VB_STAGES 8→5 / LTV_STAGES 9→6 = 죽은 키 `price_filtered`/`mcap_pass`/`trade_amount_pass` 제거 + step1 라벨 `universe_candidates` → "시총+거래대금 컷 통과" + BFB step1 동일 라벨 시정) / `ScanMonitor.cycle175.test.tsx` (의미 전환 — '원천 유니버스 후보' not.toContain) / `ScanMonitor.cycle178.test.tsx` 신규 (4) | 사용자 보고 "변동성돌파/롱테일변동성돌파 조건검색 현황 이상 — stock_master 원천 유니버스 후보가 적고 2,3,4가 0인데 5에서 살아남". 진단 = 사이클 175 가 VB/LTV/BFB step1 을 "stock_master 원천 유니버스 후보"로 라벨했으나 (a) VB/LTV `_empty_scan_stats` 의 `price_filtered`/`mcap_pass`/`trade_amount_pass` 3키는 사이클 108 list_by_filter 단일 호출 전환 이후 백엔드 미세팅 = 죽은 키 → 항상 0 (= "2,3,4가 0") (b) `universe_candidates` = list_by_filter 결과 = 시총+거래대금 컷 *후* (VB/LTV/BFB 는 full universe → union=fetch buffer 무의미) → "원천" 오라벨 + 작은 수 ("원천이 적음"). 백엔드 정밀 audit = VB 5키(`universe_candidates`/`universe_filtered`/`candle_fetch_ok`/`k_value_computed`/`final_prepared`) + LTV +`consecutive_limit_pass` 모두 prepare() 에서 실세팅 확인 (로컬 `stats` 별칭 통해 — `self._scan_stats[...]` grep 만으론 miss). 시정 = VB_STAGES 죽은 3키 제거(8→5) + LTV 제거(9→6) + step1 라벨 "시총+거래대금 컷 통과" + BFB step1 동일. 5전략 전수 STAGES = live 백엔드 키만 참조 확정 (BFB 8 / VCP 8 / donchian 8 / momentum 6 audit). 죽은 키는 백엔드 `_empty_scan_stats` 잔존하나 프론트 미참조(무해, LOW 정리 인계). 매매 안전성 무영향 — frontend-only, 백엔드/risk/order_engine/realtime/auth/api diff 0. 프론트 ScanMonitor 8파일 31 PASS + tsc 0. **커밋/푸시 보류 (사용자 승인 대기)**. |
| 2026-06-26 | 사이클 175 — 대시보드 조건검색 현황(ScanMonitor scan_stats) ↔ 조건검색 추적(DB funnel) 정합 (관찰성 전용, 매매 무관) | `src/engine/strategies/donchian_swing.py` + `vcp_breakout.py` (`_empty_scan_stats` + `_scan_universe` `universe_union` 노출 = 합집합 union_tickers, VCP `return_stage_counts=True` 추가) / `frontend/src/types/trading.ts` (`ScanStats.universe_union`) / `frontend/src/components/ScanMonitor.tsx` (SWING/VCP STAGES "합집합" → `universe_union` + VB/LTV/BFB 폐기 "blng 0/1/3" 라벨 제거) / `tests/unit/engine/strategies/test_cycle175_universe_union_scan_stats.py` 신규 (4) / `frontend/src/components/__tests__/ScanMonitor.cycle175.test.tsx` 신규 (4) / `test_vcp_breakout_mcap_pass.py` mock 적응 (`return_stage_counts` 튜플) | 사용자 보고 "20일 신고가 스윙(donchian) 합집합이 왜 95? 조건검색 추적 메뉴에 맞춰야. 나머지도 점검". 진단 = ScanMonitor `universe_candidates`("코스피200+코스닥150 합집합" 라벨) = `len(list_by_filter 결과)` = **시총+거래대금 컷 *후*(~95-99)**, 진짜 union(~348)은 `return_stage_counts→union_tickers`로 **DB funnel(조건검색 추적)에만** 노출 → 두 funnel source 괴리 + VB/LTV 폐기 "blng 0/1/3"(거래량순위 API, 사이클 108 폐기) 잔존 라벨. 5전략 전수 동일 패턴. 시정 = donchian/VCP `universe_union` 노출(ScanMonitor "합집합" = union ~348 → DB funnel step1 정합) + VB/LTV/BFB 라벨 "stock_master 원천 유니버스 후보"로 시정(VB/LTV/BFB 숫자는 각자 DB funnel step1=filtered 와 이미 정합, 라벨만 결함). 매매 안전성 무영향 (scan_stats 관찰성 한정, risk/order_engine/realtime/auth/api/order/base diff 0). 백엔드 **3,220 PASS / 0 fail** + 프론트 **27 PASS + tsc 0** × flakiness. **계획상 175**(plan funnel 관찰성 통합), 176/177(거래대금 결손 긴급) 먼저 구현돼 날짜상 뒤지나 plan 번호 보존. 커밋(push 보류). |
| 2026-06-26 | 사이클 177 — cycle 145 가드 None/비숫자 하드닝 + 사이클 176 장전보존 e2e 정밀검증 (LOW, 사이클 176 후속) | `src/api/condition.py` (`inquire_stock_basics` cycle 145 `_ZERO_VALUE_SKIP_KEYS` 가드 except `pass`→`continue` — None/비숫자도 skip = 기존 거래대금 보존) / `tests/unit/engine/test_cycle176_e2e_premarket_preservation.py` 신규 (5 — 실제 `inquire_stock_basics`(KIS만 mock)+scanner 머지 통째 end-to-end) | 사용자 "3 제대로 검사" → 사이클 176 머지 정밀검증. 기존 5 단위테스트가 `inquire_stock_basics` 통째 mock → **cycle 145 0-skip ↔ scanner 머지 연결고리 미검증** → 실제 코드 태우는 e2e 5건 추가(장전 `"0"`/빈문자열 → 거래대금 보존, 장중 실값 콤마 → 덮어씀, 비-skip 키 시총 → 덮어씀, 신규 ticker 안전). 검증 중 잠재 갭 1건 발견 = skip-키 `value=None` 시 `float("None")` ValueError → `pass`(merge) → merged_raw[key]=None → 기존 거래대금 None 덮어씀(소실). FHKST 는 장전 문자열 `"0"` 반환(null 아님)이라 이론적이나 하드닝 = except `continue`(skip-키 전부 숫자 필드 → 비숫자=junk → 덮어쓰지 않고 기존 보존). cycle 145/155 회귀 0 (비숫자-merge 단언은 비-skip 키 iscd_stat_cls_code/vi_cls_code 한정 = 영향 0) + G-145-RAW-4 `numeric_value==0` 유지. 매매 안전성 무영향 (risk/order_engine/realtime/auth/api/order/base/strategies diff 0 = condition.py 1 파일만). 백엔드 3,216 PASS / 0 fail × flakiness. 라이브 교차확인 = 6/26 09:24 재시작 후 universe 비영(donchian 99/LTV 26/VCP 347 = 거래대금 정상 작동). 커밋(push 보류). 인계: 6/26 KIS 잔고 500 halt 가 드러낸 "잔고 영속 500 fallback" 코드개선(MEDIUM) + 전체 코드리뷰 재개. |
| 2026-06-25 | 사이클 176 — 거래대금 결손 시정 (basics refresh 기존 raw 머지 보존, HIGH 매매 기회 / scanner 매수 진입 전 영역) | `src/engine/scanner.py` (`_stock_master_basics_refresh_once` `existing_raw_by_ticker` 보관 + upsert 전 `{**기존, **신규}` 머지, `getattr`/`model_copy` graceful) / `tests/unit/engine/test_cycle176_basics_refresh_raw_merge.py` 신규 (5) | 사이클 175 인계 사이클 B. 근본원인 (코드 정독 확정) = `src/api/condition.py::inquire_stock_basics` 가 `merged_raw = dict(ctpf_output)` 로 raw 를 CTPF 에서 신규 빌드 (기존 DB raw 미read) + 개장 전 FHKST `acml_tr_pbmn=0` → 사이클 145 `_ZERO_VALUE_SKIP_KEYS` 가드 `continue` skip → merged_raw 거래대금 부재 + `stock_master.upsert_one` 이 `upsert(on_conflict="ticker")` 로 raw 통째 교체 → **부팅 전 basics refresh 가 매일 전 종목 거래대금 삭제** (사이클 145 "기존 raw 보존" 은 read-merge 부재로 무효 = no-op). 운영 실측 (Supabase MCP) = 거래대금 보유 **6/3573**, VB/LTV/donchian/BFB universe 0 — prepare 가 삭제 *전* 도느냐 레이스 악화 (6/22 prepare 10회 승 → 6/23 2회 → 6/24 0회). SAFETY = universe 0 → 미거래(오매수 아님, trade_history 6/24 0건). 시정 = scanner refresh 가 이미 `list_all` 로 로드한 기존 row 의 raw 보관 → upsert 전 머지 → cycle 145 skip 15키 (거래대금/거래량/per/pbr/외국인/신고가/실적) 기존 값 보존, 새 키 우선. **blast radius 최소** (scanner basics refresh 한정, `upsert_one` 전역 의미변경 회피 + 추가 DB read 0 = scanner 가 이미 rows 보유). bare-object fake (`model_copy`/`raw` 부재) `getattr` graceful → 기존 cycle126 basics refresh 20 PASS 회귀 0. 매매 안전성 무영향 — risk/order_engine/realtime/auth/api/order/strategies **diff 0** (scanner basics refresh = 매수 진입 전, 사이클 38 명문화). 백엔드 **3,154 PASS / 0 fail** (시간/날짜 flaky 8파일 제외 — stale_watcher 동시호가 08:30~09:00 call-auction-skip + cycle173 어댑터 하드코딩 날짜 `date(2026,6,20)` 6/25 stale, 모두 내 변경 무관 기존 결함). cycle176 격리 5 PASS. **커밋/푸시 보류** (사용자 승인 대기). 운영 복원 = 장중 deploy 재시작 시 boot refresh 가 FHKST 실값(>0) 적재 + 머지 보존 → 당일 universe 복구. **인계**: 기존 flaky 테스트 freeze (cycle173 `today_kst` + stale_watcher `is_call_auction_now`) 별도. |
| 2026-06-24 | 사이클 175 — system_logs retention PostgREST row-cap silent 결함 항구 시정 (루프 배치, HIGH 운영 안정성 / 매매 무관) | `src/db/system_logs.py` (`_purge_by_cutoff` SELECT(`PURGE_SELECT_BATCH=1000`)+DELETE 를 drained 까지 루프 + 안전 cap 3중 = `PURGE_MAX_ITERATIONS=2000` 런어웨이 + `MAX_PURGE_BATCH=100_000` 누적 상한 + 마지막 페이지 조기 종료) / `tests/unit/db/test_cycle175_log_retention_loop.py` 신규 (8) / `test_system_logs_retention.py` + `test_cycle150_supabase_capacity.py` 의미 전환 2 / `_workspace/red/cycle175_log_retention_loop.md` / `src/db/CLAUDE.md` + HARNESS_CHANGELOG 명세 갱신 | 사용자 보고 "데이터양 리미트 초과" → 메인 세션 진단 = Supabase **535MB > 무료 500MB**, 주범 system_logs 242MB / 884K행. 근본원인 = 사이클 150 의 `SELECT(id).limit(MAX_PURGE_BATCH=100_000)` 이 Supabase PostgREST **`db-max-rows=1000` 기본 cap** 에 silent 절단 → 하루 1회(20:10) × 1000행만 삭제 vs INFO 30K+/일 생성 → 640K 적체 (운영 로그 `[log_retention] info_deleted=1000 high_deleted=1000` 매일 정확히 1000 = 증거). 사이클 6 `.limit()` AttributeError(사이클 150 시정)에 이은 *또 다른* silent cap. 사용자 승인 = 루프 배치(no-migration) — `_purge_by_cutoff` drained 까지 루프 → retention 1회가 backlog 전량 드레인(재적체 방지), migration/RPC/PostgREST 설정 변경 0, DELETE chain `.limit()` 미사용 보존(사이클 6 영구 차단) + cutoff=None RuntimeError 보존. **메인 세션 즉시 완화**: 운영 backlog 665,671행 (INFO>2일 642,211 + WARNING/ERROR>30일 23,460) raw SQL 삭제 + `VACUUM FULL system_logs` → **535→368MB** (무료 한도 즉시 복귀, system_logs 242→75MB). 매매 안전성 무영향 — system_logs 순수 관찰성, risk/order_engine/scanner/realtime/auth/api/order/strategies **diff 0**. 백엔드 3,206 PASS / 0 fail / db 격리 42 PASS × flakiness 0. team-leader TDD (stall분 메인 세션 tester 마무리). **사이클 B 인계 (별도)**: 거래대금 결손 (`inquire_stock_basics` merged_raw 신규빌드 + `upsert_one` raw 통째교체 → 개장 전 FHKST acml_tr_pbmn=0 skip 시 기존값 소멸, VB/LTV/donchian/BFB universe 0) + stock_master_daily daily purge 부팅 연결폭주 회피. |
| 2026-06-22 | 사이클 173 — 5 전략 prepare 일봉 source KIS→DB 전환 (HIGH, 매수 target 행위 보존) | `src/db/stock_master_daily.py` (`get_recent_daily_normalized` 락/신선도 게이트 + 헬퍼 3 `_row_has_lock`/`_extract_raw`/`_kis_fallback` + `DAILY_STALENESS_DAYS=4`) / `src/engine/strategies/volatility_breakout.py` + `long_tail_volatility.py` + `bull_flag_breakout.py` + `vcp_breakout.py` (각 `_fetch_one` `fetch_daily_candles` → `get_recent_daily_normalized(days, min_required)`) / `donchian_swing.py` (어댑터 전환 + 신고가 candles 단일 source = `get_donchian_high` 별도 DB 폐기) / `tests/unit/db/test_cycle173_adapter_lock_stale_gate.py` 신규 (13) / `tests/unit/engine/strategies/test_cycle173_prepare_db_equivalence.py` 신규 (27) / `test_cycle172_safety_no_trading_diff.py` 의미 전환 2 (SAFETY-2 xfail + SAFETY-3 헬퍼 위임) / `test_cycle123_donchian_db_high.py` 모듈 xfail 의미 전환 / `_workspace/red/cycle173_prepare_db_equivalence.md` + `_workspace/domain_consult/cycle173_prepare_db_equivalence.md` / 3 CLAUDE.md (root/strategies/db) + HARNESS_CHANGELOG 명세 갱신 | 승인 설계 `funnel-vast-wolf.md` 사이클 173 절 + domain-expert 동등성 자문. 5 전략 (momentum 제외) prepare 일봉 source KIS→DB 어댑터 전환 (매수 target 행위 보존). 핵심 = 어댑터 락 게이트 (수정주가 divergence silent 결함 방어, 자문 완화책 1) — DB 윈도우 내 1 row 라도 `flng_cls_code not in ("","00")` OR `prtt_rate` 비-0 → KIS 강제 폴백. **team-leader 운영 DB 실측 확정 (자문 보정)**: flng_cls_code 기본 "00"(99.6%)/prtt_rate 기본 0(99.7%, 자문의 `!=1.0` 은 틀림 = 전 종목 폴백 유발) → 비기본=락 보수적 판정. 검사 = DB top-level 컬럼만 (KIS 추가 호출 0건 탐지). days/min_required 명시 (None 의존 금지): VB/LTV 22, donchian 63(필수 61 절대 하향 금지), BFB 35, **VCP 100 cap 유지 (220 미사용, 자문 쟁점3 옵션(가) 행위 보존 — 172 인계 "220/120" 폐기, effective_ema_long ≈75 식 변경 0)**. donchian 신고가 candles 단일 source (자문 §249 락 종목 신고가/EMA 혼재 차단). 신선도 게이트 (max_bas_dd > today-4 → 폴백). 매매 안전성 무영향 — risk/order_engine/realtime/auth/api/order **diff 0 라인** (직접 검증) + prepare check_exit/buy 호출 0 (사이클 38) + 보유 절대 보호 (32 R4) + 사이클 158/163/170/143/157 영속. 백엔드 3,196 PASS / 0 fail × flakiness 0 / 가드 40 + 의미 전환 3. 변경 6 파일 (어댑터 1 + 5 전략). **커밋/푸시 보류 (장중 HIGH, 사용자 승인 대기)**. 사이클 174+ 인계: VCP 220 EMA 원설계 복원 (backtest 동반) + 락 종목 재backfill (완화책 2) + `[prepare_db_fallback]` 가시화 (완화책 3) + D+1 라이브 동등성 검증 (boot fallback count + funnel 후보 집합 동일). |
| 2026-06-22 | 사이클 172 — stock_master_daily 220일 확보 (LOW~MEDIUM, 데이터 plumbing, 사이클 173 prepare DB일봉 전환 선행) | `src/api/condition.py` (+128L `fetch_daily_candles_ranged` 단일 윈도우 100건 경계 + `fetch_daily_candles_backfill` 220일=윈도우×3 병합 dedupe, `FID_ORG_ADJ_PRC="0"` 기존 정합, memcache 미사용 별도 함수) / `src/db/stock_master_daily.py` (+70L `DAILY_RETENTION_DAYS` 150→230 + `get_recent_daily_normalized` 어댑터 raw JSONB 반환) / `src/engine/scanner.py` (+37L `_stock_master_daily_load_once` VCP universe 220일 backfill 분기 + `vcp_universe_tickers` set + `_DAILY_LOAD_VCP_BACKFILL_DAYS=220`) / `tests/unit/api/test_cycle172_daily_ranged_backfill.py` 신규 (9) / `tests/unit/db/test_cycle172_retention_and_adapter.py` 신규 (7) / `tests/unit/engine/test_cycle172_vcp_universe_backfill.py` 신규 (8) / `tests/unit/ast/test_cycle172_safety_no_trading_diff.py` 신규 (4) / `test_cycle150_supabase_capacity.py` 의미 전환 1 (retention 150→230) / `_workspace/red/cycle172_daily_220_backfill.md` 신규 / 3 CLAUDE.md + HARNESS_CHANGELOG 명세 갱신 | 승인 설계 `funnel-vast-wolf.md` 사이클 172 절. 전략별 최대 일봉 lookback = VCP 220일 (나머지 5전략 ≤65일, 현 retention 150 충분). 현재 backfill T-100/retention T-150 → VCP universe (KOSPI200∪KOSDAQ150 348종목) 220일 단독 부족. KIS MCP 정본 재확인 = FHKST03010100 호출당 최대 100건 → 날짜 윈도우 ×3 페이지네이션 (윈도우 경계 겹침 → dedupe, 최고 윈도우 430 달력일 커버 = 220 영업일 충분). **prepare/매수 target 미변경** (173에서 prepare 전환, 172=데이터 plumbing + 어댑터 정의만 호출처 0). 분할 fetch `FID_ORG_ADJ_PRC="0"` = 기존 fetch_daily_candles 정합 (173 DB-source vs KIS-source 동등성 게이트 보장, 정본 샘플 "1"이나 본 코드베이스 사이클 14부터 "0"). scanner VCP 분기 = 16:00 daily task 만 (장중 실행 금지, 사이클 38/122). 매매 안전성 무영향 — risk/order_engine/realtime/auth/api/order **diff 0 라인** (직접 검증) + 신규 함수 매매 hot path 참조 0 + 사이클 81 G-AST1 raw 분리. 회귀 가드 27 + 의미 전환 1 / 백엔드 3,161 PASS × flakiness 0. **커밋/푸시 보류 + 실제 운영 DB backfill 실행 보류** (사용자 승인 대기). 사이클 173 인계: 5 전략 prepare DB일봉 전환 (HIGH 동등성 게이트 + domain-expert 자문) + D+1 운영 DB 220일 실측 (push 후 16:00 task 발화). |
| 2026-06-22 | 사이클 171 — 저녁 16:20 잠정 funnel 캡처 + 수동 trigger 단계별 캡처 (MEDIUM, 관찰성 전용, 운영자 가치) | `supabase/migrations/040_funnel_provisional.sql` 신규 (운영 DB 적용 완료, `is_provisional BOOLEAN NOT NULL DEFAULT FALSE` additive) / `src/db/strategy_funnel.py` (`insert_snapshot` `is_provisional: bool = False` 인자 + payload 동행) / `src/engine/scheduler.py` (`capture_funnel_snapshots(registry, *, is_provisional)` 모듈 헬퍼 추출 + `_auto_capture_funnel_snapshots` 위임 + `TIME_EVENING_FUNNEL_CAPTURE=time(16,20)` + `_evening_funnel_capture_once` (count_all 폴링 + prepare + capture) + `_evening_funnel_capture_task_loop` (run_periodic_task_loop 답습) + task_attrs 4 위치) / `src/routes/strategy_funnel.py` (`trigger_snapshot` step_no=99 단독 → `capture_funnel_snapshots(is_provisional=False)` 단계별 전체) / `frontend/src/api/strategy-funnel.ts` (`FunnelSnapshot.is_provisional?`) / `frontend/src/pages/StrategyFunnel.tsx` (amber "잠정" 배지) / `tests/unit/db/test_cycle171_funnel_provisional.py` 신규 (4) / `tests/unit/engine/test_cycle171_evening_funnel.py` 신규 (14) / `tests/contract/test_cycle171_snapshot_route_steps.py` 신규 (2) / `frontend/src/pages/__tests__/StrategyFunnel.cycle171.test.tsx` 신규 (3) / 의미 전환 4건 (C-3 contract 단계별 / cycle159 stagger allowlist / cycle134 라인 임계 ≤3,990 / zombie expected_members 17종) / `_workspace/red/cycle171_evening_funnel.md` 신규 / 5 CLAUDE.md + HARNESS_CHANGELOG 명세 갱신 | 자문 `cycle171_master_funnel_timing_redesign.md` 의제 4 우선순위 2 (저녁 잠정 prepare/funnel) + 의제 6 (a) (수동 trigger 단계별) 채택. funnel = 순수 관찰성 + D-1 일봉 기반 → 장중 불변 → 전날 저녁 (16:20) 미리 생성하면 운영자가 밤에 다음 영업일 후보 확인 (현 09:30 개장 후 캡처는 늦음). 16:00 일봉 → 16:10 basics → 16:20 funnel (count_all 폴링 대기 + 5 전략 prepare + capture is_provisional=True) → 16:30 마스터 순서. 공통 헬퍼 3 호출처 공유 (09:30 자동 False / 16:20 저녁 True / 수동 trigger False) — 09:30 행위 보존 (단계별 + step_no=99). 매매 안전성 무영향 — scanner/risk.on_tick/order_engine/realtime/auth/api/order.py diff 0 (git 검증) + check_exit/buy funnel hook 0건 (SAFETY-1 HIGH) + capture 헬퍼 매매 모듈 참조 0 (SAFETY-2 HIGH) + 사이클 32 R4/38/132(momentum 제외)/158(재시도 hook)/170(in-place upsert) 영속. migration additive (장중 안전, 기존 row 자동 false). 백엔드 3,134 PASS × 3회 flakiness 0 / 프론트 324 PASS + tsc 0 / 신규 가드 23 (HIGH SAFETY 2). 커밋/푸시 보류 (월요일 장중, 사용자 승인 대기). 사이클 173 인계: prepare DB일봉 전환 (HIGH 동등성) + 사이클 174 아침 08:46 마스터 델타 (잠정 → 확정). |
| 2026-06-20 | 사이클 170 — 전략 funnel 관찰성 결함 3건 시정 (행위 보존, MEDIUM+LOW) | `src/engine/strategy_base.py` (`_record_funnel_step` in-place upsert + `_reset_funnel_steps(stages)` 0-시드, +44L) / `src/db/stock_master.py` (`list_by_filter(return_stage_counts=False)` 단계별 ticker 누적, +45L) / `src/engine/strategies/donchian_swing.py` (`_scan_stage_counts` 필드 + step1=union/step2=trade + FUNNEL_STAGES[1] 라벨 "시총+거래대금 컷 통과" + reset STAGES 인자) / `volatility_breakout.py` + `long_tail_volatility.py` (`_universe_candidate_tickers` step1 + reset 인자) / `bull_flag_breakout.py` + `vcp_breakout.py` (step_conditions 정합 + reset 인자) / `tests/unit/engine/strategies/test_cycle170_card_{b,c}_*.py` + `tests/unit/db/test_cycle170_card_a_*.py` 신규 (36 케이스) / `test_cycle119_donchian_scan_universe.py` + `test_cycle153_donchian_index_filter.py` (mock tuple 적응) / `_workspace/red/cycle170_funnel_observability.md` + `_workspace/refactor/2026-06-20_funnel_observability_review.md` 신규 / 4 CLAUDE.md (root/engine/strategies/db) + HARNESS_CHANGELOG 명세 갱신 | funnel = 순수 관찰성 (매매 정상, list_by_filter robust, donchian 실제 유니버스 321종목). 운영 DB 실측 (project_id etaligxesjtjfkbntdve) = donchian union 348 → 시총컷 348 → 거래대금컷 321 정상이나 funnel DB 6/19 step1=0/step4=7 (파이프라인 논리 불가능). 사용자 결정 B→A→C. 카드 B (0-시드 `_reset_funnel_steps(stages)` + `_record_funnel_step` in-place upsert → 조기반환/실패 run step4=7 stale 잔존 영구 차단, 5 전략 prepare+retry STAGES 인자) + 카드 A (`return_stage_counts=True` 단일 호출 attrition union/mcap/trade 노출 → donchian step1/step2 collapse 차단, 기존 호출자 미지정 회귀 0) + 카드 C (VB/LTV step1 placeholder → 실제 후보, BFB/VCP step_conditions 정합, step1 "원천 유니버스 후보" 의미 통일). 매매 안전성 무영향 — scanner/risk.on_tick/order_engine/realtime/auth/api/order.py diff 0 (git 검증) + 매수 풀 불변 (G-A-1 직접 검증) + check_exit_signal/check_buy_signal funnel hook 0건 (G-C-3 SAFETY) + momentum 영구 제외 (사이클 132) + 사이클 32 R4/38/143/145/153/157 영속. 백엔드 3,117 PASS × 3회 flakiness 0 / 신규 가드 36 (HIGH G-B-3/G-A-1 + SAFETY G-C-3/C-4). mock 적응 (cycle119/153 tuple 반환, 의미 전환 아님). 사이클 171+ 인계: D+1 09:30 자동 발화 후 Supabase MCP 단조 감소 재확인 (step1≥...≥step9 + step4=7/step1=0 소멸) + 적재부 숫자형 정규화 (사이클 168). |
| 2026-06-20 | 사이클 169 — 종목마스터 "변경이력" 탭 빈 화면 결함 시정 (사이클 150 신 스키마 프론트 미동기화 회귀, LOW UI 읽기 경로) | `frontend/src/types/stock-master.ts` (`StockMasterHistoryItem` 신 스키마 `{ticker, seq:0\|1, change_type:'INSERT'\|'UPDATE'\|'DELETE', raw, changed_at}` — `id`/`before_raw`/`after_raw`/`'TTL_REFRESH'` 폐기) / `frontend/src/pages/StockMaster.tsx` (변경이력 탭 4 컬럼 = 스냅샷 최신본/직전본 배지 + 변경일시 KST + 변경유형 배지 + raw `CollapsiblePre`, seq ASC 정렬, `CHANGE_TYPE_COLORS` TTL_REFRESH 제거) / `frontend/src/test/handlers.ts` (MSW history seq/raw 2 스냅샷) / `e2e/fixtures/api-mocks.ts` (Playwright history seq/raw, LIFO 정합) / `frontend/src/pages/__tests__/StockMaster.test.tsx` (H-HISTORY 의미 전환 + 사이클 169 신규 6 케이스 G-169-1~4 + AST 2) / `e2e/stock-master.spec.ts` 주석 갱신 / 4 CLAUDE.md (root/frontend/routes) + HARNESS_CHANGELOG 명세 갱신 | 사용자 보고 "변경이력에 내용 안나오는건 지난번 데이터폭주 수정하면서 적용된건가?" → 원인 확정 = 사이클 150 회귀. 사이클 150 (데이터폭주/Supabase 용량초과) migration 036 이 `stock_master_history` 스키마 재설계 (`id`/`before_raw`/`after_raw` 제거 → `seq INT`(0=최신본/1=직전본) + `raw JSONB`, `(ticker, seq)` PK, 92K→7,146 row) 했으나 프론트 UI 미동기화 — `StockMaster.tsx` 가 여전히 `item.before_raw`/`after_raw` 참조 → undefined → 빈 화면. 백엔드 `list_history` = `select("*")` pass-through 라 정상 (변경 0). 운영 DB 실측 (project_id etaligxesjtjfkbntdve) = 7,146행 / distinct ticker 3,573 / seq0 3,573 + seq1 3,573 (데이터 정상 존재, UI 가 안 읽던 것). 사용자 결정 Option A = seq0 최신본 / seq1 직전본 2 스냅샷 각각 표시. seq ASC 정렬 = UPDATE 시 seq0/seq1 changed_at 동일(now())이라 changed_at 정렬만으론 비결정 → 프론트 seq 재정렬. 매매 안전성 무영향 — 프론트 UI 읽기 표시 한정, 백엔드/DB/scanner/engine/risk/order_engine/realtime/auth diff 0 + migration 불필요. 프론트 50 파일 321 PASS × 3회 flakiness 0 (사이클 166 312 → 169 321, +9 = 신규 6 + 의미 전환 흡수) / tsc 0 에러 / e2e_mocks 가드 25 PASS. 회귀 가드 = stock_master_history 스키마 변경 시 type+렌더+MSW+e2e mock 4 영역 동기화 의무 (사이클 150→169 교훈). 사이클 170+ 인계: 사이클 168 인계 적재부 condition.py 숫자형 정규화 (근본 해소). |
| 2026-06-20 | 사이클 168 — 종목마스터 UI 시총/거래대금 필터 0건 결함 시정 (jsonb string → 생성 컬럼 전환, MEDIUM UI 읽기 경로) | `supabase/migrations/039_stock_master_numeric_generated_cols.sql` 신규 (생성 컬럼 `hts_avls_eok` 억원 + `acml_tr_pbmn_won` 원 STORED + 인덱스 2, 운영 DB 적용 완료) / `src/db/stock_master.py` (`list_paged_by_filter` `.gte("raw->hts_avls", N)` → `.gte("hts_avls_eok", N)` + `.gte("raw->acml_tr_pbmn", N)` → `.gte("acml_tr_pbmn_won", N)`, +15L/-10L) / `tests/unit/db/test_cycle168_list_paged_generated_cols.py` 신규 (10 케이스 — COL 2 + THRESH 4 + EMPTY 2 + AST 2) / `_workspace/red/cycle168_list_paged_jsonb_string_filter.md` 신규 / 3 CLAUDE.md (db/routes/root) + HARNESS_CHANGELOG 명세 갱신 | 사용자 보고 "종목마스터에서 필터링이 이름빼고는 작동을 안하네". 근본 원인 = 적재부 `condition.py` merge 가 KIS 응답 문자열을 그대로 저장 → `raw.hts_avls`/`raw.acml_tr_pbmn` 가 운영 DB 에 전부 jsonb *string* (실측 number=0/string=3573·3441). 종전 `q.gte("raw->hts_avls", N)` jsonb numeric gte 는 PostgreSQL jsonb 정렬에서 number > string 이라 항상 false → 어떤 임계든 0건 (시총·거래대금 필터 무력화, market `.eq`/name `ilike` 는 정상). 사이클 128 가정("jsonb number 2,696/2,697")이 string 으로 회귀. 사용자 결정 Option A = 생성 컬럼(STORED bigint) + 인덱스 — 비숫자는 `~ '^[0-9]+$'` 가드로 NULL graceful. 운영 DB 즉시 적용 (Supabase MCP) + 실측 재현 `hts_avls_eok>=1000`=1,734 / `acml_tr_pbmn_won>=10000000000`=366 (종전 0). get_stats 점검 = 무관 (`raw->>` text 존재성 체크라 numeric 비교 아님, 실측 3566/3441/3566). 매매 안전성 무영향 — scanner `list_by_filter`(Python-side int 파싱)/risk/order_engine/realtime/auth diff 0 (UI 읽기 경로 한정) + 생성 컬럼 raw 읽기만(GENERATED) → 사이클 81 G-AST1/108/166 영속. 사이클 128/166 테스트 관용 substring 매칭 → 의미 전환 0건 (여전히 PASS). 백엔드 3,081 PASS × flakiness 0. 사이클 169+ 인계: 적재부 condition.py 가 숫자형으로 저장하도록 정규화 (근본 해소, 별도 사이클) 검토. |
| 2026-06-20 | 사이클 167 — 시총 헬퍼 3개 dead code 폐기 (LOW, 행위 보존) | `src/engine/scanner.py` (`market_cap_master_to_millions` + `validate_market_cap_consistency` + `get_market_cap_millions` 3 함수 폐기, -~110L) / `tests/unit/ast/test_cycle167_ast_no_dead_market_cap_funcs.py` 신규 (18 케이스 AST 기반 — `_name_is_reintroduced` 탐지기로 def/async/메서드/할당/import/호출 재도입 포착 + 탐지기 self-test, `/code-review max` 보강) / `test_cycle129_master_load_once.py` (G-ML3/ML4/ML5 xfail 의미 전환 + docstring 동기화) / `test_cycle129_master_uri_priority.py` (G-UP3 xfail 의미 전환 + docstring 동기화) / `test_cycle166_krx_fallback_eok_unit.py` (`TestHelperUnitDocumentation` 제거) / `_workspace/red/cycle167_market_cap_dead_code.md` 신규 / 3 CLAUDE.md + HARNESS_CHANGELOG 명세 갱신 | 사이클 166 인계 "헬퍼 명칭 통일" 진단 결과 3 헬퍼가 production 호출 0건 (dead code) 확인 → 명칭 통일 대신 폐기가 합당 (사용자 결정). 세 함수가 서로만 호출하는 폐쇄 그래프 (외부 진입점 0). 실제 시총 필터는 `list_by_filter` / `list_paged_by_filter` 직접 수행 (사이클 166 억원 정합 완료). 사이클 103 strategy.py dead code 폐기 패턴 100% 답습 (callsite 0건 → 폐기 + AST 영구 가드). `/code-review max` 보강: AST 가드 정규식→AST 기반 견고화 (별칭 재export·공백 def false negative + docstring 언급 false positive 동시 차단) + cycle129 docstring xfail 동기화. 의미 전환 4건 (사이클 66 K-2): 사이클 129 G-ML3/ML4/ML5/UP3 헬퍼 존재 계약 → xfail. 매매 안전성 무영향 (dead code, 행위 0) — risk/order_engine/realtime/auth diff 0 + scanner 시총 필터 본체 변경 0 + 사이클 81 G-AST1/116/153/166 영속. 사이클 168+ 인계: D+1 후보 풀 폭증 모니터링 (사이클 166). |
| 2026-06-19 | 사이클 166 — hts_avls 시가총액 단위 결함 시정 (백만원→억원, HIGH 매매 기회 손실 후보 풀 95.4% 축소) | `src/db/stock_master.py` (`list_by_filter` python `hts_avls × 100_000_000` + `list_paged_by_filter` jsonb `min_market_cap // 100_000_000`) / `src/engine/scanner.py` (KRX 폴백 `mktcap_won // 100_000_000` 원→억원 + 3 미사용 헬퍼 docstring 억원 명문화) / `frontend/src/pages/StockMaster.tsx` (`formatMarketCap`/detail hts_avls 렌더 억원 10,000억=1조 + FIELD_LABELS "시가총액 (억원)") / `tests/unit/db/test_cycle166_hts_avls_unit_correction.py` 신규 (12) / `tests/unit/engine/scanner/test_cycle166_krx_fallback_eok_unit.py` 신규 (7 AST 단위 가드 포함) / `frontend/src/pages/__tests__/StockMaster.cycle166.test.tsx` 신규 (3) / `test_cycle108_list_by_filter.py` + `test_cycle128_list_paged_by_filter.py` 의미 전환 / `_workspace/domain_consult/cycle166_hts_avls_unit_correction.md` 신규 / 4 CLAUDE.md + HARNESS_CHANGELOG 명세 갱신 | 운영 DB 대형주 9종목 전수 `실제시총(원)/hts_avls ≈ 10^8` + KIS FHKST01010100 정본 → hts_avls = 억원 확정. 사이클 108/128 "백만원" 가정 = silent 결함 (100배 어긋남) → min_market_cap=1,000억 시 후보 풀 1,734 → 80 (95.4% 축소, 시총 1,000억~10조 1,654종목 5전략 후보 전수 탈락). 사용자 결정 Q-A 포함 (scanner list_by_filter + UI 페이징 list_paged_by_filter) + Q-B 분리 (헬퍼 명칭 167+ 인계) + Q-C 포함 (프론트 표시). domain-expert 자문 채택 = 억원 통일 / DB 재적재 불필요 (16:10 KIS task 억원 덮어씀) / KRX 폴백 억원 통일 / 추가 가드 불필요 (5중 안전망 영속). team-leader 신규 발견 = UI 페이징 동일 결함 전파 (`_eok_to_won` → 내부 백만원). get_market_cap_millions/validate_market_cap_consistency/market_cap_master_to_millions = production 미사용 (호출처 0) → 단위 명문화만. 회귀 가드 22 케이스 (운영 실측 1,734/80 재현 + jsonb/python 양쪽 정합 + KRX 절삭 경계 + AST `1_000_000` 잔존 0). 매매 안전성 8영역 + risk/order_engine/realtime/auth diff 0 + 사이클 38/81 G-AST1/108/116/153 영속 (scanner 매수 진입 전 한정). 사이클 167+ 인계: 헬퍼 명칭 통일 (refactor) + D+1 후보 풀 폭증 모니터링. |
| 2026-06-19 | 사이클 165 — docstring 보강 + CLAUDE.md 반복 단어 정리 (LOW, 행위 변경 0) | `src/api/order.py` (+23L/-1L `place_order` TR_ID 분기 TTTC0011U/0012U + ORD_DVSN + EXCG_ID_DVSN_CD + `cancel_order` TTTC0013U) / `src/api/balance.py` (+15L/-5L 4 거부 분류 헬퍼 msg_cd 누적 APBK1943/APBK3013/APBK0919/EGW00120/APBK1234 인용) / `src/api/quotation.py` (+5L `inquire_ccnl` FHKST01010300 + 시세 풀 라우팅) / `src/realtime/handler.py` (+18L/-7L `_handle_tick` H0STCNT0/H0NXCNT0/H0UNCNT0 KIS MCP 정본 fields[0]~[9] + `_handle_execution` 26 컬럼) / `src/engine/risk.py` (+17L/-1L `on_tick` 가드 계층 L0~L4 + 매도/손절 평가 시점 명시) / `src/engine/order_engine.py` (+38L/-9L `_handle_buy_fill`+`_handle_sell_fill` docstring + 가드 매트릭스) / 4 CLAUDE.md (frontend / api / db / strategies) 반복 단어 30건 → 0건 정리 | Phase 1 진단 (3 Explore agents) = 41,000+ 라인 / 208+ 파일 / 897+ 함수 전체 리뷰. A 등급 (안전성) + 구조 복잡도 의제 14 카드 식별. 사이클 165 = LOW 위험 영역만 (docstring + 단어 정리). 사용자 5회+ 명시 지적 패턴 = `영구 영속이 / 영영 / 영역 영구 영속 / 영영 영영` grep 후 의미 보존 영역 (사이클 이력 / 영속 의무 / 의도된 단발 "영구 차단" 등) 보존 + 무의미 반복어만 제거. production 행위 변경 0 (docstring + 인용만). 백엔드 풀 3,040 PASS / 162 xfailed / 0 FAIL. 매매 안전성 8영역 변경 0 + 사이클 19/30/38/88/102/107/128/145/147/155/161/163 영역 명시 의무 보존. 사이클 166+ 인계: HIGH 분해 (scheduler / scanner / websocket / base.py) + MEDIUM (recommendation_engine except 정제 / load_pending_ndc 검증 hook / GET /stock-master/list response schema test) + order_engine.py 신규 진단 4건 (L1231 미사용 매개변수 + 3 broad except). |
| 2026-06-18 | 사이클 164 — 시가대기 silent 결함 시정 (5분 주기 자동 시가 확정 재시도, HIGH 매매 안전성) | `src/engine/scheduler.py` (+90L net `_confirm_breakout_open_prices_if_pending` 신규 메서드 + `_scan_loop` hook 통합) / `tests/unit/engine/test_cycle164_open_confirm_retry_chain.py` 신규 (10 케이스 HIGH 5) / `_workspace/red/cycle164_open_confirm_retry_chain.md` 신규 / 의미 전환 1건 (사이클 134 라인 임계 ≤3,750 → ≤3,830) | 사용자 보고 = 6/18 11:03 KST EC2 재기동 후 VB/LTV 후보 8 종목 시가 / 타겟가 미확정 + 시가대기 상태 영구 잔존. 근본 원인 = `_wait_until(TIME_KRX_OPEN_CONFIRM=09:00:05)` 1회 호출 영역 = 09:00 이후 재기동 시 `_targets[ticker]["boards"]["main"]["open_price"]` 메모리 휘발 + 자동 재발화 hook 부재. 옵션 B 채택 = `_scan_loop` 5분 주기 hook = 미확정 종목 자동 시가 확정 재시도 (idempotent + disabled skip + graceful try/except). 이중 안전망 = VB `check_buy_signal` L654-657 tick fallback + `_scan_loop` 5분 재시도. 의제 #1 raw.hts_avls 데이터 손상 = **False Alarm** 확정 (사용자 보고 = 키 존재 vs 임계 카운트 혼동). 부차 발견 = `master_raw.prdy_avls_scal` 단위 = 백만원 (CLAUDE.md 명세 "억원, ×100" 결함, LOW 사이클 165+ 인계). production +90L net / 회귀 가드 10 케이스. 백엔드 2,872 PASS × 3회 flakiness 0. 매매 안전성 8영역 + risk/order_engine/realtime/auth diff 0 + 사이클 26/38/81/88/102/134/162/163 영속. |
| 2026-06-18 | 사이클 163 — 2차 _boot stock_master race 가드 + 5 전략 prepare 재시도 hook 통일 + `_handle_buy_fill` DB 예외 격리 (HIGH 매매 안전성) | `src/db/stock_master.py` (+30L `count_active()` 신규, 사이클 128 답습) / `src/engine/boot_manager.py` (+35L prepare 호출 전 5분 cap polling 가드, 10초 주기 + 사이클 88 G-REJECT graceful) / `src/engine/order_engine.py` (+43L `_handle_buy_fill` 전량 체결 분기 3 영역 try/except 분리) / `src/engine/strategies/long_tail_volatility.py` (+14L 사이클 158 VB hook 답습) / donchian_swing.py / bull_flag_breakout.py / vcp_breakout.py (각 +14L) / `tests/unit/db/test_cycle163_count_active.py` 신규 (2) / `test_cycle163_boot_prepare_guard.py` 신규 (4) / `test_cycle163_prepare_retry_hook.py` 신규 (4) / `test_cycle163_buy_fill_db_error_isolation.py` 신규 (6) / `test_cycle163_ast_persistence.py` 신규 (3) / `_workspace/domain_consult/cycle163_handler_exception_and_boot_race.md` 신규 (322L 자문 산출물) / `_workspace/red/cycle163_boot_race_and_handler_exception.md` 신규 / integration mock 갱신 3건 | 의제 #5 = 6/18 08:22~08:27 KST 2차 _boot 시점 `stock_master 0건` 32회 폭주 결함 시정. `skipped_ttl=2696` = stock_master DB 영역 채워져 있음 + 사이클 158 hook 90초 본질적 부족 확정. boot_manager `_boot()` 영역 prepare 호출 *전* `stock_master.count_active()` 가드 5분 cap polling (10초 주기). 5 전략 (LTV/donchian/BFB/VCP) prepare 자동 재시도 hook 사이클 158 VB 패턴 답습. 의제 #6 = 6/17 12:43:09 알지노믹스 BUY 직후 `WebSocket 메시지 핸들러 예외: tr_id=H0STCNI0` 결함 진단. KIS MCP 정본 = 26 컬럼 매트릭스 + `tr_key=` 빈 영역 = HTS ID 단위 push 정합. 결함 원인 = 메시지 디코딩 아닌 `_handle_buy_fill` 내부 DB INSERT (save_position / update_trade_status) 예외 추정. 시정 = `_handle_buy_fill` 전량 체결 분기 3 영역 try/except 분리 + DB 영역 graceful (callback 다른 영역 변경 0). production +110L net / 회귀 가드 19 케이스 / 사이클 163 격리 19/19 PASS × 3회 flakiness 0 / 인접 회귀 44/44 PASS / 백엔드 3,019 PASS. 매매 안전성 8영역 + 사이클 32 R4/38/88 G-REJECT-1/147/161/162 변경 0 (G-163-SAFETY-1 정적 검증). 사이클 164+ 인계 = master_raw 1순위 + raw 2순위 chain (D 옵션) / raw.hts_avls 데이터 손상 추적. |
| 2026-06-18 | 사이클 162 — 익일청산큐 DB 영속화 + 동시호가 시간대 stale 회피 (HIGH 매매 안전성 직결) | `supabase/migrations/038_pending_next_day_clear.sql` 신규 (운영 DB 적용 완료) / `src/db/pending_next_day_clear.py` 신규 (4 CRUD) / `src/engine/scheduler.py` (+26L 3 hook = 2 등록 + drain finally + reset_daily purge) / `src/engine/boot_manager.py` (load_pending_ndc 복구 hook) / `src/engine/session.py` (`is_call_auction_now()` 신규) / `src/engine/stale_watcher_core.py` (`_call_auction_skip` hook) / `tests/unit/db/test_cycle162_pending_next_day_clear_crud.py` 신규 (7 케이스 HIGH 3) / `tests/unit/engine/test_cycle162_call_auction_stale_skip.py` 신규 (12 케이스 HIGH 6) / `_workspace/domain_consult/cycle162_pending_persist_and_call_auction.md` 신규 (자문 산출물) / 의미 전환 1건 (사이클 134 라인 임계 ≤3,700→≤3,750) | 의제 D = 알테오젠/알지노믹스 6/17 15:20 강제청산 누락 (EC2 재기동 시 `_pending_next_day_clear: set[tuple]` 메모리 휘발) 결함 시정. `(target_date, ticker, strategy_id)` 복합 PK + `_boot()` 복구 chain + DB 실패 시 메모리 보존 graceful (사이클 88 답습). 의제 E = 6/17 15:21:48 KST `subscribed=10 fresh=0 stale=10 ratio=0%` 5분 주기 재구독 폭주 결함 시정. KIS MCP 정본 = H0UNMKO0 `MKOP_CLS_CODE` 110 (장전 동시호가 08:30~09:00) / 121 (장후 동시호가 15:20~15:30) 코드 기반 인지 + 시간 기반 폴백. `stale_watcher_core` 영역 `_call_auction_skip` hook = 동시호가 시간대 stale 종목 전체 skip + `[stale_skip_call_auction]` WARNING 1행. production +5 파일 / 회귀 가드 19 케이스 (HIGH 9). 백엔드 3,011 PASS × 3회 flakiness 0 / 사이클 162 격리 19/19 PASS. 매매 안전성 8영역 + risk/order_engine/realtime/auth diff 0 + 사이클 29/32 R4/38/88/135/149 영속. KIS LMS chain 위험 감소. |
| 2026-06-17 | 사이클 161 — `_handle_buy_fill` 영역 체결단가 정합 시정 (HIGH 매매 안전성 직결) | `src/engine/order_engine.py` (+24L `_handle_buy_fill` 3 분기 price 인자 명시 + 보정 INSERT UniqueViolation 강제 UPDATE) / `tests/unit/engine/test_cycle161_buy_fill_price_consistency.py` 신규 (380L 7 케이스) / `_workspace/red/cycle161_kis_price_consistency_and_pending_recovery.md` 신규 / `src/engine/CLAUDE.md` 명세 갱신 | 사용자 보고 005940 BUY 33,400원 vs HTS 33,350원 (+50원 차이) 시정. 근본 원인 = `_handle_buy_fill` `update_trade_status(BUY, COMPLETED, strategy=strategy_id)` 호출 시 `price` 인자 누락 → PENDING INSERT record_price (주문가 / scanner current_price) 그대로 잔존. KIS MCP 정본 검증 = 체결통보 H0STCNI0 `CNTG_UNPR` (idx 10) = 실제 체결단가 + 주문 응답 (TTTC0011U/0012U) 체결가 응답 부재. 시정 = 사이클 147 `_handle_sell_fill` 패턴 100% 답습 (price 인자 + UniqueViolation 강제 UPDATE). 회귀 가드 7 케이스 (HIGH 4 = 57%) + AST G-161-K-6 미래 silent 결함 영구 차단. 백엔드 2,678 PASS / 사이클 161 격리 7 PASS × 3회 flakiness 0 / 사이클 147 영역 회귀 0. 매매 안전성 무영향 — risk/realtime/auth/`_handle_sell_fill` hot path 변경 0. 사이클 162+ 인계: 의제 A `_boot()` PENDING 자동 sync hook + domain-expert 자문. |
| 2026-06-17 | 사이클 160 hotfix — `_wait_until` 본질 복원 (HIGH 매매 안전성 직결 silent 결함, 사이클 152 의미 전환) | `src/engine/scheduler.py` (+25L `_wait_until(target, *, advance_if_passed=False)` 2 모드 분기) / `src/engine/task_loop_helper.py` (+5L Protocol 시그너처 + while 루프 `advance_if_passed=True` 명시) / `tests/unit/engine/test_cycle160_wait_until_break_restore.py` 신규 (+7 케이스 HIGH 5) / 의미 전환 3건 (사이클 152 PAST-1 `advance_if_passed=True` 갱신 / 사이클 106 mock 시그너처 `**kwargs` 흡수 / 사이클 134 라인 임계 3675→3700) / `_workspace/red/cycle160_wait_until_break_restore.md` 신규 | 사용자 보고 = 알테오젠(196170, VB, 367,000원) + 알지노믹스(476830, LTV, 110,000원) 15:20 강제청산 미발화. 근본 원인 100% 확정 = 사이클 152 hotfix (`36d2d2e`, 6/16 18:12 KST) 가 `_wait_until` 본질 (target 도달 즉시 break) 을 깨뜨림. `now_dt >= target_dt` 시 무조건 `target_dt += timedelta(days=1)` → target 정확 도달 시점부터 영원히 return 불가 → `run_daily()` 의 `_wait_until(15:20)` 영구 정체. 18시간 누적 silent 결함 (15:20 강제청산 / 15:30 phase 전환 / 19:50 매수 중단 / 20:00 자문 / 20:10 정산 모두 누락). 시정 = 2 모드 분기 (default 즉시 break = run_daily / advance_if_passed=True = task_loop_helper 폭주 차단). production +30L net / 회귀 가드 7 케이스 / engine 디렉토리 1,614 PASS 0 FAIL / 사이클 160 격리 7/7 PASS. 매매 안전성 8영역 + risk/order_engine/realtime/auth/scanner diff 0 + 4 task 폭주 차단 (사이클 152) + run_daily phase 전환 (사이클 160) 본질 복원 양립. 운영자 즉시 액션 = 알테오젠/알지노믹스 HTS 수동 매도 + EC2 자동 배포 대기. 6/18 _boot `_sync_orders_to_db` 자연 회복. |
| 2026-06-17 | 사이클 159 — Supabase HTTP/2 race stagger 임계 상향 (0/60/120/180 → 0/240/480/720초, MEDIUM) | `src/engine/scheduler.py` (+4L net 4 task wrapper `initial_delay_secs=240/480/720`) / `tests/unit/engine/test_cycle159_stagger_threshold.py` 신규 (+243L 9 케이스 HIGH 2) / `_workspace/red/cycle159_supabase_stagger_threshold_uplift.md` 신규 / `_workspace/refactor/cycle159_supabase_stagger_review.md` 신규 (refactor-expert 자문 5건) | 사이클 158 (0/60/120/180초 stagger) 운영 효과 = 24h master_skip 992 / basics_skip 1,958 / Server disconnected 303 / ConnectionTerminated 310 / deque mutated 16 — stagger 60s < 단일 task 처리 280s 50% 감소 후 잔존. refactor-expert 옵션 C 채택 = 4 task 간격 240초로 상향 (총 wall-clock 14분). 정기 schedule (16:00/16:10/16:30/20:00) wait_time 변경 0 — `immediate_first_run` 분기만 적용. `deque mutated during iteration` httpx 내부 race = 사이클 160 의제. production +4L net / 회귀 가드 9 케이스. 백엔드 2,978 PASS × 3회 flakiness 0 / 사이클 159 격리 9/9 PASS. 매매 안전성 8영역 + realtime/auth/api/risk/order_engine diff 0 + 사이클 17/19/31 R6/32 R4/38/54/57 V-1/106/134/152/157/158 영속. D+1 6/18 운영 실측 예상 = master_skip <300 (-70%) / basics_skip <600 (-70%) / ConnectionTerminated <100 (-68%). |
| 2026-06-17 | 사이클 158 — 3 결함 통합 시정 (momentum 로그 폭주 + stock_master 0건 race + Supabase 연결 race, MEDIUM) | `src/engine/strategies/momentum.py` (+26L `_next_day_clear_logged_today: DailyEmitCap[str]` + `check_exit_signal` 익일 청산 분기 cap + `reset_next_day_clear_logged_today()` 헬퍼) / `src/engine/strategies/volatility_breakout.py` (+19L VB `prepare()` 0건 시 자동 재시도 cap 3회 + sleep 30초) / `src/engine/task_loop_helper.py` (+10L `initial_delay_secs: int = 0` 인자 신규, 회귀 보존) / `src/engine/scheduler.py` (+16L 4 task wrapper stagger 0/60/120/180초 + `_reset_daily_state` momentum cap reset 호출) / `tests/unit/engine/strategies/test_cycle158_momentum_next_day_clear_cap.py` 신규 (6 케이스) / `tests/unit/engine/strategies/test_cycle158_prepare_auto_retry.py` 신규 (5 케이스) / `tests/unit/engine/test_cycle158_task_stagger.py` 신규 (5 케이스) / 의미 전환 2건 (사이클 143 monkeypatch + 사이클 134 라인 임계 3650→3675) | 운영 사례 = 2026-06-17 08:13~08:50 KST EC2 재시작 직후 (사이클 157 배포 08:08 KST). Q1 = 씨에스윈드(112610) momentum 익일 청산 KIS APBK0918 거부 후 logger.info 매초 폭주 (26회/30초). 시정 = 사이클 31 R6 / 57 V-1 DailyEmitCap 패턴 답습 = 1회/ticker/일 cap. Q2 = `_boot()` 후속 prepare() 가 `_full_universe_load_once` 완료 전 호출 → 5 전략 stock_master 0건 (VCP 제외 사이클 157 list_by_filter). 시정 = VB prepare 0건 시 자동 재시도 hook. Q3 = EC2 재시작 직후 4 task (full_universe / basics / daily / master) 동시 발화 → Supabase HTTP/2 ConnectionTerminated 폭주. 시정 = stagger 60초 간격 (사이클 134 `run_periodic_task_loop` 영역 `initial_delay_secs` 인자 추가). production +71L net / 회귀 가드 16 케이스 / 백엔드 2,655 PASS × 3회 flakiness 0 / 사이클 158 격리 16/16 PASS. 매매 안전성 8영역 + risk/order_engine/realtime/auth/api diff 0 + 사이클 19/31 R6/32 R4/38/54/57 V-1/106/134/152/157 영속. |
| 2026-06-17 | 사이클 157 — VCP hardcoded list 폐기 + 6 전략 master_block hook 통합 + FUNNEL_STAGES +1단계 (HIGH 사이클 153 인계 종결) | `src/engine/scanner.py` (+89L `apply_master_block_filter(tickers, protected_tickers)` 공통 헬퍼 + momentum scan_stocks 내부 hook) / `src/engine/strategies/vcp_breakout.py` (+93L net, Q1 hardcoded list 폐기 + `list_by_filter(is_kospi200=True, is_kosdaq150=True)` 전환 + FUNNEL_STAGES 9단계 + hook) / `src/engine/strategies/volatility_breakout.py` (+45L net, VB 6단계 + hook) / `src/engine/strategies/long_tail_volatility.py` (+49L net, LTV 7단계 + hook) / `src/engine/strategies/donchian_swing.py` (+53L net, 9단계 + hook) / `src/engine/strategies/bull_flag_breakout.py` (+47L net, 9단계 + hook) / `tests/unit/engine/strategies/test_cycle157_master_block_hook_and_vcp_listfilter.py` 신규 (22 케이스 HIGH 6) / 의미 전환 5건 (사이클 47/50/148/153) | Q1=VCP hardcoded list 폐기 (KOSPI_200_TICKERS + KOSDAQ_150_TICKERS 폐기 + fetch_stock_detail 124회/일 → 0). Q2=6 전략 (VB/LTV/donchian/BFB/VCP/momentum) `_is_master_blocked_for_entry` hook 통합 (13건 차단 = master_raw 7 + raw 6, 보유 종목 절대 보호 사이클 32 R4). Q3=FUNNEL_STAGES +1단계 (각 전략 step "1단계 진입 차단 13건 통과" 추가, momentum 미적재 사이클 132 영속). production +295L net / 백엔드 2,639 PASS × 3회 flakiness 0 / 사이클 157 격리 22/22 PASS / 매매 안전성 8영역 + risk/order/realtime/auth diff 0. 사이클 156 Q0 (개장 전 배포) + 사이클 157 Q1+Q2+Q3 (10시 이후 배포) 분리 적용. |
| 2026-06-17 | 사이클 156 Q0 — VB/LTV/BFB `nxt_tradable=True` 강제 필터 제거 (HIGH 사이클 108 silent 결함 종결, 후보 풀 5.2배 확장) | `src/engine/strategies/volatility_breakout.py` (-1L `nxt_tradable=True` 제거 + step_conditions 라벨 갱신) / `src/engine/strategies/long_tail_volatility.py` (-1L + 라벨) / `src/engine/strategies/bull_flag_breakout.py` (-1L) / `tests/unit/engine/strategies/test_cycle156_nxt_filter_removal.py` 신규 (+92L 6 케이스 HIGH 3) | 사용자 verbatim "nxt_tradable은 주문처리할 때 항상 넥스트 실행 후에 거래소로 나가는걸 해당 값을 보고 바로 넥스트나 거래소로 주문제출하도록 하기 위해 추가관리하라고 했던거야. 모든 거래 종목이 넥스트트레이드에서 거래가능한건 내 의도와 다른데...?". 운영 DB 실측 = total 3,573 / nxt_true 579 (16.2%) / nxt_false 2,994 (83.8%) → 5.2배 확장. 근본 원인 = 사이클 108 (2026-06-11) Plan Phase A 도입 시점 `nxt_tradable=True` 강제 필터 silent (사용자 의도 = 주문 시점 NXT/KRX 분기용, 사이클 54 _strategy_exchange_async). 주문 시점 분기 메커니즘 변경 0 (사이클 54/B-1/55 R-1 영속). donchian = 사이클 121 영역에서 이미 nxt_tradable=None 영속 (변경 0). 회귀 가드 6 케이스 / strategies 329 PASS 회귀 0. 매매 안전성 8영역 + order_engine [nxt_downgrade] 영속 + sell_rejection NXT 거부 좀비 차단 영속. 사이클 156 Q1~Q3 (VCP hardcoded list 폐기 + A 옵션 hook + funnel step 추가) = 10시 이후 배포 영역 별도 사이클. |
| 2026-06-17 | 사이클 155 — KIS API 미관리 데이터 통합 (FHKST01010100 merge 5→35 키 + 진입 차단 7→13건, HIGH+MEDIUM 40 키) | `src/api/condition.py` (+94L `_FHKST_MERGE_KEYS` 35 키 상수 + `_ZERO_VALUE_SKIP_KEYS` 14 키 + merge 확장) / `src/engine/scanner.py` (+86L `_is_master_blocked_for_entry(master_raw, raw=None)` 시그너처 확장 + FHKST 6 키 차단) / `tests/unit/api/test_cycle155_merge_35_keys.py` 신규 (7) / `tests/unit/engine/scanner/test_cycle155_block_13_keys.py` 신규 (4) / `tests/unit/engine/scanner/test_cycle155_safety.py` 신규 (4) / `tests/unit/ast/test_cycle155_ast_merge_keys.py` 신규 (3) / `_workspace/red/cycle155_kis_api_data_integration.md` 신규 (작업 지시서) | 사용자 verbatim "KIS API에서 TR호출로 받아오기는 하지만 종목마스터에 넣어서 관리하지 않는 데이터가 많은 것 같아. 향후 쓸만한 데이터는 미리 종목마스터에 관리해뒀으면 해". Phase 1 진단 = FHKST01010100 88 응답 키 중 5개만 적재 (83 키 미관리). 사용자 결정 Q1=B (HIGH+MEDIUM 일괄) + Q2=A (raw JSONB merge, 사이클 107 답습) + Q3=A (FHKST 단일 source) + Q4=A (16:10 task 동행) + Q5=A (TDD) + Q6=A (단일 사이클). 신규 30 키 (per/pbr/hts_frgn_ehrt/frgn_ntby_qty/stck_mxpr/stck_llam/vol_tnrt/prdy_vrss_vol_rate/w52_*/d250_*/진입차단 6/new_hgpr_lwpr_cls_code/eps/bps/whol_loan_rmnd_rate/ssts_yn/last_ssts_cntg_qty/vi_cls_code/ovtm_vi_cls_code/bstp_kor_isnm). 사이클 145 0 값 skip 14 키 확장. `_is_master_blocked_for_entry` 1단계 차단 7→13건 확장 (master_raw OR FHKST raw). production +180L net / 회귀 가드 18 케이스 (HIGH 7) / 백엔드 2,931 PASS × 3회 flakiness 0 / 사이클 155 격리 18/18 PASS / 매매 안전성 8영역 + 사이클 32 R4/38/81/88/107/108/129/144/145/149/153/154 변경 0 (risk/order_engine/realtime/auth diff 0). D+1 16:10 KST 자동 발화 → 신규 30 키 ~3,000 종목 적재 예상. 사이클 156+ 인계: 매수 진입 hook 영역 `_is_master_blocked_for_entry` 호출 사이트 + list_by_filter per_max/pbr_max 인자 + 전략별 활용. |
| 2026-06-16 | 사이클 154 hotfix — KOSPI200 정본 코드값 영역 시정 (사이클 153 1,796 과대 영역 영구 차단, HIGH) | `src/engine/scanner.py` (-1L/+5L net +4L `_stock_master_master_load_once` KOSPI 분기 영역 `code_val not in ("", "0")` 시정) / `tests/unit/engine/test_cycle154_kospi200_code_value_hotfix.py` 신규 (+95L 17 actual PASS HIGH 4) | 사이클 153 운영 DB 적용 결과 KOSPI200=1,796 (정상 200 대비 9배 과대) 발견. 근본 원인 = `bool("0".strip())=True` → `kospi200_apnt_cls_code="0"` 1,596 종목 잘못 편입. Supabase MCP 운영 DB 실측: "0"=1,597 미편입 / "1"~"9","A","B"=199 편입 ≈ KOSPI200 정합. 시정 = `code_val not in ("", "0")` 영역 ("0" 추가 제외) + 운영 DB UPDATE backfill 즉시 적용 (1,796 → 199 / 합집합 1,945 → 348 정상화). production -1L/+5L / 회귀 가드 17 케이스 (parametrize 15 + AST 2, HIGH 4). 매매 안전성 8영역 + 사이클 153 변경 0. 사용자 보고 6/16 "5개" → 사이클 153 "1,945" (과대) → 사이클 154 "348" 영역 정상화 확정. |
| 2026-06-16 | 사이클 153 — donchian KOSPI200/KOSDAQ150 영역 복원 + stock_master.is_kospi200/is_kosdaq150 컬럼 신규 (HIGH 사이클 121 silent 결함 종결) | `supabase/migrations/037_stock_master_kospi200_kosdaq150.sql` 신규 (+36L, 운영 DB 적용 완료) / `src/db/stock_master.py` (+84L `list_by_filter` 인자 `is_kospi200`/`is_kosdaq150` 추가 + `upsert_master_raw` 2 컬럼 동시 upsert) / `src/engine/scanner.py` (+30L `_stock_master_master_load_once` KOSPI 분기 `kospi200_apnt_cls_code` / KOSDAQ 분기 `ksq150_nmix_yn`) / `src/engine/strategies/donchian_swing.py` (+5L `_scan_universe` 영역 `list_by_filter(is_kospi200=True, is_kosdaq150=True)` 호출) / `tests/unit/db/test_cycle153_list_by_filter_index_flags.py` 신규 / `tests/unit/engine/test_cycle153_master_load_kospi200_kosdaq150.py` 신규 / `tests/unit/engine/strategies/test_cycle153_donchian_index_filter.py` 신규 (13 케이스 HIGH 7) | 사용자 보고 6/16 UI 영역 donchian step_no=1 "코스피200+코스닥150 합집합" survived=5 결함. 근본 원인 = 사이클 121 (2026-06-12) Plan Phase B 영역 `_scan_universe()` stock_master 전환 시 KOSPI200/KOSDAQ150 필터 영구 누락 silent + FUNNEL_STAGES[0] step_name 미동기화. 사용자 결정 Q1=A (KIS 공식 마스터 source, `kospi200_apnt_cls_code` + `ksq150_nmix_yn`) + Q2=A (인자 추가, 사이클 108 답습) + Q3=A (TDD 정공). 운영 DB 적용 결과 = KOSPI200=1,796 (과대, 사이클 154 hotfix 의제) / KOSDAQ150=149 / 합집합=1,945 (사용자 보고 5 → 1,945 복원). production +119L net + migration 36L. 백엔드 2,883 → 2,896 PASS × 3회 flakiness 0 / 사이클 153 격리 13/13 PASS / 매매 안전성 8영역 + 사이클 32 R4/38/81 G-AST1/108/129/143/146 변경 0 (vcp_breakout 영역 영향 0 검증 영속). 사이클 154 인계: KOSPI200 정본 코드값 재자문 (KIS MCP) + vcp_breakout hardcoded list 영역 폐기. |
| 2026-06-16 | 사이클 152 hotfix — `_wait_until` target 이후 시점 폭주 결함 영구 차단 (HIGH 운영 안정성 직결) | `src/engine/scheduler.py` (-5L/+22L net +17L `_wait_until` 영역 재설계) / `tests/unit/engine/test_cycle152_wait_until_runaway_hotfix.py` 신규 (+155L 3 케이스 HIGH 2) | 사용자 보고 6/16 18:00 KST "종목마스터 일간작업 중 에러가 엄청 나고 있어". 근본 원인 = `_wait_until(target)` 영역 = `now >= target` 시 즉시 break → EC2 배포가 target 이후 시점 시작 시 task_loop_helper while 루프가 once 호출 무한 반복. 18:14:00 ~ 18:23:31 KST 영역 사이클 122/126/129/150 task 모두 폭주 + Supabase HTTP/2 ConnectionTerminated / Server disconnected / Broken pipe 폭주. 시정 = `target_dt += timedelta(days=1)` 다음 날 동일 시각 대기 + `asyncio.sleep(min(wait_secs, 60))`. 사이클 101/106 시점부터 영속한 silent 결함 영구 차단. engine 1,527 PASS 회귀 0 / 사이클 152 격리 3/3 PASS. 운영자 EC2 재시작 후 18:36:34 KST 일제 발화 정상 (daily_purge deleted=0 protected=1 영속 + daily_load failed=0 + basics_refresh updated=3572/3573 + master_load updated=3557/4373) → 18:41 이후 ERROR/WARNING 0건. |
| 2026-06-16 | 사이클 151 — 사이클 148 VB 가격 필터 후처리 패턴을 4 전략 확대 (LTV/donchian/BFB/VCP, MEDIUM 매매 기회 손실 영구 차단) | `src/engine/strategies/long_tail_volatility.py` (+63L) / `src/engine/strategies/donchian_swing.py` (+63L) / `src/engine/strategies/bull_flag_breakout.py` (+63L) / `src/engine/strategies/vcp_breakout.py` (+64L) — 4 전략 각 `_apply_price_filter_in_prepare()` 신규 메서드 + `_scan_universe()` 호출 1줄 (사이클 148 VB 패턴 100% 답습) / `tests/unit/engine/strategies/test_cycle151_4_strategies_price_filter.py` 신규 (+599L 32 케이스) / `_workspace/red/cycle151_4_strategies_price_filter_expansion.md` 신규 | 사용자 verbatim "아까 변동성돌파에만 적용했던 작업 나머지 전략에도 확대하자". 사이클 148 영역 4 전략 (LTV/donchian/BFB/VCP) 확대 — 모멘텀 N/A (실시간 scan). PriceFilter 단일 source (`system_config.get_price_filter`) + 보유 종목 절대 보호 (사이클 32 R4) + raw.bfdy_clpr miss graceful (사이클 64) + scanner `_apply_price_filter` 영속 (이중 안전망). production +253L net / 회귀 가드 32 케이스 (4 전략 × 6 PRICE/SAFETY + 공통 AST 8). 백엔드 2,848 → 2,880 PASS × 3회 flakiness 0 / 사이클 151 격리 32/32 PASS / 매매 안전성 8영역 + 사이클 30/32 R4/38/64/65/143/148 변경 0 (check_exit_signal 호출 0 + risk/order import 0 + DEFAULT_PARAMS 가격 키 추가 0). 6 전략 prepare → scanner 정합 완료 = UI funnel 영역 차단 종목 노출 결함 영구 차단. 사이클 152+ 인계: D+1 운영 실측 + NEW-B/NEW-C (사이클 147) + 카드 #23/#26 HIGH. |
| 2026-06-16 | 사이클 150 — SUPABASE 용량초과 시정 3 영역 통합 (HIGH 운영 안정성 + 사이클 6 silent 결함 24일 종결) | `supabase/migrations/036_stock_master_history_seq.sql` 신규 (+93L, 운영 DB 적용 완료) / `src/db/stock_master_daily.py` (+77L `purge_old_rows()` + `DAILY_RETENTION_DAYS=150`) / `src/db/system_logs.py` (+23L net `_purge_by_cutoff` 2-step subquery 전환) / `src/engine/scheduler.py` (+88L `TIME_STOCK_MASTER_DAILY_PURGE=16:15` + task loop + task_attrs 4 위치) / `tests/unit/db/test_cycle150_supabase_capacity.py` 신규 (+415L 25 케이스 HIGH 7) / `tests/unit/db/test_system_logs_retention.py` + `tests/unit/engine/test_cycle134_task_loop_helper.py` + `tests/unit/engine/test_scheduler_stop_zombie_tasks.py` 사이클 66 K-2 의미 전환 3건 / `_workspace/red/cycle150_supabase_capacity_3_areas.md` 신규 | 사용자 verbatim "SUPABASE 용량초과. 종목마스터는 2일치만 관리하자. (최신본 + 직전본). 종목마스터히스토리도 용량절감 정책이 필요해." Phase 1 진단 = 632MB / 무료 500MB 초과. 결정적 신규 발견 = 사이클 6 retention `.limit()` AttributeError silent 24일 (2026-05-21~6-12, supabase-py DELETE chain `.limit()` 미지원). 영역 A = stock_master_history PK (ticker, seq=0/1) + trigger 재설계 (TTL_REFRESH 미발화 = `OLD.raw IS DISTINCT FROM NEW.raw`) → 92K→4,358 row (-96.9%, -195.7MB). 영역 B = stock_master_daily T-150일 retention cron (VCP T-120일 + 30일 안전 마진) 매일 16:15 KST + protected_tickers (사이클 32 R4 답습). 영역 C = `_purge_by_cutoff` 2-step subquery (`.in_("id", subquery)`) 영역 사이클 6 영구 실효. production +285L net / migration 036 Supabase MCP 운영 DB 즉시 적용 완료 (632MB → 437MB -30.9%, cron 1차 발화 후 예상 ~170MB = 무료 tier 34%). 백엔드 2,848 PASS × 3회 flakiness 0 / 사이클 150 격리 25/25 PASS / 매매 안전성 8영역 + 사이클 32 R4 + 38 + 48 + 81 G-AST1 + 84 변경 0. 사이클 151+ 인계: D+1 운영 실측 (cron 1차 발화 효과 측정) + NEW-B/NEW-C (사이클 147 인계) + 카드 #23/#26 HIGH. |
| 2026-06-16 | 사이클 149 — 종목별 H0UNMKO0 구독 확장 + VI/거래정지 stale 회피 (HIGH 매매 안전성 직결) | `src/api/market_operation.py` 신규 (+173L `MARKET_OP_TR_ID=H0UNMKO0` + 10 컬럼 `parse_market_op_payload` + `MarketOpEvent` dataclass + `inquire_vi_status_today` REST 보조 FHPST01390000) / `src/engine/market_operation_monitor.py` 신규 (+158L `_vi_active_tickers` + `_halt_active_tickers` + `_market_op_last_event` + `is_ticker_stale_excluded()` 핵심 hook) / `src/engine/scheduler.py` (+90L `_subscribe_market_operation_tickers()` + 5분 delta + `_reset_daily_state` 동행) / `src/engine/stale_watcher_core.py` (+52L VI/halt 회피 hook, 사이클 135 grace 패턴 답습) / `src/realtime/handler.py` (+28L parse + record_market_op_event) / `src/engine/boot_manager.py` (+13L REST 폴백 seed 부팅 1회) / `_workspace/domain_consult/cycle149_h0unmko0_per_ticker_subscription.md` 신규 (5 의제 자문) / `_workspace/red/cycle149_h0unmko0_per_ticker_stale_skip.md` 신규 / 회귀 가드 16 케이스 신규 (4 파일) | 사용자 보고 6/16 14:00 KST "장중에 장이 중단되거나 특정종목 VI 등이 발생할 경우 시세체결가가 수신되지 않으니까 구독이 종료된걸로 오해할 수 있을 것 같네". KIS MCP 정본 = H0UNMKO0 10 컬럼 (TRHT_YN/VI_CLS_CODE/OVTM_VI_CLS_CODE/ISCD_STAT_CLS_CODE 등) 단일 WebSocket으로 VI+거래정지+종목상태 모두 수신. 종목별 구독 (tr_key=종목코드, 메인 WS 단일, HIGH bypass + LOW cap=20). 사용자 결정 Q1=A (보유+익일청산+후보 합집합) + Q2=A (REST 부팅 1회) + Q3=A (domain-expert 자문 사전 + TDD). 자문 5건 채택 (VI_CLS_CODE truthy 매핑 + HIGH bypass + LOW cap=20 + MRKT_TRTM 제외). 047040 (대우건설) + 475150 (SK이터닉스) 사용자 보고 ping-pong 영역 영구 차단. production +331L 신규 + 수정 4 / 회귀 가드 16 케이스 (24 actual). 백엔드 2,823 PASS × 3회 flakiness 0. 사이클 88 G-REJECT-3 4→5(135)→**8(149)** dict 분리 영속 + 매매 안전성 8영역 영속 (risk/order/auth 변경 0, check_exit_signal 항상 발화). 사이클 134 scheduler.py 임계 ≤3,450→≤3,550 갱신. |
| 2026-06-16 | 사이클 148 — VB `prepare()` 가격 필터 후처리 추가 (사이클 64 scanner 정합, MEDIUM 매매 기회 손실 영구 차단) | `src/engine/strategies/volatility_breakout.py` (+66L net `_apply_price_filter_in_prepare()` 신규 메서드 + `_scan_universe()` 호출 1줄) / `tests/unit/engine/strategies/test_cycle148_vb_prepare_price_filter.py` 신규 (+260L 12 케이스 HIGH 4 + MEDIUM 3 + LOW 5) / `_workspace/red/cycle148_vb_prepare_price_filter.md` 신규 / `_workspace/domain_consult/cycle148_vb_price_filter_in_prepare.md` 신규 (5건 의제 자문) | 사용자 보고 6/16 11:58 KST = VB 후보 6 종목 (298040/000660/009150/402340/011070/012450) UI 현재가 "-" = scanner `_apply_price_filter` `bfdy_clpr > max=500,000` 차단 → 시세 구독 신청 자체 안 함. 근본 원인 = VB `prepare()` 시총+거래대금만 평가 (31종목 확정) vs scanner 가격 min/max 적용 (25종목) 불일치. 사용자 결정 Q1=B (VB 단독 우선, LTV/donchian/BFB/VCP 사이클 149+ 인계) + Q2=C (PriceFilter 단일 source = system_config `price_filter_min`/`price_filter_max` 키 재사용, scanner↔prepare 정합 보장 + PUT 즉시 무효화) + Q3=A (scanner `_apply_price_filter` 영속 이중 안전망) + Q4=B (MEDIUM, prepare = 매수 진입 *전* 한정 + scanner 이중 안전망). domain-expert 자문 사전 5건 의제 채택. PriceFilter 비활성 (min=0, max=0) → 전체 통과 / 보유 종목 → 무조건 통과 (사이클 32 R4 + 사이클 30 005935 영속) / raw.bfdy_clpr miss → graceful 통과 (사이클 64 답습). 백엔드 2,787 → 2,799 PASS × 3회 flakiness 0 / 사이클 148 격리 12/12 PASS / 매매 안전성 8영역 + 사이클 30/64/65/143 변경 0. Supabase MCP 운영 실측 검증 = 11:11:27~33 KST 12 종목 (사용자 보고 6 + 추가 6) 차단 발화 확정. 사이클 149+ 인계: 4 전략 동일 패턴 + Settings UI 안내 ("prepare 가격 필터 익일 반영") + NEW-B/NEW-C nxt_tradable + inquire-balance 5xx (자연 회복 영속) 선택. |
| 2026-06-16 | 사이클 147 — `_handle_sell_fill` strategy 폴백 chain + 보정 INSERT UniqueViolation graceful (HIGH 매매 손익 정합) | `src/db/trade_history.py` (+102L `_lookup_strategy_from_trade_history` + `_update_trade_status_by_order_no` 2 헬퍼 신규) / `src/engine/order_engine.py` (+66L net `_handle_sell_fill`/`_handle_buy_fill` strategy fallback chain + UniqueViolation → 강제 UPDATE) / `tests/unit/engine/test_cycle147_sell_fill_strategy_fallback.py` 신규 (414L 5 케이스 HIGH 3) / `tests/unit/db/test_cycle147_trade_history_strategy_lookup.py` 신규 (103L 3 케이스) / `tests/unit/ast/test_cycle147_ast_sell_fill_fallback.py` 신규 (109L 2 케이스 HIGH 1) / `_workspace/red/cycle147_sell_fill_strategy_fallback_race.md` 신규 (172L) | 6/16 005940 NH투자증권 매도 체결통보 후 trade_history PENDING 6h 잔존 + callback exception 영역 시정. `_order_strategy` 매핑 dict miss 시 momentum 하드코딩 폴백 결함 → trade_history `(ticker, order_no, trade_type)` lookup 으로 정합 strategy 복구 + miss 시 WARNING + momentum 최종 폴백. 사이클 30 부분 UNIQUE 인덱스 violation 시 강제 UPDATE (order_no 단일 키 정합). production +168L net / 회귀 가드 626L 10 케이스. 백엔드 2,787 PASS × 3회 flakiness 0 / 사이클 147 격리 10 PASS / 매매 안전성 8영역 + 사이클 88 G-REJECT-3 + 사이클 73 UNIQUE 변경 0. Phase 1 5건 재평가 결과 = false alarm 1 + 무영향 4 → 종결. 사이클 148 인계: NEW-B `master_load_skip` 161,766회 (HIGH) + NEW-C `nxt_tradable` viol 3,538회 (MEDIUM) + inquire-balance 5xx graceful (운영 hot path 영역). |
| 2026-06-16 | 사이클 146 — 긴급 3건 통합 시정 (HIGH 매매 안전성 직결) | `src/db/stock_master.py` (+23L `upsert_master_raw` `nxt_tradable=False` 명시) / `src/engine/scheduler.py` (+25L `start()` `KisApiError` graceful) / `tests/unit/db/test_cycle146_upsert_master_raw_nxt.py` 신규 (4 케이스) / `tests/unit/engine/test_cycle146_start_graceful_recovery.py` 신규 (4 케이스) | 결함 #1 KIS 잔고 5xx `start()` graceful → 매매 프로세스 보존 (8시간 손실 차단). 결함 #2 nxt_tradable NOT NULL viol 영구 차단 (DB DEFAULT 이중 안전망). 결함 #3 raw acml_tr_pbmn=0 = 사이클 144 16:10 task 자동 복구 + 운영자 수동 trigger 위임. production +48L net / 회귀 가드 8 케이스 / 사이클 66 K-2 의미 전환 1건. 백엔드 2,777 PASS × 3회 flakiness 0. |
| 2026-06-16 | 사이클 145 — 4건 결함 통합 시정 (결함 #1 deploy migration 자동 적용 + 결함 #2 raw 0 덮어쓰기 금지 HIGH + 결함 #3 funnel UPSERT + 결함 #4 UI 라벨) | `supabase/migrations/035_strategy_funnel_snapshots_upsert.sql` 신규 (44L, 운영 DB 적용 완료) / `src/db/strategy_funnel.py` (+25L/-6L UPSERT 전환) / `src/api/condition.py` (+20L/-1L 0 덮어쓰기 금지 graceful) / `frontend/src/components/ScanMonitor.tsx` (+5L/-1L UI 라벨 명확화) / `.github/workflows/deploy.yml` (+23L psql migration 자동 적용 + graceful) / `tests/unit/db/test_strategy_funnel_crud.py` (사이클 66 K-2 의미 전환 3건) / `tests/unit/db/test_cycle145_funnel_upsert.py` 신규 (120L 4 케이스) / `tests/unit/api/test_cycle145_raw_preserve.py` 신규 (135L 4 케이스) / `tests/unit/workflows/test_cycle145_deploy_migration.py` 신규 (75L 5 케이스) / `_workspace/red/cycle145_4_defects_integration.md` 신규 / `src/db/CLAUDE.md` + `src/api/CLAUDE.md` 명세 갱신 | 결함 #1 (사이클 129 `df8cf82` D+3 silent — migration 034 운영 DB 미적용) 영구 차단 = deploy.yml psql + `supabase/migrations/*.sql` 자동 apply 영역 + SUPABASE_DB_URL secret 영역 + graceful skip. 결함 #2 (HIGH 매매 안전성 직결 — 6/16 07:54~07:59 KST 장 시작 *전* FHKST01010100 acml_tr_pbmn=0 덮어쓰기 → stock_master 2,697 ticker 중 거래대금>0 3 종목만 silent) = merge 영역 `int(value) == 0` 가드 + `continue` skip + 사이클 81 G-AST1 영구 강화. 결함 #3 (운영 DB 6/15 BFB step_no=1 = 8 row 영역) = migration 035 운영 DB 적용 완료 (Supabase MCP) + `insert_snapshot()` UPSERT 전환 + snapshot_at 키 폐기. 결함 #4 (UI 운영자 오인) = `ScanMonitor.tsx` 신고가 후보 라벨 → `'신고가 후보 (donchian)'`. production +71L net + 회귀 가드 13 케이스. 백엔드 2,769 PASS × 3회 flakiness 0 / 프론트 312 PASS 영속 / 사이클 145 격리 13 PASS / 사이클 66 K-2 의미 전환 3건. 사이클 146+ 인계: 자동 발주 중단 + 운영자 의무 (GitHub secrets SUPABASE_DB_URL 등록). |
| 2026-06-16 | 사이클 144 영역 B — 카드 #27 graceful 가시화 강화 (LOW, 사이클 129/132 인계 종결) | `src/api/condition.py` (+53L `_graceful_failed_counter` 모듈 전역 dict + 3 헬퍼 + FHKST01010100 except 분기 호출) / `src/engine/scanner.py` (+21L reset + collect + summary `graceful_failed` 키 + emit `graceful_failed_fhkst=N` 필드) / `tests/unit/api/test_cycle144_graceful_visibility.py` 신규 (260L 10 케이스) / `_workspace/red/cycle144_graceful_visibility.md` 신규 / `src/api/CLAUDE.md` 명세 갱신 | 사이클 130 권고 카드 #27 (NEW LOW) 영역 종결. 사이클 107 도입 graceful 분기 silent fail 가시화. `_graceful_failed_counter["fhkst01010100_failed"]` + asyncio.Lock 동시성 보호 + 3 헬퍼 (_record/get/reset). scanner `_stock_master_basics_refresh_summary` emit `graceful_failed_fhkst=N` 필드 운영자 즉시 진단. KisApiError "OPSQ1002 SESSION FULL" 사이클 129 영역 가시화 직접 검증 (G-144-INT-3). 영역 A D+1 운영 실측: 6/16 09:30~ 자동 발화 (시간 의존, 자동 측정). production +74L net. 백엔드 2,756 PASS × 3회 flakiness 0 / 사이클 144 격리 10 PASS / 매매 안전성 무영향 (logging 영역 한정). 사이클 145+ 인계: **자동 발주 중단** (Q2=D 사용자 명시 대기). |
| 2026-06-16 | 사이클 143 — VB 5단계 + LTV 6단계 funnel hook 정상 활성화 (사이클 140 자문 영속, 사이클 132 인계 종결) | `src/engine/strategies/volatility_breakout.py` (+75L VB 5단계 FUNNEL_STAGES + hook) / `src/engine/strategies/long_tail_volatility.py` (+105L LTV 6단계 hook) / `frontend/src/pages/StrategyFunnel.tsx` (-3L 사이클 132 VB/LTV 안내 메시지 제거) / `tests/unit/engine/strategies/test_cycle143_vb_ltv_funnel_hook.py` 신규 (320L 15 케이스) / `frontend/src/pages/__tests__/StrategyFunnel.cycle132.test.tsx` (G-FE-132-C 의미 전환) / `_workspace/red/cycle143_vb_ltv_funnel_hook.md` 신규 / `src/engine/strategies/CLAUDE.md` 명세 갱신 | 사이클 140 자문 결과 영속 시정. VB 5단계 (유니버스→시총+거래대금→가격 정합성→일봉 fetch→K값) + LTV 6단계 (VB 1~4+consecutive_limit_pass+K값). `_record_funnel_pipeline_step()` 호출 + `_reset_funnel_steps()` + `excluded` 수치 사유 (사이클 47 FUNNEL_STAGES 위임 패턴 답습). production +177L net. 사이클 132 UI 안내 메시지 영구 제거 + 사이클 66 K-2 의미 전환 1건. 백엔드 2,746 PASS × 3회 flakiness 0 / 프론트 312 PASS 영속 / 사이클 143 격리 15 PASS / G-143-SAFETY-1~3 직접 검증. 사이클 144 인계: D+1 운영 실측 (자동) + 카드 #27 graceful 가시화 LOW. |
| 2026-06-15 | 사이클 142 — **LTV 결함 #1+#2 통합 시정 (HIGH 매매 안전성 직결)** | `src/engine/scheduler.py` (+21L net 결함 #1+#2 시정) / `tests/unit/engine/test_cycle142_ltv_defect_fix.py` 신규 (350L 15 케이스) / `tests/integration/test_force_clear_1520.py` (의미 전환 2건) / `tests/unit/engine/test_cycle134_task_loop_helper.py` (라인 임계 ≤3,400→≤3,425) / `_workspace/red/cycle142_ltv_funnel_and_defect_fix.md` 신규 / `src/engine/CLAUDE.md` 명세 갱신 | 사이클 141 자문 (refactor + domain 병렬) 영속 시정. 결함 #1 = `_force_clear_main_only` `keeps_post_nxt` continue 폐기 → LTV `check_force_clear()` 호출 영속 (상한가 모드 종목 제외 + 일반 종목 청산, 사이클 38 명세 정합). 결함 #2 = `_execute_next_day_clear` 트레일링 분기 LTV `_limit_up_reached.add(ticker)` + `pos.high_since_buy = today_open` (VB 변경 0). 후성 093370 운영 사례 (6/12 매수 17,150 → 6/15 일중 고점 23,700원 (15:55) → 마감 22,300원 -5.91%) 시뮬레이션 TRAILING_STOP 정합 발화 영속. 사이클 66 K-2 의미 전환 3건. 백엔드 2,731 PASS × 3회 flakiness 0 / 사이클 142 격리 15 PASS / G-142-SAFETY-1~4 직접 검증. 사이클 143 인계: VB 5단계 + LTV 6단계 funnel hook (사이클 140 자문 영속). |
| 2026-06-15 | 사이클 140 — VB/LTV funnel 단계 정의 domain-expert 자문 (옵션 B 단계 정의만 영속, TDD 보류) | `_workspace/domain_consult/cycle140_vb_ltv_funnel_step_definition.md` 신규 (자문 산출물) | 사이클 132 Q1=C 인계 VB/LTV funnel 단계 정의 자문. 권고 = VB 5단계 + LTV 6단계 funnel hook (추정 +210~270L LOW~MEDIUM 위험). 사용자 결정 = 옵션 B (단계 정의만 영속, TDD 보류). 사이클 142+ 인계. 코드 변경 0 / 매매 안전성 무영향. 사이클 141 인계: LTV 결함 통합 시정 (refactor + domain 자문 병렬). |
| 2026-06-15 | 사이클 139 — **카드 #25 완전 종결** AST DRY 4차 마이그레이션 (LOW, 22 파일 잔존 전수) | `tests/unit/ast/test_cycle139_ast_dry_card25_termination.py` 신규 (170L 4 케이스) / 22 파일 마이그레이션 (사이클 73×2/83×2/89/91/94/96/97/98/99/100×2/101×4/102/107/108/126/129) / `_workspace/red/cycle139_ast_dry_card25_termination.md` 신규 | **사이클 130 권고 카드 #25 (LOW, 4순위) 완전 종결**. 누적 49 파일 (사이클 136 7 + 137 10 + 138 10 + 139 22) + 잔존 1 파일만 (read 0 / parse 0). 헬퍼 추가 0건 / 자체 헬퍼 3 파일 폐기. 백엔드 2,716 PASS × 3회 flakiness 0 / AST 146 PASS / 매매 안전성 무영향. |
| 2026-06-15 | 사이클 138 — AST 가드 49 파일 DRY 3차 마이그레이션 (LOW, 10 파일) | `tests/unit/ast/test_cycle138_ast_dry_3rd_migration.py` 신규 (148L 4 케이스) / 10 파일 마이그레이션 (사이클 76/89/92/98/103×2/112/115×2 + external_llm_reject) / `_workspace/red/cycle138_ast_dry_3rd_migration.md` 신규 | 사이클 130 권고 카드 #25 (LOW) 3차 마이그레이션. 누적 27 파일 (사이클 136 7 + 137 10 + 138 10) + 잔존 ~12 파일 (사이클 139 카드 #25 완전 종결 예상). 헬퍼 추가 0건 (기존 8 함수 충분, `_ast_helpers.py` 241L 영속 ≤ 250L). 백엔드 2,712 PASS × 3회 flakiness 0 / 사이클 138 격리 4 PASS / AST 디렉토리 전수 142 PASS / 매매 안전성 무영향 (테스트 영역 한정, production 영향 0) |
| 2026-06-15 | 사이클 137 — AST 가드 49 파일 DRY 2차 마이그레이션 (LOW, 10 파일) | `tests/unit/ast/_ast_helpers.py` 185L → 240L (+55L 2 헬퍼 `find_constant_value` + `count_string_occurrences`) / `tests/unit/ast/test_cycle137_ast_dry_continuation.py` 신규 (149L 4 케이스) / 10 파일 마이그레이션 (사이클 72/74/79/81/84/90/93/122/124/128) | 사이클 130 권고 카드 #25 (LOW) 후속 마이그레이션. 누적 17 파일 (사이클 136 7 + 137 10) + 잔존 ~22 파일 (사이클 138/139 종결 예상). 의미 전환 1건 (사이클 136 D1 compact 200L → 250L K-2). 백엔드 2,708 PASS × 2회 flakiness 0 / AST 디렉토리 전수 138 PASS / 매매 안전성 무영향 (테스트 영역 한정) |
| 2026-06-15 | 사이클 136 — AST 가드 49 파일 DRY 1차 마이그레이션 (LOW, 7 파일 헬퍼 위임) | `tests/unit/ast/_ast_helpers.py` 신규 (185L, 5 헬퍼) / `tests/unit/ast/test_cycle136_ast_helpers.py` 신규 (279L, 8 케이스) / 7 파일 헬퍼 위임 (사이클 78/89/101/102/110/127/129) | 사이클 130 권고 카드 #25 (LOW, 4순위) 1차 마이그레이션. 점진 마이그레이션 (사이클 136 = 7 파일 / 사이클 137+ = 10 파일/사이클). 사이클 89 G-AST1 의미 전환 영속 헬퍼 영역 보존 + 사이클 101 await + Call 자연 흡수 (ast.walk recursive). 백엔드 2,704 PASS × 2회 flakiness 0 / 사이클 136 격리 8 PASS / AST 디렉토리 전수 134 PASS / 매매 안전성 무영향 (테스트 영역 한정, production 영향 0). 사이클 137 인계: AST DRY 후속 마이그레이션 10 파일/사이클 |
| 2026-06-15 | 사이클 135 — WebSocket 구독 ACK grace period 신규 (MEDIUM~HIGH, +93L net) | `src/engine/stale_diagnostics.py` (+12L `SUBSCRIBE_GRACE_SECS = 180` 상수) / `src/engine/stale_manager.py` (+3L facade re-export, `__all__` 11 상수) / `src/engine/stale_watcher_core.py` (+30L grace 가드 `_is_within_grace`) / `src/realtime/websocket.py` (+30L `_subscribed_at` dict + 5 사이트 record/pop) / `src/realtime/websocket_pool.py` (+18L 합산 property) | Phase 1 진단 = 구독 ACK 직후 60s 이내 첫 시세 미입수 시 즉시 stale 판정 → 강제 재등록 KIS LMS chain 위험. 사용자 결정 Q1=A 의제 채택 + Q2=180s + Q3=A domain-expert 자문 + Q4=A HIGH 일관 grace. 자문 산출물 `_workspace/domain_consult/cycle135_websocket_grace_period.md` 5건 채택 (P95 안전 마진 정합). G-GRACE-7 직접 검증 (첫 시세 입수 후 60s 변경 0 = 사이클 29 005935 보호) + G-SAFETY-1/2 (STALE_FRESHNESS_SECS=60 변경 0, risk/order_engine import 0). 사이클 88 G-REJECT-3 4 dict → **5 dict 분리** (`_subscribed_at` 신규). 백엔드 2,696 PASS × 2회 flakiness 0 / 사이클 135 격리 14 PASS / 매매 안전성 무영향 (stale_watcher_core 영역 한정). 사이클 136 인계: 카드 #25 AST 가드 49 파일 DRY (LOW, -1,500L) |
| 2026-06-15 | 사이클 134 — scheduler 4 task loop 공통 헬퍼 추출 (MEDIUM, -103L) | `src/engine/task_loop_helper.py` 신규 (111L `run_periodic_task_loop` 팩토리) / `src/engine/scheduler.py` 3,501L → 3,398L (-103L, -2.9%) 4 task loop facade 전환 | 사이클 130 권고 카드 #21 (MEDIUM, 3순위) Green + 사이클 133 master metrics 통합 후 자연 발주. 사이클 67 facade + 사이클 79 G-AST2 task_attrs 4 위치 + 사이클 106 race 차단 + 사이클 88 graceful 헬퍼 흡수. 백엔드 2,682 PASS × 2회 flakiness 0 / 사이클 134 격리 14 PASS / 사이클 66 K-2 의미 전환 4건 (cycle106/122 가드 = 헬퍼 위임 + task_loop_helper.logger + summary prefix 흡수) / 매매 안전성 무영향 (lifecycle hook 영역 한정). 사이클 135 인계: WebSocket 구독 ACK grace period (Q3=A 180s, P95 안전 마진 정합) |
| 2026-06-15 | 사이클 133 — metrics 3 모듈 공통 헬퍼 + master metrics 일관성 통합 (LOW) | `src/engine/metrics_collector.py` 신규 (100L 공통 헬퍼 `make_metrics_collector` 팩토리) / `src/engine/stock_master_master_metrics.py` 신규 (39L master 일관성 facade) / 3 기존 facade 298L → 132L (-166L, -56% — 사이클 67 stale_manager.py 패턴 답습) / `src/engine/scheduler.py` (+4L master metrics 통합) | 사이클 130 권고 카드 #24 (LOW, 2순위) Green + 사이클 129 master_load_once metrics collector 부재 일관성 결함 해소. 백엔드 2,668 PASS × 3회 / 프론트 312 PASS / 사이클 133 격리 15 PASS / 사이클 66 K-2 의미 전환 1건 (사이클 89 G-AST1 `_has_function_def` 모듈-레벨 export 흡수) / 매매 안전성 무영향 (metrics 영역 한정, risk/order/realtime/auth 변경 0) |
| 2026-06-15 | 사이클 132 — strategy_funnel 결함 시정 (momentum funnel 영구 제외 + 휴장일 UI 안내) (MEDIUM) | `src/engine/strategies/momentum.py` (+13L docstring) / `src/routes/strategy_funnel.py` (+41L `_resolve_business_day` + 응답 확장) / `frontend/src/pages/StrategyFunnel.tsx` (+30L amber 배너 + 안내) / `frontend/src/api/strategy-funnel.ts` (+6L 타입) | 사용자 보고 "VB/LTV 2,3,4 단계 0 / donchian/BFB 전부 0 / VCP 베이스 이후 0". Supabase MCP READ-ONLY 진단 = VB/LTV/momentum hook 호출 0건 (사이클 39+41 명세 누락) + donchian/BFB/VCP 휴장일 영역만 0 (영업일 정상). 사용자 결정 Q1=C VB/LTV → 사이클 133 domain-expert 자문 + Q2=C momentum 영구 제외 + Q3=A 휴장일 UI + Q4=A TDD. 백엔드 2,653 PASS × 3회 / 프론트 312 PASS / 사이클 132 격리 13 PASS / 매매 안전성 무영향 (prepare 메모리 적재 + GET 응답 schema 한정) |
| 2026-06-15 | 사이클 131 — routes 4 POST fire-and-forget 헬퍼 추출 (LOW, -45L) | `src/routes/stock_master.py` 372L → 327L (헬퍼 `_make_background_runner` + `_TASK_REGISTRY` + `_dispatch_refresh` 추출) | 사이클 130 권고 카드 #22 1순위 Green. 4 POST 라우트 (universe/basics/daily/master) 동일 fire-and-forget 패턴 통합. 행위 보존 + 사이클 127 BackgroundTasks 영속 + 사이클 128 envelope 영속. 사이클 66 K-2 의미 전환 4건 (cycle110/120/127 가드 헬퍼 dispatch chain 흡수). 백엔드 2,645 PASS / 사이클 131 격리 13 PASS / 매매 안전성 무영향 (라우트 영역 한정, scanner/risk/order/realtime/auth 변경 0) |
| 2026-06-14 | 사이클 130 — refactor-review 권고 카드 6장 산출 (코드 변경 0 / 매매 안전성 무영향) | `_workspace/refactor/2026-06-14_review.md` 신규 (322L) | 사이클 67 (`stale_manager.py` 1,099L 분해 카드 #14 종결) 이후 62 사이클 누적 = CLAUDE.md "5 사이클 누적" 12배 초과 영구 차단 의무. 사용자 결정 Q1=A refactor-review 단독 + Q2=E refactor-expert 자율 범위 + Q3=A HIGH 카드만 domain-expert 평가 + Q4=A 권고만 + 사이클 131+ Green 별도. 권고 카드 6장: #21 scheduler 4 task loop 헬퍼 (MEDIUM, -193L) / #22 routes 4 POST fire-and-forget 헬퍼 (LOW, -115L) / **#23 scanner.py 분해 (HIGH, -2,581L, 사이클 67 패턴 답습)** / #24 stock_master metrics 3 모듈 공통 헬퍼 (LOW, -218L) / #25 AST 가드 49 파일 DRY (LOW, -1,500L 추정) / **#26 scheduler.py 추가 분해 (HIGH, -3,297L, 매매 hot path 인접)**. 합계 -7,904L 추정 (사이클 67 -1,003L 대비 7.9배). 사이클 131 = #22 단독 Green (refactor-expert 1순위 우선) |
| 2026-06-14 | 사이클 129 — KIS 공식 일일 마스터 파일 (kospi_code.mst / kosdaq_code.mst) 도입 + master_raw 별도 컬럼 + 16:30 KST 자동 task (HIGH) | `src/api/kis_master.py` 신규 (294L) / `supabase/migrations/034_stock_master_master_raw.sql` 신규 / `src/engine/scanner.py` (+289L 시총 환산 × 100 + 1단계 차단 7건 + master_load_once) / `src/engine/scheduler.py` (+86L 16:30 task) / `src/routes/stock_master.py` POST `/master/refresh` / `frontend/src/pages/StockMaster.tsx` 4번째 버튼 | 사용자 verbatim "종목마스터 만들 때 아래 소스코드 참고해줘" + KIS 공식 샘플 코드 제공. 사용자 결정 Q4=A 마스터 우선 + Q5=C 전수 보존 + Q6=C master_raw 별도 컬럼 + Q12=A × 100 단위 환산 (team-leader 자체 자문 영역 "× 10,000" 결함 자체 발견 + 사용자 verbatim "× 100" 정합 검증 후 시정). KOSPI 70 / KOSDAQ 64 field_specs 분기 + cp949 + ZIP + httpx SSL 옵션 C (verify=True 우선 + 폴백). 백엔드 2,632 PASS / 프론트 307 PASS / e2e 30 PASS × 3회 flakiness 0 / 사이클 129 격리 36 PASS / 매매 안전성 무영향 (scanner 매수 진입 전 영역 한정, risk/order/realtime/auth 변경 0) |
| 2026-06-13 | 사이클 128 — 종목목록 4 필터 신규 + 전체현황 4 카운트 silent cap 영구 시정 (HIGH) | `src/db/stock_master.py::get_stats()` 4 카운트 count="exact" + filter 분리 / `src/db/stock_master.py::list_paged_by_filter()` 신규 / `src/routes/stock_master.py` GET /list 4 query param + envelope 응답 / `frontend/src/pages/StockMaster.tsx` 상단 인라인 4 컨트롤 | 사용자 보고 "전체종목수를 제외하고는 페이징처리 중에 엉망" = `.range(0, 9999)` PostgREST 1000행 silent cap 영구 확정 (nxt 실측 400 → UI ~150 잠재 silent 결함). 4 카운트 모두 count="exact" + filter (사이클 126 패턴 답습) + JSONB jsonb operator path (`raw->'hts_avls'` numeric 비교, Supabase MCP 검증). 종목목록 = 시장 select + 시총/거래대금 억원 input + 종목명 검색 + 400ms 디바운스 + URL query 동기화. 백엔드 2,287 PASS / 프론트 302 PASS / 사이클 128 격리 26 PASS / xfail 의미 전환 3건 (사이클 124/126 패턴) / 매매 안전성 무영향 |
| 2026-06-13 | 사이클 127 — 3 작업 fire-and-forget BackgroundTasks + 5초 폴링 진행 가시화 (HIGH) | `src/engine/refresh_progress.py` 신규 / `src/routes/stock_master.py` (BackgroundTasks 전환 + GET /refresh-progress) / `frontend/src/components/RefreshProgressBanner.tsx` 신규 | 사이클 126 사용자 보고 "13분 후 KIS API 일시 결함 토스트" = axios timeout silent 결함 시정. 라우트 동기 응답 → BackgroundTasks (response 후 schedule) + 상단 배너 동적 5초/60초 폴링 + state 기반 409 가드. 백엔드 2,271 PASS / 프론트 295 PASS / 사이클 127 격리 33 PASS / xfail 의미 전환 9건 (사이클 66 K-2 패턴) / 매매 안전성 무영향 |
| 2026-06-13 | 사이클 126 — 종목마스터 UI/데이터 결함 4건 통합 시정 (1000 cap + 목록 4 컬럼 부족 + NXT/정지/관리 미입수 + 일봉 미적재) (MEDIUM) | `src/db/stock_master.py::get_stats()` count="exact" / `src/engine/scanner.py::_stock_master_basics_refresh_once` 신규 / `src/routes/stock_master.py` POST 2 신규 | get_stats PostgREST 1000행 한도 시정 + 리스트 4 컬럼 (시총/거래대금/현재가/전일대비) + KRX 폴백 하드코딩 False 시정 (KIS CTPF1002R 매스 보강 task 매일 16:10 KST) + 일봉 task 진단 강화. 백엔드 2,250 PASS / 프론트 289 PASS / 사이클 126 격리 16 PASS / 매매 안전성 무영향 (scanner 단계 한정) |
| 2026-06-12 | 사이클 124 — 종목마스터 UI 확장 + 신규 데이터 UI 동기화 영구 가드 (MEDIUM) | `src/db/stock_master.py` (+25L) / `src/db/stock_master_daily.py` (+15L) | 사용자 보고 ("정보가 너무 제한적" + "신규 수집 데이터 UI 노출 의무" + "지침 추가") 즉시 시정 (단순 시정 + LOW 위험 + UI + READ-ONLY 영역... |
| 2026-06-12 | 사이클 123 — donchian_swing `prepare()` `get_donchian_high` 헬퍼 전환 (LOW) | `src/engine/strategies/donchian_swing.py` (+14L L237 직전) / `tests/unit/engine/strategie... | 사용자 메시지 "다음 사이클" → 사이클 122 인계 HIGH 카드 1차 처리. team-leader Phase 1 진단 (Q1~Q4 4 의제 정리) + 사용자 결정 전수 채... |
| 2026-06-12 | 사이클 122 — KIS 일봉 도입 + `stock_master_daily` 정규화 | `supabase/migrations/033_stock_master_daily.sql` 신규 / `src/db/stock_master_daily.py` 신규... | 사용자 보고 "KIS로 일봉 가져오고 신규 테이블로 저장하자" 의도 달성. team-leader Phase 1 진단 결정적 발견 = `fetch_daily_candles` (... |
| 2026-06-12 | 사이클 121 — Plan Phase B donchian `stock_master` 전환 (MEDIUM) | `src/engine/strategies/donchian_swing.py` (+82/-39 _scan_universe) / `tests/unit/engine... | 사이클 100 인계 Plan Phase B (사이클 119 발주 시점, team-leader 자동 진행). MEDIUM 위험 = 멀티데이 보유 영역. Q2=D 임시 완화 =... |
| 2026-06-12 | 사이클 120 — TTL 우회 옵션 추가 (HIGH) | `src/engine/scanner.py` (force 인자 3 함수) / `src/routes/stock_master.py` (force=True 디폴트) | 사이클 119 효과 검증 시점 TTL 영역 결함 발견 (skipped_ttl=2,697 = 사이클 116/118/119 매핑 미적용). 사용자 즉시 결정 = force 인자... |
| 2026-06-12 | 사이클 119 — KRX → KIS 정합 키 전수 매핑 확장 (HIGH) | `src/engine/scanner.py` (+30L 4 매핑) / `tests/unit/engine/scanner/test_cycle119_krx_kis_... | 사용자 보고 정밀 진단 + KIS MCP 정본 검증 + 4 Explore agent 병렬. 사용자 가설 "TR 코드 못 찾음" 반박 (TR 코드 정합 영구 확정). 진짜 원인... |
| 2026-06-12 | 사이클 118 — KRX ACC_TRDVAL → KIS `acml_tr_pbmn` 거래대금 매핑 (HIGH) | `src/engine/scanner.py` (+ACC_TRDVAL 매핑) / 7 PASS | 사이클 117 운영 실측에서 발견된 거래대금 매핑 누락. 사이클 108 list_by_filter 영역 정합 의무. 매매 안전성 무영향. 사이클 119+ 후속: Plan Ph... |
| 2026-06-12 | 사이클 117 — hotfix — KRX basDd 전일 영업일 영역 + 빈 응답 시 KIS 폴백 + source 키 (HIGH) | `src/engine/scanner.py` (+~30L basDd 전일 + 7일 재시도) / `src/routes/stock_master.py` (+~7L... | 사용자 보고 즉시 시정 (사이클 115 도입 후 영업일 영역 결함 발견). 사용자 결정 C 통합 = basDd 전일 + 빈 응답 재시도 + KIS 폴백 trigger + so... |
| 2026-06-12 | 사이클 116 — KRX MKTCAP (원) → KIS `hts_avls` (백만원) 단위 환산 (HIGH) | `src/engine/scanner.py` (+11L MKTCAP 환산) / 6 PASS | 사이클 115 KRX 1차 분기 활성 시 단위 정합 의무 (1,000,000배 왜곡 차단). 사이클 81 G-AST1 의도 정확 해석 = KIS CTPF 호출 영역 키 보호... |
| 2026-06-12 | 사이클 115 — KRX OPEN API endpoint 실제 통합 | `src/api/krx.py` (+98L 4 endpoint) / `src/engine/scanner.py` (+~190L Q3=C 폴백) | Phase 1 진단 결정적 발견 = 사이클 112 인프라 추상 결함 3건 시정 (외부 정본 2건 독립 일치 검증). 사용자 키 활성화 후 401/400 silent 결함 차단... |
| 2026-06-12 | 사이클 114 — CI fail 무시 차단 패턴 영구 가드 | `.github/workflows/deploy.yml` (workflow_run + conclusion success 가드) / `.github/workfl... | 사이클 108~112 5 사이클 연속 CI red 무시 push 위험 패턴 = 단일 근본 원인 (deploy.yml on push 직접 trigger). workflow_ru... |
| 2026-06-12 | 사이클 113 — hotfix — CI fail 시정 | `tests/unit/ast/test_cycle93_ast_chain_required.py` xfail / `frontend/src/api/realtime-... | 사이클 108부터 누적 4건 일괄 시정. 사이클 110 시정 의미 전환 = 사이클 93 AST 의도 보존 + xfail 자동 가시화 (사이클 66 K-2 패턴). 사이클 10... |
| 2026-06-12 | 사이클 112 — openapi.krx.co.kr 정식 OPEN API 인프라 사전 구성 | `src/models/krx_open_api.py` 신규 / `src/db/system_config.py` (+120L) | 사용자 요구 (사이클 110 인계 KRX 정보데이터시스템 OPEN API 도입 영역). Phase 1 진단 결정적 발견 (인증 / URL / 호출 / Rate Limit /... |
| 2026-06-11 | 사이클 110 — routes/stock_master.py silent 결함 (LOW) | `src/routes/stock_master.py` (L52~L83, 28L 시정 — `_full_universe_load_once` 단일 호출 + 응답 7... | 사용자 보고 (운영자 "지금 새로고침" 100% 실패) 즉시 시정 (근본 원인 = 사이클 101 시정 동행 누락 silent 결함). team-leader Phase 1 진단... |
| 2026-04-22 | 초기 구성 (team-leader / backend-dev / frontend-dev / tdd-engineer / tester) | 전체 | TDD-First Trading Team 출범 |
| 2026-05-21 | `domain-expert` (데이/스윙 트레이더 자문) + `refactor-expert` (주기적 리팩토링) 합류, KIS MCP 접근 명시 (backend-dev / tdd-engineer / tester / | 에이전트 7명 / 스킬 9개 | 매매 의사결정 깊이 보강 + 코드 품질 드리프트 흡수 + KIS 스펙 정본 통일 |
| 2026-05-21 | `kis-mcp-query` 스킬에 KIS 공식 저장소 경로 + 설치 가이드 + 공식 프롬프트 도구 (`kis_easy_code`/`kis_detailed_code`) 활용 안내 추가 | skills/kis-mcp-query | 공식 저장소 (koreainvestment/open-trading-api) 가 정본임을 명시, 비공식 fork 사용 차단 |
| 2026-05-21 | 사이클 28 — ~37 (운영 진단 + 결함 | `src/engine/scheduler.py` 외 다수 + 신규 라우트 `/api/strategy-funnel` + migration 029/030 | 09:13 VB 미매수 사고 진단을 시작점으로 stale 추적 → 결함 가시화 → 시정 + funnel DB 추적 → UI 강화. 백엔드 1625 PASS / 프론트 160... |
| 2026-05-22 | 사이클 38 — ~43 (정책 명문화 + funnel 진단 + ping-pong 가시성 + 라벨 통일 6 사이클) — 상세는 `docs/HARNESS_CHANGELOG.md... | `src/engine/strategies/long_tail_volatility.py` / `strategy_base.py` | 백엔드 1671 PASS / 프론트 160 PASS / 매매 안전성 무영향. 사이클 33 BFB+VCP fix 효과 확인 (BFB 30→24, VCP 113→113), 사이클... |
| 2026-06-11 | 사이클 109 — `market-cap` 화이트리스트 누락 silent 결함 (HIGH) | `src/api/base.py` (+4L `_QUOTE_ALLOWED_PATHS` 1 path 추가) / `tests/unit/api/test_cycle10... | 사용자 보고 (stock_master 192 ticker 잔존 = 사이클 106 시정 후 미반영) 즉시 시정 (근본 원인 = 사이클 101 도입 시점 silent 결함). t... |
| 2026-06-11 | 사이클 108 — **Plan Phase A 완료 (LOW) | `src/api/condition.py` (+1L `hts_avls` 5번째 키 추가) / `src/db/stock_master.py` (+~60L `lis... | team-leader Phase 1 진단 (사이클 104 인계 Q5=B LOW 위험 자문 생략 영속 + 사이클 107 raw 보강 의존성 해소 완료 확인) + 사용... |
| 2026-06-11 | 사이클 107 — **stock_master raw 보강 (HIGH) | `src/api/condition.py` (+44L `inquire_stock_basics` CTPF1002R + FHKST01010100 merge 영역... | team-leader Phase 1 진단 결정적 발견 (CTPF1002R 3 키 부재 + FHKST01010100 3 키 존재 KIS MCP 정본 확정) + 사용자... |
| 2026-06-11 | 사이클 106 — **stock_master 갱신 영역 영구 (LOW) | `src/engine/scheduler.py` (+21/-14L 순증 +7L `_full_universe_load_task_loop` start() 직후 즉... | 사용자 보고 (191 ticker stock_master 결함 + UI 사이클 94 안내 배너 무효 영역) 즉시 시정 (4 영역 통합). 41 사이클 연속 옵션 A... |
| 2026-06-11 | 사이클 104 — **Playwright E2E spec 신규 | `e2e/realtime-health.spec.ts` 신규 (8 케이스, 380L) / `e2e/strategies.spec.ts` 신규 (9 케이스, 188L) | E2E spec 신규 (사이클 103 신규 페이지 검증) + silent 결함 2 영구 시정. 40 사이클 연속 옵션 A 패턴 영속. tdd-engineer Red + fro... |
| 2026-06-11 | 사이클 103 — **4 영역 통합 (LOW) | `frontend/src/types/realtime-health.ts` 신규 (33L) / `frontend/src/api/realtime-health.ts... | 사이클 102.5 회고 결정적 사실 (코미코 STOP_LOSS 운영 가시화 부족 = 단일 근본 원인 운영 영역 -2.4% apply) 즉시 시정 (4 영역 통합). 39 사이... |
| 2026-06-11 | 사이클 102 — **시세 구독 영역 전면 재검토 (HIGH) | `src/realtime/websocket.py` (+7L L162~L165 `_last_ws_message_at` 인스턴스 변수 + L607~L609 `_... | 사용자 신규 요구 (재구독 영역 전면 재검토) + refactor-expert + domain-expert 병렬 자문 일치 = 사이클 88 G-REJECT (재구독... |
| 2026-06-11 | 사이클 100 — **UI prefix 정정 (LOW) | `src/db/stock_master.py` (+13/-7L count_eager_refresh_today 3 prefix OR) / `tests/unit/... | 사이클 99 D+1 운영 측정 (2026-06-11 목 08:01 KST) 시점 사용자 첨부 UI = "오늘 자동 갱신 횟수 0" 결함 영역 즉시 시정 + 사용자 요구 4 영... |
| 2026-06-10 | 사이클 99 — **사이클 91 페이징 영역 영구 폐기 (LOW) | `src/engine/scanner.py` (-53L 순감, 페이징 영역 전수 폐기 + 단일 호출 + docstring 명문화) / `tests/unit/e... | 사용자 보고 (60 ticker + 3952ms 영속 페이징 시도) = KIS API 본질 한계 영구 확정 (volume_rank + fluctuation 모두 페이징 미지원... |
| 2026-06-10 | 사이클 98 — **KIS fluctuation API `FID_RANK_SORT_CLS_CODE` 1줄 silent 결함 (LOW) | `src/engine/scanner.py` (+4L 1줄 시정 + docstring chk 인용) / `tests/unit/engine/scanner/tes... | 사용자 보고 (0 ticker + 262ms 영역) = 사이클 97 즉시 silent 결함. team-leader Phase 1 진단 (Supabase MCP 운영 실증 OP... |
| 2026-06-10 | 사이클 97 — **KIS volume_rank API 영구 폐기 (LOW) | `src/engine/scanner.py` (+113/-106 = 순 +7L, KIS fluctuation 영역 신규 + KIS volume_rank 영역... | 사용자 보고 (60 ticker 영속 + 3438ms 소요 영역) = 사이클 91 페이징 영역 코드 정합 영속 + KIS API 영역 자체 한도 영구 확정. 단일 근본 원인... |
| 2026-06-10 | 사이클 96 — **KIS API "0000" 응답 단일 페이지 30 한도 (LOW) | `src/engine/scanner.py` (-5L 순감 = +55/-60 영역 복원 + 페이징 결합 + post-split 폐기) / `tests/unit... | 사용자 보고 (29 ticker 영속) 즉시 시정 (사이클 94 도입 KIS API "0000" 단일 페이지 한도 + 페이징 미지원 silent 결함 단일 근본 원인 확정).... |
| 2026-06-10 | 사이클 95 — **chicken-and-egg lock-in 영구 (LOW) | `src/engine/scanner.py` (+9L unknown 영역 + emit 확장) / `frontend/src/pages/StockMaster.ts... | 사용자 보고 (78 ticker 영속) 즉시 시정 (사이클 94 chicken-and-egg lock-in 단일 근본 원인 확정). 32 사이클 연속 옵션 A 패턴 영속. t... |
| 2026-06-10 | 사이클 94 — **FID_INPUT_ISCD 60 ticker silent 결함 영구 (LOW) | `src/engine/scanner.py` (L1368~1559 `_MARKET_INPUT_ISCD` 단일화 + `_classify_market` 헬퍼 +... | 사용자 보고 (60 ticker 영속 결함) 즉시 시정 (사이클 91 페이징 시정 후에도 영속 = 별개 단일 근본 원인 KIS 정본 `FID_INPUT_ISCD = "0000... |
| 2026-06-10 | 사이클 93 — **호출 chain broken silent 결함 영구 (LOW) | `src/engine/scheduler.py` (+16L L2591~L2660 `_universe_eager_refresh_loop` 2 분기 chain +... | 사용자 보고 stock_master 60 ticker 영속 결함 + 사이클 87 Phase 2 Q3/Q9 0건 silent 결함 = 단일 근본 원인 (가설 E 호출 chain... |
| 2026-06-10 | 사이클 92 — **07:50 KIS API 강제 중단 충돌 영구 (LOW) | `src/engine/scheduler.py` (+2L L51~L53 시간 상수 시정) / `src/realtime/websocket.py` (+85L im... | 사용자 운영 보고 (07:45 우리 기동 vs 07:50 KIS API 강제 접속 중단 충돌) 즉시 시정 (사용자 결정 Q28~Q33 + Q32 domain-expert P1... |
| 2026-06-09 | 사이클 91 — **KIS volume_rank 페이징 누적 누락 silent 결함 영구 (LOW) | `src/api/base.py` (+36L tr_cont 시그너처 + 응답 헤더 주입 + AsyncMock graceful) / `src/engine/sca... | 사이클 89 silent 결함 (KIS volume_rank 페이징 누적 누락) + 사용자 운영 실측 보고 즉시 시정 (사용자 결정 A 즉시 발주). 28 사이클 연속 옵션... |
| 2026-06-09 | 사이클 90 — **stock_master 500+ 수동 trigger API (POST /api/stock-master/refresh-universe) + UI "지금 새... (LOW) | `src/routes/stock_master.py` (+37L POST /refresh-universe + asyncio.Lock + 409 Conflict... | 사이클 89 후속 (장 종료 후 즉시 발화 영역 보강) — 사용자 보고 "장 종료 후라 시작이 안돼" 즉시 시정. 27 사이클 연속 옵션 A 패턴 영속. team-leader... |
| 2026-06-09 | 사이클 89 — **stock_master 500+ universe 확장 | `src/engine/stock_master_metrics.py` 신규 (84L) / `src/engine/scanner.py` (+신규 함수 5 + 상수 4) | 사용자 요구 (stock_master 500+ 확장 + 각 전략 필터링) + UI 결함 2 영역 (종목명 미표시 + 영문 필드명 + 사이클 언급 + 영문 용어) 통합 시정.... |
| 2026-06-09 | 사이클 88 — **외부 LLM 의견서 영구 기록 + 영구 차단 AST 가드 3** (코드 변경 0, **25 사이클 연속 옵션 A 패턴 (MEDIUM) | `_workspace/external_llm_reviews/2026-06-09_realtime_review.md` 신규 (디렉토리 + 통합 영구 기록 7 영... | 타 LLM 외부 의견서 (사용자 첨부, 시세 수신 오류 + 구조 단순화 관점) 처리 방침 결정 사이클 — Plan 영속 (A 점진 흡수). 25 사이클 연속 옵션 A 패턴 영... |
| 2026-06-09 | 사이클 85 — **stock_master UI 메뉴 구축 프론트 단독 단계 (LOW) | `frontend/src/types/stock-master.ts` 신규 (45L) / `frontend/src/api/stock-master.ts` 신규 (... | 사이클 83 후속 3 단계 분할 2/3 (프론트 단독) — stock_master UI 메뉴 구축 (사용자 요구 영속). team-leader Phase 1 진단 (5 의제... |
| 2026-06-09 | 사이클 84 — **stock_master UI 메뉴 구축 백엔드 단독 단계 | `supabase/migrations/032_stock_master_history.sql` 신규 / `src/db/stock_master.py` (+97L) | 사이클 83 후속 3 단계 분할 1/3 (백엔드 단독) — stock_master UI 메뉴 구축 (사용자 요구). team-leader Phase 1 진단 (5 의제 Q5~... |
| 2026-06-09 | 사이클 83 — **#82-A 옵션 1 (MEDIUM) `_scan_loop` 후보 풀 ticker `stock_master` eager refresh 영역 확장 (MEDIUM) | `src/engine/scanner.py` / `src/engine/scheduler.py` | 사이클 82 진단 인계 후속 #82-A (MEDIUM 신규) 영구 시정 — 사이클 81 시정 실효성이 stock_master 적재 영역에 결정적 의존 → eager refre... |
| 2026-06-08 | 사이클 81 — **사이클 64 silent 결함 (HIGH) | `src/engine/scanner.py` (-7L 순감, L166 `prdy_clpr` → `bfdy_clpr` 가격필터 키 시정 + L188~198 로그... | 사이클 64 시점 silent 결함 (Q2 자문 옵션 A KIS API 응답 키 명명 가정 오류) 영구 시정 — 단일 근본 원인 (`prdy_clpr` ≠ `bfdy_clpr... |
| 2026-06-08 | 사이클 80 — hotfix 4 단계 통합 — **사이클 79 e2e fail → 사이클 65 hotfix #2 가드 잘못된 FIFO 가정 영구 (MEDIUM) | `frontend/src/pages/Settings.tsx` (+2L, L43 + L692 useQuery retry:1 명시) / `frontend/src... | 사이클 79 1차 + 재실행 모두 fail (flaky 아닌 진짜 결함 확정) → 옵션 C 정밀 조사 (Playwright trace console error + resour... |
| 2026-06-08 | 사이클 79 — 카드 (LOW) | `src/engine/scheduler.py` (+2L, L742 finally + L866 stop() 양쪽 task_attrs 튜플에 `_api_reco... | 사이클 78 부차 발견 (`stop()` task cancel 목록 `_api_recovered_collector_task` 누락) 영구 시정. team-leader 진단 생... |
| 2026-06-08 | 사이클 78 — **사이클 74 도입 누락 silent 결함 (HIGH) | `src/engine/scheduler.py` (+26L / -2L, `_api_recovered_collector_loop` 본체 swing/stale f... | 사이클 78 C 진단 (Supabase READ-ONLY) 으로 카드 #19 거짓 알람 종결 + 사이클 74 도입 누락 silent 결함 (flush 호출 사이트 0건 = 메... |
| 2026-06-08 | 사이클 77 — hotfix 2단 통합 — **e2e flaky 영구 차단 | `e2e/fixtures/api-mocks.ts` (hotfix #1 +26L + hotfix #2 +7/-2 = 합 +31L / -2L, 4 라우트 추가... | 사이클 73 1차 + 사이클 74 1차 + 사이클 76 1차/재실행 = flaky → 진짜 결함 확정 chain. 사이클 77 hotfix 2단 진화 = ECONNREFUSE... |
| 2026-06-08 | 사이클 76 — 카드 #23 (MEDIUM) **api retry 5.80x dup 시정 (MEDIUM) | `src/api/base.py` (+150L, 신규 헬퍼 7 + state 3 + 모듈 상수 2 + `_request` 호출자 정정 `_warn_http_s... | 사이클 74 발견 신규 결함 시정 (api retry 5.80x dup) — 사이클 18 60s WARNING dedupe + 사이클 74 5분 recovered collec... |
| 2026-06-08 | 사이클 75 — 카드 #19' (HIGH) **e2e api-mocks flaky 영구 차단 (HIGH) | `e2e/fixtures/api-mocks.ts` (+53L, 7 endpoint group 추가 — cash-usage-ratio + 4 integrati... | 사이클 73 1차 + 사이클 74 1차 e2e fail (flaky 2회 누적 확인) — 진짜 결함 영역 확정 후 사이클 65 hotfix #2/#3 패턴 답습. team-l... |
| 2026-06-08 | 사이클 74 — **WebSocket 로그 sampling/aggregation/rate cap (옵션 E-1) 도입 (HIGH) | `src/realtime/websocket.py` +95L (collector + record/flush + metrics_loop + lifecycle +... | 사이클 71/73 운영 실증 잔존 dup 영역 (합법적 반복) 흡수 = 사이클 42 `[ws_heartbeat]` 5분 통계 패턴 답습. team-leader Phase 1... |
| 2026-06-08 | 사이클 73 — 카드 #19 (HIGH) **WebSocket dup 핵심 영역 (HIGH) | `src/realtime/websocket.py` (-19L 합, R-1 `_verify_subscriptions_after_reconnect` write_... | 사이클 72 hotfix F-2 (WebSocket dup 핵심 영역) 즉시 인계 카드 #19 (HIGH) 완전 종결 — R-1/R-2/S-1 3 사이트 시정 + AST G-... |
| 2026-06-08 | 사이클 72 — hotfix — system_logs **이중 INSERT 시정 (HIGH) | `src/main.py` (+30 lines, `_DbLogHandler` 500ms TTL dedupe 캐시 신규) / `src/realtime/webso... | 사이클 71 운영 hot path 실증 발견 silent 결함 (이중 INSERT 2.89x dup) 즉시 시정 — 사용자 결정 (매매 미발생 09:25 영역 안전). tea... |
| 2026-06-08 | 사이클 69 — 카드 #18 (LOW) **영구 폐기 (LOW) | `src/db/CLAUDE.md` (+19 lines, §TIMESTAMPTZ 사실 명문화 신규) / `docs/HARNESS_CHANGELOG.md` 사이... | 사이클 68 인계 카드 #18 (UTC 잔존 데이터 백필) = 사실 점검 결과 **불필요** 확정. team-leader Supabase MCP 13 테이블 전수 점검 후 r... |
| 2026-06-07 | 사이클 68 — 카드 #17 (LOW) 기타 INSERT 모듈 UTC → KST 일관성 (LOW) | `src/db/_kst.py` 신규 35L / `src/db/stock_master.py` (G-2 upsert + G-11 is_stale, +12 lines) | 사이클 65 hotfix H2/H2-bis 인계 카드 #17 (LOW) 완전 종결 — system_logs 외 기타 INSERT/UPSERT 모듈 timestamp KST 일... |
| 2026-06-06 | 사이클 67 — refactor #2 후속 카드 #14 (MEDIUM) `stale_manager.py` 1,099L → **4 sub-module + facade 96L 분해 (MEDIUM) | `src/engine/stale_manager.py` 1,099 → 96L (facade) / `src/engine/stale_diagnostics.py`... | refactor #2 후속 카드 #14 (MEDIUM) **완전 종결** — `stale_manager.py` 1,099L 모듈 비대화 해소 + 책임 분리 (진단/복구/유니버... |
| 2026-06-06 | 사이클 66 — `_resubscribe_stale_priority` cap=10 결함 (HIGH) | `src/engine/stale_manager.py` ~22L 교체 (L1023-L1057) + docstring Q6-4 의미 전환 명시 / `tests/... | 사이클 63 발견 결함 (K-2 PASS = 결함 confirm) 영속 시정 — 사이클 29 005935 사고 패턴 영구 차단. domain-expert 자문 옵션 A 전부... |
| 2026-06-06 | 사이클 65 — hotfix — e2e settings.spec.ts FAIL 시정 | `frontend/src/components/PriceFilterCard.tsx` (+1) / `frontend/src/components/TradeAmou... | 사이클 65 e2e 결함 = TradeAmountFilterCard 추가로 카드 수 임계 초과 + React Query 기본 retry 3회 누적 → 5초 timeout 초과... |
| 2026-06-06 | 사이클 65 — 거래대금 동행 필터 — scanner 단계 작전주 차단 보강 (HIGH) | `src/db/system_config.py` (TradeAmountFilter + 2 함수) / `src/engine/scanner.py` +234L (t... | Q7-5 (사이클 64 자문) HIGH 즉시 발주 확정 인계 — 사이클 62 갭상승 회피 효과 폐기 위험 보강. 사이클 64 패턴 직답습 (HIGH 3 = C 2 + G 1)... |
| 2026-06-06 | 사이클 64 — 가격 필터 scanner 이전 + 단순화 (HIGH) | `src/db/system_config.py` (mode 폐기, 2 키) / `src/engine/risk.py` -178L (가격 필터 전체 폐기) | 사용자 의도 재정의: 사이클 62 `risk.on_tick` (매수 신호 판단용) → scanner 단계 (WS 구독 *전* 종목 풀 차단) — 자원 절약 + 후보 풀 자체... |
| 2026-06-06 | 사이클 63 — Phase 2-A3 — refactor #2 stale_manager 추출 3단계 (HIGH) | `src/engine/stale_manager.py` +345L (731→1,076) / `src/engine/scheduler.py` -308L (3,31... | refactor #2 stale_manager 3 단계 분할 **완료** (A1+A2+A3 = 11 함수 + 10 상수 누적 ~937L 이주). 사이클 51 boot_mana... |
| 2026-06-05 | 사이클 62 — 가격 필터 — 매수 진용 가격대 차단 신규 기능 | `src/db/system_config.py` (PriceFilter Pydantic + 3 키 헬퍼) / `src/engine/risk.py` (on_ti... | domain-expert 자문 Q1~Q6 + Q7~Q11 전부 적용 (옵션 A) — Q2 fallback / Q4 3 모드 (HARD 단일 반박, 시장 신뢰 형성) / Q5... |
| 2026-06-05 | 사이클 61 — Phase 2-A2 — refactor #2 stale_manager 추출 2단계 (MEDIUM) | `src/engine/stale_manager.py` +373L (358→731) / `src/engine/scheduler.py` −290L (3,605→... | A1 완료 후 도메인 자문 옵션 B 3 단계 분할의 2 단계 — 사이클 60 패턴 직답습 (logger 명시 binding / re-export / 12 파일 분리). tes... |
| 2026-06-04 | 사이클 60 — Phase 2-A1 — refactor #2 stale_manager 추출 1단계 (LOW) | `src/engine/stale_manager.py` 신규 / `src/engine/scheduler.py` 5 상수 re-export + 5 wrapper | refactor-review 2026-06-04 카드 #2 (HIGH) 의 A1 단독 진행 — 사이클 51 boot_manager 답습 + 도메인 자문 (영구 hot path... |
| 2026-06-04 | 사이클 60 — hotfix — CI backend-test 6시간 hang 영구 차단 | `tests/unit/engine/test_b1_market_closed_zombie_block.py` / `tests/unit/engine/test_ord... | 사이클 58 hotfix 와 유사 패턴 (production code 변경 → mock 갱신 누락). 영구 timeout 가드로 동일 결함 재발 시 1분 내 FAIL 보장.... |
| 2026-05-31 | 사이클 49 — VCP Pullback "마지막 폭 0.0%" 결함 | `src/engine/strategies/vcp_breakout.py` (DEFAULT_PARAMS + `_check_pullback_sequence` 재설... | 백엔드 1748 PASS (1742→+6) / 다른 5 전략 + scan/매매 흐름 무영향 — 매수 진입 임계만 시정. 사이클 41 funnel 진단의 종결 (진단만 했고 시... |

### 테스트 실행

```bash
pip install -r requirements-dev.txt # 1회
python -m pytest -q # 백엔드 전체
cd frontend && npm install && npm test # 프론트엔드 전체
cd.. && npx playwright install && npx playwright test --config=e2e/playwright.config.ts # E2E

# 영향 테스트만 (PR 빠른 피드백)
python tools/test_impact/build_index.py
node tools/test_impact/build_index_frontend.mjs
pytest $(python tools/test_impact/affected.py origin/main --target=backend)
```

## 빌드 & 실행

```bash
# Docker (권장)
docker compose up --build # 개발: 프론트 :3000, 백엔드 :8002
docker compose -f docker-compose.prod.yml up --build -d # 프로덕션: Nginx :80

# 로컬
pip install -r requirements.txt && uvicorn src.main:app --host 0.0.0.0 --port 8001 --reload
cd frontend && npm install && npm run dev
```

## 환경 변수
`.env` 필수 (`.env.example` 참고).
- `KIS_ENV`: `vts`(모의) | `real`(실전)
- `KIS_APP_KEY_REAL/VTS`, `KIS_APP_SECRET_REAL/VTS`, `KIS_ACCOUNT_NO_REAL/VTS`
- `KIS_HTS_ID`: 실전 체결통보(H0STCNI0) 구독 키
- `SUPABASE_URL`, `SUPABASE_KEY`
- `AUTO_START`: 서버 기동 시 자동 매매 시작 (DB `system_config.auto_start` 우선, 매일 시작 전 재확인)
- `DKSTOCK_REGIME_ENABLED` (기본 false): dkstock.cloud 매크로 + 매수 가드 + cash_usage_ratio 자동 조정 활성화
- `KIS_MCP_ENABLED` (기본 false): 외부 백테스트 MCP 서버 활성화. 자문 직후 6 전략 × 2 kind = 12 job fire-and-forget

## 다중 전략 (요약)

6 전략: `momentum` / `volatility_breakout` / `long_tail_volatility` / `donchian_swing` / `bull_flag_breakout` / `vcp_breakout`.

상세 매수/청산/tradable_boards/exchange 는 **`src/engine/strategies/CLAUDE.md`** 참조.

### 새 전략 추가
1. `src/engine/strategies/` 에 `StrategyBase` 서브클래스 (`prepare/check_buy_signal/check_exit_signal/calc_buy_quantity`)
2. `src/engine/scheduler.py` `__init__` 에서 `registry.register()`
3. 필요 시 `scanner.py` 에 스캔 함수 추가
4. `strategies/CLAUDE.md` 표 + `_workspace/00_leader_trading_rules.md` 명세 추가

### 자금 관리
- 프론트 Settings → `PUT /api/strategies/weights` → `StrategyRegistry.allocate_funds()`
- `position_ratio` 는 **전략 할당 자금 기준** (순자산 × 전략비중 × position_ratio = 종목당 매수금액)
- 전략 간 동일 종목 중복 매수 방지: `registry.is_ticker_blocked_for_buy()` (보유/주문중/당일매도 통합 차단)
- **`cash_usage_ratio`**: `system_config.cash_usage_ratio` 키 — `_boot()` 가 `summary.net_asset × ratio` 로 `allocate_funds()` 호출. 범위 `[0.0, 1.0]`, 5% 단위, 기본 1.0. Settings 슬라이더로 조정 → **다음 영업일부터 반영**. `auto_regime_adjust=true` (기본) + `DKSTOCK_REGIME_ENABLED=true` 시 매크로 레짐 `cash_min` 기반 자동 갱신 (`clamp((100-cash_min)/100, 0.0, 1.0)`)

### 외부 통합 (백테스트 + 매크로 레짐)
- 20:00 AI 자문 INSERT 직후 외부 MCP 백테스트 (`http://43.202.187.5:3846/mcp`) — 6 전략 × 2 kind = 12 job fire-and-forget → `parameter_recommendations.backtest_summary` JSONB
- 매크로 레짐 (`dkstock.cloud`) — `regime/vix/fear_greed` 4 임계 OR 매수 가드 (4 모드: HARD/WARN/SOFT/OFF) + `cash_usage_ratio` 자동 조정
- 활성화 토글: `KIS_MCP_ENABLED` / `DKSTOCK_REGIME_ENABLED` (Settings UI 즉시 토글 가능). 외부 다운 시 graceful — 자문 INSERT 보존, summary=null, 매수 가드 비활성
- 운영 가이드: [`docs/backtest-monitoring.md`](docs/backtest-monitoring.md)

## 핵심 안전 규칙 (절대 깨지 말 것)

상세 메커니즘은 `src/engine/CLAUDE.md` · `src/realtime/CLAUDE.md` 참조. 여기서는 **금기**만:

- **체결통보 구독 (H0STCNI0/H0STCNI9) 제거 금지** — 미구독 시 포지션 등록·손절 불가
- **uvicorn 단일 워커 필수** — `--workers` 금지 (스케줄/포지션/WebSocket 중복)
- **주문번호 매핑** (`_order_qty/_order_strategy/_order_ticker/_pending_buy_orders`) 등록은 `place_order` 응답 직후 동기 영역, `await insert_trade` 진입 전 — 시장가 즉시체결 race 시 매핑 누락하면 기본값 "momentum" 으로 잘못 INSERT됨
- **체결통보 선행 race 가드** (`_completed_orders` set + UPDATE 0건 보정 INSERT) 제거 금지 — 시장가 즉시체결 + REST 응답 지연 시 trade_history 가 PENDING 영구 잔존
- **`_reset_daily_state()` 제거 금지** — 정산 후 미초기화 시 pending_buys/positions/sold_today 가 다음 날까지 잔류
- **익일 청산** 은 scheduler 에서 시가 수신 후 30s 안정화 처리 — `_pending_next_day_clear` 보류 후 09:00 KRX 시장가. `high_since_buy` 폴백 금지. on_tick 즉시 청산 금지
- **NXT 매도 거부 좀비 차단** — `is_market_closed_rejection` (APBK0918 + 장운영시간 외) 이면 `execute_sell` 이 positions(메모리/DB) 보존 + 재시도 중단. `is_insufficient_quantity` / `is_insufficient_cash` 와 분리. `SellRejectionTracker.is_blocked()` 진입 게이트는 **2단계 TTL** — KRX 메인(09:00~15:30) 거부 = 5분 TTL (일시 장애 가정), NXT 시간대(08:00~09:00 / 15:30~20:00) 거부 = 다음 KST 09:00 TTL. `market_order_disallowed` 거부 = 30초 TTL (동일 tick 폭주 차단). NXT 폴백 실패 시 `_pending_next_day_clear` 익일 청산 자동 전환. `_reset_daily_state` 동행 clear (`_sell_rejection.reset_daily()` 4 필드 일괄 위임, `OrderEngine.reset_daily_state()` 캡슐화 보존). 호환 layer property `_market_closed_blocked` / `_market_closed_blocked_logged_today` 는 tracker 내부 dict/set 직접 노출 (is 동일성 보장)
- **매수/매도 시장가 거부 → 지정가 5호가 폴백 1회** — `is_market_order_disallowed` (msg1 키워드 `시장가매매불가` / `시장가호가불가` / `최유리/최우선지정가 주문만` / `지정가 및 최유리` — APBK1943/APBK3013) 매칭 시 `step_up(buy)/step_down(sell)` 으로 `LIMIT` 재시도. 매핑 동기 + race 가드 동일 규약
- **KIS 거부 응답 영구 저장** — `_request` 가 `rt_cd != "0"` 시 `system_logs` prefix `[kis_rejection]` + path/tr_id/msg_cd/msg1 + body 주요 키 (민감 키 마스킹) fire-and-forget
- **WebSocket 시세 보유·익일청산 우선 보장** — `MAX_SUBSCRIPTIONS=41` KIS 공식 한도. HIGH (보유/익일청산) `bypass_limit=True` 절대 보장. 후순위 drop 시 `[priority_drop]` INFO + WARNING `system_logs`. HIGH 단독 41 초과 ERROR
- **WebSocket 다중 안전망** — F1 (재연결 1회) + `_scan_loop` (5분) + K stale watcher (120s, 1~5회 즉시 강제 재등록 + 6회 초과 시 5분 cooldown 기반 시간 기반 force_retry + 시간당 12회 cap) + `_resubscribe_stale_priority` (5분 우선) 4중. K stale watcher 양쪽 분기에 우선순위 분리 (positions/`_pending_next_day_clear` HIGH+bypass=True, 그 외 후보 LOW+bypass=False — 메인 편중 차단). `_subscriptions` ACK 정합성 가드 (orphan ACK race 차단)
- **세션 단위 silent inactive 자동 reconnect** — `_detect_silent_inactive_sessions` 3중 가드: `fresh_ratio < SILENT_INACTIVE_FRESH_RATIO_THRESHOLD(=0.2, 20%)` + `subscribed_count >= 5` + 5분 지속 → `_ws.close()` 강제 reconnect. 시간당 세션당 2회 cap (LMS/앱키 정지 위험 차단)
- **stale universe 가드** — `_evaluate_universe_guard`: stale>5 + `today_volume < UNIVERSE_LOW_VOLUME_THRESHOLD(=10_000)` 종목 자동 unsubscribe + `_universe_excluded_today` 등록 + `[universe_excluded]` INFO + `inquire_ccnl` 으로 마지막 체결시각 로그. 보유/익일청산 절대 보호 + `_reset_daily_state` 동행 clear (영구 블랙리스트 금지)
- **`trade_history` 중복 INSERT 차단** — `_sync_orders_to_db` 는 `get_today_buy_trades_for_sync()` / `get_today_sell_trades_for_sync()` 사용 (dedupe 없음 + CANCELLED 제외). DB 부분 UNIQUE 인덱스 `(ticker, order_no, trade_type)` 이중 안전망. 기존 `get_today_buy_trades()` 의 ticker dedupe 는 포지션 복구용 — 절대 sync 중복 판정에 사용 금지
- **NXT 거래가능 사전 판별** — `stock_master.nxt_tradable=False` 면 NXT/SOR → KRX 강제 다운그레이드 + `[nxt_downgrade]`. `_boot()` eager 사전 갱신 (보유 + `_pending_next_day_clear` 합집합). 거부 사후 보강 `stock_master.upsert_one(ticker, nxt_tradable=False)`
- **종목코드 형식 비대칭** — 진입은 6자리 숫자만 (`isdigit()`), 사후처리는 6자리 영숫자 (`isalnum()`) — ETF·신주인수권 자동매매 차단 + 좀비 포지션 방지
- **1주 폴백은 전략 잔여 자금 기준** — 6 전략 `calc_buy_quantity()` 가 `StrategyBase._fallback_one_share(current_price)` 공통 헬퍼. 잔여 = `total_investment - (positions buy_price×qty + pending_buy_amounts 합)`
- **VB 당일 15:20 일괄매도** — `DEFAULT_TRADABLE_BOARDS=("main",)`, POST_NXT 추가 금지. `_force_clear_main_only` 가 15:20 일괄 청산. 15:30 이후 호출은 시간 가드로 skip
- **`tradable_boards` 는 매수 진입 전용 (명문화)** — 매도/손절/Trailing/익일청산/15:20 강제청산/상한가 손절 모니터링은 어떤 전략에서도 PRE/MAIN/POST 무관 항상 작동. `risk.on_tick` 의 `check_exit_signal` 분기는 `session_tracker.is_tradable` 검사 *전* 진입. LTV `DEFAULT_TRADABLE_BOARDS=("pre_nxt", "main", "post_nxt")` (사용자 의도 — 연속 상한가 익일 청산 + 야간 매수)
- **donchian_swing `_swing_rest_poll_loop`** 제거 금지 — 09:30~15:20 60s REST 폴링으로 멀티데이 손절 평가 보강
- **모든 시각 데이터 KST 강제** — 백엔드 `_to_kst(iso)` 헬퍼 + `_today_kst_iso()` timezone 명시 (`+09:00`). 프론트 `Intl.DateTimeFormat(timeZone='Asia/Seoul')` 명시. `new Date(iso).getHours()` 브라우저 로컬타임 추출 금지
- 매매 파라미터 (`DEFAULT_PARAMS`) 변경 시 `_workspace/00_leader_trading_rules.md` 동기화

### 코딩 컨벤션
- Python: pydantic + async/await
- TS: 모든 API 응답은 `frontend/src/types/` 정의 사용
- API 응답 래퍼: `{ success: bool, data: T, message: str }` (`models/response.py` `ApiResponse`)
- KIS 호출은 반드시 `src/api/base.py::kis_request()` 또는 `kis_get_quote()` 경유 (Rate Limit·재시도·메트릭)
- TR_ID 는 `settings.get_tr_id()` 사용 — 하드코딩 금지

## DB 스키마 (Supabase)

마이그레이션: `supabase/migrations/`. CRUD 모듈 상세: `src/db/CLAUDE.md`.

| 테이블 | 용도 |
|--------|------|
| `trade_history` | 거래 내역 (status: PENDING/COMPLETED/PARTIAL/CANCELLED) |
| `daily_performance` | 일일 실적 (date+strategy 복합PK, TWR 누적, 실현손익 기준) |
| `positions` | 보유 포지션 영속화 (ticker PK) |
| `strategy_config` | 전략 설정 (strategy_id PK, params JSONB) |
| `system_config` | 시스템 설정 (auto_start, cash_usage_ratio, buy_block_mode + 4 임계값, dkstock_regime_enabled, kis_mcp_enabled 등) |
| `system_logs` | 시스템 로그 |
| `parameter_recommendations` | 20:00 AI자문 (target_date+strategy_id UNIQUE). `recommended_weight`/`code_review_notes`/`applied_weight`/`weight_reasoning`/`backtest_summary` JSONB |
| `daily_log_reports` | 20:10 일일 로그 분석 (target_date UNIQUE, metrics 에 api_metrics/strategy_funnel/by_ticker_pnl/by_hour_pnl/next_day_clear 포함). 토큰/지연/비용 5 컬럼 — input_tokens/output_tokens/total_tokens/latency_ms/cost_estimate_usd (migration 031, 모두 NULL 허용) |
| `stock_master` | KIS CTPF1002R 캐시 (ticker PK, 24h TTL) + **사이클 129 `master_raw` JSONB + `master_raw_updated_at TIMESTAMPTZ` 신규** (KIS 공식 일일 마스터 파일 영역 — 시총/거래정지/관리종목/지수편입/재무 ~30 키, raw 영역 분리 보호). NXT 거래가능 사전 판별. **사이클 168 생성 컬럼 `hts_avls_eok bigint`(억원) + `acml_tr_pbmn_won bigint`(원) GENERATED ALWAYS STORED** (migration 039 — raw.hts_avls/acml_tr_pbmn 가 jsonb *문자열* 로 저장되어 UI `list_paged_by_filter` jsonb numeric gte 가 0건 silent 결함 → 생성 컬럼 numeric 비교 + 인덱스로 시정. 비숫자는 `~ '^[0-9]+$'` 가드로 NULL. raw 읽기만 = 사이클 81 G-AST1 영속) |
| `stock_master_daily` | KIS FHKST03010100 일봉 정규화 (migration 033). PK `(ticker, bas_dd)` + OHLCV + change_rate + raw JSONB. 매일 16:00 KST 적재 (백필 시 T-100일, 이후 D-1 영업일 증분). **사이클 172 — VCP universe (KOSPI200∪KOSDAQ150) 는 220일 backfill** (분할 fetch 윈도우×3) + retention `DAILY_RETENTION_DAYS=230` (사이클 173 prepare DB일봉 전환 선행). donchian (20일 신고가) / VCP (베이스+Pullback 220일) / VB (ATR) 전략 활용 |
| `backtest_runs` | 외부 MCP 백테스트 영속화 (`(target_date, strategy_id, params_kind)` UNIQUE. 6 전략 × 2 kind = 12 row/사이클) |
| `market_regime_snapshots` | dkstock.cloud 매크로 일일 스냅샷. `_boot()` 시점 1행. `buy_blocked`/`computed_cash_usage_ratio`/`raw_response JSONB` 영구 기록 |
| `kis_quote_accounts` | 보조 KIS 시세 수신 계좌 (UUID PK, label UNIQUE, active=true 부분 인덱스). `list_accounts()` 60s TTL 메모리 캐시 |
| `strategy_funnel_snapshots` | 전략별 조건검색 단계별 후보/탈락 종목 영구 추적. `(target_date, strategy_id, step_no, snapshot_at)` UNIQUE. `survived_tickers` JSONB cap 200 / `excluded_sample` JSONB cap 20. 수동 trigger `POST /api/strategy-funnel/snapshot` (현재 최종 단계 `step_no=99` 만, 자동 hook 은 후속 사이클) |

> **`trade_history` 부분 UNIQUE 인덱스 (migration 029)**: `uq_trade_history_ticker_order_no_type ON (ticker, order_no, trade_type) WHERE order_no IS NOT NULL AND order_no != ''`. `_sync_orders_to_db` 핑퐁 INSERT 영구 차단 + NULL/빈 order_no (수동 매매 사전 등) 호환.

> **`stock_master` / `stock_master_daily` UI 동기화 의무**: stock_master 컬럼 / raw JSONB 키 / stock_master_daily 컬럼 추가 시 UI 동기화 의무 영속. 전략에서 종목마스터 데이터 참고 시점 영역부터 신규 수집 데이터는 UI 노출 의무. 절차 = (1) `frontend/src/types/stock-master.ts` interface 갱신 (2) `frontend/src/api/stock-master.ts` 호출 영역 갱신 (3) `frontend/src/pages/StockMaster.tsx::FIELD_LABELS` 한글 라벨 + `CATEGORY_KEYS` 배치 + `HIGHLIGHT_KEYS` 핵심 키 추가 (4) `GET /api/stock-master/stats` 응답에 진단 카운트 추가 + 카드 1개 추가 (5) `frontend/src/test/handlers.ts` MSW + `e2e/fixtures/api-mocks.ts` Playwright LIFO 정합 갱신 (6) 회귀 가드 추가. 상세는 `frontend/CLAUDE.md` 참조.

## Docker / 배포

- `Dockerfile` / `frontend/Dockerfile` 멀티스테이지 (dev: hot-reload / prod: non-root + Nginx)
- `docker-compose.yml` (개발 hot-reload) / `docker-compose.prod.yml` (prod)
- **토큰 캐시 영속화**: `./.token_cache:/app/.token_cache` 디렉토리 볼륨 양쪽 compose 동일 마운트. KIS `/oauth2/tokenP` 분당 1개 한도 + 컨테이너 재기동 시 토큰 24h 유효 보존. `.gitignore` 등록 (`.token_cache/` + 구 `.token_cache_quote_*.json` 호환). 구 경로 `.token_cache.json` 존재 시 자동 마이그레이션
- **`.token_cache` 빌드 시점 권한 보장**: `Dockerfile` prod 스테이지가 `mkdir -p /app/.token_cache` → `chown -R appuser:appuser /app` → `USER appuser` 순서. 호스트 bind mount 가 root:root 로 생성되어 `appuser` 가 쓰기 거부되던 결함 영구 차단 (회귀 가드: `tests/integration/test_dockerfile_token_cache_perms.py`)
- `frontend/nginx.conf`: 정적파일 + `/api` → backend:8000 프록시
- 타임존 `TZ=Asia/Seoul`, vite 프록시 타겟은 `VITE_API_URL` 분기
- **EC2 t4g.small (ARM, ap-northeast-2)** 서비스 경로 `~/auto_stock/`
- 자동 배포: `git push origin main` → GitHub Actions 가 EC2 SSH → `git pull` + 재빌드 (`.github/workflows/deploy.yml`). push 시각 → pool_start 지연 1~5분
- GitHub Secrets: `EC2_HOST`, `EC2_USERNAME`, `EC2_SSH_KEY`
- **로컬과 EC2 동시 실행 금지** — KIS 동일 계정 동시 접속 충돌
- 운영 가이드: KRX 메인 시간 (09:00~15:30) 중 빈번한 push 자제 — `_scan_loop` 5분 race 가능. NXT 애프터 (15:30~) 또는 익일 07:50 _boot 전 push 권장

## 디렉토리 역할
- `src/auth/` — KIS OAuth 인증/토큰 (메인 + 보조 multi)
- `src/api/` — KIS REST (주문·잔고·조건검색·일봉) + 시세 풀 (`base.py::_request_via_quote_pool` + path 화이트리스트 가드). `quotation.py::inquire_ccnl(ticker, market='J')` — FHKST01010100 주식현재가 시세, output[0] + today_volume 합산 + graceful None (stale universe 가드용)
- `src/realtime/` — KIS WebSocket (시세·체결통보·H0NXMKO0) + WebsocketPool 멀티 세션 분배
- `src/engine/` — 매매 핵심 (전략·레지스트리·주문·리스크·스케줄러). `recommendation_engine.py` 20:00 AI자문 / `log_analysis_engine.py` 20:10 일일 분석 / `backtest_engine.py` + `backtest_yaml.py` / `market_regime.py`
- `src/engine/strategies/` — 6 전략 명세 (전용 CLAUDE.md)
- `src/services/` — 외부 서비스 클라이언트 (`mcp_client.py` 백테스트 MCP / `dkstock_client.py` 매크로 / `quote_session_health.py` 보조 세션 health monitor)
- `src/db/` — Supabase CRUD
- `src/routes/` — FastAPI 엔드포인트
- `src/models/` — Pydantic 모델
- `frontend/` — React 대시보드
