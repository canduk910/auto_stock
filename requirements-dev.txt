# 개발/테스트 전용 의존성. 운영 컨테이너에는 포함하지 않는다.
-r requirements.txt

pytest>=7.4
pytest-asyncio>=0.23
pytest-mock>=3.12
pytest-timeout>=2.3
freezegun>=1.4
respx>=0.21
coverage[toml]>=7.4
pytest-cov>=5.0
pyyaml>=6.0
# Python 3.9 .venv 호환용 — pydantic v2 가 PEP 604(`X | None`) 어노테이션을 평가할 때 사용.
# 운영 컨테이너(3.11+)에는 영향 없으나 dev 의존성에는 필수.
eval_type_backport>=0.2
